# Prime-lattice research lines

This file holds the current mathematical questions suggested by the durable prime-lattice intuitions. It is not a roadmap, task queue, status page, or history.

## Extract transport information special to the actual low Weil eigenbranch

**Linked intuition:** `MI-016-weil-form-observability-is-stronger-than-moment-observability`.

PL-280--PL-284 give the aperture ledger, terminating fixed-aperture positivity certificate, and a conditional positive endpoint at `a=0.8` with a simple isolated ground state and tiny explicit margin.

PL-285--PL-289 separate ambient form regularity from actual eigenstate graph regularity. The eigen-equation gives a uniform squared-log Fourier moment and hence an `O(log^-2(1/|delta|))` translation modulus on source-static aperture intervals.

PL-290 now proves that this is the sharp generic modulus of the full graph domain for the actual active `2,3,4` von-Mangoldt comb. High-frequency graph-domain witnesses phase-lock the `2` and `3` channels while the `4` channel returns coherently, giving a matching inverse-squared-log lower bound for the complete source-static form. The graph domain is therefore not locally Lipschitz in aperture and does not justify a uniform Feynman--Hellmann derivative.

The live theorem must use information **strictly smaller than the graph ball**: a cancellation identity from the low eigen-equation, stronger regularity proved specifically for the ground branch, a quantitatively tiny state-specific moment constant, or a direct finite positivity certificate over a nontrivial aperture interval. Generic regularity consequences of `D(T)` are exhausted.

## Separate ambient topology, graph-domain sharpness, branch-specific structure, source activations and global coercivity

The ambient logarithmic form ball has sharp inverse-log transport. Actual eigenstates lie in the stronger graph domain, but PL-290 shows that the graph ball itself has sharp inverse-squared-log transport and no bounded first aperture derivative. A smoother individual eigenbranch remains possible only through extra source/eigen-equation structure.

Endpoint isolation, graph-domain membership, graph-ball sharpness, branch-specific cancellation, explicit transport constants, activation control and aperture-uniform positivity are therefore separate gates. Do not infer the next one from the previous one without a theorem using the additional structure.
