# Prime-lattice research lines

This file holds the current mathematical questions suggested by the durable prime-lattice intuitions. It is not a roadmap, task queue, status page, or history.

## Transfer the true moving endpoint through its exact prime-coordinate probability law

**Linked intuitions:** `MI-044-finite-prime-wold-classification-does-not-globalize-atomically`, `MI-045-fixed-gap-meet-join-data-collapse-to-finite-congruence-decoration`, `MI-046-growing-gap-arithmetic-complexity-pays-a-reciprocal-continuation-tariff`, `MI-047-mesoscopic-continuation-phase-can-miss-the-prime-pair-parity-sector`, `MI-048-cubic-phase-can-reopen-arithmetic-support-yet-average-to-a-universal-control`, `MI-049-abel-cubic-singular-series-asymptotics-isolate-an-rh-equivalent-zero-layer-at-model-level`, `MI-050-the-zero-sensitive-exact-kernel-bridge-now-ends-in-a-signed-prime-variance-problem`, `MI-051-cubic-source-replacement-has-a-variance-power-saving-curve`, `MI-052-cubic-filter-pushes-subleading-ramanujan-mass-to-a-moving-square-free-boundary`, `MI-053-the-abel-cusp-creates-a-deterministic-25-28-ramanujan-background`, `MI-054-finite-cusp-jet-subtraction-moves-the-model-obstruction-to-the-true-endpoint`, `MI-055-the-full-ramanujan-endpoint-is-a-heavy-tailed-independent-prime-law-carrying-the-zeta-zero-divisor`.

PL-391--PL-406 transfer the RH-equivalent model zero layer through the exact incomplete-gamma kernel, isolate the critical Ramanujan covariance, and show that the cubic test projects out the divergent diagonal. PL-407 calibrates the precision required by a generic source-subtracted short-interval variance route: at `H=X^theta`, an LPZ-shaped relative saving exponent `mu` preserves the `H^(-7/4)` layer only when `mu>3theta/(4(1-theta))` after absolute values are taken.

PL-408 localizes the model correction away from fixed Ramanujan interiors. PL-409 identifies the first sharp regulator alias: the kernel's `|x|^7` cusp produces a same-sign `J_8(q)H^(-8)` background whose cumulative contribution at `Q=H^kappa` meets the zero-sensitive `H^(-7/4)` layer at `kappa=25/28`.

PL-410 shows that this is the first term of a complete finite cusp-jet ladder, not a terminal boundary. The nonsmooth orders `8,14,20,...` map to explicit Jordan-totient aliases. After subtracting the first `M` exact finite counterterms, the remainder on `Q=H^kappa` is below `H^(-7/4)` whenever `kappa<1-3/(4(6M+7))`. Thus for every fixed `kappa<1`, finitely many explicit counterterms remove the entire fixed fractional interior at the zero-sensitive scale. The obstruction is genuinely at the true endpoint `q/H=Theta(1)`, where the moment expansion is no longer termwise integrable.

PL-411 reorganizes that full endpoint exactly. Summing all Ramanujan denominators after the divisor Möbius transform leaves a probability measure `nu` on odd square-free integers with independent prime coordinates

`P(p|M)=1/(p-1)^2`.

The entire model statistic becomes `2A_eta^[2](H)=E[P_eta(H/(2M))]`. This law has tail `P(M>X)~1/(2X)` and moments `E(M^a)<infinity` exactly for `a<1`, explaining why finite cusp-jet counterterms cannot simply be globalized to the endpoint. Its Mellin transform factors through the classical singular-series Dirichlet series and carries the denominator `zeta(2s+4)`, placing possible nontrivial-zero singularities at `s=rho/2-2` and recovering the `-7/4` critical exponent under RH, subject to possible cancellation by the remaining factors.

The live fork is now more structured. On the model side, analyze the signed archimedean expectation against this fixed heavy-tailed Bernoulli prime law and determine which zero singularities survive without accidental cancellation. On the arithmetic side, transfer the actual prime source to that endpoint carrier, either through the signed cubic functional itself or through a source-subtracted variance theorem above the PL-407 precision curve. Extending another fixed fractional denominator range is no longer the right target: PL-410 renormalizes it to finite order, while PL-411 identifies the exact endpoint object that remains.

## Keep exact-kernel transfer, endpoint law and actual prime source distinct

The continuation kernel and critical model covariance are no longer the bottlenecks, and no fixed fractional denominator interior is a fundamental model-side barrier. PL-410 can suppress every such fixed range below the zero-sensitive scale with finitely many exact cusp counterterms.

PL-411 does not remove the endpoint; it gives it an exact representation. The heavy-tailed independent-prime law belongs to the singular-series **model** and its continued Mellin transform contains the classical zeta-zero divisor, but no RH criterion follows until possible local/numerator cancellation is controlled. The surviving arithmetic difficulty is still source replacement: show that the actual prime spectral statistic tracks the signed endpoint functional at the required precision, or prove an equivalent prime-variance theorem. A future proof must keep the model probability law, its analytic continuation, the archimedean filter and the actual prime source as separate objects until each transfer is justified.