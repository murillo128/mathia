# Prime-lattice research lines

This file holds the current mathematical questions suggested by the durable prime-lattice intuitions. It is not a roadmap, task queue, status page, or history.

## Prove the uniform source-amplitude gate and the fixed-threshold noncollapse without importing the wrong Hopf structure

**Linked intuitions:** `MI-016-weil-form-observability-is-stronger-than-moment-observability` through `MI-026-the-threshold-archimedean-remainder-reverses-the-hopf-sign`.

PL-313--PL-323 close the moving Green endpoint transfer at each fixed aperture. The exact source equation, conservative singular-kernel flux identity and positive jump-kernel maximum principle force the primitive critical coefficient and `Qm(Q)->0`; there is no remaining generic analytic task to manufacture a finite endpoint limit.

PL-324--PL-325 classify the first arithmetic activation. Positivity survives on the odd Weil sector exactly until `a=(1/2)log2`; immediately afterward the first-prime translation has the wrong modulus sign. PL-326--PL-327 show that fixed-aperture endpoint asymptotics and canonical half-log boundary regularity do not orient the shrinking first-prime reflection: an antisymmetric layer of amplitude `L_delta^(-1/2)` can reverse a positive `delta/L_delta` sign reserve.

PL-329 supplies the sharp source-selected gate. If `S_delta` is the endpoint-source sup norm and `C_delta` the selected critical coefficient, eventual positivity follows from

`(1+S_delta)/(|C_delta| sqrt(L_delta)) -> 0`.

PL-330 proves norm-resolvent continuity and transports isolated eigenbranches continuously in `L^2`/graph topology, while PL-331 shows every fixed logarithmic Fourier seminorm still misses the hostile moving layer. PL-332 then shows, conditionally on the same uniform endpoint remainder, that any phase-aligned `L^2`-continuous branch satisfies `C_delta -> C_0`. Once the uniform source/remainder estimate is established, moving-family coefficient collapse is not an independent problem.

PL-333 audits the remaining fixed-threshold gate `C_0!=0` against the natural logarithmic-Laplacian Hopf route. Exactly at `a_0=(1/2)log2`, the first-prime translation has zero overlap, but Suzuki's bounded archimedean remainder has strictly positive off-diagonal kernel on the whole threshold square. Quantitatively it violates the first Beurling--Deny contraction inequality, so the full threshold form is not a Dirichlet form and its semigroup is not positivity preserving.

The obstruction is sharper even if a nonnegative threshold eigenstate were granted. Writing the threshold equation through the logarithmic Laplacian gives a boundary forcing term `-2K_0u`; because `K_0` is strictly positive, `L_Delta u` approaches a strictly negative value at the boundary. The state is therefore a strict **subsolution** near the endpoint, whereas the standard logarithmic-Laplacian Hopf theorem requires a nonnegative supersolution. The direct implication “positive threshold state + standard Hopf lemma => C_0!=0” is blocked by the exact completed archimedean term.

The first-prime frontier is correspondingly narrower but more source-specific. One still needs the actual completed-Weil branch to satisfy the uniform endpoint/source control required by PL-329, and one still needs the single threshold statement `C_0!=0`; PL-333 says that this nonvanishing must come from the **full threshold operator**, not from importing positivity/Hopf theory of the principal logarithmic-Laplacian form. A successful proof must compare the archimedean remainder and threshold eigenstate directly or find another source-selected nonvanishing identity.

## Keep fixed-window certificates and scalable tail geometry separate

PL-328 records a non-peer-reviewed, not independently reproduced computer-assisted source claiming full complex-test Weil coercivity at `a=17/16`, with active prime powers `2,3,4,5,7,8`. Conditional on independent validation it would certify every smaller aperture through the common-form dictionary, but it is not reproduced Mathia evidence.

Its structural warning remains relevant: beyond the `8` threshold, dropping frequency-dependent positive tail information creates a phase-locked negative high-frequency defect that no fixed bounded compact correction repairs. The first-prime source-amplitude problem and later multi-channel tail retention are different interfaces; a global theorem must control both.

## Keep transport, asymptotic uniqueness, operator positivity and destination orientation separate

The topology hierarchy is now explicit. Norm-resolvent/graph continuity transports the branch; every fixed logarithmic seminorm still misses the shrinking sign margin; a uniform `O(1/Q)` endpoint remainder makes the asymptotic coefficient visible on fixed blocks and lets `L^2` continuity transport it. PL-333 adds that positivity properties of the principal limiting form need not survive the bounded completed archimedean perturbation at the threshold. The source equation/full operator remains responsible both for the moving-scale remainder and for any fixed-threshold boundary nonvanishing.