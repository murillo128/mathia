# Prime-lattice research lines

This file holds the current mathematical questions suggested by the durable prime-lattice intuitions. It is not a roadmap, task queue, status page, or history.

## Put arithmetic coupling into the completed transition layer without buying it only through the gap

**Linked intuitions:** `MI-044-finite-prime-wold-classification-does-not-globalize-atomically`, `MI-045-fixed-gap-meet-join-data-collapse-to-finite-congruence-decoration`, `MI-046-growing-gap-arithmetic-complexity-pays-a-reciprocal-continuation-tariff`.

PL-391--PL-394 show that the frozen multiplicative geometry can scalarize while the continuation-faithful approximate functional equation retains reflected modes and the Gamma-phase cross channel. PL-395 rules out the broad off-diagonal stationary shell in the sharp moving main sum; PL-396 restores the exact incomplete-gamma transition layer; PL-397 computes its leading fixed-gap kernel and shows that the error-function weights and quadratic chirp are additive and factorization-blind.

PL-398 closes the first arithmetic overlap decoration. At fixed `d`, `gcd(r,r+d)=gcd(r,d)`, so meet data range only over divisors of `d` and join correction is determined by the same finite information. PL-399 then closes the immediate growing-gap repair. In the mesoscopic regime `1<<d<<r`, at the nominal saddle `T=2 pi r(r+d)` the larger-mode continuation coefficient is asymptotic to `1/(sqrt(2) pi d)`, while the smaller coefficient tends to one. Increasing the prime support of the gap therefore pays a reciprocal analytic tariff and no longer retains the order-one fixed-gap overlap.

The live question must consequently retain arithmetic information that is not purchased solely by increasing gap complexity. Can a completed observable carry separate endpoint factor vectors, Möbius/von-Mangoldt weights, divisor structure, cross-level data or another coupling `A(r,r+d)` whose source gain survives both the universal transition kernel and, when `d` grows, the `1/d` continuation tail? Aggregation over many gaps remains possible, but its signed arithmetic contribution must be controlled after the continuation weights are included rather than counted from gap support alone.

## Keep sharp-cutoff geometry, continuation scale and arithmetic discrimination separate

PL-395 remains exact for the sharp main sum; PL-396 restores the smooth transition; PL-397 shows that the fixed-gap leading kernel is universal; PL-398 reduces fixed-gap meet/join data to finite congruence information; PL-399 shows that mesoscopic gap growth weakens the larger continuation channel by `1/d`. None is itself arithmetic rigidity. Future constructions must state which completed quantity retains unbounded source information, how that information is weighted across fixed or growing gaps, and why the resulting signed/positive mechanism remains individually zero-sensitive rather than collapsing to scalar transition or density effects.