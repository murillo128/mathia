# Prime-lattice research lines

This file holds the current mathematical questions suggested by the durable prime-lattice intuitions. It is not a roadmap, task queue, status page, or history.

## Return to the arithmetic sign gate after closing endpoint transfer and order positivity

**Linked intuitions:** `MI-016-weil-form-observability-is-stronger-than-moment-observability` through `MI-022-odd-parity-postpones-order-positivity-failure-exactly-to-the-first-prime-event`.

PL-313--PL-323 close the moving Green endpoint transfer. The exact source equation, conservative singular-kernel flux identity and positive jump-kernel maximum principle force the primitive critical coefficient and `Qm(Q)->0`; there is no remaining generic analytic task to manufacture a finite endpoint limit.

PL-324 tests whether the RH-relevant odd Weil sector can supply the missing sign through standard positivity-preserving semigroup theory. Parity removes the full-space archimedean Beurling--Deny obstruction completely: the odd continuous modulus defect has the favorable sign at every aperture. Consequently the semigroup is positivity preserving exactly until the first prime event,

`0<a<=(1/2)log 2`.

Immediately after that threshold a cross-half prime-power translation has the wrong modulus sign, and a two-bump test makes it dominate the continuous correction. This is the clean arithmetic sensitivity that the full-space criterion lacked, but it is a **route boundary**: standard order positivity fails precisely when rational-prime arithmetic becomes active.

The live theorem is therefore narrower. The already transported rank-one endpoint datum must couple to the rational-prime Weil form through a signed first-crossing, eigenstate-orientation, boundary/flux or scalar compatibility law that does not replace the prime correlations by modulus/order positivity. Any successful theorem must use source-specific arithmetic absent from the universal endpoint transport and survive beyond the PL-324 first-prime threshold.

## Keep analytic transport, cone positivity and arithmetic orientation separate

PL-322--PL-323 are real-variable/nonlocal transport results. PL-324 is an exact order-positivity classification on the odd sector. Neither supplies the final arithmetic orientation: endpoint coercivity transports information, while parity shows that the positive cone stops being compatible exactly when the prime source enters.

Future work should preserve the closed Green transfer as an interface and work directly with the signed rational-prime contribution. Reproving generic endpoint regularity or searching for a standard positive-semigroup continuation past the first prime event is no longer a live direction.
