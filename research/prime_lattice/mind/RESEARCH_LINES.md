# Prime-lattice research lines

This file holds the current mathematical questions suggested by the durable prime-lattice intuitions. It is not a roadmap, task queue, status page, or history.

## Prove the source-amplitude gate for the actual shrinking first-prime eigenbranch

**Linked intuitions:** `MI-016-weil-form-observability-is-stronger-than-moment-observability` through `MI-025-source-amplitude-controls-first-prime-reflection`.

PL-313--PL-323 close the moving Green endpoint transfer at each fixed aperture. The exact source equation, conservative singular-kernel flux identity and positive jump-kernel maximum principle force the primitive critical coefficient and `Qm(Q)->0`; there is no remaining generic analytic task to manufacture a finite endpoint limit.

PL-324--PL-325 classify the first arithmetic activation. Positivity survives on the odd Weil sector exactly until `a=(1/2)log2`; immediately afterward the first-prime translation has the wrong modulus sign. PL-326--PL-327 show that fixed-aperture endpoint asymptotics and the canonical half-log boundary regularity do not orient the shrinking first-prime reflection: an antisymmetric layer of amplitude `L_delta^(-1/2)` can reverse a positive `delta/L_delta` sign reserve.

PL-329 supplies the source-selected quantitative gate. If `S_delta` is the exact endpoint-source sup norm and `C_delta` the selected critical coefficient, the reflection is eventually positive when

`(1+S_delta)/(|C_delta| sqrt(L_delta)) -> 0`.

The threshold is sharp for the known sign-reversing family. PL-330 then shows that ordinary spectral topology is not the blocker: despite the raw edge flip failing bounded-operator convergence, the full family is norm-resolvent continuous and simple eigenbranches can be transported continuously in the logarithmic graph norm.

PL-331 closes the remaining qualitative all-log regularity shortcut. The same critical sign-reversing layer converges to zero in **every fixed logarithmic Fourier seminorm** `X_m`; hence even the projective Fréchet topology generated by all fixed `m` cannot control the moving sign. The exact source equation still magnifies that invisible layer to the critical `sqrt(L_delta)` source scale.

The live missing quantifier is now sharply branch-specific. Prove that the **actual completed-Weil eigenbranch** satisfies the PL-329 source-amplitude condition, including non-collapse of `C_delta`, or prove a quantitatively stronger regularity estimate whose constants can be optimized with `m=m(delta)` or give genuine positive-order control. Adding more fixed logarithmic moments, graph continuity or norm-resolvent continuity cannot close the sign.

## Keep fixed-window certificates and scalable tail geometry separate

PL-328 records a non-peer-reviewed, not independently reproduced computer-assisted source claiming full complex-test Weil coercivity at `a=17/16`, with active prime powers `2,3,4,5,7,8`. Conditional on independent validation it would certify every smaller aperture through the common-form dictionary, but it is not reproduced Mathia evidence.

Its independent structural warning remains relevant: beyond the `8` threshold, dropping frequency-dependent positive tail information creates a phase-locked negative high-frequency defect that no fixed bounded compact correction repairs. The first-prime source-amplitude problem and later multi-channel tail retention are different interfaces; a global theorem must control both the selected moving state and the positive tail as arithmetic channels activate.

## Keep spectral transport, regularity and destination orientation separate

The topology hierarchy is now explicit. Bounded-operator norm is too strong and sees a fake source jump; norm-resolvent and graph continuity transport the branch but miss the shrinking sign margin; every fixed logarithmic seminorm still misses the critical boundary layer; the source equation sees exactly that layer at the required scale. Any future regularity proposal should be tested against PL-331 before being treated as a substitute for source amplitude.