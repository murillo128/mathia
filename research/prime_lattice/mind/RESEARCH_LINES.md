# Prime-lattice research lines

This file holds the current mathematical questions suggested by the durable prime-lattice intuitions. It is not a roadmap, task queue, status page, or history.

## Prevent the first forbidden localized Weil crossing with state-specific control beyond the sharp full-comb form modulus

**Linked intuition:** `MI-016-weil-form-observability-is-stronger-than-moment-observability`.

PL-280--PL-282 give the one-sided aperture ledger, global exhaustion, and a terminating finite positivity certificate at each fixed aperture. PL-283--PL-284 add a conditional computer-assisted positive endpoint at `a=0.8` with a simple isolated ground state and a tiny but explicit positive margin.

PL-285 closes naive bounded-operator transport after scaling to a fixed interval. Already-active prime-power channels are compressed translations; ambient `L^2` operator norm is discontinuous, while the canonical logarithmic quadratic-form modulus is sharply `Theta(1/log(1/|a-b|))`.

PL-286 closes the generic cancellation escape for the complete active arithmetic comb on the first source-static interval. With active prime powers `2,3,4`, Kronecker phase locking makes the `2` and `3` channels add coherently while the `4=2^2` channel returns through a full phase turn. The full three-channel prime comb therefore has the same sharp inverse-logarithmic worst-case modulus as the termwise bound.

The live theorem must now use information unavailable on an arbitrary `H^log` energy ball. Candidate currencies are uniform stronger regularity/high-frequency-tail control of the actual low eigenbranch, cancellation forced by its eigen-equation or variational structure, or another exact source-specific identity. Generic frequency splitting and generic cancellation among the finite moving von-Mangoldt channels are both exhausted.

## Separate endpoint isolation, qualitative continuity, sharp generic full-comb continuity, and aperture-uniform coercivity

A simple positive ground state makes local tracking meaningful, but PL-285--PL-286 show that neither optimizing the generic form topology nor summing the whole active source comb produces a useful transport radius. A future endpoint certificate matters globally only if it composes with **state-specific** source information about the canonical low branch.