# Prime-lattice research lines

This file holds the current mathematical questions suggested by the durable prime-lattice intuitions. It is not a roadmap, task queue, status page, or history.

## Transfer the true moving endpoint through its exact prime-coordinate probability law

**Linked intuitions:** `MI-044-finite-prime-wold-classification-does-not-globalize-atomically`, `MI-045-fixed-gap-meet-join-data-collapse-to-finite-congruence-decoration`, `MI-046-growing-gap-arithmetic-complexity-pays-a-reciprocal-continuation-tariff`, `MI-047-mesoscopic-continuation-phase-can-miss-the-prime-pair-parity-sector`, `MI-048-cubic-phase-can-reopen-arithmetic-support-yet-average-to-a-universal-control`, `MI-049-abel-cubic-singular-series-asymptotics-isolate-an-rh-equivalent-zero-layer-at-model-level`, `MI-050-the-zero-sensitive-exact-kernel-bridge-now-ends-in-a-signed-prime-variance-problem`, `MI-051-cubic-source-replacement-has-a-variance-power-saving-curve`, `MI-052-cubic-filter-pushes-subleading-ramanujan-mass-to-a-moving-square-free-boundary`, `MI-053-the-abel-cusp-creates-a-deterministic-25-28-ramanujan-background`, `MI-054-finite-cusp-jet-subtraction-moves-the-model-obstruction-to-the-true-endpoint`, `MI-055-the-full-ramanujan-endpoint-is-a-heavy-tailed-independent-prime-law-carrying-the-zeta-zero-divisor`, `MI-056-away-from-five-dealiasing-is-a-source-realized-model-repair`, `MI-057-away-from-five-repair-is-a-finite-fourier-variance-vector`.

PL-407--PL-411 move the model obstruction from fixed fractional Ramanujan ranges to the true endpoint and reorganize that endpoint exactly as an expectation over an odd square-free random integer whose prime coordinates are independent with `P(p|M)=1/(p-1)^2`. The law is heavy-tailed and its Mellin transform contains the zero-sensitive divisor `1/zeta(2s+4)`.

PL-412 audits the cancellation caveat in that Mellin factorization instead of assuming every transformed zeta zero survives. Among the local Euler factors, `p=5` is the unique one whose zero locus can meet the RH-relevant half-strip `1/2<Re(rho)<1`; after deleting that coordinate, the only remaining cancellation mechanism is the numerator `zeta(rho/2)`. A descent under the functional equation rules out persistent numerator cancellation and yields the exact model criterion

`RH iff N_5(s)-2/(5(s+1)) is holomorphic in Re(s)>-7/4`.

This is a repaired model criterion, not a proof of RH. Its significance is that the previously hidden finite-prime alias is explicit and finite rather than being folded into an unaudited Euler-product remainder.

PL-413 closes the source/model arbitrariness of that repair. Restricting the singular-series Ramanujan source to denominators coprime to an odd prime `p` transports under the PL-411 endpoint reorganization exactly to conditioning the Bernoulli endpoint law on `p` not dividing `M`. For `p=5`, the source statistic “singular series away from 5” therefore produces exactly the conditioned law used in PL-412. The de-aliasing is source-realized by a standard arithmetic operation rather than chosen after seeing the continued model.

PL-414 then identifies the exact actual-prime observable produced by that source operation. The multiplier `g_5(h)=beta_5(h)^(-1)` is both a normalized average over admissible residue classes modulo `5` and the finite Fourier projector

`g_5(h)=76/75-(4/75) sum_{j=1}^4 e(jh/5)`.

Consequently the away-from-five cubic source-replacement error is exactly a fixed combination of five short-interval quadratic variance sectors at rational frequencies `j/5`, `j=0,...,4`. If all five source-subtracted triangular remainders obey the PL-407-shaped estimate

`|Delta T_{X,j}(L)| << X L (L/X)^mu (log X)^B`,

then the previous threshold is unchanged: for `H=X^theta`, `mu>3 theta/(4(1-theta))` suffices, with the same square-root calibration boundary `theta=2/5`. Controlling only the untwisted sector is insufficient.

The live actual-prime question is therefore finite and quantitative. Prove or refute the required power-saving signed estimate simultaneously for the five fixed mod-5 variance sectors on the kernel's moving short-interval range. The local `p=5` repair, its source provenance, and its actual-prime residue/Fourier representation are already exact; further model-side pole bookkeeping is secondary unless it changes this five-sector transfer target.

## Keep source projection, endpoint law, analytic continuation and actual primes distinct

PL-412 corrects earlier unqualified pole inferences: denominator zeros must be audited against local and numerator cancellation before an RH-equivalent model criterion is credited. PL-413 then shows that the successful finite-prime repair is already present at the arithmetic-source level. PL-414 adds that the repaired source has an exact actual-prime representation as a five-component fixed-modulus variance vector. None of these steps supplies the power-saving estimate required to transfer the zero-sensitive endpoint criterion to primes.

A future proof must therefore preserve four separate objects: the declared away-from-5 source observable, its exact conditioned endpoint probability law, the meromorphic continuation that carries the zero divisor, and the five mod-5 twisted actual-prime variance sectors whose signed remainder must be controlled. The first three maps are now explicit; the unresolved bridge is the quantitative estimate on the fourth object, not another representation choice.