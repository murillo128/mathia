# MI-013 — Source-Hamiltonian centering fixes regulator covariance below critical scale; critical transport is nonclosable

**Evidence level:** supported by the exact Fock coefficient carrier and complete-incidence/renormalization boundaries through WP-266; a closable regulator-independent completed Weil orientation remains open.

WP-256 produces the exact positive Mangoldt half-density from prime-word multiplicity and arithmetic energy. WP-259 passes canonically to the permutation-fixed sector, retaining the prime-power diagonal `Lambda(n)/sqrt(n)`. WP-260 shows that canonical bosonic Gibbs/Fisher convexity inserts the wrong occupation factor.

WP-261--WP-263 test increasingly global cross-prime mechanisms. Finite-dimensional coherent collapse is nonclosable at the critical scale. Infinite-dimensional locally finite incidence is bounded but leaves support/phases programmable. Conditional-Gibbs complete incidence lets the source Hamiltonian choose a canonical weighted complete graph, but critical unit-diagonal normalization collapses fixed prime-prime correlations because `sum_p1/p` diverges.

WP-264 exposes a nontrivial finite part after subtracting the divergent diagonal coefficient, with one positive direction but an unfixed diagonal shift. WP-265 shows that this is not merely formal counterterm freedom: leading-only sharp and Gibbs regulators generated by the same source Hamiltonian land in different inertia classes.

WP-266 identifies the strongest source-native repair currently available. Let

\[
H e_p=(\log p)e_p,
\qquad
\mathbb E_H(A)=\sum_pP_pAP_p.
\]

Because the prime energies are simple, this diagonal expectation is determined by `H`. Applying the connected projection `I-\mathbb E_H` **before the limit** removes both the divergent one-body term and the regulator-dependent finite remainder. The sharp and Gibbs families become

\[
a_Xa_X^*-D_X,
\qquad
a_\varepsilon a_\varepsilon^*-D_\varepsilon,
\]

and both converge in trace norm to

\[
K=aa^*-D,
\qquad d_p=\frac{\log p}{p^{3/2}}.
\]

This is not merely a normalization convenience: every finite compression has exactly one positive direction and is strictly negative on the primitive hyperplane `ker a^*`. The tested regulator ambiguity is therefore removable by an intrinsic source operation.

But the repair lives one factor `p^{-1}` below the Weil critical half-density. The canonical source-Hamiltonian transport `S=e^{H/2}` restores the critical amplitudes on finite compressions and preserves their inertia, yet globally it produces the rank-one coefficient vector

\[
b_p=\sqrt{\frac{\log p}{\sqrt p}},
\qquad
\sum_p|b_p|^2=\infty.
\]

Hence the corresponding rank-one quadratic form is nonclosable on `ell^2`. The canonical operation that fixes scale at finite level destroys the global analytic category. The connected remainder also supplies no Gamma/polar term.

The live compatibility problem is therefore sharper than regulator selection: **derive one source-native completion in which regulator covariance, critical scaling, closability, and the archimedean sector coexist**. Reintroducing a scalar diagonal counterterm merely restores the programmability already diagnosed. A successful boundary/cohomological mechanism must explain the critical transport itself or replace it by a different canonical global carrier.

**Boundary.** WP-266 does not rule out nonlinear quotients, boundary sectors, cohomological completions, or other global mechanisms. It proves that the most canonical connected complete-incidence remainder is regulator-independent only in a subcritical trace-class category, while its direct Hamiltonian rescaling to the Weil prime amplitude is globally nonclosable.