# MI-013 — Critical coherence needs an independently selected nonlocal arithmetic sampling law

**Evidence level:** supported by the exact carrier/category/sign/compression results through WP-274; a completed Weil-positive correspondence remains open.

WP-267--WP-270 separate source coherence, critical-amplitude hostability, positive spectral hostability, and category descent. The canonical zeta-pole completion `T_pole=nu_(1/2)-cosh(x/2) dx` becomes tempered if and only if RH, so ordinary distributional descent is not a free preprocessing step. WP-271--WP-272 then show that finite-order conditional positivity and continuous scalar filtering cannot repair the sign while retaining the Mangoldt atoms.

WP-273 moves to non-scalar compression. In the canonical positive-minus-positive Krein realization, every nonnegative subspace is the graph of a contraction over its arithmetic projection. For function-generated ranges this is exactly a weighted sampling inequality: prime-power samples must reconstruct the full pole-continuum norm. Localizable continuous spaces fail, because a cutoff can isolate an atom-free interval where the arithmetic term vanishes and the continuum remains negative.

WP-274 tests the most immediate global analytic escape. The pole weight canonically normalizes the source by `g=cosh(x/2)^(1/2)f`, turning the discrete term into

`sigma_Lambda = sum Lambda(n)/(n+1) (delta_(log n)+delta_(-log n))`.

This positive measure is Herglotz-admissible unconditionally. Classical Herglotz--Clark inversion can therefore manufacture a meromorphic inner function whose model space is isometric to `L^2(sigma_Lambda)`. On the corresponding pulled-back range the pole-completed form is exactly zero. So a nonlocal analytic positive/null compression **does exist**.

But the construction is universal for Poisson-finite positive-minus-positive Jordan data. The target sampling measure is supplied first, the inner function is reconstructed from it, and Clark's theorem returns the same sampling identity. No primes, Euler product, functional equation, or Riemann-specific sign mechanism is needed for that logical pattern. The apparent success is representation-theoretically tautological and survives matched replacement of the arithmetic atoms.

The surviving condition is therefore stronger than nonlocality: the analytic object must be **selected independently of the target carrier whose positivity it is meant to explain**. Mathia/source geometry must force the inner function, canonical system, de Branges Hamiltonian, boundary law, or equivalent object before the Mangoldt measure is used as its spectral data. Only then can reproducing the finite-prime term—and simultaneously the Gamma and polar sectors—carry explanatory content.

The reusable gate is now sixfold: **hostability, category descent, scalar sign, arithmetic retention, nonlocal reconstruction, and independent model selection are separate compatibility tests**. Passing nonlocal sampling by inverse representation does not advance the Weil sign problem.

**Boundary.** WP-274 does not rule out de Branges/model/canonical-system routes; it rules out deriving their object from the desired normalized carrier and then citing the resulting sampling identity as evidence. The exact Clark construction treats the pole-completed Mangoldt-minus-pole carrier, not the full completed Weil functional with its Gamma contribution.