# MI-064 — The full bounded Bost--Connes positive cone cannot isolate prime-power support

**Evidence level:** exact C-star-algebraic synthesis from [WP-392](../../findings/WP-392-full-bost-connes-positive-cone-cannot-isolate-prime-power-support.md), strengthening the diagonal obstruction [WP-391](../../findings/WP-391-bost-connes-divisibility-diagonal-cannot-isolate-prime-power-support.md) and interpreted against the source-forced critical Gram of WP-216. The crossed-product representation and norm density are classical; the line-specific content is the exact mixed-prime diagonal recurrence and its positivity consequence for the full bounded source algebra.

In every standard positive-energy representation of the Bost--Connes C-star algebra, let

`a_A(k)=<A epsilon_k,epsilon_k>`.

If a bounded Bost--Connes element has `a_A(k)=0` for every integer carrying at least two distinct prime factors, then its diagonal vanishes for **every** `k`. Positivity then forces the whole operator to vanish, because zero diagonal on a total basis gives `A^(1/2) epsilon_k=0` for all `k`.

The mechanism survives the passage from the projection diagonal to the full crossed product. A finite algebraic element sees only finitely many divisibility indices and finite-order cyclotomic phases. After collecting them into one modulus `L`, multiplication by `Q=(L+1)(2L+1)` preserves the entire finite-stage diagonal while making every `kQ` mixed-prime. Norm approximation transfers that recurrence to the full bounded C-star algebra.

Thus source-forced off-diagonal terms do not rescue an **exact positive** prime-power selector inside the same bounded category. This sharply separates bounded source algebra from arbitrary Hamiltonian spectral coding: target-chosen `F(H)` can interpolate prime-power values precisely because it lives in a larger programmable diagonal class.

The next Bost--Connes escape must genuinely change category or delay selection. Unbounded/closable forms, modular or GNS constraints, a one-sided correspondence defect, a different boundary algebra, or finite--archimedean/global assembly remain possible only if their defining law is fixed by the source before prime-power support is inspected.

**Boundary.** The theorem concerns the norm-closed bounded Bost--Connes C-star algebra in the standard positive-energy representations and exact mixed-prime nullity. The von Neumann bicommutant is `B(ell^2(N^x))` and already contains arbitrary diagonal selectors, so passing to weak closure without an additional canonical constraint merely restores programmability. Unbounded affiliated operators, modular weights, approximate suppression, and global completion remain outside the no-go.