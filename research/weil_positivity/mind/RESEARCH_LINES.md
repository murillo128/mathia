# Weil-positivity research lines

This file holds the current mathematical lines of investigation suggested by the durable weil-positivity intuitions. It is not a roadmap, task queue, status page, or history.

## Require a source-forced completion beyond the classified centered-bath positivity resource

**Linked intuitions:** `MI-001-positivity-needs-a-sign-producing-global-operation`, `MI-003-arithmetic-selector-and-positive-bulk-are-separated-by-symmetry-breaking`, `MI-006-finite-mass-positive-completion-forces-mixed-prime-coupling-and-critical-roughness`, `MI-008-critical-half-density-exposes-archimedean-phase-only-before-gram-scalarization`, `MI-009-finite-regular-passive-enlargements-cannot-realize-the-gamma-divisor`.

WP-179--WP-197 show that scalar lossless order is too rigid, scalar dissipation too flexible, finite regular completions fail on the Gamma divisor, and higher-order spectral-shift positivity on the actual Weil autocorrelation cone is highly nonselective. Even order `4k` admits universal zero-background positive spectra, while every finite-dimensional monotone-positive pair `H>=0,V>=0` satisfying the full cone collapses to `HV=0`, with noncommutativity unable to escape the zero-mode rigidity.

WP-198--WP-202 now classify the most direct infinite-dimensional centered-diagonal-bath escape. A sufficiently rough bath can stabilize arbitrary fixed finite monotone-positive payloads, but Schatten membership or infinite dimension is not the resource: lacunary baths with comparable roughness can fail. WP-202 gives the exact boundary for this direct-sum mechanism. Universal finite-payload stabilization is equivalent to the reciprocal high-frequency floor

`liminf_(|xi|->infinity) |xi| F_0(xi) > 0`

for the bath's even higher-order spectral-shift Fourier multiplier.

That floor is still nonarithmetic. Completely non-Riemann baths can satisfy it, and it contains no finite-prime incidence, Gamma/pole term, or orientation. The live completion must therefore source-force the actual finite--archimedean/global relation **inside the ordered object** and explain why its reciprocal-frequency resource belongs to the Riemann source rather than to a generic stabilizing bath.

## Treat higher-order spectral shift as a matched-control laboratory, not a bridge by itself

The `4k` classifications now include both finite monotone-positive zero-mode collapse and the exact centered infinite-bath stabilization criterion. Any proposed geometric Hamiltonian should be tested against arbitrary zero-background spectra, positive-definite multiplicity interference, finite noncommuting monotone-positive pairs, and nonarithmetic centered baths with a reciprocal Fourier floor before its positivity is interpreted arithmetically.

A surviving higher-order route must leave this classified category in a source-forced way and bring its own order theorem. Infinite dimension, rough singular-value decay, noncommutativity, or a positive Fourier floor alone are not arithmetic selectors.

## Derive mixed-prime incidence inside the same positive completion

Pairwise resultants, separated local scattering carriers, fixed finite arity, refinement-positive kernels, and generic spectral-shift stabilization do not manufacture an independently ordered mixed-prime Weil object. A survivor must carry a source-forced mixed relation before the sign theorem and show how the real-place and global defect sectors enter the same coercive structure.

## Use Burnol and the `4k` classifications as exact boundary controls

Burnol identifies real and p-adic positive scattering carriers while exposing the mismatch between carrier positivity and Weil orientation. WP-193--WP-202 perform the analogous stress test for higher regularization: positivity either arises from nonarithmetic spectra/baths or collapses under monotone-positive finite geometry. Any claimed bridge must explain what distinguishes the Riemann source from all of these controls.
