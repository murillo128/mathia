# Weil-positivity research lines

This file holds the current mathematical lines of investigation suggested by the durable weil-positivity intuitions. It is not a roadmap, task queue, status page, or history.

## Source-force a completed sign structure that retains both Gamma provenance and critical roughness

**Linked intuitions:** `MI-001-positivity-needs-a-sign-producing-global-operation`, `MI-003-arithmetic-selector-and-positive-bulk-are-separated-by-symmetry-breaking`, `MI-006-finite-mass-positive-completion-forces-mixed-prime-coupling-and-critical-roughness`, `MI-008-critical-half-density-exposes-archimedean-phase-only-before-gram-scalarization`, `MI-009-finite-regular-passive-enlargements-cannot-realize-the-gamma-divisor`.

WP-179--WP-202 classify a broad higher-order spectral-shift positivity resource. Finite monotone-positive geometry collapses at the zero mode, while an infinite centered diagonal bath can stabilize arbitrary fixed finite payloads exactly when its even multiplier has a reciprocal high-frequency floor. That resource is quantitative but nonarithmetic.

WP-203--WP-205 show that exact Gamma provenance and positive extension geometry do not select the needed object. The Riemann Gamma factor has a canonical number-operator determinant, finite smooth functional calculus is too regular, and a singular fractional extension realizes the critical reciprocal floor only after externally choosing one order `n=4k` and the matching channel multiplicity.

WP-206 replaces that artificial fractional multiplicity by a genuine geometric mechanism: a `d=n-1` mode oscillator with ordinary inverse automatically has the critical Schatten roughness and reciprocal floor. But its determinant belongs to the Barnes multiple-gamma hierarchy, whose level multiplicities grow as `binom(m+d-1,d-1)`. Exact Euler-Gamma divisor with positive determinant weights forces `d=1`; higher-dimensional multiplicity cannot cancel positively.

WP-207 tests the natural cohomological escape. A Koszul complex implements the required Barnes finite difference intrinsically, and its cohomology is positive, but exactness collapses the surviving spectrum to a one-mode quotient. The Euler divisor is recovered precisely by removing the Barnes multiplicity that generated the reciprocal floor. Keeping the chain-level roughness instead requires a graded supertrace/signed cancellation.

The live completion must therefore source-force a **global sign architecture in which the cancellation creating the Euler-Gamma normalization and the roughness creating the positivity floor are not demanded from the same positive scalar spectrum**. It must also couple finite Mangoldt/polar sectors in the same ordered object. Merely adding oscillator dimension, fractional extension geometry, a Barnes determinant, or a standard Koszul/Hodge resolution is now a matched control rather than a Weil bridge.

## Treat higher-order spectral shift as a matched-control laboratory, not a bridge by itself

The `4k` classifications now include finite monotone-positive zero-mode collapse, the exact centered infinite-bath criterion, exact Gamma-determinant nonuniqueness, smooth-calculus failure, positive fractional-extension realization, dimension-forced Barnes roughness, and the Koszul fork between positive roughness and the Euler divisor.

A surviving higher-order route must leave this category in a source-forced way and bring its own order/sign theorem. Infinite dimension, rough singular-value decay, noncommutativity, a positive Fourier floor, Gamma/Barnes determinant provenance, fractional extension, or cohomological exactness alone are not arithmetic selectors.

## Derive mixed-prime incidence inside the same positive completion

Pairwise resultants, separated local scattering carriers, fixed finite arity, refinement-positive kernels, and generic spectral-shift stabilization do not manufacture an independently ordered mixed-prime Weil object. A survivor must carry a source-forced mixed relation before the sign theorem and show how the real-place and global defect sectors enter the same coercive structure.

## Use Burnol and the higher-order classifications as exact boundary controls

Burnol identifies real and p-adic positive scattering carriers while exposing the mismatch between carrier positivity and Weil orientation. WP-193--WP-207 perform the analogous stress test for higher regularization: positivity either arises from nonarithmetic spectra/baths, conflicts with the exact Gamma divisor, or recovers that divisor only by collapsing the roughness that supplied the floor. Any claimed bridge must explain what distinguishes the Riemann source from all of these controls.
