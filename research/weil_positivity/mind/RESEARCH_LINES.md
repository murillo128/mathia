# Weil-positivity research lines

This file holds the current mathematical lines of investigation suggested by the durable weil-positivity intuitions. It is not a roadmap, task queue, status page, or history.

## Source-force both the reciprocal-floor geometry and the spectral-shift order

**Linked intuitions:** `MI-001-positivity-needs-a-sign-producing-global-operation`, `MI-003-arithmetic-selector-and-positive-bulk-are-separated-by-symmetry-breaking`, `MI-006-finite-mass-positive-completion-forces-mixed-prime-coupling-and-critical-roughness`, `MI-008-critical-half-density-exposes-archimedean-phase-only-before-gram-scalarization`, `MI-009-finite-regular-passive-enlargements-cannot-realize-the-gamma-divisor`.

WP-179--WP-202 classify a broad higher-order spectral-shift positivity resource. Finite monotone-positive geometry collapses at the zero mode, while an infinite centered diagonal bath can stabilize arbitrary fixed finite payloads exactly when its even multiplier has a reciprocal high-frequency floor. That resource is quantitative but nonarithmetic.

WP-203 shows that exact archimedean provenance does not select it. The Riemann Gamma factor has a canonical number-operator relative zeta determinant, yet two baths built from the same Gamma number operator and carrying the same exact determinant can lie on opposite sides of the reciprocal-floor boundary. The determinant does not fix the required roughness.

WP-204 sharpens the local obstruction. Every finite `C^1` compact functional calculus of the Gamma inverse resolvent is too smooth: it falls into `S^(n-1)` and cannot realize the `WP-202` floor at order `n=4k`. The successful power sits exactly at a singular fractional threshold rather than arising from a finite smooth determinant jet.

WP-205 shows that this singularity can nevertheless be geometrically natural. Fractional extension geometry realizes the critical inverse power as a positive Neumann-to-Dirichlet response. With identical channels, exact unscaled Gamma determinant normalization plus `S^n` membership and the reciprocal floor force `M=n-1` channels with exponent `s=1/(n-1)`. But the source still leaves the infinite ladder `n=4k`; neither the Gamma determinant nor the extension theorem selects `k`.

The live completion must therefore source-force **two choices before positivity is interpreted arithmetically**: the singular reciprocal-floor geometry and the relevant spectral-shift order/channel multiplicity. It must also place the finite Mangoldt and polar sectors in the same ordered object. A generic fractional extension at an externally chosen `4k` remains a matched control, not a Weil bridge.

## Treat higher-order spectral shift as a matched-control laboratory, not a bridge by itself

The `4k` classifications now include finite monotone-positive zero-mode collapse, the exact centered infinite-bath stabilization criterion, exact Gamma-determinant nonuniqueness, smooth-calculus failure, and a positive fractional-extension realization with an unselected mod-four ladder. Any proposed geometric Hamiltonian should be tested against all of these controls before its positivity is interpreted arithmetically.

A surviving higher-order route must leave this classified category in a source-forced way and bring its own order theorem. Infinite dimension, rough singular-value decay, noncommutativity, a positive Fourier floor, Gamma determinant provenance, or fractional-extension positivity alone are not arithmetic selectors.

## Derive mixed-prime incidence inside the same positive completion

Pairwise resultants, separated local scattering carriers, fixed finite arity, refinement-positive kernels, and generic spectral-shift stabilization do not manufacture an independently ordered mixed-prime Weil object. A survivor must carry a source-forced mixed relation before the sign theorem and show how the real-place and global defect sectors enter the same coercive structure.

## Use Burnol and the higher-order classifications as exact boundary controls

Burnol identifies real and p-adic positive scattering carriers while exposing the mismatch between carrier positivity and Weil orientation. WP-193--WP-205 perform the analogous stress test for higher regularization: positivity either arises from nonarithmetic spectra/baths or remains underdetermined even after exact Gamma and extension geometry are imposed. Any claimed bridge must explain what distinguishes the Riemann source from all of these controls.
