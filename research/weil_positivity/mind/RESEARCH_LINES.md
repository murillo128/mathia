# Weil-positivity research lines

This file holds the current mathematical lines of investigation suggested by the durable weil-positivity intuitions. It is not a roadmap, task queue, status page, or history.

## Seek a source-forced completion whose order acts on the Weil test cone itself

**Linked intuitions:** `MI-001-positivity-needs-a-sign-producing-global-operation`, `MI-003-arithmetic-selector-and-positive-bulk-are-separated-by-symmetry-breaking`, `MI-006-finite-mass-positive-completion-forces-mixed-prime-coupling-and-critical-roughness`, `MI-008-critical-half-density-exposes-archimedean-phase-only-before-gram-scalarization`, `MI-009-finite-regular-passive-enlargements-cannot-realize-the-gamma-divisor`.

WP-179--WP-193 show that scalar lossless order is too rigid, scalar dissipation too flexible, finite regular completions fail on the Gamma divisor, local positive scattering carriers do not order the signed Weil block, adelic causality is RH-equivalent, and higher-order spectral-shift positivity must be tested on the actual autocorrelation cone. Even orders `2 mod 4` fail that cone, while order `4k` requires positive definiteness of the even spectral-shift density.

WP-194--WP-197 now show that this `4k` gate is both nonempty and severely nonselective. Source-centered Taylor remainders give full autocorrelation-cone positivity for arbitrary self-adjoint Schatten spectra; finite commuting off-center backgrounds can recover the same sign whenever their spectral multiplicity measure is positive definite; and for **every finite-dimensional monotone-positive pair** `H>=0`, `V>=0`, with or without commutativity, full `4k` Weil-cone positivity forces `HV=0`. WP-197 obtains the noncommuting rigidity from positive source jumps of the higher-order spectral-shift density, so commutativity reappears as a consequence rather than an assumption.

Thus zero centering, positive-definite multiplicity interference, and finite-dimensional noncommuting positive-energy coupling all fail to supply arithmetic selection. The live completion must source-force the actual finite-prime, Gamma, polar/global incidence and orientation inside the same ordered object, and its sign must survive the Weil autocorrelation cone without reducing to universal zero-mode support or generic spectral multiplicity.

## Treat higher-order spectral shift as a matched-control laboratory, not a bridge by itself

The exact `4k` classifications provide strong controls. A proposed geometric Hamiltonian should be tested against arbitrary zero-background spectra, positive-definite difference-spectrum multiplicities, and the full finite-dimensional `H>=0,V>=0` zero-mode theorem before its positivity is interpreted arithmetically.

A surviving higher-order route must leave the classified category in a source-forced way: infinite-dimensional/continuous-spectrum structure with controlled source jumps, a sign-indefinite but geometrically constrained perturbation, a completed functional with genuine finite--archimedean coupling, or another order mechanism not reducible to the bare Taylor remainder. **Finite-dimensional noncommutativity alone is no longer a live exit.** Every category change must come with its own order theorem.

## Derive mixed-prime incidence inside the same positive completion

Pairwise resultants, separated local scattering carriers, fixed finite arity, refinement-positive kernels, and universal spectral-shift positivity do not manufacture an independently ordered mixed-prime Weil object. A survivor must carry a source-forced mixed relation before the sign theorem and show how the real-place and global defect sectors enter the same coercive structure.

## Use Burnol and the `4k` classifications as exact boundary controls

Burnol identifies real and p-adic positive scattering carriers while exposing the mismatch between carrier positivity and Weil orientation. WP-193--WP-197 perform the analogous test for higher regularization: the target cone imposes Fourier positive-definiteness, but that property either arises from completely nonarithmetic spectra or, under finite monotone-positive geometry, forces the perturbation onto the universal zero mode. Any claimed bridge must explain what distinguishes the Riemann source from all of these controls.
