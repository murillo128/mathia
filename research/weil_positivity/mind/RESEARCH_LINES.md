# Weil-positivity research lines

This file holds the current mathematical lines of investigation suggested by the durable Weil-positivity intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among mathematically different mechanisms.

## Couple the arithmetic selector before positive refinement fixes the wrong depth law

**Linked intuitions:** `MI-001-positivity-needs-a-sign-producing-global-operation`, `MI-003-arithmetic-selector-and-positive-bulk-are-separated-by-symmetry-breaking`, `MI-004-mangoldt-anchor-is-unbounded-in-positive-hardy-norm`, and `MI-006-finite-mass-positive-completion-forces-mixed-prime-coupling-and-critical-roughness`.

The canonical full-chord refinement carrier is now classified across regular and first singular responses. Repeated-prime coarse response is stationary; regular full-fiber traces are zero or extensive; repeated-prime Green traces produce generic harmonic/log-cover growth; and regular positive new-prime puncture responses remain uniformly bounded instead of acquiring `log q`.

WP-139 also closes the minimal zero-shift singular puncture escape. At `d=2` the normalized pseudodeterminant finite part is exactly `-log 16+log(1-1/m)` for every odd prime or composite matched fiber degree. The only divergent term is the universal extra-nullity `log(1/lambda)`; after intrinsic normalization the finite part is bounded and prime-blind.

A decisive finite--archimedean mechanism must therefore form the arithmetic selector before or inside a genuinely new singular/global positive assembly, or derive a source-forced depth-dependent/nonlinear observable whose normalization and sign are independent of the target coefficient. Generic cover logarithms, universal nullity poles, and bounded positive puncture shifts are controls, not the missing prime selector.

## Change the operator before asking anchored or distributional Gamma carriers to inherit positivity

**Linked intuitions:** `MI-002-operator-category-is-a-hard-compatibility-gate` and `MI-003-arithmetic-selector-and-positive-bulk-are-separated-by-symmetry-breaking`.

Bounded radial--solenoid Gamma intertwiners vanish by spectral type, exact distributional intertwiners collapse to nonclosable point samplers, and critical sampling is not square summable. The direct sign-inheritance workaround is closed as well: a nonzero positive anchored form killing the zero mode cannot remain contractive under the old diagonal Gamma heat semigroup even on a finite sample block.

A surviving bridge must therefore introduce a genuinely new coupled generator, domain, boundary response, or cohomological geometry and prove its own positivity/coercivity.

## Classify singular, nonlocal, nonlinear, or higher-cohomological alternatives beyond canonical cover/refinement quotients

**Linked intuitions:** `MI-005-cover-coinvariants-classicalize-or-collapse`, `MI-002-operator-category-is-a-hard-compatibility-gate`, and `MI-006-finite-mass-positive-completion-forces-mixed-prime-coupling-and-critical-roughness`.

Zeroth cover coinvariants classicalize to trace or collapse; passive shorting and same-algebra deletion fail; regular repeated/new-prime positive assembly has the wrong depth scale; the first Green singularity yields universal harmonic logarithms; and the minimal singular one-hole finite part is bounded after the canonical normalization. Any surviving category must genuinely change the architecture and identify what source-specific information remains after these controls.
