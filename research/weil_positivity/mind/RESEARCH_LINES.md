# Weil-positivity research lines

This file holds the current mathematical lines of investigation suggested by the durable Weil-positivity intuitions. It is not a roadmap, task queue, status page, or history.

## Reconcile regulator invariance, critical scaling, closability, and archimedean completion in one source-native object

**Linked intuitions:** `MI-001-positivity-needs-a-sign-producing-global-operation`, `MI-010-source-forced-critical-gram-can-have-the-wrong-weil-orientation`, `MI-011-prime-ray-marginals-do-not-determine-mixed-prime-coupling`, `MI-012-automorphic-multiplicity-does-not-prevent-global-scalarization`, `MI-013-critical-sector-transport-needs-new-global-arithmetic-structure`.

WP-216--WP-260 show that canonical gauge/Toeplitz/Morita/KMS routes either miss the critical sector, scalarize the needed relation, or carry the wrong occupation factor even after the exact Mangoldt half-density appears in symmetric Fock space.

WP-261--WP-266 separate closability, programmability, critical normalization, and finite-part sign. Hamiltonian diagonal centering removes the tested sharp/Gibbs regulator ambiguity without a scalar counterterm: both connected families converge to the same trace-class index-one operator `K=aa^*-D`. But the source-canonical transport `e^{H/2}` that restores the Weil half-density on finite compressions produces a non-square-summable coherent coefficient vector and is globally nonclosable.

WP-267 closes the most direct category-change escape. Closing the critical first-layer scalar row as a linear relation yields

\[
\overline{\operatorname{graph}L_\sigma}
=\ell^2(\mathbb P)\times\mathbb C\Omega,
\qquad 0<\sigma\le1.
\]

The relation is maximally multivalued and its canonical regular operator part is zero. For the full Fock boundary map, only the bounded prime-separable higher-layer shift survives in the regular part. Thus standard relation closure deletes **exactly** the common-vacuum first layer that supplied the critical prime amplitudes and all mixed-prime Gram entries. At `\sigma=1/2`, the same classification kills the critical rank-one direction exposed by WP-266.

The missing theorem is therefore not “allow a singular boundary object.” A successful completion must introduce additional source-derived structure **before closure** so that bulk prime data remain coupled to the new boundary value, while simultaneously preserving regulator covariance, critical half-density, a valid global analytic category, and the missing Gamma/polar sector. Choosing a single-valued section after the relation has become the whole product is programmable extra structure, not a canonical repair.

## Treat coefficient sourcing, interaction, connected centering, singular closure, critical scaling, and Weil orientation separately

The Fock hierarchy solves coefficient sourcing and symmetric-sector criticality. Complete incidence supplies mixed-prime coupling. Hamiltonian diagonal centering solves the tested regulator finite-part mismatch below critical scale. WP-267 shows that standard linear-relation closure does not preserve the critical coherence hidden in the nonclosable row: it regularizes by discarding that coherence.

Being canonical and well defined below critical scale is therefore not the same as furnishing the critical Weil form, and merely enlarging the operator category is not enough. Future progress must identify a genuinely new global carrier or boundary law that retains the arithmetic coupling instead of treating the critical boundary coordinate as free multivalued data.