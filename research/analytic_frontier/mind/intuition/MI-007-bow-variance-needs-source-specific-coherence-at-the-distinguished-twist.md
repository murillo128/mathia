# MI-007 — Bow completion is an absolute, phase-sensitive endpoint-memory problem

**Evidence level:** supported by exact matched-control boundaries through ANF-183

Target-sized bow completion consumes a positive-real shifted correlation at the distinguished logarithmic twist, equivalently an adjacent-scale Fejer-energy defect. Prime-scale matched packets can store the required `a_X=sqrt(delta X log X)` endpoint memory while remaining invisible to broad source-blind tests.

ANF-180--ANF-182 identify a sharp local-complexity scale. Fixed finite continuous probe families collapse to rank one on a carrier-adapted band, and derivative jets remain asymptotically blind whenever `r_X a_X/K -> 0`. For sector-aligned target-sized packets, order `Theta(K/a_X)` must detect an `Omega(a_X)` discrepancy.

ANF-183 identifies the same resolution boundary in physical twist coordinates. After rephasing at the first active site, a dangerous packet of physical width `Theta(a_X)` can satisfy

\[
H_X(\tau)=H_X(0)+o(a_X)
\]

uniformly on every aperture `|tau|=o(X/a_X)`. Conversely, the symmetric three-point curvature

\[
C_X(\tau)=2H_X(0)-H_X(\tau)-H_X(-\tau)
\]

has size `Omega(a_X)` for some `|tau|=Theta(X/a_X)` for every packet in the sector-aligned control class. The `K` in the derivative threshold is therefore a coordinate-normalization cost: the physical reciprocal-width aperture is `X/a_X`.

The unresolved information is source-specific rather than another generic smoothness norm. A successful theorem can attack the signed Fejer shell directly, constrain actual prime/rough placement, or control a source-faithful three-height curvature with absolute error `o(a_X)` at physical separation `Theta(X/a_X)`. ANF-183 only proves that such an aperture is sufficient to discriminate the matched packet; it does not prove that the true source is estimable there.

**Boundary.** The sector alignment and carrier-selected placement belong to the matched control. Neither critical derivative order nor three-point twist curvature is presently an available arithmetic estimate for `Lambda-Lambda^sharp`.
