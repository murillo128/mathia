# MI-007 — Bow completion is a carrier-placement problem at a deterministic critical high-pass

**Evidence level:** supported by exact matched-control boundaries through [ANF-186](../../findings/ANF-186-prime-rough-source-marginals-still-permit-square-root-endpoint-memory-under-exact-carrier-placement.md). ANF-185 gives an exact deterministic critical detector for the sector-aligned matched-control class; ANF-186 shows that matching the natural prime/rough coefficient marginals, mean, diagonal scale and local absolute mass still does not exclude a dangerous packet. No current theorem supplies the required absolute estimate or joint-placement law for the actual prime-minus-Ramanujan residual.

Target-sized bow completion consumes a positive-real shifted correlation at the distinguished logarithmic twist, equivalently an adjacent-scale Fejer-energy defect. Prime-scale matched packets can store the required `a_X=sqrt(delta X log X)` endpoint memory while remaining invisible to broad source-blind tests.

ANF-180--ANF-183 identify the sharp local-complexity and physical-resolution scale. Fixed finite continuous probe families collapse to rank one on a carrier-adapted band, derivative jets remain blind below order `K/a_X`, and dangerous packets of width `Theta(a_X)` can remain flat throughout every twist aperture `o(X/a_X)`.

ANF-185 removes the adaptive sample. Triangular averaging over one fixed aperture, rephased only at the deterministic base `X`, gives the exact positive multiplier `W(y)=1-sinc^2(y/2)`. At `T=CX/a_X` it detects every sector-aligned target-sized packet, and the aperture average collapses exactly to one weighted residual sum at the original distinguished height. The weight has uniformly bounded total variation, so this representation does not hide a stronger source norm.

ANF-186 then asks whether realistic one-point source statistics themselves exclude the dangerous packet. They do not. A deterministic control can use exactly the source-sized signs `+A_X` and `-B_X` at the natural prime and rough-composite densities, match zero first moment and the `asymp log X` diagonal variance, satisfy a local absolute-mass envelope `O(H+log X)`, and still load endpoint charge `>=a_X` for `Theta(K)` prefixes. Its decisive freedom is that sign locations are chosen after reading the exact logarithmic carrier.

The reusable endpoint lesson is therefore sharper than “source-specific coherence is needed.” Once the reciprocal-width aperture is reached, generic packet discrimination is solved, and even realistic source marginals are insufficient. The missing resource is **arithmetic placement relative to the carrier**: a theorem controlling the joint law of prime/rough signs and the distinguished logarithmic phase, or directly the deterministic weighted residual / signed Fejer scale difference at absolute scale `a_X`.

The live alternatives are concrete: prove `o(a_X)` control of the ANF-185 weighted prime-minus-Ramanujan residual at `T~X/a_X`; prove the signed Fejer scale-difference estimate directly; or prove that actual arithmetic placement forbids the carrier-aligned two-sign configuration constructed in ANF-186.

**Boundary.** ANF-186 is a matched control, not a model of the actual primes. It does not show that the true residual realizes dangerous carrier alignment, and ANF-185 remains a discrimination theorem rather than the bow variance estimate. What is ruled out is deriving the missing estimate from coefficient alphabet, one-point densities, mean balance, diagonal variance or local absolute-mass bounds alone.