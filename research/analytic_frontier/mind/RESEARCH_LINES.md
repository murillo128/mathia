# Analytic-frontier research lines

This file holds the current mathematical questions suggested by the durable analytic-frontier intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether the surviving same-profile cascade is fatally non-excisable

**Linked intuition:** `MI-005-one-pair-collapse-is-curvature-seeded-before-higher-order-effects`.

ANF-134--ANF-143 remove raw depth, span, slack-spend, coefficient-spread, and fixed exponential zero-pinching as existential obstructions to a same-profile cascade. The live gate remains whether the positive late cloud carries source energy/coherence that is fatal for the near-extremizer or can always be excised as negligible mass while preserving the source objective.

## Control the signed endpoint-local moving-window correlation at the distinguished twist

**Linked intuition:** `MI-007-bow-variance-needs-source-specific-coherence-at-the-distinguished-twist`.

ANF-150--ANF-169 reduce bow variance to positive Fejer source energy minus an exact endpoint-completion square and then show that any target-sized completion forces one endpoint and one dyadic lag shell to carry a large positive real shifted correlation. ANF-170 rewrites that shell as a current coefficient paired with a preceding dyadic moving-window sum and derives a positive square-function witness `Q_H` by Cauchy--Schwarz.

ANF-171 identifies the sharp boundary of that positive relaxation. A constant-modulus geometric packet can make `A Q_H` live exactly at the ANF-170 danger scale and asymptotically saturate complex Cauchy--Schwarz while the real endpoint correlation is smaller by a vanishing factor because the moving window sits at a quarter-turn phase. The missing datum is therefore the real alignment

`alpha_H = Re S_H / sqrt(A Q_H)`,

not moving-window energy by itself. Large `Q_H` is a necessary shadow of danger, but it is not a source-faithful decision statistic at the target scale.

The live theorem is to control the signed quantity `Re S_H` for the actual twisted residual `r_X(n)n^{-iU}`, or to derive a source theorem that jointly controls its energy and real alignment strongly enough to make every dangerous dyadic shell impossible. Positive square-function estimates remain useful only when paired with an arithmetic phase/alignment mechanism; replacing the signed correlation by `Q_H` alone loses exactly the information exposed by ANF-171.

## Treat low-spectrum abundance, carrier resonance, polylogarithmic discrepancy, and phase-blind square energy as controls

The distinguished twist is a unitary gauge, low positive directions are abundant, the bare carrier fails at the endpoint threat scale, arbitrarily strong fixed-log long-interval cancellation can coexist with target-sized completion, and target-scale moving-window energy can coexist with negligible real completion through phase orthogonality. The remaining gain must use the fixed arithmetic amplitude-and-phase law or an equivalent source-faithful signed correlation, not another coefficient-generic positive proxy.
