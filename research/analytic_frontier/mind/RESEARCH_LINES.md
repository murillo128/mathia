# Analytic-frontier research lines

This file holds the current mathematical questions suggested by the durable analytic-frontier intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether the surviving same-profile cascade is fatally non-excisable

**Linked intuition:** `MI-005-one-pair-collapse-is-curvature-seeded-before-higher-order-effects`.

ANF-134--ANF-143 progressively remove raw depth, span, slack-spend, coefficient-spread, and fixed exponential zero-pinching as existential obstructions to a same-profile cascade. The live gate remains whether the positive late cloud carries source energy/coherence that is fatal for the near-extremizer or can always be excised as negligible mass while preserving the source objective.

## Convert bow source alignment into a Fejer-null-frequency correlation theorem

**Linked intuition:** `MI-007-bow-variance-needs-source-specific-coherence-at-the-distinguished-twist`.

ANF-150--ANF-154 reduce the bow to source placement in one fixed untwisted moving-window spectrum and identify the sharp first positive scale `Theta(K/N^2)`. ANF-155 shows that this low sector is already thick: on `N=mK`, `Omega(KJ)` positive directions occur below scale `O(J^2K/N^2)`, so dimension scarcity cannot obstruct cancellation. ANF-156 removes most of the edge-normalization opacity by dualizing the positive spectrum to a Fejer Toeplitz finite section plus a positive boundary correction of rank at most `2K-2` and trace `K-1`.

The live theorem is now directional and source-specific. Translate projection of the demodulated post-sieve innovation onto the true low singular sector into a concrete twisted prime/Ramanujan correlation statement, including the explicit prefix/suffix boundary tax. Either force enough source mass into that sector at the required collapsing scale or prove that the arithmetic source necessarily pays positive Fejer/boundary energy.

## Treat low-spectrum abundance, twist-dependent spectra, and approximate periodicity as controls

The distinguished twist is a unitary gauge, the low positive spectrum is abundant rather than exceptional, and bulk positive eigenvalue counts are Fejer-controlled up to `O(K)` boundary-sensitive directions when `K=o(N)`. None of this implies projector alignment of the actual prime source. Exact periodic-kernel rigidity only becomes global at the much stronger `q=o(K/N^2)` scale, and even Toeplitz counting information does not determine the bottom `O(K)` projectors. The missing arithmetic datum is source placement, not another geometry-only spectral count.
