# Analytic-frontier research lines

This file holds the current mathematical questions suggested by the durable analytic-frontier intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Resolve the compact Montgomery--Taylor five-point separation/mismatch remainder

**Linked intuitions:** `MI-001-global-conjugation-geometry-carries-horizontal-information`, `MI-002-finite-pair-enrichment-dualizes-to-one-signed-profile`, and `MI-004-complete-scalar-control-is-diffraction-realizability`.

ANF-054--ANF-057 close the equal-height case and a support-free relative-height tube under the explicit curvature gate. ANF-058 retains the horizontal phase and proves that any zero must satisfy `K_J(d)<-K_0/3`. ANF-059 then evaluates that gate for the fixed Montgomery--Taylor profile using its exact curvature transform and an outward-rounded finite certificate.

The remaining scalar zero set is now confined by

`q>0.1409` and `0.545<|d|<1.01`.

The decisive scalar calculation is therefore finite and profile-specific: prove the exact Montgomery--Taylor five-point form positive on that remaining compact region, or produce a genuine zero there. Further support/notch tuning or horizontal-free coefficient optimization is not a distinct mechanism.

## Seek information beyond scalar pair/five-point realizability only if the compact remainder survives

**Linked intuitions:** `MI-001-global-conjugation-geometry-carries-horizontal-information` and `MI-003-ordered-local-consistency-escapes-pair-scalarization`.

Pairwise and finite symmetric scalar controls repeatedly compress horizontal geometry into one signed diffraction profile. If the exact five-point compact remainder is not closed within that category, a new carrier must retain ordered or multi-point horizontal consistency before scalarization and must be tested against the exact ANF-058--ANF-059 curvature-correlation control.

## Distinguish local ordered consistency from larger block size

**Linked intuitions:** `MI-003-ordered-local-consistency-escapes-pair-scalarization`.

Increasing a scalar block without preserving which horizontal relations are shared can reproduce the same realizability geometry at higher cost. A useful extension must identify a compatibility constraint that cannot be reconstructed from the existing pair/five-point scalar data, and a decisive matched control should preserve all existing correlations while varying that ordered relation.

## Preserve horizontal information at the microscopic scale used by the final theorem

**Linked intuitions:** `MI-001-global-conjugation-geometry-carries-horizontal-information`.

ANF-058--ANF-059 confirm that horizontal separation is not decorative: retaining the phase eliminates all heights outside one narrow negative-curvature lobe. Any handoff to another line must preserve the horizontal scale on which that correlation changes; an averaged or fixed-scale certificate that loses it is not equivalent evidence.
