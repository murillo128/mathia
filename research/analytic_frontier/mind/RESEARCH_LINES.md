# Analytic-frontier research lines

This file holds the current mathematical questions suggested by the durable analytic-frontier intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether the surviving same-profile cascade is fatally non-excisable

**Linked intuition:** `MI-005-one-pair-collapse-is-curvature-seeded-before-higher-order-effects`.

ANF-134--ANF-143 remove raw depth, span, slack-spend, coefficient-spread, and fixed exponential zero-pinching as existential obstructions to a same-profile cascade. The live gate remains whether the positive late cloud carries source energy/coherence that is fatal for the near-extremizer or can always be excised as negligible mass while preserving the source objective.

## Control the endpoint Fejer scale-slope defect with the actual prime/rough placement law

**Linked intuition:** `MI-007-bow-variance-needs-source-specific-coherence-at-the-distinguished-twist`.

ANF-150--ANF-172 reduce bow variance to a phase-sensitive endpoint problem. Target-sized completion forces a large positive real dyadic shifted correlation, and that signed shell is exactly a dyadic defect in the scale slope of a positive endpoint Fejer family. A single moving-window energy is only a necessary shadow because phase orthogonality can make the energy critical while killing the real completion.

ANF-173 removes the earlier upper-range artifact: by raising the fixed Taylor degree, available exact-twist discorrelation extends through the full endpoint horizon `X^(5/8+eta) <= H <= K`. It also shows that arbitrary fixed-log savings, qualitative fixed-order Gowers uniformity, and bounded-test relative decorrelation can coexist with target-sized endpoint charge through a tiny carrier-aligned drift.

ANF-174 shows that the tiny-amplitude freedom is not essential. A vanishing-density subset of a coarse grid, chosen only where the **exact** logarithmic carrier lies in one angular sector, can use amplitudes restricted to `{0,1}` to store charge `asymp sqrt(X log X)` inside `o(K)` sites and then hold it for `Theta(K)` prefixes. Every bounded-test correlation on long intervals is nevertheless power-small, and every fixed-order Gowers norm is power-small. Hence coefficient quantization, a nonzero amplitude floor, nonnegative signs, sparsity, and generic relative pseudorandomness still do not anchor the endpoint.

ANF-175 removes the remaining one-point-scale mismatch in that control. Atoms of size `log X` on candidate sites spaced by `log X` obey a local total-variation envelope `O(H+log X)`, matching the natural prime-sized magnitude/density scale. Such an envelope forces any critical charge `sqrt(X log X)` to require physical loading length `Omega(sqrt(X log X))`, and exact carrier-sector selection attains that lower bound. The whole packet then has only `O(sqrt(X log X))` total mass, so on every interval with `H>=X^(5/8+eta)` it satisfies correlation `o(H log^{-A} X)` against **every bounded test**, for every fixed `A`, while still forcing bow-scale completion and the target Fejer defect. Thus the placement obstruction is sharply localized at the near-square-root loading scale below the current all-interval activation barrier.

The live theorem must therefore control the exact Fejer scale-slope defect, or a source-faithful equivalent, using the **joint placement of the actual prime/rough residual mass relative to the distinguished carrier**. Quantitatively, one viable route is an all-interval exact-carrier estimate reaching `H asymp sqrt(X log X)` with a genuine relative saving; another is an absolute carrier-sector/prefix discrepancy `o(sqrt(X log X))` even if the theorem remains above `X^(5/8+eta)`; a third is direct control of the signed dyadic/Fejer statistic. Further fixed-log improvement that leaves the activation scale at `5/8` cannot detect the ANF-175 matched control.

## Treat low-spectrum abundance, carrier resonance, polylogarithmic discrepancy, phase-blind uniformity, and amplitude quantization as controls

The distinguished twist is a unitary gauge, low positive directions are abundant, the bare carrier fails at the endpoint threat scale, arbitrarily strong fixed-log long-interval cancellation can coexist with target-sized completion, and target-scale moving-window energy can coexist with negligible real completion through phase orthogonality. ANF-173 adds that full-endpoint fixed-order relative uniformity can coexist with a dangerous accumulated charge. ANF-174 adds that even a nonnegative `{0,1}` amplitude alphabet and vanishing support density can coexist with the same dangerous memory while all long-interval bounded-test and fixed-order Gowers statistics decay by powers. ANF-175 further matches prime-sized atom magnitude and density and shows that the dangerous packet can be fully assembled on the sharp `sqrt(X log X)` scale while its total mass is already below every fixed-log error allowance on the current `H>=X^(5/8+eta)` range. A viable source theorem must constrain the absolute signed/scale-difference statistic actually consumed downstream, or the arithmetic placement law at this near-square-root loading scale that forces it small.