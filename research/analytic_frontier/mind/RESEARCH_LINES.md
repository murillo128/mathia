# Analytic-frontier research lines

This file holds the current mathematical questions suggested by the durable analytic-frontier intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether the surviving same-profile cascade is fatally non-excisable

**Linked intuition:** `MI-005-one-pair-collapse-is-curvature-seeded-before-higher-order-effects`.

ANF-134--ANF-143 remove raw depth, span, slack-spend, coefficient-spread, and fixed exponential zero-pinching as existential obstructions to a same-profile cascade. The live gate remains whether the positive late cloud carries source energy/coherence that is fatal for the near-extremizer or can always be excised as negligible mass while preserving the source objective.

## Control endpoint-local shifted correlation at the distinguished twist

**Linked intuition:** `MI-007-bow-variance-needs-source-specific-coherence-at-the-distinguished-twist`.

ANF-150--ANF-157 reduce bow variance to positive Fejer source energy minus an exact endpoint-completion square. ANF-158--ANF-165 push any dangerous completion beyond every fixed polylogarithmic enlargement of `R_TV=(X K log X)^(1/3)`, while ANF-166--ANF-168 show that arbitrary fixed-log all-interval cancellation, two-sided local activity budgets, exact centering, real pre-demodulated coefficients, and the exact logarithmic carrier still do not anchor the absolute endpoint primitive.

ANF-169 converts that surviving obstruction into an exact source statistic. The endpoint square equals a target-negligible edge diagonal plus a triangular sum of endpoint-local shifted products. Hence any completion `>= delta X K log X` forces one endpoint and one dyadic lag block to carry positive normalized correlation `gg_delta X log X/log K`. For the actual source these products are

\[
(\vartheta-Q_R)(n)(\vartheta-Q_R)(n+h)
\left(\frac{n+h}{n}\right)^{-iU},
\]

with the prime--prime, prime--Ramanujan, Ramanujan--prime, and Ramanujan--Ramanujan signs fixed exactly as in ANF-149.

The live theorem is therefore to rule out those positive dyadic endpoint blocks, ideally by estimating the four signed pieces together rather than separately. A uniform bound `o(X log X/log K)` for every endpoint/dyadic normalized block is sufficient to make endpoint completion `o(XK log X)`. Generic interval discrepancy or carrier geometry is progress only if it can be converted into this bilinear endpoint currency.

## Treat low-spectrum abundance, carrier resonance, and polylogarithmic discrepancy as controls

The distinguished twist is a unitary gauge, low positive directions are abundant, the bare carrier fails at the endpoint threat scale, and even arbitrarily strong fixed-log long-interval cancellation can coexist with target-sized completion in matched controls. The remaining gain must use the fixed arithmetic amplitude law or an equivalent source-faithful correlation estimate, not another coefficient-generic reformulation of the same increment geometry.
