# Analytic-frontier research lines

This file holds the current mathematical questions suggested by the durable analytic-frontier intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether the surviving same-profile cascade is fatally non-excisable

**Linked intuition:** `MI-005-one-pair-collapse-is-curvature-seeded-before-higher-order-effects`.

ANF-134--ANF-143 remove raw depth, span, slack-spend, coefficient-spread, and fixed exponential zero-pinching as existential obstructions to a same-profile cascade. The live gate remains whether the positive late cloud carries source energy/coherence that is fatal for the near-extremizer or can always be excised as negligible mass while preserving the source objective.

## Control the square-root-scale endpoint-completion currency in the bow identity

**Linked intuition:** `MI-007-bow-variance-needs-source-specific-coherence-at-the-distinguished-twist`.

ANF-150--ANF-157 reduce bow variance to positive Fejer source energy minus an exact endpoint-completion square. ANF-158 localizes the threat to cumulative residual modes, while ANF-159--ANF-161 show that the bare parity-quantized carrier cannot produce the required endpoint spike.

ANF-162 now uses the actual prime-source residual `r_X=\vartheta-Q_R`: Brun--Titchmarsh plus the counterterm gives a local second-moment bound on every interval of length at least `sqrt(X/log X)`. This sharpens the dangerous cumulative scale to a physical wavelength `asymp sqrt X`. A target-sized completion then forces a twisted prefix/suffix of size `Omega(sqrt(X log X))` spread across `Omega(sqrt X)` sites.

The live theorem is therefore exact and source-side: rule out that square-root-scale cumulative concentration for the arithmetic residual, or prove a positive combined Fejer/completion inequality in the same normalization. Further carrier resonance or generic low-spectrum counting does not address the remaining obligation.

## Treat low-spectrum abundance and carrier resonance as controls

The distinguished twist is a unitary gauge, low positive directions are abundant, and the bare carrier has already failed at the endpoint threat scale. ANF-162 also shows that the current square-root cutoff is sharp from its available local `L^2` and pointwise inputs alone. Any improvement must add arithmetic cancellation/coherence beyond those generic bounds rather than repackage the same geometry.