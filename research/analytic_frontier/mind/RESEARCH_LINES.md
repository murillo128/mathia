# Analytic-frontier research lines

This file holds the current mathematical questions suggested by the durable analytic-frontier intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether the surviving same-profile cascade is fatally non-excisable

**Linked intuition:** `MI-005-one-pair-collapse-is-curvature-seeded-before-higher-order-effects`.

ANF-134--ANF-143 remove raw depth, span, slack-spend, coefficient-spread, and fixed exponential zero-pinching as existential obstructions to a same-profile cascade. The live gate remains whether the positive late cloud carries source energy/coherence that is fatal for the near-extremizer or can always be excised as negligible mass while preserving the source objective.

## Control absolute endpoint charge, not only long-interval increments

**Linked intuition:** `MI-007-bow-variance-needs-source-specific-coherence-at-the-distinguished-twist`.

ANF-150--ANF-157 reduce bow variance to positive Fejer source energy minus an exact endpoint-completion square. ANF-158--ANF-165 progressively move the dangerous completion from square-root localization to the cubic total-variation scale `R_TV=(X K log X)^(1/3)` and then beyond every fixed polylogarithmic multiple of that scale under the strongest current all-interval source controls.

ANF-166 identifies the exact remaining information gap. A matched control can satisfy the diagonal normalization, pointwise/local mass bounds, exact total centering, and stronger-than-any-fixed-log cancellation on every interval above `H_0`, while a short endpoint charge leaves a primitive plateau of size `asymp sqrt(X log X)` and hence target-sized endpoint square energy. An interval bound `|sum_I b_n| <= rho_X |I|` can therefore exclude this mechanism by itself only if `rho_X H_0=o(sqrt(X log X))`. At the current `H_0=X^(5/8+eta)` this requires a genuine power saving, not arbitrary logarithmic improvement.

The live theorem is now sharply source-specific: anchor the absolute endpoint primitive below the `sqrt(X log X)` scale, lower the cancellation activation essentially to square-root length, control the primitive square function directly, or prove a prime/rough structural law that forbids persistent endpoint charge. Merely strengthening relative long-interval discrepancy without crossing this absolute-prefix gate cannot close the bow obstruction.

## Treat low-spectrum abundance, carrier resonance, and polylogarithmic discrepancy as controls

The distinguished twist is a unitary gauge, low positive directions are abundant, the bare carrier fails at the endpoint threat scale, and even arbitrarily strong fixed-log long-interval cancellation can coexist with target-sized completion in matched controls. Any further gain must control the absolute primitive or a source-faithful equivalent rather than repackage the same generic increment geometry.
