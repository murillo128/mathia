# RE-055 — all-layer gap synchronization is reproducible by one PNT-dense generalized-prime source

**Status:** `EXACT-DERIVED + NEGATIVE/OBSTRUCTION + GENERALIZED-PRIME-COMMON-SOURCE-CONTROL + ALL-LAYER-EVENT-FREE-CHAMBERS + PNT-SCALE-DENSITY + SHARP-GAP-EXPONENT + ADAPTIVE-ONE-CELL-FAN + COUNTEREXAMPLE-NARROWING + PRIOR-ART-BOUNDED`. `RE-054` shows that a genuine adaptive threshold cell is much more than one first-layer prime gap: because the same ordinary primes generate every CA layer, the selected support chamber pulls back to synchronized prime-free intervals near

\[
Y,\qquad (2Y)^{1/2},\qquad (3Y)^{1/3},\ldots .
\]

That common-source synchronization is genuine arithmetic structure, but **the layer-event synchronization by itself is still not ordinary-prime rigid**. The generalized first-layer control of `RE-053` can be upgraded to one single increasing real label sequence which drives *every* prime-power layer map, has PNT-scale counting and the same prescribed worst-case gap exponent, and nevertheless contains infinitely many one-cell adaptive blocks whose support chamber is free of generalized events in **all layers simultaneously**.

More precisely, fix

\[
\frac12<\theta<1,
\qquad
J=[b_0,b_1]\Subset(0,1/2),
\qquad
b_0>1-\theta,
\qquad c_0>0.
\tag{1}
\]

There exists one increasing sequence of real labels

\[
\mathcal Q=\{q_1<q_2<\cdots\},
\tag{2}
\]

such that

\[
\boxed{
\Pi_{\mathcal Q}(x)\sim\frac{x}{\log x}}
\tag{3}
\]

and every sufficiently late consecutive gap satisfies

\[
\boxed{
q_{n+1}-q_n=o(q_n^\theta).
}
\tag{4}
\]

For every label `q` and layer `j>=1`, use the **same** CA-form increment

\[
\lambda_j(q)
:=
\log\!\left(1+\frac1{q+q^2+\cdots+q^j}\right)
\tag{5}
\]

and define its layer-event coordinate by

\[
\boxed{
\eta_j(q)\log\eta_j(q)
=
\frac{\log q}{\lambda_j(q)}.
}
\tag{6}
\]

Then there are arbitrarily large synthetic blocks of the exact one-cell type constructed in `RE-053`, with intrinsic scale `T`, left-endpoint amplitude `A_{b_0}=c_0`, and first-layer boundary labels `q_-<q_+` belonging to `\mathcal Q`, such that

\[
\mathcal H_T
:=
(\eta_1(q_-),\eta_1(q_+))
\tag{7}
\]

contains **no event from any layer generated by any label in `\mathcal Q`**:

\[
\boxed{
\eta_j(q)\notin\mathcal H_T
\qquad
(q\in\mathcal Q,\ j\ge1).
}
\tag{8}
\]

The local adaptive amplitudes, strict supporting concavity, exact selector equation, exact first-layer transition masses, and exact first-layer support slopes are unchanged from `RE-053`. In particular the whole threshold interval `J` is carried by one exposed state, while the two support boundaries are genuine consecutive events in the **full generalized layer event staircase**, not merely in its first layer.

Consequently, for every fixed `j`, the inverse chamber

\[
\mathcal Q_{T,j}:=\eta_j^{-1}(\mathcal H_T)
\tag{9}
\]

is label-free and has the same asymptotic scale as the physical inverse gaps of `RE-054`:

\[
\boxed{
\mathcal Q_{T,j}\cap\mathcal Q=\varnothing,
}
\tag{10}
\]

\[
\boxed{
q=(jT)^{1/j}(1+o_j(1))
\quad(q\in\mathcal Q_{T,j}),
}
\tag{11}
\]

and

\[
\boxed{
|\mathcal Q_{T,j}|
=
\left(
 c_0(b_1-b_0)j^{-(j-1)/j}+o_j(1)
\right)
T^{1/j-b_0}\log T.
}
\tag{12}
\]

Thus the square-root companion gap, every other fixed-layer companion, and even the stronger fact that the complete support chamber is event-free across the whole generalized layer tower can coexist with (3)--(4) whenever `b_0>1-theta`.

The conclusion is a sharp information boundary for `RE-054`. It **does not** construct a Robin counterexample or a generalized CA integer. The synthetic intrinsic state is not asserted to equal the global cumulative event mass of `\mathcal Q`, and `\mathcal Q` is not required to generate the ordinary standard/reciprocal prime races or an Euler product with the arithmetic of `zeta`. What it does show is that

\[
\boxed{
\text{PNT density}
+
\text{one common label source for every CA layer}
+
\text{all-layer event-free support chambers}
+
\text{a }\theta\text{-gap ceiling}
}
\]

still does **not** obstruct a bounded one-cell threshold fan on the side `b_0>1-theta`. Any continuation through the near-critical band must therefore use source information beyond synchronized layer-event placement: global accumulated CA mass, integer-prime arithmetic, the Euler-product height, or the same-label standard/reciprocal race coupling of `RE-040`.

## 1. The `RE-053` one-cell chamber has the required sub-`theta` width

Start with the one-cell construction of `RE-053`. At intrinsic scale `T`, it gives three local states with the middle state exposed for all `b in (b_0,b_1)` and two exact generalized first-layer labels `q_-<q_+`. Their transitions satisfy

\[
\Delta Y=\log q,
\qquad
\Delta F=\log(1+q^{-1}),
\qquad
\frac{\Delta F}{\Delta Y}
=
\frac{\log(1+q^{-1})}{\log q},
\tag{13}
\]

while the first-layer event coordinate is exactly (6) with `j=1`.

`RE-053` proves

\[
q_+-q_-
=
\bigl(c_0(b_1-b_0)+o(1)\bigr)
T^{1-b_0}\log T
\tag{14}
\]

and

\[
\eta_1(q)=q+\frac12+O((\log q)^{-1}).
\tag{15}
\]

Hence the full first-layer support chamber has width

\[
\boxed{
W_T:=|\mathcal H_T|
=
\bigl(c_0(b_1-b_0)+o(1)\bigr)
T^{1-b_0}\log T.
}
\tag{16}
\]

The same construction gives `Y=T+O(log T)`, `a=(c_0+o(1))T^{-b_0}`, and the adaptive selector displacement

\[
Z_b-Y
=(b+o_J(1))aY\log Y
=O_J(T^{1-b_0}\log T).
\tag{17}
\]

The secant--tangent slack at the two first-layer boundaries is `O(T^-2)` in slope coordinates, hence only `O(log T)` after applying the inverse of `g(x)=1/(x log x)`. Therefore both support endpoints lie in

\[
\boxed{
T+O_J(T^{1-b_0}\log T).
}
\tag{18}
\]

This localization will let us place the chamber away from all deep events generated by labels that we do not wish to modify.

The exponent condition in (1) already implies that its first-layer width is compatible with the prescribed metric ceiling:

\[
\frac{W_T}{T^\theta}
\asymp
T^{1-b_0-\theta}\log T
\longrightarrow0.
\tag{19}
\]

The point of the remaining construction is to preserve this compatibility **simultaneously in every layer using one common label set**.

## 2. Deep events can be quarantined below a subpower base cutoff

For real `q>1`, set

\[
S_j(q):=q+q^2+\cdots+q^j.
\tag{20}
\]

Because

\[
\lambda_j(q)
=\log(1+S_j(q)^{-1})
\le S_j(q)^{-1}
\le q^{-j},
\tag{21}
\]

equation (6) gives the exact lower bound

\[
\boxed{
\eta_j(q)\log\eta_j(q)
\ge q^j\log q.
}
\tag{22}
\]

Fix a finite depth `m` satisfying

\[
\frac1{m+1}<b_0.
\tag{23}
\]

At a large dyadic trial scale `X`, define `B_X>1` by

\[
B_X^{m+1}\log B_X
=10X\log X.
\tag{24}
\]

Then

\[
B_X=X^{1/(m+1)+o(1)}.
\tag{25}
\]

If `q>B_X` and `j>=m+1`, equations (22)--(24) imply

\[
\eta_j(q)>3X
\tag{26}
\]

for all sufficiently large `X`. Therefore **any** layer event lying in the candidate physical window `[X/2,3X]` is of one of two types:

1. it comes from a label `q<=B_X`; or
2. it has layer depth `j<=m`.

The first class is finite and sparse enough to avoid by moving the block scale. Indeed, if `q` is bounded below by any fixed number greater than one and `eta_j(q)<=3X`, (22) implies `j=O(log X)`. A PNT-scale real mesh has `O(B_X)` labels below `B_X`, so there are at most

\[
O(B_X\log X)
\tag{27}
\]

protected event coordinates in `[X/2,3X]`.

By (18), a chamber generated from any `T in [X,2X]` lies within distance

\[
O_J(X^{1-b_0}\log X)
\tag{28}
\]

of `T`. Excluding that neighborhood around every protected event removes total length at most

\[
O_J\!\left(
B_X X^{1-b_0}(\log X)^2
\right)
=
o(X),
\tag{29}
\]

because (23)--(25) give `1/(m+1)-b_0<0`. Hence there exists a `T in [X,2X]` for which the complete chamber (7) avoids every event generated by every protected label `q<=B_X`, in every layer.

This is the key diagonal freedom. Deep layers need not be deleted. Their possible bases are so small relative to `T` that their event coordinates form a finite set of points which occupies `o(X)` total forbidden room after thickening by the whole adaptive chamber width.

## 3. Delete only the finitely many macroscopic inverse chambers

Choose such a safe `T` and perform the exact local construction of Section 1. Keep the two new boundary labels `q_-` and `q_+`. For every existing label `q>B_X`, delete `q` if

\[
\eta_j(q)\in\mathcal H_T
\tag{30}
\]

for some `1<=j<=m`.

No deeper deletion is needed. By (26), a label above `B_X` cannot have a layer `j>=m+1` event in the chamber. Labels below `B_X` were protected by the choice of `T`. Therefore after the finite deletion step,

\[
\boxed{
\eta_j(q)\notin\mathcal H_T
\quad
\text{for every retained label }q
\text{ and every }j\ge1.
}
\tag{31}
\]

The inserted first-layer events `eta_1(q_-)` and `eta_1(q_+)` are the chamber endpoints, and their higher-layer events lie far to the right. Thus they are consecutive boundary events in the **entire** retained layer-event multiset.

For each fixed `j<=m`, eventual monotonicity of `eta_j` makes the deleted set in that layer one real interval, namely `eta_j^{-1}(H_T)`. The fixed-layer Jacobian already proved in `RE-054` gives

\[
q=(jT)^{1/j}(1+o_j(1))
\tag{32}
\]

throughout that interval and

\[
|\eta_j^{-1}(\mathcal H_T)|
=
\left(j^{-(j-1)/j}+o_j(1)\right)
T^{-(j-1)/j}W_T.
\tag{33}
\]

Inserting (16) gives exactly (12).

The same conclusion (10) actually holds for **every** layer, including `j>m`, because (31) is an all-layer statement. The asymptotic formula (12) is asserted only one fixed layer at a time, where the `RE-054` inverse-event expansion applies directly.

## 4. Every synchronized hole remains below the same `theta` gap ceiling

Let

\[
x_{T,j}:=(jT)^{1/j}.
\tag{34}
\]

For each fixed `j`, equations (11)--(12) give

\[
\frac{|\mathcal Q_{T,j}|}{x_{T,j}^\theta}
\ll_{J,j}
T^{(1-\theta)/j-b_0}\log T.
\tag{35}
\]

Since

\[
\frac{1-\theta}{j}-b_0
\le
1-\theta-b_0<0,
\tag{36}
\]

we obtain

\[
\boxed{
|\mathcal Q_{T,j}|
=o(x_{T,j}^\theta)
}
\tag{37}
\]

for every fixed layer. The worst exponent is the first layer; higher-layer synchronized holes are *easier*, not harder, to fit under the same local `theta` ceiling. This is the metric reason that merely adding finitely many synchronized layer gaps cannot cross the `RE-053` boundary.

The statement can be made global with one label sequence. Start from the elementary real mesh

\[
r_n=n\log n
\tag{38}
\]

for sufficiently large `n`. It satisfies

\[
\Pi_R(x)\sim\frac{x}{\log x},
\qquad
r_{n+1}-r_n=O(\log r_n).
\tag{39}
\]

Perform the chamber surgery above inductively. At stage `k`, take a depth `m_k->infinity` with `1/(m_k+1)<b_0`, and choose the trial scale so large that

\[
B_{X_k}>2T_{k-1},
\tag{40}
\]

all fixed-layer asymptotics through depth `m_k` have relative error below `1/k`, and every newly enlarged mesh gap is below `x^theta/k` at its own local scale. This is possible because each stage has finite depth and the ratios in (35) tend to zero.

Condition (40) makes the base-label ranges modified at distinct stages disjoint. Later stages never delete an earlier boundary label and cannot create a new event inside an earlier chamber, because all later labels lie to its right and `eta_j(q)>q` for sufficiently large labels.

At a layer-`j` hole, the number of deleted baseline mesh points is

\[
O\!\left(
\frac{|\mathcal Q_{T,j}|}{\log x_{T,j}}+1
\right).
\tag{41}
\]

Relative to the baseline count `x_{T,j}/log x_{T,j}`, this is

\[
O\!\left(
\frac{|\mathcal Q_{T,j}|}{x_{T,j}}
\right)
=
O_{J,j}(T^{-b_0}\log T)
=o(1).
\tag{42}
\]

Because each stage contains only finitely many such holes and its entire affected base range lies beyond the previous stage, the scales can be chosen diagonally so that the cumulative counting perturbation is `o(x/log x)` at every stage boundary and throughout each affected range. The two inserted boundary labels per block are negligible. This proves (3).

Likewise, outside the deleted inverse chambers the original mesh gaps are logarithmic. Inside a modified region, a new consecutive-label gap is bounded by one inverse-chamber length plus neighboring logarithmic mesh gaps. By the stage choice following (35), its ratio to `x^theta` is at most `O(1/k)`. Since the affected ranges escape to infinity and `k->infinity`, this proves (4).

The construction therefore uses **one and the same final label sequence** for every layer and every selected block. It is not a collection of separately tuned layerwise controls.

## 5. What this control matches, and what it deliberately does not

The control matches substantially more arithmetic structure than `RE-053`:

- the exact adaptive one-cell amplitude law and left-endpoint normalization;
- the small positive-height regime;
- the exact first-layer CA mass, abundance gain, support slope, and event-coordinate formulas at both boundaries;
- one common real label set driving the exact layer increment (5) and event map (6) for **all** `j`;
- adjacency of the two boundary events in the complete generalized layer-event staircase;
- consequently, the entire synchronized inverse-gap family of `RE-054`;
- PNT-scale label density and a global `o(x^theta)` consecutive-label gap ceiling.

What remains unmatched is equally important. The labels are real, not ordinary integer primes. The intrinsic synthetic coordinate `Y` is not asserted to equal the **global accumulated** layer mass

\[
\sum_{q,j:\eta_j(q)\le Z}\log q
\tag{43}
\]

of the final sequence. Nor is the synthetic height asserted to arise from a global Euler product over `\mathcal Q`, and no generalized zeta function is required to reproduce the standard/reciprocal prime-race coordinates of `RE-040`. Thus this is not a generalized CA counterexample and not a model of false RH.

Those exclusions identify the remaining source information rather than weakening the negative conclusion. `RE-054` showed that first-layer-only controls missed a real common-source fact. The present construction shows that **common-source layer-event placement, even in the stronger all-layer event-free form, is still synthetically reproducible**. The discriminator has to involve how those labels accumulate into the state or into the analytic prime races, not merely where their CA-form layer events occur.

## 6. Stress tests, prior-art boundary, and research consequence

The construction does not use an independence heuristic. The deep-event avoidance in Section 2 is deterministic and depends only on the exact lower bound (22). The surgery does not move the two boundary labels, so it does not perturb the exact local equations inherited from `RE-053`. Sparse deletion does not manufacture the PNT asymptotic by assumption: equations (41)--(42), together with separated block scales, directly preserve the elementary mesh count.

A falsification of the stated obstruction would have to identify a property used by a proposed proof that is **not** preserved above. Requiring the actual integers `2,3,5,...`, requiring the global accumulated CA identity `Y=Phi(Z)`, requiring the ordinary Euler product, or using the joint standard/reciprocal prime races are valid ways to leave the control class. Rephrasing the same support chamber as several inverse gaps is not.

The literature boundary is conservative. Real generalized-prime systems and sparse perturbations of such systems are classical Beurling-prime territory; no novelty is claimed for the existence of real prime-like sequences with PNT-scale counting. Mantovanelli's 2026 work is the closest current source for the genuine ordinary-prime layer-event staircase. A targeted search of Beurling/generalized-prime perturbations, CA prime-layer work, and simultaneous short-interval/gap literature found no source constructing this **adaptive one-cell, all-layer-event-free matched control** or identifying it as the obstruction boundary for a Robin threshold fan. No external theorem is load-bearing here, so `SOURCES.md` requires no addition.

The research consequence is negative but sharp. `RE-054` correctly identified synchronized higher-layer gaps as arithmetic structure absent from the first-layer control. `RE-055` now shows that this structure, even upgraded to a complete event-free chamber generated by one common PNT-dense label source, still does not force fan fragmentation when `b_0>1-theta`. Therefore the near-critical false-RH band cannot be attacked by stacking more CA-layer gap invoices alone.

The next useful theorem has to spend a different currency: an exact relation between the adaptive chamber and the **global accumulated** prime-layer mass, an integer-prime restriction that generalized real labels cannot reproduce, or the same-source standard/reciprocal race relation of `RE-040` at a resolution strong enough to interact with the chamber. That is now a narrower target than the generic “exploit higher layers” continuation left open by `RE-054`.