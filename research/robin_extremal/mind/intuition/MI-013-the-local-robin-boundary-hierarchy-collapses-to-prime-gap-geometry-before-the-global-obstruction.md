# MI-013 — The local Robin boundary hierarchy collapses to prime-gap geometry before the global obstruction

**Evidence level:** supported by RE-090--RE-102 on sufficiently late false-RH compact threshold fans above `Theta_*=265657/307200`. This does not give a global RH frontier or eliminate higher-layer effects away from the two adjacent local support boundaries.

RE-101 closes the last mixed first/depth-two sampled-host estimate below the previous `173/200` barrier. RE-102 then recomputes the entire local support-boundary decomposition rather than continuing to sharpen that channel.

Above `Theta_*`, outside a threshold set of vanishing measure, both adjacent physical support boundaries of every surviving selected cell contain first-layer CA events. Pulling those boundaries back through the first-layer event map gives one full interval between consecutive ordinary primes, with the chamber width equal to the corresponding prime gap up to the deterministic event-map error. The deeper-boundary hierarchy is therefore exhausted at the local cell boundary.

This changes the remaining currency. Local boundary depth is no longer where a stronger analytic estimate should be spent: the surviving chamber geometry is already ordinary-prime-gap geometry. Any further RH-sensitive information must enter through **nonlocal/source-conditioned assembly** of selected states—higher-layer ties or switches across the fan, correlations among successive cells, selector constraints, or another mechanism not determined by the two local boundary coordinates alone.

The useful diagnostic is to separate a local source map from the global selected-state path. Once both adjacent boundaries have first-layer contact, improving another depth-two or depth-three host count cannot change the local geometry. A new theorem must explain how the ordinary-prime-gap chambers are assembled or weighted by the full CA source.

**Boundary.** Two-sided first-layer contact permits higher-layer ties at the endpoints and does not say the selected fan is first-layer-only. The result closes the current local boundary-depth decomposition, not the global Robin criterion.