# Robin-extremal research lines

This file holds the current mathematical questions suggested by the durable robin-extremal intuitions. It is not a roadmap, task queue, status page, or history.

## In the attained finite-edge branch, rule out cancelling-phase near-edge companions or the diffuse cloud

**Linked intuitions:** `MI-001-robin-failure-requires-self-tangent-half-mass-event-gaps`, `MI-002-ordinary-sigma-rejects-composite-substitute-atoms-at-every-fixed-ca-layer`, `MI-003-fixed-threshold-cells-freeze-prime-source-data`, `MI-004-higher-layer-ca-switches-carry-quantized-residual-charge`, `MI-005-matched-first-layer-packets-conserve-the-endpoint-prime-multiset`, `MI-006-first-layer-timing-is-large-in-transport-but-summable-in-robin-metric`, `MI-007-matched-packets-convert-selector-dilation-into-chebyshev-underdensity`, `MI-008-reciprocal-mertens-recovery-forces-corrected-chebyshev-growth`, `MI-009-smooth-dilation-front-is-not-the-obstruction`, `MI-010-deep-support-boundaries-have-vanishing-threshold-measure`, `MI-011-depth-two-short-interval-elimination-is-governed-by-an-exceptional-mass-max-law`, `MI-012-source-conditioned-bad-host-sparsity-converts-directly-into-a-depth-two-frontier-gain`, `MI-013-the-local-robin-boundary-hierarchy-collapses-to-prime-gap-geometry-before-the-global-obstruction`, `MI-014-the-last-local-endpoint-tie-is-a-four-exponentials-boundary`, `MI-015-surviving-robin-fans-pay-near-square-root-complexity-or-exponential-stretch`, `MI-016-positive-robin-peaks-force-a-larger-normalization-corridor-before-recurrence`, `MI-017-a-finite-zero-edge-upgrades-robin-recovery-to-a-power-corridor`, `MI-018-an-isolated-finite-zero-edge-determines-every-fixed-algebraic-ca-height-correction`, `MI-019-strong-finite-edge-atoms-can-only-hide-through-reciprocal-scale-annular-phase-competition`.

RE-112--RE-123 reduce the attained finite-edge branch to a complete signed subedge zero packet and show that regular CA states sample every fixed algebraic continuum excursion with superalgebraic phase accuracy. RE-121--RE-122 localize any algebraically visible anomaly to zeros approaching the attained edge within `O(log U/U)` at polynomial ordinate height.

RE-124 localizes cancellation around an individually strong atom. RE-125 proves that exact multiplicity and neighbors within `c_*/U` reinforce rather than cancel because their target-demodulated coefficients stay in a common right-half-plane sector. RE-126 then uses the local zero count to show that hiding a strong atom forces a distinct near-edge companion with logarithmically comparable coefficient size and vanishing normalized ordinate gap.

RE-127 supplies the missing sign information. If the strong target remains hidden on the matched observation window, at least one companion in

`c_*/U < |gamma_1-gamma_0| <= U^(-1+eta)`

must satisfy the genuinely cancelling projection

`-Re(a_(rho_1,K)(U) exp(i(gamma_1-gamma_0)U)) >= c |a_(rho_0,K)(U)|/log U`.

Thus the companion is not merely nearby and strong: after target-frequency demodulation it must already point into the opposite half-plane. Since the source coefficient phase lies in a fixed sector `[0,omega_*]+O(1/U)` with `omega_*<pi/2`, the scaled signed gap obeys a fixed cancellation-arc constraint modulo `2pi`. The required pair still has horizontal depth `O(log U/U)`, polynomial height and normalized spacing `|gamma_1-gamma_0| log(2+gamma_0)->0` along an unbounded hidden sequence.

The strong-atom escape is therefore now a sharply specified **near-edge pair plus phase-sector condition**. A useful theorem can attack either the existence of infinitely many such off-line/nonconsecutive pairs or the required demodulated phase relation. Generic local crowding is no longer enough. The genuinely diffuse branch, where no individual coefficient reaches the algebraic threshold, remains separate and still needs a collective cancellation argument.

If the edge is not attained, RE-113's separate obstruction remains. The `Theta=1` branch also remains separate.

## Keep the exact binary tie local

The surviving local first/depth-two tie remains a Four-Exponentials contingency. The edge-profile and fixed-order finite-edge expansions do not require resolving it. A global argument should remain robust to that binary alternative unless exact transcendence becomes load-bearing.

## Keep boundary-mass rate, atom strength, pair spacing, cancelling phase and diffuse cancellation distinct

RE-123 eliminates CA phase placement as a fixed-algebraic-scale bottleneck. RE-124--RE-125 show that an individually strong atom cannot be hidden by distant modes, exact multiplicity or an arbitrarily tight same-sector cluster. RE-126 turns required annular mass into a logarithmically comparable distinct companion. RE-127 shows that at least one such companion must contribute a negative signed center projection.

The remaining selector must therefore control either **strong near-edge zero pairs with vanishing normalized gap and the required cancellation arc**, or **diffuse many-mode boundary mass**. Spacing without phase, phase without comparable amplitude, and generic zero counts are each insufficient by themselves.