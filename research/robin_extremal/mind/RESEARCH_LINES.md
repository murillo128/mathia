# Robin-extremal research lines

This file holds the current mathematical questions suggested by the durable Robin-extremal intuitions. It is not a roadmap, task queue, status page, or history.

## Exploit CA self-tangent selection inside the mixed prime race, rather than bound the race globally

**Linked intuition:** `MI-001-robin-failure-requires-self-tangent-half-mass-event-gaps`.

RE-001--RE-006 reduce sufficiently large clean Robin counterexample peaks to source-selected regular self-tangent CA states and give an exact first-layer selector for their position between consecutive primes. RE-007 shows that, unless the Chebyshev error tunes itself to a specific negative square-root excursion, the bracketing prime gap must itself be of square-root size.

RE-008 couples the selector to the exact Euler-product height before taking absolute values. The endpoint `vartheta(p)-p` term cancels, and every unbounded clean counterexample sequence forces a positive square-root-scale excursion of the endpoint-subtracted Mertens remainder `M_*(p)`. RE-009 identifies the normalized remainder with the classical mixed reciprocal/standard prime-race coordinate `E_{pi_l}(p)-E_theta(p)+o(1)` and records the crucial prior-art boundary: a global one-sided bound for this mixed race is already RH-equivalent.

The live theorem must therefore use the **extra CA self-tangent selection of the boundary primes**. It should show that primes selected by clean counterexample geometry cannot realize the required mixed-race excursion, or else force the hypothetical sequence into the sparse higher-layer/tied event chamber. A global bound on `M_*` is not an intermediate theorem.

## Do not replace source selection by global workload or global prime-race bounds

RE-005 gives regular self-tangent local peaks below the Robin barrier whose Robin values increase, so global peak monotonicity is false. RE-009 gives the analogous analytic warning: globally controlling the normalized mixed prime-race coordinate strongly enough in one direction already settles RH.

A viable argument must condition on the exact self-tangent/clean-counterexample selector and extract additional arithmetic rigidity from that selected subsequence.

## Separate clean first-layer peaks from the sparse exceptional event process

Prime-gap and mixed-race reductions apply only after proving that the relevant boundaries are clean first-layer events. A complete continuation must either control the selected mixed-race coordinate in that chamber or establish a source-selection theorem ruling out concentration on higher prime-power/tied transitions.