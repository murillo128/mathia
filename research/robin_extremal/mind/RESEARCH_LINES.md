# Robin-extremal research lines

This file holds the current mathematical questions suggested by the durable Robin-extremal intuitions. It is not a roadmap, task queue, status page, or history.

## Resolve the vertical Euler continuum beyond the complex single-center absolute-tail floor

**Linked intuitions:** `MI-001-robin-failure-requires-self-tangent-half-mass-event-gaps` through `MI-055-separated-continuum-prediction-has-a-universal-unit-wiener-exponent-floor`.

The scalar Euler program separates positive-real compression from vertical-phase resolution. RE-196--RE-203 show that the positive real ray can be nearly blind and finite vertical panels can still be aliased. RE-204 moves to a whole vertical interval and proves that separated hiding has an exponential signed-prefix cost. RE-205--RE-208 construct ordinary-prime repairs in a small explicit range while respecting fixed KV envelopes.

RE-216 first improved the constructive side by moving the analytic expansion center, reducing the proven separated-predictor Wiener exponent to at most `6.081`. RE-218 retunes the same single-center negative-binomial architecture to

`C_sep <= 5.996390`.

At `H=log 8`, the corresponding ordinary-prime hiding range improves to `c<0.0400989...`. The arithmetic realization is unchanged; the improvement is entirely in the pure continuum predictor.

RE-209--RE-215 establish the source-independent converse/capacity side. The endpoint-zero Kaiser--Bessel test gives

`R >>_H (1-epsilon) e^(HT)/(HT log(HT))`,

so bounded prime capacity forces `HT_Q <= (1/2)log Q + log log Q + log log log Q + O_H(1)`. RE-217 packages the same coercivity as a universal separated-prediction Wiener floor and yields the global interval

`1 <= C_sep <= 5.996390`.

RE-219 proves that scalar retuning inside the one-positive-real-center negative-binomial architecture cannot approach the unit floor when the discarded tail is certified termwise in absolute value. Uniformly vanishing tail error forces the leading Wiener exponent to be at least two:

`2 <= C_(1c,abs,R+) <= 5.996390`.

RE-220 closes the corresponding **complex single-center** escape. For every nonzero center `a`, radialization to `rho=|a|` weakly improves the worst-case contraction on the symmetric frequency arc while preserving the exact coefficient Wiener norm. The phase of `a` factors out at each output degree, so it creates no hidden coefficient cancellation, and the same radial comparator also has no worse termwise absolute-tail certificate. Hence

`2 <= C_(1c,abs,C) <= 5.996390`.

The constructive frontier is therefore sharper than after RE-219. Merely rotating one Taylor center cannot lower the factor-two representation floor. A serious route toward `C_sep=1` must use genuinely **multi-center/minimax geometry, a different approximation basis, or a cancellation-sensitive remainder estimate**. Further optimization of a lone complex or positive-real center inside absolute negative-binomial tail domination cannot resolve even the interval `(1,2)`.

## Keep exact identification, stable inversion, source capacity and prediction representation distinct

RE-174 gives exact source identification from rich Euler data, but the later results show why injectivity is not the selector-scale statement. Positive-real data can be nearly singular; finite vertical panels can be aliased; and a continuum interval forces an exponentially large signed-prefix primitive.

RE-217 separates the **global** prediction floor from prime-placement capacity. RE-218 changes only an explicit constructive predictor. RE-219--RE-220 add a third currency: a lower bound tied to one representation/certificate class. The factor-two floor belongs to **all complex single-center negative-binomial approximants whose tail is controlled termwise in absolute value**; it is not a source-capacity theorem and cannot be promoted to arbitrary separated measures.

RE-220 also illustrates why apparent representation enrichment must be audited against the actual consumed norm and error certificate. The complex parameter enlarges the formal class, but on the symmetric arc its phase disappears from the exact Wiener cost and can only worsen the certified contraction at fixed modulus. The richer parameterization is therefore a coordinate redundancy for this architecture, not new prediction leverage.

The full ledger includes spectral interval width, logarithmic support gap, global predictor/Wiener norm, representation-local approximation geometry, single- versus multi-center structure, tail certificate (absolute versus cancellation-sensitive), total reciprocal variation, signed prefix primitive, prime-count capacity, Chebyshev discrepancy, window-tail norm, integer multiplicities and KV source fidelity. They remain independent until an exact theorem relates them.

## Control one-sided reciprocal escape independently

The zero-side packet obstruction remains separate. One-sided vertical phase geometry can hide a scale-maximal target along lacunary stages while fixed-order typical local-correlation statistics remain unchanged. Closing that branch still needs target-conditioned exceptional geometry rather than average-center statistics.