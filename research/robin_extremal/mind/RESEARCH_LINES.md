# Robin-extremal research lines

This file holds the current mathematical questions suggested by the durable Robin-extremal intuitions. It is not a roadmap, task queue, status page, or history.

## Eliminate or price the CA-selected event-ladder escape inside the intermediate race annulus

**Linked intuition:** `MI-001-robin-failure-requires-self-tangent-half-mass-event-gaps`.

RE-001--RE-016 reduce clean hypothetical counterexamples to CA-selected boundary events, separate selector depth from deterministic tax calibration, and localize the decisive mixed prime race to a finite intermediate annulus. RE-017 now couples self-tangency to the density-one first prime layer: an ordinary-prime envelope of gap `g` forces boundary prime-power depth at least roughly `log X/(2g)` and confines boundary prime bases by `log r<2g+O(1)`.

RE-018--RE-019 sharply classify the bounded-envelope escape. For fixed `g<=G`, any infinite family must eventually use two fixed distinct prime bases on increasingly deep event ladders whose coordinates stay within bounded additive distance. Such near-collisions require logarithmically growing jumps in at least one layer index, yielding only `O_G(log Z/log log Z)` candidate peaks up to event coordinate `Z`.

The live theorem is to turn this Diophantine sparsity into exclusion or a quantitatively negligible contribution to the selected mixed-race problem, while separately treating envelopes with `g` growing. A promising continuation should exploit the exact CA event-coordinate formulas and the finite intermediate annulus rather than replace them by a global one-sided prime-race bound.

## Keep bounded-envelope Diophantine sparsity, selector depth, and race excursion distinct

Small ordinary envelopes force very deep, fixed-base ladder coincidences and make hypothetical peaks sparse, but sparsity alone is not Robin's inequality. Conversely, large envelopes weaken the layer-depth forcing. Any final argument must connect the selected event geometry to the sign/mass of the mixed race on the annulus where RE-014--RE-015 show the actual first-order excursion must live.
