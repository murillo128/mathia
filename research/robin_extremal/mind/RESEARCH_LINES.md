# Robin-extremal research lines

This file holds the current mathematical questions suggested by the durable Robin-extremal intuitions. It is not a roadmap, task queue, status page, or history.

## Use genuinely multiplicative ordinary-prime coherence beyond the sharp gap-capacity threshold

**Linked intuition:** `MI-001-robin-failure-requires-self-tangent-half-mass-event-gaps`.

RE-049--RE-053 show that fringe capacity is negligible, metric prime-gap capacity is decisive only above its sharp exponent threshold, and PNT-dense generalized labels can reproduce the near-critical one-cell fan below that threshold.

RE-054 adds a real source constraint: one ordinary prime generates synchronized events in every fixed CA layer, so one threshold cell forces linked prime-free intervals at scales `Y,(2Y)^(1/2),(3Y)^(1/3),...`.

RE-055--RE-056 show that this is still not enough. One PNT-dense label set can drive the entire layer tower and reproduce the all-layer event-free chamber, and the labels can even be forced to distinct positive integers while keeping the same sharp gap ceiling. Common-source synchronization and integrality are therefore matched controls, not the missing ordinary-prime rigidity.

The live near-critical theorem must consume **primality or multiplicative arithmetic proper**, or a global common-source law such as accumulated CA mass, Euler-product height, or the standard/reciprocal race coupling that an arbitrary PNT-dense integer label set does not satisfy.

## Treat fringe capacity, metric gap capacity, all-layer synchronization, integrality, and multiplicative source identity as separate currencies

The first four can all be reproduced on the near-critical side by explicit controls. Any surviving argument must identify the exact ordinary-prime multiplicative/global relation that those controls cannot fake.
