# Robin-extremal research lines

This file holds the current mathematical questions suggested by the durable Robin-extremal intuitions. It is not a roadmap, task queue, status page, or history.

## Resolve the vertical Euler continuum between constructive hiding and the sharpened source-capacity boundary

**Linked intuitions:** `MI-001-robin-failure-requires-self-tangent-half-mass-event-gaps` through `MI-055-separated-continuum-prediction-has-a-universal-unit-wiener-exponent-floor`.

The scalar Euler program separates positive-real compression from vertical-phase resolution. RE-196--RE-203 show that the positive real ray can be nearly blind and finite vertical panels can still be aliased. RE-204 moves to a whole vertical interval and proves that separated hiding has an exponential signed-prefix cost. RE-205--RE-208 construct ordinary-prime repairs in a small explicit range while respecting fixed KV envelopes.

RE-216 first improved the constructive side by moving the analytic expansion center, reducing the proven separated-predictor Wiener exponent to at most `6.081`. RE-218 now pushes the same mechanism below six with an explicit retuning: the off-center choice `alpha=13/25`, `a=2.29`, `lambda=3.992` gives

`C_sep <= 5.996390`.

At `H=log 8`, the corresponding ordinary-prime hiding range improves from `c<0.0395410...` to `c<0.0400989...`. The arithmetic realization is unchanged; the improvement is entirely in the pure continuum predictor.

RE-209--RE-213 establish the unit exponential converse and show that KV fidelity closes the exact half-log endpoint and a thicker subendpoint corridor. RE-214 then isolates a source-independent layer: bounded ordinary-prime edit multiplicity alone implies `R<<sqrt(Q)`, so finite carrier capacity already forbids every fixed height above `HT=(1/2)log Q` up to a logarithmic boundary layer.

RE-215 sharpens that layer. Replacing the squared Kaiser test by the endpoint-zero continuous Kaiser--Bessel window cancels the nonintegrable first tail and gives

`R >>_H (1-epsilon) e^(HT)/(HT log(HT))`.

Combining this with bounded prime capacity yields

`HT_Q <= (1/2)log Q + log log Q + log log log Q + O_H(1)`.

The leading threshold `1/(2H)` is unchanged, but the source-independent boundary layer is now only one `log log Q` plus `log log log Q`. KV remains genuinely stronger at the exact endpoint because it forces a deficit of order `omega(V(Q))` below it.

RE-217 isolates the remaining spectral constant. The same RE-215 test implies that every finite or measure-valued separated predictor satisfying `sup_(|t|<=T)|1-mu_hat(t)|<=epsilon<1` must pay `||mu||_TV >>_H (1-epsilon)e^(HT)/(HT log(HT))`. Defining `C_sep` as the optimal leading Wiener-norm exponent for fixed-corridor separated prediction now gives the rigorous interval

`1 <= C_sep <= 5.996390`.

This is the clean leading-order bottleneck. RE-208 already realizes every regular discrete predictor with strict coefficient exponent below `1/2`, so a predictor with `C_sep=1` would make the existing arithmetic interface reach every fixed `c<1/(2H)`, exactly the leading bounded-prime capacity boundary. Progress should therefore continue to target the pure prediction problem: push the explicit Wiener exponent toward one or prove a spectral lower bound strictly above one.

## Keep exact identification, stable inversion, source capacity and fidelity distinct

RE-174 gives exact source identification from rich Euler data, but the later results show why injectivity is not the selector-scale statement. Positive-real data can be nearly singular; finite vertical panels can be aliased; and a continuum interval forces an exponentially large signed-prefix primitive.

RE-218 sharpens the constructive resource distinction: **spectral prediction norm is not the same currency as prime-placement capacity**. The earlier constants `12 log 2`, `6.081`, and now `5.996390` moved without changing the ordinary-prime realization. RE-217 and RE-218 therefore sandwich the pure prediction constant at `1 <= C_sep <= 5.996390`, while RE-215 separately identifies the half-logarithmic leading carrier ceiling and KV supplies the finer source-fidelity obstruction at the endpoint.

The full ledger includes spectral interval width, logarithmic support gap, predictor/Wiener norm, total reciprocal variation, signed prefix primitive, prime-count capacity, Chebyshev discrepancy, window-tail norm, moment-cancellation order, integer multiplicities and KV source fidelity. They remain independent until an exact theorem relates them.

## Control one-sided reciprocal escape independently

The zero-side packet obstruction remains separate. One-sided vertical phase geometry can hide a scale-maximal target along lacunary stages while fixed-order typical local-correlation statistics remain unchanged. Closing that branch still needs target-conditioned exceptional geometry rather than average-center statistics.