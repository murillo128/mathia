# Robin-extremal research lines

This file holds the current mathematical questions suggested by the durable Robin-extremal intuitions. It is not a roadmap, task queue, status page, or history.

## Exclude source-selected Chebyshev residual alignment at clean self-tangent peaks

**Linked intuition:** `MI-001-robin-failure-requires-self-tangent-half-mass-event-gaps`.

RE-001--RE-003 reduce a sufficiently large Robin counterexample peak to a regular self-tangent colossally abundant state whose adjacent transition gap must absorb asymptotically half the logarithmic mass of its two boundary jumps. RE-004 makes the boundary dichotomy quantitative: higher-layer/tied transitions have zero count density, so infinitely many hypothetical counterexample peaks either concentrate on that sparse exceptional chamber or are bracketed by two ordinary first-layer prime insertions.

RE-006 sharpens the clean first-layer chamber. If consecutive primes `p<q` bracket such a self-tangent local peak at `X=log C`, then `p<X<q` and the position inside the prime gap is exactly

`X-p = Phi_{>=2}(X) - (p-vartheta(p))`.

Local maximality confines this residual to a window whose nonemptiness forces `q-p>log p+o(1)`. The live source theorem is therefore to exclude repeated alignment between the accumulated higher-layer mass and the Chebyshev deficit at these source-selected self-tangent returns, or prove that any counterexample sequence must concentrate on the zero-density higher-layer chamber.

## Do not replace the source-selected gate by a global workload monotonicity claim

RE-005 gives explicit regular self-tangent CA local peaks below the Robin barrier whose Robin values increase, so the exact workload between genuine peaks does not have a universal nonpositive sign. Any monotonicity or sign theorem must be asymptotic and conditioned by the actual counterexample/barrier selection, not asserted for all regular self-tangent peaks.

## Separate first-layer prime gaps from the sparse exceptional event process

Prime-gap input applies only after proving that the relevant boundaries are clean first-layer events. A complete continuation must either control the exact Chebyshev residual window in that chamber or establish a source-selection theorem ruling out concentration on higher prime-power/tied transitions.