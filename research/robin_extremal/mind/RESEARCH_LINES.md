# Robin-extremal research lines

This file holds the current mathematical questions suggested by the durable robin-extremal intuitions. It is not a roadmap, task queue, status page, or history.

## Use quantized higher-layer cross-cell charges beyond frozen one-cell data

**Linked intuitions:** `MI-001-robin-failure-requires-self-tangent-half-mass-event-gaps`, `MI-002-ordinary-sigma-rejects-composite-substitute-atoms-at-every-fixed-ca-layer`, `MI-003-fixed-threshold-cells-freeze-prime-source-data`, `MI-004-higher-layer-ca-switches-carry-quantized-residual-charge`.

RE-049--RE-053 show that fringe capacity is negligible, metric prime-gap capacity is decisive only above its sharp exponent threshold, and PNT-dense generalized labels can reproduce the near-critical one-cell fan below that threshold.

RE-054--RE-058 progressively tighten source fidelity. One prime generates synchronized fixed-layer events; synchronization and integer labels can still be faked; but an ordinary composite cannot realize the prime-form `sigma(n)/n` increment because its internal prime divisors move the first event to square-root scale. Thus an all-layer matched control preserving ordinary `sigma` is forced back to genuine prime coordinates.

RE-059 shows that one genuine threshold cell still contains only finite source information. After endpoint correction the standard and reciprocal prime sums are frozen while one CA state remains selected; the raw pair has at most one binary fringe-prime update.

RE-060 supplies the first exact source-bearing cross-cell coordinate in this framework. At an adjacent adaptive tie separated by one fixed higher-layer event `j>=2`, the normalized nonlinear-residual jump tends to

\[
-1+j(\chi_+-\chi_-)
\in\{-j-1,-1,j-1\},
\]

which can never vanish. The first-layer cancellation of RE-045 is therefore exceptional: a higher-layer event leaves a unit charge, while endpoint first-layer fringe updates arrive only in `j`-sized quanta.

The live theorem is now global across genuine prime cells. It must show that higher-layer tied switches of this kind are forced or sufficiently abundant on the false-RH counterexample blocks, control how their quantized charges compose across longer event sequences, or derive another cross-cell multiplicative constraint that beats the sharp gap-capacity barrier. Re-sampling one fixed chamber cannot supply the missing information.

## Treat metric capacity, synchronization, integrality, local sigma fidelity, within-cell source dimension, and cross-cell charge as separate currencies

Gap geometry, common-source event towers, and integer labels can be reproduced by controls. Ordinary `sigma` removes composite fake atoms locally, but genuine primality alone does not make a fixed chamber information-rich. New source information enters through event transitions; RE-060 proves one noncancelling higher-layer jump law, but not the global event geometry needed for a contradiction.
