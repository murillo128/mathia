# Global mathematical questions

## Which source resources reach the destination before observation becomes decoding, classical recoding or a target-mixed completion class?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate information survival from the modulus, source class and category consumed by the target.

Prime Circle closes the direct post-compression functional-equation ladder. PC-310 shows that the first non-collapsing mesh-scale Poisson layer is already an invertible cyclic decoder. PC-311 sends the direct bilinear mixed-conductor pairing to a finite bundle of ordinary `L(s+t,chi)` functions. PC-312 rules out accidental scalar finite-Fourier self-duality of the canonical coefficient, and PC-313 shows that retaining `(d,F_N d)` only yields the universal Hurwitz swap relation. A surviving carrier must retain source-native information **before** periodic scalarization or use state not determined by that universal Fourier pair.

Arithmetic Fidelity now supplies both negative and positive completion boundaries. AF-365 protects principal parts under global eventual transformed control; AF-366 exposes invisible pole-carrying tails behind every finite prefix; AF-367 keeps zero geometry mixed inside a positive multiplicative near-square-root Beurling class; AF-368 shows that arbitrarily accurate fixed-logarithmic scalar counting need not identify the rational-prime source. AF-369 then narrows to integer-supported Delone systems: every source in that class still has the Riemann divisor in `Re s>0`, but the discarded pole residue equals the generalized-integer density and `residue=1` exactly characterizes `Q=P`. The relevant resource is therefore not “more data” in isolation but the **joint fibre of source category and retained analytic normalization**.

Prime Lattice moves in the opposite direction by lowering the destination bill. PL-322 makes the residual moment automatic from conservative endpoint flux and the convergent source trace; PL-323 forces `Qm->0` from the source-coupled coercive equation; PL-321 then closes the moving Green transfer. The analytic endpoint gate is no longer live for the actual branch. What remains is the rational-prime-specific sign/first-crossing law carried by the transferred scalar.

The useful quantity across these lines is the **destination margin obtained before observation crosses into generic inversion, classical recoding, universal transform closure or an admissible-completion fibre that still mixes source/target states**. AF-369 is the positive reminder that a very small retained datum can suffice once the source fibre has already been narrowed enough.

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) now separates signed cancellation, sparse source sensitivity, shallow ancestry reach, wrong-population pseudorandomness, mesoscopic background dilution and exceptional zero geometry.

Nyman--Beurling classifies the signed exterior escape increasingly tightly. NB-148 cancels the universal Hadamard logarithm with a zero-mass sampler but exports equal negative Poisson mass. NB-149 proves that controlling that adverse lobe by Jordan/total-variation domination restores a positive `Theta(log G)` budget. NB-150 then closes the simplest location-sensitive mesoscopic escape: for any two-point horizontal difference with `1<<L_G=o(G)`, Riemann--von Mangoldt density forces both positive and negative lobes to contain `Theta(log G)` actual-zero mass, while an `O(log G)` target packet inside `o(L_G)` contributes only `O(log G/L_G)`. The surviving signed route must therefore remain microscopic/nonuniform, exploit exceptional source placement, use signed prime-side cancellation, or couple to the Nyman target before lobe separation.

Robin Extremal has two distinct target-conditioned gates. RE-151--RE-152 show that one-sided reciprocal-scale hiding can occur at lacunary exceptional stages while fixed-order globally averaged local statistics remain unchanged. RE-153 identifies the direct self-tangent source quantity: a Pareto-weighted average of `(t-vartheta(t))/sqrt(t)` must reach `sqrt(2)-o(1)`. RE-154 then quantifies its sensitivity: relocating only `sqrt(p)/log p` post-selector labels, an asymptotically vanishing fraction of a proportional shell, moves that normalized source coordinate by order one while preserving exact agreement below a fixed multiple of `p` and PNT-scale density.

Farey Discrepancy and Möbius Cancellation exhibit the same logical issue in different categories. FD-149 shows that bounded-distortion ancestry must reach essentially the typical squarefree prime-factor depth `log log T`; shallower reach sees too little anchor-descendant mass. MC-320 shows that polylogarithmically small character sums on every polynomial-length integer interval can coexist with maximal bias on the exact dyadic prime shell. Ambient rarity, connectivity, interval pseudorandomness or broad smoothing is not control in the destination measure.

The live question is therefore quantitative and logical: what theorem is uniform on every target-bearing configuration, or directly controls the destination-weighted source component, and does its rate beat the visibility loss without changing to an observation category that erases or swamps the gain?

## Which growing source law is informative rather than merely connected, high-dimensional, zero-free, interval-pseudorandom or reconstructive?

Farey Discrepancy prices both local reweighting and nonlocal reach. FD-147 identifies finite-`L^q` binary ancestry recovery with an anchored Cheeger ratio. FD-148 shows that fixed-seed positive expansion under one-step nonuniform weights requires singular endpoint marginals. FD-149 proves that bounded child-marginal distortion avoids that tax only by reaching depth `L_T >= log log T-O(sqrt(log log T))`. A source-native ancestry mechanism must now explain either the singular mass or the typical-depth reach it spends.

Möbius Cancellation gives two complementary warnings and one partial source gain. MC-317 shows the constant-defect half-rate code endpoint is generically realizable. MC-319 finds a genuinely arithmetic property: the forced common radical is smooth enough for strong family zero-free control, but its known prime-sum conversion is too coarse. MC-320 rules out ordinary interval cancellation as the missing bridge: an imprimitive conductor-`4` control can be almost perfectly pseudorandom on integer intervals and maximally biased on the dyadic prime shell. The remaining discriminator is the **primitive conductor/source provenance** of the endpoint characters together with a prime-sensitive shell theorem.

The next useful theorem in any growing representation must survive its own exact algebra, matched-control class and **resolution/observation transfer**. “Many observations,” “connected,” “zero-free,” “small interval sums” and “source-generated” are not scalar resources until the destination population, norm and scale are priced.

## Which representation-faithful null remains after nuisance variables, weak topology and universal finite scalar positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) now has exact next gates on both the Visual and Weil-positivity sides.

VIS-242--VIS-245 make the symmetric-Dirichlet tent calibration and concentration covariance explicit. VIS-246 proves that conditioning on concentration `C` is **not** an exact nuisance quotient for `m>=3`: the slice law retains the factor `(prod X_i)^(alpha-1)`. The exact sufficient coordinate is instead `L=sum log X_i`, so `Law(bar T|L)` is `alpha`-free. The remaining question is whether that conditional law is tractable/nondegenerate and preserves the prime-specific signal; a sufficient nuisance quotient can also remove power.

WP-319--WP-321 show that source-specific derivative-biased Mangoldt laws and even the whole bounded-gap scalar Julia family are reproducible in the natural weak topology after atomwise smoothing. WP-322 makes every finite exact boundary Loewner matrix a universal resolvent Gram. WP-323 closes the higher-derivative variant: arbitrary fixed finite confluent Pick jets are higher-resolvent Grams as well. Exact arithmetic values and derivatives may survive while every positivity theorem inherited from their finite scalar Pick matrix remains universal.

The common discriminator is whether the destination uses a relation that survives honest nuisance propagation and whose sign/validity is not automatic under the relevant matched-control category. On the Visual side this means a predeclared residual after an exact nuisance quotient or independently frozen nuisance. On the Weil side it means a support-sensitive non-Pick invariant or a change to matrix/operator/infinite-domain/global finite--archimedean structure.

## Which noncompact source channels and reserve scales must survive a positivity reduction?

Weil Inertia has changed obstruction class twice. WI-304--WI-306 prove that Liu's frozen `beta_0=1/16` tail-dropped comparison develops an essential negative band at the first active prime. WI-307 shows that the reserve is not intrinsic: the unbounded archimedean symbol allows arbitrary reserve lifting at every fixed aperture, moving the essential spectrum positive and leaving a finite/discrete negative sector.

WI-308 then restores the omitted source exactly. Clipping the source symbol produces an exact positive tail multiplier with global lower bound `c_(chi,B)>0`, so fixed-aperture positivity reduces to the finite spectral inequality `lambda_min(D_(L,chi,B))>=-c_(chi,B)`. A hypothetical first crossing would instead force a negative eigenvalue below `-c`, giving a gapped Fredholm witness. The live problem is now to make the tail gap effective, control the finite spectrum against it, and preserve that comparison uniformly as `L->infinity` while paying the reserve/Fourier-cutoff cost.

A global impossibility theorem must survive all admissible reserve choices and the exact retained tail; a construction must show that this source-exact budget dominates the finite sector and still pass the signed-density quotient of WI-241.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier has become more specific rather than closer to an RH proof. AF-369 shows that source category plus one natural discarded scalar can restore binary prime identity even when the divisor is universal. FD-149 prices bounded-distortion nonlocal ancestry at typical `log log T` depth. MC-320 separates primitive source provenance from common-modulus and interval pseudorandomness. NB-150 rules out mesoscopic two-point signed localization by actual-zero background occupation. RE-154 gives an exact sparse post-selector sensitivity scale for the Pareto Robin source. VIS-246 replaces concentration conditioning by the exact log-product nuisance quotient. WI-308 replaces an unspecified finite-sector bill by an exact tail gap. WP-323 extends scalar Pick universality through every fixed finite derivative jet.

For every proposed route ask: **what exact object reaches the destination; under which norm, scale, quantifier, topology, observation population and admissible source class; which universal/nuisance component has been removed and what debt did that removal create; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, universal transform closure or Gram positivity; and does the surviving source theorem dominate the destination loss without reconstructing or assuming the endpoint?** A new representation is useful only when one of those answers changes mathematically.