# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry or programmable completion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity has now closed both the natural scalar gauge orbit and the canonical infinity-based moment-Schur escape more sharply. WP-307 keeps the pole-normalized Mangoldt curvature positive under every density-normalized orientation-preserving affine spectral recentering. WP-308 closes constant scalar Herglotz projective recodings. WP-309 proves that allowing the scalar automorphism itself to depend holomorphically on the spectral parameter adds no freedom: every jointly holomorphic family of half-plane automorphisms is constant by Schwarz--Pick rigidity.

WP-310 adds a different admissibility obstruction. The canonical critical Mangoldt positive source has density-one infinite mass and a logarithmic Herglotz end, whereas the classical infinity/moment Schur transform requires finite-mass `-s_0/z` asymptotics. Formally applying the first Schur step with any fixed finite `s_0` eventually leaves the Herglotz half-plane. Finite cutoffs admit the transform, but their zeroth moment diverges; subtracting the unit-density background makes the remainder signed. Thus the standard source-moment Schur recursion does not inherit positivity in the critical limit for free.

The remaining positive route must therefore introduce genuinely new finite--archimedean geometry before scalar comparison: a finite-reference interpolation transform with source-derived data, an independently justified cutoff/renormalization law, a noninvertible Herglotz operation with independent meaning, compression/generalized resolvent not equivalent to the finite-moment infinity step, operator/matrix-valued boundary sector, determinant/scattering object, added archimedean channel, or nonseparable finite--archimedean coupling. Spectral-variable scalar coordinate freedom and the canonical finite-moment Schur step are no longer live repairs.

## Which source resources survive into the exact destination margin, topology and moving resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) keep conditioning, source supply and destination sensitivity distinct.

Analytic Frontier still requires a joint arithmetic-placement law relative to its distinguished logarithmic carrier; matched one-point statistics are exhausted.

Arithmetic Fidelity now separates full-source fidelity from discriminator fidelity inside one Riesz representation. AF-349 proves that every fixed-order full Riesz profile is injective and exactly stable in a source-matched derivative-variation metric: finite-horizon variation equals coefficient `ell^1` distance up to `r!`. AF-348 nevertheless gives sources only `O(1)` apart in raw profile amplitude after enough smoothing. AF-350 calibrates that weak quotient: `R_r[a-b](X)=O(X^theta)` forces `A-B` to be holomorphic on `Re s>theta`, so every Dirichlet principal part strictly to the right of the error line survives even though coefficient/support information may not. For `Lambda-1`, the same statement turns a Riesz error exponent into a zero-free half-plane. The perfect-power controls pass because their transform difference from unit mass is entire. A terminal topology can therefore be useless for source recovery while remaining exactly sufficient for a narrower analytic discriminator.

Nyman--Beurling now has an explicit source-faithful separation between formal interpolation and genuine zero packets. NB-129 makes natural-prefix phase information target-visible only at a reciprocal-height target-angle scale. NB-130--NB-131 show that rank-`R` formal zero powers can interpolate arbitrary natural-prefix data and be translated to every height with only unitary row modulation while formal Burnol mass vanishes. NB-132 shows that genuine right-half-plane zeta zeros in a fixed-width band below polynomial height cannot realize that rank: symmetry plus explicit zero counting gives `d(F)<=A(W/(4 pi)+0.10076) log R+o(log R)`, and when the leading coefficient is below `1/log q` the packet recurrence becomes visible inside the natural cutoff. The formal control is then excluded, but the visible annihilator may still be badly conditioned; the live question in this regime is a quantitative Burnol-scale target margin, while superpolynomial heights or wider bands retain a rank-visibility problem.

Prime Lattice now shows why even the right scalar endpoint moment is not enough without tail topology. PL-313 identifies the rank-one weighted corner functional. PL-314 proves that weighted `L^1` mismatch alone does not pass through the finite-`s` Green transfer: a moving diagonal atom samples `Q=r/s` and can detect sparse bumps of negligible total mass. The condition `Qm(Q)->0` is sufficient, so the actual completed-Weil branch must supply a quantitative remainder or anti-concentration law before the scalar matching theorem is valid.

Prime Flute's hard-cutoff angle remains noncompact, so progress there still requires a new split or observable.

These lines support a sharper cross-line statement: **terminal stability is topology-, scale-, rank-, admissibility- and discriminator-relative**. AF-349 exhibits exact recovery in a strong derivative norm while AF-350 shows that a much weaker amplitude quotient still retains half-plane singularity data; PL-314 exhibits failure of weak integrated control against a moving atom; NB-132 shows that source-specific rank control can invalidate a formal interpolation escape without yet buying quantitative conditioning; WP-310 shows that even a positive source can lie outside the admissibility class of a desired positivity-preserving transform. A usable source resource need not preserve the whole source, but it must preserve the exact feature, norm, effective rank and moving resolution the endpoint consumes, and the final transform must actually be defined on that source class.

## Which growing source law is genuinely informative rather than sparse, diluted, under-resolved or circular?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now exposes four distinct dilution/circularity boundaries. FD-128 shows that infinite exact Möbius ancestry can remain cheap when the active prime set is sparse. FD-129 shows that complete growing prime-prefix ancestry can become circular when paired with a stage-uniform critical envelope. FD-130--FD-131 show that global or rankwise mean ancestry can dilute fixed-rank or finite-prime cuts. FD-132 shows that every fixed prime coordinate can converge while a moving central-rank defect remains macroscopic. FD-133 strengthens that last obstruction to every fixed polynomial active-prime family: for each `alpha<1`, the same positive-density wrong source has `sup_(p<=H^alpha) P_(p,r,H)->0`. Prime-coordinate breadth alone is therefore still noncoercive while the norm averages over the moving rank interface. The live currencies are simultaneous rank-and-prime control, local/`L^infinity` ancestry, scale-sensitive nonsummable weighting, a growing exact anchor, the near-full-prime regime `alpha(H)->1`, or destination-side coercivity.

Möbius Cancellation now has pointwise, generic collective, quadratic collective and field-packaged arithmetic capacities. MC-300 prices the Lambert-W ceiling of the current Page/Deuring--Heilbronn channel. MC-301 reaches conductor `sqrt(y)` by the multiplicative large sieve. MC-302 exploits quadraticity and reaches conductor `y`, but even an ideal loss-free theorem of the same shape leaves an `O(log y)` capacity against only about `log y/R` endpoint energy at `R~log_2 log y`. MC-303 then packages the independent quadratic modes into a degree-`2^R` multiquadratic field. The conductor--discriminant formula turns the Page-forced source-cost spectrum into `log |D_K| >= (log y)^(2-o(1))/log log y`, making generic effective Chebotarev far too expensive at shell scale. Exact packaging has therefore not compressed the source cost; it has moved it into field discriminant complexity.

The cross-line lesson is stronger than “use stronger norms.” FD-133 and MC-303 show two different ways nominal breadth can fail: a theorem may monitor polynomially many coordinates while normalization hides the moving active layer, or it may combine all coordinates into one algebraic object while the theorem acting on that object charges the same complexity through another invariant. **Active-scale visibility and effective family capacity are separate from coordinate count and representation compactness.**

## Which support geometries survive the one-signed Weil first-crossing constraint?

WI-294 changes the large two-island frontier. Complete screening of the actual nonnegative null mode makes the pole term strictly positive, so zero archimedean energy requires negative gamma energy. WI-295 retains the exact digamma multiplier and makes the support restriction quantitative: the exact bathtub profile `H(mu)` has one positive zero `mu_*`, and every normalized one-signed completely screened null mode obeys `mu_*<|{v>0}|<=log 2`.

WI-295 also adds an amplitude tariff. If two positive-support pieces at separation `R` have `L^1` masses `M_1(A),M_1(B)`, then `M_1(A)M_1(B)<-H(mu)/(4 cosh(R/2))`. Widely separated pieces can therefore coexist only with exponentially unbalanced `L^1` amplitude. The large symmetric two-island branch remains closed, but the live many-component problem is now sharper: can fixed positive support measure, exponentially decaying remote mass and `L^2` normalization coexist with the complete-screening null equation, or can source-side regularity convert the `L^1` tariff into an `L^2` contradiction? Sign-changing modes, incomplete screening and bounded aperture remain distinct.

## What should Prime Circle retain after centered Gram formation and independent averaging collapse?

PC-295--PC-298 identify the universal diffuse square-zero class and its defect scale `r_q`. PC-299 identifies the first positive quadratic defect below that scale as a Fejer-smoothed source autocorrelation, so source Fourier phase is lost in the positive power quotient.

PC-300 closes the most direct independently sampled noncommutative repair: independently sampled source-coupled cyclic profiles factor after expectation through powers of one real symmetric reflection-even Hankel matrix. PC-301 identifies an earlier quotient that does not depend on independence. For the centered window, each same-source cross-Gram carrier satisfies `G_(-s)=G_s` pointwise, so **every** cyclic word built only from those carriers is invariant under independent reflection of every source coordinate for arbitrary correlated or reused-source laws. Correlations among the unoriented classes can survive, but coordinatewise reflection-odd source information is already gone before averaging.

The surviving question is therefore not merely which dependence structure avoids factorization. To recover oriented source information, the construction must retain oriented frames before same-source Gram contraction, justify an asymmetric source window/observable, use a cross-level carrier not determined by `G_s`, or prove that the RH-relevant relation genuinely lives on the coordinatewise sign quotient. Higher loop length and arbitrary source coupling inside the same centered Gram carrier are no longer distinct repairs.

## Can the required Robin cancelling clusters occur for actual zeros?

RE-129 merges spacing, comparable magnitude and cancelling phase into one inverse-window companion for a hidden dominant algebraically strong atom. RE-130 removes the diffuse-cloud branch as an independent algebraic escape and transfers the off-critical zero-density exponent into the atom scale. RE-131 constrains hiding on a fixed relative window: a distinct companion must lie at polylogarithmic-reciprocal ordinate distance, carry at least a `1/log U` fraction of the dominant amplitude, have cancelling target-demodulated phase and be horizontally locked within `O(log log U/U)`.

RE-132 shows that an extremely sparse simple formal spectrum can realize the required doublet on every sublinear window. RE-133 now closes the apparent macroscopic-window repair in the same formal class. A growing reciprocal subset cluster produces a contractive product and hides a dominant atom uniformly on `|u-U|<=kappa U`, while lacunarity permits the whole off-critical counting function to remain below any prescribed divergent one-level envelope. Thus global sparsity and observation-window size do not by themselves rule out the local complexity needed after atom extraction.

The formal spectrum is not asserted to be zeta. The live attained-edge question is therefore local and source-specific: can actual off-critical zeta zeros support the required reciprocal-scale growing clusters with their horizontal/phase compatibility, or does an Euler-product, explicit-formula, local-spacing or other arithmetic law forbid them? The nonattained edge and `Theta=1` remain separate.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225 supplies the coherent-block lower bound and VIS-226 matches it with the absolute tapered envelope. Throughout `H log y/y ->0`,

`I_abs(B) asy M_abs(A) asy U_abs^R(B) asy y/(H log y)`,

and the Haar-normalized scale is `y/(sqrt(H) log y)` up to constants. The static bounded-versus-divergent question is therefore closed at the full coarse order, not just from below.

VIS-227 calibrates the deterministic critical occupancy floor. VIS-228 shows that subtracting that floor still leaves a generic stochastic contribution: for independent uniform occupancy with the same `(m,J)`, `E[S]=binom(m,2)/J`, `Var(S)=binom(m,2)(1/J)(1-1/J)`, and `E[S-S_min]=Theta(J)` when `m/J` tends to a positive constant. An extensive excess above balanced occupancy is therefore not prime-specific evidence.

The critical pair-collision route now begins after stochastic centering against an admitted matched occupancy law, including the actual one-point probabilities when nonuniform. The existing local clue asks whether a centered residual survives cell-origin perturbations and stronger logarithmic-gap controls. If not, progress requires a different observable rather than another normalization of the same pair count. Sharp objective constants, optimizer geometry and dynamical access time remain separate questions.

## Global discriminator: price what the endpoint actually sees, including dependence, quantifiers, admissibility and capacity

The newest evidence strengthens [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md). AF-350 shows that weak source recovery can coexist with exact singularity fidelity above an error line. FD-133 shows that even polynomial active-prime uniformity can miss a defect concentrated on a moving rank layer. MC-303 shows that exact algebraic packaging can preserve rather than remove source cost by transferring it into discriminant complexity. NB-132 shows that a genuine source law can restore effective-rank visibility while leaving conditioning open. PC-301 shows that arbitrary source dependence cannot restore orientation erased before averaging. PL-314 shows that weak tail integrability misses a moving atomic observer. RE-133 shows that extreme one-level sparsity plus a macroscopic observation window still does not exclude growing reciprocal local clusters. VIS-228 shows that subtracting a deterministic collision floor leaves an extensive stochastic null baseline. WI-295 converts one-signed screening into both a support floor and a separated-mass tariff. WP-310 shows that a desired positive transform can fail already at source-class admissibility.

The common accounting rule is now more precise. For every proposed route ask: **what is the source discriminator; at which stage is it quotiented; in which coordinates/ranks/directions does it live; what topology and normalization retain it; which quantifiers are uniform at the active scale; which dependence survives the carrier before averaging; what resolution or moving scale does the endpoint sample; how large is the admissible family capacity relative to the obstruction; does a new representation actually reduce complexity or merely rename its invariant; is the final transform admissible for the source class; and do the hypotheses already reconstruct the desired endpoint?**

A route can fail by under-resolution, rank excess, unstable or topology-mismatched inversion, inter-rank or intra-rank dilution, pointwise-versus-uniform quantifier mismatch, family-capacity slack, complexity transfer under repackaging, pre-averaging carrier quotient, factorization under independence, moving-scale concentration, global-count/local-cluster mismatch, deterministic or stochastic control baselines, transform inadmissibility, terminal quotienting, universality, gauge rigidity, certificate exhaustion or circularity.

If a transfer between lines is only plausible rather than established, keep it as a clue rather than promote it to global mathematical knowledge.
