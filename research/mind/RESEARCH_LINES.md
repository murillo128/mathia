# Global mathematical questions

## Which independently selected source object can supply the continuum before producing a nonlocal positive law?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) separate nonlocal realizability, independent model selection, spectral compatibility and continuum provenance.

WP-267--WP-274 show that hostability, category descent, scalar sign, localizable compression and inverse construction all fail as free routes to the desired Weil orientation. Clark/de Branges machinery can manufacture a positive nonlocal model from the desired normalized carrier itself, so existence alone is a representation tautology when the target measure is supplied first.

WP-275 adds a source-side spectral-type obstruction: the natural arithmetic log-energy carrier is pure point while the pole continuum has nonatomic absolutely continuous multiplication spectrum, so a bounded exact intertwiner of the canonical modulation groups is zero. WP-276 blocks trace-class and trace-class-resolvent source surgery from manufacturing the missing continuum out of wholly singular ingredients.

WP-277 strengthens that obstruction on Mathia's actual logarithmic arithmetic Hamiltonian. It has compact resolvent, so if the auxiliary zeroth-order sector also has compact resolvent then every bounded self-adjoint coupling—and every symmetric coupling of relative bound `<1`—preserves compact resolvent. Merely leaving trace class, Hilbert--Schmidt or compact ideals is not enough.

The live bridge must therefore explain the continuum **before** positivity. Either derive an intrinsic non-compact-resolvent/a.c. archimedean or boundary sector at zeroth order, or justify a genuinely supercritical domain-changing/infinite-volume mechanism that destroys compact resolvent for an independent reason. Only then can finite--archimedean coupling and the Weil sign become explanatory rather than reverse-engineered.

## Which source restrictions actually shrink the target-reachable failure set at the metric the theorem consumes?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) distinguish nominal source reduction from the geometry actually seen by the destination.

AF-300--AF-305 replace deleted linear directions by intrinsic source dimension, positive reachable bodies and finally a sharp discrete saturation boundary. AF-306 now splits that boundary into exact and stable questions. Saturated equal-weight prime supports can be exactly zero-free while having zero distance from zero, and same-fiber saturated supports can have normalized zero margins arbitrarily close to both `0` and `1`. The live exact-zero problem is therefore genuinely discrete, while stable margin recovery is already obstructed by saturation itself.

PF-315--PF-316 give the complementary operator statement. The full physical angle is `(T_H V_H,Z_LH)^T`, but `V_H` cannot hide a bad conditional endpoint for free: its deviation from a unitary is quadratically paid by `Z_LH`, and compactness/weak trace of the full angle is equivalent to compactness/weak trace of `(T_H,Z_LH)`.

Prime Circle and Visual Exploration sharpen the observation-scale bill. PC-274 proves the raw ordered near-resonance law prime-blind at every bandwidth, and PC-275 identifies the first centered fixed-bandwidth correction as the classical logarithmic-integral source bias. VIS-198 closes the quadratic weighted-crowding obstruction throughout every polynomially growing strictly subcritical horizon by combining the same-sign fiber reduction with a three-form upper-bound sieve and taper localization. The remaining Visual frontier is the slow-horizon/critical transition, not local prime-tuple admissibility.

## Which residual cancellation theorem survives after generic abundance, additive thickness, coding and completed quadratic summaries are exhausted?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) place both Farey and Möbius beyond their previous generic barriers.

FD-083 removes the counting-cardinality obstruction by giving the physical occupied set full polynomial exponent. FD-084 strengthens this to a consecutive additive block of occupied horizons of length `H^(Theta-delta)` inside every fixed-power future window. The remaining Farey gate is not scarcity or isolated recurrence but **compatible multiplicative transport**: polynomial additive thickness still does not provide an integer-ratio chain or prove that the existing local gains compose.

MC-252 removes quadratic reciprocity as a generic fix, MC-253 shows bounded-order shell pseudorandomness is rank-blind, and MC-254 removes ordinary full Rédei rank. MC-255 now removes the whole completed higher `2`-class rank tower as a shortcut: genuine prime controls can have vanishing `r_4,r_8,...` while the selected lower/shell cross-block has arbitrarily large kernel, with those blind directions filtered out before the classical `R_8` domain. A surviving algebraic route must act directly on localized bipartite information or bypass generation with signed cancellation of the rough Möbius coset.

## Which source-specific equation prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia now close complementary generic regularity and screening escapes.

PL-289 upgrades actual one-dimensional Weil eigenstates to a squared-log Fourier moment. PL-290 proves the corresponding `Theta(log^-2(1/|delta|))` aperture modulus is sharp on the full graph ball even for the actual active `2,3,4` source comb. Graph-domain membership therefore does not yield a Lipschitz or differentiable aperture family. A stronger transport theorem must use structure special to the low eigenbranch: source cancellation, stronger branch regularity, a tiny explicit constant, or interval-wide certification.

WI-264 screens all active finite-aperture von-Mangoldt lags with an endpoint-bump autocorrelation, and WI-265 shows the same vector can satisfy every generic first-crossing operator condition in a flexible positive model. WI-266 nevertheless excludes that explicit family from Suzuki's **actual** null equation: on a nonempty support-gap interval all discrete translations vanish while the monotone continuous kernel gives a strict one-sided convolution sign.

The next Weil-Inertia theorem should generalize that source-specific rigidity to arbitrary screened support geometry or exhibit a genuinely source-compatible multi-component countermodel. Weil Positivity supplies the global counterpart: the correct source continuum must already exist, or arise through a genuinely stronger category change, before a sign-producing global law can be meaningful.

## Which quantitative inverse law makes a finite future affordable?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) separate exact source consistency from the rate needed at the actual horizon.

NB-082 identifies the stationary finite-prediction excess `a_(psi,L)`: depth-`L` prediction-volume charge is comparable to `R a_(psi,L)`, so qualitative outerness is insufficient. NB-083 sharpens the calibration. An outer polynomial whose inverse has a unit-circle singularity satisfies `a_(psi,L)~kappa/L`, forcing depth `L=Omega(R)` and hence an exponential-size physical future merely for bounded screening. If every zero stays strictly outside the closed disk, prediction is geometric and logarithmic depth gives a polynomial-size future.

The arithmetic Nyman question is therefore to prove a genuinely nonstationary inverse/prediction rate for the canonical innovations that beats the boundary-singular matched control at the required `1/R` scale, or to avoid the finite-prediction boundary layer with a different stable-mode certificate. Forward smoothness, cyclicity and exact branching do not determine affordability.

## Which singular selection channel remains after regular event mass is removed?

RE-083--RE-085 show that endpoint selector data reconstruct the first-layer event process and that every bounded fixed-order mixed-event mass statistic becomes asymptotically first-layer. RE-086 opens one surviving singular channel quantitatively: when a fixed-layer higher event is selected as an adjacent support boundary, its generating base prime must carry an anchored neighboring gap with normalized capacity `G/(p log p)` at least the exposed cell scale. Tied boundaries force simultaneous resonances at power-related prime scales.

The Robin frontier is therefore to bound the frequency or total threshold capacity of these anchored gap/event resonances, or move to the remaining singular mechanisms—skipped-span conditioning, tied-state admissibility beyond endpoint geometry, and discontinuous rightmost-maximizer selection. Vanishing higher-layer mass alone cannot decide any of them.

## Can the two genuine flute angles satisfy the required ideal count?

PF-316 removes the last residual-source ambiguity at the endpoint. Sequential elimination still gives the exact block-column representation, but singular-value recovery shows that `T_H` cannot be hidden by `V_H`; attenuation is paid quadratically by the direct angle `Z_LH`.

The next target is therefore exact: determine the compactness/weak-trace singular-value laws of `T_H` and `Z_LH` on canonical tails, then close the compatible infinite realization, symmetric reassembly and signed target composition gates. Failure of either channel is already a full physical-angle obstruction.
