# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry, singular source calculus, finite-mass decay or programmable representation?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate source provenance, target sensitivity, relational structure, reachability, readout fidelity and motion law.

Weil Positivity has closed scalar recovery, fixed positive multi-channel recovery, finite multiplicative-equivariant depth, flat transport and several curvature/readout escapes. WP-300--WP-303 trap ordinary local/moving/spectral constructions between universal geometry, readout loss, singular commuting source calculus and free programmability. WP-304 adds the mass-class barrier: bounded source resolvents decay like `1/y`, while the first pole-normalized infinite-mass Mangoldt Herglotz channel reaches only a unit-density-universal leading logarithm whose real part disappears under canonical evenization.

WP-305 now tests the most economical orientation repair at the first nonconstant real order. Reflection restores the sign of the leading logarithm for the canonical measure `sum Lambda(n)/(n+1) delta_(log n)`, but its source-fixed curvature is `+A_0/y^2` with `A_0>0`, whereas the Riemann digamma sector has `-1/(24y^2)`. Constants and positive spectral dilation cannot repair the sign. The live route must therefore derive, **before Gamma comparison**, an additional finite--archimedean operation that changes the source remainder while retaining an independent positivity theorem. Reproducing `log y` is too generic; producing a nonuniversal remainder is not enough if its first exact coefficient points the wrong way.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) keep conditioning, source supply and target sensitivity distinct.

Analytic Frontier now has a sharp bow interface beyond source marginals. ANF-185 replaces adaptive critical probing by one deterministic one-height high-pass with transition width `a_X`. ANF-186 then matches the natural prime/rough coefficient alphabet, densities, mean, diagonal variance and local absolute-mass envelope while still producing target endpoint memory by placing signs after reading the exact logarithmic carrier. The remaining theorem is a **joint arithmetic-placement law relative to the carrier**, or direct `o(a_X)` control of the deterministic weighted residual / signed Fejer defect. One-point source statistics are exhausted.

Arithmetic Fidelity has now identified what its sign statistic actually measures. AF-338 gives fixed-order full-Euler sign regularity once `sigma>r-1`; AF-339--AF-343 show that after pole cancellation the prime layer retains the RH pole discriminator and `Theta(P/log P)` variation against broad baselines; AF-344 shows a fixed prime-independent perfect-power background can collapse that variation to `O(P^(1/k))` while moving cost into sparse high-amplitude support. AF-345 then proves, for nonnegative sub-prime-height backgrounds, the exact identity `V=2J_b`, where `J_b` is the number of consecutive-prime gaps carrying any composite background mass. With retained gap masses `m_j`, `V^(q-1) sum m_j^q >= 2^(q-1)(sum m_j)^q`. The live source resource is therefore **prime-gap mass concentration**, or another invariant consumed by the observation theorem, not sign count/support cardinality by themselves.

Nyman--Beurling has crossed from fixed-packet stability to an explicit rank--height tradeoff. NB-123--NB-124 make the actual zero-power annihilator a stable bounded dual functional in the Nyman cell metric, so every fixed packet has a cutoff-uniform target-angle gap. NB-125 keeps the annihilator product root by root and combines it with the explicit Vinogradov--Korobov zero-free region. Polynomially near-perfect capture now forces an additive budget in confluent rank and ordinate height: under polynomial height, `d(F)>=(3alpha/4+o(1)) log R/loglog R`, while fixed rank forces superpolynomial/exponential-in-a-power height. Horizontal approach to `Re s=1` is therefore not a free escape variable. The remaining bridge is to couple this **rank-weighted height entropy** to NB-116's target-bearing high-zero compression loss.

Prime Lattice has completed the regular limiting Volterra range problem. PL-309 localizes the trace to an exponentially thin layer, PL-310 gives the universal singular profile `C/sqrt(e^(2r)-1)`, and PL-311 isolates the endpoint condition `h(0)=0`. PL-312 proves that for `C^1` forcing this is the **only regular outer solvability condition** and gives an explicit unique right inverse once it holds; the excluded homogeneous mode is exactly the singular amplitude profile. The remaining analytic resource is therefore a finite-`s` **corner-to-outer matching law that supplies the one scalar endpoint cancellation and selects `C`**, followed separately by the rational-prime sign/first-crossing theorem. More regular inversion of the limiting outer operator is closed.

Prime Flute's sharp-cutoff completed-angle branch remains noncompact: adjacent Fourier modes straddling the hard cutoff generate order-one canonical correlations on infinitely many separated modules. A viable continuation must change splitting/observable rather than sharpen the same compactness estimate.

## Which cancellation theorem remains after representation enlargement and source equations stop adding endpoint force?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy has moved the surviving resource from generic source coherence all the way to actual Möbius sign correlation. FD-121--FD-124 force a positive average deficit whenever a polynomially bounded common source is sampled along a finite-log-entropy horizon schedule; FD-125 shows repeated squaring evades that generic cost. FD-126 strengthens the matched control to one permanent source with exact squarefree support, exact `+-1` amplitudes on every squarefree integer and global `O(sqrt n)` cumulative size, yet its floor-quotient energy still saturates along repeated-square horizons. The next theorem must therefore use **correlation among the true Möbius signs**—coprime multiplicativity, a growing exact divisor/cross-horizon identity family, or another nonredundant sign law—not merely support, amplitude, envelope or common-source consistency.

Nyman--Beurling provides the complementary positive example of a source relation that really does shrink the relaxed class in the destination norm. The zero-power annihilator is not merely exact: NB-125 shows that its missing-root product forces actual zeta zeros to pay for near-perfect capture through rank-weighted height entropy. The unresolved step is not another source equation but a theorem coupling that packet entropy to the continuum compression/target-mass budget.

Möbius Cancellation has also sharpened its endpoint bill. MC-293 collapses the low-source-cost near-negative spectrum to at most one Landau--Page mode; MC-294 forces many selected directions into a high-cost quotient; MC-295 shows that a generic fixed-weight model can still saturate those bills. MC-296 then exploits an exact endpoint defect partition: every subset sum has bias `(-1)^|I|(1-2d(I))`, so outside the half-mass window almost every selected combination must be Page-high-cost. Under equal defects and `R=o((log y)^2)`, a fraction `1-O(R^-1/2)` of the entire selected Fourier subspace is forced expensive. The live theorem is now **collective arithmetic realizability of that expensive subset spectrum**—a source-cost/multiplicity, occupancy or quotient-capacity law—not another generic coding inequality.

These lines make the cross-line discriminator for “more source structure” sharper. Farey shows that even a physical support/amplitude alphabet can leave a saturating sign assignment; Arithmetic Fidelity shows that a scalar sign budget factors through prime-gap occupancy and can be traded against mass concentration; Möbius shows that an endpoint partition can force almost an entire subset spectrum to be expensive; Nyman shows a source relation whose exact destination-aware margin can be converted into a quantitative rank--height budget. The useful question is therefore which **joint source correlation/concentration law is actually consumed by the endpoint**, not how many syntactic constraints the source satisfies.

## Which exact source incidence survives support relaxation in Weil Inertia?

WI-288 reduces complete one-signed symmetric two-island screening to the nearest-prime-power Lambert gap, WI-289 identifies prime squares as the unique critical composite layer, WI-290 gives at least `X^(3/8-o(1))` positive-length composite-safe components, and WI-291 confines eligible ordinary-prime upper hosts to only `X^(1/10+o(1))` rare giant-gap intervals.

WI-292 localizes the reservoir against those hosts. After enlarging each host by one Lambert radius, integer-power spacing bounds show that only `O(X^(1/10+epsilon))` composite-power cuts can meet the eligible host family. Hence the full screened candidate set has at most that many components, and only an `X^(-11/40+o(1))` fraction of the global lower safe-component reservoir can even enter an upper host.

The bottleneck is therefore no longer global abundance. It is a **rare-host cross-scale incidence theorem** on the surviving `X^(1/10+o(1))` candidate pieces. Sharpening the global lower-reservoir count cannot close the branch. Null-equation amplitudes/signs or a different support topology remain separate escapes.

## What should Prime Circle do after the complete diffuse one-profile absolute spectrum classicalizes?

PC-285 is the unconditional matched-control boundary and PC-286--PC-290 give the conditional sub-square-root spectral collapse. PC-291--PC-294 separate microscopic relation onset, worst-case local amplitude, averaged local amplitude and total Hilbert--Schmidt mass.

PC-295 now closes the remaining absolute spectral-shape escape under `(log q)^2 sqrt(q beta_q/N)->0`. The entire ordered singular-value vector of the normalized boundary-log matrix converges in expected `ell^infinity` distance to the deterministic classical Green sequence `(1/2,1/2,1/4,1/4,...)`. Prime and count-matched Li-grid sources satisfy the needed diffuseness at `N~sqrt(q)`. Thus higher absolute spectral moments, edges and small-singular-value statistics are functions of a profile that has already classicalized.

Any surviving prime signal must lie **before or outside absolute one-profile singular-value compression**: orientation/phase, signed or nonnormal structure, joint multi-profile data, cross-level/source coupling, or an exceptional non-diffuse regime. Reprocessing the same absolute spectrum is closed.

## What survives after regular-CA placement and generic local cancellation cease to be bottlenecks?

RE-119--RE-123 show that regular CA placement and every fixed algebraic edge hierarchy collapse to a stable profile whenever weighted near-edge mass is sufficiently flat. RE-124 localizes cancellation around an individually strong atom; RE-125 excludes the sub-reciprocal reinforcing core; RE-126 turns the remaining annular mass into a distinct logarithmically comparable near-edge companion with vanishing normalized ordinate gap.

RE-127 now preserves the sign of that localization. A hidden strong atom forces at least one such companion to have **negative target-demodulated center projection** of size `gg |a_0|/log U`; equivalently its scaled signed gap plus the source-coefficient phase lies in a fixed cancellation arc modulo `2pi`. The strong-atom branch is therefore no longer “close pair plus unspecified phase.” It requires an algebraically strong near-edge pair with both vanishing normalized spacing and a concrete cancelling-phase condition. The diffuse branch, where no individual coefficient reaches threshold, remains separate.

## What is the real-elliptope scale of the centered symmetric-taper prime-phase torus?

VIS-213--VIS-222 reduce fixed-dimensional worst starts to unit-modulus torus optimization, close weaker magnetic/cycle certificates and show that the full correlation SDP is universally constant-factor equivalent to the exact torus objective.

VIS-223 removes an additional representation artifact for midpoint-symmetric tapers. Midpoint centering factors the taper transform as a vertex coboundary times a real even response, so the canonical Hermitian matrix is diagonally gauge-equivalent to a **real symmetric signed ratio-shell matrix**. For a real objective the complex correlation SDP collapses exactly to the ordinary real elliptope. The torus variables remain continuous phases, but generic complex edge phase is no longer a matched property of this branch; cycle holonomy reduces to sign parity.

The live static test is therefore `U_abs(B_(y,H))/sqrt(R_v)` for the real signed prime matrix, with controls preserving edge magnitudes and the signed-shell representation. By VIS-222, boundedness or divergence transfers within a universal constant to the exact static worst-start objective. Rank-one stress is an optimizer-recovery issue and one-parameter access time remains separate.

The cross-line lesson with Weil Positivity is now more concrete. Visual Exploration shows that canonical gauge reduction can remove apparent complex geometry before the faithful optimization surrogate is evaluated; Weil Positivity shows that canonical orientation and subleading spectral data can invalidate an apparently correct leading Gamma-like response. In both cases, **representation choices must be audited before their geometric agreement is counted as arithmetic structure**.