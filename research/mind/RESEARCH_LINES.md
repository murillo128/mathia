# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal, noncompact or finite-degree Julia-trivial scalar geometry?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity brackets the natural scalar moving-boundary escapes tightly. WP-314 makes the canonical empty-side moving Julia tangent universal. WP-315 shows that the intrinsic support-side Mangoldt atom scale is not locally compact because bounded prime clusters can collapse arbitrarily many comparable atoms. WP-316 closes the opposite isolated regime: if one pole really dominates, scalar Julia reduction turns the normalized response into a removable constant.

WP-317 closes every fixed finite-multipole repair in the same scalar category. A regular Julia step sends `N` positive atoms to `N-1` positive interlacing atoms and repeated reduction terminates; reverse augmentation is classical rational Herglotz continued-fraction data. The surviving scalar route must therefore produce a canonical **genuinely unbounded/infinite collective geometry** that is locally controlled, source-specific and Gamma-compatible, or leave the audited scalar category.

## Which source resources survive into the exact destination margin, topology, quotient and moving resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) keep conditioning, source supply, destination sensitivity and quantifiers distinct.

Arithmetic Fidelity now has both an exact moving-bandwidth law and an exact bounded-ratio scalarization law. AF-359 shows that if `delta log T->u`, the smoothed critical shell retains fraction `e^-2u` of endpoint energy and the difference carries `(1-e^-u)^2`; order-one absolute detection begins only at `T=exp(Theta(1/delta))`. AF-360 removes the obvious normalized-phase escape: on every bounded-ratio shell the Riesz multiplier is asymptotically one common complex scalar, so projective shape remains endpoint-blind for every path `delta->0`, `T->infinity`. Relative amplitudes across shells also need exponential separation for an order-one effect.

Prime Lattice gives a complementary exact threshold for a moving physical observer. PL-316 shows zero-extension `H^{1/2}` can be arbitrarily small while a moving Green diagonal extracts order-one mass; PL-317 shows every `H^{1/2+eta}` gain is sufficient to kill that sample. Nyman--Beurling now makes its worst-cell source currency equally explicit: NB-142 shows that a logarithmic-rank polynomial confluence cell forces an order-`log G` positive jump of the zero-count remainder `R(T)`. A sublogarithmic short-increment law on those polynomial scales would close the current packet obstruction even though global density alone does not.

The common requirement is a **target-aware coupled-scale modulus in the topology, quotient, bandwidth, local source law and quantifier actually consumed by the endpoint**.

## Which growing source law is informative rather than under-resolved, source-reconstructing, shareable or generically realizable?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy and Prime Circle now show that exact source capacity and stable usable information can diverge in both directions. FD-142 shows one fixed scalar with `Theta(log T)` bits reconstructs the squarefree prefix, while FD-143 proves that arbitrary source-independent observation schemes with total prime-cell budget `o(sqrt(log log T))` can still be fooled by one common infinite prime coloring on a subsequence. PC-306 is even more extreme algebraically: at prime conductor the exact first centroid already identifies every uniform vertex subset, yet a support and its disjoint one-step translate have first `K=o(q)` Fourier packets only `O(K/q)` apart. Exact injectivity can therefore coexist with divergent analytic conditioning.

Möbius Cancellation separates source nonshareability from generic representation geometry. MC-312 forces a redundancy/source-scale tradeoff for a common lower-prime package. MC-313 sharpens the generalized-weight hierarchy but also proves that its current coarse scale is compatible with ordinary rate-one-half binary codes. The next theorem must use **arithmetic realizability**, a sharper second-order source budget or a genuinely joint hierarchy rather than another generic coding inequality.

The live cross-line question is not “how many coordinates are retained?” but **how many admissible source states remain stably distinguishable at the actual precision, how that resolution is produced, which costs can be shared, and whether a generic ambient object already realizes the same invariant**.

## Which support geometries survive the one-signed Weil constraint after compact-window verification?

WI-300 independently verifies Zhu's pinned compact-window positivity with `lambda_0.8>=8.9e-18`. WI-301 removes first-crossing minimality: any nonzero one-signed compactly supported test that screens the powers-of-two ladder has `q_arch(f)>=8.9e-18||f||_2^2`. A nonpositive test must therefore repay a uniform reserve through unscreened prime-power interaction. The live question remains where that reserve must occur and whether it can be converted into coercivity once sign changes are allowed.

## Can target-relative actual-zero geometry close the Robin packet after transfer is removed?

RE-139--RE-142 build a rightmost-anchored Turán certificate and show that the current upper source envelopes permit the generic transfer loss `G(U,T)=T^-2+o(1)` even when retained-core complexity is negligible. RE-143 proves that this loss is not intrinsic to classical power-sum observability: anchoring at the original strong atom and applying Turán's First Main Theorem to zeros on or to its right gives a witness with no rightmost transfer factor.

The price moves into a one-sided cancellation remainder. For an algebraically strong target, zeros farther than `H log U/U` to the left are negligible to arbitrary algebraic order, so only a shrinking near-left slab can cancel the rightward witness. The live theorem is therefore target-relative: control signed Robin mass, occupancy or phase cancellation in that `O(log U/U)` left neighborhood while separately paying rightward mode complexity and the high-height tail.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225--VIS-231 fix the subcritical static order and the deterministic/marginal/dependence baselines for the critical collision count. VIS-232--VIS-235 show how a higher-order statistic can escape a **generic** first-order conditional type. VIS-236 then proves that the actual ordered prime-coordinate representation is much thinner: its labels are nondecreasing, the lag-two statistic is already an occupancy functional and the endpoint-matched first-order type has one realization.

The live control must therefore randomize a genuinely variable monotone object—ordered counts, normalized gaps, within-cell positions or another increasing-point-process coordinate—while keeping both the statistic nondegenerate and the sample space representation-faithful.

## Xi Flow: move from local known-window inversion to global support discovery and zero-sensitive transport

Xi Flow has a uniformly conditioned local nonlinear holomorphic chart for known consecutive packets with linearly many samples. The live problem remains global: locate and recenter unknown windows without an oracle, assemble separated or multiscale packets, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side source control to a zero-sensitive quantity.

## Global discriminator: price what the endpoint sees and what the representation can actually realize

The newest evidence strengthens one common distinction: **retained information is meaningful only relative to observer topology, moving resolution, source-state capacity, provenance, closure/incidence structure, representation support, ambient realizability, target-relative geometry and worst-case quantifier**. AF-360 shows normalized shape can survive while the endpoint discriminator remains hidden behind common scalarization; FD-143 gives an explicit growing blind-capacity regime; PC-306 separates exact algebraic recovery from stable Fourier separation; MC-313 shows generic codes can realize the coarse hereditary profile; NB-142 converts a worst-cell obstruction into a precise short remainder-increment budget; RE-143 relocates a global transfer loss into a thin target-relative slab; VIS-236 rejects null freedom outside representation support; WP-317 rejects fixed finite collectivity as generic rational Herglotz geometry.

For every proposed route ask: **what is the source discriminator; how many source states are stably distinguishable at the actual precision; is that resolution source-native or a disguised label; which quotient frequencies see it; what scale does the observer sample; what closure or overlap makes source costs reusable; can a generic ambient object realize the same invariant; which exceptional configurations remain possible; what topology and normalization retain the signal; which sigma-field does the null preserve; is the null support faithful; what target-relative neighborhood can still cancel the witness; and does a local source scale have controlled genuinely nonclassical collective geometry?**

The productive next theorem is the one that closes one of those exact failure modes with source-native information rather than relabelling it.