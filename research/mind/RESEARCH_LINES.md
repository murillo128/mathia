# Global mathematical questions

## Can the source-native pole tangent be lifted to a canonical completed operator with the correct sign?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate continuum provenance, spectral layers, model selection and sign.

WP-267--WP-277 rule out a sequence of free constructions: local positive hosts, inverse reconstruction from an already supplied target, exact bounded pure-point-to-a.c. intertwining, and bounded/subcritical mixing of compact-resolvent sectors. WP-278 then gives the surviving infinite-volume category a source-native measure-level realization: the critical Mangoldt half-density has high-energy tangent `cosh(x/2)dx`, exactly the polar continuum of the completed carrier.

WP-279 shows why the same scalar tangent expansion cannot simply continue to Gamma. After the pole term is removed, nontrivial-zero modes lie parametrically above the Gamma layer, even under RH. WP-280 tests the canonical primitive escape: retaining the critical Chebyshev primitive really restores the central Gamma slope, but the normalized logarithmic-primitive limit that isolates it is itself equivalent to RH, as is eventual sign of the same primitive.

WP-281 closes the fixed finite-state local compromise. Even under RH, a stable local regularizer leaves a nonconstant uniformly almost-periodic zero field. Any further fixed finite-dimensional stable linear response has a rational transfer function; retaining the Gamma zero mode while forcing convergence would require that rational function to vanish at every nontrivial-zero ordinate.

WP-282 closes the most canonical positive infinite-dimensional enlargement too. An arbitrary positive continuum of exponential relaxations has a Stieltjes transfer with `Re H(i gamma)>0` at every finite frequency, so it cannot annihilate even one zero mode while preserving the Gamma DC component. Infinite dimension therefore does not help if the response remains completely monotone/positive-relaxive.

The remaining burden is genuinely global and category-changing: construct an independently selected operator/boundary/cohomological architecture that couples finite and archimedean sectors before scalarization, or an independently forced oscillatory/non-Stieltjes infinite-dimensional, scale-nonlocal or nonlinear mechanism whose sign can be audited without subtracting the zero divisor or importing an RH-equivalent primitive limit/sign condition.

## Which source restrictions actually shrink the target-reachable failure set at the metric the theorem consumes?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) distinguish source reduction, exact failure incidence, exact recovery, non-vacuity and stable distance from failure.

AF-300--AF-305 replace ambient deleted directions by intrinsic source dimension, reachable positive bodies and a sharp discrete saturation boundary. AF-306 proves that every fixed saturated support can approach zero arbitrarily closely. AF-307 classifies the first prime-support exact cases, AF-308 makes the ambient exact-hit cone Lebesgue-null, and AF-309 proves that the same cone is dense with dense complement. AF-310 isolates the elementary relation obstruction: a normalized common-time distinct-prime phase point has a cyclic integer relation lattice of rank at most one, excluding the forced rank-two zero geometries at `k=3,4` but not the pentagon. AF-311 strengthens the source side with Baker theory and still finds generic pentagon compatibility. AF-312 adds the first nonlinear post-hit rigidity: a hypothetical five-prime hit cannot repeat at harmonics `2,3,4,5`, and any fixed integer time lattice contains only finitely many hits.

AF-313 separates first incidence from post-incidence information loss: on the five-point unit-circle zero fiber, `(s_2,s_3)` reconstructs the entire unordered phase multiset except for the rotated-regular-pentagon fiber, which distinct rational-prime phases exclude at nonzero time. AF-314 then removes the remaining common-phase gauge: `kappa=s_2^3/s_3^2`, continuously extended by `kappa=0` on that exceptional orbit, is a complete coordinate for the unordered shape modulo common rotation. The generic quotient is real two-dimensional, so one complex coordinate is dimension-tight among continuous Euclidean summaries even though recovery is still nonuniform near `kappa=0`. Thus the open `k>=5` problem is specifically **first nonlinear nontorsion incidence**; neither linear relation theory, recurrence sparsity nor post-hit exact quotient recovery decides whether the first hit occurs, and raw harmonic count is not by itself the intrinsic information bill.

Prime Circle gives a matched-control analogue. PC-272--PC-276 show that ordered information, the raw near-resonance law and every finite fixed-bandwidth logarithmic centering can all be classical source data. PC-277 closes the intrinsically normalized scalar law whenever `log B=o(sqrt(log q))` using a denominator-matched exact-`Li` control, while `(B+1)^2>q` forces universal exact rational resonance. Only the intermediate rational-microscopic scalar window remains unresolved before one must leave that observable.

Prime Flute sharpens the metric distinction. PF-315--PF-318 reduce the full physical angle to `T_H` and `Z_LH`, localize the direct channel, and identify `O((1+log P_n)/P_n^2)` local squared Hilbert--Schmidt mass as endpoint-complete. PF-319 proves that reciprocal-prime form-smallness need not control a **raw mixed seam-normalized precision block**. PF-320 then proves that the actual intrinsic direct angle is stable anyway: multiplicative closeness of the whole positive local forms gives `O(beta_n)` angle perturbation and therefore exactly the PF-318 mean-square budget, independent of the external seam anisotropy that defeats the raw-block shortcut. The remaining direct theorem is the shear-deleted local pant-angle envelope with actual seam energy and full common completion, not transport of the shear correction through the raw block.

Visual Exploration sharpens the observation-scale bill in another direction. VIS-200 proves that changing the sieve/taper cutoff cannot improve the leading four-form pointwise threshold. VIS-201 then removes that pointwise local-factor loss by averaging the nondegenerate three-polynomial conductor fibers with discriminant-uniform Nair--Tenenbaum bounds. VIS-202 closes the remaining scalar-cutoff freedom: within the resulting dimension-four fiberwise sieve plus taper-tail majorant, some cutoff succeeds if and only if `log H/sqrt(log y)->infinity`. VIS-203 then tests equal-difference aggregation for a broad autocorrelation-taper class and proves that every exact shell coefficient is nonnegative: multiplicities within one `xi` reinforce rather than cancel, and subquadratic windows do not attenuate the dangerous `y^-2` shells. The residual problem is shell packing, cross-shell interference, a genuinely signed taper, or a different dephasing resource; the square-root-log boundary remains a method threshold, not a proved threshold of the true collision energy.

## Which residual cancellation theorem survives after generic abundance, exact embeddings and conductor descent are exhausted?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) place Farey and Möbius beyond their previous generic barriers.

FD-083--FD-085 show that full-counting occupation and polynomial additive blocks do not imply a finite-entropy multiplicative chain. FD-086 restores the physical source and makes the normalized Jordan/Schur packet coherent throughout each frontier-length block. FD-087 separates exact embedding from useful normalized capacity, and FD-088 escapes the small-anchor capacity bound with repeated-prime deflation. FD-089 compresses the row-adaptive packet to canonical prime contractions. FD-090 then removes the predecessor-count entropy loss itself: after normalizing each lower mask by its natural scale `A_p^(2Theta)`, the prime weights are summable and one predecessor carries a fixed positive fraction of the parent **frontier-normalized** energy for every fixed admissible contraction band.

FD-091 localizes the remaining recursive-eligibility failure. Every odd `p`-channel can strip its second prime copy and reembed four-adically at scale `4H/p^2`, preserving the quotient shell, restoring a nonsquarefree mask and improving the normalized family to `p^(-4 sigma)` weights. The same universal repair is scale-neutral for `p=2`. Farey therefore needs a fixed lower-state mask fraction/composition law on the odd branch or a theorem for the dyadic terminal carrier, not more predecessor counting.

MC-252--MC-256 eliminate nonlocalized fixed-order residue rigidity. MC-257 removes the wild prime-square direction from the moving shell, and MC-258 shows that descent from `R^2` to `R` is exactly offset by the projector's exterior `1/R`, preserving the support exponent. MC-259 compresses the structured smooth-modulus obstruction to at most one blind pretender, while MC-260 shows that the closest support-sensitive sparse variance theorem starts only above the useful modulus range. MC-261 identifies the other exit exactly: full shell generation is equivalent to every reduced class modulo `R` having a `y`-smooth representative with `y=(1+o(1))log R`.

MC-262 now closes the ambient growing-order quadratic-large-sieve route. Liu's explicit uniformity proves parity balance through `t=o(log^(4)y)`, but the first support where a blind relation can survive is already of order `log log y`; beyond the fourth-iterated-log scale the explicit loss plus ambient-modulus wastage consumes the global saving. The remaining analytic exit is a shell-product-family theorem uniform at the actual `log log y` support scale, or direct control of the single source-selected blind mode.

Both lines now isolate a source-scale endpoint after generic bookkeeping has been removed: Farey needs recursively eligible normalized transport with a dyadic exception, while Möbius needs shell-specific logarithmic simultaneous generation or signed/product-family cancellation at the actual source modulus and order.

## Can source-specific regularity and source-specific order become quantitative enough to control a Weil crossing?

Prime Lattice and Weil Inertia now separate generic regularity/order structure from what is selected by the actual source equation.

PL-285--PL-292 move from inverse-log ambient transport through graph-domain `log^-2` transport to optimized all-log subpower transport for actual eigenstates. PL-293 supplies a sharp generic endpoint obstruction: the optimal zero-exterior logarithmic-Laplacian boundary law has amplitude `ell^(1/2)`, and any nonvanishing channel of that scale excludes `H^s` for `s>=1/2`. PL-294 independently closes the standard positivity-improving shortcut: the first Beurling--Deny criterion for the full localized Weil form holds exactly only up to `a_BD=(1/2)log rho_pl≈0.1406`, strictly before the first prime-power shift. At `a=0.8` ordinary semigroup positivity can no longer certify ground-state sign. Neither result decides the actual eigenbranch; the remaining gain must be zeta-specific boundary cancellation, a source-specific sign theorem, a different derivative identity or a bypass of the Hopf/regularity route.

WI-264--WI-269 show that finite-lag screening and generic first-crossing substitutes fail to capture Suzuki's source equation; every nonzero localized null mode is prime-translation support-dominating, ordinary positivity preservation fails, but the exact multiplier ground-state form is PSD and yields a nodal compensation tariff. WI-270 sharpens the one-signed branch: every admissible cut must be paid either by prime-power crossing overlap or by bilateral short-range mass. WI-271 makes the source sign-change scale decisive: an internal one-signed gap of width at least `h_0=log rho` forces positive von-Mangoldt cross-gap mass.

WI-272 turns that hard gap threshold into a quantitative tail budget. For a one-signed internal gap of width `L<h_0`, the only positive continuous-source payment is `B(L)=int_L^h0 w(h)dh`, and complete prime screening requires `B(L)^2>=lambda_(r_L)lambda_(r_R)` together with a compatible left/right mass ratio. Because `B(L)` vanishes quadratically at `h_0`, every proper-aperture margin has a strict exclusion collar below the sign-change length; because it diverges logarithmically as `L->0`, narrow or gapless screening remains live. The one-signed residual is therefore short-range connectivity strong enough to pay every cut, while the sign-changing branch still requires control of long-range archimedean compensation.

The shared lesson is not that regularity and order are interchangeable. It is that generic operator structure has now reached explicit sharp controls, and the remaining gain must come from how the actual arithmetic source selects and couples its state.

## Does nonsummable Blaschke structure force nonterminal Nyman entropy, or must the radial proof change carrier?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate stationary inverse calibration, radial floors, terminal entropy and the actual visible future geometry.

NB-082--NB-085 reduce persistent continuation to finite-future prediction, construct the exact source-specific Toeplitz certificate along integer-dilation descendant rays, and show that every such ray has a strictly positive Szegő prediction floor. NB-086 then lower-bounds the aggregate radial charge by terminal visible Gram entropy, and NB-087 extracts a canonical q-adic scalar sibling-energy distortion whose squared mass already pays part of that bill.

NB-088 makes the dyadic scalar channel source-exact. After restoring the off-critical inner factor, the raw terminal innovation has `||J_X \widetilde D_j||^2=1-1/X`; all arithmetic deformation is a nonnegative causal Blaschke-delay energy. Every fixed finite Blaschke divisor is silent at total squared scale. NB-089 extends this to the full infinite product whenever the total horizontal off-critical mass `sum_(Re rho>1/2)(Re rho-1/2)` is finite: terminal delay remains `O(j^-1)` and the aggregate scalar distortion still vanishes.

NB-090 closes the whole current-window matrix escape under the same hypothesis. The raw terminal Gram is exactly a rank-one perturbation of identity; the Blaschke deformation changes terminal pairings only by `O_A(R^-1)`, and causal inner order preserves a uniform spectral floor. Hence the normalized full terminal determinant deficit stays `O_A(1)`. Sibling contrasts and cross-block correlations inside the terminal block cannot make the NB-086 lower bound diverge in the summable-horizontal-mass regime.

NB-091 shows that moving to any other **fixed multiplicative depth** still does not recover a source signal after calibration. For the raw band `m,...,R-1`, `det K_tilde_(m,R)=m/R` exactly; on `m=ceil(R/q)` with fixed `q`, the normalized raw correlation entropy is `R Phi_q+O_q(1)` with `Phi_q>0` for `q>=3`. But under summable horizontal mass the actual Blaschke-deflated entropy differs from that universal raw baseline by only `O_A,q(1)`. The linear divergence of an uncentered fixed-ratio determinant is therefore a calibration artifact, not off-critical information.

The next theorem must therefore either exclude the summable-horizontal-mass regime with a stronger zero statistic, or make source information visible at a **growing horizon** `q=q(R)` with quantitative control of the deteriorating fixed-`q` constants, or move to a genuinely inter-scale statistic coupling different bands. Earlier indices at another constant depth are no longer enough; fixed-ratio contiguous-band Gram determinants are exhausted after universal calibration.

## Which ordinary-prime or selector law survives after exact mixed-depth incidence itself becomes noncoercive?

RE-083--RE-088 reconstruct selector geometry, isolate anchored gap costs and remove sufficiently deep selected boundaries under an off-RH exponent gap. The challenged RE-089 statement has been withdrawn. RE-090 removes fixed same-depth higher-layer chambers above `Theta>0.57`, and RE-091 combines sparse depth-`>=3` hosts with Stadlmann's prime-gap second moment to force a first-layer boundary atom on almost every surviving cell once `Theta>469/600`.

RE-092 shows that simply pairing the two anchored gap **magnitudes** of a mixed `1/r` cell does not improve the RE-091 exponent. For the residual depth-2 channel the scalar host/moment barrier is `173/200`.

RE-093 then preserves the exact cross-scale identity that RE-092 discarded and still reaches the same obstruction. In a PNT-dense common integer source, the depth-2 map `q -> x_q=eta_1^(-1)(eta_2(q))~q^2/2`, the oriented adjacent first-layer boundary, and full generalized event-staircase adjacency can all be realized while saturating the Stadlmann gap-square budget exactly through `Theta<=173/200`. Thus abstract same-source incidence, orientation and event adjacency do not add coercivity beyond the scalar moment package.

RE-094 quantifies the next ambient short-interval route instead of merely invoking it. If intervals of length `X^vartheta` have bad-start measure `X^(m(vartheta)+o(1))`, the residual depth-two threshold mass vanishes only when `Theta>max(1/2+vartheta,m(vartheta))`. Beating `173/200` therefore requires some `vartheta<73/200` with a genuine power-saving exceptional exponent `m(vartheta)<173/200`. Current general Gafni--Tao majorants do not certify this near the frontier, while almost-all results with exceptional size only `X(log X)^(-B)` remain polynomially too large despite reaching much shorter intervals.

In `469/600<Theta<=173/200`, the next Robin theorem must therefore use information the ambient integer-start controls do not have: **ordinary primality/multiplicative arithmetic at the sampled points `x_q`, a gap/short-interval theorem conditioned on `q` prime, standard/reciprocal prime-race coupling, accumulated CA height, skipped-span constraints, or actual rightmost-maximizer compatibility**. Such source-conditioned information can bypass the RE-094 exceptional-mass max law rather than merely optimize interval length. For `Theta>173/200`, the existing depth-2 host/moment estimate already makes the channel negligible; below `469/600`, the finite mixed-depth reduction remains the weaker carrier.

## Can the shear-deleted direct flute angle and the conditional channel satisfy the required ideal counts?

PF-316 removes the residual-source ambiguity at the endpoint: the physical angle is weak trace exactly when `T_H` and `Z_LH` are. PF-317 localizes the direct channel and PF-318 shows that logarithmic target rank makes a local squared Hilbert--Schmidt budget `O((1+log P_n)/P_n^2)` endpoint-complete.

PF-319 proves that the PF-232 relative form sandwich cannot be converted into a stronger **raw seam-normalized mixed-block** bound without controlling the metric conversion. PF-320 bypasses that target instead of repairing it. The intrinsic low/high angle of a positive form is Lipschitz under the multiplicative form sandwich, so the hypercycle-shear correction itself already has squared Hilbert--Schmidt mass `O((1+log P_n)/P_n^2)` in the actual angle metric, independently of the raw seam amplification. Positive local forms reassemble by contraction sandwich.

The remaining direct burden is therefore a weak-`S_1` envelope for the **shear-deleted local pant angles** with actual seam normalization and full common finite-pant completion. Once that reference family is controlled, PF-320 makes the shear perturbative at the endpoint. Independently control `T_H`, then close the compatible infinite realization, symmetric side and signed target composition. Constant-mode pant blowup remains a required negative control.