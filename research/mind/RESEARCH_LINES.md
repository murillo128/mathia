# Global mathematical questions

## Can a source-native signed transformation become admissible without erasing the localization needed by Weil positivity?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate source provenance, admissibility and target sensitivity.

Weil Positivity has closed positive additive preservation, scalar nonlinear post-processing and the entire finite canonical dilation-coboundary hierarchy as easy escapes. WP-290--WP-291 show that finite source-native pole killing preserves Mangoldt localization but leaves temperedness exactly RH-equivalent.

WP-292 supplies the complementary failure mode: counting convolution removes the nontrivial-zero poles and permits unconditional temperedness after finite differencing, but only by replacing the prime-power carrier with an all-integer one. WP-293 closes exact coefficientwise scalar relocalization: Dirichlet-convolution uniqueness forces the inverse kernel to be Möbius, whose transform is `1/zeta` and therefore restores the signed reciprocal-zeta zero screen.

WP-294 closes the apparent “coarse coefficients, exact final form” loophole in the same architecture. Equality on the full Weil autocorrelation cone forces equality of the underlying distributions; the isolated log-integer atoms then force exact Mangoldt coefficients, and WP-293 again forces Möbius inversion. A smooth non-finite remainder cannot hide atomic errors without importing compensating finite-place atoms.

The open theorem must therefore leave **direct one-channel scalar arithmetic convolution**, not merely relax coefficient accuracy. Derive a genuinely multi-channel, operator-valued, non-translation-invariant/boundary-conditioned or nonlinear upstream coupling that becomes admissible unconditionally while preserving enough arithmetic localization for the exact Weil form. An approximate/restricted intermediate criterion remains possible only if a separate stability/equivalence theorem reaches the exact canonical endpoint.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) separate source-resource pricing, destination sensitivity and relaxation sharpness.

Arithmetic Fidelity now has zero joint stability margin and no finite exponential-sum Bézout certificate at the exact eight-prime target. AF-330 forces any exact hit to have exponentially algebraic common parameter `-it`; AF-331 shows that this exceptional real-time set is countable but dense; AF-332 proves that at nonzero real time at most two prime-phase coordinates can be algebraic. AF-333 adds the sharp conditional closure: under Schanuel, the common-time phase tuple has algebraic codimension at most one, while a unitary zero already forces two independent polynomial relations.

AF-335 separately fixes the information budget on the even middle-singular branch. Near a regular `2m`-gon the source condition `e_m=0` costs one real dimension, but forgetting the product phase makes its low-harmonic image lose two; the harmonic map therefore has one-real-dimensional local fibres and adjoining `E in S^1` is dimension-minimal as a continuous exact lift. This sharpens **post-incidence fidelity** without helping the still-unconditional first-incidence problem: dimension deficit is not nonintersection, and exact one-phase repair is not a stability margin.

Prime Flute has a concrete destination-resolution test rather than only an aggregate normalized commutator, but PF-340 makes its true scalar target two-sided. PF-337 identifies positive Fourier off-diagonal mass, PF-338 compresses the finite-boundary odd sector to the half-cuff cross-parity defect, and PF-339 reaches seam-width scale. PF-340 then proves that the symmetric raw response on cuff `n` is `(A_n^-+A_n^+)/2`, so the explicit two-mode test is `Delta_n^-+Delta_n^+=o(w_n)`, not a one-pant estimate. The `r_n->0` source geometry controls the right pant only. The cheapest falsifier must therefore prove common sign, dominance, or a direct seam-scale lower bound for the **two-pant sum** before higher modes are relevant.

Nyman--Beurling has now closed the one-zero true-multiplicity rescue inside its terminal-band/full-backfill geometry. NB-111 identifies the exact Laguerre representation with intrinsic depth `T=2(1-a)log(m/r)` and proves target-poorness whenever `d/T<1/8`, uniformly including `d<=(1/8-epsilon)log G`. NB-112 then uses off-critical functional-equation symmetry plus the Bellotti--Wong zero-counting constant: a zero `beta+i gamma` is paired with a distinct same-ordinate zero of equal multiplicity, so `mult(beta+i gamma)<=(0.10076+o(1))log gamma`, strictly below `1/8`. Thus the full derivative packet supplied by any sufficiently high single off-critical zero is target-poor. The surviving zero-state route must obtain rank from **several distinct zeros** or leave the NB-111 geometry; improving the one-zero Laguerre constant is no longer needed merely to defeat actual multiplicity.

## Which cancellation theorem remains after fixed local algebra, scalar decoding and generic perturbative shortcuts are closed?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track the relocated endpoint costs.

Farey Discrepancy has localized the critical transport loss to the terminal `H/p` prime fan and closed progressively richer fixed-local escapes. FD-111 reduces every fixed polynomial in finitely many local multiplicative shifts to the same odd/even power-source algebra. FD-112 then shows the two-child mediant split has only one residual shape coordinate after conservation. FD-113 extends that classification to every fixed or predeclared finite Stern--Brocot descendant template: all affine-normalized descendant positions and gaps are rational functions of one split scalar. Higher-dimensional Plücker minors may be nonzero because this one-scalar image is curved, but they do not certify a second information coordinate. A surviving refinement route must add an independently defined discrepancy/source residual, state-dependent ancestry, cross-horizon persistence or another genuinely nonlocal carrier.

Möbius Cancellation has closed positive and signed **full-cube pointwise scalar decoding** at the relevant source depth, and MC-281 makes the surviving arithmetic-image route exact through the virtual endpoint leverage/synthesis cost. MC-282 shows this certificate is stronger than nonblindness itself. MC-284 now adds that the actual localized shell remains Boolean-complete on every coordinate set with effective conductor `Q(U)<=exp(b sqrt(log y))`: every prescribed Legendre cell occurs abundantly, even allowing the Page exceptional mode. Hence any Christoffel certificate with empirical energy below one must import coordinates with conductor beyond that stretched-exponential window. The residual is not merely “use localization”; it is **explain directional separation using high-conductor/collective shell information not already saturated by Page--Siegel pattern realization**, or find a source-native operator inverse.

Prime Lattice has moved from generic perturbation failure to a sharply source-structured forcing gate. PL-301 shows the actual Suzuki remainder is BV-stable; PL-302 proves the arithmetic shift comb is not contractive once the `n=3` channel activates; PL-303 proves that even the exact odd spectral gap cannot upgrade ordinary `L^2` forcing to `BV`; and PL-304 extends the same logarithmic-versus-linear frequency obstruction to every ordinary `L^p`, `1<=p<=infinity`, and to the full finite-measure forcing space. The missing theorem must therefore exploit a **proper source-generated range** of the differentiated commutator/eigen-equation, an exact cancellation or compatibility law, a graph norm that already charges first-order frequency, or a direct branch-variation principle. Generic ambient forcing-space strengthening is closed; the independent sign gate remains separate.

## Which exact null-mode operator constraint survives sharp support relaxation in Weil Inertia?

[MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track the shift from support geometry to operator-domain information.

WI-281--WI-283 prove that complete one-signed screening, all prime-power lags, full span and prime-translation support domination still leave the scalar packing relaxation sharp. WI-284 recovers the first-crossing equation at operator level: complete screening deletes the prime translation form on the whole selected support subspace, so the prime-deleted gamma-plus-pole form must be PSD there with bottom exactly zero. WI-285 shows that every fixed finite collection of prime bases is too weak: one can screen all their powers while retaining arbitrarily negative prime-deleted directions.

WI-286 begins using the growing active prime family through the nearest-prime width consequence and identifies the square-root exponent as the first-order spectral threshold. Its exact current best-unconditional numerical margin is under adversarial review, so that number is not used in global synthesis. WI-287 sharpens the invariant part of the argument: for `H(x)=sqrt(x)F(x)`, the width-relaxed two-island bottom is

`lambda_width=(1/2)log x-log F-F+O(1)`,

with transition `F_*(x)=W(sqrt(x))=(1/2)log x-log log x+log 2+o(1)`. Below `F_*-omega(1)` the compressed two-island class becomes positive; above `F_*+omega(1)` the width relaxation still admits explicit negative odd directions.

The next theorem must either cross this Lambert-`W` width endpoint or use information discarded by nearest-prime compression: simultaneous screening by the aperture-growing prime family, amplitudes from the null equation, richer support topology or another collective source coupling. A fixed finite lattice template and a scalar gap-width statement are now separately priced and neither is the full WI-284 constraint.

## What should Prime Circle do with an observable that RH itself makes non-prime?

PC-285 strengthens unconditional Li-grid reproduction of the exact boundary-log finite-section spectrum to the Vinogradov--Korobov source-transport range. PC-286 first pushed the same law conditionally under RH to `B=o(sqrt(q)/(log q)^6)`. PC-287 sharpens the deterministic singularity treatment: flat value clipping removes one transport logarithm and the actual square-summed logarithmic depth removes two more, giving RH-forced collapse whenever

`B=o(sqrt(q)/(log q)^3)`.

The one-profile source-placement branch therefore has a reversed diagnostic nearly up to the rational square-root scale: persistent order-one prime-versus-Li separation in that range would contradict RH, while collapse does not imply RH. The exponent `3` is a loss of the current clipping/transport proof rather than an intrinsic threshold. The remaining one-profile frontier is the residual polylogarithmic neighborhood of the square-root denominator scale or a different RH signature than separation. Otherwise the architecture should move to cross-level, multi-source, directional, noncommuting or richer observables.

## Which Robin recovery obstruction remains after the full attained edge face is shown compatible with the adaptive ray?

RE-112 sharpens the false-RH finite-edge source envelope to the true power `Y^Theta` for `1/2<Theta=sup Re(rho)<1`. RE-113 then projects the source onto the actual edge-zero phase `F_Theta` and locks the adaptive selector to that phase. Genuinely sublinear recovery becomes strictly super-minimal, while any fixed-constant near-minimal family must be macroscopic with order-one favorable edge phase; if the edge is not attained, such a family is impossible.

RE-114 tests the natural two-coordinate strengthening by projecting the reciprocal-Mertens error onto the same actual edge face. The selector forces both `F_Theta(log Y)=b lambda_Y+o(1)` and `G_Theta(log Y)=(1-b)lambda_Y+o(1)`. However the full attained edge face is uniformly almost periodic and the sign-correct exact mixed ray `bG_Theta=(1-b)F_Theta` with both coordinates bounded positively occurs **syndetically** in logarithmic phase. Joint leading-edge Chebyshev/Mertens compatibility therefore does not obstruct the selector by itself.

The live attained-edge theorem must now control **where the CA selector lands relative to those bounded-gap compatibility windows**, use lower-order source information beyond the leading edge face, or couple recovery geometry to selector placement in a way the almost-periodic edge model cannot preserve. The `Theta=1` branch remains the separate unconditional RE-109--RE-111 problem.

## What remains after the exact visual pair-difference cancellation taxonomy is exhausted?

VIS-211 suppresses the one-cancellation family in the strictly subcritical regime. The finite-start baseline is the surviving VIS-193 result: every deterministic start window of quadratic length inherits the long-start mean-square scale uniformly in its location. VIS-194--VIS-202 then show how coefficient-weighted collision geometry can beat worst spacing in substantial subcritical corridors.

VIS-212 was deleted as a duplicate of VIS-193, so it supplies no independent evidence. The frontier remains coefficient-weighted compression of the averaging horizon, genuinely pointwise coherent starts, and the critical `H asymp y/log y` regime. A new higher-cancellation hierarchy would require a new observable.
