# Global mathematical questions

## Which independently selected source object turns critical mixed-prime data into a nonlocal positive law?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) now separates nonlocal realizability from independent model selection.

WP-267--WP-273 show that hostability, category descent, scalar sign and localizable compression all fail as free routes to the desired Weil orientation. A surviving positive compression must be globally nonlocal and reconstruct continuum mass from arithmetic samples.

WP-274 closes the next obvious escape. Pole normalization turns the critical Mangoldt source into the positive Herglotz measure with masses `Lambda(n)/(n+1)`, and classical Clark theory can manufacture a model space on which the pole-completed form is identically zero. But the same inverse construction works for generic Poisson-finite singular Jordan data. A model space built *from the target sampling measure* is therefore a representation tautology, not an arithmetic sign mechanism.

The live bridge is now independent selection: derive an inner function, canonical system, de Branges Hamiltonian, matrix/noncommutative coupling, or equivalent nonlocal object from source/Mathia geometry **before** using the desired Mangoldt carrier as spectral data, and then prove that its pre-existing positive law reproduces the finite-prime, Gamma and polar sectors with the correct Weil orientation.

## Which information survives exact source fibers, weighted observation horizons, and defect-scale interfaces?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) now distinguish one-sided source restrictions from jointly energy-constrained source geometry.

AF-300 proves that a genuine complex-affine source dimension `r` changes the optimal leverage bill to `r^(-1/2)` but leaves every remaining complex direction essential. AF-301 moves to positive sources: coordinate deletion is governed by a fiberwise reachable cone, and three discarded multiplicatively independent prime phases can recurrently make that cone fill the entire complex response plane. Positivity without magnitude/normalization/discreteness/multiplicative constraints does not supply fidelity.

PF-313 identifies bounded source-weighted Green strength exactly with Schur-defect domination. PF-314 then proves that an **internal energy-normalized source sector of the same positive form** satisfies that domination automatically after sequential shorting. The source representation therefore decides whether diagonal inverse strength is a real gate or already controlled by positivity; external injections remain separate.

Prime Circle still supplies both sides of a quotient boundary: fixed-upper swap-invariant statistics factor through prime-ratio autocorrelation, while accepted ordered mesoscopic information does not. Its target relevance remains open.

VIS-192--VIS-194 separate Cesaro cancellation, coarse uniform finite-start control, and the coefficient-weighted effective start horizon. The deterministic relative error is priced by `D_v=(sum |c_lambda|^2/delta_lambda)/(sum |c_lambda|^2)`, not necessarily the worst `y^2` gap. The live arithmetic question is whether tapered prime-ratio energy forces `D_v=o(y^2)` or the downstream target already supplies `L>>D_v`.

## Which source cancellation theorem controls exceptional Farey scales or low-order Möbius quotient structure?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md) now places the two lines beyond their previous subpower/rank barriers.

FD-079 upgrades Farey energy occupation from equal power exponents to a fixed positive **logarithmic-mean** lower bound. The annular energy-weighted occupation has bounded logarithmic entropy, positive geometric/arithmetic mean, and exceeds every sufficiently small fixed threshold on a positive lower logarithmic proportion of endpoint scales. The remaining failure can only hide in sparse exceptional scales or in a mismatch between this recurrence currency and the stronger pointwise/local control consumed downstream.

MC-250 upgrades Möbius quotient control from torsion order alone to an exact order--multiplicity tradeoff. At level `ell^a`, the blind primary shadow is an `F_ell` code with dimension `m_(ell,a)` and minimum distance at least `D_y`, hence `D_y+m_(ell,a)-1` cannot exceed the selected shell occupancy in `1 mod ell^a`. Large order and large multiplicity cannot coexist freely. The surviving algebraic obstruction is concentrated in large **low-order** primary structure, or alternatively in the signed amplitude of the exact rough Möbius coset.

## Which source-specific inequality prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia continue to close complementary generic escapes.

PL-285--PL-288 separate the compact simple ground branch from the ambient inverse-log modulus and show that extraordinary `L^2` tail localization still does not transport the tiny endpoint positivity margin. The next theorem needs weighted Fourier-tail regularity, source-specific head--tail cancellation, or a direct interval certificate.

WI-259 reduces first-prime activation to exact positivity at `r=7/20`. WI-260--WI-261 show that the audited public paths do not certify the intended exact matrix. WI-262 adds an independent failure: the interval LDL judge double-updates off-diagonal Schur entries and admits an exact rational indefinite false positive. The next gate is therefore a two-stage independent certificate: exact theorem-object enclosure **and** a mathematically valid positivity judge with correct algebra and directed rounding. Only then does the one-prime phase cone become established input.

Weil Positivity supplies the global counterpart: even a genuine nonlocal positive representation carries no source-specific sign information if the analytic object was reverse-engineered from the desired carrier.

## Which scale-distributed residue survives after local and atomic structure is exactly factored out?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md) now has explicit refinement and carrier decompositions on both Nyman and Robin sides.

NB-078 resolves each `q`-adic refinement into nonnegative continuation and prediction information charges. Along a refinement ray the uncancelled continuation is exactly `J_m=J_0+sum(ell_r-c_r)`. A stable tail must create an unbounded cumulative excess of new continuation over new prediction; summable excess on one ray kills it. Mesoscopic localization remains a possible mechanism, but the arithmetic target is now the **increment imbalance**, not comparison of two final large determinants.

RE-081 removes every discrete higher-layer prime-power atom from the leading long-fan transport. Under the RE-080 long-stretch normalization, both total and reciprocal-weighted discrete contributions are negligible, and the complementary power-front law survives in the continuous selector-dilation measure. The live Robin theorem must therefore constrain that adaptive continuous source path or exclude the long same-block branch; discrete event sparsity cannot close it.

The common question is which source theorem controls the location or scale-by-scale balance of the residue that remains after exact local/atomic factors are removed.

## Does the complete flute response remain controlled after internal source strength is separated from angle?

PF-313 makes defect-scale source domination the exact diagonal Green criterion. PF-314 now shows that the criterion is automatic when the forcing is the residual coupling of an energy-normalized source sector inside the same positive form.

The immediate task is representation-theoretic: derive the actual canonical forcing through the same elimination used to define the post-low constant/high block. If it is internal and uniformly energy-bounded, the left diagonal gate is closed and the remaining burden is normalized angle, multiplicity/ideal counting, the symmetric reassembly side and signed composition. If it is an external injection or its source-energy norm grows, that map is the precise missing estimate. A raw Green-potential bound without this source classification is no longer the right target.