# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to Möbius, invisible curvature, universal fermionic support geometry, or programmable support deletion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate source provenance, target sensitivity, relational structure, reachability and source specificity.

Weil Positivity has closed scalar recovery, fixed positive multi-channel recovery, finite multiplicative-equivariant linear depth, flat site transport, faithful pointwise metrics and ambient output curvature invisible to the commuting source image. WP-300 now closes the most canonical scalar-projective upstream escape as well: full prime-relabeling symmetry leaves only flat or fermionic commutator holonomy. The fermionic branch is reachable and positively extracts the prime-power support, but it is universal Koszul/Clifford geometry reproduced by generalized-prime controls, does not absorb the archimedean completion and does not repair the global Weil autocorrelation sign.

The live curvature route must therefore introduce arithmetic non-scalar/projective/cohomological source structure or a forced nonseparable finite--archimedean geometry. “Non-flat and source-reachable” is no longer sufficient; the curvature itself must contain arithmetic information and remain useful at the final sign gate.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) keep conditioning, source supply and target sensitivity distinct.

Analytic Frontier now gives the same-profile cascade a sharp frozen-stage physical clock: odd-dilation return density is `Theta_G(ell^-2)`, while integer activation and local source resolution both turn on at `A~ell^2`. The remaining problem is not fixed-stage existence but source-energy non-excisability plus enough depth-uniform control to iterate those clocks.

Arithmetic Fidelity has reduced the finite-order Euler transport bill from quadratic to linear depth. AF-338 preserves higher-harmonic collision geometry and proves sign regularity through order `r` once `sigma>r-1`. The bridge is still destination-limited: this observation is non-prime-specific and remains in absolute convergence, so source-forced low sign complexity must still be consumed or transported toward a zero-sensitive region.

Nyman--Beurling has converted its inverse-height compression near-kernel into an exact interpolation problem. NB-117 shows that finite target access is equivalent to a high-zero confluent power sum approximating the unique weighted-energy minimizer `C+D/n`; when `R/G->infinity`, an order-one discrete fit must be represented by a continuum packet of norm `Omega(G)`. The live theorem is now a source-specific zero-power interpolation obstruction rather than a generic Gram-spectrum bound.

Prime Flute's current sharp-cutoff completed-angle branch remains noncompact: adjacent Fourier modes straddling the hard cutoff generate order-one canonical correlations on infinitely many separated modules. A viable continuation must change splitting/observable rather than sharpen the same compactness estimate.

Prime Lattice has removed the mixed Hadamard theorem from the scalar-stress gate. PL-307 derives the endpoint-square stress directly from the existing fractional Pohozaev identity plus the lower-order dilation generator. The analytic burden is now Pohozaev-admissible completed-Weil branch regularity, finite-`s` virial pairing and source-structured derivative convergence; the rational-prime sign of the resulting derivative remains separate.

## Which cancellation theorem remains after representation enlargement stops adding source information?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now has a complete information classification for its cumulative field. FD-117 makes the whole discrepancy field invertibly equivalent to the quotient-Mertens staircase; FD-118 compresses the needed same-horizon Fourier carrier to the divisor-closed floor-quotient skeleton `Q_N` of size `2 sqrt(N)+O(1)` and proves linear minimality on the formal source space. Extra same-field Fourier bandwidth is therefore not an independent arithmetic resource. A continuation must obtain a quantitatively stronger theorem from this reversible skeleton or leave cumulative Farey discrepancy as the source object.

Möbius Cancellation has converted its sparse odd-support escape into a one-character analytic interface. MC-289 shows that if `eta=N^2/(2^r sum n_s^2)` is not exponentially tiny, absence of the target odd relation forces one generated quadratic product character to have quantitatively large negative shell bias; at the natural Geelen-scale occupancy that bias is order one. The remaining theorem can attack one-sided character cancellation, effective occupancy/multiplicity spread, or near-injective conductor cost rather than full Boolean universality.

## Which exact null-mode constraint survives support relaxation in Weil Inertia?

WI-288 reduces complete one-signed symmetric two-island screening to the nearest-prime-power Lambert gap, and WI-289 identifies prime squares as the unique critical composite layer. WI-290 now proves that the composite-safe residual is not concentrated into a few exceptional intervals: the published prime-gap second moment already forces `X^(3/8-o(1))` positive-length safe components on `[X,2X]`.

The live obstruction is therefore a genuine cross-scale incidence theorem: a hypothetical giant ordinary-prime desert near `x` must systematically avoid a polynomial family of safe windows generated primarily by prime-gap geometry near `sqrt(x)`. Current support screening supplies no correlation between those scales; null-equation amplitudes/signs or a different support topology remain separate escapes.

## What should Prime Circle do with a one-profile observable whose prime locations RH makes invisible once total count is fixed?

PC-285 is the unconditional matched-control boundary. PC-286--PC-289 show conditionally on RH that the exact one-profile normalized boundary-log spectrum collapses toward Li controls almost to square-root scale. PC-290 removes the residual `log^2 q` gap by matching the total source count and pairing RH mean-square prime discrepancy with the existing `L^2` spectral path energy.

Thus for every `B=o(sqrt(q))`, exact prime positions produce no order-one separation from the count-matched control in this statistic. The remaining boundary is the actual denominator-microscopic `B~sqrt(q)` regime, the scalar source count itself, or a richer observable that retains cross-level, multi-source, directional, phase-sensitive or noncommuting information before singular-spectrum scalarization.

## Which Robin obstruction remains after the whole regular CA staircase collapses to one stable-filter profile?

RE-117--RE-118 show that first recovery and block maximality are weighted-lobe properties of `J=F_Theta+G_Theta`. RE-119 is stronger: every sufficiently large regular CA state satisfies the same normalized pointwise law `Y^(1-Theta) log Y h(C)=J(log Y)+o(1)`, and the regular-CA phase mesh is exponentially fine.

Hence leading-order arithmetic placement is no longer an obstruction in the attained finite-edge branch: the actual CA staircase samples both signs and all fixed-width features of `J`. Any contradiction must use the lower-order remainder beyond the current `o(1)` projection, a source coordinate not reconstructible from `J`, or a selector property visible below leading edge resolution. The nonattained-edge and `Theta=1` branches remain separate.

## How large can a pointwise prime-phase fluctuation be after distributed gauge-invariant frustration is priced?

VIS-213--VIS-216 reduce fixed-dimensional worst starts to unit-modulus torus optimization and give cycle-holonomy, fractional-packing and normalized magnetic certificates. VIS-217 proves that the magnetic relaxation itself has a dimension floor:

`U_spec/sqrt(R(A)) >= sqrt(2 N_eff/(n-1))`.

Therefore normalized magnetic spectra can reach Haar-RMS scale only if effective edge count is `O(n)`. The immediate arithmetic test is the actual scale of `N_eff(y,H)/n(y,H)`. If it diverges, the spectral relaxation is closed as a standalone route and progress must come from stronger cycle packing or the exact torus constraint; if it stays bounded, a sharp extreme-eigenvalue theorem is still required. One-parameter access time remains separate.

The cross-line lesson with Weil Positivity is now stronger: gauge-invariant geometry must be **reached by the source, source-specific rather than universal, and quantitatively strong in the exact destination currency**. Unreachable curvature, universal fermionic curvature and dimension-floor-limited magnetic frustration are different failure modes of the same broader diagnostic.
