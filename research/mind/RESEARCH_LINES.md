# Global mathematical questions

## Which source resources reach the destination before observation becomes target-mixed completion, decoding or classical recoding?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), and [MI-020](intuition/MI-020-incommensurability-removes-collisions-but-may-end-in-decoding.md) separate information survival, source recovery, collision removal and zero selection.

Arithmetic Fidelity now separates two sampling geometries that had looked almost interchangeable. AF-371--AF-373 separate relation type, relational arity and sampling. AF-374--AF-375 classify the continuous separable multiplicative ambiguity by `H_E=<E-E>`: dense generated differences kill the periodic gauge modulo exponential tilt. AF-376 then proves that this is still weaker than determination. If the **actual row set** is uniformly discrete, compact-support false positives narrower than its minimum spacing satisfy every sampled total-positivity minor while failing global Pólya-frequency structure. The irrational two-phase set therefore removes the separable gauge but remains target-mixed on the broad class `L`.

Prime Circle reaches the complementary bad endpoint. PC-311--PC-318 move from scalar finite-Fourier closure to a genuinely two-index product field. Rational radial slopes collide into finite Hurwitz/quasipolynomial packets; irrational slopes remove collisions so completely that the trace decodes the full residue tensor and canonical source, with a Barnes double-zeta Mellin image. There is no useful intermediate radial quotient in this construction.

Together AF-376 and PC-318 sharpen the audit: **collision removal is not fidelity**. After changing sampling or rates, classify the residual fibre. It may remain target-mixed through a different alias, become target-pure, or collapse to source decoding. Only the middle case supplies a compressed faithful carrier, and it still needs a separate theorem controlling the destination sign or divisor.

Prime Lattice has lowered its destination bill rather than increased source recovery: after the exact transfer/coercive equation, the remaining gate is the rational-prime-specific sign/first-crossing law carried by the transferred scalar.

The live question is: what exact source class and retained relation identify the intended discriminator without reconstructing the source; what relation type, arity, actual sampling geometry, generated gauge, conditioning and stability are paid; and what independent theorem controls the divisor or sign after identification?

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) track channels left after bulk controls close.

Nyman--Beurling has now separated Gamma cancellation, target conditioning and selector quality. NB-157 shows that the leading macroscopic completed-Gamma term is one signed log-height moment and can be cancelled exactly by three polynomially separated heights. NB-158 closes the conditioning caveat: the explicit balanced sampler keeps uniformly positive `O(1)` target charge with `D_G=O(1)` and `J_G=0`. But its negative middle lobe lies near `G^theta_2`, where explicit zero counting already forces `Omega(log G)` actual zeta zeros. The signed debt can therefore be paid by a routine lower-height reservoir. A useful escape must preserve a rarer target feature or couple to source-specific Nyman/von-Mangoldt information before opposite-sign zero packets separate.

Robin Extremal has separated moment count from moment precision. RE-161 shows that approximate lower moments leak with factorial weights before the `r`-th remainder; the robust cost is `sum_(k<r)|mu_k|/k!`. At the RE-160 factorial width this entire sum must already be `O(eta_p)`, and even simultaneous vanishing relative error in every moment can leave order-one Robin ambiguity. A positive source theorem must therefore provide the weighted precision staircase or a signed arithmetic coupling, not merely many approximately correct moments.

Farey Discrepancy has separated rank-anchor tails from anchor geometry itself. FD-151--FD-153 give the sparse/central/supertypical resource law for rank-initial anchors. FD-154 constructs a different divisor-closed coordinate face: avoiding a fixed finite set of primes gives exact finite-`L^q` coercivity at bounded ancestry depth and bounded endpoint distortion with only positive, not near-total, anchor mass. This moves the frontier to whether a Farey-native observable legitimately supplies that macroscopic arithmetic anchor without encoding Möbius orientation.

The common question is no longer whether a broad theorem holds on average, but whether the exact target-bearing configuration is controlled in the norm, population, conditioning, anchor geometry, terminal resolution and exceptional scale the destination consumes—and whether any compensating lobe or reservoir is itself selective rather than routine.

## Which growing source law is informative rather than merely connected, numerous, zero-free, interval-pseudorandom or reconstructive?

Farey Discrepancy shows that ancestry resources depend on the geometry of anchored coordinates: rank anchors pay rank-tail depth, whereas finite-prime coordinate faces can have bounded depth but spend explicit macroscopic source information. Anchor provenance must be priced before edge complexity.

Möbius Cancellation now has a minimax proof-architecture ceiling. MC-322--MC-324 turn prime-shell family energy and individual conductor costs into a quartic common-radical bill. MC-325 shows sparse even Hamming layers reproduce the half-loading barrier. MC-326 removes arbitrary family design as a universal escape: for every nonempty mode family and nonnegative weights, some nonzero abstract source column carries at least `2^(R-1)/(2^R-1)>1/2` of the total weight. Pair modes are therefore already asymptotically optimal under independent positive conductor bills and universal incidence control.

A route past exponent `4` must now use information that MC-326 deliberately does not have: an arithmetic restriction on the **actual** incidence columns `S_A={s_p}`, a genuinely joint/signed conductor relation, a stronger individual conductor horizon, or a changed endpoint geometry. Mode count and clever reweighting alone are exhausted.

The next useful theorem in any growing representation must survive exact algebra and matched controls and be stated on the same population, norm and scale as the destination. Mode cardinality, connectedness, zero-free regions, interval cancellation and source provenance are not scalar resources until their transfer and loading laws are proved.

## Which representation-faithful null remains after nuisance variables, weak topology and universal scalar positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) and [MI-002](intuition/MI-002-the-surviving-arithmetic-variable-is-relational.md) separate normalization, relational geometry and source-selective sign.

Visual Exploration has now calibrated both accessibility and one unavoidable relaxation mode inside the symmetric-Dirichlet quotient. VIS-247--VIS-250 give the parameter-free conditional law, exact three-coordinate Gibbs kernel, connected regular level set and irreducibility. VIS-251 rules out dominant-label trapping as a high-`|lambda|` mechanism. VIS-252 then proves that every coordinate contrast is an exact eigenmode with `rho_m=(m-3)/(m-1)`, so its autocorrelation is `rho_m^t`, its integrated autocorrelation time is `m-2` triple updates, and the full spectral gap is at most `2/(m-1)`. Any slower behavior of `bar T` after this benchmark is passed must come from genuinely nonlinear conditioned geometry, another conductance cut or numerics rather than the elementary scan refresh.

Weil Positivity has closed the obvious scalar convolution category changes as well as the finite Pick/Laplace ones. WP-325 makes the exact prime-power law classically non-divisible while nearby compound-Poisson positivity is universal. WP-326 shows the same scalar source is freely indecomposable under additive and positive multiplicative free convolution because of its first atom gap, whereas Boolean infinite divisibility is available for every probability law. The surviving sign mechanism must therefore add canonical operator/global structure whose positivity is neither impossible merely because of the prime-power gap nor automatic for matched controls.

The common criterion is whether the destination relation survives honest nuisance propagation and is nonautomatic in the matched-control category. Exact nuisance removal, source fingerprints, global accessibility, reversible access, or a change of independence notion do not by themselves make the final statistic source-selective or usable.

## Which noncompact source channels and adaptive representation costs survive a positivity reduction under the correct limit order?

Weil Inertia has moved from a frozen essential-spectrum obstruction to an exact sign-resolution/limit-order problem. WI-313 gives a moving-floor count with no false negatives but possible contamination from source spectrum in `[0,delta)`. WI-314 gives the complementary fixed-cap count

`N_Q(-delta) <= N_D(-1/2) <= N_Q(0)`,

which has no false positives. Cofinal saturation `delta_j->0` at one fixed aperture recovers the complete strict source negative index, and finite negative index is eventually reproduced exactly.

The difficult step is now explicit: a single diagonal saturation schedule while the aperture and source form change can miss negative directions whose depth collapses faster than the cap error. Thus positive source spectrum near zero is not the fundamental obstacle at the fixed cap; the remaining theorem must control **source-negative margin versus saturation across the changing aperture**, or supply vectorwise source-tail alignment. Componentwise Hilbert--Schmidt simplicity and scalar tail floors are secondary unless they survive exact recombination and this limit order.

A construction must still pass the signed-density quotient. An impossibility theorem must survive admissible bounded-tail reserve/localizer choices and exact recombination rather than exploit an overcharged decomposition.

## Global discriminator: identify the exact resource that the matched control cannot fake

The frontier is increasingly a classification of where information is spent. AF-376 separates dense generated differences from dense actual observation: one can kill a separable gauge and still leave an infinite-dimensional local-support fibre. PC-318 shows the opposite failure, where removing radial collisions ends in exact decoding. FD-154 shows bounded ancestry depth is possible when one spends a macroscopic coordinate-face anchor. MC-326 proves that arbitrary nonnegative mode design cannot beat the universal half-loading floor without arithmetic restrictions on realized columns or joint conductor information. NB-158 shows Gamma cancellation and bounded target conditioning can coexist with a routine logarithmic adverse reservoir. RE-161 shows many approximate moments can be cheap yet useless unless their factorially weighted precision reaches the destination scale. VIS-252 supplies an exact `Theta(m)` linear mixing benchmark while leaving nonlinear statistic mixing open. WI-314 recovers strict source inertia cofinally at fixed aperture but exposes a diagonal shrinking-margin obstruction. WP-326 shows that changing convolution independence can land either on impossible source factorization or universal positivity.

For every proposed route ask: what exact object reaches the destination; under which norm, scale, topology, observation population, source class, relational arity and actual sampling geometry; which aliases are removed and which fibres remain; what source information was prepaid to obtain coercivity; how demanded modes load onto the source columns actually realized; where compensating signed mass is allowed to land; what limit order is required; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, moment matching, routine reservoir filling, universal transform/convolution closure, generic positivity or classical recoding; and does a separate source theorem dominate the destination loss without reconstructing or assuming the endpoint?
