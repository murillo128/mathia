# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry, singular source calculus or programmable representation?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate source provenance, target sensitivity, relational structure, reachability, readout fidelity and motion law.

Weil Positivity has closed scalar recovery, fixed positive multi-channel recovery, finite multiplicative-equivariant depth, flat transport and several curvature/readout escapes. WP-300 leaves prime-symmetric scalar projective curvature only flat or universally fermionic. WP-301 shows that arbitrary noncommuting local defects in a positive frustration-free model collapse under the zero-mode readout to common-kernel selection, screening nonzero archimedean spectrum.

WP-302 traps ordinary moving kernels between homogeneous rigidity and free programmability. WP-303 closes the most canonical source-forced spectral interpolation between them: every moving spectral cut of one fixed source operator is a pairwise-commuting projector family, hence norm-discrete and strongly derivative-zero wherever differentiable. The canonical source threshold is therefore a singular filtration, while freeing the spectral realization/compression reaches the POVM-universal category by Naimark dilation. The surviving curvature route must be forced by a genuinely varying noncommuting or nonlocal source law before target comparison, and its informative spectrum must survive the final readout.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) keep conditioning, source supply and target sensitivity distinct.

Analytic Frontier now has a sharp bow interface beyond source marginals. ANF-185 replaces adaptive critical probing by one deterministic one-height high-pass with transition width `a_X`. ANF-186 then matches the natural prime/rough coefficient alphabet, densities, mean, diagonal variance and local absolute-mass envelope while still producing target endpoint memory by placing signs after reading the exact logarithmic carrier. The remaining theorem is a **joint arithmetic-placement law relative to the carrier**, or direct `o(a_X)` control of the deterministic weighted residual / signed Fejer defect. One-point source statistics are exhausted.

Arithmetic Fidelity has localized its source-complexity obstruction to the first prime layer. AF-338 gives fixed-order full-Euler sign regularity once `sigma>r-1`. AF-339--AF-340 show that constant pole cancellation creates `Theta(P/log P)` sign variation. AF-341 removes all higher prime powers and retains an RH-equivalent right-half-plane pole discriminator after centering the prime layer, but the same leading sign variation remains. The analytically harmless higher-power tail is one-signed and carries no such zero poles. The live bridge must reorganize the prime layer itself or replace ordered sign complexity by a different finite source certificate.

Nyman--Beurling has pushed its inverse-section matched control beyond fixed source sections. NB-118 forces strong target rescue into `O_M(1/R)` retention under fixed Nyman orthogonality; NB-119 shows exact perfect-target matched controls at that scale. NB-120 proves that even `M=M(R)->infinity` with `M^6(1+log M)/R->0` still admits perfect capture at `(1+o(1))/(kappa_infty R)`, with a finite positive limiting repair constant from an exact GCD/Jordan-totient tail-Gram factorization. The live theorem must distinguish actual zero-power packets from a matched-control class surviving growing source equations, force a faster source-section transition with audited constants, or use cross-cutoff coherence.

Prime Lattice has removed fixed-order regularity and now identified the singular scale itself. PL-308 gives exact weak Pohozaev/virial stress for every fixed `s>0`. PL-309 shows that the renormalized small-order trace samples an `exp(-Theta(1/s))` boundary layer and can vary arbitrarily under smooth forcings whose bulk `L^infty` norm tends to zero. Generic bulk convergence therefore cannot close the limit. The completed-Weil branch must control its actual eigenforcing on the stretched boundary coordinate or obtain uniform weak-virial/derivative-measure convergence that bypasses pointwise trace recovery; the rational-prime sign remains separate.

Prime Flute's sharp-cutoff completed-angle branch remains noncompact: adjacent Fourier modes straddling the hard cutoff generate order-one canonical correlations on infinitely many separated modules. A viable continuation must change splitting/observable rather than sharpen the same compactness estimate.

## Which cancellation theorem remains after representation enlargement and source equations stop adding endpoint force?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy has crossed the first genuine cross-horizon coercivity boundary. FD-117--FD-120 show that the whole cumulative field, its minimal floor-quotient skeleton and even complete single-horizon squarefree/unit-amplitude admissibility can still support critical near-extremizers. FD-121 proves that the same source cannot do so at both `N` and `2N`: `Delta_N+Delta_(2N)` pays a fixed positive fraction of the combined endpoint energy. Cross-horizon coherence is therefore a real critical-scale resource. The remaining gate is **pointwise globalization**: prevent alternation between cheap and expensive horizons or transfer the dyadic excess to the designated RH-relevant endpoint.

Möbius Cancellation has upgraded exceptional-character rigidity from one-mode bias to collective dual geometry. MC-289--MC-290 force collision-rich shells toward a near-affine exceptional character unless effective occupancy collapses. MC-291 shows that the near-negative spectrum itself has a dual odd-girth gap; unless exponentially sparse in its span, it shares one exact preferred-sign cell, and non-negligible effective occupancy bounds its independent rank. The remaining arithmetic theorem can attack occupancy, exceptional rank/size, conductor or the impossibility of common-cell concentration for the actual Legendre shell.

## Which exact source incidence survives support relaxation in Weil Inertia?

WI-288 reduces complete one-signed symmetric two-island screening to the nearest-prime-power Lambert gap, WI-289 identifies prime squares as the unique critical composite layer, and WI-290 gives at least `X^(3/8-o(1))` positive-length composite-safe components on `[X,2X]`. WI-291 confines eligible ordinary-prime upper hosts to a much rarer large-gap family with summable logarithmic measure.

The bottleneck is therefore a sharply asymmetric cross-scale incidence theorem, not abundance. A fragmented safe reservoir generated largely by prime gaps near `sqrt(x)` must be localized relative to rare selected gaps near `x`; the available count exponents do not yield the intersection by pigeonhole. Null-equation amplitudes/signs or a different support topology remain separate escapes.

## What should Prime Circle do after mesoscopic amplitude appears in universal Hankel geometry?

PC-285 is the unconditional matched-control boundary. PC-286--PC-290 show conditionally on RH that, once total source count is matched, the exact one-profile normalized boundary-log singular spectrum collapses toward the control for every `B=o(sqrt(q))`.

PC-291 shows that the first denominator-microscopic relation forced at `B~sqrt(q)` is spectrally negligible. PC-292 proves the associated `T=o(N/log q)` invisibility scale is operator-norm sharp: a band `T~alpha N/log q` already has order-one normalized norm. But the saturating example is the universal same-source anti-diagonal/Hankel geometry. The first destination-visible mesoscopic contribution therefore does not yet carry prime specificity.

The surviving one-profile question is whether an order-one **prime-specific remainder remains after the universal mesoscopic Hankel contribution is matched or removed**. Otherwise the architecture must retain information discarded before one-profile singular-spectrum scalarization: cross-level, multi-source, directional, phase-sensitive, raw-resonance or noncommuting data.

## What survives after regular-CA placement reduces algebraic anomalies to polynomial-height edge approach plus phase?

RE-119--RE-121 show that regular CA placement and every fixed algebraic edge hierarchy collapse to a stable profile whenever weighted near-edge mass is sufficiently flat, even without horizontal isolation. RE-122 converts that flatness into a zero-geometry condition. If `H(t)` is the least ordinate of a subedge zero within horizontal distance `t`, then `W(t)<<log(eH(t))/H(t)`. An order `nu^-A` anomaly forces a subedge zero into an `O(log nu/nu)` horizontal layer at ordinate `O(nu^A log nu)`.

Thus a fixed-order finite-edge anomaly requires **polynomial-height approach to the edge**. Superpolynomial height approach makes every fixed algebraic correction edge-determined. This remains only a positive-envelope condition: signed phases can still cancel. The nonattained-edge and `Theta=1` branches remain separate.

## How strong must distributed prime-phase frustration be before it reaches the destination norm?

VIS-213--VIS-216 reduce fixed-dimensional worst starts to unit-modulus torus optimization and give holonomy, fractional-packing and normalized magnetic certificates. VIS-217--VIS-218 close the normalized magnetic relaxation in the strictly subcritical prime regime because effective edge dimension is too large.

VIS-219 now prices the stronger packing certificate exactly. If `P=min(P_+,P_-)`, then `(B-P)/sqrt(R)=sqrt(2N_eff)(1-P/B)`. Haar-RMS-scale certification therefore requires **near-total weighted-envelope saturation**, not merely extensive frustration or a positive packing fraction. In the subcritical prime regime the relative deficit must shrink like `O(sqrt(H) log y/y)`. The corrected proposed clue asks whether this saturation occurs; if not, the exact fixed-modulus torus problem remains after both relaxations are exhausted.

The cross-line lesson with Weil Positivity is sharper. Visual Exploration exhibits quantitative relaxation loss: genuine gauge-invariant geometry survives but its certificate must almost saturate the destination budget. Weil Positivity exhibits categorical and topological loss: a zero-mode readout erases spectrum, a fixed spectral calculus produces singular commuting motion, and a free positive representation can encode the target. In both cases the question is not whether nontrivial geometry exists, but whether a source-forced observable retains exactly the information, regularity and scale consumed by the destination theorem.