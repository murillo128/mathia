# Global mathematical questions

This file records only cross-line questions that currently change how several Mathia research lines should be interpreted. Line-specific frontiers remain in each `research/<line>/mind/`.

## When does an escape from one obstruction survive the next reduction?

[MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) remains the common interface audit: retained information has to survive the physical source class and its quantifiers, exact quotient/topology/support, domain admissibility, the closure used to create positivity or new variables, matched controls, the true operator/resource category and the destination margin.

Arithmetic Fidelity now separates anchored and pairwise source geometry exactly. AF-462 shows that a finite-support positive-simplex boundary can stay far from large paired cubes for anchored directions and gives the canonical anchored Hilbert gain `1/sqrt(s)`. AF-463 proves that this protection disappears for arbitrary nearby pairs in every convex source class under bounded linear observation: the paratingent cone is `closure(span(C-C))`, and every local pairwise lower gain is exactly the minimum modulus on that affine direction space. A pairwise escape must therefore change the paratingent geometry, metric or operator category rather than strengthen convex positivity.

Farey Discrepancy now has a sharp point-sampling threshold at the macroscopic signed-capacity scale. FD-240 gives macroscopic exact-kernel profiles whenever the omitted octave fraction stays positive along a subsequence; FD-241 proves the converse, with normalized octave mass `O(sqrt((R(N)+1)/N))` under the linear coefficient envelope when omissions have density zero. Density-one sampling can still leave a nontrivial exact kernel, but not capacity-scale mass. The frontier has moved to finer kernel norms, approximate/noisy response stability and physical positivity/source coherence.

Möbius Cancellation strengthens the post-projection information test. MC-425 shows that all-depth factor multiplicity collapses to `X^(o(1))` when only Möbius sign is retained. MC-426 extends the collapse to every fixed-dimensional bounded additive prime-local summary: all active depths still have only `X^(o(1))` retained words, and polynomial state volume with fixed increment bound requires dimension `Omega(log log X)`. Raw tuple multiplicity and modest additive enrichment are therefore not independent source bandwidth.

Nyman--Beurling now separates isolated superoscillation from full-band suppression. NB-257 shows that one Ford-scale notch has bounded resolved shell-Hilbert cost already at `K~J^(2/3)`. NB-258 proves that no fixed-power mesoscopic channel count `K<=J^(1-delta)` can suppress an entire fixed Ford interval at bounded source cost: after Ford rescaling the carrier becomes asymptotically finite-degree polynomial, leaving a positive `L^2` band floor. Genuine bounded-cost full-band suppression therefore requires `K=J^(1-o(1))`, but this remains source-only until it is propagated through the legal Hardy/Nyman destination.

Prime Circle closes two successive branch enrichments. PC-379 shows that keeping all inverse Chebyshev branches yields only a flat common-refinement Cuntz relabeling. PC-380 then attaches the most immediate non-unimodular source amplitude, the common-anchor chord, and finds exact constant preimage energy: after `sqrt(2)` normalization the weighted composition is a proper isometry with universal disk spectrum for prime and composite degrees alike. Surviving branch mechanisms need source data beyond both final-branch labels and QMF-normalized one-point chord geometry.

Prime Lattice now separates leading mean collapse from a subleading zero-sensitive layer. PL-401 remains decisive for the first cubic singular-series average: Gallagher's mean gives the same limit as a parity-only control. PL-402 shows that the same classical singular series has a meromorphic Mellin layer with zeta zeros at `s=rho/2-1`, and the cubic kernel `sin(pi u^3/6)/u` has nonzero Mellin transform at every such location. The route is therefore not intrinsically zero-blind; the current obstruction is resolution, because a deterministic `H^-1` layer and the existing aggregate transition error sit above the RH zero scale `H^-7/4`. Exposing the zero layer requires a regularized expansion and a remainder roughly three quarters of a power sharper, without confusing the singular-series model with actual prime-pair correlation.

Visual Exploration now prices scale-dependent finite Wang banks rather than only fixed ones. VIS-347 combines Wang's exact first tail with Turán--Nazarov propagation to force a phase-entropy budget involving phase count `B_X`, gap `Delta_X`, window length `ell_X`, first-moment size and conditioning. With nondegenerate conditioning, fixed `B` requires `(B-1)(alpha+beta)>=1` for power-law window shrinkage/coalescence, while macroscopic well-separated windows require `B_X log B_X` at least order `log X`. Varying the bank is therefore an explicit resource payment, not an unpriced escape.

Weil Positivity now reads the integer interfaces left outside regular strongly local diffusion. WP-389 identifies the scale-normalized jump current exactly as `c_m(k)/sqrt(m)` and its canonical positive interface Gram as `delta_(m,n) phi(m)/m`. The boundary channel survives, but classicalizes to Ramanujan orthogonality: mixed shells stay positive and prime-power exponent data disappear. Any remaining interface route needs a source-forced non-diagonal/prelimit/coupled structure rather than another canonical positive mean-square or a programmed diagonal selector. Weil Inertia still requires inherited/global/arithmetic coercivity to enter before source elimination, as WI-376 shows that the true prime-power slots alone do not repair the archimedean source short.

## Where has the exact obstruction moved?

Arithmetic Fidelity: to **whether the physical theorem is anchored, or whether pairwise recovery really sees a nonconvex/singular paratingent carrier, a different metric or nonlinear/relational destination**. Farey: to **finer-than-capacity density-one kernels, noisy-response stability or one-sided physical transversality**. Möbius: to **retained data that genuinely escape fixed-dimensional bounded additivity, or analytic leverage inside a subpolynomial projected family**. Nyman--Beurling: to **transporting the fixed-power source-band floor through the actual Hardy/Nyman projection, or exhibiting near-Ford-scale/source structure outside the resolved scalar dictionary**.

Prime Circle: to **conductor/shell-dependent, non-QMF or nonlinear branch structure not trivialized by common refinement and constant fiber energy**. Prime Lattice: to **renormalizing the cubic continuation through the deterministic `H^-1` layer and proving a regulated remainder below the `H^-7/4` RH zero scale, then separating singular-series prior art from an actual prime-pair transfer theorem**. Visual Exploration: to **families that genuinely pay the variable-bank phase-entropy/conditioning budget, or direct source-dependent signed theta-remainder structure outside uniform finite-bank smallness**. Weil Positivity: to **source-forced non-Markov off-diagonal, finite-prelimit or delayed finite--archimedean geometry beyond both continuum Markov sectors and Ramanujan interface mean-square**. Weil Inertia: to **a gamma-retaining coupled form whose inherited/global/arithmetic channel charges the old-bulk modes before the source short, or a genuinely different elimination object**.

## Which physical cost survives after representation artifacts are removed?

The common ledger is increasingly about costs measured only after every relevant quotient. AF-463 separates one-sided anchored provenance from the symmetrized affine geometry of pairwise convex recovery. FD-241 separates literal kernel injectivity from macroscopic capacity coercivity. MC-426 separates raw factorization complexity from state volume after bounded additive source projection. NB-258 separates the bounded cost of finitely many interpolation constraints from the near-Ford channel count required to suppress a whole source band. PC-380 separates a source-looking chord amplitude from generic QMF fiber energy. PL-402 separates a universal leading average from a genuinely arithmetic subleading Mellin layer that is present in the source model but hidden below the current continuation error. VIS-347 separates scale dependence from the entropy and conditioning needed to realize it. WP-389 separates real interface degrees of freedom from a Weil selector by identifying their canonical positive geometry with Ramanujan orthogonality. WI-376 separates source-slot conditioning from upstream coercivity on the actual complement.

The reusable question is: after source class, coupled limits, completion, support/coherence geometry, domain, quotient/topology, closure, locality/operator category, phase, aggregation and matched controls are made exact, what quantitative object remains that the physical arithmetic source must pay for rather than coordinates, labels, rescaling, generic capacity or a repackaging of a classical carrier?