# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Move the strongest matched control all the way to the actual source category without subtracting the target arithmetic

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Analytic Frontier now closes all local cardinality-five complex geometry through the curvature gate, leaving the genuinely finite-height two-pair scalar problem as the first unresolved five-point layer. Visual Exploration strengthens the Farey endpoint control from one fan to every pre-fixed fixed-`nx` totient layer, while VIS-030 shows why a growing endpoint subtraction is no longer neutral: it becomes a Möbius/totient Riesz channel. Xi Flow supplies the complementary source qualification: translated super-mesoscopic counting eliminates macroscopic folded null modes once the borderline flux resource is retained.

A decisive result must therefore match both the destination quotient and the source regime while auditing the control itself. Remove deterministic boundary/symmetry/sampling effects only up to the point where the null remains target-neutral, then use source theorems to exclude controls that are genuinely inadmissible at the claimed scale.

## Separate completed criterion encoding, observation sufficiency, aliasing, and arithmetic selection

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Prime Lattice now makes the fixed-step quotient precise. Fine/dense observation sets can retain RH-equivalent information once Suzuki's completed scalar/kernel already exists. A fixed step still admits an exact upper-bounded resonant off-line control; on the lower side, PL-159--PL-160 show that every attained rightmost frontier forces negative escape, and PL-161 shows that any one-sided bounded fixed-step sequence phase-pins near-frontier zeros to the sampled identity.

The live lower fixed-ray alternative under RH failure is therefore an **unattained phase-pinned horizontal frontier**, not generic finite aliasing. The program-level theorem remains upstream: derive an arithmetic polarization/sign on the completed zero-sensitive object itself, or a source-specific anti-aliasing relation genuinely weaker than the target criterion. Separate boundedness on two incommensurable rays is insufficient unless the same near-frontier zeros are coupled across both scales.

## Match infinite assembly to the exact information profile, ideal, and energy-domain budget

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Arithmetic Fidelity now resolves more than exact entropy. AF-120--AF-122 distinguish bounded exact description, information-dimension slope, and the entire conditional fine-quantization entropy profile ordered by the decoder modulus. Weil Positivity supplies the operator analogue: finite-cutoff resultant positivity has a globally collapsed energy space, and WP-150 shows that every vertex-local normalization restoring bounded fixed-shell energy erases the fixed arithmetic edges, with normalized mass potentially escaping to infinity instead.

A positive must derive from the source the exact description profile, recovery regularity, topology, ideal, sampling geometry, and dense domain consumed downstream. Finite-stage positivity, zero information dimension, or per-tolerance approximation is not enough without a coherent non-escaping limit in the destination category.

## Resolve the moving-comparator problem at the exceptional-character frontier or change the signed feedback architecture

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction` and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

MC-065--MC-067 show that signed quadratic feedback removes one absolute conductor penalty but exposes a split-prime `p^{-theta}` budget. MC-068 turns that budget into family sparsity: only `O(log X)` prime-conductor candidates can survive below square-root scale. MC-069 is sharper through a quasi-subpower range: every nonexceptional primitive character is excluded and at most the unique Landau--Page exceptional character remains.

The live transfer is therefore not a broad moving-character search. It must control the possible exceptional comparator coherently across scale, extend pointwise arithmetic beyond the present conductor range, or derive a genuinely signed remainder theorem that avoids the positive feedback budget.

## Leave support-one positive-compression universality, or prove new arithmetic control for singular changing tests

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

WI-153--WI-155 close separately valid support-one scalar censuses, pointwise-PSD matrix kernels, and direct universal Loewner matrix lifts at the sharp CCLM/Montgomery--Taylor extremal. WI-156 closes finite post-hoc scalar portfolios. WI-157 shows that regular changing support-one families have only `O((log T)^-2)` finite-height main-term headroom and no asymptotic gain under uniform `o(1)` arithmetic error.

WI-158 locates the honest remaining scalar boundary: the published pointwise BGSTB error does not control singular near-extremizers at the natural dual norm threshold. A live scalar improvement must prove a stronger frequency-sensitive or averaged remainder theorem for that family. Otherwise it must retain joint, sign-indefinite, nonlinear, source-restricted, higher-correlation, or support-greater-than-one information that cannot be reduced to positive scalar compressions.

## Couple provenance before positive completion or all-prime replication classicalizes it

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

The zero-order cyclotomic resultant retains the prime-power selector, but fixed finite repair has unbounded mixed-prime inertia and its canonical positive graph completion is globally short-circuited by spectator primes. WP-150 adds that vertex-local finite-energy normalization cannot rescue the same interaction: it drives every fixed arithmetic edge to zero. Prime Flute continues to show independently that post-processing cannot recreate provenance after a prime-blind quotient.

A live route must change the source coupling before these losses, or use a genuinely nonlocal/cross-level/cohomological operation whose selector, domain, and sign survive the strongest controls. A moving/cutoff-following topology is relevant only if its arithmetic meaning and non-escape are source-forced rather than chosen to keep the desired coefficients visible.

## Derive the Xi borderline flux budget and turn source-rigid near-lattice structure into signed fixed-time coercivity

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

XF-032--XF-034 identify geometric gap ramps as universal finite-range null modes. XF-035--XF-037 now close the static source-coercivity bridge conditional on the borderline resource `M V_M=O(1)`: full translated Xi counting forces uniform gap flattening, vanishing total log-gap variation, and zero density of order-`1/M` nonlattice contrasts. The old macroscopic tent obstruction is not source-admissible.

The missing theorem is dynamical. Derive `V_M=O(1/M)`, or an equivalent compactness budget, from the exact `L_lambda`/`L_w` interaction while retaining collision-positive coverage; then convert the resulting sparse near-lattice residual into a favorable tapered derivative and a height-uniform backward-time margin. A decisive negative must now be a source-compatible sparse/sub-super-mesoscopic misalignment, not another macroscopic fold.
