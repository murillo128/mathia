# Global mathematical questions

## When does repeated observation actually shrink the admissible source fibre?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md), [MI-022](intuition/MI-022-unbounded-compatibility-can-turn-finite-cutoff-ambiguity-into-a-stability-problem.md), and [MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md) separate retained information, common-source compatibility, conditioning, acquisition geometry, aggregate readout capacity and final destination selection.

Arithmetic Fidelity now separates three resources at smooth order three. AF-394 identifies the exact continuum target: `q=-(log f)''` must satisfy `q>=0`, `(log q)''<=2q` on `{q>0}`, and `{q>0}` must be connected. The shrinking solid hierarchy recovers the first two analytic conditions but not component topology. AF-395 shows fixed successor radius can miss that topology on a vanishing prime-log mesh. AF-396 makes the reach scale sharp for the explicit disconnected-curvature control: if `R_N` is successor depth around the `N`th prime, `R_N=o(N)` is eventually blind, `limsup R_N/N<e-1` remains blind, and `liminf R_N/N>e-1` finds a negative minor in every large tail block. **Resolution is not relational reach**; this topology costs `Theta(N)` prime-index reach, with only the exact `e-1` boundary left as a conditioning question.

Farey Discrepancy shows that redundancy is source-class relative. FD-171 proves consecutive midpoint horizons have zero exact redundancy over unrestricted formal sources. FD-172 shows a bounded squarefree multiplicative source cone turns the transformed defect into nonnegative mass and lets dyadic horizons certify the Möbius target. FD-173 closes the exact sparse-horizon classification: target-rigidity holds iff every odd prime-power orbit hits the observed half-annulus union. Even `{B^k}` works for every fixed integer `B>=2`, so multiplicative-gap coverage is not the invariant. The open issue is now **finite-scale orbit-hitting time and stability**, together with weakening the source architecture without restoring signed null directions.

Robin Extremal gives the converse warning about source tariffs. RE-180 proves that honest nonnegative prime multiplicity forces any early two-jet repair to pay `Omega(sqrt(p)/log p)` actual deletions unless it pays a remote KV reset budget. RE-181 then constructs a `{0,1,2}` ordinary-prime source that pays exactly that order of tariff, matches the boundary value and first slope, stays under every fixed KV envelope, and still preserves a transient Robin excursion. A necessary resource bill is not a selector if the declared source class can afford it. The live distinction is placement/joint geometry of the compensators, not deletion count itself.

Together AF-396, FD-173 and RE-181 sharpen the multiscale lesson. Raw scale count, ambient row rank and lower-bound tariffs are not intrinsic information currencies. One must declare the admissible source fibre and ask which relations remain accessible at the destination's physical scale: arbitrarily fine local sampling may still require linear-in-index reach to recover topology; nonlinear source positivity can reduce an ambient irredundant temporal system to sparse orbit-hitting target tests; and a large discrete source tariff can remain nonselective when a matched source construction realizes it.

## Which quantitative resource reaches a thin or moving destination?

Prime Lattice retains the cleaner first-prime dependency graph. PL-329 gives the sharp source-amplitude condition for the shrinking reflected sign; PL-330 transports isolated eigenbranches in norm-resolvent/`L^2` topology; PL-331 proves every fixed logarithmic Fourier seminorm still misses the hostile moving layer; and PL-332 shows that, once the endpoint remainder is family-uniform, `L^2` continuity transports the selected asymptotic coefficient to the fixed threshold. PL-333 blocks the full-space Hopf shortcut. PL-334 now closes the even-parity repair: the real-even Beurling--Deny threshold is exactly `a=log rho_pl`, still strictly below the first-prime aperture `(1/2)log2`. The target parity therefore does not restore a positivity-preserving semigroup near the first-prime wall. The remaining gates are the genuine uniform source/remainder estimate and a proof of `C_0!=0` from the **full completed operator** or a non-Markov state-specific identity.

Visual Exploration now has a scale-sharp moving-test transfer law. VIS-259--VIS-264 separate packet conditioning, universal pole background and the prime-power curvature residual. VIS-265 imports Wang's short-interval pair-correlation support restriction `lambda<theta`, and VIS-266 shows `T`-dependent tests are allowed when their support and seminorms fit the proof budget. VIS-267 then proves that a width-`b` smooth packet carrying unit `2m`-th moment necessarily has `||g||_inf \gtrsim b^(-(2m+1))` and `||g'||_inf \gtrsim b^(-(2m+2))`, with matching constructions. The current Wang interface therefore requires `1/(L b^(2m+2))->0`; for `b=L^(-q)` this gives the explicit gate `q<1/(2m+2)`. The barrier is not “fixed tests only” but an unavoidable moment-order/seminorm budget inside a fixed support margin.

Nyman--Beurling now prices both Hilbert and non-Hilbert source-faithful acquisition. NB-176--NB-180 show genuine Euler frequencies are nonselective if weak channels may be renormalized independently. NB-181 proves that retaining the true amplitudes inside the natural feature Hilbert space makes a Hadamard-scale `N x N` response cost RMS norm `Omega(sqrt N)`. NB-182 then prices the physical scalar escape outside `ell^2`: the Euler--Wiener budget is `W(a) asymp1/a`, normalized height/Fourier--Stieltjes acquisitions obey the same nuclear tariff, and full packing-rank phase coding still costs `Omega(sqrt N)` gain. Known high-ordinate cancellation buys only `kappa_T asymp1/(aH_T)`, leaving the square crossover `aH_T^2=O(1)` as the point where stronger height-specific arithmetic cancellation becomes necessary.

Xi Flow gives the inverse lesson: once the correct consecutive packet is known, local holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local inversion itself.

## How much source information can an analytic architecture exploit at bounded conditioning?

Möbius Cancellation now separates acquisition syntax from intrinsic reliability geometry. MC-338--MC-345 show that bounded-energy dephasing requires extensive joint source information and that fixed-quality noisy coordinate access pays channel capacity. MC-346--MC-347 add the logarithmic reliability tariff and replace raw query count by accumulated discrimination when fidelity varies. MC-348 removes the raw-coordinate model altogether: for the complete arbitrary transcript, vanishing average source-coordinate error forces `Omega(R log(1/bar p))` symmetrized KL across the natural neighboring-state graph. One-shot joint coding, adaptivity or high-dimensional mixing can change how this edge divergence is supplied, but cannot eliminate it when the destination really needs those neighboring source states distinguished.

FD-169--FD-173 and NB-178--NB-182 provide complementary warnings. Unrestricted acquisition can hide the inverse in the sensor; changing horizons can hide many rows behind one spatial coordinate; strong nonlinear source constraints can reduce ambiently irredundant rows to sparse orbit-hitting tests; enlarged duals can manufacture generic metric phase codes; genuine Euler labels remain nonselective if amplitudes are normalized away; and even the correct non-Hilbert Euler scalar topology carries an explicit Wiener acquisition tariff. Source-information cost is meaningful only after source class, target quotient, acquisition topology, operator variation, amplitudes, matched controls and downstream resolution are fixed.

This updates [MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md): measurement count, nominal dimension, positive noise, adaptivity and canonical probe labels are secondary. The load-bearing quantities are inverse modulus, orbit-hitting scale, source-class-relative kernel, per-access capacity, source-edge transcript divergence, source-amplitude conditioning, acquisition-space norm and the number of genuinely independent operator states required by the destination.

## Which spectral or positivity enrichment survives its intrinsic quotient and density law?

Prime Circle now has an exact dyadic decomposition of its growing-conductor Green defect. PC-325--PC-330 establish the phase/autocorrelation quotient, universal Hilbert background and failure of smooth Li subtraction. PC-331 writes the source-sensitive excess as a positive Hankel/Hardy-square energy `E` of Fourier magnitude squares. PC-332 sharpens this to `E asymp sum_m Gamma_a(2^m)Gamma_b(2^m)`, where `Gamma` is normalized cumulative Fourier energy. Uniform dyadic concentration gives only `O(log q)` excess; any superlogarithmic excess forces a specific dyadic scale with anomalous magnitude concentration. This is a much sharper diagnostic, but a magnitude-matched control still reproduces it exactly and phase information remains absent.

Weil Positivity has narrowed the Maaß--Selberg escape from generic self-adjointness to specifically arithmetic rigidity. WP-345 shows the positive height boundary square is divisor-blind; WP-346 realizes the scalar CDD control with positive self-adjoint Robin channels. WP-347 now constructs the matched control on the **same finite-volume one-cusp hyperbolic surface** via a real self-adjoint point interaction: the perturbed Eisenstein coefficient acquires the exact relative factor `S(1-s)/S(s)`, and the continued resonance divisor moves while Hilbert positivity, cusp geometry and a full Eisenstein/relative-trace framework remain. What the control destroys is Hecke/Euler/local-global structure. The surviving route must therefore use that arithmetic rigidity essentially rather than generic automorphic-looking realizability.

Weil Inertia gives a positive example of source correction removing a false resonance but now blocks the simplest exceptional-set repair. WI-327 restores the exact signed coefficient `Lambda-Lambda_X^sharp` and kills the cubic resonance on density-one starts. WI-328 shows an entire still-live bow is twist-frozen on the same endpoint cubic source block: neighboring zero ordinates change the normalized signed sum only by `o(1)`. Bow-internal averaging therefore does not create independent samples that de-exceptionalize one bad start. The route must use pointwise source information, a bow/exceptional-set incompatibility, cross-scale structure, or a genuinely longer source block.

These lines support [MI-024](intuition/MI-024-control-subtraction-must-match-the-load-bearing-source-channel.md): a matched control or subtraction is useful only when it acts in the representation and norm carrying the alleged signal. Smooth density subtraction can miss sparse Hilbert energy; an exact source correction can cancel a generic resonance; a source-sensitive excess may still factor through a phase-blind magnitude quotient; and even positive one-cusp self-adjoint scattering may be too universal to constrain its continued divisor.

## Which source fingerprint is selective rather than merely reconstructive?

Robin Extremal and Farey Discrepancy give opposite source-class effects. FD-173 shows strong multiplicative positivity can make extremely sparse infinite-horizon target testing rigid through prime-power orbit recurrence, even though the ambient temporal linear system has exact null directions. RE-181 shows strong-looking ordinary-prime support, honest bounded multiplicity, exact two-jet matching and KV fidelity can still admit an explicit matched repair that moves the Robin destination. Source restrictions matter only when they remove the *destination-relevant* matched fibre, not merely when they make reconstruction more expensive.

Arithmetic Fidelity exactly separates local analytic recovery from global target topology and now prices that topology by linear prime-index reach on its canonical control. Prime Circle isolates a dyadic source-sensitive Hardy-square profile but still inside the autocorrelation quotient. Visual Exploration gives a sharp theorem-access tariff for normalized moment packets but not arithmetic selectivity. Nyman--Beurling shows source fidelity includes charged acquisition topology in both Hilbert and Wiener geometries. Weil Inertia shows the exact signed source removes generic resonance while the natural nearby spectral family remains too coherent to fix exceptional quantifiers. Weil Positivity shows even one-cusp self-adjoint hyperbolic realization can remain divisor-mobile without arithmetic rigidity. Prime Lattice shows the completed operator can lose the necessary order structure before arithmetic activation even after target parity is imposed.

The program must therefore separate **source fingerprint**, **admissible source fibre**, **target quotient**, **acquisition/transport topology**, **source amplitude and renormalization cost**, **relational reach versus spatial resolution**, **channel-matched control subtraction**, **conditioning/capacity/reliability**, **local analytic recovery versus global component topology**, **resource lower bound versus matched realizability**, **joint versus scalarized geometry**, **full-operator versus principal-form order structure**, **exceptional-set quantifiers versus coherence windows**, and the final **destination-selective sign or zero theorem**.

## Global discriminator: identify the exact resource a matched control cannot fake

For each proposed route ask: what exact source distinction reaches the destination; what source class and target quotient are being compared; what information is regenerated for free; whether horizon/operator changes contribute independent constraints and whether source restrictions collapse their kernel through repeated defect orbits; whether the inverse has been moved into the sensor; whether weak source channels were independently renormalized and at what cost; whether the declared acquisition topology contains the natural source readout and what gain it charges; what native norm gives a useful inverse modulus; what complete-transcript divergence survives at the downstream resolution; whether finer sampling also increases relational reach or only shrinks the physical stencil; whether a lower-bound tariff is actually unaffordable inside the source class; whether a larger witness class merely certifies generic metric packing; whether local certificates recover only the differential cone or also component topology; whether a control subtraction matches the norm/channel carrying the signal; whether a source-sensitive scalar still factors through a phase/autocorrelation quotient; whether a positive/self-adjoint realization still permits divisor-changing controls **inside the claimed geometric category**; whether a limiting positivity/Hopf theorem survives the bounded completed operator terms and target parity; whether an almost-all theorem controls the distinguished sequence or the available family is frozen on the source coherence scale; and what theorem finally converts the survivor into a Riemann-zero or positivity restriction.

A useful bridge should fail on a matched control for an independently arithmetic reason. Exact reconstruction, unbounded observation count, unrestricted target-aware mixing, one spatial sensor with uncharged horizon variation, ambient row-rank lower bounds that disappear inside the source class, genuine frequency labels with erased amplitudes, bounded Hilbert duality that excludes the natural functional, non-Hilbert acquisition with uncharged Wiener gain, arbitrarily fine sublinear-reach stencils, a large but realizable source tariff, a rich spectrum, a critical boundary, source labels, a positive principal form, parity restriction without full-operator order positivity, operator-valued lifts, adaptive scalarization, singular tangents, a positive divisor-blind one-cusp self-adjoint boundary norm, a nonzero low-frequency carrier, a large Gram/nuclear response, benign forward regularity, positive readout noise, adaptive or jointly coded observations without source-edge discrimination, infinitely many remote resets, smooth PNT subtraction, a phase-blind dyadic Hardy excess, zero-set membership, raw positive von Mangoldt weighting, or many nearby zero ordinates inside one twist-frozen bow are not enough unless the surviving quantity is stably distinguishable, charged in the correct source geometry, theorem-accessible at the required quantifier and scale, robust under the full source operator, and selectively coupled to the RH destination.