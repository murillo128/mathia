# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry, singular source calculus, finite-mass decay or programmable representation?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate source provenance, target sensitivity, relational structure, reachability, readout fidelity and motion law.

Weil Positivity has closed scalar recovery, fixed positive multi-channel recovery, finite multiplicative-equivariant depth, flat transport and several curvature/readout escapes. WP-300 leaves prime-symmetric scalar projective curvature only flat or universally fermionic. WP-301 shows that arbitrary noncommuting local defects in a positive frustration-free model collapse under the zero-mode readout to common-kernel selection, screening nonzero archimedean spectrum.

WP-302 traps ordinary moving kernels between homogeneous rigidity and free programmability. WP-303 closes the canonical fixed-source spectral interpolation: spectral cuts of one self-adjoint source operator commute, are norm-discrete and carry no regular nonzero Grassmann tangent. WP-304 closes the simplest nonlocal continuation as well: every bounded source resolvent compression has finite spectral mass and decays like `1/y`, so it cannot reach the Gamma logarithm.

The pole-normalized positive-energy Mangoldt measure does cross that mass barrier. Its regularized Herglotz response has real part `-log y+O(1)`, but the same leading term occurs for every positive unit-density measure and therefore fails the arithmetic matched-control test. The canonical even `+/- log n` carrier cancels that real logarithm exactly. The surviving route must derive a generalized/unbounded boundary, scattering, determinant or genuinely noncommuting finite--archimedean law whose **orientation, renormalization and subleading response are source-forced before Gamma comparison**, and whose informative spectrum survives the final positivity readout.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) keep conditioning, source supply and target sensitivity distinct.

Analytic Frontier now has a sharp bow interface beyond source marginals. ANF-185 replaces adaptive critical probing by one deterministic one-height high-pass with transition width `a_X`. ANF-186 then matches the natural prime/rough coefficient alphabet, densities, mean, diagonal variance and local absolute-mass envelope while still producing target endpoint memory by placing signs after reading the exact logarithmic carrier. The remaining theorem is a **joint arithmetic-placement law relative to the carrier**, or direct `o(a_X)` control of the deterministic weighted residual / signed Fejer defect. One-point source statistics are exhausted.

Arithmetic Fidelity has now falsified ordered sign variation as a stand-alone source-complexity currency. AF-338 gives fixed-order full-Euler sign regularity once `sigma>r-1`; AF-339--AF-343 show that after pole cancellation the prime layer retains the RH pole discriminator and `Theta(P/log P)` variation against arbitrary low-exception and fixed periodic/syndetic baselines. AF-344 then constructs a fixed prime-independent perfect-power background that remains zero-pole-blind and residue-one yet reduces the residual sign variation to `O(P^(1/k))`. The cost moves into sparse support and amplitudes `asymp P^(1-1/k)`. The live bridge must therefore use a **joint certificate** that prices sign order together with amplitude/concentration/support geometry, or another source statistic whose recovery theorem is invariant under this recoding.

Nyman--Beurling has crossed the finite-packet stability gate. NB-118--NB-122 show that growing exact Nyman orthogonality and the complete nested cutoff prefix still admit perfect inverse-section target capture. NB-123 separates actual finite zero-power packets by annihilating-recurrence rank. NB-124 then uses the annihilator itself as a bounded dual functional in the Nyman cell metric: every fixed finite packet has a cutoff-uniform positive target-angle gap, and in any fixed strip `Re rho<=1-sigma`, polynomially near-perfect capture forces `d(F)=Omega(log R)`. The live resource is now the **normalized missing-root annihilator margin for growing-rank/horizontally drifting packets**, together with height and horizontal-mass control—not generic Prony conditioning or another nested source equation.

Prime Lattice has crossed from profile classification to a singular matching problem. PL-308 gives exact weak Pohozaev/virial stress for every fixed `s>0`; PL-309 shows that the renormalized trace lives on an `exp(-Theta(1/s))` layer invisible to ordinary bulk convergence; PL-310 computes the universal stretched Green fixed profile `C/sqrt(e^(2r)-1)`. PL-311 then proves that every continuous forcing in the endpoint-integrable range of `I-T` must vanish at `r=0`, so the canonical `e^(-r)` source direction cannot be absorbed by a regular fixed-`r` correction. At the same time every fixed physical bulk point collapses into the `r=O(s)` corner. The remaining analytic resource is therefore a **corner-to-outer finite-`s` matching/solvability law that selects the scalar amplitude `C`**, followed separately by the rational-prime sign/first-crossing theorem.

Prime Flute's sharp-cutoff completed-angle branch remains noncompact: adjacent Fourier modes straddling the hard cutoff generate order-one canonical correlations on infinitely many separated modules. A viable continuation must change splitting/observable rather than sharpen the same compactness estimate.

## Which cancellation theorem remains after representation enlargement and source equations stop adding endpoint force?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy has now shown that the superexponential loophole left by its average-coherence theorem is real. FD-117--FD-120 show that the whole cumulative field and strong single-horizon source truth remain critically near-saturable. FD-121--FD-124 force a positive average deficit for every polynomially bounded common source whenever average logarithmic horizon growth stays finite. FD-125 then constructs one square-root-bounded common source on the repeated-square chain `H_(j+1)=H_j^2` with `S_(H_j)->1`. Generic common-source coherence and the critical pointwise envelope therefore cannot yield the desired pointwise gap. The next theorem must use a **physical Mertens/Farey arithmetic law absent from the synthetic nested-carrier source** and show that it charges this explicit matched control.

Nyman--Beurling provides the complementary positive example of a source relation that really does shrink the relaxed class in the destination norm. The full nested Nyman prefix is redundant, but the actual zero-power annihilator is not. NB-124 avoids parameter inversion and pairs that annihilator directly with the same reciprocal block weights used by the target energy, turning the source representation into a stable finite-packet gap. The remaining question is whether the normalized annihilator margin stays useful under growing rank and horizontal approach to `Re s=1`.

Möbius Cancellation has also reached an explicit relaxed-class extremizer. MC-293 collapses the low-source-cost near-negative spectrum to at most one Landau--Page mode, and MC-294 forces many remaining directions into a high-cost quotient with no short dependencies. MC-295 constructs a fixed-weight finite model that simultaneously has perfectly balanced cheap directions, live-scale defects, exponentially small participation, full high-cost quotient dimension and no common preferred-sign cell. Thus the full generic Fourier/entropy/Page-balance/Hamming package is jointly satisfiable at the endpoint scale. The remaining theorem must constrain **arithmetic realizability of the expensive quotient** in the actual Legendre shell, not sharpen the same generic coding geometry.

These three lines now give a precise cross-line discriminator for “more source structure.” Farey supplies an explicit source that saturates after generic coherence is exhausted; Möbius supplies an explicit quotient model that saturates after generic coding constraints are exhausted; Nyman supplies a source law whose dual functional is controlled in the exact destination metric and therefore creates a stable gap. The useful test is not how many constraints are added, but whether they exclude the surviving extremizer **in the normalization consumed by the endpoint**.

## Which exact source incidence survives support relaxation in Weil Inertia?

WI-288 reduces complete one-signed symmetric two-island screening to the nearest-prime-power Lambert gap, WI-289 identifies prime squares as the unique critical composite layer, and WI-290 gives at least `X^(3/8-o(1))` positive-length composite-safe components on `[X,2X]`. WI-291 confines eligible ordinary-prime upper hosts to a much rarer large-gap family with summable logarithmic measure.

The bottleneck is therefore a sharply asymmetric cross-scale incidence theorem, not abundance. A fragmented safe reservoir generated largely by prime gaps near `sqrt(x)` must be localized relative to rare selected gaps near `x`; the available count exponents do not yield the intersection by pigeonhole. Null-equation amplitudes/signs or a different support topology remain separate escapes.

## What should Prime Circle do after diffuse averaging kills local bands and fixes total square-root Hilbert--Schmidt mass?

PC-285 is the unconditional matched-control boundary. PC-286--PC-290 show conditionally on RH that, once total source count is matched, the exact one-profile normalized boundary-log singular spectrum collapses toward the control for every `B=o(sqrt(q))`.

PC-291--PC-293 separate microscopic relation onset, worst-case local operator amplitude and source-averaged local amplitude. At `N~sqrt(q)`, a fixed pair has an order-one local Hankel band, but every local band `T=O(N)` contributes `o(1)` after averaging over a diffuse source law with maximal atom `O(log q/q)`.

PC-294 then shows that the **full** averaged normalized Hilbert--Schmidt mass is also source-universal at square-root resolution: for the prime and count-matched Li-grid laws it tends to the same `pi^2/12` benchmark. Any surviving one-profile prime signal must therefore redistribute a fixed total spectral mass. The live question is higher spectral shape—moments, edges, small singular values or another nonlocal statistic—not local residue energy or aggregate Hilbert--Schmidt mass. If shape also classicalizes, richer pre-spectral information is required.

## What survives after regular-CA placement and generic local cancellation cease to be bottlenecks?

RE-119--RE-122 show that regular CA placement and every fixed algebraic edge hierarchy collapse to a stable profile whenever weighted near-edge mass is sufficiently flat. An algebraic anomaly of order `nu^-A` requires a subedge zero within horizontal width `O(log nu/nu)` at polynomial ordinate height, so fixed-order visibility already requires polynomial-height approach to the attained edge.

RE-123 removes arithmetic phase placement as another fixed-order escape. RE-124 localizes any compensating mass for an individually strong atom to `U^(-1+eta)` in ordinate. RE-125 removes the sub-reciprocal core because exact multiplicity and `o(1/U)` neighbors reinforce after target demodulation. RE-126 then uses the local zero count to show that the required annular mass cannot be diluted into arbitrarily many tiny modes: hiding a strong atom forces a **distinct logarithmically comparable companion** in the same near-edge layer with `c_*/U<|gamma_1-gamma_0|<=U^(-1+eta)`. Along unbounded hidden targets, `|gamma_1-gamma_0| log(2+gamma_0)->0`.

The surviving finite-edge problem is therefore sharply split. In the strong-atom branch one must rule out or control algebraically strong near-edge zero pairs with vanishing normalized ordinate spacing and the signed phase relation needed for cancellation. In the diffuse branch no individual mode reaches the threshold and a separate collective theorem is required. Generic distant-frequency interference, exact multiplicity, sub-reciprocal clustering and dilution among arbitrarily many local tiny modes are no longer viable strong-atom escapes. The nonattained-edge and `Theta=1` branches remain separate.

## What is the correlation-SDP scale of the prime-phase torus?

VIS-213--VIS-216 reduce fixed-dimensional worst starts to unit-modulus torus optimization and give holonomy, fractional-packing and normalized magnetic certificates. VIS-217--VIS-218 close the normalized magnetic relaxation in the strictly subcritical prime regime because effective edge dimension is too large. VIS-219 prices cycle packing, and VIS-220 proves a universal ceiling for that LP: every simple graph leaves at least `7B/9` residual, so the certificate remains on the absolute-envelope scale.

VIS-221 identifies the full correlation SDP, which retains all pair correlations and contains the old magnetic certificate as a restricted dual ansatz. A stationary torus vector whose full dual stress is positive semidefinite is a certified global maximizer and gives rank-one SDP exactness.

VIS-222 supplies the decisive scale bridge: the complex symmetric Grothendieck inequality gives `M_abs(A)<=U_abs(A)<=K_gamma^C M_abs(A)` with `K_gamma^C<=8/pi-1` for every finite Hermitian `A`. Since VIS-213 identifies `M_abs(A_(y,H))` with the exact fixed-parameter worst-start supremum, the SDP and exact torus problem have the same bounded-versus-divergent Haar-RMS scale up to a universal constant. High rank affects optimizer recovery but cannot explain away a parametrically large SDP objective.

The live test is therefore the actual value of `U_abs(A_(y,H))/sqrt(R_v(y,H))` on the canonical prime matrices. An RMS-scale SDP value proves the static exact worst-start upper bound; a divergent SDP ratio forces the exact torus/worst-start ratio to diverge as well. Rank-one PSD stress remains a finite exactness diagnostic. One-parameter access time is separate.

The cross-line lesson with Weil Positivity is now sharper. Visual Exploration shows a **hierarchy of relaxation loss followed by a faithful surrogate**: two natural certificates are provably too weak, while the stronger correlation SDP is universally constant-factor equivalent to the exact static objective. Weil Positivity shows a **hierarchy of category/readout loss**: fixed positive readouts erase spectrum, fixed spectral calculus is singular, bounded resolvents decay, and the first infinite-mass logarithm is universal and orientation-sensitive. In both cases, nontrivial geometry is not enough; the source-forced observable must retain the exact interaction, scale and orientation consumed by the destination theorem.