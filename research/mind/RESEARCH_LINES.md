# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Carry the strongest matched control into the exact source-conditioned observable without losing phase, score, or gauge information

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Analytic Frontier now confines any Montgomery--Taylor five-point scalar zero to the joint residual region `q>0.1409` and `0.545<|d|<1.01`; horizontal phase enters exactly through the curvature correlation rather than as a decorative parameter. Visual Exploration gives the statistical analogue: residual direction is meaningful only in a frozen common Fisher geometry, with exact distortion control for bounded gauge families and complete sign non-identifiability under unrestricted mixed-sign reweighting.

Xi Flow supplies the strongest current source/control separation. XF-052--XF-054 identify the endpoint prime-free normal form and prove that the exact shrinking memory statistic remains `o(1)` uniformly over every fixed heat interval. The open cross-line bridge is therefore not construction of an endpoint carrier, but preservation of the source-only horizontal/mesoscopic information when it is converted into the transition-side Cauchy/coercive observable.

A decisive theorem must state the exact source-conditioned category, transport the strongest matched control to that same observable and scale, and prove that the residual survives without gauge tuning, phase averaging, or another quotient that removes the discriminating variable.

## Separate missing relation, completed encoding, averaging, analytic transport, and arithmetic zero selection

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Prime Lattice shows that addition is genuinely absent from the multiplicative exponent quotient, but regularized determinants, uniform shift averaging, theorem-accessible logarithmic sectors, and complete degree-four additive averaging can delete or universalize that channel. The live theorem must distinguish the gates: a relation is absent from the old quotient; the source selects a non-diffuse coupling; the analytic carrier preserves it through regularization/averaging/limits; only then can a sign/coercivity/zero-selection theorem use it.

Prime Circle and Weil Positivity now give a second exact example. The signed cyclotomic radial flux carries a real prime-power selector, yet scalar Mellinization is classical zeta/Ramanujan data, constant symmetric cross-shell coupling collapses to the von-Mangoldt endpoint vector, and pointwise-positive prime-power flux is not a positive semigroup coefficient. Introducing a selector is only the first gate; its ordered/signed content must survive the operation that turns it into a usable global object.

## Match infinite assembly to source-fixed metric, relative operator ideal, and canonical limiting topology

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Arithmetic Fidelity now separates several source geometries exactly. Without a source metric, full generator gauge leaves projection geometry. Covariance-only affine naturality forces the Mahalanobis metric and a second-order ceiling; a source-natural statistical family can instead supply full-law Fisher geometry, but AF-141 shows that each downstream compression retains only the conditional-score component.

Prime Flute supplies the operator analogue. The complete short central Dirichlet sector is benign in every `S_r`, `r>1`. Absolute recoupling across a pinching cut nevertheless has an order-one zero-mode trace budget, while matched prime/clone recoupling cancels that mode and becomes trace-summable. The remaining burden is the actual body/interface/global Dirichlet-to-Neumann assembly. A theorem must take the relative difference before ideal norms and prove that the cancellation survives the full uncut geometry.

## Find a source-fixed signed decomposition that reduces the target-bearing mode, not merely a supercritical complement

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Möbius Cancellation has crossed the old generic support barrier. MC-088--MC-089 show that a support-supercritical common-prime omitted block can be recursively cheap on every large scale because Euler-factor deletion reduces it to smaller Mertens problems. MC-090 then exposes the remaining information bottleneck: the retained gcd-sieve carrier still contains the unsifted top Huxley--Watt/Mertens mode with coefficient one.

A useful source-fixed residual must therefore make **both** sides genuinely weaker than the target, or estimate them jointly so the top mode cancels before absolute bounds. Support size, properness, sign retention, and even a power-cheap complement are not enough when the retained statistic is algebraically target-equivalent.

## Cross the actual source-level coercivity threshold with new budget or signed orientation, not positive repartitioning of existing slack

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Weil Inertia closes smooth and naive singular repairs around confluence. WI-168 bounds same-deficit integer depth by `rank J<=4 Delta`; WI-169 is stronger and directional: the source tensor is negative definite on the horizontal odd quotient, so positive promotion of that quotient consumes the horizontal charge rather than exploiting it. The full positive odd layer is fundable only from a separate population-sized `B` budget.

Any remaining coercive route must therefore derive an independently evaluated source scale/budget or use a genuinely different signed/mixed observable. Discontinuity, inverse normalization, extra positive rank, or repartitioning existing slack is not coercivity.

## Couple provenance before positive scalarization, self-adjointization, algebraic incidence, closure, or averaging classicalizes it

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Weil Positivity and Prime Circle now expose the sign/order boundary together. The cyclotomic radial flux has total mass `Lambda(n)` and pointwise positivity exactly on prime powers, but WP-163 shows the sparse support occurs only at the critical Mellin homogeneity `alpha=1`; WP-164 rules out a positive semigroup origin; PC-179 shows shellwise Mellin zeros are exactly the classical zeta zeros; and PC-180 shows constant self-adjoint first-order shell coupling collapses to `Lambda Lambda^T/2`.

A live mechanism must therefore retain a **source-forced nonseparable signed or ordered relation before the operation that makes it scalar, symmetric, positive, closed, or averaged**. A plausible route may use radial-depth-dependent/nonlocal coupling, a second noncommuting structure, or another source-fixed operation, but it must preserve mixed-prime cancellation and beat the eta/zeta and endpoint controls.

## Convert Xi's now-uniform source selector into the transition-side coercive estimate

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

The previous endpoint-transport line has materially narrowed. XF-052 identifies the exact simple-pole prime-free normal form below `log 2/2`; XF-053 proves all finite heat jets remain background-only there; XF-054 proves that the actual shrinking memory statistic remains uniformly `o(1)` over every fixed heat interval by moving the height-independent carrier to `A log T` and using Xi symmetry plus the Euler product.

The highest-leverage Xi theorem is now the next handoff: convert this source exclusion into the signed tapered derivative/Cauchy coercivity required near transition while preserving the `log^2 T` mesoscopic scale. A failure must identify information lost by that specific transform or inequality; endpoint replenishment, complex roots, collisions, and raw infinite-sum ambiguity no longer explain the gap.
