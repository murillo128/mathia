# Global mathematical questions

## Which source resources reach the destination before observation becomes decoding, classical recoding or a target-mixed completion class?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md) separate information survival, source recovery and zero selection.

Arithmetic Fidelity now gives two exact examples of class-relative recovery. AF-369 makes pole residue `1` a complete rational-prime discriminator inside the integer-supported Delone class even though the divisor is universal there. AF-370 uses Hamburger rigidity: inside the ordinary-Dirichlet finite-order class, the exact completed Riemann functional equation gives `f=c zeta`, and `a(1)=1` fixes `f=zeta`. The source category and retained analytic datum must therefore be priced jointly.

Prime Circle supplies the complementary warning. PC-311--PC-313 show that mixed-conductor bilinear fusion classicalizes to one periodic coefficient and that adjoining its finite-Fourier dual only gives universal Hurwitz closure. PC-314 then proves that for the smallest canonical mixed pair both scalar eigensectors have linearly many zeros in strips with `Re s>1`. Exact Riemann-type symmetry can therefore be present without any RH-like zero-selection mechanism.

Prime Lattice has lowered its destination bill rather than increased source recovery. PL-321--PL-323 close the moving Green transfer, residual moment and coercive equation for the actual branch. The remaining gate is the rational-prime-specific sign/first-crossing law carried by the transferred scalar.

The live audit is: **what exact source class and retained relation identify the intended object, what stability/conditioning is paid to reach the destination, and what separate theorem controls the divisor or sign once the object has been identified?** Decoding, universal transform closure and source recovery are not substitutes for the final zero-sensitive implication.

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) track the increasingly narrow channels left after bulk controls close.

Nyman--Beurling now closes both obvious horizontal scales for the two-point signed exterior sampler. NB-150 shows that mesoscopic separation `1<<L_G=o(G)` makes both signed lobes background-filled at `Theta(log G)` mass. NB-151 closes the fixed-scale loophole under a logarithmic target packet: the exact horizontal `xi'/xi` conservation law forces `Theta(log G)` adverse zero charge into a bounded neighboring annulus. A surviving signed route must change kernel geometry, shrink the scale, use signed prime information, or couple to target-bearing Nyman structure before lobe separation.

Robin Extremal makes the source channel terminal. RE-153 identifies the Pareto-weighted Chebyshev-deficit average consumed by a self-tangent counterexample. RE-154 shows order-one sensitivity to sparse post-selector relocation at fixed multiplicative distance. RE-155 pushes the matched-source obstruction through every fixed fraction `(1-epsilon)U_A(p)` of the full Robin horizon: exact agreement there plus PNT-scale control in the terminal fraction still leaves an order-one ambiguity. The residual theorem must reach a shrinking terminal layer or use stronger arithmetic information that forbids the coherent relocation.

Farey Discrepancy separates two endpoint taxes. FD-150 gives the explicit child depth/distortion phase curve `I(alpha)=1-alpha+alpha log alpha`, but the fixed-anchor parent amplification remains order `T` at every ancestry depth. Typical-depth reach can remove the child sparsity bill; it cannot regularize the anchor.

The common question is no longer whether a broad theorem holds on average, but whether the exact target-bearing configuration is controlled in the norm and population the destination consumes.

## Which growing source law is informative rather than merely connected, high-dimensional, zero-free, interval-pseudorandom or reconstructive?

Farey Discrepancy now prices the positive ancestry route explicitly: a fixed anchor always needs order-`T` parent concentration, while depth `alpha log log T` requires child amplification at least `sqrt(log log T)(log T)^(I(alpha))`. Any positive mechanism must explain those source costs rather than merely exhibit connectivity or extra generations.

Möbius Cancellation provides a useful closure in the correct observation category. MC-317 shows that generic coding capacity is insufficient; MC-319's smooth-modulus zero-free information is too coarse; MC-320 shows ordinary interval cancellation can coexist with maximal dyadic prime-shell bias. MC-321 then applies a prime-supported Halász--Montgomery family estimate directly to the `R` distinct simultaneously biased endpoint characters and rules out the exact natural reciprocal endpoint for its common-modulus range. The missing resource was **prime-shell family energy**, not primitive conductor provenance alone.

The next useful theorem in any growing representation must survive its exact algebra and matched controls and be stated on the same population, norm and scale as the destination. Many observations, connectedness, zero-free regions, small interval sums and source provenance are not scalar resources until that transfer is proved.

## Which representation-faithful null remains after nuisance variables, weak topology and universal finite scalar positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) continues to separate observable normalization from the induced source law.

Visual Exploration has now closed the structural nuisance-quotient question for the symmetric-Dirichlet model. VIS-246 identifies the log-product `Lambda=sum log X_i` as the sufficient coordinate; VIS-247 gives the exact `alpha`-free conditional coarea law `Z(lambda)^(-1)J(x)^(-1)d sigma_lambda` and an exact witness that the tent statistic need not be constant on a level. The remaining gate is the actual high-dimensional critical-prime residual under this frozen conditional law, not whether nuisance elimination exists in principle.

Weil Positivity remains the finite-scalar universality warning. WP-319--WP-323 show that source-specific values and arbitrary fixed finite confluent Pick jets may survive while their scalar positivity is still a universal resolvent Gram. The useful discriminator must therefore be support-sensitive and non-Pick, or move to matrix/operator/infinite-domain/global finite--archimedean structure.

The common criterion is whether the destination relation survives honest nuisance propagation and is nonautomatic in the matched-control category.

## Which noncompact source channels and adaptive representation costs must survive a positivity reduction?

Weil Inertia has moved from a frozen essential-spectrum obstruction to an adaptive finite-sector problem. WI-307 raises the exterior reserve and makes the fixed-aperture essential spectrum positive. WI-308 restores the clipped source tail exactly and gives a positive multiplier. WI-309 chooses endpoint-singular adaptive localizers so that the tail floor is **uniform**: `T_L>=c_0 I` with `c_0=1/(64 pi e)`, while the essential spectrum of the comparison stays in `[1,infinity)`.

The remaining price is the compact/discrete sector. As the localizer approaches the `L^2` endpoint, the localization-defect compact correction loses regularity; WI-309 does not uniformly control its finite negative eigenvalues. The live theorem is therefore a joint estimate on that adaptive compact sector against the fixed source-tail gap, not a sharper tail lower bound or a return to the frozen `beta_0=1/16` comparison.

A construction must show that the exact source budget dominates the finite sector uniformly enough for the aperture limit and then still pass the signed-density quotient of WI-241. An impossibility theorem must survive all admissible adaptive reserve/localizer choices.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is more specific, not closer by rhetoric. AF-370 shows exact source identification can arise from source class plus functional equation without supplying zero selection; PC-314 supplies the complementary off-critical functional-equation control. FD-150 separates fixed-anchor parent singularity from the child depth trade. MC-321 closes the natural reciprocal endpoint by matching theorem and observation category. NB-151 closes the fixed-scale two-point signed isolation loophole. RE-155 moves the Robin source bill into a shrinking terminal horizon layer. VIS-247 makes the nuisance quotient explicit and nondegenerate. WI-309 makes the clipped-tail gap uniform and exposes the compact sector as the remaining operator cost.

For every proposed route ask: **what exact object reaches the destination; under which norm, scale, quantifier, topology, observation population and admissible source class; which universal/nuisance component has been removed and what debt did that removal create; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, universal transform closure or Gram positivity; and does a separate source theorem dominate the destination loss without reconstructing or assuming the endpoint?** A new representation is useful only when one of those answers changes mathematically.
