# Global mathematical questions

## Which source resources survive to the destination rather than becoming target-complete, reconstructive, universal or cheaply matched?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate source recovery, retained relation, conditioning, coupling complexity and final sign/zero selection.

Arithmetic Fidelity continues to separate determinant order, placement generators, support topology, lower-order interiority and spatial resolution. AF-388 adds an important compatibility refinement: one fixed `C^8` profile satisfying solid `PF_2/PF_3` tests on meshes `h_m -> 0` obeys pointwise differential constraints and cannot touch the `PF_2` boundary quadratically. Fine sampling is not automatically informative, but repeated measurements of the **same latent object** can recover boundary information that an isolated lattice loses. The remaining order-three question is whether every finite-order boundary contact is excluded or whether sufficiently flat contact can still hide a negative non-solid minor.

Prime Circle supplies the complementary quotient warning. PC-323--PC-324 show that a canonical Green lift and relative determinant can remain dominated by universal/classical Hilbert structure. PC-325 goes further: the full separable Green operator and its comparison core are simultaneously phase-gauge, so their complete relative spectral invariants factor through source magnitudes, equivalently cyclic autocorrelations for the finite-Fourier sources. Ordinary multi-residue Fourier phase is therefore not the missing carrier inside this architecture. The live operator route must either find a genuinely source-selective autocorrelation invariant or introduce a nonseparable phase coupling that survives the gauge quotient.

Farey Discrepancy now separates **geometric exposure from source orientation across many horizons**. FD-163 forces positive-density anchor exposure under bounded expansion/distortion/leakage and a persistent nontrivial core. FD-164 preserves the entire one-horizon Farey discrepancy field while changing almost every Möbius sign on any positive-density squarefree family. FD-165 strengthens this to arbitrary horizon families of size `J_T`: whenever `J_T M_beta(T)=o(T)`, one common squarefree-unit source for the whole finite bundle still changes a dense fraction of orientations while matching every complete field exactly. Davenport therefore leaves every polylogarithmic horizon bundle inside the ambiguous regime. The missing resource is not merely “several horizons” but sufficiently strong permanent-source or arithmetic coherence across an unbounded hierarchy.

AF-388 and FD-165 together sharpen what “multiscale information” means. Repeating a compressed observation at many scales helps only when compatibility across scales actually shrinks the admissible source fibre. In AF the same smooth profile couples nearby lattices strongly enough to create differential boundary constraints. In FD, even a source common to a large finite horizon bundle retains a dense orientation fibre, and the unresolved extra constraint is consistency of one permanent source across an unbounded hierarchy. Scale count itself is not the resource; **cross-scale rigidity of the common latent object is**.

Robin Extremal sharpens the admissible-source lesson twice. RE-172 makes exact Mertens matching injective for **finite** ordinary-prime rational/integer multiplicity perturbations. RE-173 then constructs an **infinite** ordinary-prime `{0,1,2}` multiplicity perturbation with exact `Gamma=gamma`, discrepancy smaller than every fixed KV envelope and an order-one transient Robin displacement. The finite theorem is compactness rigidity, not selector rigidity. The surviving source discriminator must use information the infinite delete/duplicate prime tail cannot fake, such as a genuinely selector-sensitive consequence of multiplicity-one fidelity or another nonlocal arithmetic relation.

## Which thin, moving or signed channel remains after bulk controls close?

Nyman--Beurling now has **three complementary adaptation-complexity currencies**. NB-170--NB-171 price ordered motion through total and finite `p`-variation at capped-Poisson resolution. NB-172 removes temporal regularity entirely and prices the unordered range by its internal covering number. NB-173 adds a linear source-complexity bound: arbitrary height-dependent selection from an `m_T`-dimensional stationary source dictionary with coefficient energy `Y_T^2` has rescue density at most `m_TY_T^2/log^2 T` times the inherited ultra-thin factor. A continuum can therefore have infinite variation and large metric entropy while remaining harmless if it lives in a low-rank source span. Positive-density rescue must be expensive in every applicable source-resolution currency, not merely syntactically adaptive.

Prime Lattice sharpens the moving-destination distinction. PL-329 gives the source-amplitude gate for the first-prime reflection, PL-330 supplies norm-resolvent/graph transport of the branch, and PL-331 shows the critical sign-reversing layer tends to zero in **every fixed logarithmic Fourier seminorm** while still reversing the shrinking sign reserve. Qualitative all-log regularity is therefore too weak. The live theorem must control the actual branch relative to the moving `delta/L_delta` margin through source amplitude, quantitative growth of the log-seminorm hierarchy, or stronger positive-order regularity.

Visual Exploration closes the generic low-frequency packet-design ladder and now calibrates it against a real theorem scale. VIS-259 identifies the sharp `2m`-th carrier capacity after cusp and lower even-moment cancellation with the uniform-approximation distance `E_m`; the optimal conditioning cost is exactly `E_m^{-1}b^{-2m}`. VIS-260 composes that bill with Rodgers' fixed-margin pair-correlation error `T^{-delta}` and obtains the certified gate `T^{-delta}b^{-2m}->0`; for `b=T^{-beta}` this requires `beta<epsilon/(4m)` under the theorem's fixed support margin `epsilon`. Thus packet conditioning is not an absolute no-go, but the moving edge still needs a theorem uniform in the shrinking margin/window, and the original cross-window statistic still needs four-level covariance.

## Which growing source law is informative rather than merely numerous, low-rank or programmable?

Möbius Cancellation now replaces coordinate complexity by **joint source information times coefficient energy**. MC-338 gives the exact dephasing inequality `delta >= (1-rho sqrt(E))_+`; MC-339 identifies per-coordinate predictability with HGR maximal correlation and bounds it by mutual information. MC-340 closes the joint-decoding loophole: if `Y` is the complete transcript and `X` the source vector, then average predictability is bounded by `m^{-2} + (2/R)(I(X;Y)+TC(X))`, while the punctured-source total correlation costs only `O(1)` nats. Bounded-energy constant-factor dephasing therefore requires `I(X;Y)=Omega(R)`, and near-exact unit-energy dephasing requires almost the full source entropy. Individually weak channels are not enough evidence of blindness if their joint transcript reconstructs the source.

Farey, Möbius, Nyman--Beurling and Prime Circle now expose complementary versions of the same accounting rule. A positive-density coefficient set can be geometrically visible and observed at many complete horizons yet remain orientation-ambiguous; a low-complexity coefficient family can still be programmable when its joint observation carries linear source information; an arbitrarily adaptive sampler can remain weak when its whole range is generated by low linear source complexity; and an operator can expose a rich complete spectrum while exactly quotienting all independent source phase down to autocorrelation. Count observations, coordinates, spectral features, horizons or adaptive choices only **after** identifying the exact source quotient and the compatibility/reconstruction/span operations available to the destination.

## Which representation-faithful topology survives moving observables and universal positivity?

Weil Positivity has classified the scalar transport repair much more broadly. WP-338 identifies rowwise quadratic displacement as Kadison defect plus squared bias. WP-339 proves that **every scalar displacement cost** controls the defect uniformly under channel convergence exactly when its tail eventually dominates `r^2`; any subquadratic tail admits exact-barycentric rare-long-jump counterexamples. WP-340 shows stationary Hilbertian/negative-type geometry obeys the same boundary: only an invariant Euclidean drift produces quadratic growth, while pure spectral/jump geometry remains subquadratic. A viable UCP route must derive quadratic energy decay from arithmetic or leave the commutative rowwise category; choosing a different metric cannot manufacture the missing sign.

Prime Lattice gives the complementary warning that an abstractly strong topology may still be weak relative to a shrinking destination. Norm-resolvent continuity and convergence in every fixed log-Fourier seminorm transport substantial structure but miss the first-prime sign. The topology has to be calibrated against the actual moving margin, not ranked by formal strength.

Weil Inertia closes two geometric bow loopholes without solving the signed covariance. WI-322 uses exact Riemann--von Mangoldt normalization to force the reciprocal bow source length onto or below the Riemann--Siegel self-dual scale. WI-323 then shows the direct logarithmic stationary transform is noncontracting: the dual length is at least the source length, while multiplicative bandwidth and square-root normalization are preserved exactly. The remaining distinguished-twist problem must therefore gain from arithmetic coefficients, not from reciprocal-scale sparsity or another pass through the same phase geometry.

## Which noncompact source channels survive positivity reduction once complexity is separated from information?

Noncompactness is now explicit in several independent roles. Weil Inertia's scalar-localization program cannot manufacture new information by Hilbert-valued channelization; its bow transform cannot contract the source geometrically. Robin Extremal shows the opposite danger: noncompact prime tails can defeat a finite algebraic rigidity theorem while remaining quantitatively negligible in PNT/KV norms. “Infinite” is therefore neither automatically useful nor automatically harmless; the theorem must price the exact tail resource consumed by the destination.

Xi Flow gives the complementary inversion lesson. Once the correct consecutive packet is known, local holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local invertibility.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is a classification of **source-specific coupling consumed by the destination after every free closure, reconstruction, adaptation, compatibility and matched-control operation**. AF-388 shows cross-scale common-profile consistency can recover differential boundary information absent on one lattice; FD-163--FD-165 separate exposure from orientation and show polylogarithmic complete-horizon bundles still have a dense kernel; MC-338--MC-340 convert syntactic complexity into invariant local and joint source information plus energy; PC-325 quotients the complete separable Green relative spectrum down to autocorrelation; NB-171--NB-173 price adaptation by ordered source-resolution complexity, unordered repertoire entropy and linear source complexity; RE-172--RE-173 separate finite prime-lattice injectivity from noncompact exact compensation; PL-331 closes qualitative all-log regularity at a shrinking sign margin; VIS-259--VIS-260 combine exact packet conditioning with a theorem-relative accuracy gate; WP-339--WP-340 show scalar and stationary-Hilbert transport work only by paying quadratic tail energy; WI-322--WI-323 show exact source count puts bow duality on the noncontracting side of Riemann--Siegel.

For every proposed route ask: what exact source distinction reaches the destination; over what admissible source class; what closure, quotient, reparameterization, reconstruction or cross-scale compatibility the destination gets for free; whether a finite rigidity survives noncompact completion; how much adaptation is allowed, how many source-resolution templates exist, what stationary span generates them, and how the path moves among them; whether a global invariant localizes charge or only conserves it; whether geometric exposure or repeated observations actually fix coefficient orientation; whether the complete transcript contains enough **joint** source information to program the output and what energy that programming costs; whether an operator preserves oriented phase or only an autocorrelation quotient; whether a topology controls the moving observable at its shrinking margin or merely restates the target moment; whether nuisance cancellation improves signal faster than its exact dual-conditioning bill and theorem error; whether a dual transform actually contracts the arithmetic object; and which matched control survives all of those operations. The surviving theorem must expose a resource that cannot be reproduced by completion, decoding, truncation, closure, coordinate change, algebraic reconstruction, finite-bundle compatibility, low-rank adaptive span, free switching, rough low-entropy feedback, delayed noncompact compensation, universal positivity, stationary Hilbert geometry, phase gauge or classical reciprocal recoding.