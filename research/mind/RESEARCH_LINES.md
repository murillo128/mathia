# Global mathematical questions

## Which source identity turns critical mixed-prime data into a genuinely nonlocal positive sampling law?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) separates coherence, hostability, category descent, scalar sign, and source-retaining compression.

WP-267--WP-272 show that the critical Mangoldt amplitude can be hosted on a source-native rapid-energy rigging, while ordinary positive stationary spectral repairs either require a stronger category or erase the arithmetic atoms. Finite-order conditional positivity and continuous scalar filtering are exhausted.

WP-273 adds a stronger RH-independent compression boundary. In the canonical positive-minus-positive realization of the pole-completed carrier, every positive range is the graph of a contraction from weighted prime-power samples to the pole-continuum component. Any nonzero continuous test space stable under local cutoffs fails this inequality, and on a bounded interval with `N` prime-power atoms a supported positive subspace has dimension at most `N`.

The live geometry must therefore be globally nonlocal in a source-native way: an analytic/interpolation/model-space law, matrix/noncommutative coupling, or another construction that canonically reconstructs continuum mass from arithmetic samples before sign is read. Merely enlarging the host, filtering the same carrier, or choosing a localizable positive subspace is no longer enough.

## Which information survives the exact destination readout after quotient, source restriction, resolution, and defect-scale interface losses?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) now have sharper source-dimension, observation-window, branch-tail, activation, and interface controls.

AF-295 identifies eta zero-count margin as intrinsic projective distance after quotienting global complex amplitude/phase. AF-298 leaves the optimal ambient `N^(-1/2)` observation bill; AF-299 proves that an unrestricted nontrivial linear quotient collapses exact failure distance. AF-300 resolves actual complex-affine source restriction: a proved intrinsic source dimension `r` genuinely replaces `N` by `r`, but every remaining complex source direction is still essential. The next compression escape must be non-complex-linear arithmetic admissibility, a genuine saturated symmetry, or a changed observable/failure geometry.

PF-312 separates normalized angle from diagonal inverse strength. PF-313 identifies bounded source-weighted Green strength exactly with a form domination `J J* <= K^2 F`, equivalently defect-scale decay on near-unit singular directions. The destination interface is not controlled by a raw Green norm but by whether the actual source/reassembly maps avoid those directions at `sqrt(1-sigma^2)` scale.

Prime Circle still supplies both sides of a quotient boundary: fixed-upper swap-invariant statistics factor through prime-ratio power-spectrum data, while the accepted ordered mesoscopic witness does not. The open question is whether that extra ordered information survives matched controls strongly enough to matter to the target.

VIS-190--VIS-191 identify `y/log y` as the sharp absolute-envelope resolution threshold. VIS-192 proves signed start variance `asymp 1/H` in the diverging subcritical regime; VIS-193 upgrades this to every deterministic start interval of length `L>=C y^2`. The remaining observation question is whether coefficient-weighted prime-ratio spacing can shorten that coarse worst-gap horizon, or whether the target naturally supplies a sufficient start budget.

## Which source cancellation theorem crosses the remaining subpower concentration and low-order quotient-rank barriers?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md) compares Farey and Möbius without transferring operators.

FD-072--FD-077 move Farey from protected record episodes to full-counting-exponent frontier-near source abundance. FD-078 now rules out polynomial dilution even after weighting horizons by their physical Schur energy: annular total energy, nonsquarefree energy and Schur residual all have exponent `2 Theta`, so energy-weighted occupation loses no fixed positive power. The remaining Farey exit is a nonvanishing aggregate lower bound, a source-specific no-concentration theorem eliminating subpower loss, or another exact aggregation bridge.

MC-244--MC-246 collapse the complete blind family to one rough Möbius coset `-H`; MC-248 proves generic fixed witness exponents cannot force quotient triviality through support. MC-249 adds a progression-occupancy squeeze on every prime-power divisor of `exp(G/H)`, pushing surviving torsion toward low order. But low exponent does not bound primary rank or quotient index. The exact Möbius exits are now simultaneous low-order shell generation or signed cancellation of the complete rough-coset aggregate.

## Which source-specific inequality prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia now close complementary generic escapes.

PL-285--PL-287 separate the sharp ambient inverse-log modulus from the actual compact simple ground branch. PL-288 shows why the existing finite reduction still cannot make that improvement quantitative: despite an exact-state `L^2` tail below `6.65e-9`, bounded-shift uncertainty remains around `10^-8` against a `10^-17` positivity margin, and the `sqrt(lambda_1)` tail scale is sharp from the available block/eigenvector data. The remaining theorem is weighted Fourier-tail regularity, source-specific cross-term cancellation, or a direct interval certificate.

WI-258 supplies the exact source-coupled phase cone; WI-259 shows prime activation first requires finite-scale positivity beyond `(log 2)/2`. WI-260 shows the public FP-0.35 generator is not yet an exact certificate. WI-261 further proves that the nominal exact-prime proofctl path judges a surrogate: its exact-prime reconstruction is not used in assembly, a midpoint-generated interval can exclude the true prime entry, and the judge changes the required nonzero `c_L` to zero. The next gate is independent exact recertification of the intended `lambda_(7/20)>0` matrix; only then does the one-prime phase problem become established input.

Weil Positivity supplies the global counterpart: even if a carrier can be hosted, positivity that retains the arithmetic source now requires a nonlocal sampling/reconstruction law rather than a scalar or localizable repair.

## Which scale-distribution or continuation geometry survives after local structure is exactly factored out?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md) now has sharper exact residual coordinates on both Nyman and Robin sides.

NB-074--NB-076 reduce stable continuation to raw volume minus future-conditioned cancellation and resolve that cancellation into a nonnegative lattice of endpoint partial correlations. NB-077 then proves that rectangular cross-cut `kappa` area is a log-determinant information charge preserved monotonically under exact integer branching; raw continuation volume is also monotone. Signed refinement therefore cannot erase coarse predictive information. The live Nyman gap is **mesoscopic localization**: force enough of that branch-stable charge to remain near the moving cut to recover `L_R-O(1)`, because total preserved charge may spread over descendant rectangles whose width grows with scale.

RE-076--RE-079 reduce the late Robin fan to one positive transport measure `mu`: corrected Chebyshev growth is its total mass, while fringe-corrected Mertens decrease plus first-layer births is its `1/(s log s)`-weighted mass. RE-080 then localizes that remaining freedom on any long same-block fan: if selected logarithmic sizes stretch by `r->infinity`, the effective selector lies between complementary power scales `r^(1-b_1)` and `r^(1-b_0)`, with unweighted and weighted moments developing opposite power-front tail bounds.

The live Robin question is source-specific and branch-sensitive: can ordinary-prime event geometry realize those moving complementary fronts, or can the long-stretch branch itself be excluded? Bounded-stretch fans remain separate. The common Nyman/Robin question is which renormalized global residue remains after local structure is exactly factored out and whether the arithmetic source constrains its **location** strongly enough to matter.

## Does the complete flute response remain controlled after defect-scale source strength is separated from angle?

Prime Flute remains at the final interface frontier. PF-303--PF-312 close scalar depth, entrance leakage, fixed-low-band screening, first residual conversion, and a supposed second inverse-normalized angle as explanations for the explicit reservoir.

PF-313 identifies the remaining diagonal Green factors exactly. Uniform boundedness of `G_C^(1/2)J_C` is equivalent to `J_C J_C* <= K^2(S-ER^(-1)E*)`, and similarly on the high side. On near-unit `T_H` directions, the actual source/reassembly must vanish at the defect scale `sqrt(1-sigma^2)` or the corresponding diagonal strength diverges.

The unresolved theorem is therefore source-specific: prove these defect-scale dominations for the physical maps, or show that normalized angle, multiplicity/ideal decay, or signed reassembly absorbs any divergent side. A return-potential route is useful only if it proves one of these exact source-versus-shorted-energy inequalities.
