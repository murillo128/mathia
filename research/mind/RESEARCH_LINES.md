# Global Mathia research lines

This file holds genuinely cross-line mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history.

## Measure the complexity that survives the exact endpoint quotient, not the complexity visible upstream

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-002-the-surviving-arithmetic-variable-is-relational`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Arithmetic Fidelity shows that hidden provenance should be quotiented by the downstream discriminator before recovery cost is charged. Möbius Cancellation shows the signed analogue: shell mass and fixed local filter order are not endpoint complexity because every fixed finite local filter followed by absoluteization leaves almost-square bulk; even uniform fixed-power transfer attenuation needs logarithmic growing range. Prime Lattice likewise places shifted parity outside the subpower local-factor sigma-algebra.

Analytic Frontier adds an adversarial correction to the notion of “intrinsic complexity.” ANF-080 temporarily made sorted-prefix multiplicity variation look like the surviving scalar resource, but ANF-081 closes every finite real multiset with one fixed two-level-clipping certificate. A complexity measure that disappears under a stronger destination certificate was proof complexity, not source complexity. The current Analytic Frontier begins only in genuinely nonreal geometry outside the ANF-082 quantitative tube.

The cross-line question is therefore to identify an effective complexity that remains load-bearing **after the best known quotient/certificate is applied**: provenance code, signed nonlocal degree coupling, parity-sensitive tail interaction, nonreal conjugation geometry, or another source-specific relational resource.

## Separate retention, extractability, activation, theorem-scale visibility, and local source resolution

A resource can survive algebraically and still fail to matter at the final theorem. Weil Inertia now gives the strongest chain: a bow may carry coherent reciprocal signal; the complement may cancel it; unit trace may block activation; the global normalization may make it invisible; and localizing to the bow may still leave the required prime Dirichlet polynomial longer than the available height. Reopening the source at count saturation reduces the surviving problem to a signed self-dual shifted-von-Mangoldt covariance, not to positive prime sparsity.

Prime Flute provides the operator analogue. The complete decoupled thin sector reaches weak `S_1` and the exact-area body comparison reaches strong endpoint `L^1`, yet the uncut operator may still lose logarithms through the conservative splice/critical gradient halves. Xi Flow has excellent center-local source recovery and enough local frequency bandwidth, but the normalized Vieta dictionary can still be ill-conditioned. Visual Exploration separates a frozen witness from a full-law claim and now also separates observed second/third-order structure from exact shared-prime-phase null geometry.

A candidate should state which of these gates it crosses and at what scale. Passing retention is not evidence for visibility; passing visibility is not evidence that the source theorem can resolve the needed arithmetic object.

## Require stable inversion or normalization after exact recovery

**Linked intuitions:** `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure` and `MI-008-exact-recovery-is-not-stable-transport`.

Arithmetic Fidelity and Xi Flow now expose the same failure in different categories. AF-167 gives exact finite recovery of a Blaschke zero divisor from degree plus `n` phase-gradient moments, while AF-168 shows the inverse is only Hölder at collisions and becomes uniformly Lipschitz only away from the discriminant. XF-078 gives exponentially accurate center-local finite-band approximation with the correct mode count, but XF-080 shows that outer-mode normalization can amplify hidden coefficient smallness into a macroscopic Vieta mode.

The next cross-line bridge theorem should therefore control the **inverse map actually consumed downstream**, not only the forward approximation. Degree, multiplicity, separation, normalization denominators, outer coefficients, and other singular loci belong to the mathematical claim whenever they determine whether retained information remains usable.

## Treat scalar spectral features and visual structure as controls until matched source classes are classified

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Prime Circle now gives a particularly sharp scalar control. Minimal reciprocal renormalization at `0<alpha<=1` yields an exact prime-power Fourier-sign classifier, but the same family creates real zeros on sufficiently large prime shells at `alpha=2`. The feature is arithmetic-classifying yet deformation-dependent.

Weil Positivity gives the operator-category analogue. Lossless `det`/`det_2` inherit a one-sided passive phase and are too rigid for Gamma; higher modified determinants and dissipative Schur one-ports can change phase orientation but are then too flexible for passivity to certify the sign. Visual Exploration gives the statistical analogue: covariance and third-order resonances can be exactly forced by the shared-prime-phase null.

Across these lines, a spectral zero, phase sign, determinant response, covariance ridge, or bispectral band becomes arithmetically interesting only after matched controls show that it is source-forced rather than selectable by deformation, regularization, dissipation, smoothing overlap, or harmonic resonance.

## Match local-to-global transport to the exact topology, norm, and conditioning consumed downstream

Prime Flute and Xi Flow sharpen this warning from different categories. Prime Flute has local endpoint budgets but still needs an endpoint conservative splice in the uncut operator topology. Xi Flow no longer has a center-domain or local bandwidth mismatch, but it still needs a conditioned map into the weighted log-Vieta/selector resource. Prime Lattice shows the observation-side version: changing affine window regimes either erases the target, reduces to pointwise evaluation, or imports the hard zero-frequency cancellation.

A bridge theorem should therefore be formulated directly in the destination topology and coordinates. A stronger but mismatched norm can fail for irrelevant reasons; a weaker one can lose the discriminator; an accurate forward norm with a singular inverse can also be useless.

## Demand new coercivity whenever a no-go category is exited

Weil Positivity now makes the principle two-sided: the closed lossless passive category is too rigid, while higher regularization and dissipation escape only by forfeiting automatic phase order. Möbius Cancellation similarly shows that leaving fixed local filtering for growing/nonlocal transforms does not supply endpoint cancellation; source-uniformity, conditioning, and signed reconstruction must be proved afterward. Xi Flow's local surrogate escape from global seam bandwidth still needs a conditioned divisor/resource map.

The general research line is to pair every category change with a replacement coercivity, reconstruction, or stability theorem stated **after** the change. “Not covered by the obstruction” is only a frontier condition, not positive evidence.