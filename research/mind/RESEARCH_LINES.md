# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Characterize complete support, target transport, category-relative repair, and cross-scale compactness before choosing a realization

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Arithmetic Fidelity now separates gates that finite-dimensional intuition tends to merge. Repair can exist continuously and canonically in a nonlinear metric category even when linear splitting fails, while global Lipschitz regularity or a linear tangent can force the split back. Compact-target fidelity across growing resolution separately requires coherent non-escaping witnesses.

A decisive positive must therefore prove target recovery and stable realization in a category whose source equivalences, target transport, repair regularity/naturality, perturbation geometry, and cross-scale compactness are all independently specified. Per-cutoff exact recovery or a category chosen after seeing the target is insufficient.

## Distinguish exact arithmetic/source fidelity from zero selection and from scalarizable readouts

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Prime Lattice now provides a sharp positive example: critical-line reflection phase can identify Grosswald--Schnitzer generators, and integer discreteness compresses any fixed prefix to finitely many phase samples, even though the whole control family shares the zeta zero divisor. Prime Circle provides the complementary scalarization boundary: low connected full-chord spectral layers reduce to classical pair/triple singular-series functionals.

A decisive RH mechanism must therefore separate three questions explicitly: does the representation identify the arithmetic source; does the readout remain irreducibly nonclassical under matched controls; and what independent sign/unitary/coercive theorem localizes the already represented zero divisor?

## Make matched controls as strong as the claimed quotient, source regime, cancellation algebra, bulk law, reference class, and regularity category

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

The control depth has increased. Analytic Frontier reduces finite pair-feature enrichments to one signed profile with explicit normalization slack. Möbius Cancellation exposes RH-equivalent annular zero/Mellin modes if coarse and oscillatory pieces are decoupled. Prime Circle reaches pair/triple singular-series controls. Prime Flute reaches compact-reference instability and complete marked-tail equivalence with an all-composite clone. Prime Lattice separates continuous from integer phase identifiability. Weil Positivity closes endpoint-damping and broad local regularization escapes.

Weil Inertia adds an important qualification: an exact representation-level screening alias remains valid, but independent Fujii number variance rules out its canonical long double-density extremizer at positive zeta density. Thus a control used to prove nonidentifiability must match the represented quotient; a control used to model the actual source at positive density must additionally satisfy known source constraints at that scale.

## Target the arithmetic polarization that links exact zero-sensitive representations to positivity

**Linked intuitions:** `MI-004-critical-axis-is-not-zero-selection`.

The program has strong controls on both sides of the desired bridge. Deninger's template explains how a positive Hodge pairing would force the half-axis; Meyer realizes the complete zeta divisor spectrally; Suzuki supplies an unconditional positive zeta-derived Hilbert norm; Grosswald--Schnitzer phase data can even recover arithmetic generators inside an isozero family. Weil Positivity independently supplies a positive Riemann Gamma Schoenberg symbol, yet broad canonical local couplings to the critical finite-prime carrier diverge.

The live theorem is not another critical normalization, source fingerprint, trace formula, zero-spectrum model, or positive auxiliary space. It is a **canonical arithmetic identification/polarization on the zero-sensitive object itself**, or an equivalent upstream sign theorem, that excludes the off-axis alternative.

## Derive quantitative non-escape after microscopic rescaling and through global assembly

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim` and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

The branches expose complementary escape modes: witnesses lose compactness, full-rank systems lose coercivity, fixed-depth exponent gains disappear at `1/log T` horizontal scale, absolute zero-gap continuation shrinks like `1/log^2 T`, spectral defects classicalize in local windows, marked prime-flute tail lengths converge uniformly to a composite clone, and relative scalars move under reference changes.

A decisive positive must carry a uniform margin in the target normalization and show that local information survives the **complete assembly** used by the final operator, orbit sum, determinant, or positivity theorem. Nonzero finite-stage structure and pointwise vanishing error are both insufficient without a summability/coercivity statement.

## Classify the universal boundary model and its information budget before using a singular, limiting, finite-window, nonlinear, or refinement escape

**Linked intuitions:** `MI-006-classify-the-boundary-model-before-calling-it-arithmetic` and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Current boundary models are increasingly explicit: category-relative quotient repair, signed support-one duality with normalization slack, Huxley--Watt iteration versus RH-equivalent modes, singular-series local chord hierarchies, deterministic sampling/Poisson bulk, Grosswald--Schnitzer finite-versus-analytic phase fidelity, complete marked-tail clone equivalence, support-one screening plus source-admissibility tests, and critical positive-completion bulk/low-frequency roughness.

A decisive positive must identify a source-specific residual **after** the applicable boundary model and information budget are removed or matched, then prove that residual remains quantitatively accessible in the same category. A decisive negative is a theorem that the entire proposed escape factors through the universal model.
