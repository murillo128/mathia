# Global mathematical questions

## When does repeated observation actually shrink the admissible source fibre?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md), [MI-022](intuition/MI-022-unbounded-compatibility-can-turn-finite-cutoff-ambiguity-into-a-stability-problem.md), and [MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md) separate retained information, common-source compatibility, conditioning, acquisition geometry, aggregate readout capacity and final destination selection.

Arithmetic Fidelity now separates three resources at smooth order three. AF-394 identifies the exact continuum target: `q=-(log f)''` must satisfy `q>=0`, `(log q)''<=2q` on `{q>0}`, and `{q>0}` must be connected. The shrinking solid hierarchy recovers the first two analytic conditions but not component topology. AF-395 shows fixed successor radius can miss that topology on a vanishing prime-log mesh. AF-396 locates the first-order reach wall at `(e-1)N`; AF-397 resolves the next scale by showing that detection needs only one local mesh width of overhang past physical log-prime reach one. For `R_N=floor((e-1)N-alpha N/log N)`, `alpha>e` is eventually blind and `alpha<e` eventually detects, giving `R_N^crit=(e-1)N-eN/log N+O(N log log N/log^2 N)`. The first-order equality radius is therefore detecting. **Resolution is still not relational reach**; this topology costs linear prime-index reach, but the boundary is controlled by overhang relative to mesh rather than by a fixed determinant margin.

Farey Discrepancy shows that redundancy is source-class relative. FD-171 proves consecutive midpoint horizons have zero exact redundancy over unrestricted formal sources. FD-172 shows a bounded squarefree multiplicative source cone turns the transformed defect into nonnegative mass and lets dyadic horizons certify the Möbius target. FD-173 closes the exact sparse-horizon classification: target-rigidity holds iff every odd prime-power orbit hits the observed half-annulus union. FD-174 makes every fixed integer geometric ladder finitely certifying: a bounded-denominator Dirichlet step plus integer separation gives an explicit sample prefix `K_B(X)=O_B(X^Q_B log X)` that controls all prime coordinates through `X`. The open issue is no longer existence of a finite certificate for these ladders, but its sharp growth, source weakening and destination stability.

Robin Extremal gives the converse warning about source tariffs and finite scalar constraints. RE-180 proves that honest nonnegative prime multiplicity forces any early two-jet repair to pay `Omega(sqrt(p)/log p)` actual deletions unless it pays a remote KV reset budget. RE-181 constructs a `{0,1,2}` ordinary-prime source that pays exactly that order of tariff while matching the first two Euler boundary coordinates. RE-182 extends the construction to **every fixed finite jet depth**: after recentering the jets, fixed multiplicative repair shells have a uniformly conditioned Vandermonde response, so bounded honest prime repairs can match all derivatives through any fixed `K`, preserve KV fidelity and still retain the transient Robin excursion. A necessary resource bill or any fixed finite list of scalar Euler jets is therefore not a selector if the declared source class can afford and interpolate it. The live distinction is growing-dimensional/joint source information or selector-coupled placement geometry.

Together AF-397, FD-174 and RE-182 sharpen the multiscale lesson. Raw scale count, ambient row rank, fixed constraint count and lower-bound tariffs are not intrinsic information currencies. One must declare the admissible source fibre and ask which relations remain accessible at the destination's physical scale: arbitrarily fine local sampling may still require linear-in-index reach to cross a topology wall, with the final threshold set by mesh-relative overhang; nonlinear source positivity can reduce an ambient irredundant temporal system to sparse orbit-hitting target tests and arithmetic discreteness can make those tests finitely certifiable; and a large discrete source tariff plus arbitrarily many *fixed* scalar jets can remain nonselective when a matched source construction realizes them.

## Which quantitative resource reaches a thin or moving destination?

Prime Lattice now isolates one principal remaining first-prime gate. PL-329 gives the sharp source-amplitude condition for the shrinking reflected sign; PL-330 transports isolated eigenbranches in norm-resolvent/`L^2` topology; PL-331 proves every fixed logarithmic Fourier seminorm still misses the hostile moving layer; and PL-332 shows that, once the endpoint remainder is family-uniform, `L^2` continuity transports the selected asymptotic coefficient to the fixed threshold. PL-333 blocks the ordinary full-space Hopf shortcut and PL-334 blocks the even Markov repair. PL-335 then finds a genuinely state-specific odd mechanism: monotonicity of the completed Archimedean kernel plus odd reflection reverses the remainder sign, putting the lowest odd threshold state under an antisymmetric logarithmic-Laplacian Hopf lemma and forcing a noncollapsed half-log boundary amplitude. The fixed `C_0!=0` gate is therefore closed on the RH-relevant odd ground branch. The remaining first-prime burden is the **uniform source/remainder estimate** required by PL-329 as the rational-prime channel turns on.

Visual Exploration now has a scale-sharp and leading-null-correct moving-test transfer law. VIS-259--VIS-264 separate packet conditioning, universal pole background and the prime-power curvature residual. VIS-265 imports Wang's short-interval pair-correlation support restriction `lambda<theta`, and VIS-266 shows `T`-dependent tests are allowed when their support and seminorms fit the proof budget. VIS-267 proves that a width-`b` smooth packet carrying unit `2m`-th moment necessarily has `||g||_inf \gtrsim b^(-(2m+1))` and `||g'||_inf \gtrsim b^(-(2m+2))`, with matching constructions. VIS-268 adds the missing theorem-interface condition: Wang's leading functional contains both `g(0)` and `int |alpha|g(alpha)dalpha`, and both can be annihilated without changing those sharp seminorm exponents. The current interface therefore still requires `1/(L b^(2m+2))->0`; for `b=L^(-q)` the gate remains `q<1/(2m+2)`. The bottleneck is now a source-specific arithmetic residual that survives this fully leading-null class and dominates the amplified remainder.

Nyman--Beurling now separates worst-case and typical-height acquisition. NB-176--NB-180 show genuine Euler frequencies are nonselective if weak channels may be renormalized independently. NB-181 proves that retaining true amplitudes inside the natural feature Hilbert space makes a Hadamard-scale `N x N` response cost RMS norm `Omega(sqrt N)`. NB-182 prices the physical scalar escape outside `ell^2`: the Euler--Wiener budget is `W(a) asymp1/a`, and the worst-case local source norm left a square crossover `aH_T^2=O(1)`. NB-183 shows that crossover is **not** a typical-height obstruction: for the actual capped-Poisson seminorm `mathcal H_(a,B)(t)`, `T^(-1) integral_T^(2T) mathcal H^2 << 1+(Ta^4)^(-1)`. Thus for `a->0` with `Ta^4->infinity`, the charged scalar acquisition ratio beats the generic `sqrt N` packing threshold on density-one heights. The live problem is coherent source-natural rank/phase reuse on a common good-height set, plus the ultra-thin `Ta^4=O(1)` regime, not mere scalar exposure on typical heights.

Xi Flow gives the inverse lesson: once the correct consecutive packet is known, local holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local inversion itself.

## How much source information can an analytic architecture exploit at bounded conditioning?

Möbius Cancellation now separates acquisition syntax from intrinsic reliability geometry. MC-338--MC-345 show that bounded-energy dephasing requires extensive joint source information and that fixed-quality noisy coordinate access pays channel capacity. MC-346--MC-347 add the logarithmic reliability tariff and replace raw query count by accumulated discrimination when fidelity varies. MC-348 removes the raw-coordinate model altogether: for the complete arbitrary transcript, vanishing average source-coordinate error forces `Omega(R log(1/bar p))` symmetrized KL across the natural neighboring-state graph. One-shot joint coding, adaptivity or high-dimensional mixing can change how this edge divergence is supplied, but cannot eliminate it when the destination really needs those neighboring source states distinguished.

FD-169--FD-174 and NB-178--NB-183 provide complementary warnings. Unrestricted acquisition can hide the inverse in the sensor; changing horizons can hide many rows behind one spatial coordinate; strong nonlinear source constraints can reduce ambiently irredundant rows to sparse orbit-hitting tests; integer arithmetic can make qualitative recurrence finitely certifiable; enlarged duals can manufacture generic metric phase codes; genuine Euler labels remain nonselective if amplitudes are normalized away; the correct non-Hilbert Euler topology carries a Wiener acquisition tariff; and average vertical source cancellation can make that tariff affordable on most heights without constructing coherent high-rank reuse. Source-information cost is meaningful only after source class, target quotient, acquisition topology, operator variation, amplitudes, matched controls, quantifier and downstream resolution are fixed.

This updates [MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md): measurement count, nominal dimension, positive noise, adaptivity and canonical probe labels are secondary. The load-bearing quantities are inverse modulus, orbit-hitting and certification scale, source-class-relative kernel, per-access capacity, source-edge transcript divergence, source-amplitude conditioning, acquisition-space norm, height quantifier and the number of genuinely independent operator/source states required by the destination.

## Which spectral or positivity enrichment survives its intrinsic quotient and density law?

Prime Circle has reached a classical autocorrelation boundary. PC-325--PC-330 establish the phase/autocorrelation quotient, universal Hilbert background and failure of smooth Li subtraction. PC-331 writes the source-sensitive excess as a positive Hankel/Hardy-square energy of Fourier magnitude squares, and PC-332 decomposes it dyadically. PC-333 then shows that the surviving cumulative-band estimate is, at reciprocal scale, equivalent up to constants to cyclic short-interval prime-discrepancy variance. The scalar Green-Hardy target is therefore not an autonomous spectral mechanism: absent a geometry-forced proof or stronger structure, it is the classical short-interval variance problem in Fourier coordinates. Magnitude-matched controls still reproduce the whole scalar statistic and phase information remains absent.

Weil Positivity has narrowed the Maaß--Selberg escape past generic arithmeticity. WP-345 shows the positive height boundary square is divisor-blind; WP-346 realizes the scalar CDD control with positive self-adjoint Robin channels; WP-347 moves the control onto the same one-cusp hyperbolic surface via a point interaction. WP-348 now finds the same logical freedom inside unperturbed squarefree congruence scattering: ramified local factors are source-defined reciprocal unitary-axis factors with explicit off-critical divisors. Moreover fixed channel combinations cannot strip one bad-prime logarithmic derivative while retaining the common completed-zeta block. The surviving route must distinguish the **unramified global normalizer from ramified local dressing before scalarization**, inside a genuinely positive arithmetic mechanism; broad labels such as Hecke, Euler or congruence arithmetic are not selective enough.

Weil Inertia gives a positive example of source correction removing a false resonance but now closes both obvious bow-height de-exceptionalization scales. WI-327 restores the exact signed coefficient `Lambda-Lambda_X^sharp` and kills the cubic resonance on density-one starts. WI-328 shows an entire still-live bow is twist-frozen on the endpoint cubic source block. WI-329 moves to the first natural scale where bow heights genuinely dephase, but the matching almost-all nilsequence theorem is already uniform over all fixed-degree polynomial phases on one common good set of starts. Height diversity therefore does not create independent exceptional sets. The remaining obstruction is the distinguished source start and, separately, the stronger `L^2` norm required by the exact covariance.

These lines support [MI-024](intuition/MI-024-control-subtraction-must-match-the-load-bearing-source-channel.md): a matched control or subtraction is useful only when it acts in the representation and norm carrying the alleged signal. Smooth density subtraction can miss sparse Hilbert energy; an exact source correction can cancel a generic resonance; a source-sensitive excess may collapse to classical short-interval variance after a phase-blind magnitude quotient; phase diversity may leave one common arithmetic exceptional set; and even genuine arithmetic scattering may contain ramified divisor-mobile inner factors that positive fixed-channel geometry cannot strip selectively.

## Which source fingerprint is selective rather than merely reconstructive?

Robin Extremal and Farey Discrepancy give opposite source-class effects. FD-173--FD-174 show strong multiplicative positivity can make extremely sparse infinite-horizon target testing rigid and finitely certifiable through prime-power orbit recurrence plus integer separation, even though the ambient temporal linear system has exact null directions. RE-181--RE-182 show that ordinary-prime support, bounded honest multiplicity, KV fidelity and **any fixed finite Euler-jet depth** still admit explicit matched repairs that move the Robin destination. Source restrictions matter only when they remove the *destination-relevant* matched fibre, not merely when they make reconstruction more expensive or add finitely many scalar constraints.

Arithmetic Fidelity exactly separates local analytic recovery from global target topology and now prices the topology wall through second order in prime-index reach. Prime Circle isolates a dyadic Hardy-square profile and then classicalizes its scalar band theorem as short-interval variance. Visual Exploration has a complete leading-null theorem-access tariff but still lacks an arithmetic residual above it. Nyman--Beurling shows source fidelity includes charged acquisition topology in both Hilbert and Wiener geometries, while NB-183 warns that worst-case acquisition barriers may disappear on density-one heights without supplying coherent rank. Weil Inertia shows the exact signed source removes generic resonance while both frozen and genuinely dephased bow-height families fail to repair the exceptional-start quantifier. Weil Positivity shows that even genuine congruence arithmetic carries ramified off-critical inner factors unless the unramified block is isolated by stronger pre-scalar structure. Prime Lattice now shows the opposite subtlety: completed operator positivity may fail globally/evenly, yet an exact odd reflected kernel comparison can still produce the state-specific Hopf sign needed for fixed-threshold noncollapse.

The program must therefore separate **source fingerprint**, **admissible source fibre**, **target quotient**, **acquisition/transport topology**, **source amplitude and renormalization cost**, **relational reach versus spatial resolution**, **mesh-relative overhang at topology boundaries**, **fixed versus growing source-coordinate depth**, **channel-matched control subtraction**, **conditioning/capacity/reliability**, **local analytic recovery versus global component topology**, **resource lower bound versus matched realizability**, **joint versus scalarized geometry**, **ramified versus unramified arithmetic structure**, **global operator positivity versus state-specific reflected sign mechanisms**, **exceptional-set quantifiers versus typical-height affordability and phase diversity**, and the final **destination-selective sign or zero theorem**.

## Global discriminator: identify the exact resource a matched control cannot fake

For each proposed route ask: what exact source distinction reaches the destination; what source class and target quotient are being compared; what information is regenerated for free; whether horizon/operator changes contribute independent constraints and whether source restrictions collapse their kernel through repeated defect orbits; whether qualitative recurrence is quantitatively certifiable at the needed source scale; whether finitely many source coordinates can be interpolated by a matched repair and how their depth must grow to become selective; whether the inverse has been moved into the sensor; whether weak source channels were independently renormalized and at what cost; whether the declared acquisition topology contains the natural source readout and what gain it charges; whether that gain is worst-case, typical or pointwise at the destination's selected height; what native norm gives a useful inverse modulus; what complete-transcript divergence survives at the downstream resolution; whether finer sampling also increases relational reach or only shrinks the physical stencil; whether a topology crossing needs a fixed analytic margin or only overhang relative to mesh; whether a lower-bound tariff is actually unaffordable inside the source class; whether a larger witness class merely certifies generic metric packing; whether local certificates recover only the differential cone or also component topology; whether a control subtraction matches the norm/channel carrying the signal; whether a source-sensitive scalar still factors through a phase/autocorrelation quotient or a classical window-variance transform; whether a positive/self-adjoint realization still permits divisor-changing controls **inside the claimed arithmetic category** and whether ramified factors can be separated from the unramified block before scalarization; whether failure of global Markov positivity still leaves a source-specific reflected maximum principle on the actual state; whether an almost-all theorem controls the distinguished sequence or is already uniform over the available phase family; and what theorem finally converts the survivor into a Riemann-zero or positivity restriction.

A useful bridge should fail on a matched control for an independently arithmetic reason. Exact reconstruction, unbounded observation count, unrestricted target-aware mixing, one spatial sensor with uncharged horizon variation, ambient row-rank lower bounds that disappear inside the source class, qualitative orbit recurrence without certification, any fixed finite list of scalar Euler jets, genuine frequency labels with erased amplitudes, bounded Hilbert duality that excludes the natural functional, non-Hilbert acquisition with uncharged Wiener gain, typical-height affordability without coherent rank, arbitrarily fine sublinear-reach stencils, a large but realizable source tariff, a rich spectrum, a critical boundary, source labels, a positive principal form, global/even Hopf failure when an exact odd reflected mechanism survives, operator-valued lifts, adaptive scalarization, singular tangents, a positive divisor-blind one-cusp boundary norm, generic congruence arithmetic with unstripped ramified inner factors, a nonzero low-frequency carrier, incomplete leading-functional cancellation, a large Gram/nuclear response, benign forward regularity, positive readout noise, adaptive or jointly coded observations without source-edge discrimination, infinitely many remote resets, smooth PNT subtraction, a phase-blind Hardy excess that is equivalent to classical short-interval variance, zero-set membership, raw positive von Mangoldt weighting, many twist-frozen bow ordinates, or many genuinely dephased heights controlled by one common exceptional set are not enough unless the surviving quantity is stably distinguishable, charged in the correct source geometry, theorem-accessible at the required quantifier and scale, robust under the full source operator, and selectively coupled to the RH destination.