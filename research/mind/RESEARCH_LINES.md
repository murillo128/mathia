# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Characterize complete support, admissible interaction closure, and robust distance from loss before choosing a repair

**Linked intuitions:** `MI-001-preserve-structure-before-compressing` and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Arithmetic Fidelity now supplies exact models for support loss, interaction closure, collective instability, and perturbative fragility. Multichannel exact support is a fusion-span question; stable use requires a collective lower-frame bound rather than pairwise angles; and in several natural categories the fidelity modulus is exactly the distance to the collision boundary. Prime Circle supplies parallel algebraic controls: finite cotangent complexity and fixed-support depth remain in endpoint/fixed-state closures, while fixed translation-invariant nonlocal kernel statistics reduce to classical Fourier data.

A decisive negative is a conflicting pair inside the **complete** statistic consumed by the mechanism or a proof that its distance-to-loss margin vanishes in the intended completion. A decisive positive must prove target recovery and stable realization in the admitted category without source-tuned programming. Downstream diagonalization, positivity, determinants, or infinite precision cannot repair annihilated support or zero-margin recovery.

## Distinguish exact joint relational fidelity from global rational-prime specificity and realizability

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`.

Higher-order or marked relational data can be exactly faithful modulo an intended gauge, but local joint fidelity can still be shared by non-Riemann sources or lost under the topology needed by the final theorem. Prime Flute retains directed multi-neck structure while an all-composite clone matches strong operator categories. Prime Lattice pointing and depth expose arithmetic data whose ordinary Gram spectra repeatedly classicalize. Weil Positivity recovers exact finite coefficients in positive pointed-cover families while fixed positive/coherent assembly still misses the global sign.

A decisive mechanism must pass four gates: prove joint relational completeness modulo the intended gauge; prove that the retained structure distinguishes the exact rational-prime source from matched controls; realize that discriminator stably in the required topology/operator category; and show that the global completion preserves it while supplying the needed sign, continuation, or spectral theorem.

## Make matched controls as strong as the claimed quotient, weighted correlation layer, normalization, and operator ideal

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`.

Controls must match the exact information layer consumed by the proposed invariant. Prime Circle now requires matching endpoint/Fourier closure **and** canonical Hilbert/Carleman conductor limits. Prime Flute requires the all-composite clone in the same relative operator category, not merely a local geometric control. Prime Lattice requires matching the PNT/log-uniform envelope and the prolate finite-rank sampling phase. Weil Inertia requires the exact signed finite-window Ramanujan operator rather than scalar support or positive additive energy. Weil Positivity requires matching fixed positive mixtures and finite coherent reference/global assemblies, not just one positive `log n` carrier.

A canonical relative object is useful only when its reference, algebra, topology, normalization, weights, module/action, operator function, and ideal are forced independently of the desired signal. The mathematical goal is to identify the first matched-control depth at which an invariant changes and verify that this depth coincides with the claimed source of arithmetic information.

## Separate critical-axis generation and RH-equivalent diagnostics from zero selection

**Linked intuitions:** `MI-004-critical-axis-is-not-zero-selection`.

A distinguished axis, exact finite Euler coefficients, or even an RH-equivalent boundedness criterion is weaker than a new zero-selection mechanism. The half-weight `1/2` is canonically generated in several unrelated ways, and Weil Positivity derives the complete scalar finite coefficient `Lambda(n)/sqrt(n)` from its pointed cover package. None of this supplies the missing global Weil sign.

The missing theorem is an independent selector or sign estimate derived upstream from rational-prime structure. Repackaging the continued zero divisor into a bounded signal, hull, Koopman system, pair-correlation statistic, Poisson bulk law, trace-class determinant, regularized determinant, or canonically normalized positive carrier is diagnostic/kinematic unless the decisive support or sign theorem is proved without importing the zero locations or a classical equivalent.

## Derive a realization category whose support, stability, ideal, and source theorem all survive global assembly

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`.

The current branches expose complementary category barriers. Prime Circle shows both nuclear zero-density and universal conductor-limit obstructions. Prime Flute shows that changing resolvent function can change the available scattering theorem without creating a new phase or zero set. Prime Lattice shows `S_2` universality together with a classical Nyquist-controlled `S_1` boundary. Weil Inertia shows that positive scalar interfaces erase the only remaining signs and complete-period scalar blocks are orthogonal. Weil Positivity shows that fixed positive/coherent assembly preserves finite coefficients yet forces the logarithmic response to a singular or moving-scale boundary.

The program-level question is whether an intrinsic middle category can satisfy **support fidelity, positive stability margin, matched-control separation, compatible operator ideal/functional calculus, global assembly/sign compatibility, and source-faithful analytic control simultaneously**. A no-go showing that every intrinsic category fails at least one of these gates would be equally decisive.

## Classify the universal boundary model before using a singular, limiting, or finite-window escape

**Linked intuitions:** `MI-006-classify-the-boundary-model-before-calling-it-arithmetic` and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Several current escapes live exactly where a simpler category breaks: large-conductor Hardy limits, `S_2` versus `S_1` Gram topology, finite-window Ramanujan truncation, singular cover angles, residue subtraction, or vanishing distance to a collision set. The new cross-line evidence shows that these boundaries often carry their own universal model — Hilbert/Carleman operators, prolate rank floors, LCM boundary ranks, weight anomalies, or coherent collective blocks.

A decisive positive must identify a source-specific residual **after** that boundary model is removed or matched and prove that it survives in the same category. A decisive negative is a theorem that the entire boundary escape factors through the universal model. Merely observing noncompactness, singularity, stronger-norm failure, or a new regularized object is no longer enough.
