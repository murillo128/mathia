# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry or programmable completion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity has now closed both the orientation-preserving affine orbit of the canonical Mangoldt carrier and the canonical scalar Herglotz projective gauge. WP-307 proves that the pole-normalized logarithmic coefficient `J=A_0-C_+^2/2` stays positive under every density-normalized positive affine spectral recentering. WP-308 then classifies constant positivity-preserving Möbius recodings of the scalar reflected Herglotz response: non-affine maps compactify the logarithmic end, while every map preserving that end is positive affine and retains the wrong positive `y^-2` curvature.

The remaining positive route must therefore introduce genuinely new finite--archimedean geometry before scalar comparison: an operator/matrix-valued boundary sector, source-derived `z`-dependent transform, quotient/compression with a new positivity theorem, determinant/scattering object or nonseparable finite--archimedean coupling. Neither input-coordinate nor scalar output-coordinate retuning is still a live repair.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) keep conditioning, source supply and destination sensitivity distinct.

Analytic Frontier still requires a joint arithmetic-placement law relative to its distinguished logarithmic carrier; matched one-point statistics are exhausted.

Arithmetic Fidelity now has a uniform transport obstruction. AF-346 shows that sign/gap concentration can be saturated by simple perfect-power recodings. AF-348 strengthens AF-347 from aligned cutoffs to the whole cutoff axis: for fixed `k`, Riesz smoothing of order at least `k-1` makes the sparse weighted `k`-power background only bounded-error distinguishable from dense unit mass for every large real cutoff. The source certificate must therefore be source-discriminating **and quantitatively transported by the destination operation**, not merely visible before smoothing.

Nyman--Beurling now supplies a positive scale law as well as an obstruction. NB-127 says every fixed finite multibase system is globally unstable. NB-128 shows that the full natural prefix `n<=R` has a deterministic phase inverse modulus through height `Theta(R)`, with local ordinate resolution `Theta(1/log R)`. The missing bridge has moved from phase recurrence to amplitude visibility: target-bearing primitive mass must be forced onto cells where that phase modulus fires.

Prime Lattice similarly identifies rather than merely localizes its endpoint channel. PL-313 proves that a resolved physical corner reaches the first regular outer correction through one weighted scalar `M_±`, carried by the unique obstructed outer direction. The open theorem is the actual completed-Weil overlap decomposition that makes this scalar finite, matches the singular amplitude and balances it against the source.

Prime Flute's hard-cutoff angle remains noncompact, so progress there still requires a new split or observable.

## Which growing source law is genuinely informative rather than sparse, under-resolved or circular?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now has both sides of the scale-breadth boundary. FD-128 shows that even infinite exact Möbius ancestry can remain cheap when the active prime set is sufficiently sparse. FD-129 shows that the opposite extreme can become circular: complete prime-prefix ancestry through `Y_j`, together with a stage-uniform critical envelope on the recovered prefix, already transfers that envelope to the physical Mertens function and is RH-equivalent at the `x^(1/2+epsilon)` scale. The useful source law must therefore be broad enough to reduce free-core capacity without simply reconstructing the target and assuming its endpoint estimate.

Möbius Cancellation supplies a different scaling obstruction and a conditional arithmetic gain. MC-298 proves that every fixed finite hierarchy of endpoint intersections can be moment-matched while Page parity changes. MC-299 then shows that if the one allowed Page exception is genuinely near-negative, Deuring--Heilbronn repulsion expands the band in which every *other* shell mode is forced balanced from `exp(O(log y/log log y))` to `exp(O(log y log log log y/log log y))`. Bounded overlap order still cannot reconstruct parity, but any surviving multi-mode exceptional family must now pay a larger source-cost bill.

Together with AF-348 and NB-128, these results sharpen the discriminator: the relevant resource is not “more structure” in the abstract but a law whose **order, density, resolution and uniformity match the destination without importing the endpoint itself**.

## Which support geometries survive the one-signed Weil first-crossing constraint?

WI-294 changes the large two-island frontier. Complete screening of the actual nonnegative null mode makes the pole term strictly positive, so zero archimedean energy requires negative gamma energy. Fourier uncertainty then imposes an absolute positive lower bound on support measure. The PNT forces a completely screened symmetric two-island width to vanish at large aperture, contradicting that floor.

Thus the large one-signed symmetric two-island branch is closed without a Cramér-scale prime-gap theorem. The live complete one-signed problem is whether a more complicated support topology can retain fixed positive total measure while avoiding all active prime-power translations and satisfying the null equation. Sign-changing modes, incomplete screening and bounded aperture remain distinct.

## What should Prime Circle retain below the diffuse square-zero defect?

PC-295 preserves the universal Green singular sequence, PC-296 collapses eigenvalues, and PC-297 identifies the universal square-zero operator class. PC-298 now quantifies the defect: under the same diffuse regime there is a square-zero approximant with expected operator error

`r_q = delta_q^(1/3) + delta_q^(1/2) log q + (log q)/N`.

Every shrinking pseudospectral scale `epsilon_q` satisfying `epsilon_q >> r_q` remains universal after its natural rescaling. The surviving one-profile question is therefore genuinely subdefect: does arithmetic information live at scale `O(r_q)` or below? Otherwise progress must use coupled profiles/levels, source-dependent left/right relations or a non-diffuse regime.

## Can the required Robin dominant cancelling pairs persist?

RE-129 merges spacing, comparable magnitude and cancelling phase into one inverse-window companion for a hidden dominant algebraically strong atom. RE-130 then removes the diffuse-cloud branch as an independent algebraic escape: the full absolute packet mass is at most `C sqrt(M_K) log(e/M_K)`, so algebraic packet mass always contains an algebraically strong atom.

The attained finite-edge algebraic frontier is therefore reduced to an actual zero-geometry question: can an off-critical edge support infinitely many dominant near-edge cancelling pairs with the RE-129 spacing and phase geometry? The nonattained edge and `Theta=1` remain separate.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225 supplies the coherent-block lower bound and VIS-226 matches it with the absolute tapered envelope. Throughout `H log y/y ->0`,

`I_abs(B) asy M_abs(A) asy U_abs^R(B) asy y/(H log y)`,

and the Haar-normalized scale is `y/(sqrt(H) log y)` up to constants. The static bounded-versus-divergent question is therefore closed at the full coarse order, not just from below.

The live questions are the critical/supercritical transition, sharp constants and optimizer geometry, faithful matched controls that distinguish arithmetic organization within the known scale, and dynamical access time of the physical prime-log orbit.

## Global discriminator: scale, transport and noncircularity must all survive composition

The newest evidence strengthens [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md). AF-348 makes downstream erasure uniform rather than subsequential. FD-129 shows that a seemingly stronger source hypothesis can cross from informative to circular by reconstructing the physical prefix and importing its target envelope. MC-299 shows genuine arithmetic feedback can enlarge a source-cost exclusion band without solving collective capacity. NB-128 shows that growing observation complexity can recover a quantitative phase modulus, but only before amplitude weighting. PC-298 pushes a universal quotient down to an explicit shrinking defect scale. PL-313 identifies a rank-one endpoint channel without yet proving the actual branch matching. RE-130 removes a diffuse algebraic escape by an absolute-mass interpolation. VIS-226 closes an entire static scale by matching lower and upper structures. WP-308 closes the standard scalar Herglotz boundary-coordinate gauge without closing genuinely higher-dimensional or `z`-dependent boundary geometry.

The common accounting rule is now stricter: after identifying a source coordinate, ask **at what order/density/resolution it is visible, whether every intervening map transports it with a quantitative margin, and whether the assumptions used to enforce it have already reconstructed the endpoint statement**. A route can fail by under-resolution, erasure, relaxation, universality or circularity. If any transfer between lines is only plausible rather than established, it belongs in a clue rather than in global mathematical knowledge.
