# Global mathematical questions

## Which source resources survive to the destination rather than becoming target-complete, reconstructive, universal or cheaply matched?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate source recovery, retained relation, conditioning, coupling complexity and final sign/zero selection.

Arithmetic Fidelity now separates **determinant order, placement generators, support topology, lower-order interiority and spatial resolution**. AF-379/AF-381 make vanishing prime-log sampling target-complete. AF-383 removes one spatial tuple at every fixed determinant order, and AF-385 localizes the strict lower-order side information to the sampled solid hierarchy. AF-386 shows that at order two strictness can be replaced exactly by connected positive support. AF-387 proves that this does not extend to order three: a connected `PF_2` sequence can pass every solid `3x3` test and still have a negative gapped `3x3` minor because lower-order zero minors inside the support block sign propagation. The remaining higher-order compression problem is therefore a zero-minor/interiority classification, not merely a finer support-topology condition.

Prime Circle supplies the complementary zero-selection warning across the coupling axis. PC-319--PC-320 preserve an off-critical local divisor near weak coupling; PC-321 becomes zero-free under strong additive exponent; PC-322 canonically selects `u=1` and the universal Hilbert `1/2` threshold. PC-323 retains the full Green operator and still finds only a rank-one finite channel tensored with the Hilbert core modulo trace class. PC-324 then shows that even genuine off-band zeros of the relative perturbation determinant occur in one-residue non-arithmetic controls as classical generalized-Hilbert edge eigenvalues. Source specificity has been pushed beyond “relative spectrum exists” into genuinely multi-residue interference or another relative invariant that matched one-channel controls cannot reproduce.

## Which thin, moving or signed channel remains after bulk controls close?

Nyman--Beurling has moved from worst-case compact transport to a **persistence-with-adaptation** discriminator. NB-165--NB-167 identify one-Poisson-width capped transport `Xi_G`; NB-168 rules out block-wide logarithmic rescue for a stationary compact template; NB-169 prices piecewise refreshes. NB-170 now replaces literal switch count by bounded variation in the same capped metric. A path with normalized variation `V_T` has only `M_eff(T) ≍ 1+V_T H_T/log T` source-distinguishable templates at logarithmic rescue resolution, where `H_T=min(a_T^(-1),log T/log log T)`. Under a subcritical stationary baseline, positive-density rescue forces `V_T ≳ T a_T^2 log^3 T/(X_T^2 H_T)`. Continuous adaptation is therefore a genuine resource only when its **Poisson-resolution path length** crosses the moving refresh tariff; arbitrary measurable/infinite-variation feedback and sparse exceptional heights remain outside this closure.

Robin Extremal now separates **global normalization from spatial localization** at the exact endpoint. RE-169 turns Mertens matching into a compensation-debt law for the Robin kernel and shows that vanishing future KV capacity makes that debt rigid. RE-170 allowed an order-one transient excursion while matching `Gamma` to critical precision; RE-171 sharpens this to exact `Gamma(Q_p)=gamma` with exact final repayment. No stronger scalar Mertens precision remains to buy. The missing source resource is necessarily selector-sensitive localization or another invariant constraining where future opposite-sign debt can live.

Farey Discrepancy now prices terminal migration for arbitrary finite prime anchors by the **first omitted prime**. FD-160 shows sparse holes disappear as an escape after divisor closure; FD-161 recovered a growing Boolean face for initial-prime anchors. FD-162 counts all squarefree primary states below `ell_S`, giving average closure pressure `(Q_sf(ell_S-1)-1)/2`. If the anchor contains the first `m` primes, this is `(1/(2 zeta(2))+o(1))m log m`. Hence every anchor family that eventually contains each fixed prime pays growing terminal-closure pressure; a genuine anchor escape must retain a fixed low-prime hole along a subsequence or let another budget fail.

Prime Lattice continues to separate spectral transport from sign transport at the first arithmetic activation. PL-329 orients the shrinking first-prime reflection under a source-amplitude hypothesis, while PL-330 gives norm-resolvent continuity and continuous simple eigenbranches across the activation. The sign-reversing perturbation can nevertheless vanish in that graph norm. The blocker is not branch continuity but control relative to the much smaller `delta/L_delta` destination margin.

## Which growing source law is informative rather than merely numerous, low-rank or programmable?

Möbius Cancellation now makes **source-relative reconstruction complexity** explicit. MC-332 shows unrestricted mode-dependent rows contain the exact conjugate-character matched filter; MC-333--MC-334 separate rank/energy and centered variation; MC-335 shows low Walsh degree in standard endpoint coordinates prevents approximate dephasing. MC-336 changes to source phases, where the matched filter is degree one, and repairs even rank by hiding each coefficient's own phase. MC-337 shows that odd-rank source phases satisfy `prod_i x_i=1`, so the hidden phase is algebraically recoverable from the others. Yet recovery costs degree `R-1`: every submaximal degree keeps normalized dephasing error near one, even after all nonconstant quotient characters are reachable. The invariant resource is therefore not syntactic variable exclusion but the cost of reconstructing the target phase after exact source relations are quotiented out.

The common lesson from AF-387, FD-162 and MC-337 is that a structural restriction must survive the **completion operation natural to the destination**. Connected support does not remove lower-order degeneracy; an apparently terminal or perturbed anchor regenerates squarefree volume under principal closure; and a hidden source phase can reappear through an exact parity relation. “Connected,” “terminal,” “local,” “low-rank,” “low-degree” and “variable-excluded” are not proof resources until the relevant closure, quotient and reconstruction costs are explicit.

## Which representation-faithful null survives nuisance removal, weak convergence and universal positivity?

Visual Exploration now has sharp low-frequency conditioning through quartic order. VIS-256 shows fixed polynomial moments cannot beat the sine-kernel `|q|` cusp. VIS-257 cancels the cusp with sharp quadratic cost `8b^(-2)`. VIS-258 cancels cusp plus quadratic channel and proves the sharp quartic inequality `|m_4|<=E_* b^4||nu||_TV`, `E_*=0.06346155...`, so unit quartic signal costs `15.75757... b^(-4)`. A quartic source route therefore needs a genuine zeta-minus-null quartic coefficient and transfer error `o(b^4)` after the exact conditioning amplification; extra cancellation is not free nuisance removal.

Weil Positivity now identifies **quadratic displacement energy** as the exact moving-observable stability quantity. WP-336--WP-337 decompose sufficient control into channel-error × bidirectional propagation and coordinate-bias × propagation asymmetry. WP-338 shows that rowwise `W_2^2` is exactly Kadison defect plus squared barycentric bias. Every `W_p`, `p<2`, can vanish while a fixed defect survives on growing support; every `p>=2` controls the defect, but `W_2` does so by directly measuring the second moment one wanted to prove small. Ordinary Wasserstein strengthening therefore closes as an independent repair: a useful theorem must force quadratic transport energy from source structure rather than assume it in metric form.

Prime Lattice's PL-330 gives the complementary warning: even a topology strong enough to transport the full spectral branch can remain too weak for a shrinking sign observable. The issue is not whether a norm is abstractly strong, but whether it controls perturbations relative to the destination margin that must be oriented.

## Which noncompact source channels survive positivity reduction once complexity is separated from information?

Weil Inertia closes scalar-localization self-bootstrap under arbitrary Hilbert-valued diagonal amplification. WI-320 supplies the fixed short prime-free witness; WI-321 shows the scalar semibound amplifies dimension-free and by polarization already generates the corresponding Loewner Gram inequality. Fixed PSD cross-channel metrics are consequences of the scalar premise, not new information. A viable vector route must add coupled inequalities outside that scalar-generated cone.

Xi Flow gives the opposite inversion lesson. XF-185--XF-186 show that once the correct consecutive packet is known, linear and local nonlinear holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local invertibility.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is a classification of **source-specific coupling consumed by the destination after all free closure, reconstruction and matched-control operations have been applied**. AF-387 separates support connectivity from lower-order interiority; NB-170 replaces free continuous adaptation by a Poisson-resolution variation tariff; RE-171 exhausts scalar Mertens precision and leaves spatial localization; FD-162 prices anchor exhaustion by first-omitted-prime squarefree volume; MC-337 replaces syntactic phase hiding by quotient-aware reconstruction complexity; PC-324 classicalizes one-channel relative-determinant edge zeros; VIS-258 gives the sharp quartic conditioning bill after cusp and quadratic nulling; WP-338 identifies quadratic transport as the exact UCP defect scale rather than an independent topology.

For every proposed route ask: what object reaches the destination and at what moving scale; over what family of centers/parameters the mechanism must persist; how much adaptation is permitted and what its path length costs at the source's actual resolution; what closure/completion/reparameterization or algebraic reconstruction the destination can perform for free; which lower-order degeneracies survive connected support and arbitrarily fine local sampling; whether an anchor still looks sparse or terminal after intrinsic divisor closure; whether an excluded coefficient variable is reconstructible through source relations and at what degree; whether a global invariant localizes mass or only conserves its total; whether relative spectral zeros survive elementary matched channels; whether a stronger topology merely restates the desired second moment; whether nuisance cancellation improves signal faster than its sharp conditioning bill; and whether every approximation error is small only absolutely or after multiplication by the geometric amplifier of the moving observable. The surviving theorem must expose a resource the destination uses and a matched control cannot reproduce by completion, decoding, truncation, closure, coordinate change, algebraic reconstruction, free switching, high-variation feedback, phase matching, delayed compensation, universal spectral background, classical edge spectrum, thin-tail averaging, channel duplication, universal positivity or classical recoding.