# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry, singular source calculus, finite-mass decay or programmable representation?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate source provenance, target sensitivity, relational structure, reachability, readout fidelity and motion law.

Weil Positivity has closed scalar recovery, fixed positive multi-channel recovery, finite multiplicative-equivariant depth, flat transport and several curvature/readout escapes. WP-300 leaves prime-symmetric scalar projective curvature only flat or universally fermionic. WP-301 shows that arbitrary noncommuting local defects in a positive frustration-free model collapse under the zero-mode readout to common-kernel selection, screening nonzero archimedean spectrum.

WP-302 traps ordinary moving kernels between homogeneous rigidity and free programmability. WP-303 closes the canonical fixed-source spectral interpolation: spectral cuts of one self-adjoint source operator commute, are norm-discrete and carry no regular nonzero Grassmann tangent. WP-304 closes the simplest nonlocal continuation as well: every bounded source resolvent compression has finite spectral mass and decays like `1/y`, so it cannot reach the Gamma logarithm.

The pole-normalized positive-energy Mangoldt measure does cross that mass barrier. Its regularized Herglotz response has real part `-log y+O(1)`, but the same leading term occurs for every positive unit-density measure and therefore fails the arithmetic matched-control test. The canonical even `+/- log n` carrier cancels that real logarithm exactly. The surviving route must derive a generalized/unbounded boundary, scattering, determinant or genuinely noncommuting finite--archimedean law whose **orientation, renormalization and subleading response are source-forced before Gamma comparison**, and whose informative spectrum survives the final positivity readout.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) keep conditioning, source supply and target sensitivity distinct.

Analytic Frontier now has a sharp bow interface beyond source marginals. ANF-185 replaces adaptive critical probing by one deterministic one-height high-pass with transition width `a_X`. ANF-186 then matches the natural prime/rough coefficient alphabet, densities, mean, diagonal variance and local absolute-mass envelope while still producing target endpoint memory by placing signs after reading the exact logarithmic carrier. The remaining theorem is a **joint arithmetic-placement law relative to the carrier**, or direct `o(a_X)` control of the deterministic weighted residual / signed Fejer defect. One-point source statistics are exhausted.

Arithmetic Fidelity has localized its source-complexity obstruction to the first prime layer and priced the simplest variable-background escape. AF-338 gives fixed-order full-Euler sign regularity once `sigma>r-1`. AF-339--AF-341 show that after pole cancellation the prime layer alone retains the RH right-half-plane pole discriminator and `Theta(P/log P)` ordered sign variation, while higher prime powers are analytically harmless there. AF-342 proves that an arbitrary baseline cannot reduce that variation below prime scale unless it reacts on a prime-scale set of prime sites or prime-adjacent composites. If its transform remains zero-pole-blind, the residual still carries the RH discriminator. The live bridge must show that such exceptional structure is generated by genuinely cheaper retained data, prove a growing-order observation theorem, or replace ordered sign variation by another finite source certificate.

Nyman--Beurling has now exhausted **all nested natural-cell cutoff coherence in the audited mixed-tail regime**. NB-118--NB-120 show that even `M=M(R)->infinity` with `M^6(1+log M)/R->0` admits perfect target capture at `(1+o(1))/(kappa_infty R)`. NB-121 makes every fixed nested ladder redundant with its maximal section. NB-122 extends this to the entire prefix `2<=r<=R`: one maximal matched control satisfies every smaller target projection exactly, and early cutoffs become even more compressed relative to their own `1/r` scale. The live theorem must use actual zero-power/confluent structure, force a faster source-section transition, or introduce genuinely non-nested/tail-sensitive information; widening the nested prefix is no longer an escape.

Prime Lattice has removed fixed-order regularity and classified the singular principal boundary geometry. PL-308 gives exact weak Pohozaev/virial stress for every fixed `s>0`; PL-309 shows the renormalized trace lives on an `exp(-Theta(1/s))` layer invisible to ordinary bulk convergence. PL-310 computes the stretched Green limit: a Volterra kernel plus a diagonal atom whose nonzero fixed-point cone is exactly `C/sqrt(e^(2r)-1)`. The shape is universal under bounded lower-order perturbations, so the remaining analytic resource is compactness/pass-through for the actual completed-Weil branch plus the scalar amplitude, followed separately by the rational-prime sign/first-crossing theorem.

Prime Flute's sharp-cutoff completed-angle branch remains noncompact: adjacent Fourier modes straddling the hard cutoff generate order-one canonical correlations on infinitely many separated modules. A viable continuation must change splitting/observable rather than sharpen the same compactness estimate.

## Which cancellation theorem remains after representation enlargement and source equations stop adding endpoint force?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy has moved beyond a finite squarefree network. FD-117--FD-120 show that the whole cumulative field, its minimal floor-quotient skeleton and complete single-horizon squarefree/unit-amplitude admissibility can still support critical near-extremizers. FD-121--FD-122 make one common source incompatible with simultaneous equality-ray tracking along every fixed squarefree dilation. FD-123 closes the bounded nonsquarefree-reset escape in Cesàro mean: for any bounded integer-ratio chain and any polynomially bounded common source, the average normalized skeleton deficit stays uniformly positive. This applies unconditionally to the physical Mertens source using only `|M(n)|<=n`.

The remaining Farey gate is therefore genuinely **pointwise or unbounded-scale**. FD-123 still permits sparse near-saturation, does not assign the average cost to the designated RH-relevant endpoint, and gives constants that deteriorate with the ratio ceiling. The next theorem must globalize the bounded-scale average obstruction to the prescribed endpoint, control unbounded/supermultiplicative scale jumps, or import a new arithmetic law beyond cumulative discrepancy.

Nyman--Beurling is the complementary falsifier for multiscale slogans. There, even the full growing nested prefix adds no independent source equation because all smaller constraints are projections of one maximal section. Together with Farey, this gives a concrete cross-line test: **coherence becomes proof currency only when the joint constraints are nonredundant near the local extremizers**. When they are nonredundant, a weak global envelope can sometimes turn pairwise incompatibility into average coercivity; when they are nested, no amount of scale multiplicity helps.

Möbius Cancellation has now separated finite-geometric rigidity from source complexity. MC-289--MC-292 force collision-rich shells toward a near-affine exceptional spectrum and bound its independent bias complexity by odd-girth/common-cell and entropy budgets. MC-293 shows that below source cost `D_y=exp(kappa log y/loglog y)` the near-negative spectrum contains at most one mode, the possible Landau--Page exception. Near-affinity of that singleton at the live defect scale forces its real zero within `O(1/(log y loglog y))` of `1`. Hence any larger independent exceptional family must be high-source-cost. The remaining arithmetic theorem must control that high-cost family, connect source cost to occupancy/multiplicity, or show that the singleton exceptional mode cannot carry the endpoint mechanism.

## Which exact source incidence survives support relaxation in Weil Inertia?

WI-288 reduces complete one-signed symmetric two-island screening to the nearest-prime-power Lambert gap, WI-289 identifies prime squares as the unique critical composite layer, and WI-290 gives at least `X^(3/8-o(1))` positive-length composite-safe components on `[X,2X]`. WI-291 confines eligible ordinary-prime upper hosts to a much rarer large-gap family with summable logarithmic measure.

The bottleneck is therefore a sharply asymmetric cross-scale incidence theorem, not abundance. A fragmented safe reservoir generated largely by prime gaps near `sqrt(x)` must be localized relative to rare selected gaps near `x`; the available count exponents do not yield the intersection by pigeonhole. Null-equation amplitudes/signs or a different support topology remain separate escapes.

## What should Prime Circle do after diffuse source averaging kills the local square-root residue sector?

PC-285 is the unconditional matched-control boundary. PC-286--PC-290 show conditionally on RH that, once total source count is matched, the exact one-profile normalized boundary-log singular spectrum collapses toward the control for every `B=o(sqrt(q))`.

PC-291 shows that the first denominator-microscopic relation forced at `B~sqrt(q)` is spectrally negligible. PC-292 proves the worst-case operator threshold sharp: a fixed same-source pair already has order-one normalized norm on a local band `T~N/log q`. PC-293 then shows that this mechanism disappears under the actual source averaging. For any diffuse law with maximal atom `O(log q/q)`, every local band `T=O(N)` at `N~sqrt(q)` contributes `o(1)` to the averaged normalized singular-spectrum law. Both the prime source and the count-matched Li-grid control lie in this class.

The local logarithmic singularity is therefore not the missing source-averaged square-root carrier, even though it has an order-one fixed-pair extremizer. The surviving one-profile question is whether an order-one **prime-specific remainder lives in the nonlocal residue complement** after source count and diffuseness are matched. Otherwise the architecture must retain information discarded before one-profile singular-spectrum averaging: cross-level, multi-source, directional, phase-sensitive, raw-resonance or noncommuting data.

## What survives after regular-CA placement and distant-frequency cancellation cease to be bottlenecks?

RE-119--RE-122 show that regular CA placement and every fixed algebraic edge hierarchy collapse to a stable profile whenever weighted near-edge mass is sufficiently flat. An algebraic anomaly of order `nu^-A` requires a subedge zero within horizontal width `O(log nu/nu)` at ordinate `O(nu^A log nu)`, so fixed-order visibility already requires polynomial-height approach to the attained edge.

RE-123 removes arithmetic phase placement as another fixed-order escape: the regular-CA mesh samples the entire signed subedge packet superalgebraically well. RE-124 then removes generic distant-frequency cancellation around an individually strong mode. A subedge atom of algebraic size can remain hidden only if comparable coefficient mass lies within ordinate distance `U^(-1+eta)` for every fixed `eta>0`, i.e. on a near-reciprocal `U^(-1+o(1))` cluster scale, or if the relevant algebraic mass is genuinely diffuse so that no individual atom reaches the target size.

The surviving finite-edge problem is therefore the existence of polynomial-height edge approach together with the **allocation and signed correlation of that boundary mass**: strong atoms require reciprocal-scale clustering to cancel, while diffuse packets need a separate collective cancellation theorem. The nonattained-edge and `Theta=1` branches remain separate.

## Can the full correlation SDP reach Haar-RMS scale on the prime-phase torus?

VIS-213--VIS-216 reduce fixed-dimensional worst starts to unit-modulus torus optimization and give holonomy, fractional-packing and normalized magnetic certificates. VIS-217--VIS-218 close the normalized magnetic relaxation in the strictly subcritical prime regime because effective edge dimension is too large. VIS-219 prices cycle packing, and VIS-220 proves a universal ceiling for that LP: every simple graph leaves at least `7B/9` residual, so the certificate remains on the absolute-envelope scale.

VIS-221 identifies the next stronger static object. The full correlation SDP retains all pair correlations and contains the old magnetic certificate as a restricted dual ansatz. A stationary torus vector whose full dual stress is positive semidefinite is a certified global maximizer and gives rank-one SDP exactness. For the prime matrices, however, usefulness still demands `U_abs=O(sqrt(R))`, equivalently an SDP deficit `B-O(sqrt(R))`; a constant-factor envelope improvement is not enough.

The live test is therefore the signed correlation-SDP value of the canonical prime matrices. An RMS-scale value would already prove the static upper bound. A rank-one PSD-stress solution would solve the finite torus optimum exactly. A large high-rank SDP value would close only this relaxation, not lower-bound the exact torus optimum. One-parameter access time remains separate.

The cross-line lesson with Weil Positivity is now sharper. Visual Exploration shows a **hierarchy of relaxation loss**: two natural certificates are provably too weak, yet a strictly stronger global correlation relaxation remains live. Weil Positivity shows a **hierarchy of category/readout loss**: fixed positive readouts erase spectrum, fixed spectral calculus is singular, bounded resolvents decay, and the first infinite-mass logarithm is universal and orientation-sensitive. In both cases, nontrivial geometry is not enough; the source-forced observable must retain the exact interaction, scale and orientation consumed by the destination theorem.