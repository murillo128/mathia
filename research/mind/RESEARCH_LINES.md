# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry or programmable completion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity has closed the natural scalar gauge orbit and the canonical scalar Schur repairs sharply. WP-307 keeps the pole-normalized Mangoldt curvature positive under density-normalized orientation-preserving affine spectral recentering; WP-308 closes constant scalar Herglotz projective recodings; WP-309 shows that holomorphic spectral dependence adds no scalar automorphism freedom. WP-310 adds an admissibility obstruction: the critical positive Mangoldt source has density-one infinite mass and a logarithmic Herglotz end, outside the finite-moment asymptotic class required by the classical infinity/moment Schur transform. WP-311 tests the natural fixed-interior scalar escape: it is admissible and positivity-preserving, but after unit-log end normalization it inserts a positive `log(y)/y` term above the Riemann-Gamma `y^-2` curvature scale.

The remaining positive route must therefore introduce genuinely new finite--archimedean geometry or a source-justified renormalization before scalar comparison. Scalar freedom can fail because a transform is not defined on the source class or because an admissible transform contaminates an asymptotic scale larger than the target discriminator.

## Which source resources survive into the exact destination margin, topology and moving resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) keep conditioning, source supply and destination sensitivity distinct.

Analytic Frontier still requires a joint arithmetic-placement law relative to its distinguished logarithmic carrier; matched one-point statistics are exhausted. Prime Flute's sharp physical low/high split is already noncompact, so progress there still requires a new split or observable rather than stronger estimates inside the same hard cutoff.

Arithmetic Fidelity now locates the Riesz conditioning boundary exactly. AF-349 proves full-source recovery in a strong derivative-variation metric while raw amplitude can collapse distinct sources. AF-350 shows that an `O(X^theta)` Riesz quotient still preserves Dirichlet principal parts in `Re s>theta`. AF-352 sharpens AF-351 from positive integer order to **every fixed real `delta>0`**: the critical beta multiplier decays like `|gamma|^(-1-delta)`, which is absolutely summable against zero density, while the multiplier remains nonzero in the half-plane needed to detect off-critical zeros. The unsmoothed `delta=0` endpoint fails the sharp `sqrt(X)` norm. The conditioning threshold is therefore mode-density versus multiplier-decay, not nominal transform order.

Nyman--Beurling now separates recurrence visibility from quantitative conditioning even across finitely many views. NB-132 can force a genuine narrow polynomial-height zero packet below the recurrence-visibility rank budget. NB-133 shows that the same coarse rank allowance plus Vinogradov--Korobov radial depth permits a formal phase cluster with a superpolynomially small scalar annihilator margin. NB-134 strengthens the obstruction: for every fixed finite family of integer bases, simultaneous Diophantine recurrence produces polynomial-height matched packets whose recurrences are visible at every base while all scalar annihilator margins collapse simultaneously. A repair must use actual-zero arrangement information, growing representation complexity, genuinely joint non-scalar information, or a different target functional.

Prime Lattice keeps the moving-observer version separate. PL-314--PL-315 show that weighted `L^1`, arbitrary smoothness, every fixed `W^{k,1}`, all-log Fourier moments and every `H^alpha`, `alpha<1/2`, can still miss a Green atom sampling `Q=r/s`. A source theorem must force moving-scale anti-concentration such as `Qm(Q)->0`, an `o(1/Q)` remainder, one-sided tail structure or a direct moving-sample estimate.

These lines support a concrete common statement: **terminal stability is topology-, scale-, mode-density-, phase-arrangement-, representation-complexity- and discriminator-relative**. A transform may improve conditioning without moving the discriminator; a recurrence may be fully visible in several fixed representations yet remain catastrophically ill-conditioned; strong global regularity may still miss a moving singular observer.

## Which growing source law is genuinely informative rather than sparse, diluted, over-rigid or circular?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now brackets the missing source law from both sides. FD-132--FD-134 show that primewise mean ancestry can monitor an explicit `H^(1-o(1))` family and still miss a positive-density defect concentrated on a moving central multiplicative-rank strip. FD-135 closes the obvious local repair in the opposite direction: in the squarefree unit-sign class, every local ancestry defect is `0` or `2`, so any `L^infinity` bound below `2` is exact ancestry. With a rank-`K` Möbius anchor, exact ancestry through `p<=Y` reconstructs the entire horizon iff the first `(K+1)`-prime `Y`-rough core exceeds `H`; `Y^(K+1)>=H` already suffices. The useful law must therefore live between mean dilution and exact rough-core reconstruction.

Möbius Cancellation now separates pointwise exceptional-zero control, family energy, field complexity, common-ramification capacity and quotient-support capacity. MC-304 packages all selected quadratic modes into one squarefree radical `P` and derives a radical-size/totient-density energy dichotomy. MC-305 uses the exact endpoint sign pattern that energy forgets: the shell occupies only `R` of the `2^R` sign cells of `(Z/PZ)^*/ker Theta`. Brun--Titchmarsh forces

`log(y/P) <= (2+o(1)) R log y/2^R + O(1)`,

so at `2^R~kappa log y` one already needs `P>=y/(log y)^(2/(kappa log 2)+o(1))`. Energy capacity and quotient-support capacity are distinct; the remaining theorem must rule out the near-shell radical/sign-cell configurations actually left by both constraints.

The cross-line lesson is sharper than “use stronger norms.” Nominal breadth can fail because normalization hides a moving active layer, while extreme locality can become exact source reconstruction. Algebraic packaging can move complexity into a different invariant, while an endpoint support pattern can impose a capacity constraint invisible to energy. **A source condition is useful only if its active-scale resolution and residual freedom are both explicit.**

## Which support geometries survive the one-signed Weil first-crossing constraint?

WI-294 restores the sign constraint of the actual completely screened first null mode and makes the pole contribution positive. WI-295 gives the exact gamma-bathtub support floor `mu_*<|{v>0}|<=log 2` and an exponentially decaying separated-`L^1` mass tariff.

WI-296 retains the actual global mass `s=||v||_1^2` in the same bathtub and closes it against the positive pole term on the same null vector. Complete screening forces

`H(s)+2s<0`,

hence, with `sigma_*=inf{u>0:H(u)+2u<0}`,

`mu_*<sigma_*<||v||_1^2<=|{v>0}|<=log 2`

and `P_a(v)>2 sigma_*`. The many-component escape can no longer make the **global** `L^1` mass arbitrarily small. The remaining issue is spatial distribution: can most forced mass stay in a bounded core while arbitrarily weak remote satellites satisfy screening, or can source-side tightness combine the global floor and separated-mass tariff into an `L^2` contradiction?

Unrestricted supported spectral relaxation, sign-changing modes, incomplete screening and bounded aperture remain distinct branches.

## What should Prime Circle retain after centered Gram formation and oriented-frame classicalization?

PC-295--PC-301 show that the universal diffuse square-zero class and centered same-source Gram formation erase orientation before averaging. PC-302 tests the simplest pre-Gram repair: reflection acts by latent mode reversal, so unlabelled frame/subspace geometry remains gauge; keeping labels recovers an odd sine cross-Gram, but same-law averaging classicalizes it to positive covariance of additive Fourier coefficients.

The next carrier must preserve a source orientation that is neither removable by latent mode relabelling nor reduced by the first positive quadratic compression to classical sine/cosine Fourier covariance. A canonical chirality/mode label, higher joint information, an independently justified order-sensitive observable, an asymmetric source window or a genuinely cross-level carrier remain distinct possibilities.

## Can the required Robin cancelling geometry occur for actual zeros?

RE-129--RE-131 turn hiding of a dominant algebraically strong atom into a nearby, logarithmically comparable companion with tightly constrained ordinate, horizontal displacement and cancelling phase. RE-132 shows that a sparse simple formal spectrum can realize the required doublet on sublinear windows. RE-133 remains under unresolved adversarial review, so no synthesis is strengthened from that growing-cluster matched control while its sidecar remains open.

RE-134 independently gives the positive local compactness statement: a packet with uniformly bounded cardinality inside a fixed reciprocal-scale box has a uniform visibility floor on a fixed-relative window. A full hidden packet must therefore lose compactness through unbounded local occupancy, order-one mass escaping every fixed reciprocal box, or both. If exterior cancellation is reciprocal-scale tight, occupancy in a fixed box must diverge.

The live attained-edge question remains a two-part source theorem: bound actual off-critical reciprocal-box occupancy at the dominant phases and prevent order-`M_K(U)` cancelling mass from escaping to larger reciprocal radii. The accepted cross-line clue connecting this to Nyman zero-count/conditioning controls is research workflow state, not mathematical evidence; its transfer still has to be proved inside Robin.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225--VIS-226 fix the strictly subcritical static Ising/torus/SDP scale up to constants. VIS-227 calibrates the deterministic critical occupancy floor. VIS-228 removes the uniform-independent false positive, and VIS-229 gives the exact nonuniform independent center/variance, including the `q_3-q_2^2` heterogeneity contribution.

VIS-230 now removes the next false baseline. For a stationary Markov cell process with the same one-point law, the exact center is

`E[S]=sum_(r=1)^(m-1)(m-r)c_r`,

where `c_r=P(X_0=X_r)`. In a finite ergodic chain the shift from the independent center is `mA-B+o(1)` and may be extensive. Matching the full one-point cell distribution therefore does not center a dependent control.

The critical collision route now begins only after fixing an admitted dependence model and calibrating both its lag-return center and its covariance. Only a standardized residual surviving that control should be tested for coupling to the signed prime-phase energy. If dependence explains it, a genuinely different observable is needed rather than another normalization of pair collisions.

## Xi Flow: move from local known-window inversion to global support discovery and zero-sensitive transport

Xi Flow has a uniformly conditioned local nonlinear holomorphic chart for known consecutive packets with linearly many samples. XF-185--XF-186 show that growing known-packet complexity is not itself a local conditioning barrier. The live problem is global: locate and recenter unknown windows without an oracle, assemble separated or multiscale packets, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side source control to a zero-sensitive quantity.

## Global discriminator: price what the endpoint actually sees

The newest evidence sharpens several existing global intuitions without establishing a new cross-line theorem. AF-352 moves the Riesz conditioning threshold from integer order to the open endpoint `delta=0`; FD-135 shows that escaping mean dilution by full locality can jump directly to exact rough-core reconstruction; MC-305 adds quotient-support occupancy as a capacity currency beyond energy; NB-134 shows that finitely many fixed phase views can fail simultaneously; VIS-230 adds lag-return structure to the admissible stochastic baseline; WI-296 turns one-signed screening into a global `L^1` mass floor. Robin's accepted Nyman transfer remains a clue rather than evidence while the cross-line dictionary is unproved.

For every proposed route ask: **what is the source discriminator; at which stage is it quotiented; in which coordinates/ranks/directions does it live; what topology and normalization retain it; which quantifiers are uniform at the active scale; which dependence and orientation survive the carrier; what phase arrangement, resolution or moving scale does the endpoint sample; how large is the admissible family and quotient-support capacity relative to the obstruction; does stronger locality merely reconstruct the source; does a representation reduce complexity or move it to another invariant; is the final transform admissible for the source class; does it preserve the asymptotic hierarchy containing the discriminator; and do the hypotheses already encode the desired endpoint?**

A route can fail by under-resolution, rank excess, simultaneous phase recurrence, unstable or topology-mismatched inversion, inter-rank or intra-rank dilution, over-rigidity and circular reconstruction, family-energy slack, quotient-support capacity, complexity transfer under repackaging, carrier gauge, classicalization after positive compression, moving-scale concentration, local noncompactness, deterministic/marginal/dependent control baselines, transform inadmissibility, asymptotic contamination, terminal quotienting, universality or certificate exhaustion.

If a transfer between lines is only plausible rather than established, keep it as a clue rather than promote it to global mathematical knowledge.