# Global mathematical questions

## Which source resources survive the exact destination quotient, resolution and quantifier?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) separate source supply, stable transport, normalization, destination sensitivity and worst-case geometry.

Arithmetic Fidelity now separates exact arithmetic survival from extraction cost inside one explicit channel. AF-362 prices a declared target by the exponential Picard moment of its scaled spectral-energy distribution; AF-363--AF-364 give the half-order transition and the critical `Theta(e^v/sqrt(v))` growing-band cost, with the separate absolute factor `e^u`. AF-365 then proves that every fixed real Riesz order has a zero-free beta Mellin multiplier on `Re s>0`: modulo an `O(X^theta)` profile error, all Dirichlet principal parts above `theta` survive exactly. For the centered rational-prime source this includes poles at off-critical zeta zeros. The remaining bridge is therefore not “does arithmetic information survive?” but **can a destination consume that preserved pole discriminator with a quantitative modulus without reconstructing exponentially ill-conditioned source detail?**

Prime Circle provides a complementary resolution boundary. Exact degree-one cyclotomic data already identify the complete finite source, yet PC-307--PC-309 show that true prime packets and disjoint translates can be arbitrarily close throughout every sublinear ordinary Fourier bandwidth and every uniformly transport-stable single-level geometry. Stable separation begins only after paying mesh/linear-scale sensitivity or leaving that category. Exact identification and stable destination margin are again different resources.

Prime Lattice has narrowed its moving-observer gate. PL-314--PL-318 show that integrated tails, smoothness, critical Sobolev control and even bounded logarithmic-Laplacian forcing do not exclude sparse moving-diagonal saturation. PL-319 gives an exact Tauberian replacement: after the leading `Q^(-1/2)` boundary coefficient is matched, an integrable residual with vanishing singular translation remainder satisfies `F(Q)=2Qm(Q)-M+o(1)`, so convergence of the source trace is equivalent to `Qm(Q)->0`. The completed-Weil eigen-equation supplies the source-trace convergence structurally. The remaining theorem is the boundary expansion plus residual class, not another generic forcing norm.

Nyman--Beurling supplies a source-specific local budget. NB-144 uses the actual Euler-product half-plane to impose a Poisson-mass tax on zero packets and forces near-one logarithmic confluence to become sublogarithmic. NB-145 proves that this tax is already optimal inside the entire positive exterior-sampling cone built only from Poisson positivity plus the pointwise absolute Euler-product majorant. The fixed-interior `Theta(log G)` gap therefore requires a qualitatively new source resource — signed/oscillatory control, correlations, a stronger local moment law, or a theorem that surviving packets must be target-bearing — not more positive sampling.

Robin Extremal now prices local visibility and one actual-zeta exterior component at one observation point. RE-145 gives the local reciprocal-box floor `exp(-O(R))`. RE-147 replaces the Ingham near-edge density tax by Pintz's `Theta((1-Theta)^(3/2))` tax, and RE-148 localizes the RE-145 witness into the same early subwindow as the shell estimate. Positional synchronization is closed. The remaining obstruction is quantitative: the currently proved local floor decays with an exponent constant above `19`, while the shell gain has exponent below `1/2`, so increasing a common `R` worsens the certified comparison. Near-left and vertically remote near-horizontal pieces remain uncontrolled as well.

The common requirement is a **coupled-scale modulus in the topology, quotient, absolute error floor, declared target, source law and worst-case quantifier actually consumed by the endpoint**. Exact information survival, source-trace convergence, local population budgets and normalized geometry help only when their rates and categories line up with that destination.

## Which growing source law is informative rather than unanchored, under-exposed, reconstructive, shareable or generically realizable?

Farey Discrepancy exposes three ordered coefficient-side barriers. FD-145 comes first: relative ancestry equations on a visible component disconnected from the finite Möbius anchor have an exact `Z_2` orientation gauge. FD-146 restores the full connected ancestry graph and every local edge, yet uniformly normalized finite-`L^q` edge energy can still vanish across a fixed-rank wall while coefficient error stays macroscopic. Connectivity must therefore be followed by quantitative anchor exposure in the actual observation norm. Only after both gates survive does FD-144 become the information-budget lower bound: `o(r_T)` arbitrary source-independent normalized ancestry observations can still be fooled by one common source. The opposite high-information boundary remains FD-142, where one exact additive coordinate can reconstruct a squarefree prefix.

Möbius Cancellation closes its generic matched-control coding frontier. MC-314 converts the natural equal-share source scale into a constant generalized-weight defect at logarithmic rank. MC-315--MC-316 identify projective occupancy, systematic submatrix rank, erasure ambiguity and primal/dual generalized weights as equivalent generic presentations. MC-317 proves that full binary `[2R,R]` codes with the required endpoint exist for all sufficiently large `R`. Generic binary coding/projective geometry therefore cannot supply the missing obstruction; the next theorem must constrain arithmetic realizability of the reciprocal-prime/source-incidence matrices or introduce a new invariant not already realized by those matched controls.

Prime Circle sharpens the stability side of the same accounting. Exact low-dimensional algebraic identification coexists with vanishing translated-source distance throughout every sublinear ordinary Fourier bandwidth and every uniformly transport-stable single-level geometry with `o(q)` modulus; mesh-scale/linear sensitivity then exposes classical/discrete source structure. Across these lines, “number of coordinates” is not a meaningful scalar resource. One must price anchor connectivity, normalized cut exposure, effective source-state resolution, topology, precision, stable separation and the exact ambient realizability frontier of the invariant being used.

## Which local zero laws survive after global density, functional equation and transfer arguments are exhausted?

Nyman--Beurling and Robin Extremal continue to show why broad population envelopes alone are insufficient, but both now identify sharper boundaries on what local source coupling can buy.

In Nyman, xi-type entire realizability and global zero laws still permit target-poor logarithmic confluence, while the actual Euler product supplies a local Poisson budget that forbids part of that geometry. NB-145 proves that positive exterior resampling cannot improve the fixed-interior coefficient: the one-point NB-144 argument already solves the minimax problem for that positive proof class. The live fixed-interior route must therefore change sign structure, prime-side information, local moment input or target geometry.

In Robin, source anchoring makes population control useful only after horizontal displacement has been converted into coefficient/height cost. RE-147 substantially improves that population tax near the finite edge, and RE-148 shows the local visibility witness can be placed on the same early window. Yet the resulting exponents are incompatible in the wrong direction. The remaining problem is no longer where to compare the estimates but how to improve their rate ordering while also controlling the near-left and vertically remote components.

Prime Lattice gives a different local-law mechanism. PL-319 shows that once the source equation supplies a convergent endpoint trace, moving-scale anti-concentration becomes a Tauberian consequence **provided** the leading boundary profile has been removed and the residual is integrable with vanishing singular remainder. This is a source-coupled local law rather than a stronger global norm.

The shared lesson is now threefold: first represent the exceptional geometry in a currency the source theorem actually controls; then determine whether the chosen proof class is already optimal in that currency; finally compare the resulting rate with the destination's local visibility loss. A stronger theorem that stays inside an already saturated proof cone or has the wrong exponent ordering does not cross the endpoint.

## Which arithmetic quantity remains after universal source mass is quotiented away?

Weil Inertia separates total unscreened mass from the signed quantity seen after the explicit formula. WI-301 proves that complete one-signed dyadic screening forces a uniform archimedean reserve. WI-241 then shows, parity-free, that the pole cancels the density-one prime main term exactly; the residual source variable is the smoothed signed discrepancy of `d(psi-x)`, together with a bounded nonnegative density-one autocorrelation term. Thus “some prime-power mass survives” is not yet coercivity: the live bridge must show that the forced source contribution survives the density-one quotient with the correct sign.

WI-302 records a source-exact compact certificate claim through aperture `17/16`, but it remains author-certified computational evidence pending independent replay and analytic audit. WI-303 resolves the structural clue without validating that certificate. The displayed rank-two tail term is a noncanonical finite-rank minorant of a stronger positive tail operator; rank zero, one, two and higher extractions are structurally available. A separate compact-repair obstruction shows what changes past `log 8 / 2`: a tail-dropped architecture cannot be rescued by finite-rank/compact corrections, whereas Liu retains a noncompact source-exact positive tail supplier before extracting a finite certificate. The live questions are independent replay and how much noncompact tail information must survive, not rank-two minimality.

## What constitutes a representation-faithful null after nuisance variables are removed?

Visual Exploration now separates five calibration layers. VIS-237 removes grid-origin phase exactly through a triangular pair-gap functional; VIS-238 removes artificial Poisson count variance; VIS-239 removes an independently specified one-point intensity while showing that same-sample empirical-CDF flattening destroys spacing geometry; VIS-240 supplies a fixed-count, uniform-intensity Dirichlet gap dependence family containing iid uniform spacings at `alpha=1`.

VIS-241 adds the endpoint boundary. For a fixed circular configuration, random cutting contributes an exact conditional variance `V_cut`, while the cut average `bar T` is an intrinsic circular pair functional. Under random gap geometry `G`,

`Var(T)=Var_G(bar T)+E_G[V_cut]`.

Endpoint phase and dependence geometry are therefore distinct null channels. If endpoints are nuisance, average them out and the live calculation is `Var_G(bar T)` under a frozen independently chosen dependence parameter. If endpoints are source-bearing, retain the exact additional cut channel. The endpoint semantics must be fixed before looking at confirmation data.

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) records the broader exact lesson shared with Weil Positivity: normalization can simplify or universalize the observable while moving source specificity into an induced law. VIS-241 sharpens this by showing that the law itself can factor into intrinsic geometry plus a removable representation-boundary nuisance.

## Which scalar or local reductions remain structurally capable of carrying arithmetic specificity?

Weil Positivity has closed the pure-measure scalar Julia positivity mechanism sharply. WP-314--WP-317 show that the empty-side moving tangent is universal, atom-mass scaling can lose compactness under prime clustering, isolated poles scalarize, and every fixed finite multipole reduces through classical interlacing degree drops. WP-318 extends this to arbitrary finite or infinite positive measures: after derivative biasing at a regular node, the normalized response is exactly `tau-K_nu`, the Boolean self-energy profile of a probability law; its Herglotz sign is universal, and every gap-supported probability law is realized by a matched positive control.

The scalar source question is therefore whether the specific derivative-biased Mangoldt probability laws satisfy a source-forced compactness, rigidity or limiting property absent from matched controls, and whether that property couples canonically to the Gamma/global counterterms. Otherwise the construction must leave the audited pure-measure scalar category through an affine mass-at-infinity channel, matrix/operator-valued structure, generalized resolvents or nonseparable finite--archimedean coupling.

Xi Flow remains a different global-local problem: its local packet inversion is well conditioned once the packet is known, but support discovery, multiscale assembly and transport from theta-side control to a zero-sensitive destination remain the unresolved gates.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current corpus repeatedly separates nearby notions that broad prose tends to collapse: exact prime-pole fidelity from target-relative Riesz recovery and absolute attenuation (AF-362--AF-365); ancestry consistency from component anchoring, quantitative cut exposure and then observation count (FD-145--FD-146, FD-144); first-order source scale from a generic constant-defect coding endpoint known realizable (MC-314--MC-317); exact finite source identification from sublinear Fourier/transport instability and then mesh-scale resolution (PC-306--PC-309); xi-type analytic realizability from an Euler-product Poisson budget whose entire positive sampling cone is now optimized (NB-143--NB-145); generic critical/bounded-forcing control from a source-trace Tauberian mechanism that still requires boundary residual matching (PL-314--PL-319); local reciprocal visibility from a source-coupled shell budget whose location is synchronized but whose exponents still have the wrong ordering (RE-145--RE-148); nuisance quotienting from fixed-count/intensity/dependence calibration plus an explicit endpoint-phase variance decomposition (VIS-237--VIS-241); total arithmetic reserve from signed density-subtracted discrepancy and compact finite-rank extraction from noncompact tail retention (WI-241, WI-303); and scalar Julia positivity from source-specific restrictions on an induced derivative-biased probability law (WP-318).

For every proposed route ask: **what exact quantity reaches the destination; at what scale, topology and precision; which universal component, gauge or nuisance channel is quotiented out; what source law remains after that quotient; is the proof class already optimal for that law; what exceptional configuration still survives; can a matched non-arithmetic object realize the same invariant or induced law; and do the source-side gain and destination-side loss have compatible rates?** The productive next theorem is the one that closes one of those explicit gaps without reconstructing the answer, double-counting an equivalent constraint, or importing the endpoint in disguise.