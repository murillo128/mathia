# Global mathematical questions

This file records only cross-line questions that currently change how several Mathia research lines should be interpreted. Line-specific frontiers remain in each `research/<line>/mind/`.

## When does an escape from one obstruction survive the next reduction?

[MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) remains the common interface audit, [MI-030](intuition/MI-030-effective-complexity-is-a-geometry-weighted-budget.md) sharpens the resource side, [MI-031](intuition/MI-031-source-dependence-must-change-the-surviving-quotient-not-just-the-representation.md) sharpens the provenance side, [MI-032](intuition/MI-032-ambient-obstructions-matter-only-on-the-admissible-image.md) adds the reachability/quotient side, and [MI-033](intuition/MI-033-determinant-wrappers-do-not-create-new-effective-rank-after-carrier-reduction.md) adds a nonlinear carrier-and-scale audit.

Arithmetic Fidelity has separated metric deformation, hard source restriction, finite-resolution soft restriction, realizable secant geometry and boundary conditioning. AF-475 converts a source compressibility profile `tau_s<=phi(s)` into the achieved coordinate condition curve `K_phi(delta)`, while AF-476 gives the complementary nonlinear lower bound on the full profile envelope through realizable head-plus-private-tail cubes. AF-477 shows that finite affine rank `d` still leaves some `r`-sparse positive moment fiber with an exact Hamming cube of dimension `floor(r/(d+1))`. AF-478 supplies the complementary positive mechanism: bounded Jordan support on every realizable oriented secant gives scale-free Hilbert fidelity, and ordered Gibbs families achieve it through one likelihood-ratio crossing on anchored parameter ranges. AF-479 then shows that one-crossing sign geometry does not by itself control an open singular boundary. For the prime Gibbs curve the canonical local margin is `rho(1+epsilon)~sqrt(P(2)/2)/log(1/epsilon)`, so the full family loses a uniform lower constant while remaining pointwise injective. The arithmetic subclass therefore needs both realizable sign/Jordan breadth and effective norm breadth over the actual downstream parameter range, together with a discriminator not shared by matched nonprime sources.

Farey Discrepancy now separates support conditioning, paired reachability and full global extension. FD-252 shows that exact-omission support geometry can retain arbitrarily bad induced-divisor conditioning. FD-253--FD-254 show that the paired image can be much thinner and price fixed-dilation companion chains by a congruence-restricted ordered-factorization exponent. FD-255 then restores the global divisor rows omitted by those restricted inverses. Under the full coefficient envelope every realized increment satisfies `|g(d)|/d<=C_b sigma_{-1}(d)`, and every `K=o(H)` exact-omission family has total coefficient mass `O(H^2 Phi(2K/H))` with `Phi(delta)=delta log(e+log(1/delta))`. Thus polynomial support/companion inverse extremizers are not globally extendable data, and the macroscopic exact-zero response branch is closed throughout the sublinear regime. The remaining response boundary is linear-density omission or sharpness within the extendable image; source positivity and arithmetic realization remain separate.

Möbius Cancellation has moved beyond raw relational capacity, nonzero top interaction and growing rank as standalone certificates. MC-436 shows that global prime-gap order can attain `(r-1)!=N_r^(1-o(1))` source types, while MC-437 says divisor Möbius convolution keeps only the top Boolean interaction of the induced divisor statistic. MC-438 shows that additive fixed-size consecutive-chain statistics lose all interior motifs. MC-439 shows that principal-minor determinants can have a nonzero full interaction yet collapse through fixed separable rank to additive prime-feature moments. MC-440 clears that rank obstruction with the path-distance kernel but places gap order two homogeneous degrees below permutation-blind leading terms. MC-441 now factors the entire first order-sensitive coefficient through one endpoint-plus-nearest-neighbor scalar `J(g)`. A surviving gap-order route therefore needs not only growing rank and subleading separation but a source-specific effect that survives this exact low-complexity factorization, or it must descend to lower determinant layers/change observable.

Nyman--Beurling has now separated logarithmic aggregate Ford coercivity from the target quadratic norm. NB-264--NB-267 force `Omega(m)` aggregate mesh response down to `O(log K)` rank-coupled coverage under bounded resolved cost. NB-268 gives a legal uniform positive carrier with the same aggregate linear obstruction but only `sum |D_n|^2 asymp m`; after the live `1/m` damping its diagonal quadratic diagnostic is `asymp 1/m -> 0`. Thus neither generic `ell^1`-to-`ell^2` conversion nor diagonal sample energy bridges NB-267 to the Hardy/Nyman distance. The decisive object is now the actual projected Gram form on that carrier: a lower bound would expose the missing off-diagonal coercivity, while decay would exhibit a genuine escape candidate. Sublogarithmic aggregate minimality remains a separate source-side question.

Prime Circle now has a support-and-transport classicalization boundary extending through simultaneous positive Kron deletion and all normalized determinant regularization scales in the current complete chord family. PC-384--PC-387 complete the scalar full/primitive inverse-power phase diagram, PC-388 identifies the primorial primitive boundary with the classical rough-number wheel, and PC-389--PC-390 reduce one-step pairwise/Laplacian/Kron transport to Buchstab deletion plus the deleted incident star. PC-391 shows that simultaneous deletion adds only a positive contrast sector of rank at most `k-1`, with rank fraction `O(log p/p)->0`. PC-392 reduces regularized determinant ratios to that sector; PC-393 observes that the sector annihilates the common zero mode and uses complete survivor connectivity to obtain a mean-zero spectral floor. The resulting normalized determinant correction is `O((log p)^2/p)` uniformly for every `lambda>=0`, including the pseudodeterminant endpoint. The remaining route is therefore genuinely exceptional/cross-level information or a state/operator category outside positive complete Kron transport, not a smaller regulator.

Prime Lattice has converted its moving model endpoint into a fixed prime-coordinate probability object. PL-407 calibrates the prime-variance precision needed to preserve the cubic `H^(-7/4)` layer. PL-409 identifies the first Abel-cusp alias and its `25/28` crossover, and PL-410 expands the full finite cusp jet: orders `8,14,20,...` map to explicit Jordan-totient counterterms, so every fixed `Q=H^kappa`, `kappa<1`, can be pushed below the zero-sensitive scale by finitely many exact subtractions. PL-411 then sums the entire Ramanujan endpoint exactly as an expectation over an odd square-free random integer whose prime coordinates are independent with `P(p|M)=1/(p-1)^2`. The law has tail `P(M>X)~1/(2X)`, moments only below order one, and a Mellin transform carrying the classical `1/zeta(2s+4)` divisor. This explains why finite cusp jets fail exactly at the true endpoint and places the `-7/4` zero-sensitive scale inside one fixed heavy-tailed model law. The live bridge is now signed transfer between the actual prime source and this endpoint functional, or an equivalent variance theorem; any Mellin-based RH criterion must separately rule out cancellation of the candidate zero poles.

Visual Exploration has now priced the positive fixed-depth Wang hierarchy through one-scale Hermite filtrations and finite one-branch nested Newton chains. VIS-358--VIS-360 show that regular dense clusters, separated multiclusters and colliding centers reduce to the fixed-depth occupancy-width law once the correctly rescaled polynomial-sampling Gram is nondegenerate. VIS-361 extends that boundary to arbitrary positive diagonal weights. VIS-362 handles outer Gram collapse onto `L<q` separated atoms with nonvanishing masses and gives the exact balanced Hermite depth `floor((q-1)/L)`. VIS-363 removes the nonvanishing-mass assumption when `pi_ell asymp epsilon^(2a_ell)`: every jet gets cost `a_ell+d`, and the `q`-th smallest cost is exactly the singular exponent. VIS-364 then prices a strictly nested one-branch hierarchy: for nodes `0,epsilon^(kappa_1),...,epsilon^(kappa_(q-1))`, the singular exponents are the cumulative Newton sums `K_r=sum_(i<=r)kappa_i`. Merely stacking smaller positive scales therefore adds no unpriced source-free resource. The remaining compact frontier is recursive branching or active-subspace degeneration not reducible to one finite valuation ledger, coefficient geometry outside positive diagonal weighting, growing order/scale/phase complexity, or a new signed source estimate.

Weil Inertia and Weil Positivity have both moved from broad structural hopes to sharply localized category/domain questions. WI-382 proves that eventual one-sided boundedness of the fixed-width renormalized Chebyshev remainder is RH-equivalent, WI-383 gives it a negative recurrent floor, WI-384 removes the apparent moving source-perforation reserve, and WI-385 restores the archimedean block to show exact asymptotic cancellation of the stationary reserve. A surviving Weil-inertia route must therefore use semidefinite rather than coercive information, exact domain exclusion, a genuinely different source coupling or another trial regime. On the positivity side, WP-391--WP-394 rule out exact prime-power selection in the canonical divisibility diagonal, the full bounded positive Bost--Connes cone, Woronowicz affiliation with the same unital algebra, and positive unbounded forms whose canonical resolvent returns to that represented source algebra. WP-395 then tests a genuine critical-KMS von Neumann enlargement: its modular centralizer is diffuse `L^infinity(hat Z,m_Haar)`, so every standard countable arithmetic orbit is null. Exact prime-power support is either the zero affiliated operator or, if imposed only by point values, not an operator invariant. The surviving category must therefore be both genuinely new and fine enough to retain the arithmetic datum after its quotient/measure-class identifications.

## Where has the exact obstruction moved?

Arithmetic Fidelity: to **the actual arithmetic approximation profile, independent source breadth, secant sign/Jordan geometry and boundary norm-breadth profile realizable inside the prime-derived subclass, together with a discriminator not shared by matched nonprime Gibbs families**. Farey: to **the linear-density exact-omission boundary or sharpness inside the globally extendable image; sublinear macroscopic exact-zero response is closed, while source admissibility remains separate**. Möbius: to **whether the exact first order-sensitive path scalar `J(g)` carries source-specific usable arithmetic information, or to richer lower determinant layers/other global observables if matched controls reproduce it**. Nyman--Beurling: to **the actual Hardy/Nyman Gram form on the explicit uniform carrier, with sublogarithmic aggregate coverage minimality kept separate**.

Prime Circle: to **unnormalized exceptional effects, individual outliers, cross-level contrast evolution, or phase/state/operator structure outside positive complete Kron transport; normalized log-determinants are closed uniformly through the pseudodeterminant endpoint**. Prime Lattice: to **signed source replacement at the exact heavy-tailed Bernoulli Ramanujan endpoint, or an equivalent prime-variance theorem; the fixed fractional interiors are renormalizable and the Mellin zero divisor still needs a noncancellation argument**. Visual Exploration: to **branching or active-subspace-changing multiscale degeneration beyond a single finite Hermite/Newton valuation ledger, coefficient geometry outside positive diagonal weighting, growing order/scale/phase complexity, or a new signed source estimate**. Weil Inertia: to **semidefinite rather than coercive use of the exact form, an exact graph/domain exclusion of the fixed-width source-zero quasimodes, a different source coupling, or a genuinely different trial regime**. Weil Positivity: to **a source-forced category whose canonical transforms do not fall back into the represented bounded Bost--Connes algebra and whose quotient/measure class still retains the arithmetic distinction; the critical modular centralizer itself erases exact orbit support**.

## Which physical cost survives after representation artifacts are removed?

The newest results sharpen the distinction between ambient complexity and the carrier that survives all exact constraints. Arithmetic Fidelity shows that even one-crossing secants can acquire logarithmically growing norm breadth near a singular normalization boundary. Farey shows that polynomially bad restricted inverse directions can disappear when global divisor-row extension is imposed. Visual Exploration shows that even recursively smaller positive separations can be fully priced when they form one Newton chain: their costs accumulate rather than opening a new free direction. Prime Lattice turns the apparent moving endpoint into a fixed heavy-tailed probability law and shows that failure of finite-moment renormalization is itself a feature of that law. Weil Positivity adds a different failure mode: a genuine category enlargement can make the target discrete orbit null rather than more expressive.

MI-033 records the parallel nonlinear lesson from Möbius Cancellation and Prime Circle. In MC-439 the determinant factors through fixed additive feature moments; MC-440 clears that rank obstruction but places gap order two homogeneous degrees below permutation-blind leading terms; MC-441 then compresses the first surviving ordinal layer to one endpoint-adjacency scalar. In PC-392 the determinant reduces to the thin contrast carrier, and PC-393 shows that the ambient constant zero mode cannot be used to amplify it because the contrast is orthogonal to that mode and the mean-zero carrier remains conditioned. The common proof order is to identify the source-sensitive carrier first, then its internal conditioning, and only then ask whether a nonlinear observable introduces genuinely new rank, scale or source sensitivity.

The other current currencies remain equally post-quotient. Möbius adds full interaction, effective interaction rank, factorization complexity and separation scale to source entropy. Nyman--Beurling separates aggregate linear mesh mass, diagonal quadratic energy and the full projected Gram norm. Prime Lattice prices and removes every fixed finite regulator jet before crediting the endpoint zero-scale signal, then represents the endpoint by its exact source law. Visual Exploration prices nested scales through cumulative interpolation pivots before crediting recursion. Weil Positivity now requires not only a larger operator category but survival of the arithmetic datum under its measure-class equivalence.

These are not one theorem and their currencies are not numerically interchangeable. The adversarial commonality recorded in MI-030--MI-033 is an order of proof: remove universal scaffolds, symmetry, known kernels, classical preselection and category-programmable choices; restrict to the actual globally extendable image; identify the exact surviving carrier and quotient; then price every linear or nonlinear loss, weak mode and compensating resource in the source-varying quantity and asymptotic scale that reaches the destination. The latest evidence sharpens these line-specific audits but does not justify a stronger universal mathematical bridge across the lines.