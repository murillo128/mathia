# Global mathematical questions

## Which source identity fixes a nonprogrammable mixed-prime finite part?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) shows that proper marginals, exact diagonal coefficients, positivity, and closability do not determine a unique global coupling. The Weil-positivity Fock branch successively closes finite-dimensional coherent collapse, programmable locally finite incidence, and normalized complete-Gibbs incidence as sufficient selectors.

WP-264 sharpens the remaining question. Before critical unit-diagonal normalization, the Hamiltonian-selected complete incidence has an exact finite-part decomposition whose trace-class remainder has one positive direction and is strictly negative on a canonical codimension-one primitive hyperplane. The missing interaction is therefore not simply absent. But the admissible finite parts form

\[
R_C=aa^*-2D+CD_W,
\]

and every `C<=0` has the same one-positive-direction signature. The critical Weil half-density `D_W` is precisely the unfixed counterterm direction.

A viable global kernel must therefore fix both **correlation geometry and finite renormalization** from the source before comparison with the target. A sign/index theorem that survives a family of inequivalent counterterms is not selective enough. Equality with the completed Weil pairing and its Gamma/polar terms remains a further gate.

## What readout retains source-dependent information after matched controls?

For Prime Circle, [MI-003](intuition/MI-003-arithmetic-significance-begins-after-universal-control-subtraction.md) removes the obvious finite spectral labels and total-winding reformulation. A surviving statistic must use information below the IDS/counting level: shrinking-window placement, subleading projective phases, transfer growth, near-resonant scaling, or a genuinely long source-dependent itinerary.

For Nyman--Beurling, [MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md) is now a one-operator problem. NB-060 proves that every finite-time defect belongs to the generalized nullspace of the single dyadic backward shift `B_2`, and that a nonzero finite-time defect exists at any scale iff `ker B_2` is already nonzero. The remaining collective question is whether the closed union of `ker B_2^k` reaches the canonical residual, not whether a specially tuned large cutoff contains the first defect.

Robin Extremal has moved from one-event detection to a bounded-packet classification. RE-061--RE-062 show that the normalized higher-layer charge of a bounded packet is reciprocal-depth mass plus two fringe bits. Asymptotic cancellation can persist only through deep-layer escape or a finite Egyptian-fraction core with fringe change `0 -> 1`. The global question is whether the actual false-RH fan can remain inside those exceptional architectures.

Visual Exploration closes a broader matched-null region than before. VIS-177 improves the fixed-moment character gap and extends the Lindeberg prime-power Gaussian law to every subpolynomial continuous support `y(H)=H^{o(1)}`. Merely leaving polylogarithmic support is therefore not a source-sensitive escape; the surviving boundaries are genuinely polynomial-or-faster support requiring new transfer theory, discrete/Gram sampling, hybrid prime/zero variables, or stronger-than-weak observables.

## Which quantitative observation family reaches the required source aperture?

With bounded local Euler degree, [MI-005](intuition/MI-005-topology-is-part-of-the-arithmetic-claim.md) separates finite prime-power depth from unbounded prime breadth. [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) now has a sharper finite-prefix calibration from AF-282--AF-283. For the two-prime mixed channel on the known prefix `1,...,N`, phase labels have spacing `Theta(1/N)` and stable coordinatewise mixed degree is sharply `Theta(N)`: `o(N)` degree cannot have a uniform inverse, while a dense square with `L=30N` is uniformly stable.

The remaining finite-prefix acquisition uncertainty is therefore **sample count**, not hidden exponential coefficient conditioning. The proved stable square uses `Theta(N^2)` scalar moments while rank gives only `Omega(N)`. Sparse mixed-index design, timing precision, source weighting, known-support assumptions, and target-sensitive compression remain separate bills.

Analytic Frontier gives a different aperture law. ANF-183 shows that endpoint memory can remain invisible to every physical twist window `o(X/a_X)` while a rephased three-point curvature at separation `Theta(X/a_X)` must detect the sector-aligned matched packet. The remaining difficulty is source access with absolute `o(a_X)` error at that aperture, not another source-blind smoothness statistic.

Xi Flow gives the converse calibration: once a consecutive packet window is known, XF-186 provides an `O(m)` holomorphic measurement bank uniformly bi-Lipschitz on a constant nonlinear cell for `m<=N`. Local inverse conditioning is not the bottleneck there; support discovery, global assembly, source-side access, and transport to a zero-sensitive quantity are.

## Which source-specific inequality prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia now agree on a concrete localization boundary without asserting the same operator. PL-280--PL-281 prove that the canonical support-localized negative Morse index is monotone and exactly exhausts the global compact-support negative index. A finite negative sector cannot hide beyond every finite aperture; if the global index is finite it stabilizes at a finite radius.

WI-247--WI-251 attack the complementary first-crossing witness. Under RH failure the odd sector has an attained first zero mode at finite radius. Layer-cake averaging removes the smooth IMS collar cost and compresses the full nested localization family to one signed radial von-Mangoldt flux inequality with a finite universal mean-side budget and a strictly positive integrated interior gap term.

Together these results remove two generic escape routes: **unseen support drift** and **arbitrary smooth-collar cost**. The remaining obstacle is source-specific sign/coercivity at the first crossing. WP-264 supplies an adversarial calibration: even an exact Hodge-like index-one signature can be compatible with an unfixed finite counterterm. Therefore a useful sign theorem must also fix the arithmetic scale/counterterm it uses; index structure alone is not a substitute for source canonicity.

## Does the complete flute response preserve the weighted source budget?

PF-297 shows that complete Robin factors preserve positive `q`-mass, and PF-298 shows that the tight-pant scalar constant channel has only `O(s_n)` relative killing. PF-299 identifies the exact accumulation threshold of the prime seam geometry:

\[
(s_n)\notin\ell^1,
\qquad
(s_n)\in\ell^q\quad(q>1).
\]

Thus the current scalar loss bound sits exactly at the critical endpoint. A superlinear power law makes the scalar loss summable; a matching linear lower law forces complete scalar absorption. The next scalar theorem is the first-order asymptotic of the actual relative killing, not another uniform local contraction.

This does not settle the physical mixed response. The scalar constant compression is not known to be invariant under the complete `P/H` Schur map. The unresolved theorem must derive the actual return/reassembly equation and account for nonconstant-mode damping, support loss, source-adapted forcing/cancellation, and any physical-high remainder; the global heavy-range singular-value problem remains separate.