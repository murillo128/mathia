# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Compute the complete control family, including finite boundary defects, before increasing complexity inside a carrier

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

The control depth has increased beyond asymptotic or bulk matching. Analytic Frontier shows that a profile can pass every long-lattice/Mellin test and still fail on one finite edge-relaxed cluster; its complete scalar floor is a finite-particle stability constant. Visual Exploration shows the analogous reconstruction closure: all circular log-modulus modes, regular-disk phase, and connected overlap alignment are already fixed by zero sources plus boundary data and one global gauge. Prime Circle's full-chord Bloch carrier is not only reflection-symmetric but strictly accretive and zero-free in its formal critical strip for arbitrary root subsets.

A decisive positive must therefore identify the **complete** admissible control family at the exact finite, boundary, and global assembly level, then exhibit a residual that survives it quantitatively. More channels, shells, points, refinement depth, or asymptotic window size are not evidence when the carrier's full control model already reproduces them.

## Distinguish source identification, readout fidelity, divisor orientation, native positivity, and completed zero selection

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational` and `MI-004-critical-axis-is-not-zero-selection`.

Prime Lattice now separates these gates especially sharply. Strong phase data can identify arithmetic generators; Möbius small-sum rigidity can act on the reciprocal divisor orientation; the native HLS zeta kernel has unconditional positive-power/compound-Poisson structure while failing complete Pick; and global completion converts screw/CND/Lévy positivity into an RH-equivalent object. PL-144 reduces the completed target further: for the actual zeta function, Suzuki's scalar sign `Psi(t)>=0` is already equivalent to RH.

Prime Circle provides the complementary symmetry control: its exact half-reflection comes with a universal zero-free strip, so a critical-looking axis can organize a carrier while having the wrong spectral geometry for Riemann zeros. A decisive RH mechanism must state separately what fixes the source, which divisor the readout sees, which positivity is unconditional/native, and what independent theorem forces the completed zero-sensitive sign or polarization.

## Make local repairs uniformly conditioned, range-faithful, and natural before globalizing them

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Arithmetic Fidelity shows that every finite-dimensional restriction of a global quotient defect can have an exact ambient repair even when a positive norm-level defect survives. The missing resource is a uniform finite interpolation bound: bounded finite repairs compactify into a bidual extension, and for `1`-complemented targets the finite and global optimal costs coincide.

A positive construction must therefore specify the observation topology and conditioning budget, prove coherent finite-to-global gluing, and then show that the repair lands in the source-forced target range with the required equivariance, order, positivity, or locality. Per-cutoff exact recovery or a completion-valued repair chosen after seeing the target is insufficient.

## Require quantitative visibility horizons and inverse transfer when changing smoothing, summability, regularization, or scale

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim` and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Möbius Cancellation now has an exact multiscale countercontrol. A fixed terminal-prime perturbation can remain polynomially large in the ordinary sum while endpoint-vanishing Riesz kernels miss it through a long future window; MC-046 computes the order-dependent visibility threshold. MC-047 shows that stronger power-aware pretentiousness can detect the same defect at the correct normalization, but detection still needs an independently cancellative comparator or signed transfer theorem.

A decisive use of smoothing or regularization must state how quickly endpoint information re-enters, which future scales are controlled, and what quantitative inverse theorem transports the bound back to the target observable. Formal invertibility of a full transform does not justify a diagonal or too-short-window estimate.

## Measure coercivity in the depth/support-matched spectral distribution, not one weakest mode or algebraic rank

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim` and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Weil Inertia gives the clearest example. A single normalized Schur eigenvalue can collapse from endpoint tapering while every diagonal odd distance and the total horizontal remainder remain extensive. WI-134 retains the whole spectrum and proves that macroscopic bounded square depth can be screened near Lamzouri sharpness only if a macroscopic lower spectral sector collapses toward zero.

This reinforces the wider Mathia rule: exact rank, one minimum eigenvalue, average overlap, or one finite witness may be the wrong quantitative carrier. A positive must control the spectral quantile, width, frame bound, depth-weighted trace, or other distributional margin actually paired with the arithmetic mass in the final inequality.

## Require exact arithmetic scale and source specificity, not only positivity, singularity, or the right asymptotic shape

**Linked intuitions:** `MI-004-critical-axis-is-not-zero-selection` and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Weil Positivity now classifies canonical refinement responses across stationary, extensive, bounded, and logarithmic regimes. WP-139 closes the minimal singular new-prime puncture finite part: after intrinsic normalization it is bounded and prime-blind, while the divergent zero-shift term is only a universal nullity defect. Prime Lattice reaches an analogous boundary from completed probability/kernel geometry: a positive native Euler carrier can be exact and unconditional while the completed sign is already the RH-level statement.

A decisive positive must produce the exact finite arithmetic coefficient or sign from source-forced geometry before target-aware renormalization, and must survive matched composite/generalized-prime controls. The right sign class, a logarithmic divergence, or an asymptotic `log` increment is not enough when the finite coefficient is wrong or universal.

## Convert the Xi mesoscopic carrier into source-controlled boundary flux using more than self-similar tapering

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Xi Flow has identified the fixed-time carrier at `Theta(log^2 T)` gaps with endpoint fractional/Orlicz structure and accepted finite-amplitude bulk comparison. XF-016 closes the ordinary localization shortcut: a fixed-shape self-similar cutoff has `H^{1/2}` leakage of the same order as the useful mesoscopic bulk term, and centering or envelope-only estimates do not make it lower order. The open XF-014 adversarial sidecar is excluded from accepted synthesis until resolved.

A decisive continuation must import source-specific signed exterior information or build a genuinely non-self-similar multiscale/capacitary, overlapping-window, or convergent global mechanism whose boundary budget is quantitatively smaller at the `log^-2 T` dynamical scale. Reproving bulk dissipation inside a larger smooth window does not address the remaining obstruction.
