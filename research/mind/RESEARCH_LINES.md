# Global mathematical questions

## Can a source-native signed transformation become admissible without erasing the localization needed by Weil positivity?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate source provenance, admissibility and target sensitivity.

Weil Positivity has closed positive additive preservation, scalar nonlinear post-processing and the entire finite canonical dilation-coboundary hierarchy as easy escapes. WP-290--WP-291 show that finite source-native pole killing preserves Mangoldt localization but leaves temperedness exactly RH-equivalent.

WP-292 supplies the complementary failure mode: counting convolution removes the nontrivial-zero poles and permits unconditional temperedness after finite differencing, but only by replacing the prime-power carrier with an all-integer one. WP-293 closes exact coefficientwise scalar relocalization: Dirichlet-convolution uniqueness forces the inverse kernel to be Möbius, whose transform is `1/zeta` and therefore restores the signed reciprocal-zeta zero screen.

WP-294 now closes the apparent “coarse coefficients, exact final form” loophole in the same architecture. Equality on the full Weil autocorrelation cone forces equality of the underlying distributions; the isolated log-integer atoms then force exact Mangoldt coefficients, and WP-293 again forces Möbius inversion. A smooth non-finite remainder cannot hide atomic errors without importing compensating finite-place atoms.

The open theorem must therefore leave **direct one-channel scalar arithmetic convolution**, not merely relax coefficient accuracy. Derive a genuinely multi-channel, operator-valued, non-translation-invariant/boundary-conditioned or nonlinear upstream coupling that becomes admissible unconditionally while preserving enough arithmetic localization for the exact Weil form. An approximate/restricted intermediate criterion remains possible only if a separate stability/equivalence theorem reaches the exact canonical endpoint.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) separate source-resource pricing, destination sensitivity and relaxation sharpness.

Arithmetic Fidelity now has zero joint stability margin and no finite exponential-sum Bézout certificate at the exact eight-prime target. AF-330 forces any exact hit to have exponentially algebraic common parameter `-it`; AF-331 shows that this exceptional real-time set is countable but dense; AF-332 proves that at nonzero real time at most two prime-phase coordinates can be algebraic.

AF-333 adds a sharp conditional closure. Assuming Schanuel, a common-time phase tuple from multiplicatively independent algebraic bases has algebraic codimension at most one. But on the unit torus `e_1=0` also forces the independent reciprocal equation `e_(n-1)=0`, so any equal-weight zero with `n>=3` consumes at least two algebraic codimensions. Thus Schanuel already forbids the eight-prime target from its first equation alone. The surviving **unconditional** problem is to replace this joint transcendence budget, or otherwise exclude two independent polynomial relations tied to the common prime-log parameter. Coordinatewise transcendence, exceptional-set genericity and stable conditioning do not supply that currency.

Prime Flute now has a concrete destination-resolution test rather than only an aggregate normalized commutator. PF-337 identifies positive Fourier off-diagonal mass, PF-338 compresses the finite-boundary odd sector to the half-cuff cross-parity defect, and PF-339 inserts the symmetric seam normalization. Every fixed odd-offset raw pant coupling must be `o(w_n)=o(P_n^(-1))`; equivalently, two explicit low cosine profiles exchanged by half-cuff translation must have raw one-cusp-pant energies differing by only `o(w_n)`. A nonzero seam-normalized two-mode contrast kills the translation route before any large-matrix estimate. Even-offset mass and physical reference-packet concentration remain separate only if this cheapest test survives.

Nyman--Beurling has moved beyond the fixed-rank obstruction. NB-108 makes every fixed-dimensional confluent/Jordan packet asymptotically target-orthogonal after full backfill. NB-109 extends this to growing one-zero packets whenever `d log(2+A)=o(log G)`, in particular every `d <= (1/2-epsilon) log G/log log G`; the governing cost is basis-invariant polynomial extrapolation from the resolved radial band to the target endpoint, not monomial Gram conditioning. The same leverage-target frontier survives throughout that window. The remaining one-zero route must enter the larger-rank transition toward the `O(log G)` multiplicity ceiling, use source-specific multiplicity information, or move to genuinely several-distinct-zero growing-rank geometry.

## Which cancellation theorem remains after fixed local algebra, scalar decoding and generic perturbative shortcuts are closed?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track the relocated endpoint costs.

Farey Discrepancy has localized the critical transport loss to the terminal `H/p` prime fan. FD-107 kills generic coherent-shell control, FD-108 shows first-order physical prime-divisibility energy remains power-critical, FD-109 shows raw higher-order simultaneous divisibility is a commuting product-depth resolvent, and FD-110 closes finite linear prime-order refinements. FD-111 now closes the first nonlinear variant: every fixed polynomial in finitely many local multiplicative shifts reduces to power sources whose odd sector is invertibly equivalent to the original source and whose even sector is a linear mean plus square-root error. The residual must leave the fixed finite local algebra through post-summation/cross-horizon coupling, growing degree/support, genuinely nonlocal signed structure or global descent.

Möbius Cancellation has closed positive and signed **full-cube pointwise scalar decoding** at the relevant source depth, and MC-281 makes the surviving arithmetic-image route exact. With `A` the actual shell feature matrix and `v` the virtual blind row, blindness is excluded if `v` leaves `ran(A^*)` or if its minimum signed synthesis cost `v^*(A^*A)^+v` exceeds one. MC-282 now proves that this leverage condition is not a generic consequence of arithmetic provenance or even nonblindness: a separated-prime simplex Legendre system has no blind pair while its synthesis cost tends to zero. The residual is therefore **tight-shell localization forcing the blind direction to separate**, or a source-native operator/inverse.

Prime Lattice has moved from generic perturbation failure to a sharply source-structured forcing gate. PL-301 shows the actual Suzuki remainder is BV-stable; PL-302 proves the arithmetic shift comb is not contractive once the `n=3` channel activates; PL-303 proves that even the exact odd spectral gap cannot upgrade ordinary `L^2` forcing to `BV`; and PL-304 extends the same logarithmic-versus-linear frequency obstruction to every ordinary `L^p`, `1<=p<=infinity`, and to the full finite-measure forcing space. The missing theorem must therefore exploit a **proper source-generated range** of the differentiated commutator/eigen-equation, an exact cancellation or compatibility law, a graph norm that already charges first-order frequency, or a direct branch-variation principle. Generic ambient forcing-space strengthening is closed; the independent sign gate remains separate.

## Which exact null-mode operator constraint survives sharp support relaxation in Weil Inertia?

[MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track the shift from support geometry to operator-domain information.

WI-281--WI-283 prove that complete one-signed screening, all prime-power lags, full span and prime-translation support domination still leave the scalar packing relaxation sharp. WI-284 recovers the first-crossing equation at operator level: complete screening deletes the prime translation form on the whole selected support subspace, so the prime-deleted gamma-plus-pole form must be PSD there with bottom exactly zero.

WI-285 sharply limits how that spectral contradiction can be proved. For every **fixed finite** set of prime bases there are arbitrarily sparse full-span supports that screen all powers of those bases while carrying prime-deleted Rayleigh quotient tending to `-infinity`. Fixed-family support measure, span, packing and maximal domination therefore do not control the restricted bottom.

The next theorem must be genuinely uniform in the aperture-growing family of active prime bases, or use quantitative amplitudes from the null equation or another source-specific coupling. The direct target remains to show that every completely screened admissible support has restricted prime-deleted bottom different from zero, but a proof cannot come from any fixed finite lattice-screening template. Sign-changing and incomplete-screening branches still require separate quantitative replacements.

## What should Prime Circle do with an observable that RH itself makes non-prime?

PC-285 strengthens unconditional Li-grid reproduction of the exact boundary-log finite-section spectrum to the Vinogradov--Korobov source-transport range. PC-286 goes much farther conditionally: under RH the same spectral law is Li-grid reproducible whenever `B=o(sqrt(q)/(log q)^6)`.

The one-profile source-placement branch therefore has a reversed diagnostic near the rational scale: persistent order-one prime-versus-Li separation in that range would contradict RH, while collapse does not imply RH. The remaining one-profile frontier is the polylogarithmic neighborhood of the square-root denominator scale, or a different RH signature than separation. Otherwise the architecture should move to cross-level, multi-source, directional, noncommuting or richer observables.

## Which Robin recovery obstruction remains after conditioning on the zeta zero edge?

RE-107 closes whole-fan positive Chebyshev transport. RE-108 prices normalization-only recovery, RE-109 restores the PNT-regular event source, RE-110 turns recovery into an exact one-sided workload budget, and RE-111 identifies the same-sign first-layer packet required if recovery occurs at the unconditional Korobov--Vinogradov minimum.

The strengthened RE-112 now makes the finite-edge source scale sharp. If `1/2<Theta=sup Re(rho)<1`, off-critical zero density plus the truncated explicit formula gives `psi(x)-x=O_Theta(x^Theta)` rather than the textbook `x^Theta log^2 x` envelope. The exact workload budget therefore forces

`V-Y >>_Theta h(C)Y^(2-Theta) log Y`,

hence `V-Y >> Y^(2-Theta-b) log Y` on adaptive exponent-`b` witnesses. For `b=1-Theta+eta`, the fixed-`eta` family has almost-linear spacing `Y^(1-eta)log Y` and dyadic packing `O_(Theta,eta)(T^eta/log T)`.

A recovery within a constant factor of this sharpened minimum also forces a large favorable-sign first-layer packet with `p-vartheta(p) asy Y^Theta`. The next theorem in the finite-edge branch must therefore use zero density/phase or another selected-corridor coordinate to forbid **CA-selected saturation of the true Theta-scale source envelope**. If `Theta=1`, the spectral power gain disappears and the unconditional RE-109--RE-111 KV/first-layer anti-saturation problem remains live.

## What remains after the exact visual pair-difference cancellation taxonomy is exhausted?

VIS-211 suppresses the one-cancellation family in the strictly subcritical regime. The finite-start baseline is the surviving VIS-193 result: every deterministic start window of quadratic length inherits the long-start mean-square scale uniformly in its location. VIS-194--VIS-202 then show how coefficient-weighted collision geometry can beat worst spacing in substantial subcritical corridors.

VIS-212 was deleted as a duplicate of VIS-193, so it supplies no independent evidence. The frontier remains coefficient-weighted compression of the averaging horizon, genuinely pointwise coherent starts, and the critical `H asymp y/log y` regime. A new higher-cancellation hierarchy would require a new observable.
