# Global mathematical questions

## Which source resources survive to the destination rather than becoming target-complete, reconstructive, universal or cheaply matched?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate source recovery, retained relation, conditioning, coupling complexity and final sign/zero selection.

Arithmetic Fidelity now separates **determinant order, placement generators, support topology, sampled interiority and spatial resolution**. AF-379/AF-381 make vanishing prime-log sampling target-complete. AF-383 removes one spatial tuple at every fixed determinant order, and AF-385 localizes the strict lower-order side information to the sampled solid hierarchy. AF-386 shows that at order two strict positivity is not the essential missing resource: nonnegative local solid generators on shrinking meshes characterize continuum `TN_2` exactly when `{f>0}` is connected. Two separated positive components can pass every fine local test and still fail a cross-gap minor. Resolution cannot infer the global zero-pattern relation needed to propagate local positivity.

Prime Circle supplies the complementary zero-selection warning across the coupling axis. PC-319--PC-320 preserve an off-critical local divisor near weak coupling; PC-321 becomes zero-free under strong additive exponent; PC-322 canonically selects `u=1` and the universal Hilbert `1/2` threshold. PC-323 retains the full Green operator and still finds only a rank-one finite channel tensored with the Hilbert core modulo trace class. Its essential/absolutely-continuous band depends only on source RMS. Detailed arithmetic survives only in **relative** trace-class spectral data, so operator lifting alone does not turn canonical Hilbert criticality into zero selection.

## Which thin, moving or signed channel remains after bulk controls close?

Nyman--Beurling has moved from worst-case compact transport to a **persistence-with-adaptation** discriminator. NB-165--NB-167 identify one-Poisson-width capped transport `Xi_G`; NB-168 rules out block-wide logarithmic rescue for a stationary compact template below the corresponding logarithmic scale. NB-169 prices piecewise height adaptation: a partition into `M_T` template cells contributes refresh energy `(T a_T^2)^(-1) sum Xi_j^2`. Under `Xi_j<=X_T`, positive-density rescue with `X_T=o(log T)` requires `M_T ≳ T a_T^2 log^2 T/X_T^2`. Adaptation is therefore a real resource only after its switching complexity crosses a moving Poisson-scale threshold.

Robin Extremal now separates **global normalization from spatial localization**. RE-169 turns Mertens matching into an exact compensation-debt law for the Robin kernel and shows that vanishing future KV capacity makes that debt rigid. RE-170 proves the converse boundary: two bounded-multiplicative shells can create an order-one intermediate Robin excursion and later repay it while preserving the KV envelope to vanishing relative error and matching the Mertens constant to `o((sqrt p log p)^(-1))`. Critical normalization alone is a conservation law, not finite-prefix rigidity; the missing source resource is a theorem preventing compensation while capacity remains.

Farey Discrepancy now closes static sparse holes **and terminal-core presentation** under the initial-prime avoidance architecture. FD-160 shows sparse holes disappear as an escape after divisor closure. FD-161 shows that a nontrivial `S_s`-free terminal core still has a principal divisor ideal containing a primary Boolean face through depth `b_s~log s/log log s`, forcing cut pressure of order `2^(b_s)`. Terminality before the intrinsic closure is not terminality after it. A surviving route must make the closure leave the controlled regime, change the anchor/cut geometry or budgets, or use a source law that removes the tested flips.

Prime Lattice continues to separate spectral transport from sign transport at the first arithmetic activation. PL-329 orients the shrinking first-prime reflection under a source-amplitude hypothesis, while PL-330 gives norm-resolvent continuity and continuous simple eigenbranches across the activation. The sign-reversing perturbation can nevertheless vanish in that graph norm. The blocker is not branch continuity but control relative to the much smaller `delta/L_delta` destination margin.

## Which growing source law is informative rather than merely numerous, low-rank or programmable?

Möbius Cancellation now makes **representation-relative programmability** explicit. MC-332 shows unrestricted mode-dependent rows contain the exact conjugate-character matched filter; MC-333--MC-334 separate rank/energy and centered variation; MC-335 shows low Walsh degree in the standard endpoint coordinates prevents approximate dephasing. MC-336 then changes to source-phase coordinates, where that same matched filter is degree one. Low degree becomes a genuine restriction only when paired with an information-flow rule such as self-phase exclusion. Under degree-`d` cross-phase dependence with self-phase hidden, the reachable output spectrum stops at degree `d+1`, and exact dephasing first returns at `d=R-1`.

The common lesson from AF-386, FD-161 and MC-336 is that a structural restriction matters only if it survives the **natural completion, closure or reparameterization available to the destination**. Fine local positivity tests do not see disconnected support; terminal auxiliary labels regenerate depth under divisor closure; low degree can disappear under a coordinate system that exposes the target phase. “Sparse,” “terminal,” “local,” “low-rank” and “low-degree” are not proof resources until the relevant invariant is stated after these free transformations.

## Which representation-faithful null survives nuisance removal, weak convergence and universal positivity?

Visual Exploration now has sharp low-frequency conditioning through quartic order. VIS-256 shows fixed polynomial moments cannot beat the sine-kernel `|q|` cusp. VIS-257 cancels the cusp with sharp quadratic cost `8b^(-2)`. VIS-258 cancels cusp plus quadratic channel and proves the sharp quartic inequality `|m_4|<=E_* b^4||nu||_TV`, `E_*=0.06346155...`, so unit quartic signal costs `15.75757... b^(-4)`. A quartic source route therefore needs a genuine zeta-minus-null quartic coefficient and transfer error `o(b^4)` after the exact conditioning amplification; extra cancellation is not free nuisance removal.

Weil Positivity now has two sharp moving-observable stability scales. WP-336 controls the exact-barycentric Kadison defect by **channel error × bidirectional propagation**. WP-337 shows approximate barycentric preservation adds an independent **coordinate bias × propagation asymmetry** term. A dyadic Mangoldt control keeps a fixed defect while both raw channel error and raw coordinate bias vanish because the second scaled product remains critical even when the bidirectional product is zero. Any proposed stronger topology must dominate these concrete moving-observable products or an equivalent second-moment quantity.

Prime Lattice's PL-330 gives the complementary warning: even a topology strong enough to transport the full spectral branch can remain too weak for a shrinking sign observable. The issue is not whether a norm is abstractly strong, but whether it controls perturbations relative to the destination margin that must be oriented.

## Which noncompact source channels survive positivity reduction once complexity is separated from information?

Weil Inertia closes scalar-localization self-bootstrap under arbitrary Hilbert-valued diagonal amplification. WI-320 supplies the fixed short prime-free witness; WI-321 shows the scalar semibound amplifies dimension-free and by polarization already generates the corresponding Loewner Gram inequality. Fixed PSD cross-channel metrics are consequences of the scalar premise, not new information. A viable vector route must add coupled inequalities outside that scalar-generated cone.

Xi Flow gives the opposite inversion lesson. XF-185--XF-186 show that once the correct consecutive packet is known, linear and local nonlinear holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local invertibility.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is a classification of **source-specific coupling consumed by the destination at its own scale, topology, family quantifier, closure geometry and admissible representation**. AF-386 isolates support connectivity as a relation invisible to arbitrarily fine local order-two tests; NB-169 turns free height adaptation into a refresh-cost problem; RE-170 separates global normalization from the capacity needed to localize its debt; FD-161 shows terminal support can regenerate growing depth under principal-ideal closure; MC-336 makes anti-programmability depend on self-phase information flow rather than coordinate degree alone; PC-323 pushes source specificity from a universal Hilbert continuous core into relative trace-class data; VIS-258 gives the sharp quartic conditioning bill after cusp and quadratic nulling; WP-337 separates two moving propagation amplifiers for approximate UCP rigidity.

For every proposed route ask: what object reaches the destination and at what moving scale; over what family of centers/parameters the mechanism must persist; how much adaptation/switching is permitted and what it costs; what closure/completion/reparameterization the destination can perform for free; what support relation survives arbitrarily fine local sampling; whether a terminal presentation remains terminal after intrinsic closure; whether a joint coefficient class still excludes its matched filter after moving to source coordinates; whether a global invariant localizes mass or only conserves its total; whether a spectral/operator lift removes a universal core before interpreting residual arithmetic; whether nuisance cancellation improves signal faster than its sharp conditioning bill; and whether every approximation error is small only absolutely or after multiplication by the geometric amplifier of the moving observable. The surviving theorem must expose a resource the destination uses and a matched control cannot reproduce by completion, decoding, truncation, closure, coordinate change, free switching, phase matching, delayed compensation, universal spectral background, thin-tail averaging, channel duplication, universal positivity or classical recoding.