# Global mathematical questions

## Which source identity fixes a nonprogrammable mixed-prime kernel?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) gives explicit positive higher-order perturbations invisible to every proper marginal. Adding any fixed marginal order therefore cannot identify the joint prime kernel in the ambient positive class.

[MI-002](intuition/MI-002-the-surviving-arithmetic-variable-is-relational.md) supplies a constructive comparison: a bispectral multiplicativity equation collapses residual phase freedom to one translation. An arithmetic transfer would need an identified analogue of that relation and its nondegeneracy hypotheses.

The Weil-positivity Fock branch now closes three weaker criteria. WP-261 shows that coherent collapse into a fixed finite-dimensional boundary is nonclosable at the critical Mangoldt scale. WP-262 shows that moving to infinite dimension restores bounded closability too easily: every locally finite prime graph gives an exact-half-density positive cross-prime Gram, with programmable support and phases. WP-263 then lets the source Hamiltonian select the obvious complete weighted graph, but the critical `beta=1` conditional normalization collapses on the prime layer because `sum_p1/p` diverges; unit-diagonal cutoff normalization sends every fixed prime-prime correlation to zero.

A viable kernel therefore needs a source-forced selection law that excludes hidden higher-order directions, chooses a nonprogrammable correlation geometry, and survives the critical prime-layer normalization. Equality with the completed Weil pairing and its Gamma/polar terms remains a further gate.

## What readout retains source-dependent information after matched controls?

For Prime Circle, [MI-003](intuition/MI-003-arithmetic-significance-begins-after-universal-control-subtraction.md) removes the obvious finite spectral labels and total-winding reformulation. A surviving statistic must use information below the IDS/counting level: shrinking-window placement, subleading projective phases, transfer growth, near-resonant scaling, or a genuinely long source-dependent itinerary.

For Nyman--Beurling, [MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md) identifies the collective late-span loophole as an exact finite-time localization question through NB-058. Current persisted evidence does not establish whether the finite-time arithmetic defect sectors are trivial or nontrivial, so the supported question remains whether such defects can control the canonical residual.

Robin Extremal supplies a complementary positive boundary. RE-059 shows that varying the threshold continuously inside one CA chamber only reparameterizes frozen prime-source data. RE-060 shows that a genuine higher-layer chamber transition can carry a quantized nonzero residual charge `-1+j(chi_+-chi_-)` for `j>=2`. The open global problem is whether the false-RH selector forces enough such transitions, with enough coherent structure, to yield a contradiction.

## Which quantitative observation family reaches the required source aperture?

With bounded local Euler degree, [MI-005](intuition/MI-005-topology-is-part-of-the-arithmetic-claim.md) separates finite prime-power depth from unbounded prime breadth. [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) sharpens the acquisition distinction: separate regular lattice marginals can lose mixed interactions, while complete mixed cadence moments recover the joint torus law and are exactly faithful when the common resonance intersection is trivial. If all individual cadences resonate, strict repair requires a dense generated time subgroup, and every fixed mixed-index box remains nonuniformly conditioned on an unrestricted infinite source.

AF-282 now isolates the finite-prefix **label-resolution** bill inside that picture. For the concrete joint cadences `2\pi/\log2` and `2\pi/\log3`, the joint phase nodes of `1,\ldots,N` have minimum spacing `Theta(1/N)`. Pairwise label geometry therefore requires inverse-breadth phase precision rather than exponential precision; any stronger finite-prefix instability must enter through mixed-mode degree, collective interpolation/coefficient conditioning, the noise model, or the source category. The remaining acquisition question is to price those resources jointly at the breadth actually consumed downstream, not to treat exact injectivity or node spacing as a stable inverse theorem.

Analytic Frontier gives a different sharp aperture law. ANF-183 shows that endpoint memory can remain invisible to every physical twist window `o(X/a_X)` while a rephased three-point curvature at separation `Theta(X/a_X)` must detect the sector-aligned matched packet. The remaining difficulty is source access with absolute `o(a_X)` error at that aperture, not another source-blind smoothness statistic.

Xi Flow shows the converse phenomenon: once a consecutive packet window is known, XF-186 gives an `O(m)` holomorphic measurement bank that is uniformly bi-Lipschitz on a constant physical nonlinear cell for all `m<=N`. Local inverse conditioning is therefore not the bottleneck there; unknown support, global assembly, source-side sample access, and transport to a zero-sensitive quantity are.

Visual Exploration closes both ordinary prime-power weight phases for bounded-continuous additive observables under polylogarithmic continuous windows. VIS-175 gives the generally non-Gaussian infinite weighted-Haar law for `alpha>1/2`; VIS-176 gives the Gaussian `N(0,1/8)` law for `alpha<=1/2`. The phase change at `alpha=1/2` is therefore representation structure, not source evidence. A viable visual statistic must leave that continuous additive weak-law class or demand a stronger regime with its own matched control.

## Does the complete flute response preserve the weighted source budget?

Can the physical mixed response be factored through the PF-296 `ell^1(q)` synthesis with uniformly bounded source moment and negligible physical-high residual? PF-297 shows that the canonical Robin factors preserve positive `q`-mass in the complete system, so finite-section strict contraction is only truncation leakage. PF-298 removes a second local gap: the tight-pant constant channel has relative killing only `O(s_n)` and transmission `1-O(s_n)`.

Thus no depth-uniform discount comes from either repeated local factor. The unresolved theorem must derive the actual `P/H`-split return/reassembly equation and obtain summability from accumulated nonuniform escape, support loss, nonconstant channels, source-adapted forcing, signed cancellation, or a change of coefficient currency. Even success here would leave the global heavy-range singular-value problem separate.
