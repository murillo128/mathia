# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Compute the complete destination control before increasing carrier complexity

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Analytic Frontier now makes this exact for the universal scalar route: finite binding amplifies at free density and the whole problem dualizes to a convex diffraction-realizability body; favorable local hyperuniform slopes can still fail at finite frequency. Visual Exploration adds the lower-order statistical analogue: adjacent-two-gap geometry must be treated against its finite-size RMT/arithmetic baseline. Prime Circle shows that even canonical all-to-all Kron response can remain fixed-base prime-blind.

A decisive positive must identify the complete finite, boundary, marginal/statistical, quotient, and operator control family consumed by its destination theorem and then exhibit a quantitative residual outside that body.

## Separate source fidelity, completed sign, and transport direction

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Prime Lattice shows that arithmetic phase data can identify the rational-prime source without selecting a zero divisor; native positivity is generic, while completed screw/CND positivity is already RH-level. PL-145 sharpens the interface: the canonical positive Volterra flow moves toward safer half-planes, whereas the inward direction needed for RH is not positivity preserving.

A live RH mechanism must therefore derive a source-specific completed sign/polarization or inward transport theorem rather than infer zero localization from source identification, critical normalization, or generic positive evolution.

## Turn calibrated finite visibility into original-range global assembly

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

AF-099--AF-101 close the finite-observation ambiguity: once a source norm budget is fixed, finite matrix coefficients are complete witnesses of interpolation cost. The remaining gap is range and assembly. Bidual accessibility is weaker than genuine target recovery, with the difference encoded by an exact tensor-norm comparison.

A positive must derive the budget/category from the source and prove uniformly conditioned original-range gluing with every required equivariance, positivity, locality, or order constraint.

## Find transfer theorems that are cheaper than the target they transport

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim` and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Möbius Cancellation now isolates this gate sharply. Strong power-aware data can detect a terminal defect at the right scale, but prime-only beta/Cauchy transfer overpays it, while a broad globally Liouville-close completely multiplicative comparator with the desired power saving already forces the corresponding zeta zero-free region.

The next useful transfer must exploit signed/bilinear/source-specific structure whose hypotheses are genuinely weaker than the conclusion. The same audit applies to determinant, scattering, positivity, and inverse-smoothing transfers in other lines.

## Measure coercivity in the corrected distributional quotient

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim` and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Weil Inertia replaces one minimum eigenvalue by depth-weighted low spectral mass. WI-135 removes multiplicity zero modes and WI-136 shows simple-real directions are charged by the complete Lamzouri slack, so the meaningful quotient is smaller than the earlier `V`-screened model. A decisive theorem must lower-bound the corrected spectral distribution for actual zeta zeros, not rely on raw rank or one weak mode.

## Couple provenance before universalization or classicalizing post-processing

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Prime Circle's one-hole defect and its nonlocal Kron response converge to the same fixed-base prime-blind coprime limit. Prime Flute's complete marked tail is asymptotically shared with an all-composite clone. These examples show that making an already classicalized carrier more nonlocal or spectrally elaborate cannot recreate provenance.

A live geometric/operator route must insert source-specific coupling before the universal limit, or prove that a genuinely collective all-scale assembly retains information that matched controls cannot reproduce.

## Convert Xi capacitary localization into source-controlled fixed backward time

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

XF-017 shows that a diverging logarithmic-capacity buffer beats the fixed-ratio `H^{1/2}` leakage obstruction. XF-018 renormalizes nonlinear cutoff leakage to a bounded cross-ratio kernel whose far tail is controlled by endpoint-gap/cumulative-span ratios, but neutral-mean removal introduces a span/endpoint-flux term.

The live theorem is to derive these span/flux bounds from unconditional Xi information on `Theta(log^2 T)` cores with a slowly diverging buffer and retain a positive bulk-minus-boundary margin at the `log^-2 T` dynamical scale.
