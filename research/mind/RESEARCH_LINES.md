# Global mathematical questions

## Which source resources reach the destination before observation becomes target-mixed completion, decoding, bounded interaction or classical recoding?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), [MI-020](intuition/MI-020-incommensurability-removes-collisions-but-may-end-in-decoding.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate information survival, source recovery, effective coupling complexity, collision removal and zero selection.

Arithmetic Fidelity now has both the obstruction hierarchy and an exact determining regime that survives full two-sided arithmetic sampling. AF-374--AF-376 show that killing the continuous separable gauge does not imply determination: a uniformly discrete actual row set admits compact-support false positives satisfying every sampled total-positivity minor while failing global Pólya-frequency structure. AF-377 removes the converse shortcut: even `inf gap=0` can leave every support-interaction component uniformly bounded, so arbitrary-order sampled determinants factor through bounded blocks. AF-378 removes coordinate densification as another shortcut because faithful nonlinear transport preserves the same sampled fibre. AF-379 supplies the positive criterion: density of normalized finite row-patterns modulo common translation determines every global minor, and vanishing tail mesh forces that density. AF-381 strengthens this to joint row/column pattern density: restricting **both** axes to `E={log p}` still determines every continuous translation minor, so the fully prime-indexed matrices `f(log(p_i/q_j))` are target-complete. The sampling-fidelity question is therefore solved in this intrinsic continuous translation regime even after every displayed coordinate is arithmetic; the discriminator must live in the mechanism that proves the sampled inequalities, not in the prime labels of the determining grid.

Prime Circle now makes the destination warning geometric rather than merely slice-wise. PC-319 proves that the exact canonical `(5,7)` Mordell--Tornheim carrier has zeros in `Re s>1` for every sufficiently small positive coupling. PC-320 uses joint holomorphy and Weierstrass preparation to show that each such decoupled zero extends to a local complex hypersurface finite over an open `(t,u)` bidisk, still inside `Re s>1`. Nonvanishing holomorphic completion factors preserve this divisor and local biholomorphic coordinate changes only relabel it. Thus **source recovery, multivariable nonseparability, ordinary completion and exact canonical coefficients remain orthogonal to zero selection**.

The live question is: what exact source class and retained relation identify the intended discriminator without reconstructing the source; what effective interaction arity, sampling geometry, coordinate/source-category covariance, conditioning and stability are paid; and what independent theorem controls the sign or divisor after identification? In a regime such as prime-log continuous translation positivity where even prime-ratio sampling is already determining, the remaining problem is explicitly the provenance of the source-side inequalities rather than coverage of the target relation. A useful Prime-Circle destination must likewise select something stricter than the raw PC-320 local divisor.

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), and [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) track channels left after bulk controls close and the moving structural scales on which those channels remain visible.

Nyman--Beurling now has a hierarchy of boundary-normalized signed-sampler currencies with a source-specific high-ordinate cap. NB-160 shows that many-width separation of the Jordan parts exports an `Omega(log G)` adverse-zero reservoir. NB-161--NB-162 identify `Theta=C ell/a` as the correct first-order transport currency: sub-Poisson transport suppresses both the target and signed Euler leakage, leaving one-width interlacing as the bounded-conditioning regime. NB-163 shows that finitely many local vanishing moments do not open a free channel there. NB-164 closes growing local moment order on the target side: at any fixed number of Poisson widths, target capacity decays exponentially with cancellation order, so fixed target gain requires exponential conditioning while bounded conditioning forces support to grow at least linearly in the order. NB-165 then shows that the inverse boundary-gap source tariff does **not** grow forever for the actual zeta source. The conservation remainder is governed by `C min{a^(-1), log G/log log G}`; below `a~log log G/log G`, further boundary approach no longer worsens the bill, and `C=o(log log G)` already preserves essentially full adverse debt. The surviving compact route must therefore exploit actual high-ordinate arithmetic cancellation beyond the available pointwise one-line bound while retaining target gain; neither closer boundary placement nor more local moment cancellation is a free resource.

Robin Extremal now has a sharp quantitative-source capacity rather than only an upper rigidity estimate. RE-162--RE-163 remain the exact/approximate terminal capacity theory for coarse-PNT matched controls. RE-164 bounds the remote terminal ambiguity under a Korobov--Vinogradov-scale PNT envelope. RE-165 shows that the controlling parameter

`K_b(p,T)=sqrt(p) log p exp(-b V(T))/V(T)`

is essentially sharp inside the generalized insertion class: positive insertions staying in the same PNT exponent class attain the same displacement scale. Thus `K_b->0` gives matched-source rigidity, while a positive or diverging `K_b` leaves generalized-source ambiguity possible. At the canonical cutoff `T=U_B(p)`, the fixed-`B` condition is strictly `b kappa_B>1/2`; the nominal equality lies on the ambiguous side because the next term in `V(U_B)` makes `K_b->infinity`. Terminal moment complexity is therefore relevant only after the exact source class and inversion parameter are fixed, not from a leading exponent alone.

Farey Discrepancy now prices the coordinate-face escape across the whole one-parent class. FD-154 shows that fixed finite-prime avoidance faces give exact finite-`L^q` coercivity with bounded depth and `T`-uniform distortion after spending a macroscopic arithmetic anchor. FD-155 shows that thinning this anchor by adding avoided-prime coordinates makes the direct parent load double exponential in the inverse exposure scale. FD-156 proves the sharp architecture law behind that example: every one-parent divisibility forest on the same face satisfies `P/h >= 2^|S|-1`, with equality for the direct star. Sequential rerouting can lower anchor congestion only by creating a compensating exponentially narrow cut. Thus the remaining same-anchor escape is genuinely multi-parent, not a cleverer unique ancestry chain; a different anchor geometry and a Farey-native source of its orientation remain separate possibilities.

## Which growing source law is informative rather than merely connected, numerous, zero-free, interval-pseudorandom or reconstructive?

Möbius Cancellation has moved from a half-loading minimax ceiling to two independent quartic method boundaries plus sharp admission tests for where signed information may enter. MC-327 proves that quartic saturation forces logarithmic source mass into columns with occupancy `R/2+o(R)`. MC-328 then shows that every nonnegative endpoint-polynomial weighting of degree `d_R<R/2` has exact loading at least one half on the middle Hamming band; for `d_R=o(R)` the actual source is asymptotically concentrated precisely in that blind region. Fixed even endpoint moments can concentrate on near-quadratic-conductor modes and still recover only exponent four.

MC-329 reaches the same exponent without the incidence geometry. A fixed-common-modulus prime Halász--Montgomery bias-energy estimate controls the reciprocal endpoint family through `q<=y^(4-delta)` and its exact squared-bias demand again forces `liminf log P/log y>=4` under the stated mild growth condition. Its strengthened exponent audit closes the moving-order loophole inside the same Burgess/Halász--Montgomery family: order `k` only reaches `alpha<4k/(k+1)`, the exponent is still positive at `alpha=4`, and no fixed `alpha>4` is available for any `k`. Growing the order approaches four from below but cannot cross it as a fixed-power theorem.

MC-330 closes generic signed postprocessing after `ell^2` compression. MC-331 shows that moving one common arbitrary coefficient vector **before** the character transform still cannot cross the quartic horizon because the endpoint and the coefficient-uniform prime theorem charge the same top `ell^2` norm. MC-332 then closes the opposite overcorrection: unrestricted mode-dependent rows can choose `w_(chi,r)=c_chi conjugate(chi(r))` and erase character oscillation pointwise, and at the reciprocal endpoint the full matched filter already has rank `R`. A useful joint coefficient class must therefore sit strictly between “one common vector” and “arbitrary mode dependence”: it must be source-natural, exclude or penalize the character-evaluation matched filter, and admit an analytic theorem sensitive to that restriction rather than collapsing back to either `ell^2` extreme.

Farey Discrepancy supplies the complementary source-exposure lesson. More prime coordinates can reduce the amount of anchored Möbius mass in the FD-154 family, but FD-156 shows that every one-parent implementation with fixed positive expansion then pays the same exponential Boolean packet cost in `P/h`; changing parent depth merely moves the bill. Across growing representations the useful theorem must price the exact source population, norm, scale, admissible coupling class and architecture consumed by the destination; cardinality, lower anchor mass, low matrix rank or mode dependence alone are not resources.

## Which representation-faithful null remains after nuisance variables, weak topology and universal positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md), [MI-002](intuition/MI-002-the-surviving-arithmetic-variable-is-relational.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate normalization, relational geometry, ambient representation size and source-selective sign.

Visual Exploration now has exact additive calibration plus sharp one- and two-moment nonlinear slowdown certificates inside the log-product quotient. VIS-252--VIS-253 give ordinary and logarithmic coordinate contrasts with exact eigenvalue `rho_m=(m-3)/(m-1)` and integrated time `m-2`. VIS-254 identifies the averaged block conditional variance as the heat-bath Dirichlet form: `delta_f=D_f/Var(f)` implies `tau_int(f)>=2/delta_f-1`. VIS-255 adds `eta_f=||(I-P)f||^2/Var(f)` and the sharp two-moment lower bound `B2(delta_f,eta_f)=2(1-delta_f+delta_f^2-eta_f)/(delta_f-eta_f)-1`, with an exact two-independent-block residual estimator. Thus nonlinear slowdown can still be certified when the one-moment threshold is inconclusive. Failure of both exact moment gates does not prove fast mixing, so separated-start and long-lag gates remain.

Weil Positivity now has an exact finite-cutoff readout rigidity theorem and its sharp asymptotic conditioning boundary. WP-329--WP-330 show that deterministic or readout-null operator-valued enrichment can be universal. WP-331--WP-332 show the opposite failure: under a faithful same-observable UCP readout, exact second-moment preservation kills the positive Kadison defect, puts the source observable in the multiplicative domain, and removes its off-diagonal Stinespring coupling. WP-333 shows why that fixed-cutoff theorem cannot be promoted by scalar convergence alone. If `m_R` is the least output-state mass, `||D_R||<=tau_R(D_R)/m_R`; the Mangoldt `m_R` tends to zero at least exponentially, and an explicit atom-splitting UCP channel has total-variation and all fixed-moment errors tending to zero while its source coupling stays order one. The moving thin-tail loophole is universal under matched controls, so a positive route must make the hidden coupling source-forced and destination-visible, while a global no-go must control error relative to `m_R` or in a stronger map-level topology.

Prime Lattice has now separated moving-threshold orientation from fixed-window certification. PL-324--PL-327 show that the first active `log 2` channel is an indefinite flip and that the actual sign near threshold depends on residual control beyond the canonical logarithmic `1/2` boundary exponent. PL-328 records a recent non-peer-reviewed, not independently reproduced computer-assisted claim of full Weil coercivity at `a=17/16`, after sources `2,3,4,5,7,8` are active; if validated, it moves the fixed-window certificate frontier but does not supply an aperture-uniform mechanism. Its independent structural obstruction is more reusable: a localization that drops the frequency-dependent positive tail acquires a phase-locked negative `log 8` high-frequency defect that no fixed bounded compact correction can repair. Additional source activation is therefore compatible with positivity, but scalable proofs must retain the right frequency-dependent positive information rather than replace it by a coarse scalar floor.

The common criterion is whether the destination relation survives honest nuisance propagation and is nonautomatic in the matched-control category **at the scale on which the destination consumes it**. Exact nuisance removal, source fingerprints, reversible access, positive cones, extra states, enriched ambient categories, hidden or faithfully collapsed covariance, pointwise faithful but nonuniform readout, pointwise fixed-parameter asymptotics, a classified one-prime block, a fixed-window certificate or canonical coefficients do not by themselves make the final sign/divisor source-selective.

## Which noncompact source channels and approximation costs survive a positivity reduction under the correct structural limit?

Weil Inertia has identified a quantitative threshold inside the scalar-localization escape. WI-315 removes the changing-source negative-margin obstruction because compact-support windows are nested restrictions of one global Weil form. WI-316 shows that the comparison variational bottom differs from the physical window bottom by at most the cap defect and converges, along every cofinal vanishing-cap diagonal, to the global compact-support Weil floor. Consequently a comparison lower bound `Dhat_j >= -(1/2+o(1))I` is already RH-equivalent once combined with the source-exact cap identity; a vanishing threshold defect is not a softer intermediate theorem.

WI-317 closes the fixed-defect scalar bootstrap for finite portfolios, and WI-318 extends the recurrence obstruction to arbitrary adaptive unit-window portfolios with uniformly bounded total `H^1`. WI-319 makes the required noncompactness sharp for that witness. If `M_t` is the localizer frequency second moment, then `M_t=o(t^2/log t)` still forces the continuous localization defect to vanish on the common prime-phase recurrence sequence, so any fixed contraction must pay `M_(t_k)>=c t_k^2/log t_k`. An explicit resonant three-window family at exactly that scale produces an order-one negative continuous correction and defeats the recurrence witness itself. Thus “unbounded `H^1`” was too weak a description: the known no-go survives until an almost-quadratic frequency bill is paid, but paying it only exhausts this falsifier and does not prove global contraction.

Xi Flow provides the complementary inversion lesson. XF-185--XF-186 show that known consecutive packets admit uniformly conditioned linear and local nonlinear holomorphic recovery with only linear measurement complexity, even at remote location. The remaining costs are global support discovery, assembly of separated/multiscale packets, source-valid complex access and the bridge from recovered theta-side information to a zero-sensitive Xi quantity. Local invertibility itself is no longer the bottleneck.

## Global discriminator: identify the exact resource that the matched control cannot fake

The frontier is increasingly a classification of **effective source-specific coupling that survives the actual destination map at the moving structural scale**, not nominal representation richness. AF-381 shows that even a completely prime-indexed `p/q` translation-positivity observation can be universal target-complete. FD-156 shows that unique-parent architectural ingenuity cannot remove the Boolean coordinate bill; it only trades parent load against expansion. MC-332 shows that arbitrary joint mode dependence can be worse than compression because it contains the exact matched filter and erases oscillation. NB-165 shows that geometric boundary amplification must be compared with the strongest actual source theorem: once `a` crosses `log log G/log G`, the inverse-gap tariff saturates and only further arithmetic cancellation can improve the signed conservation budget. RE-165 shows that a quantitative source theorem must likewise be used at its exact inversion parameter: leading-exponent equality can remain maximally ambiguous once the slowly varying correction is restored. VIS-255 shows that one additional exact dissipation moment can buy a strictly stronger sharp slowdown certificate because it is genuinely consumed by the spectral destination. PL-328 separates a deeper fixed-window positivity certificate from the necessity of retaining frequency-dependent tail information. WI-319 prices the scalar-localization escape at `t^2/log t` and shows the old recurrence obstruction is sharp exactly there. WP-333 shows that finite faithful readout rigidity is only uniformly useful when scalar error is normalized by the vanishing faithfulness modulus.

For every proposed route ask: what exact object reaches the destination; under which norm, scale, topology, population and **admissible source class**; what is the moving structural scale or destination margin against which errors and retained resources must be normalized; whether coordinate changes faithfully transport that source category or silently replace it; whether the matched control preserves every load-bearing source theorem rather than only a coarse asymptotic; what is the exact inversion/capacity/faithfulness parameter when leading or scalar errors balance; what is the effective interaction/coupling complexity after factorization rather than the nominal arity, rank or ambient dimension; whether the observed configurations are merely interacting or actually determining; which aliases are removed and which fibres remain; what source information was prepaid to obtain coercivity and what architectural load is paid when that exposure is reduced; how demanded modes load onto the actual source; what representation/Fourier, local-moment or approximation-basis complexity is needed to see that source; whether a joint coefficient class excludes target-matched dephasing rather than merely being low rank; whether signed/joint processing acts before or only after an information-losing positive compression; where compensating signed mass can land and what transport/conditioning price it pays; whether geometric amplification has already hit a stronger source-side ceiling; which scalar shadows, fluctuation ideals and external readout states survive an enriched construction; whether faithful readout is compatible with preserving the claimed source observable **uniformly in the cutoff**; whether an exact localization/decomposition imports genuinely new information or only reuses the same premise; whether its positive tail is retained in the frequency-sensitive topology the arithmetic channels consume; which approximation errors are genuinely weaker rather than already target-equivalent; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, moment matching, bounded interaction components, faithful coordinate redrawing, routine reservoir filling, universal convolution, readout-null noise, moving thin-tail coupling, exact-law faithful collapse, pointwise-but-nonuniform asymptotics, positive aggregation or classical recoding; and does a separate source theorem dominate the destination loss without reconstructing or assuming the endpoint?