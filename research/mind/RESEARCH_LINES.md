# Global mathematical questions

## Which source resources reach the destination before observation becomes target-mixed completion, decoding, bounded interaction or classical recoding?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), [MI-020](intuition/MI-020-incommensurability-removes-collisions-but-may-end-in-decoding.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate information survival, source recovery, effective coupling complexity, collision removal and zero selection.

Arithmetic Fidelity now separates relation type, nominal relational arity, actual sampling geometry and **support-scale interaction arity**. AF-374--AF-376 show that killing the continuous separable gauge does not imply determination: a uniformly discrete actual row set admits compact-support false positives satisfying every sampled total-positivity minor while failing global Pólya-frequency structure. AF-377 removes the converse shortcut. Even `inf gap=0` can leave every support-interaction component uniformly bounded; then arbitrary-order sampled determinants factor into bounded blocks and a finite-order PF/non-PF kernel remains an all-order false positive. A useful sampled positivity theorem therefore needs a determining source class plus genuinely unbounded interaction at the source scale, not merely dense generated differences, vanishing gaps or nominally unlimited determinant order.

Prime Circle now makes the destination warning geometric rather than merely slice-wise. PC-319 proves that the exact canonical `(5,7)` Mordell--Tornheim carrier has zeros in `Re s>1` for every sufficiently small positive coupling. PC-320 uses joint holomorphy and Weierstrass preparation to show that each such decoupled zero extends to a local complex hypersurface finite over an open `(t,u)` bidisk, still inside `Re s>1`. Nonvanishing holomorphic completion factors preserve this divisor and local biholomorphic coordinate changes only relabel it. Thus **source recovery, multivariable nonseparability, ordinary completion and exact canonical coefficients remain orthogonal to zero selection**.

The live question is: what exact source class and retained relation identify the intended discriminator without reconstructing the source; what effective interaction arity, sampling geometry, conditioning and stability are paid; and what independent theorem controls the sign or divisor after identification? A useful Prime-Circle destination must select something stricter than the raw PC-320 local divisor.

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) track channels left after bulk controls close.

Nyman--Beurling has now closed both simple extremes of the separated/collapsed signed-sampler geometry under bounded conditioning. NB-160 extends the separated-sign obstruction to arbitrary finite signed Borel height measures: sufficiently separated positive/negative Jordan supports export an `Omega(log G)` adverse-zero reservoir even with growing cardinality or continuous support. NB-161 gives the complementary transport bound. If `ell(nu_G)` is the mean optimal transport length between the two Jordan masses and `C_G` the total-variation conditioning, fixed target gain forces `C_G ell(nu_G)` to stay bounded below. Thus genuine opposite-sign collapse `ell(nu_G)=o(1)` is impossible at bounded conditioning. The surviving regime is fixed-width local interlacing with order-one transport: the same local zeros must see both signs strongly enough to cancel the separated reservoir without making the signed measure Poisson-invisible.

Robin Extremal has clarified which part of its terminal hierarchy is physically admissible under the load-bearing source theorem. RE-162--RE-163 remain the sharp exact/approximate capacity theory for coarse-PNT matched controls: exact moment threshold `L_crit=4 tau_r` and kernel-weighted Chebyshev leakage are the right terminal coordinates. RE-164 shows that these generalized-source ambiguities disappear near the canonical upper horizon if the comparison class preserves a Korobov--Vinogradov-scale quantitative PNT envelope. An order-one terminal Robin-coordinate displacement forces a relative Chebyshev excursion of size at least `eta_p/L`, while the quantitative PNT remainder makes the entire bounded terminal-shell difference `o(1)` once the same one-half inversion threshold is crossed. The live source question therefore moves below that rigidity scale or into CA selector/source coupling; more terminal moments are not the next resource when the source class already forbids the terminal perturbation.

Farey Discrepancy still separates rank-anchor tails from coordinate-face anchors. FD-153's Sathe--Selberg repair is now adversarially accepted and persisted; that closure changes confidence in the derivation, not the geometric/exponential overshoot law or the frontier. FD-154 shows bounded ancestry depth is possible only after spending explicit macroscopic source information.

## Which growing source law is informative rather than merely connected, numerous, zero-free, interval-pseudorandom or reconstructive?

Möbius Cancellation has moved from a half-loading minimax ceiling to two independent quartic method boundaries. MC-327 proves that quartic saturation forces logarithmic source mass into columns with occupancy `R/2+o(R)`. MC-328 then shows that every nonnegative endpoint-polynomial weighting of degree `d_R<R/2` has exact loading at least one half on the middle Hamming band; for `d_R=o(R)` the actual source is asymptotically concentrated precisely in that blind region. Fixed even endpoint moments can concentrate on near-quadratic-conductor modes and still recover only exponent four.

MC-329 reaches the same exponent without the incidence geometry. A fixed-common-modulus prime Halász--Montgomery bias-energy estimate controls every fixed finite character family through `q<=y^(4-delta)`. Applied to the complete reciprocal endpoint family and its exact squared-bias demand, it again forces `liminf log P/log y>=4` under the stated mild growth condition. Positive incidence/conductor accounting and common-modulus bias energy therefore stop at the same fixed-power horizon while discarding different information.

The surviving Möbius resource must distinguish arithmetic structure inside the central incidence band with Fourier degree comparable to the band, improve the prime-character horizon or its moving-order uniformity, use a genuinely joint/signed invariant not summarized by squared bias or positive conductor incidence, or change representation. Mode cardinality and another nonnegative rearrangement of the same family are not source leverage.

Farey Discrepancy continues to show that ancestry resources depend on the geometry and provenance of anchored coordinates rather than edge count alone. Across growing representations the useful theorem must be on the exact population, norm and scale consumed by the destination. Connectivity, zero-free regions, interval cancellation and source provenance are not scalar resources until their loading and transfer laws are proved.

## Which representation-faithful null remains after nuisance variables, weak topology and universal positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md), [MI-002](intuition/MI-002-the-surviving-arithmetic-variable-is-relational.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate normalization, relational geometry, ambient representation size and source-selective sign.

Visual Exploration now has both exact linear calibration and an exact one-sided nonlinear slowdown certificate inside the log-product quotient. VIS-252--VIS-253 give ordinary and logarithmic coordinate contrasts with exact eigenvalue `rho_m=(m-3)/(m-1)` and integrated time `m-2`. VIS-254 identifies the averaged block conditional variance as the heat-bath Dirichlet form: `delta_f=D_f/Var(f)` implies `tau_int(f)>=2/delta_f-1`. Thus `delta_(bar T)<2/(m-1)` rigorously proves nonlinear integrated-autocorrelation slowdown before long-lag fitting. Failure of that inequality does not prove fast mixing, so separated-start and long-lag gates remain.

Weil Positivity has now closed both the simplest homomorphic-shadow and state-readout enriched escapes. WP-328 shows that divisibility cannot be repaired merely by adding a face/state when the exact Mangoldt source remains a homomorphic free scalar shadow: bi-free marginal placement and c-free reference placement are impossible because the source is freely indecomposable, while the degenerate outer c-free placement is universally Boolean-divisible. WP-329 shows why operator-valued freeness escaping scalar shadow descent is still not enough: a deterministic amalgamation-algebra coordinate is operator-valued freely infinitely divisible for every scalar law, and the exact Mangoldt source can be stored entirely in the scalarization state while the enriched law stays unchanged. A viable operator-valued route must put arithmetic into genuinely non-deterministic mixed data before scalarization and make that data participate in a nonautomatic finite--archimedean--global sign theorem.

Prime Lattice has separated the first-prime algebra from the missing arithmetic orientation. PL-324 proves that odd parity removes the generic archimedean order obstruction until the first rational-prime event, where order positivity fails. PL-325 then reconstructs the active `log 2` overlap as an exact two-edge flip with spectrum `{-1,0,1}` and shows that this single-shift algebra, its Legendre compression and related finite-scale machinery are already public prior art. The overlap is norm-one for any positive width yet form-small near threshold against the endpoint potential, so raw operator size does not determine the Weil crossing. The live object is the signed state selected by the **full** Weil operator and its source/endpoint coupling across apertures.

The common criterion is whether the destination relation survives honest nuisance propagation and is nonautomatic in the matched-control category. Exact nuisance removal, source fingerprints, reversible access, positive cones, extra states, enriched ambient categories, a classified one-prime block or canonical coefficients do not by themselves make the final sign/divisor source-selective.

## Which noncompact source channels and approximation costs survive a positivity reduction under the correct structural limit?

Weil Inertia has identified three increasingly sharp non-resources. WI-315 removes the changing-source negative-margin obstruction because the compact-support windows are nested restrictions of one global Weil form. WI-316 shows that the comparison variational bottom differs from the physical window bottom by at most the cap defect and converges, along every cofinal vanishing-cap diagonal, to the global compact-support Weil floor. Consequently a comparison lower bound `Dhat_j >= -(1/2+o(1))I` is already RH-equivalent once combined with the source-exact cap identity; a vanishing threshold defect is not a softer intermediate theorem.

WI-317 then closes the most direct fixed-defect scalar bootstrap. Any fixed finite portfolio of source-exact smooth localizations at fixed aperture, when every localized piece is fed only the same scalar lower bound, has a simultaneous high-frequency recurrence sequence on which the localization errors are nonnegative or vanish. Even choosing the best portfolio member after seeing the test function cannot yield a uniform defect contraction. The genuinely intermediate frontier is therefore a fixed nonzero defect plus **new localized information**—stronger arithmetic estimates, mode/prime-dependent or non-scalar invariants, or an infinite/adaptive architecture whose added degrees of freedom carry source content—not merely more exact decompositions of the same scalar premise.

Xi Flow provides the complementary inversion lesson. XF-185--XF-186 show that known consecutive packets admit uniformly conditioned linear and local nonlinear holomorphic recovery with only linear measurement complexity, even at remote location. The remaining costs are global support discovery, assembly of separated/multiscale packets, source-valid complex access and the bridge from recovered theta-side information to a zero-sensitive Xi quantity. Local invertibility itself is no longer the bottleneck.

## Global discriminator: identify the exact resource that the matched control cannot fake

The frontier is increasingly a classification of **effective source-specific coupling**, not nominal representation richness. AF-377 shows that all-order sampled relations can secretly have bounded interaction arity. MC-329 shows that the complete common-modulus endpoint family still hits the same quartic horizon as a different incidence architecture when compressed to positive bias energy. NB-161 shows that fine signed cancellation becomes Poisson-invisible unless conditioning pays inversely for Jordan transport length. RE-164 shows that terminal generalized-source freedom disappears when the matched class preserves the quantitative PNT theorem already consumed by the destination. VIS-254 turns exact fiber variance into a rigorous nonlinear slowdown lower bound. PL-325 classicalizes the first-prime overlap algebra and leaves full-state arithmetic orientation as the real gate. WI-317 shows that a finite portfolio of exact localizations cannot strengthen a scalar premise by feeding it back to itself. WP-329 shows that operator-valued divisibility can be completely universal when all source specificity is deferred to the readout state.

For every proposed route ask: what exact object reaches the destination; under which norm, scale, topology, population and **admissible source class**; whether the matched control preserves every load-bearing source theorem rather than only a coarse asymptotic; what is the effective interaction/coupling complexity after factorization rather than the nominal arity or ambient dimension; which aliases are removed and which fibres remain; what source information was prepaid to obtain coercivity; how demanded modes load onto the actual source; what representation/Fourier or approximation-basis complexity is needed to see that source; where compensating signed mass can land and what transport/conditioning price it pays; which scalar shadows and external readout states survive an enriched construction; whether an exact localization/decomposition imports genuinely new information or only reuses the same premise; which approximation errors are genuinely weaker rather than already target-equivalent; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, moment matching, bounded interaction components, routine reservoir filling, universal convolution, state-only scalarization, positive aggregation or classical recoding; and does a separate source theorem dominate the destination loss without reconstructing or assuming the endpoint?
