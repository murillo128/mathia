# Global mathematical questions

## Which source resources reach the destination before observation becomes decoding, classical recoding or a target-mixed completion class?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md) separate information survival, source recovery and zero selection.

Arithmetic Fidelity now supplies a sharper relational hierarchy. AF-369--AF-370 show that small retained data can identify the source inside sufficiently rigid categories, without selecting its zeros. AF-371 separates source richness from target-faithful positivity; AF-372 shows that the correct translation-total-positivity geometry cannot in general be truncated at fixed determinant order; AF-373 separates relational arity from sampling geometry. AF-374 identifies the exact fixed-lattice quotient: multiplication by a positive periodic gauge leaves every lattice-row Toeplitz minor unchanged up to positive factors, and the ambiguity survives entireness and Gaussian decay. A determining certificate therefore needs the right relation, enough arity, a determining sampling geometry, and a source class that kills the observation gauge.

Prime Circle now has three nested classicalization boundaries. PC-311--PC-315 close scalar mixed-conductor fusion and the complete finite-Fourier orbit; PC-316 shows that the natural pre-compression product retains two source indices but lands in classical Mordell--Tornheim multiple-`L` theory; PC-317 shows that every rational one-parameter radial ray collapses those two indices again to eventually affine/quasipolynomial coefficients and a finite Hurwitz-zeta packet. Preserving more indices is meaningful only while the destination keeps enough independent geometry to avoid classical recoding, and even then a separate zero-selecting theorem is required.

Prime Lattice has lowered its destination bill rather than increased source recovery. PL-321--PL-323 close the moving Green transfer, residual moment and coercive equation for the actual branch. The remaining gate is the rational-prime-specific sign/first-crossing law carried by the transferred scalar.

The live audit is: **what exact source class and retained relation identify the intended object; what relational arity, sampling geometry, observation gauge and stability are paid to reach the destination; what information survives scalarization; and what separate theorem controls the divisor or sign once the object has been identified?** Decoding, universal transform closure, source recovery, multiple Mellin variables and large families of generic positivity inequalities are not substitutes for the final zero-sensitive implication.

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) track the increasingly narrow channels left after bulk controls close.

Nyman--Beurling now closes exterior signed sampling well into the macroscopic vertical regime. NB-154 identifies the gap-normalized condition number `D_G=V_G/(a_G q_G)`; NB-155 shows that every sublinear support width merely transports the adverse sign debt. NB-156 replaces the old coarse `B_G=o(G)` boundary by the exact completed-Gamma tariff. If the lowest sampled ordinate is `H_G=G-B_G->infinity`, the combined budget is `E_G=D_G+C_G log(G/H_G)`. Whenever `E_G=o(log G)`, a logarithmic target packet still forces `Omega(q_G log G)` adverse charge and `Omega(log G/D_G)` adverse zeros. The remaining vertical escape must spend this logarithmic tariff, descend into a genuinely different low-ordinate regime, or obtain sharper source-side cancellation/coupling.

Robin Extremal has moved beyond the all-fixed-order moment obstruction. RE-159 gives the fixed-order terminal scale `epsilon_r=epsilon_PNT^(1/r)`. RE-160 proves that when the matched order grows, the uniform stability scale is instead `tau_r=(r! epsilon_PNT)^(1/r)~(r/e)epsilon_PNT^(1/r)`, with explicit matched generalized sources giving the converse obstruction within universal constant factors. Terminal information must therefore be priced jointly by **moment order and resolution**; adding a growing list of moments is not automatically faithful.

Farey Discrepancy now has the full anchor-depth interpolation for the complement cut. FD-152 gives the central-anchor `sqrt(log log T)` Gaussian child scale. FD-153 shows that a supertypical rank anchor has geometric conditional overshoot, with an exponential moderate-deviation transition and depth `L_T~delta_T^(-1)`. Constant child depth is possible only after the anchor already fixes `1-o(1)` of the coefficient space. The apparent depth escape therefore trades directly against asymptotically total source orientation.

The common question is no longer whether a broad theorem holds on average, but whether the exact target-bearing configuration is controlled in the norm, population, conditioning, anchor mass, terminal resolution and exceptional scale the destination consumes.

## Which growing source law is informative rather than merely connected, high-dimensional, zero-free, interval-pseudorandom or reconstructive?

Farey Discrepancy now has one joint resource law across sparse, central and near-total anchors: parent distortion, direct anchor mass and residual child depth trade against one another, with FD-153 closing the near-total regime. A source-valid mechanism must explain where the anchor information and residual exposure come from without silently encoding Möbius orientation.

Möbius Cancellation now converts family-energy bounds into a stronger package-capacity obstruction. MC-322 gives a bounded `ell^2` shell-bias budget under one controlled modulus; MC-323 forces varying fixed-bias characters to the quadratic individual-conductor frontier. MC-324 then uses the endpoint's complete pair spectrum: all `binom(R,2)` pair modes retain fixed shell bias, and a weighted Plotkin double count turns their individual conductor cost into `liminf log P/log y>=4` for the common source radical. Under the natural prime cap this costs `(4-o(1))R` source coordinates. Higher mode geometry, not just generator count, is therefore a source currency.

The next useful theorem in any growing representation must survive its exact algebra and matched controls and be stated on the same population, norm and scale as the destination. Many observations, connectedness, zero-free regions, small interval sums and source provenance are not scalar resources until that transfer is proved.

## Which representation-faithful null remains after nuisance variables, weak topology and universal scalar positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) and [MI-002](intuition/MI-002-the-surviving-arithmetic-variable-is-relational.md) separate normalization, relational geometry and source-selective sign.

Visual Exploration has closed the structural nuisance quotient and exact communication-class question inside the symmetric-Dirichlet model. VIS-247--VIS-250 give the `alpha`-free conditional coarea law, exact `m=3` conditional, reversible heat-bath kernel, connected regular level set and irreducibility. The honest remaining cost is quantitative mixing and numerical calibration, not sampler existence or hidden disconnected support.

Weil Positivity now closes three nearby scalar positivity categories. WP-319--WP-323 show that source-specific values and arbitrary fixed finite confluent Pick jets may survive while their sign is a universal resolvent Gram. WP-324 shows that direct positive-source Laplace translation minors have universal alternating sign and the reversed Hankel orientation is universally positive. WP-325 closes the natural nonlinear log-Laplace escape: the tilted prime-power carrier is not even two-divisible, so `-log R_s` cannot be Bernstein; meanwhile `1-R_s` is universally Bernstein but immediately fills mixed sums and loses prime-power localization. The remaining sign theorem must be both source-selective **and actually positive for the arithmetic source**, not merely a source-specific failure of a universal positivity mechanism.

The common criterion is whether the destination relation survives honest nuisance propagation and is nonautomatic in the matched-control category. Exact nuisance removal, source fingerprints, unbounded numbers of tests, global accessibility, or exact reversible access to a quotient do not by themselves make the final statistic source-selective or usable.

## Which noncompact source channels and adaptive representation costs must survive a positivity reduction?

Weil Inertia has moved from a frozen essential-spectrum obstruction to a **deep-sector sign-resolution** problem. WI-311 makes the capped retained tail arbitrarily close to `(1/2)I`; WI-312 shows that the smooth flattening spends diverging componentwise Hilbert--Schmidt complexity. WI-313 identifies what the saturation actually preserves: total negative comparison index tracks source spectrum below `1/2`, not source negativity. The RH-relevant statistic is the comparison count below the full compensation depth `c=1/2-delta`, with

`N_Q(0) <= N_(D_hat)(-c) <= N_Q(delta)`.

Thus the comparison resolves source sign only up to a `delta` window, and shrinking that window in the known smooth construction costs `Omega(delta^(-1))` in squared Hilbert--Schmidt norm for one continuous component. The remaining theorem must control the **recombined deep negative sector** as the sign-resolution window closes; neither a large scalar floor, finite total inertia nor componentwise `S_2` simplicity is the right endpoint.

A construction must still pass the signed-density quotient of WI-241. An impossibility theorem must survive admissible bounded-tail reserve/localizer choices and exact recombination rather than exploit an overcharged or componentwise-divergent decomposition.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is more specific, not closer by rhetoric. AF-374 exposes an exact periodic observation gauge behind fixed-lattice total positivity. FD-153 shows that cheap residual ancestry depth is purchased by near-total source anchoring. MC-324 lifts pairwise shell bias into a quartic common-radical bill. NB-156 replaces the vertical-support boundary by a logarithmic completed-Gamma tariff. RE-160 gives the factorial resolution scale for growing terminal moment order. PC-317 shows that rational one-dimensional radial scalarization destroys the extra two-index information by finite Hurwitz recoding. WP-325 shows that the source-selective log-Laplace semigroup is forbidden by exact prime-power support while the available Bernstein alternative is universal. WI-313 identifies the deep compensation threshold, not total comparison inertia, as the statistic that approximates source sign.

For every proposed route ask: **what exact object reaches the destination; under which norm, scale, quantifier, topology, observation population, retained source moments, relational arity, sampling geometry and admissible source class; which observation gauge or universal component has been removed and what debt that removal creates; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, moment matching, universal transform closure, generic positivity, classical multiple-`L` recoding or near-scalar compensation; and does a separate source theorem dominate the destination loss without reconstructing or assuming the endpoint?** A new representation is useful only when one of those answers changes mathematically.
