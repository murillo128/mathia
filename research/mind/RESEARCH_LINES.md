# Global mathematical questions

## Which source resources reach the destination before observation becomes decoding, classical recoding or a target-mixed completion class?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate information survival from the modulus, source class and category consumed by the target.

Prime Circle now closes the direct post-compression functional-equation ladder. PC-310 shows that the first non-collapsing mesh-scale Poisson layer is already an invertible cyclic decoder. PC-311 sends the direct bilinear mixed-conductor pairing to a finite bundle of ordinary `L(s+t,chi)` functions. PC-312 rules out accidental scalar finite-Fourier self-duality of the canonical coefficient, and PC-313 shows that retaining `(d,F_N d)` only yields the universal Hurwitz swap relation, diagonalizing to `d±F_N d`. A surviving carrier must retain source-native information **before** periodic scalarization or use state not determined by that universal Fourier pair.

Arithmetic Fidelity sharpens the completion boundary from two sides. AF-365 protects principal parts under global eventual transformed control; AF-366 exposes invisible pole-carrying tails behind every finite prefix; AF-367 keeps zero geometry mixed even inside a positive multiplicative near-square-root Beurling class. AF-368 now shows that for every `c>0` a nonordinary Beurling system can satisfy `|N(x)-floor(x)|<c log x`. Even extremely accurate global scalar counting need not identify the rational-prime source. The missing source law must therefore be structural rather than merely a smaller fixed logarithmic counting constant.

Prime Lattice moves in the opposite direction by lowering the destination bill. PL-322 makes the residual moment automatic from conservative endpoint flux and the convergent source trace; PL-323 forces `Qm->0` from the source-coupled coercive equation; PL-321 then closes the moving Green transfer. The analytic endpoint gate is no longer live for the actual branch. What remains is the rational-prime-specific sign/first-crossing law carried by the transferred scalar.

The useful quantity across these lines is the **destination margin obtained before observation crosses into generic inversion, classical recoding, universal transform closure or an admissible-completion fibre that still mixes source/target states**.

## Which target-conditioned source law controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) now separates three different failure modes: signed cancellation that loses its gain under absolute domination, exceptional zero geometry invisible to typical-center laws, and local source information that occupies vanishing destination mass.

Nyman--Beurling classifies the signed exterior escape more tightly. NB-148 cancels the universal Hadamard logarithm with a zero-mass sampler but exports equal negative Poisson mass. NB-149 proves that controlling that adverse lobe by the negative Jordan sampler or total variation simply restores a positive `Theta(log G)` budget. A useful signed route must therefore retain sign/location information after cancellation rather than return to the positive cone.

Robin Extremal has two distinct target-conditioned gates. RE-151--RE-152 show that one-sided reciprocal-scale hiding can occur at lacunary exceptional stages while all fixed-order globally averaged local statistics remain unchanged. RE-153 independently identifies the direct source quantity paid by a self-tangent counterexample: a Pareto-weighted average of `(t-vartheta(t))/sqrt(t)` must be at least `sqrt(2)-o(1)`, and the canonical height kernel is spread over multiplicative scales with median `4p`. Sublinear additive selector neighborhoods see asymptotically none of that mass.

The live question is therefore quantitative and logical: what theorem is uniform on every target-bearing configuration, or directly controls the destination-weighted source average, and does its rate beat the visibility loss without taking absolute values that erase the gain?

## Which growing source law is informative rather than merely connected, high-dimensional, zero-free, probabilistic or reconstructive?

Farey Discrepancy prices the reweighting escape itself. FD-147 identifies finite-`L^q` binary ancestry recovery with an anchored Cheeger ratio. FD-148 shows that fixed-seed positive expansion under nonuniform weights requires singular endpoint marginals: parent amplification of order `T` or child/prime-ray amplification of order `log T`. Connectivity and mild reweighting are both too weak; an arithmetic weighting must derive the endpoint concentration it spends.

Möbius Cancellation gives two complementary warnings and one partial source gain. MC-317 shows the constant-defect half-rate code endpoint is generically realizable. MC-318 rejects a probabilistic proxy at the exact-stratum/algebraic-collapse gate. MC-319 finds a genuinely arithmetic property: the forced common radical is smooth enough for strong family zero-free control. But the known conversion reaches only broad power-interval prime sums of size `O((loglog q)^(-1/2))`, far above the dyadic `1/log y` endpoint signal. Analytic rigidity and endpoint resolution remain separate resources.

The next useful theorem in any growing representation must survive its own exact algebra, matched-control class and **resolution transfer**. “Many observations,” “connected,” “zero-free” and “source-generated” are not scalar resources until the destination norm and scale are priced.

## Which representation-faithful null remains after nuisance variables, weak topology and universal positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) now has sharper endpoints on both the Visual and Weil-positivity sides.

VIS-242--VIS-243 make the symmetric-Dirichlet tent calibration explicit. VIS-244 identifies the nuisance parameter through gap concentration `C`; VIS-245 gives the exact fixed-`alpha` covariance `Cov(C,bar T)`, completing the second-moment block. The remaining same-sample gate is not unknown dependence but the nonlinear conditional/full joint calibration after `alpha_hat(C)`. A covariance matrix alone does not make plug-in testing exact.

WP-319--WP-321 show that source-specific derivative-biased Mangoldt laws and even the whole bounded-gap scalar Julia family are reproducible in the natural weak topology after atomwise smoothing. WP-322 then closes the canonical exact finite-node escape: every boundary Loewner matrix is a universal resolvent Gram for any positive scalar Herglotz source, and finite Julia reductions are Schur complements of that same Gram. Exact arithmetic entries may survive while the **positivity theorem** remains universal.

The common discriminator is whether the destination uses a relation that survives honest nuisance propagation and whose sign/validity is not automatic under the relevant matched-control category. On the Visual side this means a predeclared residual after conditional nuisance calibration; on the Weil side it means a support-sensitive non-Pick invariant or a change to matrix/operator/global finite--archimedean structure.

## Which noncompact source channels and reserve scales must survive a positivity reduction?

Weil Inertia has changed obstruction class. WI-304--WI-306 prove that Liu's frozen `beta_0=1/16` tail-dropped comparison develops an essential compact-irrepairable negative band at the first active prime, and that same-width localizer shape cannot remove it. WI-307 shows that the reserve is not intrinsic: the unbounded archimedean symbol allows arbitrary reserve lifting at every fixed aperture. For `B>||A_(L,chi)||`, the essential spectrum is positive and any remaining negative sector is finite/discrete.

The live question is therefore not whether the frozen essential band exists, but whether the source-exact remainder controls the finite negative sector and whether the reserve/Fourier-cutoff cost remains viable as `L->infinity`. A global impossibility theorem must survive all admissible reserve choices; a construction must pay the scaling cost and still pass the signed-density quotient of WI-241.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier has become more specific rather than closer to an RH proof. AF-368 defeats fixed positive logarithmic scalar counting as a source identifier; PC-313 classifies the minimal Fourier-pair functional equation as universal; PL-323 closes the ambient endpoint-transfer gate and exposes arithmetic sign as the residual; NB-149 shows that Jordan domination forfeits signed Hadamard cancellation; RE-153 converts self-tangent height into a canonical Pareto-weighted source average; VIS-245 completes fixed-parameter nuisance covariance but leaves conditional calibration; WI-307 converts a frozen essential obstruction into a finite-index/scaling problem; and WP-322 makes finite exact scalar cross-gap Pick positivity universal.

For every proposed route ask: **what exact object reaches the destination; under which norm, scale, quantifier, topology and admissible source class; which universal/nuisance component has been removed and what debt did that removal create; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, universal transform closure or Gram positivity; and does the surviving source theorem dominate the destination loss without reconstructing or assuming the endpoint?** A new representation is useful only when one of those answers changes mathematically.