# Global mathematical questions

## Which source resources reach the destination before stable observation becomes generic decoding, classical recoding or a target-mixed completion class?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate information survival from the modulus, source class and category consumed by the target.

Prime Circle now closes three successive direct escapes. PC-310 shows that the first non-collapsing mesh-scale Poisson layer is an invertible cyclic decoder with condition number `Theta(q)`. PC-311 then shows that the direct bilinear mixed-conductor pairing at `q,r` passes to `Z/(qr)Z` and becomes a finite bundle of ordinary `L(s+t,chi)` functions. PC-312 finally proves that the canonical source weights do not accidentally restore the missing scalar modularity: the exact-period coefficient vanishes at `q,r` while its finite Fourier dual equals `2/sqrt(qr)` there, so it is not a Fourier eigenvector and cannot satisfy the corresponding scalar degree-one functional equation. Single-level lossless recovery is generic decoding; the first bilinear cross-level repair is classical periodic recoding; its source-native scalar self-duality also fails. A surviving carrier must retain genuinely nonseparable/vector-valued conductor information before those reductions and still supply a zero-sensitive destination theorem.

Arithmetic Fidelity sharpens the completion-side boundary. AF-365 protects Dirichlet principal parts under global eventual Riesz control; AF-366 shows finite prefixes admit invisible pole-carrying tails; AF-367 shows the problem persists inside a much more arithmetic positive category. Positive Beurling generalized-prime systems can agree with every ordinary prime/integer below an arbitrary finite cutoff and share near-square-root generalized-integer counting, yet have opposite RH zero geometry. Finite exact data plus positivity, Euler-product multiplicativity and that coarse counting class still do not determine the target. The missing source class must use stronger ordinary-arithmetic/global continuation or cancellation structure.

Prime Lattice moves in the opposite direction by reducing the destination demand. PL-320--PL-321 show that the full source trace plus one singular translation remainder suffice for the moving Green transfer; the old absolute-`L^1` alternative disappears. Across these lines, the useful quantity is the **destination margin obtained before observation crosses into generic inversion, classical recoding, scalar duality failure or an admissible-completion fibre that still mixes target states**.

## Which local source law controls target-bearing exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) now has corrected Robin scope and a sharper signed Nyman boundary.

NB-147 classifies positive exterior-Poisson occupancy by weighted reflection closure. NB-148 shows that cancelling the universal Hadamard log budget with a zero-mass signed sampler necessarily creates equal integrated negative Poisson mass. The signed escape is therefore not “more cancellation for free”; it needs an independent theorem controlling the negative lobe or a prime-side mechanism that pays for it.

RE-151 replaces the withdrawn symmetric packet claim with an exact one-sided obstruction: pure vertical phase geometry can hide a scale-maximal target by `exp(-cR)` on `[(1-kappa)U,U]`, while the post-anchor half remains open. RE-152 shows that such bad stages can be inserted lacunarily without changing any fixed-order globally averaged GUE-like local-correlation limit. Typical-center population/statistical laws do not exclude a target-conditioned exceptional center.

The live source question is therefore quantitative and logical: what theorem is uniform on every target-bearing local configuration, or has exceptional probability/mass small enough to eliminate all possible target stages, and does its rate beat the destination visibility loss? Average laws and integrated sign cancellation must not be silently promoted to that quantifier.

## Which growing source law is informative rather than merely connected, high-dimensional, zero-free, probabilistic or reconstructive?

Farey Discrepancy now prices the reweighting escape itself. FD-147 identifies finite-`L^q` binary ancestry recovery with an anchored Cheeger ratio. FD-148 shows that fixed-seed positive expansion under nonuniform weights requires singular endpoint marginals: parent amplification of order `T` or child/prime-ray amplification of order `log T`. Connectivity and mild reweighting are both too weak; an arithmetic weighting must derive the endpoint concentration it spends.

Möbius Cancellation gives two complementary warnings and one partial source gain. MC-317 shows the constant-defect half-rate code endpoint is generically realizable. MC-318 rejects a probabilistic proxy at the exact-stratum/algebraic-collapse gate. MC-319 finally finds a genuinely arithmetic property: the forced common radical is smooth enough for strong family zero-free control. But the known conversion reaches only broad power-interval prime sums of size `O((loglog q)^(-1/2))`, far above the dyadic `1/log y` endpoint signal. Analytic rigidity and endpoint resolution are separate resources.

The next useful theorem in any growing representation must therefore survive its own exact algebra, matched-control class and **resolution transfer**. “Many observations,” “connected,” “zero-free” and “source-generated” are not scalar resources until the exact destination norm and scale are priced.

## Which representation-faithful null remains after nuisance variables and weak topology are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) now has a closed nuisance-identification fork on the Visual side and a full unthinned weak-topology obstruction on the Weil-positivity side.

VIS-242--VIS-243 make the symmetric-Dirichlet tent calibration explicit. VIS-244 supplies an independent gap-concentration statistic `C` whose expectation identifies `alpha` and whose variance is exact. But same-configuration `C` and `bar T` are dependent. Confirmation must either freeze `alpha` independently or use the joint `(C,bar T)` nuisance law; plug-in fitting followed by a fixed-parameter test is not calibrated.

WP-319 identifies tight, source-specific derivative-biased Mangoldt gap laws. WP-320 shows sparse same-source coherence can be grafted. WP-321 removes sparsity: one atomlessly smoothed positive source matches derivative normalization and bounded-Lipschitz laws at every consecutive gap, and hence the full fixed-additive-gap Julia family locally uniformly. Non-sparse provenance in this natural weak/Julia topology is still not source-selective.

The common discriminator is now whether the destination uses a relation that survives honest nuisance treatment **and is not continuous under the matched-control topology**. On the Visual side this means a predeclared residual after nuisance propagation; on the Weil side it means exact atomic/support arithmetic, another discontinuous cross-gap invariant, or finite--archimedean coupling before scalar Julia reduction.

## Which noncompact source channels must survive a positivity reduction?

Weil Inertia closes the localizer-shape slack at the first active prime. WI-304 gives the essential numerical-range criterion `w_q>beta` for compact-irrepairable omitted atoms; WI-305 places the obstruction already at `q=2` for Liu's support and reserve. WI-306 proves the compressed-translation numerical-radius bound is exact:

`sup rho_chi(a)=cos(pi/(ceil(ell/a)+1))`.

Thus no same-width shape optimization can weaken the first-prime defect. If the reserve is artificially frozen at `1/16`, even support variation cannot cross the isolated `q=2` gate until `ell>5 log 2`. This remains a tail-dropped comparison, not a theorem about the full Weil form. The later source gate is still the signed density-subtracted discrepancy of WI-241.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier has become more specific rather than closer to an RH proof. AF-367 defeats finite-prefix plus coarse positive generalized-prime regularity; PC-311 classicalizes the direct bilinear mixed-conductor repair and PC-312 rules out accidental scalar finite-Fourier self-duality of its canonical weights; FD-148 prices nonuniform anchored expansion by singular endpoint mass; MC-319 separates zero-free rigidity from dyadic resolution; NB-148 turns signed cancellation into an equal negative-lobe debt; RE-151--RE-152 separate one-sided target-conditioned geometry from global typical-zero statistics; VIS-244 isolates nuisance fitting; WI-306 removes localizer-shape slack; and WP-321 shows that even every-gap scalar Julia coherence is weak-topology matched-control realizable.

For every proposed route ask: **what exact object reaches the destination; under which norm, scale, quantifier, topology and admissible source class; what nuisance/universal component has been removed and what debt did that removal create; what source-specific variable remains; can the matched-control class reproduce that variable by decoding, completion, averaging, smoothing or grafting; does a classical recoding preserve the exact duality the destination needs; and does the surviving source gain dominate the destination loss without reconstructing the endpoint?** A new representation is useful only when one of those answers changes mathematically.