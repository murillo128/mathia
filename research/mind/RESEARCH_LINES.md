# Global mathematical questions

## Can the source-native pole tangent be lifted to a canonical completed operator with the correct sign?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate continuum provenance, spectral layers, model selection and sign.

WP-267--WP-277 rule out a sequence of free constructions: local positive hosts, inverse reconstruction from an already supplied target, exact bounded pure-point-to-a.c. intertwining, and bounded/subcritical mixing of compact-resolvent sectors. WP-278 then gives the surviving infinite-volume category a source-native measure-level realization: the critical Mangoldt half-density has high-energy tangent `cosh(x/2)dx`, exactly the polar continuum of the completed carrier.

WP-279 shows why the same scalar tangent expansion cannot simply continue to Gamma. After the pole term is removed, nontrivial-zero modes lie parametrically above the Gamma layer, even under RH. WP-280 tests the canonical primitive escape: retaining the critical Chebyshev primitive really restores the central Gamma slope, but the normalized logarithmic-primitive limit that isolates it is itself equivalent to RH, as is eventual sign of the same primitive.

WP-281 closes the fixed finite-state local compromise. Even under RH, a stable local regularizer leaves a nonconstant uniformly almost-periodic zero field. Any further fixed finite-dimensional stable linear response has a rational transfer function; retaining the Gamma zero mode while forcing convergence would require that rational function to vanish at every nontrivial-zero ordinate. Thus internal finite-dimensional state does not evade the screening merely by being non-scalar.

The remaining burden is genuinely global and category-changing: construct an independently selected operator/boundary/cohomological architecture that couples finite and archimedean sectors before scalarization, or an independently forced infinite-dimensional/nonrational, scale-nonlocal or nonlinear mechanism whose sign can be audited without subtracting the zero divisor or importing an RH-equivalent primitive limit/sign condition.

## Which source restrictions actually shrink the target-reachable failure set at the metric the theorem consumes?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) distinguish source reduction, exact failure incidence, non-vacuity and stable distance from failure.

AF-300--AF-305 replace ambient deleted directions by intrinsic source dimension, reachable positive bodies and a sharp discrete saturation boundary. AF-306 proves that every fixed saturated support can approach zero arbitrarily closely. AF-307 classifies the first prime-support exact cases, AF-308 makes the ambient exact-hit cone Lebesgue-null, and AF-309 proves that the same cone is dense with dense complement. AF-310 isolates the elementary relation obstruction: a normalized common-time distinct-prime phase point has a cyclic integer relation lattice of rank at most one, excluding the forced rank-two zero geometries at `k=3,4` but not the pentagon. AF-311 strengthens the source side with Baker theory and still finds generic pentagon compatibility. AF-312 adds the first nonlinear post-hit rigidity: a hypothetical five-prime hit cannot repeat at harmonics `2,3,4,5`, and any fixed integer time lattice contains only finitely many hits. The open `k>=5` problem is therefore specifically **first** nonlinear nontorsion incidence; neither linear relation theory nor recurrence sparsity decides whether the first hit occurs.

Prime Circle gives a matched-control analogue. PC-272--PC-276 show that ordered information, the raw near-resonance law and every finite fixed-bandwidth logarithmic centering can all be classical source data. PC-277 closes the intrinsically normalized scalar law whenever `log B=o(sqrt(log q))` using a denominator-matched exact-`Li` control, while `(B+1)^2>q` forces universal exact rational resonance. Only the intermediate rational-microscopic scalar window remains unresolved before one must leave that observable.

PF-315--PF-318 reduce the full physical angle to the independent channels `T_H` and `Z_LH`, localize the direct channel to three pant offsets, and show that logarithmic target rank plus Hilbert--Schmidt leakage `O((1+log P_n)/P_n^2)` already gives weak trace. PF-319 proves that reciprocal-prime form-smallness in the corridor metric does not imply that seam-normalized leakage. The strengthened PF-242 now proves the canonical hypercycle multiplier itself is exponentially local in Pöschl--Teller mode index and has cut-uniform squared Hilbert--Schmidt leakage `O(P_n^-2)`. The remaining direct-channel theorem is therefore metric/operator transport of an already-local coefficient, not discovery of coefficient-level locality.

Visual Exploration sharpens the observation-scale bill in another direction. VIS-200 proves that changing the sieve/taper cutoff cannot improve the leading four-form pointwise threshold. VIS-201 then removes that pointwise local-factor loss by averaging the nondegenerate three-polynomial conductor fibers with discriminant-uniform Nair--Tenenbaum bounds. VIS-202 closes the remaining scalar-cutoff freedom: within the resulting dimension-four fiberwise sieve plus taper-tail majorant, some cutoff succeeds if and only if `log H/sqrt(log y)->infinity`. The residual problem is therefore structural fiber incidence/cancellation, a different tail mechanism, or a different dephasing resource; the square-root-log boundary is a method threshold, not a proved threshold of the true collision energy.

## Which residual cancellation theorem survives after generic abundance, exact embeddings and conductor descent are exhausted?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) place Farey and Möbius beyond their previous generic barriers.

FD-083--FD-085 show that full-counting occupation and polynomial additive blocks do not imply a finite-entropy multiplicative chain. FD-086 restores the physical source and makes the normalized Jordan/Schur packet coherent throughout each frontier-length block. FD-087 separates exact embedding from useful normalized capacity, and FD-088 escapes the small-anchor capacity bound with repeated-prime deflation. FD-089 compresses the row-adaptive packet to canonical prime contractions. FD-090 then removes the predecessor-count entropy loss itself: after normalizing each lower mask by its natural scale `A_p^(2Theta)`, the prime weights are summable and one predecessor carries a fixed positive fraction of the parent **frontier-normalized** energy for every fixed admissible contraction band. The unresolved currency is now mask/occupation inheritance and recursive eligibility, not predecessor count or frontier exponent preservation.

MC-252--MC-256 eliminate nonlocalized fixed-order residue rigidity. MC-257 removes the wild prime-square direction from the moving shell, and MC-258 shows that descent from `R^2` to `R` is exactly offset by the projector's exterior `1/R`, preserving the support exponent. MC-259 compresses the structured smooth-modulus obstruction to at most one blind pretender, while MC-260 shows that the closest support-sensitive sparse variance theorem starts only above the useful modulus range. MC-261 identifies the other exit exactly: full shell generation is equivalent to every reduced class modulo `R` having a `y`-smooth representative with `y=(1+o(1))log R`. Existing all-class smooth/product theorems operate at polynomial generator scale, so the extreme smoothness of `R` does not itself close this logarithmic-generation problem.

Both lines now isolate a source-scale endpoint after the generic bookkeeping has been removed: Farey needs recursively eligible normalized transport, while Möbius needs either shell-specific logarithmic simultaneous generation or signed/support-sensitive cancellation at the actual sub-square-root modulus.

## Can source-specific regularity and source-specific order become quantitative enough to control a Weil crossing?

Prime Lattice and Weil Inertia now separate generic regularity/order structure from what is selected by the actual source equation.

PL-285--PL-292 move from inverse-log ambient transport through graph-domain `log^-2` transport to optimized all-log subpower transport for actual eigenstates. PL-293 supplies a sharp generic endpoint obstruction: the optimal zero-exterior logarithmic-Laplacian boundary law has amplitude `ell^(1/2)`, and any nonvanishing channel of that scale excludes `H^s` for `s>=1/2`. PL-294 independently closes the standard positivity-improving shortcut: the first Beurling--Deny criterion for the full localized Weil form holds exactly only up to `a_BD=(1/2)log rho_pl≈0.1406`, strictly before the first prime-power shift. At `a=0.8` ordinary semigroup positivity can no longer certify ground-state sign. Neither result decides the actual eigenbranch; the remaining gain must be zeta-specific boundary cancellation, a source-specific sign theorem, a different derivative identity or a bypass of the Hopf/regularity route.

WI-264--WI-269 show that finite-lag screening and generic first-crossing substitutes fail to capture Suzuki's source equation; every nonzero localized null mode is prime-translation support-dominating, ordinary positivity preservation fails, but the exact multiplier ground-state form is PSD and yields a nodal compensation tariff. WI-270 sharpens the one-signed branch: every admissible cut must be paid either by prime-power crossing overlap or by bilateral short-range mass. WI-271 makes the source sign-change scale decisive: an internal one-signed gap of width at least `h_0=log rho` lies wholly beyond the positive archimedean range, so firstness forces positive von-Mangoldt cross-gap mass and hence a positive prime-power autocorrelation. Complete screening can survive only with `h_0`-gap-connected short-range support, while the sign-changing branch still requires control of long-range archimedean compensation.

The shared lesson is not that regularity and order are interchangeable. It is that generic operator structure has now reached explicit sharp controls, and the remaining gain must come from how the actual arithmetic source selects its state.

## Does nonsummable Blaschke delay force terminal Nyman entropy, or must the radial proof leave the scalar sibling direction?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate stationary inverse calibration, radial floors, scalar branching and the actual visible future geometry.

NB-082--NB-085 reduce persistent continuation to finite-future prediction, construct the exact source-specific Toeplitz certificate along integer-dilation descendant rays, and show that every such ray has a strictly positive Szegő prediction floor. NB-086 then lower-bounds the aggregate radial charge by terminal visible Gram entropy, and NB-087 extracts a canonical q-adic scalar sibling-energy distortion whose squared mass already pays part of that bill.

NB-088 makes the dyadic scalar channel source-exact. After restoring the off-critical inner factor, the raw terminal innovation has `||J_X \widetilde D_j||^2=1-1/X`; all arithmetic deformation is a nonnegative causal Blaschke-delay energy. Every fixed finite Blaschke divisor is silent at total squared scale. NB-089 extends this to the full infinite product whenever the total horizontal off-critical mass `sum_(Re rho>1/2)(Re rho-1/2)` is finite: terminal delay remains `O(j^-1)` and the aggregate scalar distortion still vanishes.

The next scalar theorem must therefore do more than prove an infinite off-critical divisor. It must force a genuinely nonsummable effective zero mass and then show that this nonsummability survives the parent/child three-delay combination strongly enough to violate the scalar `l^2` budget. Divergent horizontal mass is only a necessary escape from the new obstruction, not a sufficient entropy theorem. Otherwise the radial proof must use sibling contrasts, cross-block correlations, earlier indices or the remaining positive floor charge.

## Which cross-scale selector incidences survive after scalar gap-moment budgets become sharp?

RE-083--RE-088 reconstruct selector geometry, isolate anchored gap costs and remove sufficiently deep selected boundaries under an off-RH exponent gap. The challenged RE-089 statement has been withdrawn. RE-090 removes fixed same-depth higher-layer chambers above `Theta>0.57`, and RE-091 combines sparse depth-`>=3` hosts with Stadlmann's prime-gap second moment to force a first-layer boundary atom on almost every surviving cell once `Theta>469/600`.

RE-092 shows that simply pairing the two anchored gap **magnitudes** of a mixed `1/r` cell does not improve the RE-091 exponent. In the full-width matched allocation, the first-layer gap budget saturates the host/moment frontier while the required host-layer gap tends below the deterministic prime-gap floor. For the residual depth-2 channel the same calculation gives the scalar barrier `173/200`.

Thus in `469/600<Theta<=173/200`, the next Robin theorem must preserve the exact cross-scale source identity discarded by separate moment bounds: which `q~sqrt(X)` produces the depth-2 event, which ordinary prime gap contains its first-layer inverse image, how the two appear as adjacent support boundaries, and whether the rightmost-maximizer orientation selects that pairing. For `Theta>173/200`, the existing depth-2 host/moment estimate already makes the channel negligible. Below `469/600`, the finite mixed-depth reduction remains the weaker carrier.

## Can the local direct flute leakage and the conditional channel satisfy the required ideal counts?

PF-316 removes the residual-source ambiguity at the endpoint: the physical angle is weak trace exactly when `T_H` and `Z_LH` are. PF-317 makes the direct channel local through positivity, nearest-neighbor topology and explicit seam normalization. PF-318 shows that a Hilbert--Schmidt mean-square bound on each local nonconstant-low/high block is endpoint-complete because the low target rank is only logarithmic in the prime label.

PF-319 proves that the existing `O(P_n^-1)` corridor-relative form sandwich does **not** supply that bound after seam normalization without additional structure. The strengthened PF-242 supplies the coefficient-level structure: the exact hypercycle shear is exponentially localized in the Pöschl--Teller Jacobi basis and has cut-uniform `S_2^2=O(P_n^-2)` leakage, stronger than the final logarithmic-rank target before metric conversion. The remaining direct burden is therefore to transport that ideal locality through the corridor-to-seam normalization and Schur/resolvent/finite-pant assembly. Independently control `T_H`, then close the compatible infinite realization, symmetric side and signed target composition. Constant-mode pant blowup remains a required negative control.