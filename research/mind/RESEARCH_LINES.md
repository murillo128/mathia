# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry or programmable completion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity has closed the natural fixed scalar gauge/Schur/Julia orbit through WP-313. WP-314 now tests the canonical nonuniform escape: an empty-side regular Julia node moving to infinity produces, on its natural node scale, exactly the same boundary layer as the unit-density logarithmic control, while fixed spectral scales degenerate to a constant. Making the transform limit and high-energy limit fail to commute is therefore not enough; the moving scale itself must retain a source-specific fluctuation rather than the density-one tangent.

The surviving scalar route must derive a source-native moving scale on which arithmetic fluctuations remain visible while Pick positivity and the Gamma discriminator survive. Otherwise the construction must leave the audited scalar class through operator/matrix-valued boundary structure, generalized resolvents, noninvertible operations, an added archimedean channel or nonseparable finite--archimedean coupling.

## Which source resources survive into the exact destination margin, topology and moving resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) keep conditioning, source supply and destination sensitivity distinct.

Arithmetic Fidelity now has an explicit moving-parameter obstruction. Although every fixed Riesz order `delta>0` crosses the critical summability threshold without moving the zero discriminator, AF-354--AF-355 show that sufficiently fast `delta(X)->0` inherits the unsmoothed endpoint oscillation; under RH every polynomially vanishing order fails the sharp `O(sqrt X)` profile. The surviving slow regime requires genuinely uniform source cancellation, not fixed-parameter constants.

Nyman--Beurling now separates raw conditioning from target-bearing state geometry. NB-136 shows that all `R` natural samples can coexist with a superpolynomially collapsing lower frame bound for only `Theta(log R)` near-confluent modes. NB-137 then makes the conditioning collapse even stronger while the simple packet converges to a confluent state space whose normalized projection of the Nyman target tends to zero. A bad Vandermonde chart can be repaired by confluent coordinates without making the represented subspace useful; the missing source law must force target occupation, not merely better numerical conditioning.

Prime Lattice retains the moving-observer warning that broad global regularity can miss a critical moving sample. Robin Extremal now shows that observability can be strongly anisotropic: vertical diameter may be irrelevant while horizontal spread and packet complexity are decisive. Weil Positivity adds that a nonuniform transform limit can still classicalize to a universal tangent.

The common requirement is a **target-aware coupled-scale modulus**. Any moving smoothing order, packet rank/separation, coordinate chart, horizontal/vertical spread, transform node, representation dimension or observation scale must be priced together with the endpoint quantity actually consumed.

## Which growing source law is genuinely informative rather than sparse, diluted, over-rigid, capacity-saturating or circular?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now shows that the missing coordinate cannot be guessed from the previous counterexample. FD-137 proves physical-scale localization still misses the moving `omega(n)` wall, while exact rank conditioning exposes that particular wall maximally. FD-138 then moves the defect into an orthogonal additive prime-color coordinate and defeats a factor-two shell plus a positive-mass CLT rank window uniformly over all primes. A useful source law must be justified by the admissible source geometry, not merely chosen to kill the last witness.

Möbius Cancellation has a complementary hereditary refinement. MC-307 forces source complexity to survive target-coordinate projections. MC-308 dualizes this to arbitrary source-prime cuts: after quotienting by omitted source columns, the decisive quantity is the one-defect simplex occupancy `h_J/2^(t_J)`, which directly controls a Brun--Titchmarsh radical lower bound. The live object is now the two-sided incidence geometry between endpoint directions and lower-prime representatives, not another aggregate capacity.

The cross-line lesson is precise: breadth, locality and average accuracy are not monotone resources. A condition is informative only if its active source coordinates, residual freedom under matched controls, behavior under endpoint restrictions and source-to-target localization are explicit.

## Which support geometries survive the one-signed Weil first-crossing constraint?

WI-294--WI-297 use the actual nonnegative null-vector geometry to force support measure, global mass, exponential-moment control and an aperture-independent core. WI-298 then eliminates the remaining large-aperture complete-screening escape by symmetric rearrangement: complete screening packs the positivity set into measure at most `log 2`, rearrangement decreases the prime-deleted form, and the mode compresses to the first-prime aperture `r_2=(log 2)/2` with nonpositive Weil energy.

For a first crossing this forces `a_*<=r_2`. The live complete-screening theorem is therefore finite-scale: establish a rigorous `lambda_(r_2)>0`. That one certificate would close the entire nonnegative completely screened first-crossing branch. Sign-changing modes and incomplete screening remain separate.

## What should Prime Circle retain after finite oriented-frame classicalization?

PC-295--PC-301 show that the diffuse square-zero class and centered positive Gram formation erase orientation. PC-302 restores a labelled odd channel but identifies its quadratic content with sine/cosine Fourier covariance. PC-303 classifies every fixed finite oriented-frame tensor: it is joint additive Fourier data, and fixed polynomial invariants are classical polyspectra; for the distinct-prime source these reduce to finite polynomials in ordinary prime exponential sums.

The next carrier must therefore escape **fixed finite character algebra**, not merely increase tensor degree once. Growing tensor order, cross-level/multi-modulus operations before Fourier reduction, or a source-forced order-sensitive geometry remain distinct possibilities, but each needs a quantitative reason it is not simply asymptotically complete repackaging of classical source Fourier data.

## Can the required Robin cancelling geometry occur for actual zeros?

RE-129--RE-136 reduce attained-edge hiding to a quantitative reciprocal-scale escape problem and show that isotropic radius buys suppression only at an exponential rate. RE-137 then resolves the leading geometry anisotropically: Turan--Nazarov gives observability with a cost `exp(-H_C-CN)` depending on scaled horizontal spread and packet complexity, with **no vertical-diameter penalty**. Purely vertical matched packets still hide once enough modes cooperate, so complexity is load-bearing.

The live actual-zero theorem is now to control how many comparable off-critical modes can cooperate at a given horizontal depth, or force horizontal spread large enough that the anisotropic observability tariff already defeats the target atom. Vertical escape alone is no longer the right missing variable for bounded-complexity leading packets. Fixed higher-order transfers must also satisfy their explicit logarithmic budget.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225--VIS-231 fix the subcritical static order and the deterministic, marginal, Markov-center and Markov-variance baselines for the critical collision count. VIS-232 adds a prior model-admission test: the collision statistic is exactly a function of the occupancy histogram, so conditioning on that histogram gives zero variance; preserving the exact transition-count matrix plus endpoints also fixes it.

The critical test must therefore either fit a stochastic dependence law from independent calibration data and let confirmation occupancies fluctuate, or move to an order-sensitive statistic that remains variable inside the stronger conditional class. A null that fixes the tested statistic is not stronger evidence.

## Xi Flow: move from local known-window inversion to global support discovery and zero-sensitive transport

Xi Flow has a uniformly conditioned local nonlinear holomorphic chart for known consecutive packets with linearly many samples. The live problem remains global: locate and recenter unknown windows without an oracle, assemble separated or multiscale packets, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side source control to a zero-sensitive quantity.

## Global discriminator: price what the endpoint actually sees

The newest evidence strengthens existing global boundaries rather than producing a common RH theorem. AF-355 turns a qualitative fixed-parameter warning into a moving-order obstruction; FD-138 shows that exposing one hidden additive coordinate need not prevent a source from rotating into another; MC-308 makes hereditary complexity two-sided across the incidence map; NB-137 makes state-space target occupation more fundamental than raw near-confluent conditioning; RE-137 replaces isotropic vertical escape by horizontal-spread/complexity cost; VIS-232 distinguishes dependence modelling from conditioning away the statistic; WI-298 converts a large-aperture branch into one finite positivity test; WP-314 shows that even a deliberately noncommuting moving transform can converge to a universal control.

For every proposed route ask: **what is the source discriminator; where can it be quotiented or averaged away; which source coordinates can carry a hidden wall; what topology and normalization retain it; which constants remain uniform in the parameter limit actually used; is the relevant object a coordinate matrix or a target-bearing subspace; how does the construction behave under target projections and source cuts; which geometric directions actually enter the observability cost; does stronger locality reconstruct the source; does the null condition away the observable; is a moving limit source-specific or only a universal tangent; and do the hypotheses already encode the desired endpoint?**

A route can fail by under-resolution, rank excess, near-confluence, target-poor state geometry, moving-parameter singularity, coordinate rotation, over-rigidity, hereditary restriction failure, carrier gauge, finite Fourier classicalization, anisotropic noncompactness, packet-complexity growth, conditional-null degeneracy, transform inadmissibility, universal moving-limit classicalization or terminal quotienting. The productive next theorem is the one that closes one of those exact failure modes with source-native information rather than relabelling it.