# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Characterize the complete admissible statistic before choosing the repair

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`.

Arithmetic Fidelity now gives exact deterministic and stochastic formulations of the first gate. Given the observables naturally available to a construction, form their joint quotient; for a finite statistical experiment, form the full reference-relative likelihood-ratio statistic. Ask whether the desired discriminator/model family factors through that retained object. Only then optimize, diagonalize, take determinants, add marks, or prove positivity.

A decisive negative result is a conflicting pair inside one complete admissible fiber/statistic. A decisive positive result is exact recovery or a canonically forced lift. Breaking visible automorphisms is not by itself sufficient unless the declared category has a completeness theorem connecting symmetry to admissible observability.

## Distinguish exact relational fidelity from global rational-prime specificity

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`.

Higher-order or marked relational data can be exactly faithful modulo an intended gauge, but local fidelity can still be shared by non-Riemann sources. Arithmetic Fidelity now makes the source distinction sharper: bare multiplication retains prime type but not rational-prime norms; the exact Euler-product function can recover an unordered norm multiset, while the meromorphic divisor can lose it through zero-free factors.

A decisive mechanism must pass both gates: prove that the local relational observable is complete modulo the intended gauge, then prove that the exact destination retains the independently forced rational-prime norm/completion against matched generalized-prime controls. Provenance alone is not specificity.

## Make matched controls as strong as the claimed quotient, operator class, pairing, and analytic layer

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`.

Controls should match the exact information layer consumed by the proposed invariant. The current frontier includes an all-composite prime-flute clone in the same compact-resolvent class with summable full split-ray mismatch, a Prime-Circle Hardy algebra whose finite essential core is universal while higher relative traces are cyclotomic periods, completed Weil boundary objects whose natural limits/hulls can be explicit-zero repackagings, Yang welding controls that now reach a genuine positive-power modulus region, and positive completions that retain `log p` only in determinant data before later cancellation/absolute-value repair.

A canonical relative object is valuable only when its reference, topology, normalization, and observable class are forced independently of the desired signal. The mathematical goal is to identify the first matched-control depth at which an invariant changes and verify that this depth matches its claimed source of arithmetic information.

## Separate critical-axis generation and RH-equivalent diagnostics from zero selection

**Linked intuitions:** `MI-004-critical-axis-is-not-zero-selection`.

A distinguished axis, a real spectral carrier, correct finite Euler coefficients, or even an RH-equivalent boundedness criterion is weaker than a new zero-selection mechanism. Prime Lattice now supplies the sharp control: the second-scale completed Weil coefficient is an explicit zero-frequency sum whose boundedness on generic fixed smooth probes is RH-equivalent, yet under RH it is generically nonconvergent; its hull and canonical prime coupling only reorganize the already-present zero frequency module.

The missing theorem is an independent selector or estimate derived upstream from rational-prime structure. Repackaging the continued zero divisor into a bounded signal, hull, Koopman system, or joining is diagnostic unless the decisive bound/sign can be proved without importing the zero locations it is meant to explain.

## Identify a canonical topology stronger than matched compact background but weaker than universal obstruction

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`.

Prime Flute shows compact-resolvent class is too coarse and first-resolvent trace class too strong; the full one-dimensional Lambert/gluing defect is now summably controlled, leaving a genuinely two-dimensional global comparison. Prime Lattice shows natural bands collapse through an inverse zero-certificate regime before essential recurrence, while fixed-smooth second scale produces recurrence rather than a limit. Prime Circle and Weil Positivity supply parallel category mismatches.

The live program-level question is whether any branch forces a relative `S_p` (`p>1`), higher-resolvent, heat/wave/scattering, mesoscopic, distributional, twisted determinant, or other middle topology in which a stable arithmetic residual survives the strongest matched controls and comes with an independent selector/sign theorem. A no-go showing that every intrinsic middle category either identifies the controls or only repackages classical/explicit zero data would be equally decisive.