# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry or programmable completion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity has closed the natural fixed scalar gauge/Schur/Julia orbit through WP-313. WP-314 now tests the canonical nonuniform escape: an empty-side regular Julia node moving to infinity produces, on its natural node scale, exactly the same boundary layer as the unit-density logarithmic control, while fixed spectral scales degenerate to a constant. Making the transform limit and high-energy limit fail to commute is therefore not enough; the moving scale itself must retain a source-specific fluctuation rather than the density-one tangent.

The surviving scalar route must derive a source-native moving scale on which arithmetic fluctuations remain visible while Pick positivity and the Gamma discriminator survive. Otherwise the construction must leave the audited scalar class through operator/matrix-valued boundary structure, generalized resolvents, noninvertible operations, an added archimedean channel or nonseparable finite--archimedean coupling.

## Which source resources survive into the exact destination margin, topology and moving resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) keep conditioning, source supply and destination sensitivity distinct.

Arithmetic Fidelity now has an exact topology split at the Riesz endpoint. AF-354--AF-355 show that sufficiently fast `delta(X)->0` inherits the unsmoothed endpoint oscillation; under RH every polynomially vanishing order fails the sharp `O(sqrt X)` profile. AF-356 nevertheless proves `||E_delta-E_0||_(B^2)=O(delta)` and Lipschitz convergence of the limiting laws. The same endpoint is therefore mean-square stable but uniformly ill-conditioned. The surviving slow regime needs a tail/peak/phase-coherence resource in the actual supremum topology, not another second-moment estimate or fixed-parameter constant.

Nyman--Beurling sharpens the corresponding coordinate-versus-target distinction. NB-136 shows that all `R` natural samples can coexist with a superpolynomially collapsing lower frame bound for only `Theta(log R)` near-confluent modes. NB-137 then makes the whole simple-state span converge to a target-poor confluent state space. NB-138 removes the nonquantified-spacing escape: fixed polynomial separation `epsilon_G=G^(-B)` already supports the same vanishing target projection together with `log kappa_2(V_G)` of order `(log G)^2`. A bad chart can be repaired by confluent coordinates without making the represented subspace useful, even at an explicit polynomial confluence scale.

Prime Lattice retains the moving-observer warning that broad global regularity can miss a critical moving sample. Weil Positivity adds that a nonuniform transform limit can still classicalize to a universal tangent.

The common requirement is a **target-aware coupled-scale modulus in the topology actually consumed by the endpoint**. Any moving smoothing order, packet rank/separation, coordinate chart, transform node, representation dimension or observation scale must be priced together with the endpoint quantity actually used.

## Which growing source law is genuinely informative rather than sparse, diluted, over-rigid, capacity-saturating or circular?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now shows that even **exact** localization in the previous hidden coordinate is not generically coercive. FD-137 exposes the `omega(n)` wall by exact rank, but FD-138 rotates the defect into the additive prime-color coordinate `D(n)`, and FD-139 proves that the same source has vanishing mean ancestry uniformly over all primes inside every exact central `omega=m` cell while remaining positive-density non-Möbius. A useful source law must justify a finite source-native coordinate family or another structural restriction that prevents transverse wall rotation without already reconstructing the Möbius source.

Möbius Cancellation gives the complementary source-cut boundary. MC-307 forces complexity to survive target-coordinate projections and MC-308 dualizes this to source-prime cuts through the simplex occupancy `h_J/2^(t_J)`. MC-309 shows that below the Landau--Page source threshold, all but at most one quotient character have tiny bias; for moderate quotient dimension this forces full occupancy absent the exceptional mode and at least half occupancy in general. That is exactly where the MC-308 Brun--Titchmarsh exponent stops being positive. The live incidence theorem must therefore escape low-product moderate-rank cuts, control the exceptional mode, or combine hereditary restrictions in a genuinely stronger way.

The cross-line lesson is more specific than “use more source information.” A condition is informative only relative to the **coordinates or quotient frequencies it actually resolves**. Exact conditioning can miss a transverse additive direction; a low-cost source quotient can be forced to occupy at least half its states. Source breadth, locality and average accuracy are not monotone currencies.

## Which support geometries survive the one-signed Weil first-crossing constraint?

WI-294--WI-297 use the actual nonnegative null-vector geometry to force support measure, global mass, exponential-moment control and an aperture-independent core. WI-298 then eliminates the remaining large-aperture complete-screening escape by symmetric rearrangement: complete screening packs the positivity set into measure at most `log 2`, rearrangement decreases the prime-deleted form, and the mode compresses to the first-prime aperture `r_2=(log 2)/2` with nonpositive Weil energy.

WI-299 now carries the smooth-kernel remainder estimate all the way to that exact aperture, with `0<=delta(s)<0.21621|s|` for `|s|<=log 2` and `||R_a||<=0.353143a^2` for `a<=r_2`. The analytic tail-domain cutoff is therefore no longer the live obstruction. For a first crossing `a_*<=r_2`, and the remaining complete-screening theorem is a genuinely finite sign problem: certify `lambda_(r_2)>0` by an independent low-mode/interval-Schur argument. Further tail sharpening matters only if it changes that finite certificate.

## What should Prime Circle retain after finite oriented-frame classicalization?

PC-295--PC-301 show that the diffuse square-zero class and centered positive Gram formation erase orientation. PC-302 restores a labelled odd channel but identifies its quadratic content with sine/cosine Fourier covariance. PC-303 classifies every fixed finite oriented-frame tensor: it is joint additive Fourier data, and fixed polynomial invariants are classical polyspectra; for the distinct-prime source these reduce to finite polynomials in ordinary prime exponential sums.

The next carrier must therefore escape **fixed finite character algebra**, not merely increase tensor degree once. Growing tensor order, cross-level/multi-modulus operations before Fourier reduction, or a source-forced order-sensitive geometry remain distinct possibilities, but each needs a quantitative reason it is not simply asymptotically complete repackaging of classical source Fourier data.

## Can the required Robin cancelling geometry occur for actual zeros?

RE-129--RE-137 reduce attained-edge hiding to a selected strong packet and show that leading observability costs scaled horizontal spread plus packet cardinality, not vertical diameter. RE-138 then inserts the actual-zeta source law: algebraic strength places the distinguished atom at polynomial ordinate height, and Riemann--von Mangoldt gives

`N_U << (1+W_U)(log U + log(2+W_U))`

for a packet of absolute vertical half-width `W_U`. Consequently bounded `W_U` and `H_U=O(log U)` force a fixed polynomial visibility floor for the selected leading packet. Superalgebraic hiding must send either `H_U/log U` or the **absolute vertical span** to infinity along a subsequence.

The live theorem is no longer an independent packet-cardinality bound. For actual zeros, complexity has been converted into geometry. What remains is to control cancellation from zeros outside the selected packet, or prove that the required horizontal/absolute-vertical escape is incompatible with the source. Fixed finite-order transfers must still satisfy their explicit logarithmic stability budget.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225--VIS-231 fix the subcritical static order and the deterministic, marginal, Markov-center and Markov-variance baselines for the critical collision count. VIS-232 adds a model-admission test: the collision statistic is exactly a function of the occupancy histogram, so conditioning on that histogram gives zero variance; preserving the exact transition-count matrix plus endpoints also fixes it.

VIS-233 sharpens this into an interaction-order boundary. Endpoints plus first-order transition counts determine every translation-invariant additive range-one observable, but they need not determine the lag-two return count `T_2`, which depends on length-three blocks. The exact-control route must therefore predeclare a range-two-or-higher statistic and first prove nonzero conditional variance on the actual critical prime-phase type. Strengthening the null to preserve length-three counts would condition `T_2` away again.

The alternative stochastic route remains to fit a dependence law from independent calibration data and leave confirmation occupancies random. In either route the tested observable must lie outside the sigma-field fixed by the null before any apparent prime residual can count as information.

## Xi Flow: move from local known-window inversion to global support discovery and zero-sensitive transport

Xi Flow has a uniformly conditioned local nonlinear holomorphic chart for known consecutive packets with linearly many samples. The live problem remains global: locate and recenter unknown windows without an oracle, assemble separated or multiscale packets, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side source control to a zero-sensitive quantity.

## Global discriminator: price what the endpoint actually sees

The newest evidence strengthens one common distinction: **retained information is meaningful only relative to the observer topology, coordinate quotient or sigma-field that consumes it**. AF-356 is stable in `B^2` while singular in the critical supremum norm; FD-139 conditions exactly on rank yet leaves a transverse prime-color wall; MC-309 shows that a low-cost source quotient is forced to at least half occupancy by all but one Fourier mode; NB-138 keeps a polynomially separated simple packet target-poor despite catastrophic coordinate conditioning; RE-138 converts formal packet complexity into absolute vertical geometry for actual zeros; VIS-233 shows first-order type fixes all range-one additive statistics but leaves range-two information; WI-299 closes the smooth tail at the first-prime aperture while leaving a finite low-mode sign certificate.

For every proposed route ask: **what is the source discriminator; which coordinates or quotient frequencies see it; where can it be quotiented or averaged away; what topology and normalization retain it; which constants remain uniform in the parameter limit actually used; is the relevant object a coordinate matrix or a target-bearing subspace; which sigma-field does the null preserve and does it already determine the observable; how does source counting constrain formal packet complexity; how does the construction behave under target projections and source cuts; does stronger locality reconstruct the source; is a moving limit source-specific or only a universal tangent; and do the hypotheses already encode the desired endpoint?**

A route can fail by under-resolution, rank excess, near-confluence, target-poor state geometry, topology mismatch, moving-parameter singularity, coordinate rotation, over-rigidity, hereditary restriction failure, exceptional Fourier modes, source-count escape, carrier gauge, finite Fourier classicalization, anisotropic noncompactness, conditional-null degeneracy, transform inadmissibility, universal moving-limit classicalization or terminal quotienting. The productive next theorem is the one that closes one of those exact failure modes with source-native information rather than relabelling it.