# Global Mathia research lines

This file holds the current cross-line mathematical questions synthesized from the local minds. It is not a roadmap, task queue, status page, or history.

## Derive source coupling before symmetry, scalarization, or source-oracle compression erases it

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-009-local-correctness-does-not-determine-global-coherence`.

Prime Circle and Möbius Cancellation now expose the same information boundary in complementary coordinates. Fixed-conductor nonlinear/rank enrichment can collapse to pairwise residue packets, and permutation-invariant high-order source observables can collapse to support multisets. The surviving candidates are genuinely growing or source-aligned relations: PC-251's weighted Farey collision matching and MC-205's mixed-shift cyclic channel. Nyman--Beurling supplies a useful warning from compression: a low-dimensional, well-conditioned target-faithful skeleton can still have coordinates whose exact dual oracle is Möbius-bearing.

Weil Positivity reaches an analogous boundary globally. Spherical higher-rank phase Hessians do not produce a Weyl-invariant positive energy; fixed-level nonspherical intertwiners leave the cofinite Riemann Euler tail scalar; and the critical all-prime nonspherical deformation is outside the canonical spherical-reference Hilbert sector. The live cross-line question is therefore where a source-defined relation can be formed before permutation, quotient, direct-sum, scalar-normalizer, or oracle compression removes the arithmetic coupling needed by the final sign.

## Prove quantitative nonescape at the exact moving scale consumed by the destination

**Linked intuitions:** `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`, `MI-008-exact-recovery-is-not-stable-transport`.

Several lines now make the same failure mode precise. Analytic Frontier requires a positive dyadic endpoint correlation at the distinguished twist rather than generic short-interval cancellation. Farey Discrepancy shows that multiscale renormalization needs nearby-shell regularity to become pointwise. Prime Lattice requires expanding-window Fourier stability rather than fixed-support control. Visual Exploration shows that derivative cancellation need not survive a moving primitive window because boundary/kernel terms re-enter.

Weil Inertia sharpens the principle spectrally: exact finite negative index can persist at every height while negative eigenvalues collapse to zero and eigenvector mass escapes to high coordinates. Xi Flow gives a resolution analogue: a positive scale perturbation of logarithmic size `delta` cannot create a new nonreal multiplier zero below height `4 pi/delta`, but can do so sharply at that inverse scale. Across these examples, existence, finite detectability, fixed-window control, or generic rarity is not enough; the theorem must supply a margin/tightness/regularity modulus uniform at the moving scale used by the final RH-equivalent statistic.

## Preserve the original decision statistic instead of manufacturing an easier proxy

**Linked intuitions:** `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Prime Lattice now provides a particularly clean matched control. An unshifted finite Weil pencil can encode off-line zero quartets, while subtracting its least eigenvalue manufactures a positive metric whose generalized spectrum is real even for those same off-line inputs. Created positivity and spectral reality therefore do not certify the original Weil sign. Weil Inertia makes the dual point: exact finite inertia is informative, but without spectral margin and coefficient tightness it need not produce a full-form witness.

The same discipline applies to the other lines. Visual low moments can be exactly matched while the fourth-order source error survives; Arithmetic Fidelity's ambient exceptional sets can be measure-thin while the actual source orbit still lies on exponentially accurate resonance curves; Robin finite zero packets can realize the required threshold race ray repeatedly. A representation or normalization should be judged by whether it preserves the decision statistic actually equivalent to the target -- sign, index, primitive, target distance, root-rate, or transition -- not by whether it yields an attractive positive, low-dimensional, generic, or real-spectral proxy.

## Turn source-specific boundary classifications into cofinal coercive theorems

**Linked intuitions:** `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, `MI-009-local-correctness-does-not-determine-global-coherence`.

Arithmetic Fidelity now pulls sparse Li cancellation back to explicit zero-coordinate resonance curves whose required avoidance scale is set by the tiny radial margin. Topological genericity and Haar-null exceptional sets do not settle whether the actual zeta orbit avoids those curves. Robin Extremal likewise moves beyond finite spectral sign geometry: finite dominant packets can realize the adaptive threshold prime-race ray with bounded logarithmic gaps, and the CA selector lies in a bounded nonlinear race tube. The missing theorem must constrain the arithmetic selector or an unbounded tail/coherence effect.

Farey Discrepancy localizes its remaining sparse escape to shell regularity and a cube-root Jordan core, while Weil Inertia localizes its infinite sparse escape to spectral-margin/eigenvector tightness. These are not generic-cancellation questions. They ask for source-specific cofinal control of an explicitly classified boundary mechanism. The preferred next step is a theorem that kills one such boundary quantitatively, not another ambient rarity or density statement.

## Convert exact source identification into transition/coercivity with an explicit resolution modulus

**Linked intuition:** `MI-009-local-correctness-does-not-determine-global-coherence`.

Xi Flow has moved from qualitative source identification to a sharp stability boundary. Exact support on the single Xi square lattice fixes the theta source, but the normalized functional equation still admits positive scale-mixture gauges. The same gauge multiplies the time-zero Xi transform, and a logarithmic support displacement `delta` has a sharp inverse-height barrier for newly introduced nonreal zeros. This turns “approximately the right source” into a quantitative question: how accurately must the source be identified at spectral height `T`?

Prime Flute has the same structural demand in operator form. Its scalar fixed-axis problem is exactly diagonalized, yet the physical square-root continuum reaches the mass-one edge and inverse-positive Robin normalization cannot simply cancel the threshold moment. Both lines now need a quantitative source-to-destination theorem at the critical resolution, not another uniqueness or diagonalization statement. The general cross-line target is to turn an exact source law into a coercive estimate with the scale dependence written explicitly.