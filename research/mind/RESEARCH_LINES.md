# Global mathematical questions

## Which source resources reach the destination before observation becomes target-mixed completion, decoding or classical recoding?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), and [MI-020](intuition/MI-020-incommensurability-removes-collisions-but-may-end-in-decoding.md) separate information survival, source recovery, collision removal and zero selection.

Arithmetic Fidelity separates relation type, relational arity and actual sampling geometry. AF-374--AF-376 show that killing the continuous separable gauge does not imply determination: a uniformly discrete actual row set admits compact-support false positives satisfying every sampled total-positivity minor while failing global Pólya-frequency structure. The AF-376 proof repair under adversarial review corrects the Schoenberg reciprocal-transform argument without changing that mathematical claim, so no new intuition is promoted from the review dialogue itself.

Prime Circle now makes the destination warning geometric rather than merely slice-wise. PC-319 proves that the exact canonical `(5,7)` Mordell--Tornheim carrier has zeros in `Re s>1` for every sufficiently small positive coupling. PC-320 uses joint holomorphy and Weierstrass preparation to show that each such decoupled zero extends to a local complex hypersurface finite over an open `(t,u)` bidisk, still inside `Re s>1`. Nonvanishing holomorphic completion factors preserve this divisor and local biholomorphic coordinate changes only relabel it. Thus **source recovery, multivariable nonseparability, ordinary completion and exact canonical coefficients remain orthogonal to zero selection**.

The live question is: what exact source class and retained relation identify the intended discriminator without reconstructing the source; what arity, sampling geometry, conditioning and stability are paid; and what independent theorem controls the sign or divisor after identification? A useful Prime-Circle destination must select something stricter than the raw PC-320 local divisor.

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) track channels left after bulk controls close.

Nyman--Beurling has now removed cardinality and atomicity as escapes for the separated signed exterior sampler. NB-159 closes every fixed finite polynomial-height atomic repair. NB-160 extends the same boundary to arbitrary finite signed Borel height measures: with bounded target conditioning, polynomially high support and sufficiently separated positive/negative Jordan supports, each negative mass element sees `Theta(log G)` nearby zeros while separated positive cross-talk has only a `1/Delta` zero-summed tail. The result forces `Omega(log G)` adverse zeros even for growing-cardinality or continuous samplers. The surviving height geometry must make the two signs interlace/collapse on the `O(1)` Poisson-width scale, worsen another resource, or couple to the arithmetic observable before Jordan separation.

Robin Extremal now has both the sharp exact capacity and the sharp approximate coordinates. RE-162 proves the exact-moment threshold `L_crit=4 tau_r`, with `tau_r=(r! eta_p)^(1/r)`. RE-163 diagonalizes the terminal kernel in Chebyshev modes, with coefficient scale `(L/4)^j/j!`, and shows that approximate stability is governed by kernel-weighted Chebyshev leakage rather than qualitative raw-moment closeness. The RE-161 generalized-source obstruction can have every normalized monomial discrepancy tend to zero while a growing Chebyshev mode stays order one. The live arithmetic question is therefore whether ordinary-prime/CA structure controls those destination-adapted modes or their signed kernel-weighted sum.

Farey Discrepancy still separates rank-anchor tails from coordinate-face anchors. FD-153's repaired Sathe--Selberg proof leaves its geometric/exponential overshoot law unchanged; FD-154 shows bounded ancestry depth is possible only after spending explicit macroscopic source information. The open review state is not independent evidence.

## Which growing source law is informative rather than merely connected, numerous, zero-free, interval-pseudorandom or reconstructive?

Möbius Cancellation has moved from a half-loading minimax ceiling to a Fourier-complexity boundary on the actual source. MC-327 proves that quartic saturation forces logarithmic source mass into columns with occupancy `R/2+o(R)`. MC-328 then shows that every nonnegative endpoint-polynomial weighting of degree `d_R<R/2` has exact loading at least one half on the middle Hamming band; for `d_R=o(R)` the actual source is asymptotically concentrated precisely in that blind region. Fixed even endpoint moments can concentrate on near-quadratic-conductor modes and still recover only the same exponent four.

The surviving Möbius resource must therefore distinguish arithmetic structure inside the central incidence band with Fourier degree comparable to the band, use a genuinely joint/signed conductor inequality, improve the individual conductor horizon, or change representation. Mode cardinality and low-degree scalar reweighting are not source leverage.

Farey Discrepancy continues to show that ancestry resources depend on the geometry and provenance of anchored coordinates rather than edge count alone. Across growing representations the useful theorem must be on the exact population, norm and scale consumed by the destination. Connectivity, zero-free regions, interval cancellation and source provenance are not scalar resources until their loading and transfer laws are proved.

## Which representation-faithful null remains after nuisance variables, weak topology and universal scalar positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) and [MI-002](intuition/MI-002-the-surviving-arithmetic-variable-is-relational.md) separate normalization, relational geometry and source-selective sign.

Visual Exploration now has both exact linear calibration and an exact one-sided nonlinear slowdown certificate inside the log-product quotient. VIS-252--VIS-253 give ordinary and logarithmic coordinate contrasts with exact eigenvalue `rho_m=(m-3)/(m-1)` and integrated time `m-2`. VIS-254 identifies the averaged block conditional variance as the heat-bath Dirichlet form: `delta_f=D_f/Var(f)` implies `tau_int(f)>=2/delta_f-1`. Thus `delta_(bar T)<2/(m-1)` rigorously proves nonlinear integrated-autocorrelation slowdown before long-lag fitting. Failure of that inequality does not prove fast mixing, so separated-start and long-lag gates remain.

Weil Positivity has moved one step beyond the closure of Muraki's five scalar natural products. WP-328 shows that divisibility cannot be repaired merely by adding a face/state when the exact Mangoldt source remains a homomorphic free scalar shadow: bi-free marginal placement and c-free reference placement are impossible because the source is freely indecomposable. The opposite degenerate c-free placement is universally infinitely divisible because it reduces to Boolean convolution. A viable enriched route must therefore encode essential arithmetic in genuinely mixed data not recoverable as an obstructed or universally positive scalar shadow. Operator-valued and genuinely mixed nondegenerate constructions remain open.

Prime Lattice has now separated the first-prime algebra from the missing arithmetic orientation. PL-324 proves that odd parity removes the generic archimedean order obstruction until the first rational-prime event, where order positivity fails. PL-325 then reconstructs the active `log 2` overlap as an exact two-edge flip with spectrum `{-1,0,1}` and shows that this single-shift algebra, its Legendre compression and related finite-scale machinery are already public prior art. The overlap is norm-one for any positive width yet form-small near threshold against the endpoint potential, so raw operator size does not determine the Weil crossing. The live object is the signed state selected by the **full** Weil operator and its source/endpoint coupling across apertures.

The common criterion is whether the destination relation survives honest nuisance propagation and is nonautomatic in the matched-control category. Exact nuisance removal, source fingerprints, reversible access, positive cones, extra scalar states, a classified one-prime block or canonical coefficients do not by themselves make the final sign/divisor source-selective.

## Which noncompact source channels and approximation costs survive a positivity reduction under the correct structural limit?

Weil Inertia has now identified two different things that are **not** independent costs. WI-315 removes the changing-source negative-margin obstruction because the compact-support windows are nested restrictions of one global Weil form. WI-316 then shows that the comparison variational bottom differs from the physical window bottom by at most the cap defect and converges, along every cofinal vanishing-cap diagonal, to the global compact-support Weil floor.

Consequently a comparison lower bound of the form `Dhat_j >= -(1/2+o(1))I` is already RH-equivalent once combined with the source-exact cap identity. A vanishing threshold defect is not a softer intermediate theorem. The genuinely intermediate frontier is a **fixed nonzero defect plus an independent source-valid contraction/bootstrap**, or a restricted mode class carrying information that can later propagate to the full floor. Compact complexity and modulation remain real costs; shrinking source-margin synchronization and shrinking comparison-floor error do not.

Xi Flow provides the complementary inversion lesson. XF-185--XF-186 show that known consecutive packets admit uniformly conditioned linear and local nonlinear holomorphic recovery with only linear measurement complexity, even at remote location. The remaining costs are global support discovery, assembly of separated/multiscale packets, source-valid complex access and the bridge from recovered theta-side information to a zero-sensitive Xi quantity. Local invertibility itself is no longer the bottleneck.

## Global discriminator: identify the exact resource that the matched control cannot fake

The frontier is increasingly a classification of where information is spent. AF-376 separates gauge removal from determination. PC-320 shows that even a canonical multivariable carrier can carry a stable off-critical divisor over an open parameter set. MC-328 shows that once economical source incidence is forced into the central Hamming band, low-degree endpoint reweighting cannot see it. NB-160 shows that increasing sampler cardinality or passing to a continuum cannot erase sign debt while Jordan supports remain Poisson-scale separated. RE-163 identifies destination-adapted Chebyshev leakage as the sharp approximate terminal source currency. VIS-254 turns exact fiber variance into a rigorous nonlinear slowdown lower bound. PL-325 classicalizes the first-prime overlap algebra and leaves full-state arithmetic orientation as the real gate. WI-316 shows that a vanishing comparison-floor defect has already reached the full RH sign problem. WP-328 shows that enriched probability does not help when arithmetic survives only as an obstructed scalar shadow or a universal lift.

For every proposed route ask: what exact object reaches the destination; under which norm, scale, topology, population, source class, relational arity and sampling geometry; which aliases are removed and which fibres remain; what source information was prepaid to obtain coercivity; how demanded modes load onto the actual source; what representation/Fourier or approximation-basis complexity is needed to see that source; where compensating signed mass can land; which scalar shadows survive an enriched construction; which approximation errors are genuinely weaker rather than already target-equivalent; which local block/algebra has merely been classified without orienting the full state; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, moment matching, routine reservoir filling, universal convolution, positive-cone replacement or classical recoding; and does a separate source theorem dominate the destination loss without reconstructing or assuming the endpoint?
