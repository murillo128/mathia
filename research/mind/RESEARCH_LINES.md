# Global mathematical questions

This file records only cross-line questions that currently change how several Mathia research lines should be interpreted. Line-specific frontiers remain in each `research/<line>/mind/`.

## When does a richer representation create a discriminator rather than another absorbed, self-coupled, universalized or forgetful channel?

[MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) remains the common interface audit, sharpened by the latest source-law, exact-reduction, detector-resolution, closure, transport and normalization results.

Arithmetic Fidelity has sharp structured thresholds on both unit-weight and variable-weight positive atomic classes. `K+1` consecutive moments separate `K` from `K+1` positive unit atoms, while latent locations plus latent positive weights move the separating threshold to `2K+1` moments through the shifted Hankel determinant. Exact identifiability still leaves a shrinking remote-atom margin. The source law determines the observation threshold; stability remains a separate destination cost.

Farey Discrepancy has now removed switched conditioning and an avoidable prime-resolution loss without removing the physical capacity problem. FD-225 gives exact full-divisor inversion and FD-226 gives scale-adapted prime reservoirs. FD-227 then shows that the natural attempt to exploit those larger reservoirs smoothly is itself visible: a power-law occupancy `b_d~c d^sigma` is sent by the divisor-response operator to a Jordan-totient main mode of order `m^(sigma+1)`, and every fixed positive-order dyadic root row sees that leading power. The constant cushion is exceptional. The remaining question is whether a genuinely oscillatory switched-null occupancy can retain enough positive cellwise slack, not whether another smooth capacity profile can absorb the Mertens correction.

Möbius Cancellation has closed the apparent scalar second observable left after hyperbola compression. Finite divisor incidence collapses to the lcm semilattice; the Mangoldt/floor-quotient representation self-couples to Mertens; MC-407 Abel-collapses the prime-error increment coordinates to `C(x)=sum mu(a)(psi(x/a)-x/a)`. MC-408 shows that this scalar correlation is finite-prefix invertible and that its square-root `O_epsilon(x^(1/2+epsilon))` bound is equivalent to RH because its Mellin transform has unavoidable poles at zeta zeros. Exact recoverability and a visibly mixed arithmetic formula therefore do not imply a cheaper estimate. Useful leverage has to enter before scalarization through source-specific coefficients or retained phase/congruence/translation/bilinear information.

Nyman--Beurling now separates source specificity from source **resolution**. NB-237 identifies the exact first-order Ford observable as a microlocal signed average of `R^(-1) zeta'/zeta`, while matched critical packets can place `Theta(J)` poles at spacing `Theta(1/R)` with carrier phases locked. NB-238 tests Maynard--Pratt half-isolated-zero theory, a genuinely vertical source-specific detector, and finds its terminal clustering scale `(log T)^3` coarse-grains the entire resonant packet into one clump. The missing theorem is therefore not merely “vertical zero information”; it must retain cluster-internal occupancy, Ford depth and phase at approximately the carrier period `1/R`, or prove an equivalent cancellation estimate for the normalized current.

Prime Circle and Prime Lattice continue to give complementary closure warnings. The canonical scalar one-sided grade-raising family has a universal critical half-plane and whole-plane residual spectrum above threshold. Ambient adjoint/SOT closure of two incommensurate Nyman dilations becomes `B(L^2)`, while even the norm-closed positive algebra preserves only an abstract free commutative monoid/polydisk structure and forgets the numerical `log p` geometry. Arithmetic information must live in source-dependent domains/couplings, numerical energy weights, Mellin structure or the distinguished target, not in a universal scalar completion or unweighted ambient algebra invariant.

Robin Extremal still closes the obvious same-ray multi-center escape: serial products of individually successful centers have exactly multiplicative Wiener cost, so center count without coefficient-phase disagreement is representation-neutral.

Visual Exploration has removed its branchwise Wang occupancy transition entirely at the required upper-bound scale. VIS-329 applies Minkowski directly to the original period `m=max(u,v)` rather than first passing through the center-gap denominator. One short lattice vector controls exceptional edges, affine packet count, two-prime step and resultant simultaneously, yielding `A_M=O((log log M)^2)` uniformly throughout the natural subperiod regime. The earlier `k>L`/`k<=L` split was therefore representational rather than load-bearing for this destination. The live Wang question returns to the fixed-power source: whether a real-axis source statement strictly weaker than the RH-equivalent half-plane condition can survive the full error budget.

Weil Inertia has become a two-stage exact transport audit. WI-363 shows that half-density transport of the singular Cauchy **difference form** produces a nonconstant Jacobian multiplication term `W_A`. WI-364 shows that even before this transport, odd restriction of the regular gamma difference kernel necessarily creates a second `x+y` image channel alongside the singular Carleman image. The normalized one-cell form is therefore not yet derived from the full source operator until difference, image and multiplication channels are transported together. The common-short/inherited-pivot algebra of WI-362 remains valid conditionally on that grant.

Weil Positivity sharpens the selector-topology gate. WP-378 places the exact Mangoldt selector on an unbounded Hardy boundary trace. WP-379 shows that canonical pointing can regularize the geometry and produce a nondegenerate positive half-turn Gram, yet after the critical half-density normalization the entire limiting Gram is universal across odd prime and composite degrees while the arithmetic boundary contrast `log p/sqrt(p)` vanishes. Selector regularity is therefore not enough: the arithmetic channel has to remain coupled to the positive bulk at the destination scale.

Across all lines, enrichment deserves credit only when source information survives the exact downstream quotient, detector coarse-graining, summation identity, parity reduction, renormalization and closure actually consumed; is identifiable on the admitted source class at the required rank/resolution; cannot be cloned by the strongest matched control; and remains quantitatively visible at the destination.

## Where has the exact obstruction moved after the latest boundary results?

**Observation rank -> effective source dimension -> source-law certificate -> margin.** Arithmetic Fidelity shows that unknown weights matter as much as unknown supports: the positive finite-atomic threshold moves from `K+1` unit-weight moments to `2K+1` variable-weight moments. Exact separation still has to be priced by the disappearing-atom margin.

**Switched response -> exact inverse -> scale-adapted prime resolution -> oscillatory positive capacity.** Farey Discrepancy has removed exponential inversion cost and the extra prime-resolution loss. FD-227 further vetoes smooth positive power-law capacity fills. The unresolved object is a nonnegative, capacity-feasible oscillatory switched-null profile with enough lower slack for the physical Mertens correction.

**Incidence quotient -> hyperbola quotient -> Abel collapse -> RH-equivalent scalar correlation.** Möbius Cancellation now knows that the final scalar PNT-error observable is both information-complete and target-hard. A surviving route must retain independently controlled structure before scalar aggregation rather than celebrate another invertible coordinate system.

**Growing moments -> bounded localization -> normalized pole current -> carrier resonance -> detector granularity.** Nyman--Beurling has now tested a concrete source-specific vertical detector and found its published clump observable far coarser than the `1/R` carrier scale. The next theorem must resolve the internal geometry of one resonant Ford clump, not merely count clumps or isolated representatives.

**One-sided scalar criticality -> universal half-plane -> whole-plane residual spectrum.** Prime Circle has closed the full scalar amplitude escape. Any spectral continuation must put arithmetic in the domain, nontriangular coupling or downstream observable itself.

**Sampled prime dilations -> continuous-flow commutant -> universal norm algebra -> weight-sensitive enrichment.** Prime Lattice knows that commutant/bicommutant data forget prime timing, while norm closure preserves discreteness only as a universal free positive monoid. A surviving mechanism must use actual numerical `log p` weights, Mellin structure or target-relative geometry.

**Fragmented branch hierarchy -> direct source-period short vector -> closed occupancy channel.** Visual Exploration no longer has a surviving center-gap occupancy regime at the VIS-316 destination. The next load-bearing question lies in fixed-power source arithmetic or later global bookkeeping, not deeper symbolic decimation.

**Odd fold -> image gamma channel -> half-density form transport -> Jacobian multiplier -> full-form identity.** Weil Inertia must account for both parity-generated image terms and transport-generated multiplication terms before downstream cofinal certificates can be credited.

**Interior positive incidence -> pointed positive bulk -> universal critical Gram with vanishing selector contrast.** Weil Positivity now needs a topology or global assembly that keeps the linear Mangoldt boundary signal quantitatively coupled to the positive finite--archimedean bulk instead of merely making one or the other well behaved.

## Which physical cost survives once representation artifacts are removed?

The current ledger is source-relative. Arithmetic Fidelity prices effective latent dimension, source-law identifiability and shrinking class margin. Farey prices nullspace-compatible positive reservoir slack against the Mertens baseline. Möbius prices genuinely independent signed structure before scalarization. Nyman--Beurling prices **microlocal source resolution inside one resonant zero cluster**, not generic vertical sensitivity. Prime Circle prices source-sensitive operator structure beyond the universal scalar one-sided family. Prime Lattice prices numerical prime weights or target-relative geometry beyond universal ambient algebra type. Robin prices coefficient-phase interference. Visual Exploration has discharged the moving-upper branchwise occupancy tariff and now prices the fixed-power source improvement. Weil Inertia prices exact full-form source transport. Weil Positivity prices destination-scale coupling between the arithmetic selector, mixed-prime incidence and the positive archimedean completion.

The reusable question is: **after the strongest exact source specialization, faithful representation cleanup, identifiability test, detector-resolution audit, localization audit, exact regrouping/inversion, symmetry reduction, closure classification and required globalization step, what quantity must still be paid by the physical source rather than by coordinates, cutoff, normalization, completion or exact reconstruction?**

## Global discriminator

For every proposed route, identify the admissible source class, effective latent dimension, retained provenance, observation rank, carrier/resolution, detector granularity, finite-data fibre, inverse conditioning, localization tariff, exact regroupings or summation-by-parts identities, destination quotient, representation renormalization, parity/image channels, closure topology, retained numerical weights, finite-to-global law, defect scale and destination margin.

Then ask: **what genuinely new source information is acquired; is it identifiable on that class; is it resolved at the scale the destination consumes; does it survive every exact reduction and the actual closure/topology consumed; can a matched control reproduce it; does a coordinate change merely return the original quantity; can an invertible observable still be analytically target-hard; does normalization universalize the positive bulk while shrinking the selector; and what physical cost remains after the most faithful representation is used?** A route that survives this audit has isolated a mathematical bottleneck. A route that fails it has usually rediscovered a rank, conditioning, coarse-graining, self-coupling, resonance, symmetry, normalization, renormalization or forgetful-closure artifact in different notation.