# Global mathematical questions

## Which source resources reach the destination before observation becomes decoding, classical recoding or a target-mixed completion class?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md) separate information survival, source recovery and zero selection.

Arithmetic Fidelity now supplies three complementary boundaries. AF-369 makes pole residue `1` a complete rational-prime discriminator inside the integer-supported Delone class even though the divisor is universal there. AF-370 uses Hamburger rigidity: inside the ordinary-Dirichlet finite-order class, the exact completed Riemann functional equation gives `f=c zeta`, and `a(1)=1` fixes `f=zeta`. AF-371 then separates source richness from target faithfulness: the full reciprocal Taylor sequence may determine the analytic source while the infinite verdict “all Hankel moment matrices are positive” remains insufficient for Laguerre--Pólya membership; Schoenberg's translation total positivity is an exact relational lift for that target. Source class, retained data and the geometry of the final certificate must therefore be priced separately.

Prime Circle supplies the complementary warning. PC-311--PC-313 show that mixed-conductor bilinear fusion classicalizes to one periodic coefficient and that adjoining its finite-Fourier dual gives only universal Hurwitz closure. PC-314 proves that the canonical `(5,7)` even `+1/-1` sectors have linearly many zeros in strips with `Re s>1`; PC-315 closes the orientation loophole by proving the same for the odd `+i/-i` sectors of the unsymmetrized packet. Thus **all four scalar eigensectors in the complete finite-Fourier orbit of this canonical oriented packet have off-critical zeros in the half-plane of absolute convergence**. Exact transform symmetry, even with the packet's parity/chirality retained, is not a zero-selection mechanism.

Prime Lattice has lowered its destination bill rather than increased source recovery. PL-321--PL-323 close the moving Green transfer, residual moment and coercive equation for the actual branch. The remaining gate is the rational-prime-specific sign/first-crossing law carried by the transferred scalar.

The live audit is: **what exact source class and retained relation identify the intended object, what stability/conditioning is paid to reach the destination, what information the positivity/signature quotient actually keeps, and what separate theorem controls the divisor or sign once the object has been identified?** Decoding, universal transform closure, source recovery and a large family of generic positivity inequalities are not substitutes for the final zero-sensitive implication.

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) track the increasingly narrow channels left after bulk controls close.

Nyman--Beurling now closes both bounded two-dimensional geometry and sublogarithmically conditioned fixed-compact sampling. NB-150 shows that mesoscopic separation `1<<L_G=o(G)` fills both signed lobes with `Theta(log G)` background mass. NB-151--NB-152 show that fixed-horizontal zero-mass samplers targeting an `Omega(log G)` packet export matching adverse charge to nearby actual zeros. NB-153 allows any fixed compact complex offset cloud and condition number `C_G=||mu_G||_TV/q_G`: throughout `C_G=o(log G)`, the sampler still forces `Omega(q_G log G)` adverse charge, a fixed fraction within radius `O(C_G)`, carried by at least `Omega(log G/C_G)` zeros. Bounded vertical geometry is therefore harmless, and sublogarithmic ill-conditioning can only trade adverse population for displacement. A surviving compact route must spend at least logarithmic conditioning, while other escapes must change support scale/geometry or retain signed prime/Nyman structure before zero-side separation.

Robin Extremal now separates two terminal source scales. RE-156 gives the ordinary-prime relocation transition

`epsilon_*(p)=sqrt(log U_A(p)/(sqrt(p)log p))`,

whose fourth-root size reflects both terminal influence and shrinking local ordinary-prime supply. RE-157 shows that coarse PNT information alone leaves a larger ambiguity class: PNT-small generalized insertion placement retains order-one leverage down to

`epsilon_PNT(p)=log U_A(p)/(sqrt(p)log p)=epsilon_*(p)^2`.

Above `epsilon_PNT`, an insertion intensity tending to zero can still produce fixed source separation. Thus crossing the fourth-root ordinary-relocation scale is not enough to infer terminal source control from PNT; the positive route needs either the thinner squared scale or genuinely local ordinary-prime/CA rigidity.

Farey Discrepancy also turns its thin endpoint into a quantitative resource law. FD-151 replaces the fixed-anchor parent tax by `h<=P_T p_T/(1-p_T)`, where `p_T` is anchor mass. Bounded parent distortion therefore requires a positive-density anchor; sparse rank anchors pay the same Sathe--Selberg factor that appears in the child-depth phase curve. Growing the anchor does not remove the information bill—it moves it into direct orientation of a larger source fraction.

The common question is no longer whether a broad theorem holds on average, but whether the exact target-bearing configuration is controlled in the norm, population, conditioning, local-supply and terminal/anchor scale the destination consumes.

## Which growing source law is informative rather than merely connected, high-dimensional, zero-free, interval-pseudorandom or reconstructive?

Farey Discrepancy now prices both sides of the positive ancestry route explicitly: parent expansion obeys the anchor-mass/distortion trade `P_T p_T/(1-p_T)`, while depth `alpha log log T` prices child distortion through `sqrt(log log T)(log T)^(I(alpha))`. A source-valid mechanism must explain where those resources come from without silently encoding Möbius orientation.

Möbius Cancellation provides a useful closure in the correct observation category. MC-321 applies a prime-supported Halász--Montgomery family estimate directly to the simultaneously biased endpoint characters. MC-322 strengthens this to the energy law

`sum beta_chi^2 <<_delta 1 + m y^(-delta/24) log y`

for one common modulus `q<=y^(3-delta)`. The natural endpoint is therefore constrained not merely by a growing family count but by an `ell^2` shell-bias budget, and the source theorem reaches every fixed power below the cubic modulus boundary.

The next useful theorem in any growing representation must survive its exact algebra and matched controls and be stated on the same population, norm and scale as the destination. Many observations, connectedness, zero-free regions, small interval sums and source provenance are not scalar resources until that transfer is proved.

## Which representation-faithful null remains after nuisance variables, weak topology and universal finite scalar positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) continues to separate observable normalization from the induced source law.

Visual Exploration has now closed the structural nuisance quotient *and* supplied an explicit exact local sampler for it inside the symmetric-Dirichlet model. VIS-247 gives the `alpha`-free conditional coarea law `Z(lambda)^(-1)J(x)^(-1)d sigma_lambda` and a nondegeneracy witness for the tent statistic. VIS-248 reduces the `m=3` quotient to the exact one-dimensional law `f(q|p) proportional Delta_p(q)^(-1/2)`. VIS-249 proves that every three-coordinate conditional in the high-dimensional quotient is exactly this law, so fixed random-scan triple heat-bath updates are reversible for the target, and their regular tangent directions span the whole conditioned tangent space. The remaining honest costs are global irreducibility/mixing, numerical realization and then the frozen prime-residual test; full local rank is not a substitute for those gates.

Weil Positivity remains the finite-scalar universality warning. WP-319--WP-323 show that source-specific values and arbitrary fixed finite confluent Pick jets may survive while their scalar positivity is still a universal resolvent Gram. AF-371 gives a complementary benchmark: an infinite hierarchy of ordinary Hankel positivity can also remain target-mixed, whereas ordered translation total positivity is exact for Laguerre--Pólya membership. The useful discriminator is therefore not “more positivity” but a source-compatible relation whose sign is nonautomatic and target-faithful.

The common criterion is whether the destination relation survives honest nuisance propagation and is nonautomatic in the matched-control category. Exact removal of nuisance and even exact reversible access to the quotient do not by themselves make the final statistic source-selective.

## Which noncompact source channels and adaptive representation costs must survive a positivity reduction?

Weil Inertia has moved from a frozen essential-spectrum obstruction to a representation-stable finite-sector problem. WI-309 shows that endpoint-singular adaptive localizers can keep a uniform clipped-tail lower floor. WI-310 then proves that the **uncapped** retained tail grows without bound as the reserve rises, forcing the complementary comparison to develop arbitrarily deep discrete negative modes even independently of RH. That negative depth is a coordinate artifact, not a source obstruction.

Capping the retained excess at `1/2` preserves the exact source split and the first-crossing witness while giving the two-sided uniform bound

`1/(64 pi e) I <= That <= (1/2) I`.

The live theorem is now uniform control of the finite negative sector of this bounded retained-tail comparison. Only after the positive summand is controlled from both below and above can negative spectrum in the complement be interpreted as a meaningful arithmetic/compact-sector difficulty.

A construction must show that the exact bounded source budget dominates the finite sector uniformly enough for the aperture limit and then still pass the signed-density quotient of WI-241. An impossibility theorem must survive admissible bounded-tail reserve/localizer choices rather than exploit an overcharged decomposition.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is more specific, not closer by rhetoric. AF-371 distinguishes target-faithful relational positivity from an infinite but target-mixed PSD hierarchy. FD-151 turns anchor growth into an explicit coefficient-mass/parent-distortion bill. MC-322 upgrades family-cardinality obstruction to prime-shell bias energy below the cubic modulus scale. NB-153 prices compact signed-zero isolation by the condition-number/radius/population trade `C_G : O(C_G) : Omega(log G/C_G)`. RE-157 separates the fourth-root ordinary-supply transition from the smaller squared coarse-PNT ambiguity scale. PC-315 closes the complete scalar finite-Fourier orbit of the canonical oriented mixed packet as a zero-selection route. VIS-249 converts the exact nuisance quotient into an explicit reversible three-coordinate kernel with full local tangent span while leaving global calibration as the honest remaining cost. WI-310 removes an artificial unbounded-negative-spectrum coordinate and leaves the bounded-tail finite sector as the honest operator gate.

For every proposed route ask: **what exact object reaches the destination; under which norm, scale, quantifier, topology, observation population and admissible source class; which universal/nuisance component has been removed and what debt did that removal create; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, universal transform closure or generic positivity; and does a separate source theorem dominate the destination loss without reconstructing or assuming the endpoint?** A new representation is useful only when one of those answers changes mathematically.
