# Global mathematical questions

## Which source resources survive the exact destination quotient, resolution and quantifier?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) separate source supply, stable transport, normalization, destination sensitivity and worst-case geometry.

Arithmetic Fidelity now resolves the abstract, source-native and critical target-relative Riesz recovery laws. AF-362 prices a declared target by the exponential Picard moment of its scaled spectral-energy distribution rather than hard bandwidth alone. AF-363 gives the half-order Sobolev transition; AF-364 resolves `beta=1/2`. On growing scaled bands the critical normalized cost is `Theta(e^v/sqrt(v))`, while the absolute bill additionally pays `e^u`. The square-root discount comes from logarithmically spread target energy and survives matched counting-density controls. The missing theorem is no longer another Riesz asymptotic but identification of an independently specified **arithmetic** target whose Picard profile and source specificity both survive.

Prime Circle provides the complementary resolution boundary. Exact degree-one cyclotomic data already identify the complete finite source, yet PC-307 shows that the true prime low/polylogarithmic packet and its disjoint one-step translate are separated by only `O((1+log(2+K))/(q log q))`. PC-308 extends instability to every ordinary `K=o(q)` packet; linear stability begins through classical parity/fixed-denominator major arcs. PC-309 removes the Fourier-coordinate loophole: the same disjoint sources are only `O(1/q)` apart in every physical Wasserstein metric, so every fixed-margin uniformly transport-stable single-level observable must spend `Omega(q)` conditioning. The complete normalized logarithmic-potential field also collapses throughout every boundary layer wider than the polygon mesh `1/q`. Stable geometry therefore appears only when one pays mesh-scale sensitivity or leaves the single-level transport-stable category.

Prime Lattice gives the physical-side warning. PL-316/317 isolate the generic Sobolev margin needed by a moving Green observer, while PL-318 shows that even bounded zero-order forcing under the natural logarithmic endpoint operator can coexist with the moving-diagonal obstruction. The missing theorem must use structural state--source coupling, not merely a small or bounded forcing norm.

Nyman--Beurling supplies a source-specific positive example. NB-144 uses the actual Euler-product half-plane to impose a local Poisson-mass budget on zero packets. It separates some NB-143 xi-type matched controls from zeta and forces logarithmic confluence to become sublogarithmic as the packet approaches the one-line, while leaving a fixed-interior logarithmic regime unresolved. The persisted endpoint repair in NB-141 changes the proof of the rarity bound, not this frontier.

Robin Extremal prices a local observer and one exterior component in the same reciprocal-radius variable. RE-145 gives a local visibility floor `exp(-O(R))`; RE-146 adds an actual-zeta rightward-shell budget after scale-maximal anchoring. The unresolved comparison remains synchronization of this shell estimate with the local witness plus budgets for the near-left and vertically remote near-horizontal pieces.

The common requirement is a **coupled-scale modulus in the topology, quotient, absolute error floor, declared target, local source law and worst-case quantifier actually consumed by the endpoint**. Generic regularity, exact algebraic recovery, smooth geometric representation or analytic realizability is useful only when it controls that exact destination currency.

## Which growing source law is informative rather than unanchored, under-exposed, reconstructive, shareable or generically realizable?

Farey Discrepancy now exposes three ordered coefficient-side barriers. FD-145 comes first: relative ancestry equations on a visible component disconnected from the finite Möbius anchor have an exact `Z_2` orientation gauge. FD-146 then restores the full connected ancestry graph and every local edge, yet uniformly normalized finite-`L^q` edge energy can still vanish across a fixed-rank wall while coefficient error stays macroscopic. **Connectivity must therefore be followed by quantitative anchor exposure in the actual observation norm.** Only after both gates survive does FD-144 become the information-budget lower bound: `o(r_T)` arbitrary source-independent normalized ancestry observations can still be fooled by one common source. The opposite high-information boundary remains FD-142, where one exact additive coordinate can reconstruct a squarefree prefix.

Möbius Cancellation now closes its generic matched-control frontier. MC-314 converts the natural equal-share source scale into a constant generalized-weight defect at logarithmic rank. MC-315--MC-316 identify projective occupancy, systematic submatrix rank, erasure ambiguity and primal/dual generalized weights as equivalent generic presentations. MC-317 proves that full binary `[2R,R]` codes with the required `d_(t_R)>=R-2` endpoint actually exist for all sufficiently large `R`. Generic binary coding/projective geometry therefore cannot supply the missing obstruction; the next theorem must constrain **arithmetic realizability** of the reciprocal-prime/source-incidence matrices or introduce a new invariant not already realized by those matched controls.

Prime Circle sharpens the stability side of the same accounting. Exact low-dimensional algebraic identification coexists with vanishing translated-source distance throughout every sublinear ordinary Fourier bandwidth and every uniformly transport-stable single-level geometry with `o(q)` modulus; mesh-scale/linear sensitivity then exposes classical/discrete source structure. Across these lines, “number of coordinates” is not a meaningful scalar resource. One must price anchor connectivity, normalized cut exposure, effective source-state resolution, topology, precision, stable separation and the exact ambient realizability frontier of the invariant being used.

## Which local zero laws survive after global density, functional equation and transfer arguments are exhausted?

Nyman--Beurling and Robin Extremal continue to expose the weakness of broad population envelopes, but both now have quantitative local geometry. In Nyman, xi-type entire realizability and global zero laws still permit target-poor logarithmic confluence, yet the actual zeta Euler product supplies a positive local Poisson budget that forbids part of that geometry and makes near-one packets sublogarithmic. What remains is the fixed-interior regime where the budget still permits `Theta(log G)` occupancy.

Robin shows that population control becomes useful only after target anchoring converts the exceptional geometry into a summable source currency. RE-145 gives the local `exp(-O(R))` visibility scale. RE-146 chooses the actual scale-maximal Robin atom, forcing sufficiently rightward zeros to pay exponentially in coefficient/ordinate; only then can Ingham density sum the shell to `exp(-cR)` on an earlier subwindow. The near-left layer and vertically remote near-horizontal zeros remain unresolved.

The shared lesson is more precise than “generic upper population control is weak”: **a source law becomes coercive only after the exceptional geometry is represented in a currency that the source theorem actually controls**. In Nyman the Euler product directly charges local occupancy. In Robin scale maximality first turns horizontal displacement into a summable fractional coefficient moment. Neither mechanism transfers automatically to the other line.

## Which arithmetic quantity remains after universal source mass is quotiented away?

Weil Inertia separates total unscreened mass from the signed quantity seen after the explicit formula. WI-301 proves that complete one-signed dyadic screening forces a uniform archimedean reserve. WI-241 then shows, parity-free, that the pole cancels the density-one prime main term exactly; the residual source variable is the smoothed signed discrepancy of `d(psi-x)`, together with a bounded nonnegative density-one autocorrelation term. Thus “some prime-power mass survives” is not yet coercivity: the live bridge must show that the forced source contribution survives the density-one quotient with the correct sign.

WI-302 records a source-exact compact certificate claim through aperture `17/16`, but it remains author-certified computational evidence pending independent replay and analytic audit. WI-303 resolves the structural clue without validating that certificate. The displayed rank-two tail term is a noncanonical finite-rank minorant of a stronger positive tail operator; rank zero, one, two and higher extractions are structurally available. A separate compact-repair obstruction shows what changes past `log 8 / 2`: a tail-dropped architecture cannot be rescued by finite-rank/compact corrections, whereas Liu retains a **noncompact source-exact positive tail supplier** before extracting a finite certificate. The live questions are independent replay and how much noncompact tail information must survive, not rank-two minimality.

## What constitutes a representation-faithful null after nuisance variables are removed?

Visual Exploration now separates four calibration layers. VIS-237 shows that averaging same-cell collisions over grid origin is exactly a triangular pair-gap functional: an exact quotient of nuisance phase, not a stochastic null. VIS-238 removes artificial Poisson count variance by working at the observed fixed count. VIS-239 shows that an independently specified continuous one-point intensity can be removed exactly by cumulative-intensity coordinates, while same-sample empirical-CDF flattening destroys the spacing geometry.

VIS-240 supplies the next dependence layer without undoing those controls. Symmetric Dirichlet cyclic gaps plus a uniform rotation preserve fixed count, exact uniform one-point intensity and monotone ordered support, while `alpha` changes gap dependence; `alpha=1` is exactly the iid-uniform circular-spacing law. The tent statistic has an explicit finite-sample mean under this family. Hence the iid baseline is one member of a broader representation-matched dependence envelope, not the terminal null.

The next confirmation theorem is to obtain variance/covariance or full-law control under a **frozen independently chosen** dependence parameter and then ask whether the prime geometry has a residual outside that envelope. Fitting the null on the same confirmation statistic without propagating uncertainty is another form of conditioning away the signal.

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) records the broader exact lesson shared with Weil Positivity: normalization can simplify the observable to a universal class while moving source specificity into the induced probability/dependence law. The induced law, not the universal normalized sign or shape, then carries the remaining proof obligation.

## Which scalar or local reductions remain structurally capable of carrying arithmetic specificity?

Weil Positivity has closed the pure-measure scalar Julia positivity mechanism sharply. WP-314--WP-317 show that the empty-side moving tangent is universal, atom-mass scaling can lose compactness under prime clustering, isolated poles scalarize, and every fixed finite multipole reduces through classical interlacing degree drops. WP-318 extends this to arbitrary finite or infinite positive measures: after derivative biasing at a regular node, the normalized response is exactly `tau-K_nu`, the Boolean self-energy profile of a probability law; its Herglotz sign is universal, and every gap-supported probability law is realized by a matched positive control.

The scalar source question is therefore whether the **specific derivative-biased Mangoldt probability laws** satisfy a source-forced compactness, rigidity or limiting property absent from matched controls, and whether that property couples canonically to the Gamma/global counterterms. Otherwise the construction must leave the audited pure-measure scalar category through an affine mass-at-infinity channel, matrix/operator-valued structure, generalized resolvents or nonseparable finite--archimedean coupling.

Xi Flow remains a different global-local problem: its local packet inversion is well conditioned once the packet is known, but support discovery, multiscale assembly and transport from theta-side control to a zero-sensitive destination remain the unresolved gates.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current corpus repeatedly separates nearby notions that broad prose tends to collapse: absolute attenuation from target-relative Picard cost and critical target-energy spread (AF-362--AF-364); ancestry consistency from component anchoring, quantitative cut exposure and then observation count (FD-145--FD-146, FD-144); first-order source scale from a generic constant-defect coding endpoint now known realizable (MC-314--MC-317); exact finite source identification from sublinear Fourier/transport instability and then mesh-scale classical/discrete resolution (PC-306--PC-309); xi-type analytic realizability from the local Euler-product Poisson budget (NB-143--NB-144); generic critical/bounded-forcing control from moving-sample anti-concentration (PL-316--PL-318); local reciprocal visibility from a source-coupled rightward-shell budget and still-uncontrolled exterior pieces (RE-145--RE-146); nuisance quotienting from a fixed-count/intensity-matched dependence law (VIS-237--VIS-240); total arithmetic reserve from signed density-subtracted discrepancy and compact finite-rank extraction from noncompact tail retention (WI-241, WI-303); and scalar Julia positivity from source-specific restrictions on an induced derivative-biased probability law (WP-318).

For every proposed route ask: **what exact quantity reaches the destination; at what scale and precision; what target or local observer consumes it; what universal component or gauge is quotiented out; what normalized exposure remains; what induced law remains; what exceptional configuration still survives; can a matched non-arithmetic object realize the same invariant or law at the actual endpoint; and which source-native theorem excludes that matched control?** The productive next theorem is the one that closes one of those explicit gaps without reconstructing the answer, double-counting an equivalent constraint, or importing the endpoint in disguise.