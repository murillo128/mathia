# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry or programmable completion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity has now closed the natural scalar gauge orbit more sharply. WP-307 keeps the pole-normalized Mangoldt curvature positive under every density-normalized orientation-preserving affine spectral recentering. WP-308 closes constant scalar Herglotz projective recodings. WP-309 then proves that allowing the scalar automorphism itself to depend holomorphically on the spectral parameter adds **no** freedom: every jointly holomorphic family of half-plane automorphisms is constant by Schwarz--Pick rigidity.

The remaining positive route must therefore introduce genuinely new finite--archimedean geometry before scalar comparison: a noninvertible Herglotz operation with independent meaning, source-moment Schur/Nevanlinna transform, compression/generalized resolvent, operator/matrix-valued boundary sector, determinant/scattering object, added archimedean channel, or nonseparable finite--archimedean coupling. Spectral-variable scalar coordinate freedom is no longer a live repair.

## Which source resources survive into the exact destination margin, topology and moving resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) keep conditioning, source supply and destination sensitivity distinct.

Analytic Frontier still requires a joint arithmetic-placement law relative to its distinguished logarithmic carrier; matched one-point statistics are exhausted.

Arithmetic Fidelity now separates full-source fidelity from discriminator fidelity inside one Riesz representation. AF-349 proves that every fixed-order full Riesz profile is injective and exactly stable in a source-matched derivative-variation metric: finite-horizon variation equals coefficient `ell^1` distance up to `r!`. AF-348 nevertheless gives sources only `O(1)` apart in raw profile amplitude after enough smoothing. AF-350 calibrates that weak quotient: `R_r[a-b](X)=O(X^theta)` forces `A-B` to be holomorphic on `Re s>theta`, so every Dirichlet principal part strictly to the right of the error line survives even though coefficient/support information may not. For `Lambda-1`, the same statement turns a Riesz error exponent into a zero-free half-plane. The perfect-power controls pass because their transform difference from unit mass is entire. A terminal topology can therefore be useless for source recovery while remaining exactly sufficient for a narrower analytic discriminator.

Nyman--Beurling has now separated finite-prefix information from formal Burnol mass even more sharply. NB-129 makes natural-prefix phase information target-visible only at a reciprocal-height target-angle scale. NB-130 shows that rank-`R` formal zero powers can interpolate arbitrary data on the first `R` integers. NB-131 then vertically translates one invertible formal support shape: the prefix evaluation matrix changes only by a unitary row modulation, so the **same exact finite prefix** is reinterpolated at every height with no extra height-dependent conditioning cost while the formal finite-packet Burnol defect tends to zero. Along phase-return heights, the coefficients and any fixed collection of periodic recurrence descriptors return as well. The surviving discriminator must therefore be actual zeta-zero geometry, effective rank relative to visible depth, or conditioning uniform in the growing cutoff—not exact fixed-prefix data plus height-blind finite descriptors.

Prime Lattice now shows why even the right scalar endpoint moment is not enough without tail topology. PL-313 identifies the rank-one weighted corner functional. PL-314 proves that weighted `L^1` mismatch alone does not pass through the finite-`s` Green transfer: a moving diagonal atom samples `Q=r/s` and can detect sparse bumps of negligible total mass. The condition `Qm(Q)->0` is sufficient, so the actual completed-Weil branch must supply a quantitative remainder or anti-concentration law before the scalar matching theorem is valid.

Prime Flute's hard-cutoff angle remains noncompact, so progress there still requires a new split or observable.

These lines now support one stronger cross-line statement: **terminal stability is topology-, scale-, rank- and discriminator-relative**. AF-349 exhibits exact recovery in a strong derivative norm while AF-350 shows that a much weaker amplitude quotient still retains half-plane singularity data; PL-314 exhibits failure of weak integrated control against a moving atom; NB-131 exhibits exact finite-prefix agreement with vanishing target mass when formal rank is high enough and absolute height is only a unitary modulation. A usable source resource need not preserve the whole source, but it must preserve the exact feature, norm, effective rank and moving resolution that the final operation consumes.

## Which growing source law is genuinely informative rather than sparse, diluted, under-resolved or circular?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now exposes three distinct dilution/quantifier failures. FD-128 shows that even infinite exact Möbius ancestry can remain cheap when the active prime set is sparse. FD-129 shows that complete growing prime-prefix ancestry can become circular when paired with a stage-uniform critical envelope. FD-130 shows that full prime-direction coverage with small **global mean** ancestry defect is noncoercive because a fixed-rank cut disappears in the total edge volume. FD-131 closes the immediate rankwise repair: even the supremum of **rankwise mean** defects tends to zero for a fixed finite-prime subcube gauge. FD-132 closes the next fixed-coordinate repair: one source can have vanishing ancestry defect for every fixed prime, uniformly on every fixed finite prime set and under every fixed summable directional weighting, while differing from Möbius on positive density. The defect rides a moving central-rank boundary, so a useful approximate ancestry law must be genuinely uniform over a scale-growing active prime family, use a scale-dependent/non-summable local norm, grow its exact anchor, or bypass coefficient recovery.

Möbius Cancellation now has pointwise, generic collective and quadratic collective arithmetic capacities. MC-300 prices the Lambert-W ceiling of the current Page/Deuring--Heilbronn channel. MC-301 applies the multiplicative large sieve to the whole low-source family and reaches conductor `sqrt(y)`. MC-302 exploits that the selected source characters are quadratic and moves the natural collective horizon to conductor `y`, with capacity `y^{o(1)} log y` there. But the reciprocal endpoint-partition extremizer still has only about `log y/R` energy at the blind-support rank `R~log_2 log y`. Even an ideal loss-free quadratic theorem of the same `(C+y)` shape leaves an `O(log y)` capacity envelope, so the factor-`R` mismatch survives the power-scale extension in conductor. Progress now means a thinner selected-family capacity, more forced endpoint energy/rank, or an arithmetic coupling among modes—not merely better constants or removal of the quadratic sieve's subpower loss.

The cross-line lesson is stronger than “use stronger norms.” FD-132 and MC-302 show that **active-scale uniformity and family capacity are separate from nominal breadth**: every fixed prime coordinate can converge while the defect migrates outward, and a collective theorem can reach linear conductor while still having enough total budget to hide the full endpoint obstruction. The normalization/quantifier structure of the hypothesis is part of the theorem.

## Which support geometries survive the one-signed Weil first-crossing constraint?

WI-294 changes the large two-island frontier. Complete screening of the actual nonnegative null mode makes the pole term strictly positive, so zero archimedean energy requires negative gamma energy. Fourier uncertainty then imposes an absolute positive lower bound on support measure. The PNT forces a completely screened symmetric two-island width to vanish at large aperture, contradicting that floor.

Thus the large one-signed symmetric two-island branch is closed without a Cramér-scale prime-gap theorem. The live complete one-signed problem is whether a more complicated support topology can retain fixed positive total measure while avoiding all active prime-power translations and satisfying the null equation. Sign-changing modes, incomplete screening and bounded aperture remain distinct.

## What should Prime Circle retain after centered Gram formation and independent averaging collapse?

PC-295--PC-298 identify the universal diffuse square-zero class and its defect scale `r_q`. PC-299 identifies the first positive quadratic defect below that scale as a Fejer-smoothed source autocorrelation, so source Fourier phase is lost in the positive power quotient.

PC-300 closes the most direct independently sampled noncommutative repair: independently sampled source-coupled cyclic profiles factor after expectation through powers of one real symmetric reflection-even Hankel matrix. PC-301 identifies an earlier quotient that does not depend on independence. For the centered window, each same-source cross-Gram carrier satisfies `G_(-s)=G_s` pointwise, so **every** cyclic word built only from those carriers is invariant under independent reflection of every source coordinate for arbitrary correlated or reused-source laws. Correlations among the unoriented classes can survive, but coordinatewise reflection-odd source information is already gone before averaging.

The surviving question is therefore not merely which dependence structure avoids factorization. To recover oriented source information, the construction must retain oriented frames before same-source Gram contraction, justify an asymmetric source window/observable, use a cross-level carrier not determined by `G_s`, or prove that the RH-relevant relation genuinely lives on the coordinatewise sign quotient. Higher loop length and arbitrary source coupling inside the same centered Gram carrier are no longer distinct repairs.

## Can the required Robin dominant cancelling doublets persist?

RE-129 merges spacing, comparable magnitude and cancelling phase into one inverse-window companion for a hidden dominant algebraically strong atom. RE-130 removes the diffuse-cloud branch as an independent algebraic escape and transfers the **off-critical zero-density exponent** into the atom scale. If `N_(sigma_0)(T)<<T^kappa(log T)^q`, then the absolute packet mass satisfies

`A_K(U) << M_K(U)^(1-kappa/2) (log(e/M_K(U)))^q`.

RE-131 then constrains hiding on a fixed relative window: a distinct companion must lie at polylogarithmic-reciprocal ordinate distance, carry at least a `1/log U` fraction of the dominant amplitude, have genuinely cancelling target-demodulated phase, and be horizontally locked within `O(log log U/U)`.

RE-132 now separates global counting from local pair exclusion decisively. A formal simple functional-equation-symmetric spectrum with only `O(log log T)` off-critical points can contain globally dominant equal-amplitude `pi/U` doublets whose full packet hides uniformly on every sublinear observation window. Thus arbitrarily strong one-level zero-density upper bounds can strengthen extraction of a dominant atom but cannot by themselves rule out the reciprocal-scale cancelling companion required after extraction. The control stops hiding on a fixed relative window, so it does not defeat RE-131; the live attained-edge problem is now local actual-zero geometry or an arithmetic zero-detection relation absent from that formal spectrum. The nonattained edge and `Theta=1` remain separate.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225 supplies the coherent-block lower bound and VIS-226 matches it with the absolute tapered envelope. Throughout `H log y/y ->0`,

`I_abs(B) asy M_abs(A) asy U_abs^R(B) asy y/(H log y)`,

and the Haar-normalized scale is `y/(sqrt(H) log y)` up to constants. The static bounded-versus-divergent question is therefore closed at the full coarse order, not just from below.

VIS-227 now calibrates the first critical occupancy statistic at `H=lambda y/log y`. If `m` primes occupy `J` coherence cells, the exact balanced floor is `S_min=J binom(q,2)+rq` for `m=qJ+r`, while `m/J -> eta_v/(8lambda)`. In particular, for `lambda<eta_v/8` a positive linear same-cell collision count is forced before any prime-specific clustering enters. The critical discriminator must therefore use excess above this floor or a matched control with the same `(m,J)`, not raw collision mass.

The live questions are the critical/supercritical objective scale after universal occupancy effects are removed, sharp constants and optimizer geometry, faithful matched controls that distinguish arithmetic organization within the known scale, and dynamical access time of the physical prime-log orbit.

## Global discriminator: price what the endpoint actually sees, including dependence, quantifiers and capacity

The newest evidence strengthens [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md). AF-350 shows that weak source recovery can coexist with exact singularity fidelity above an error line. FD-132 shows that every fixed prime coordinate can look asymptotically correct while a moving active-scale defect remains macroscopic. MC-302 reaches linear conductor collectively yet leaves the same explicit factor-`R` capacity gap at the blind-support frontier. NB-131 shows that exact finite-prefix data can be held fixed while formal Burnol mass vanishes under a unitary height modulation. PC-301 shows that arbitrary source dependence cannot restore orientation erased by the centered same-source Gram carrier. PL-314 shows that weak tail integrability misses a moving atomic observer. RE-132 shows that extreme one-level sparsity does not exclude the local reciprocal doublets needed for sublinear-window hiding. VIS-227 shows that raw critical collisions contain an exact universal occupancy floor. WP-309 proves that holomorphic scalar automorphism freedom is only constant gauge.

The common accounting rule is now more precise. For every proposed route ask: **what is the source discriminator; at which stage is it quotiented; in which coordinates/ranks/directions does it live; what topology and normalization retain it; which quantifiers are uniform at the active scale; which dependence survives the carrier before averaging; what resolution or moving scale does the endpoint sample; how large is the admissible family capacity relative to the obstruction; and do the hypotheses already reconstruct the desired endpoint?** A route can fail by under-resolution, rank excess, unstable or topology-mismatched inversion, inter-rank or intra-rank dilution, pointwise-versus-uniform quantifier mismatch, family-capacity slack, pre-averaging carrier quotient, factorization under independence, moving-scale concentration, global-count/local-geometry mismatch, universal combinatorial baselines, terminal quotienting, universality, gauge rigidity, certificate exhaustion or circularity.

If a transfer between lines is only plausible rather than established, keep it as a clue rather than promote it to global mathematical knowledge.
