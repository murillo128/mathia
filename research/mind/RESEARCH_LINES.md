# Global mathematical questions

## Which source identity fixes a nonprogrammable mixed-prime finite part across regulators?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) shows that proper marginals, exact diagonal coefficients, positivity, closability, and even a sharp global signature do not determine a unique arithmetic coupling/scale. The Weil-positivity Fock branch successively closes finite-dimensional coherent collapse, programmable locally finite incidence, and normalized complete-Gibbs incidence as sufficient selectors.

WP-264 exposes a nontrivial pre-normalized finite part with one positive direction, but an unfixed counterterm family. WP-265 makes the defect stronger: two standard source-Hamiltonian regulators with **leading-only subtraction** land in different inertia classes. Sharp cutoff yields `R_(B_1)` with infinite positive index, while Gibbs/Abel damping yields `R_(B_1-gamma)` with exactly one positive direction.

A viable global kernel must therefore fix both correlation geometry and finite renormalization from the source, or provide a regulator-covariant completion in which finite and archimedean/polar terms transform together. A sign theorem at one chosen scheme is not enough when another equally natural source regulator changes the sign class.

## What readout retains source-dependent information after classical bulk/local structure is removed?

Prime Circle now has a sharper finite-lift boundary. [MI-003](intuition/MI-003-arithmetic-significance-begins-after-universal-control-subtraction.md) includes PC-269: bounded-mode nonresonant prime sequences approaching a rational torus point still converge to a deterministic periodic-Haar IDS reproduced by matched controls. Exact finite-level congruence can produce different branches at the same limiting ratios, so a viable observable must retain the relevant finite lift, growing resonance, mesoscopic drift, microscopic placement, or another source-dependent itinerary rather than the rational limit alone.

Nyman--Beurling has moved past defect existence. [MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md) now uses NB-061: every finite log window contains an infinite-dimensional arithmetic defect sector, but the canonical tail is controlled by the projection of the truncated residual onto an at-most-`R-1` dimensional visible natural space. The target question is finite-window leakage, not whether a localized annihilator exists.

Robin Extremal has similarly moved from packet algebra to successor geometry. RE-060--RE-062 classify bounded charge cancellation; RE-063 shows that every selected nonzero-fringe endpoint is followed by a deep base-two event and that such hosts form a lacunary `O(log X)` family with zero-dimensional recurrent threshold support. Egyptian cancellation is therefore forced onto a sparse successor architecture; the remaining question is whether the global false-RH fan can remain entirely inside the surviving exceptional branches.

Visual Exploration retains the broader matched-null boundary from VIS-177: subpolynomial continuous support remains Gaussian/classical under the stated tests, so merely leaving polylogarithmic support is not a source-sensitive escape.

## Which quantitative observation family reaches the required source aperture at the right resource cost?

With bounded local Euler degree, [MI-005](intuition/MI-005-topology-is-part-of-the-arithmetic-claim.md) separates finite prime-power depth from unbounded prime breadth. [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) now has a sharper finite-prefix calibration from AF-282--AF-284. For the two-prime mixed channel on the known prefix `1,...,N`, phase labels have spacing `Theta(1/N)`, stable coordinatewise mixed degree is sharply `Theta(N)`, and an **unweighted raw subset of `O(N log N)` distinct mixed moments** already gives a breadth-uniform stable inverse.

The remaining finite-prefix acquisition gap is therefore only `Omega(N)` versus `O(N log N)`. Removing the logarithm, finding an explicit deterministic design, pricing timing horizon, changing support assumptions, and converting coefficient norms are separate problems; weighted linear-size sparsification does not settle the raw unweighted acquisition model.

Analytic Frontier gives a different aperture law. ANF-183 shows that endpoint memory can remain invisible to every physical twist window `o(X/a_X)` while a rephased three-point curvature at separation `Theta(X/a_X)` must detect the sector-aligned matched packet. The remaining difficulty is source access with absolute `o(a_X)` error at that aperture, not another source-blind smoothness statistic.

Xi Flow gives the converse calibration: once a consecutive packet window is known, XF-186 provides an `O(m)` holomorphic measurement bank uniformly bi-Lipschitz on a constant nonlinear cell for `m<=N`. Local inverse conditioning is not the bottleneck there; support discovery, global assembly, source-side access, and transport to a zero-sensitive quantity are.

## Which source cancellation theorem crosses the exact selected-family budget?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md) compares two Möbius-facing frontiers without identifying their operators.

Farey discrepancy now has a positive example of family-matched cancellation. FD-062 uses the known all-short-interval Möbius estimate to move a growing nonsquarefree CRT packet **below** the deterministic host scale: the available logarithmic saving can be split between host penetration and packet width with `B+kappa<1/3`. Its remaining obstacles are the same-horizon energy denominator and the localization range below the current `vartheta>0.55` theorem.

Möbius cancellation has the complementary negative calibration. MC-232--MC-233 remove exact-incidence and primorial-roughness masks at only subpower cost, leaving ordinary reduced-residue Möbius progression sums. At incidence/dilation scales `(alpha,beta)`, support closes the region `2alpha+beta>=1/2`; below it the sufficient componentwise target still requires fixed-power cancellation `1/2-2alpha-beta`.

The reusable question is therefore not whether a representation looks simpler, but whether an available source theorem matches the exact selected family, uniformity, and quantitative budget consumed downstream.

## Which source-specific inequality prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia agree on a concrete localization boundary without asserting the same operator. PL-280--PL-281 prove that the canonical support-localized negative Morse index is monotone and exactly exhausts the global compact-support negative index. A finite negative sector cannot hide beyond every finite aperture.

WI-247--WI-251 attack the complementary first-crossing witness. Under RH failure the odd sector has an attained first zero mode at finite radius; layer-cake averaging removes the smooth IMS collar cost and compresses the nested localization family to one signed radial von-Mangoldt flux inequality with a finite universal mean-side budget and a strictly positive integrated interior gap term.

Together these remove unseen support drift and arbitrary smooth-collar cost. WP-265 supplies the adversarial calibration: even the sign class of a critical mixed-prime finite part can depend on natural regulator choice. A useful completed-Weil inequality must therefore fix, or covariantly account for, the finite arithmetic scale on which its sign is stated.

## Does the complete flute response preserve the weighted source budget?

PF-297 shows that complete Robin factors preserve positive `q`-mass, and PF-298 shows that the tight-pant scalar constant channel has only `O(s_n)` relative killing. PF-299--PF-300 identify the exact accumulation law of the prime seam geometry. With `t_n=F(p_n)`, the seam weights are an intrinsic Riemann quadrature, so for decreasing `phi`, `sum s_n phi(t_n)` converges exactly when `int phi(t)dt` does.

In particular

\[
s_n(\log p_n)^{-\beta}
\]

is nonsummable for `beta<=1` and summable for `beta>1`. At the critical law `epsilon_n\sim a s_n/(1+F(p_n))`, the isolated scalar transmission decays like `(\log p_n)^{-a+o(1)}`. The next scalar theorem should therefore identify the slowly varying factor in the actual pant killing, not merely improve `O(s_n)` to an unspecified `o(s_n)`.

This still does not settle the physical mixed response. The scalar compression is not known to be invariant under the complete `P/H` Schur map; nonconstant-mode damping, support loss, source-adapted forcing/cancellation, physical-high remainders, and the global heavy-range singular-value problem remain separate gates.