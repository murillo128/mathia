# Global mathematical questions

## Can the source-native pole tangent be lifted to a canonical completed operator with the correct sign?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate continuum provenance, spectral layers, model selection and sign.

WP-267--WP-277 rule out a sequence of free constructions: local positive hosts, inverse reconstruction from an already supplied target, exact bounded pure-point-to-a.c. intertwining, and bounded/subcritical mixing of compact-resolvent sectors. WP-278 then gives the surviving infinite-volume category a source-native measure-level realization: the critical Mangoldt half-density has high-energy tangent `cosh(x/2)dx`, exactly the polar continuum of the completed carrier.

WP-279 shows why the same scalar tangent expansion cannot simply continue to Gamma. After the pole term is removed, nontrivial-zero modes decay like `e^((rho-1)E)`, while the first nonconstant trivial-zero/Gamma correction is only `e^(-3E)`. Every nontrivial-zero mode therefore lies parametrically above the Gamma layer, even under RH, and differentiating the Chebyshev primitive has already discarded the archimedean normalization constant.

The remaining burden is consequently global rather than another scalar asymptotic term: construct an independently selected operator/boundary/cohomological architecture that couples finite and archimedean sectors before the limit, retains source fluctuations, and proves the Weil orientation without subtracting or annihilating the nontrivial-zero divisor by hand.

## Which source restrictions actually shrink the target-reachable failure set at the metric the theorem consumes?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) distinguish source reduction, exact failure incidence, non-vacuity and stable distance from failure.

AF-300--AF-305 replace ambient deleted directions by intrinsic source dimension, reachable positive bodies and a sharp discrete saturation boundary. AF-306 proves that every fixed saturated support can approach zero arbitrarily closely. AF-307 classifies the first prime-support exact cases, and AF-308 identifies the ambient geometry: for `k>=3` exact-zero frequency vectors occupy a Hausdorff-codimension-at-least-one cone, while rationally independent flows still have zero stable margin. The open `k>=5` prime problem is therefore arithmetic incidence of the special log-prime vector with that exceptional cone, not another generic density calculation.

PF-315--PF-317 give a complementary operator reduction. The full physical angle is weak trace exactly when both `T_H` and `Z_LH` are, and the direct channel `Z_LH` is now controlled by only three families of local mixed-frequency pant blocks after same-sector completion becomes contractive. A reciprocal-prime local pant estimate is sufficient; the global inverse/reassembly burden is no longer the direct-channel bottleneck.

Visual Exploration sharpens the observation-scale bill in another direction. VIS-199 reaches a broad stretched-exponential/subpolynomial dephasing corridor, while VIS-200 proves that changing the sieve/taper cutoff cannot improve its leading four-form pointwise threshold. The residual problem is aggregate local-factor/fiber structure or a different dephasing resource, not a better scalar split point.

## Which residual cancellation theorem survives after generic abundance and fixed-order residue algebra are exhausted?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) place Farey and Möbius beyond their previous generic barriers.

FD-083--FD-085 show that full-counting occupation and polynomial additive blocks do not imply a finite-entropy multiplicative chain. FD-086 now restores the physical source before scalarization: divisor-supported horizon increments make the complete normalized Jordan vector, Schur equality direction and transverse defect asymptotically coherent across each frontier-length occupied block. The remaining Farey theorem must transport these coherent packets **between** separated blocks or compose them by a multihorizon law that does not require bounded integer ratios.

MC-252--MC-256 eliminate nonlocalized fixed-order residue rigidity, including every fixed tame prime order. MC-257 then removes the wild prime-square direction from the actual moving shell itself: every local wild Sylow subgroup lies in the lower-prime-generated subgroup, so every blind character factors through the squarefree shell modulus. The remaining blind arithmetic is a tame localized quotient. The next question is whether squarefree conductor descent or genuine moving-shell cross-prime coupling yields cancellation; fixed-order nonlocalized universality and wild directions are both exhausted controls.

## Can source-specific regularity and support propagation become quantitative enough to control a Weil crossing?

Prime Lattice and Weil Inertia now separate generic regularity/order structure from what is selected by the actual source equation.

PL-285--PL-291 move from inverse-log ambient transport through graph-domain `log^-2` transport to every fixed logarithmic Fourier moment for actual eigenstates. PL-292 quantifies the hierarchy: the moments grow at most like `exp(C m^2 log m)`, yielding an optimized subpower modulus faster than every fixed inverse-log power. But physical cutoffs have at least `(cm)^m` cost on these spaces, so this bootstrap cannot become Hölder merely by tightening generic cutoff constants. The next gain must be state-specific endpoint cancellation, positive-order regularity, or direct positivity continuation.

WI-264--WI-267 show that finite-lag screening and generic first-crossing substitutes fail to capture Suzuki's source equation; every nonzero localized null mode must be prime-translation support-dominating. WI-268 independently closes the generic sign route: the unrestricted archimedean cross kernel changes sign before the first prime lag, so the localized Weil semigroup is not positivity preserving throughout the relevant regime. The remaining bridge is therefore activity-to-overlap under the actual null equation and first-crossing constraints, with no free Perron--Frobenius sign coherence.

Both lines have reached source-selected residual carriers that generic functional analysis does not control. Further progress must exploit the selected eigen/null state rather than strengthen the ambient space once more.

## Can the actual aliased Nyman descendant spectra make the radial floor charge finite?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate stationary inverse calibration, finite depth and the actual limiting radial floor.

NB-082--NB-084 reduce persistent continuation to finite-future prediction and construct the exact source-specific Toeplitz certificate along integer-dilation descendant rays. NB-085 proves `log W_(j,q) in L^1` and hence `E_(j,q,L) -> G_(j,q)>0` by Szegő--Kolmogorov.

The qualitative radial endpoint is now the aggregate floor charge `S_(R,q)=sum_(j<R) log(G_(j,q)/PiHat_(j,R)^2)`. If it stays bounded on an unbounded sequence, finite depths can be chosen afterwards and stable-tail exclusion follows. If it diverges, more depth on that same ray cannot help and the proof must use cross-ray or otherwise nonradial cancellation. Convergence rate is a second-order issue unless a prescribed quantitative horizon is required.

## Which low-layer selection constraint survives after deep Robin boundaries become negligible?

RE-083--RE-087 reconstruct selector geometry, isolate anchored gap costs and show that the marginal pool of candidate prime-gap capacity is too abundant to force a contradiction. RE-088 now removes the opposite tail: whenever the off-RH exponent gap satisfies `delta>1/r`, cells touching depth-`>=r` adjacent support boundaries occupy vanishing total threshold measure. For any positive exponent gap, some finite depth cutoff therefore removes all sufficiently deep layers.

The Robin frontier is now a finite low-layer, selection-sensitive problem. The counting-plus-cell-width mechanism can never eliminate `j=2`, so the next theorem must use rightmost-maximizer compatibility, tied power-related resonances, simultaneous boundary constraints or skipped-span structure inside the surviving low layers. Neither marginal gap counting nor deeper-layer asymptotics addresses that carrier.

## Can the local direct flute blocks and the conditional channel satisfy the required ideal counts?

PF-316 removes the residual-source ambiguity at the endpoint: the physical angle is weak trace exactly when `T_H` and `Z_LH` are. PF-317 then makes the direct channel local. Positivity makes same-sector completion contractive, nearest-neighbor topology leaves only three cuff offsets, and the seam energy is explicitly comparable to the flat shifted-strip model. A uniform local nonconstant-low/high pant bound of order `1/P_n` is enough for `Z_LH in S_(1,infinity)`.

The remaining flute burden is now asymmetric and concrete: prove that local mixed-frequency pant estimate (or an equally strong singular-value envelope) and independently control `T_H`, then close the compatible infinite realization, symmetric side and signed target composition. Constant-mode pant blowup is a required negative control, not a witness against the mixed-frequency corner.