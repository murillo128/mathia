# Global mathematical questions

## Which source resources survive to the destination rather than becoming target-complete, reconstructive, universal or cheaply matched?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate source recovery, retained relation, conditioning, coupling complexity and final sign/zero selection.

Arithmetic Fidelity now separates **determinant order, placement generators and lower-order interiority**. AF-379/AF-381 make vanishing prime-log sampling target-complete. AF-383 removes one spatial tuple at every fixed determinant order. AF-384 shows that strict total positivity through lower orders removes the remaining tuple as well: solid consecutive order-`k` minors suffice on shrinking lattices. AF-372 still forces unbounded determinant order for unrestricted `PF_infinity`. A compact generator family is therefore not intrinsically cheap; its hidden cost may be the strict source hypothesis that makes recognition complete.

Prime Circle supplies the complementary zero-selection warning at both coupling endpoints. PC-319--PC-320 show that exact canonical multivariable source recovery carries an off-critical local divisor near `u=0`. PC-321 proves that the same `(5,7)` packet is zero-free on the full right polyhalf-plane once `Re u>=5`, because strong additive exponent isolates the unique lowest output mode. Any raw-packet selector is therefore confined to finite intermediate coupling and needs an independent source-forced reason; neither weak nor strong coupling is a zero-selection principle.

## Which thin, moving or signed channel remains after bulk controls close?

Nyman--Beurling has moved from separate conditioning/transport currencies to a **Poisson-resolution signed metric**. NB-165--NB-166 use high-ordinate analyticity before absolute values. NB-167 then caps ordinary Jordan transport at one Poisson width with `d_a(s,t)=min{1,|s-t|/a}`. The resulting `Xi_G` is no larger than either conditioning or uncapped transport budgets, controls every Poisson response, and gives conservation cost `O(q_G Xi_G E_G)`. The live compact escape must therefore carry macroscopic mismatch at the resolution actually seen by the Poisson test class, not merely large variation or long transport.

Robin Extremal now has two orthogonal source-fidelity coordinates. RE-168 turns the KV envelope into a continuous spatial capacity profile with decay length `ell_KV=log T/V(T)`. RE-169 adds the global Mertens normalization: the same Robin kernel measures the difference of Mertens constants, so exact/critical-scale normalization converts a local post-selector displacement into compulsory future compensation. Under a KV-rigid tail that debt cannot survive past an `O(1)` neighborhood of the remote capacity center in `V`-coordinates. Qualitative `Gamma(Q_p)=gamma+o(1)` is still too weak; the critical precision is `o((sqrt p log p)^(-1))`.

Farey Discrepancy now pushes the hidden-fibre obstruction through truncation itself. FD-158 shows finite completed auxiliary vocabularies merely replicate the cut. FD-159 replaces the full cube by any divisor-downward active core set and obtains the exact pressure `P+L/|C|>=h bar f`, where `bar f` is average occupied primary fibre size. A truncation ceiling helps only if it genuinely thins that average fibre. The remaining structural loophole is non-downward sparse support/holes, migrating vocabulary or a source rule that changes the admissible cut, not ordinary incomplete boxes.

Prime Lattice supplies a rare positive source-selection step. PL-326--PL-327 show that canonical endpoint regularity cannot orient the shrinking first-prime reflection. PL-329 proves that the exact source equation can: after selecting `C_delta`, the residual is `O((1+S_delta)/Q)`, so first-prime orientation follows from `(1+S_delta)/(|C_delta| sqrt(L_delta))->0`. The known sign-reversing layer has exactly `S_delta asymp sqrt(L_delta)`. The remaining question is whether the actual Weil eigenbranch satisfies this source-amplitude gate uniformly through activation and then whether analogous control survives later prime-power channels.

## Which growing source law is informative rather than merely numerous, low-rank or programmable?

Möbius Cancellation now has four independent joint-coefficient currencies. MC-332 shows unrestricted mode-dependent rows contain the exact conjugate-character matched filter. MC-333 prices rowspace rank against Frobenius energy, and MC-334 prices centered variation away from every common input. MC-335 adds **mode-dependence Fourier degree**: low-degree coefficient columns cannot even approximate uniform endpoint dephasing, while exact programmability returns at the near-maximal threshold `d=R-2`. A viable analytic class must therefore exclude the matched filter in the exact norm and structural complexity actually enforced by the theorem, not by nominal rank alone.

The common lesson from FD-159 and MC-335 is that structural restriction matters only when it changes the effective carrier. Truncating coordinates without thinning the average fibre leaves the same cut pressure; permitting mode dependence without controlling its Fourier degree leaves enough capacity for a matched filter. “Sparse,” “truncated,” “low-rank” and “structured” are not resources until they produce a quantitative exclusion in the source-to-destination geometry.

## Which representation-faithful null survives nuisance removal, weak convergence and universal positivity?

Visual Exploration now has an exact cusp-adapted tradeoff. VIS-256 shows fixed polynomial moments cannot beat the sine-kernel `|q|` cusp. VIS-257 kills that cusp exactly but proves the sharp price `|m_2(nu)|<=(b^2/8)||nu||_TV`; fixed quadratic signal costs `Theta(b^(-2))` variation, and the finite-window CUE null itself has a quadratic channel. The route must therefore either calibrate that channel to `o(b^2)` under amplification or cancel it and move to a quartic/higher conditioning gate.

Weil Positivity now shows that even worst-case scalar convergence can be the wrong topology. WP-331--WP-332 give exact fixed-cutoff multiplicative-domain rigidity. WP-333--WP-334 show weak and finite averaged divergences miss a rare moving defect. WP-335 strengthens the counterexample so that **both max-divergences tend to zero and the channel converges to the identity in bounded-operator norm**, while the Kadison variance of the growing logarithmic observable stays fixed. The missing control must therefore be weighted by the energy/graph norm of the actual moving observable, or control its second-moment defect directly.

Prime Lattice's PL-329 illustrates the complementary positive pattern: a source-side norm becomes decisive when the exact source equation magnifies the same moving layer that the destination consumes. The crucial issue is not whether a norm is abstractly strong, but whether it prices the adversarial set at the scale of the destination observable.

## Which noncompact source channels survive positivity reduction once complexity is separated from information?

Weil Inertia now closes scalar-localization self-bootstrap under **arbitrary Hilbert-valued diagonal amplification**, not merely finite channelization. WI-320 supplies the fixed short prime-free witness. WI-321 shows the scalar semibound amplifies dimension-free to any complex Hilbert space and, by polarization, already generates the corresponding Loewner Gram inequality. Fixed PSD cross-channel metrics are therefore consequences of the scalar premise, not new information. A viable vector route must add coupled inequalities outside that scalar-generated cone.

Xi Flow provides the opposite inversion lesson. XF-185--XF-186 show that once the correct consecutive packet is known, linear and local nonlinear holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local invertibility.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is a classification of **source-specific coupling consumed by the destination at its own scale, topology and regularity**. The latest results sharpen different entries in the same ledger: AF-384 shows generator compression can hide a strict-interiority cost; NB-167 identifies the metric induced by one-Poisson-width source tests; RE-169 turns global normalization into compensation debt; FD-159 exposes average fibre size as the truncation invariant; MC-335 makes mode Fourier degree an anti-programmability resource; VIS-257 turns cusp cancellation into a sharp conditioning bill; WI-321 identifies the scalar-generated Loewner cone; WP-335 shows bounded topologies can miss energy of a growing observable.

For every proposed route ask: what object reaches the destination and at what moving scale; what conserved fibre, generator cone, induced metric or matched geometry must be crossed; which hypotheses make a compressed test family complete; whether truncation actually changes average interaction depth; whether a joint class excludes the target-matched filter by degree as well as rank/variation; whether a global normalization converts local gain into later debt; whether the norm controls the unbounded observable rather than only bounded shadows; and whether channel coupling lies outside what scalar polarization already implies. The surviving theorem must expose a resource the destination uses and a matched control cannot reproduce by completion, decoding, truncation, phase matching, thin-tail averaging, channel duplication, universal positivity or classical recoding.
