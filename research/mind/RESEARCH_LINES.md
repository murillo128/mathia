# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal, noncompact or Julia-trivial scalar geometry?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity now brackets the natural scalar moving-boundary escapes on both sides. WP-314 shows that an empty-side moving Julia node converges to the universal unit-density logarithmic tangent. WP-315 shows that the intrinsic support-side Mangoldt atom scale is not locally compact: bounded prime clusters collapse arbitrarily many comparable atoms into one normalized chart. WP-316 closes the opposite regime. If a gap-adapted chart really isolates one pole so that its boundary derivative dominates, classical scalar Julia reduction turns the normalized local profile into a removable real constant. For the Mangoldt source, every nonconstant scalar tangent therefore requires order-one positive multipole background.

The surviving scalar route must derive a canonical **collective multipole geometry** that remains locally controlled, nonuniversal and compatible with the Gamma discriminator. Otherwise the construction must leave the audited scalar category through matrix/operator-valued boundary structure, generalized resolvents, noninvertible operations, an added archimedean channel or nonseparable finite--archimedean coupling.

## Which source resources survive into the exact destination margin, topology and moving resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) keep conditioning, source supply, destination sensitivity and quantifiers distinct.

Arithmetic Fidelity now has an exact moving-bandwidth law. AF-358 gives the zero-Sobolev threshold `E_delta in H^s_(B,zeta) <=> s<1/2+delta`, while `E_delta-E_0` stays below `1/2`. AF-359 sharpens the simultaneous limit: if `delta log T->u`, the smoothed critical shell retains fraction `e^-2u` of endpoint energy and the difference carries `(1-e^-u)^2`. Subexponential zero bandwidth in `1/delta` is asymptotically blind to the endpoint defect; order-one detection begins only at `T=exp(Theta(1/delta))`, reciprocal to the physical Beta-kernel scale `exp(-Theta(1/delta))`.

Prime Lattice gives a complementary exact threshold for a moving physical observer. PL-316 shows that zero-extension `H^{1/2}` can be arbitrarily small while a moving Green diagonal still extracts order-one mass. PL-317 shows that every positive gain `H^{1/2+eta}` is sufficient: the zero boundary trace becomes Hölder and hence exponentially decaying in logarithmic endpoint distance, forcing `m(r/s)/s->0`. The live question is whether the actual completed-Weil eigen-equation supplies that gain or a weaker source-specific moving-tail law.

Nyman--Beurling adds a worst-cell quantifier. NB-140 makes logarithmic-rank packets inside small complex confluence cells target-poor. NB-141 proves Selberg density makes those cells power-law rare in height, yet the current global count/density/zero-free envelopes still permit a sparse infinite sequence of them. Better average rarity does not supply the uniform local occupancy theorem the target needs.

The common requirement is a **target-aware coupled-scale modulus in the topology, bandwidth and quantifier actually consumed by the endpoint**. Smoothing order, packet geometry, coordinate chart, representation dimension and observation scale must be priced together with what the destination sees.

## Which growing source law is informative rather than under-resolved, source-reconstructing or shareable?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now brackets coordinate capacity more tightly. FD-140 proves every fixed congruence-periodic alphabet leaves a finer prime-color wall. FD-141 shows one exact scalar can nevertheless encode the full squarefree source. FD-142 removes finite or polynomial precision as an automatic repair: `L(n)=log rad(n)` is injective on the squarefree prefix with spacing `>1/T`, and uniform quantization recovers all states using essentially `Theta(log T)` bits, the source-identity scale itself. A useful theorem must therefore constrain effective source-state capacity **and provenance**, not coordinate count or nominal precision.

Prime Circle exposes the same saturation issue through moments: PC-305 shows `dB>=pi(q)-2` already suffices for exact sparse source reconstruction. A sublinear tensor hierarchy can therefore be lossless before any genuinely new geometric operation appears.

Möbius Cancellation now has its first nonshareable source-incidence theorem. MC-311 forces exponentially many Page-expensive directions. MC-312 represents any common lower-prime package as an `[n,R]` binary code and combines generalized Hamming supports with the source-cut theorem to force

`(n-R+O(log R)) log Z >= (1-o(1)) log y`,

where `Z` is the largest source prime. Reuse is possible only by paying in source-coordinate redundancy or source scale. The live theorem must now charge one of those two escapes to the Möbius endpoint rather than merely recount expensive directions.

The cross-line lesson is no longer “use more source information.” A source condition is informative only relative to **how many source states it resolves, whether that resolution is circular, which closure operations the destination consumes, and how much source cost can still be shared**.

## Which support geometries survive the one-signed Weil constraint after compact-window verification?

WI-300 independently verifies Zhu's pinned compact-window positivity with `lambda_0.8>=8.9e-18`. WI-301 then removes first-crossing minimality: any nonzero one-signed compactly supported test that screens the powers-of-two ladder has `q_arch(f)>=8.9e-18||f||_2^2`. Thus a nonpositive test must repay a uniform reserve through unscreened prime-power interaction. The live question is quantitative unscreening: where the odd-prime reserve must occur, whether it can be localized/amplified into coercivity, and what survives once sign changes are allowed.

## Can the required Robin cancelling geometry occur for actual zeros after rightmost anchoring and zero-density tail lifting?

RE-139 removes the horizontal-spread tax for finite packets. RE-140 exposes the full gain/complexity/tail budget, and RE-141 spends zero density productively on the coefficient-weighted exterior tail, giving the sufficient polynomial-cutoff condition

`A/B + eta + Lambda_kappa alpha < 2-nu_I(Theta)`.

RE-142 now proves that the remaining generic transfer exponent `eta=2` is sharp relative to the current **upper-envelope** source language. One infinite formal spectrum can satisfy the relevant upper zero-density laws, zero-free region, symmetry and a Riemann--von Mangoldt background while having `G(U_j,T_j)=T_j^(-2+o(1))` and retained complexity `o(log T_j)`. Thus another upper zero-density exponent can improve the tail but cannot force relational recurrence between a strong atom and a farther-right atom.

The live theorem must be relational: couple strong-atom amplitude/horizontal position to first-occurrence height or rightward recurrence, obtain signed exterior cancellation strong enough to absorb the transfer, or replace rightmost anchoring by a target-aware/infinite-packet certificate.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225--VIS-231 fix the subcritical static order and the deterministic, marginal, Markov-center and Markov-variance baselines for the critical collision count. VIS-232 shows that conditioning on the realized occupancy histogram or exact endpoint-matched transition table conditions that statistic away. VIS-233 identifies the first interaction-order escape, VIS-234 gives the exact type-conditional PGF for the lag-two statistic `T_2`, and VIS-235 gives a cheaper exact conditional mean/zero-support gate through Whittle deletion. The exact-control route should use the deletion mean first, then the full support/variance law only on surviving realized types, before any prime-specific interpretation.

## Xi Flow: move from local known-window inversion to global support discovery and zero-sensitive transport

Xi Flow has a uniformly conditioned local nonlinear holomorphic chart for known consecutive packets with linearly many samples. The live problem remains global: locate and recenter unknown windows without an oracle, assemble separated or multiscale packets, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side source control to a zero-sensitive quantity.

## Global discriminator: price what the endpoint actually sees and what the representation can actually resolve

The newest evidence strengthens one common distinction: **retained information is meaningful only relative to observer topology, moving resolution, source-state capacity, provenance, closure/incidence structure and worst-case quantifier**. AF-359 prices the exponential spectral bandwidth of the Riesz endpoint; FD-142 shows finite-precision one-dimensional observation can already reconstruct a whole source at `Theta(log T)` bits; MC-312 turns source overlap into a redundancy/scale tradeoff; NB-141 and RE-142 show upper population control can leave a fatal sparse exceptional geometry; PL-316--PL-317 isolate an exact moving-observer Sobolev threshold; WP-316 forces any nonconstant scalar support tangent to be collective rather than isolated.

For every proposed route ask: **what is the source discriminator; how many source states are stably distinguishable at the actual precision; is that resolution source-native or a disguised label; which coordinates or quotient frequencies see it; what physical/spectral scale does the observer sample; what closure or overlap makes source costs reusable; which exceptional configurations remain possible; what topology and normalization retain the signal; which constants remain uniform; which sigma-field does the null preserve; does stronger resolution merely reconstruct the source; and does a local source scale have controlled collective geometry?**

The productive next theorem is the one that closes one of those exact failure modes with source-native information rather than relabelling it.