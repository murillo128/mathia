# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry or programmable completion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity has now closed the natural **fixed scalar** gauge/Schur/Julia orbit sharply. WP-307 keeps the pole-normalized Mangoldt curvature positive under density-normalized orientation-preserving affine spectral recentering; WP-308 closes constant scalar Herglotz projective recodings; WP-309 shows that holomorphic spectral dependence adds no scalar automorphism freedom. WP-310 adds an admissibility obstruction: the critical positive Mangoldt source has density-one infinite mass and a logarithmic Herglotz end, outside the finite-moment asymptotic class required by the classical infinity/moment Schur transform. WP-311 shows that fixed-interior Schur stripping is admissible and positivity-preserving but inserts a positive `log(y)/y` term above the Riemann-Gamma `y^-2` curvature scale.

WP-312 closes every finite fixed-real-node Julia--Nevanlinna chain. At a regular boundary node, unit-log normalization adds a strictly positive increment to the `log(y)/y` coefficient; pure Mangoldt-pole stripping avoids that order but adds positive curvature to the already-positive arithmetic `y^-2` coefficient.

WP-313 now closes the corresponding **asymptotically stable infinite-chain** escape. For the translation-invariant high-energy discriminators `L` at order `log(y)/y` and, when `L=0`, `C` at order `y^-2`, every regular finite stage contributes `+pi/d_j` to `L`, while every pole removal contributes `+Lambda(n)log n/(n+1)` to `C`. Any infinite fixed-node limit that transports the relevant invariant therefore retains the wrong sign/order; exhaustive pole stripping makes the positive curvature diverge already like `(1/2)(log X)^2` on primes.

The remaining scalar route must now be genuinely **nonuniform at infinity**: merely proving convergence of an infinite Julia/Schur algorithm is insufficient. A moving boundary layer, scale-dependent reference/node law, or another source-native mechanism must make the chain-depth and high-energy limits fail to commute while preserving positivity independently. Otherwise the route must introduce genuinely new finite--archimedean geometry through operator/matrix-valued structure, generalized resolvents, noninvertible operations, an added archimedean channel or nonseparable finite--archimedean coupling.

## Which source resources survive into the exact destination margin, topology and moving resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) keep conditioning, source supply and destination sensitivity distinct.

Analytic Frontier still requires a joint arithmetic-placement law relative to its distinguished logarithmic carrier; matched one-point statistics are exhausted. Prime Flute's sharp physical low/high split is already noncompact, so progress there still requires a new split or observable rather than stronger estimates inside the same hard cutoff.

Arithmetic Fidelity now separates qualitative threshold crossing from parameter-uniform conditioning. AF-349 proves full-source recovery in a strong derivative-variation metric while raw amplitude can collapse distinct sources. AF-350 shows that an `O(X^theta)` Riesz quotient still preserves Dirichlet principal parts in `Re s>theta`. AF-352 proves that every fixed real `delta>0` crosses the critical absolute-summability threshold without moving the off-critical Mellin discriminator, whereas `delta=0` fails the sharp `sqrt(X)` norm. AF-353 prices the approach to that endpoint: the positive-ordinate absolute zero-mode budget has

`A_+(delta)=1/(2*pi*delta^2) - (log(2*pi)+gamma_E)/(2*pi*delta) + O(1)`,

and, under RH, the actual critical profile norm is also necessarily unbounded as `delta->0+`, although no matching rate is known. A representation can therefore be faithful and well-conditioned for every fixed parameter while becoming singular in the limiting regime actually needed by a source argument.

Nyman--Beurling now separates recurrence visibility from quantitative conditioning and from representation supply. NB-132 can force a genuine narrow polynomial-height zero packet below the recurrence-visibility rank budget. NB-133 shows that the same coarse rank allowance plus Vinogradov--Korobov radial depth permits a formal phase cluster with a superpolynomially small scalar annihilator margin. NB-134 extends that collapse to every fixed finite family of fully visible geometric bases. NB-135 shows that the current explicit zero-count budget itself supplies only finitely many integer bases on which a separate full recurrence is uniformly certified: rank allowance stays `Theta(log R)` while `q`-geometric depth is `(log R)/(log q)`. The next representation must obtain more conditioned information per unit of source rank, for example by combining growing partial traces jointly rather than adding more full scalar clocks.

Prime Lattice keeps the moving-observer version separate. PL-314--PL-315 show that weighted `L^1`, arbitrary smoothness, every fixed `W^{k,1}`, all-log Fourier moments and every `H^alpha`, `alpha<1/2`, can still miss a Green atom sampling `Q=r/s`. A source theorem must force moving-scale anti-concentration such as `Qm(Q)->0`, an `o(1/Q)` remainder, one-sided tail structure or a direct moving-sample estimate.

These lines support a concrete common statement: **terminal stability is topology-, scale-, mode-density-, parameter-, phase-arrangement-, representation-complexity- and discriminator-relative**. A transform may improve fixed-parameter conditioning without moving the discriminator yet become singular at its endpoint; a recurrence may be fully visible in several fixed representations while catastrophically ill-conditioned; the source budget may cap how many such full representations are available; strong global regularity may still miss a moving singular observer. WP-313 adds that even convergence of an infinite positive transform chain is not terminal stability unless the convergence transports the high-energy coefficient actually compared at the endpoint.

## Which growing source law is genuinely informative rather than sparse, diluted, over-rigid, capacity-saturating or circular?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now brackets the missing source law in two independent coordinates. FD-132--FD-134 show that counting-measure primewise ancestry can monitor an explicit `H^(1-o(1))` family and still miss a positive-density defect concentrated on a moving central multiplicative-rank strip. FD-135 closes the hard-local repair: in the squarefree unit-sign class, any local `L^infinity` ancestry bound below `2` is exact ancestry, and with a finite-rank Möbius anchor it crosses a sharp rough-core threshold into full source reconstruction. FD-136 shows that the natural scale-balanced interpolation `dn/n` still fails: worst active-prime harmonic ancestry tends to zero while coefficient disagreement retains positive logarithmic density. Balancing physical scale does not balance the moving `omega(n)` coordinate.

Möbius Cancellation now separates aggregate endpoint capacity from **hereditary projected source complexity**. MC-304 packages the selected quadratic modes into one squarefree radical `P`; MC-305 uses the endpoint's occupation of only `R` out of `2^R` full sign cells to force `P` near shell scale. MC-306 then constructs a critical-rank disjoint-singleton source satisfying rank, conductor, full radical, totient, energy and quotient-cardinality constraints simultaneously, proving that the aggregate package is noncoercive.

MC-307 shows that this matched control fails once the exact endpoint is restricted to smaller coordinate sets. Every proper `k`-coordinate projection occupies only the `k` one-defect cells plus the all-minus cell, so Brun--Titchmarsh forces its source radical to satisfy

`P_I >= y^(1-(2+o(1))(k+1)/2^k)e^(-O(1))`.

For `k=6`, every six-subfamily has product source cost at least `y^(25/32-o(1))`, forcing all but at most five selected directions above `y^(25/192-o(1))`. When `k/2^k -> 0`, every `k`-subfamily must collectively see radical `y^(1-o(1))`. The MC-306 singleton control has fixed-`k` projected radical only `y^o(1)` and is therefore ruled out as an endpoint realization even though it matches every previous aggregate capacity.

The cross-line lesson is sharper than “use stronger norms.” Nominal breadth can fail because normalization hides a moving active layer; correcting physical-scale dilution can still miss a different coordinate; extreme locality can become exact source reconstruction; aggregate capacities can all be saturated while a hereditary restriction test still separates the target; and a limiting construction can converge while failing to transport the discriminator sampled at infinity. **A source condition is useful only if its active-scale resolution, residual freedom, behavior under the restrictions actually used by the endpoint, and source-to-target localization are explicit.**

## Which support geometries survive the one-signed Weil first-crossing constraint?

WI-294 restores the sign constraint of the actual completely screened first null mode and makes the pole contribution positive. WI-295 gives the exact gamma-bathtub support floor and an exponentially decaying separated-`L^1` mass tariff. WI-296 retains the actual global mass `s=||v||_1^2` in the same bathtub and forces `H(s)+2s<0`, hence a fixed positive global `L^1` budget.

WI-297 keeps the exponential-moment imbalance discarded by that scalar compression. For the probability law `dnu=v(x)dx/||v||_1`, the exact pole upper bound gives

`E exp(X/2) E exp(-X/2) < kappa_*`,

which implies an aperture-independent variance bound, uniform exponential `L^1` tails after recentering, and a fixed interval carrying a universal positive amount of normalized `L^2` mass. Complete one-signed screening therefore cannot evade coercivity merely by making the entire mode diffuse: a nonvanishing core is forced, while full essential span can survive only through increasingly weak remote satellites.

The remaining gate is local-to-global propagation. Can the first-crossing/null identities force a fixed amount of mass to cross successive distant cuts when full span is maintained? Such a theorem would collide directly with the pole-tail bound. Unrestricted supported spectral relaxation, sign-changing modes, incomplete screening and bounded aperture remain distinct branches.

## What should Prime Circle retain after centered Gram formation and oriented-frame classicalization?

PC-295--PC-301 show that the universal diffuse square-zero class and centered same-source Gram formation erase orientation before averaging. PC-302 tests the simplest pre-Gram repair: reflection acts by latent mode reversal, so unlabelled frame/subspace geometry remains gauge; keeping labels recovers an odd sine cross-Gram, but same-law averaging classicalizes it to positive covariance of additive Fourier coefficients.

The next carrier must preserve a source orientation that is neither removable by latent mode relabelling nor reduced by the first positive quadratic compression to classical sine/cosine Fourier covariance. A canonical chirality/mode label, higher joint information, an independently justified order-sensitive observable, an asymmetric source window or a genuinely cross-level carrier remain distinct possibilities.

## Can the required Robin cancelling geometry occur for actual zeros?

RE-129--RE-131 turn hiding of a dominant algebraically strong atom into a nearby, logarithmically comparable companion with tightly constrained ordinate, horizontal displacement and cancelling phase. RE-132 shows that a sparse simple formal spectrum can realize the required doublet on sublinear windows. The adversarial review of RE-133 has now converged with the finding surviving: a growing formal reciprocal cluster can hide the dominant atom even on a fixed-relative window while retaining simple symmetry-compatible and arbitrarily sparse one-level spectral data.

RE-134 proved a fixed-relative visibility floor for bounded-cardinality packets in one fixed reciprocal box. RE-135 removes the cardinality hypothesis completely for the physical Robin coefficient law. After reciprocal rescaling, an arbitrary finite local packet normalizes to the Fourier--Laplace transform of a probability measure on a fixed compact set; compactness and `F(0)=1` give a cardinality-free visibility floor. Hence growing occupancy inside a fixed scaled box is not the surviving escape.

The live attained-edge source theorem is now one-dimensional in the concentration--compactness sense: **prevent order-`M_K(U)` cancellation from escaping every fixed reciprocal-scale radius, or quantify the cost of that escape as the scaled radius grows.** RE-133 is consistent with the sharpened boundary because its hiding cluster has scaled diameter tending to infinity. The accepted Nyman transfer remains useful only as a clue where its cross-line dictionary has not been proved inside Robin.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225--VIS-226 fix the strictly subcritical static Ising/torus/SDP scale up to constants. VIS-227 calibrates the deterministic critical occupancy floor. VIS-228 removes the uniform-independent false positive, and VIS-229 gives the exact nonuniform independent center/variance, including the `q_3-q_2^2` heterogeneity contribution.

VIS-230 shows that matching the full one-point occupancy law does not center a dependent control: for a stationary Markov cell process, the exact mean uses the lag-return profile `c_r`. VIS-231 completes the finite-sample second-moment calculation for an admitted finite-state first-order stationary Markov null. Overlapping collision pairs require a three-time coincidence tensor and disjoint pairs require three four-time pair-pair tensors; the independent categorical variance is recovered as the rank-one specialization.

For that Markov control, center and covariance are no longer the missing quantities. The unresolved question has moved to **model admissibility and source sensitivity**: justify the dependence model independently of confirmation, include any parameter-estimation uncertainty, and ask whether the standardized prime collision residual survives. If the relevant surrogate is renewal, higher-order, nonstationary or scale-dependent, its own three-/four-time structure must be calibrated rather than borrowing the Markov formula. Only then is a bridge to signed prime-phase energy meaningful.

## Xi Flow: move from local known-window inversion to global support discovery and zero-sensitive transport

Xi Flow has a uniformly conditioned local nonlinear holomorphic chart for known consecutive packets with linearly many samples. XF-185--XF-186 show that growing known-packet complexity is not itself a local conditioning barrier. The live problem is global: locate and recenter unknown windows without an oracle, assemble separated or multiscale packets, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side source control to a zero-sensitive quantity.

## Global discriminator: price what the endpoint actually sees

The newest evidence sharpens existing global boundaries without creating a new cross-line theorem. AF-353 shows that crossing a conditioning threshold at every fixed parameter can still have a singular endpoint cost. FD-136 shows that correcting physical-scale dilution can leave moving-rank dilution untouched. MC-307 shows that a full-family matched source can satisfy every previous scalar capacity and still fail the endpoint after a six-coordinate projection, forcing hereditary polynomial source cost. NB-135 shows that source rank can cap the number of fully visible representations before representation diversity becomes useful. RE-135 collapses Robin's local occupancy escape to pure scaled-radius noncompactness. VIS-231 closes the exact Markov covariance calibration and moves the bottleneck to null-model justification. WI-297 converts pole self-consistency into spatial tightness and a fixed `L^2` core. WP-313 shows that finite-stage Julia defects also survive an infinite fixed-node chain whenever the high-energy discriminator is stable under the chain limit.

For every proposed route ask: **what is the source discriminator; at which stage is it quotiented; in which coordinates/ranks/directions does it live; what topology and normalization retain it; which constants remain uniform in the parameter limit actually used; which quantifiers are uniform at the active scale; what happens after restricting to the smaller coordinate families still seen by the endpoint; which dependence and orientation survive the carrier; what phase arrangement, resolution or moving scale does the endpoint sample; how large is the admissible family and quotient-support capacity relative to the obstruction; can all current capacities — including hereditary projected capacities — be realized simultaneously by a matched source; does stronger locality merely reconstruct the source; does a representation reduce complexity or move it to another invariant; is the final transform admissible for the source class; does it preserve the asymptotic hierarchy containing the discriminator; does that discriminator commute with any infinite-stage limit being used; and do the hypotheses already encode the desired endpoint?**

A route can fail by under-resolution, rank excess, simultaneous phase recurrence, endpoint-singular conditioning, unstable or topology-mismatched inversion, inter-rank or intra-rank dilution, scale-only balancing, over-rigidity and circular reconstruction, family-energy slack, quotient-support capacity, aggregate-capacity saturation, hereditary projection failure, missing source-to-target localization, complexity transfer under repackaging, carrier gauge, classicalization after positive compression, moving-scale concentration, exterior noncompactness, deterministic/marginal/dependent control baselines, transform inadmissibility, asymptotic contamination, noncommuting high-energy limits, terminal quotienting, universality or certificate exhaustion.

If a transfer between lines is only plausible rather than established, keep it as a clue rather than promote it to global mathematical knowledge.