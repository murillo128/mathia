# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Characterize the minimal information-preserving quotient

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`.

For a candidate chain from arithmetic data through geometry and operators to spectral or zeta data, determine the coarsest equivalence relation through which the desired arithmetic distinction actually factors. Full injectivity is unnecessary, but every early quotient must preserve the particular distinction later claimed to encode arithmetic information.

The decisive mathematical object is the fiber structure of each canonical transformation. A candidate survives this line only if the RH-relevant or prime-relational variable can be proved to factor through those fibers; if two inputs known to differ in that variable are already identified, no later unmarked observable can recover it without extra information.

## Determine the depth of ordered relational memory

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`.

The current constructions reach from single ratios to a first directed relation-between-relations through the Feshbach term `w_j^2/w_{j-1}`. Investigate whether canonical higher-order coefficients, marked residues, Weyl functions, or cross-branch mixed kernels retain longer ordered combinations rather than collapsing to endpoints or one-gap statistics.

The decisive test is a hierarchy of matched sequences that agree on progressively more local data. A genuinely deeper invariant must distinguish sequences with the same mean density and gap multiset, and eventually the same short blocks, only when the ordering depth it claims to detect is changed. Telescoping or recovery from lower-order marginals would show that the apparent depth is coordinate redundancy.

## Make universal-background subtraction canonical

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`.

Seek a representation-independent separation between carrier effects forced by dimension, ordering, density, or the ambient exact geometry and residual effects that depend on prime organization. The mathematical issue is whether the subtraction/reference can be characterized intrinsically rather than chosen after observing a desirable singularity.

A decisive test is stability across regular-lattice, density-matched, gap-marginal, and short-block-preserving controls. A proposed arithmetic feature should either vanish/change at a specific control depth predicted by its claimed mechanism, or be demoted to universal background. Equivalent canonical subtractions constructed in the circle, flute, or spectral languages should agree on the residual if they are expressing the same invariant.
