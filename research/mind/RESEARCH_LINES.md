# Global mathematical questions

## Which source resources reach the destination before observation becomes decoding, classical recoding or a target-mixed completion class?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md) separate information survival, source recovery and zero selection.

Arithmetic Fidelity now supplies five complementary boundaries. AF-369 makes pole residue `1` a complete rational-prime discriminator inside the integer-supported Delone class even though the divisor is universal there. AF-370 uses Hamburger rigidity: inside the ordinary-Dirichlet finite-order class, the exact completed Riemann functional equation gives `f=c zeta`, and `a(1)=1` fixes `f=zeta`. AF-371 separates source richness from target faithfulness: the full reciprocal Taylor sequence may determine the analytic source while the infinite verdict “all Hankel moment matrices are positive” remains insufficient for Laguerre--Pólya membership; Schoenberg's translation total positivity is an exact relational lift for that target. AF-372 shows that this correct relational geometry cannot in general be truncated at any fixed determinant order. AF-373 adds the independent **sampling-geometry** axis: on an exponentially decaying source class, all determinant orders restricted to a determining set `E` can imply full Pólya-frequency, while the integer lattice admits exact false positives. Source class, retained data, relational geometry, relational arity and sampling uniqueness must therefore be priced separately.

Prime Circle supplies two complementary classicalization warnings. PC-311--PC-315 show that mixed-conductor bilinear fusion becomes one periodic scalar packet and that the complete `+1,-1,+i,-i` finite-Fourier orbit of the canonical `(5,7)` source has abundant zeros in `Re s>1`; exact transform symmetry is not a zero selector. PC-316 then tests the simplest nonlinear operation *before* scalar compression. Pointwise multiplication preserves independent indices `k,l`, and triple Mellinization produces the coupled denominator `k^s l^t(k+l)^u`, but character decomposition identifies the result as a finite bundle of classical Mordell--Tornheim double `L`-functions. Preserving more indices and adding analytic variables can therefore still be classical recoding rather than a new zero-sensitive channel.

Prime Lattice has lowered its destination bill rather than increased source recovery. PL-321--PL-323 close the moving Green transfer, residual moment and coercive equation for the actual branch. The remaining gate is the rational-prime-specific sign/first-crossing law carried by the transferred scalar.

The live audit is: **what exact source class and retained relation identify the intended object, what relational arity, sampling geometry and stability/conditioning are paid to reach the destination, what information the positivity/signature or analytic-family quotient actually keeps, and what separate theorem controls the divisor or sign once the object has been identified?** Decoding, universal transform closure, source recovery, multiple Mellin variables and large families of generic positivity inequalities are not substitutes for the final zero-sensitive implication.

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) track the increasingly narrow channels left after bulk controls close.

Nyman--Beurling now closes exterior signed sampling through both boundary approach and every **sublinear vertical-support scale** at sublogarithmic gap-normalized conditioning. NB-154 identifies `D_G=C_G/a_G=||mu_G||_TV/(a_G q_G)` as the correct boundary-normalized condition number: `D_G=o(log G)` still forces `Omega(q_G log G)` adverse charge and `Omega(log G/D_G)` adverse zeros. NB-155 lets the vertical half-width grow with `B_G=o(G)` and proves that nothing changes except the localization radius: a fixed fraction of the debt lies within `O(B_G+C_G)=o(G)` of the target height. Boundary proximity and sublinear vertical spreading can move or amplify the signed kernel, but they do not improve relative isolation. A remaining escape must spend logarithmic conditioning, use a height-scale vertical geometry, obtain sharper signed prime-side cancellation, or preserve target-bearing Nyman/prime coupling before the zero charges separate.

Robin Extremal now has a sharp **all-fixed-order terminal moment hierarchy**. RE-156 gives the ordinary-prime relocation transition `epsilon_*(p)=sqrt(log U_A(p)/(sqrt(p)log p))`; RE-157 shows that with only terminal cardinality matched, generalized insertions retain first-order leverage down to `epsilon_PNT=log U_A(p)/(sqrt(p)log p)`. RE-158 matches total Chebyshev mass and cancels that linear placement mode, exposing the quadratic scale `epsilon_*`. RE-159 proves the general rule: if equal-cardinality terminal packets match logarithmic-position power sums through order `r-1`, the first unresolved Robin influence is the `r`-th placement moment and its sharp scale is

`epsilon_r(p)=[log U_A(p)/(sqrt(p)log p)]^(1/r)`.

Prouhet--Thue--Morse matched controls realize the corresponding `m epsilon^r` separation sharply. Thus every additional **fixed** retained terminal moment only takes another root of the same coarse-PNT scale; a positive route needs growing-order or non-moment source control rather than a finite list of endpoint summaries.

Farey Discrepancy now prices the child channel *after* a macroscopic anchor, not only the parent bill. FD-151 gives `h<=P_T p_T/(1-p_T)`, so bounded parent distortion requires positive anchor mass. FD-152 shows that for central rank anchors `r_T=log log T+z sqrt(log log T)`, the outside descendants reached by depth `L_T` are exactly the next rank shell, and `L_T=ell sqrt(log log T)` captures the conditional Gaussian tail fraction `[Phi(z+ell)-Phi(z)]/[1-Phi(z)]`. A positive-density anchor prepays typical multiplicative rank, but bounded child distortion still needs a **Gaussian fluctuation-width** depth `Omega(sqrt(log log T))` if a fixed fraction remains unanchored.

The common question is no longer whether a broad theorem holds on average, but whether the exact target-bearing configuration is controlled in the norm, population, gap-normalized conditioning, local-supply, retained-moment, anchor-rank and terminal scale the destination consumes.

## Which growing source law is informative rather than merely connected, high-dimensional, zero-free, interval-pseudorandom or reconstructive?

Farey Discrepancy now has one joint complement-cut resource law. Sparse anchors pay parent distortion; central positive-density rank anchors can make that distortion bounded but directly orient a macroscopic source fraction; after that purchase, bounded child distortion still requires the `sqrt(log log T)` Gaussian depth window of FD-152. A source-valid mechanism must explain where the anchor information and the residual child exposure come from without silently encoding Möbius orientation.

Möbius Cancellation supplies a two-scale family-energy closure in the exact observation category. MC-322 gives, for one common modulus `q<=y^(3-delta)`, a bounded `ell^2` prime-shell bias budget for subpolynomial character families. MC-323 removes the common-modulus hypothesis in every fixed subquadratic **individual-conductor** range for cubefree moduli: only `O_delta(1)` fixed-bias characters can have conductor `<=y^(2-delta)`. In the natural endpoint package this forces the common source radical to saturate its ceiling, `P=y^(2-o(1))`, with almost every source prime near its maximal logarithmic scale. “Different moduli” is therefore not a free escape; exceptional family energy has to be paid by conductor capacity.

The next useful theorem in any growing representation must survive its exact algebra and matched controls and be stated on the same population, norm and scale as the destination. Many observations, connectedness, zero-free regions, small interval sums and source provenance are not scalar resources until that transfer is proved.

## Which representation-faithful null remains after nuisance variables, weak topology and universal scalar positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) and [MI-002](intuition/MI-002-the-surviving-arithmetic-variable-is-relational.md) separate normalization, relational geometry and source-selective sign.

Visual Exploration has now closed the structural nuisance quotient and the exact communication-class question inside the symmetric-Dirichlet model. VIS-247 gives the `alpha`-free conditional coarea law `Z(lambda)^(-1)J(x)^(-1)d sigma_lambda`; VIS-248 gives the exact `m=3` discriminant conditional; VIS-249 supplies the reversible three-coordinate heat-bath kernel and full tangent span. VIS-250 proves the regular log-product level is connected (indeed `S^(m-2)`), the triple flows form one global accessibility class, and the exact heat-bath chain is `mu_lambda`-irreducible. The honest remaining cost is now **quantitative mixing and numerical calibration**, not sampler existence or hidden disconnected support. Irreducibility alone says nothing about conductance or practical convergence.

Weil Positivity still has two distinct universality closures. WP-319--WP-323 show that source-specific values and arbitrary fixed finite confluent Pick jets may survive while their scalar positivity is a universal resolvent Gram. AF-371--AF-373 provide the benchmark that the exact Laguerre--Pólya certificate lives in ordered translation total positivity, requires unbounded arity on the unrestricted class, yet can sometimes compress locations through a determining sampling set. WP-324 tests the direct Mangoldt-source transfer and finds the wrong universal category: positive-measure Laplace translation minors have a fixed alternating sign, while reversing orientation yields universal Hankel total positivity. A useful transfer would therefore need a genuinely different source-compatible relation; AF-373 does not rescue the direct WP-324 Laplace route merely by thinning its sampling set.

The common criterion is whether the destination relation survives honest nuisance propagation and is nonautomatic in the matched-control category. Exact nuisance removal, exact source fingerprints, unbounded numbers of tests, global accessibility, or exact reversible access to a quotient do not by themselves make the final statistic source-selective or computationally usable.

## Which noncompact source channels and adaptive representation costs must survive a positivity reduction?

Weil Inertia has moved from a frozen essential-spectrum obstruction to a finite-sector complexity problem. WI-309--WI-310 show that adaptive reserve lifting needs a bounded retained tail to avoid bookkeeping-generated deep negative modes. WI-311 shows that smooth frequency multiplexing can make the capped retained tail arbitrarily close to `(1/2)I`, but then the complementary comparison approaches `Q-(1/2)I`; a maximal scalar floor can therefore be representationally empty.

WI-312 makes the compact cost of that flattening quantitative. If the aggregate frequency-energy law has concentration `Q_mu(h)<=eta`, the continuous archimedean correction satisfies a lower bound of order `eta^(-1)` in squared Hilbert--Schmidt norm. Thus anti-concentrating the carriers enough to flatten the tail forces this compact component to blow up in `S_2`. Smoothness of each localizer does not give uniform compact simplicity. The remaining theorem must control the **recombined finite negative sector** through source-specific cancellation or another invariant; uniform componentwise Hilbert--Schmidt control is no longer compatible with the flattening route.

A construction must still show that the exact bounded source budget dominates the finite sector uniformly enough for the aperture/complexity limit and then pass the signed-density quotient of WI-241. An impossibility theorem must survive admissible bounded-tail reserve/localizer choices and exact recombination rather than exploit an overcharged, nearly scalar, or componentwise-divergent decomposition.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is more specific, not closer by rhetoric. AF-373 separates all-order relational arity from determining sampling geometry. FD-152 shows that a macroscopic central anchor trades the fixed-seed `log log T` child path for a residual `sqrt(log log T)` Gaussian window rather than removing the bill. MC-323 forces varying-modulus fixed-bias directions to the quadratic individual-conductor boundary and saturates the natural source radical. NB-155 shows that every sublinear vertical-support widening only enlarges the adverse-debt localization radius at the same gap-normalized conditioning threshold. RE-159 extends terminal source ambiguity through every fixed matched moment order, with the next unresolved moment appearing at the corresponding root scale `epsilon_r`. PC-316 shows that the canonical pre-compression pointwise nonlinear coupling retains two indices yet classicalizes to a Mordell--Tornheim bundle. VIS-250 closes the exact communication-class ambiguity and leaves quantitative mixing as the real null-calibration gate. WP-324 closes the direct positive-source Laplace route to Schoenberg translation positivity as universal sign-regularity/Hankel positivity. WI-312 shows that smooth tail flattening necessarily spends Hilbert--Schmidt complexity in the continuous archimedean compact component.

For every proposed route ask: **what exact object reaches the destination; under which norm, scale, quantifier, topology, observation population, retained source moments, relational arity, sampling geometry and admissible source class; which universal/nuisance component has been removed and what debt or common amplification did that removal create; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, finite-moment matching, universal transform closure, generic positivity, determinant orientation, classical multiple-`L` recoding, slow mixing or near-scalar decomposition; and does a separate source theorem dominate the destination loss without reconstructing or assuming the endpoint?** A new representation is useful only when one of those answers changes mathematically.
