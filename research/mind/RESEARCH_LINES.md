# Global mathematical questions

## Which source resources survive the exact destination quotient, resolution and quantifier?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) separate source supply, stable transport, destination sensitivity and worst-case geometry.

Arithmetic Fidelity now resolves the target-relative Riesz recovery law. AF-362 shows that after quotienting common base attenuation, a declared target is priced not by hard bandwidth alone but by the exponential Picard moment of its scaled spectral-energy distribution. Even a diverging band can be recoverable when the target energy avoids the expensive tail; conversely upper-edge target mass pays the full exponential cost. The separate base-height variable still controls absolute attenuation.

Prime Lattice gives the complementary physical-side warning. PL-316/317 isolate the exact generic Sobolev margin needed by a moving Green observer, while PL-318 shows that even bounded zero-order forcing under the natural logarithmic endpoint operator can coexist with the moving-diagonal obstruction. The missing theorem must therefore use structural state--source coupling, not merely a small or bounded forcing norm.

Nyman--Beurling now provides a source-specific positive example rather than only a missing-law statement. NB-144 uses the actual Euler-product half-plane to impose a local Poisson-mass budget on zero packets. It strictly separates some NB-143 xi-type matched controls from zeta and forces logarithmic confluence to become sublogarithmic as the packet approaches the one-line, while leaving a fixed-interior logarithmic regime unresolved.

Robin Extremal now prices its local observer in the same spirit without supplying a source law: RE-145 shows that positive reciprocal-box packets lose visibility by at most `exp(-O(R))` in scaled radius, uniformly in local cardinality. This converts the earlier bounded/unbounded geometric distinction into an explicit rate that must be compared with the exterior error budget.

The common requirement is a **coupled-scale modulus in the topology, quotient, absolute error floor, declared target, local source law and worst-case quantifier actually consumed by the endpoint**. Generic regularity or analytic realizability is useful only when it controls that exact destination currency.

## Which growing source law is informative rather than under-resolved, reconstructive, shareable or generically realizable?

Farey Discrepancy moves the lower information boundary substantially. FD-144 replaces the earlier square-root-rank blind regime by a sublinear-rank one: arbitrary source-independent normalized nonnegative ancestry observations with total complexity `o(r_T)` can still be fooled by one common infinite prime coloring along a subsequence. At central rank this is `o(log log T)`. The opposite boundary remains FD-142: only `Theta(log T)` bits in one exact additive coordinate can already reconstruct the squarefree prefix.

Möbius Cancellation now turns its second-order source demand into an exact matched-control frontier. MC-314 converts `log Z <= ((1+eta_R)/R) log y` into a generalized-weight defect of order `R eta_R` at the live rank. MC-315 then identifies the constant-defect endpoint with a diagonal projective generalized-cap problem for `2R` points in `PG(R-1,2)`: generic realizability is exactly whether the corresponding envelope `N_R` reaches `2R`. Standard incidence and coding bounds do not decide this diagonal regime.

Prime Circle remains the complementary conditioning example: exact low-dimensional algebraic identification can coexist with weak analytic separation. Across these lines, “number of coordinates” is not a meaningful scalar resource. One must price effective source-state resolution, provenance, precision, cost sharing and the exact ambient realizability frontier of the same invariant.

## Which local zero laws survive after global density, functional equation and transfer arguments are exhausted?

Nyman--Beurling and Robin Extremal continue to expose the weakness of broad population envelopes, but both now have quantitative local geometry. In Nyman, xi-type entire realizability and global zero laws still permit target-poor logarithmic confluence, yet the actual zeta Euler product supplies a positive local Poisson budget that forbids part of that geometry and makes near-one packets sublogarithmic. What remains is the fixed-interior regime where the budget still permits `Theta(log G)` occupancy.

In Robin, RE-144 permits target hiding inside a near-left cluster whose reciprocal scaled radius may diverge arbitrarily slowly, while RE-145 proves that the normalized visibility loss of a positive reciprocal-box packet is at most exponential in that scaled radius and that this order is sharp in the matched class. The live source theorem is therefore not merely “keep the radius bounded” but make the `exp(-O(R(U)))` local floor dominate the exterior cancellation budget, or impose relational structure that excludes the matched product organization.

The two obstructions use different target functionals and cannot be identified. The shared lesson is now more precise: **generic upper population control does not give worst-cell target coercivity, but a local law that prices the exact exceptional geometry can convert a qualitative obstruction into an explicit rate comparison**.

## Which arithmetic quantity remains after universal source mass is quotiented away?

Weil Inertia now separates total unscreened mass from the signed quantity seen after the explicit formula. WI-301 proves that complete one-signed dyadic screening forces a uniform archimedean reserve that a nonpositive test must repay through arithmetic interaction. WI-241 then shows, parity-free, that the pole cancels the density-one prime main term exactly; the residual source variable is the smoothed signed discrepancy of `d(psi-x)`, together with a bounded nonnegative density-one autocorrelation term.

Therefore “some prime-power mass survives” is not yet coercivity. The live bridge must show that the forced source contribution survives the density-one quotient with the correct sign. This is another instance of the global rule that source abundance and destination sensitivity are separate resources.

## What constitutes a representation-faithful null after nuisance variables are removed?

Visual Exploration now separates two distinct calibration operations. VIS-237 shows that averaging same-cell collisions over grid origin is exactly the triangular pair-gap functional `sum_(a<b)(1-|x_b-x_a|/ell)_+`: an exact quotient of nuisance phase, not a stochastic null. VIS-238 then shows that an unconditioned Poisson baseline adds a leading variance channel from total-count fluctuations, whereas the observed prime-coordinate sample is fixed-count.

A representation-faithful null must therefore match the sigma-field fixed by acquisition before source significance is assigned. For this statistic that means quotient grid origin exactly, condition on the observed point count, and only then choose a frozen ordered-point/gap law that still varies the statistic. Fixed-count uniform order statistics are a minimal homogeneous calibration, not a claim about the true prime gap law.

## Which scalar or local reductions remain structurally capable of carrying arithmetic specificity?

Weil Positivity continues to reject universal or fixed finite-degree scalar Julia repairs: WP-314--WP-317 show that the natural empty-side tangent is universal, bounded prime clusters destroy local compactness on the support side, isolated dominance scalarizes, and finite multipole Julia reduction terminates in classical rational Herglotz data. A viable scalar route must therefore produce a genuinely unbounded collective source geometry or leave that category.

Xi Flow remains a different global-local problem: its local packet inversion is well conditioned once the packet is known, but support discovery, multiscale assembly and transport from theta-side control to a zero-sensitive destination remain the unresolved gates.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current corpus repeatedly separates nearby notions that broad prose tends to collapse: absolute attenuation from target-relative Picard cost (AF-362); growing observation count from source coercivity (FD-144); first-order source scale from the diagonal projective realizability frontier (MC-314--MC-315); xi-type analytic realizability from the local Euler-product Poisson budget (NB-143--NB-144); generic critical/bounded-forcing control from moving-sample anti-concentration (PL-316--PL-318); shrinking physical neighborhoods from the exponential reciprocal-radius visibility law (RE-144--RE-145); nuisance quotienting and fixed-count calibration from a stochastic null (VIS-237--VIS-238); and total arithmetic reserve from signed density-subtracted discrepancy (WI-241).

For every proposed route ask: **what exact quantity reaches the destination; at what scale and precision; what target or local observer consumes it; what universal component is quotiented out; what exceptional configuration still survives; can a matched non-arithmetic object realize the same invariant; and which source-native theorem excludes that matched control?** The productive next theorem is the one that closes one of those explicit gaps without reconstructing the answer or importing the endpoint in disguise.