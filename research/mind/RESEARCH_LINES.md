# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Move the strongest matched control all the way to the actual source-conditioned category without subtracting the target arithmetic

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Analytic Frontier has now closed the equal-height five-point diagonal and a fixed support-free unequal-height cone; for the Montgomery--Taylor profile any remaining scalar zero must have relative mismatch `q>0.1409`. The scalar frontier is the residual fixed-kernel zero-freeness problem, not another support/notch tuning exercise. Visual Exploration supplies the statistical analogue: a common Fisher gauge must be fixed before comparing residual directions, and even the pooling/KL direction is load-bearing because linear pooling can manufacture higher-order interaction from lower-order panel heterogeneity.

Xi Flow supplies the strongest current source/control separation. A critical `q~log^2 T` memory wave survives universal fixed heat time, while the endpoint explicit formula excludes it in a compact prime-free positive-frequency band. The carrier now exists canonically through complex roots, collisions, and the infinite Xi limit. The decisive move is therefore to carry the strongest matched control to the **same source-conditioned observable and scale** used by the theorem, then prove that the source-only residual survives the destination transport rather than being removed by the representation itself.

## Separate missing relation, completed encoding, averaging, analytic transport, and arithmetic zero selection

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Prime Lattice shows that addition is genuinely absent from the multiplicative exponent quotient, but preserving it syntactically is not enough. PL-172--PL-175 show that regularized determinants, uniform shift averaging, and theorem-accessible logarithmic low-order sectors delete or universalize the additive channel. PL-176 now closes the canonical degree-four escalation: complete additive-parallelogram averaging is an ordinary Fourier fourth moment and is Gowers-flat by classical multiplicative-function cancellation.

The live theorem must therefore distinguish at least four gates: the relation is genuinely absent from the old quotient; the source selects a non-diffuse coupling/weighting; the analytic carrier preserves it through regularization, averaging, continuation, or limiting topology; and only then does a sign/coercivity/zero-selection theorem use it. An uncontrolled higher-order sector is not itself a mechanism.

## Match infinite assembly to a saturated witness profile, source-fixed metric, operator ideal, and canonical limiting topology

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Arithmetic Fidelity now gives both the composition and metric repairs exactly. AF-134 constructs the minimal backward witness saturation needed for a fixed chain. AF-135--AF-138 show that under full generator gauge target fidelity is projection geometry; compact symmetry still leaves the positive commutant metric cone, while an independently source-specified metric upgrades the invariant object to the generalized pencil `(G,M)` and its generalized Picard tail.

Prime Circle and Prime Flute provide complementary operator boundaries. PC-178 shows that primewise closure of the raw resultant is nonclosable/scalarizing. PF-169--PF-171 show that the **complete** short central sector is not the missing singular-value problem: squared blocks are trace-summable and first-resolvent blocks have exactly the sharp `S_r`, `r>1`, threshold, failing only at `S_1`. The remaining Prime-Flute burden is uncut body/interface/transmission assembly. A positive theorem must specify witness saturation, metric/gauge, domain/ideal, and global identification from the source rather than infer them from finite pieces.

## Find a source-fixed signed residual with genuine information deficit, beyond generic complement recovery

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Möbius Cancellation separates two necessary resources. MC-082 proves that unsigned local divisor-density information can be blind to Liouville parity. MC-083--MC-087 then show that restoring sign is still target-equivalent whenever the discarded information is cheap enough to restore generically: this includes direct parity/sawtooth carriers, sufficiently resolved Fourier and reciprocal-slab truncations, and arbitrary physical-space omissions with target-subordinate support.

A live source-fixed residual must therefore be **signed and genuinely under-resolved**. It must deliberately cross the generic reconstruction boundary and obtain a new arithmetic estimate on the discarded signed mass, or close a joint recurrence without reconstructing that complement at all. Properness, sparsity, selectivity, and source origin are not information-deficit certificates by themselves.

## Cross the actual source-level singularity threshold with a source-funded scale, or leave inertia regularization universality

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Weil Inertia closes both smooth and naive singular repairs around confluence. Continuous spectral detectors lose mass; Schur normalization cancels the collapsing horizontal scale; and autonomous quantized flag depth is too expensive. WI-168 makes the latter quantitative: any nested integer-depth correction paid from the same Lamzouri deficit satisfies `rank J <= 4 Delta`, with quadratic cost in depth. Near saturation it cannot create a macroscopic interaction layer.

Any remaining singular/coercive route must therefore have an independently evaluated source scale or interaction absent on the isolated confluence control. The theorem funding the new layer is itself the missing information; discontinuity, inverse normalization, or repartitioning existing slack is not coercivity.

## Couple provenance before positive scalarization, algebraic incidence, closure, or averaging classicalizes it

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Weil Positivity now exposes the sign boundary unusually clearly. WP-161--WP-162 show that the primitive cyclotomic radial path carries `Lambda(n)` exactly as the total signed inward flux, and that the flux is pointwise positive for all radial times exactly on prime powers. Yet mixed-prime Mangoldt zeros arise from positive/negative cancellation, so shellwise positive norms, energies, or damping acquire full support and destroy the selector.

Prime Circle shows that primewise closure of the raw unbounded resultant fails, Prime Lattice shows that diffuse additive averaging becomes generic pseudorandomness, and Prime Flute shows that bounded/local tail spectralization transfers to the composite clone. A live cross-line mechanism must therefore introduce and retain a **source-forced nonseparable signed relation before the operation that completes, closes, averages, or makes it positive**, and prove that the selector survives the actual destination operation.

## Control the shrinking endpoint of the exact Xi positive-frequency transport

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

XF-047--XF-048 isolate the source-specific memory selector. XF-049--XF-050 show that finite one-sided transport survives complex roots and collisions and that a compact endpoint band can be made exactly prime-free. XF-051 supplies the infinite carrier: the horizontal logarithmic derivative defines an auxiliary-height-independent positive-frequency distribution whose Burgers evolution is exactly Volterra-triangular.

The highest-leverage Xi theorem is now narrower than “construct an infinite transport.” It is to control the deterministic background/fluctuation decomposition as the selected band approaches `xi=0` at scale `1/log T`, proving that the lower-positive-band nonlinear input is `o(1)` over the relevant heat interval. A failure must identify an order-one source term from the endpoint singularity or frequencies below the memory band; high/negative frequencies, complex zeros, collisions, and raw-zero-sum divergence have been removed as structural explanations.
