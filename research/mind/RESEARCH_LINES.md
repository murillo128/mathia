# Global mathematical questions

## Which independently selected source object can change spectral realization before producing a nonlocal positive law?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) separate nonlocal realizability, independent model selection and spectral-type compatibility.

WP-267--WP-274 show that hostability, category descent, scalar sign, localizable compression and inverse construction all fail as free routes to the desired Weil orientation. Clark/de Branges machinery can manufacture a positive nonlocal model from the desired normalized carrier itself, so existence alone is a representation tautology when the target measure is supplied first.

WP-275 adds an orthogonal source-side obstruction. The natural arithmetic log-energy carrier is pure point while the pole continuum has nonatomic absolutely continuous multiplication spectrum. A bounded map that exactly intertwines the canonical modulation groups is therefore zero. The live bridge must be independently selected **and** must change, twist, enlarge or break the source-to-continuum spectral realization before its positivity law is compared with the Weil carrier.

## Which source restrictions actually shrink the target-reachable failure set?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) distinguish nominal source reduction from the geometry actually seen by the destination.

AF-300 proves that a genuine complex-affine source dimension changes the leverage bill, but every surviving direction remains visible. AF-301--AF-302 replace deleted linear directions by positive reachable cones and normalized convex bodies. AF-303 closes a fixed source-independent coefficient cap, and AF-304 now closes fixed support cardinality as a standalone repair: three available forgotten slots already recover the full planar convex hull, while an explicit two-term prime cancellation defeats every `k>=2` support cap at arbitrarily large height. A useful restriction must shrink the **actual fiberwise reachable body** through geometry-adaptive, discrete, multiplicative/nonlinear, support-pattern or finite-window structure rather than merely make coefficients smaller or sparser.

PF-315--PF-316 give the complementary operator statement. The full physical angle is `(T_HV_H,Z_LH)^T`, but `V_H` cannot hide a bad conditional endpoint for free: its deviation from a unitary is quadratically paid by `Z_LH`, and compactness/weak trace of the full angle is equivalent to compactness/weak trace of `(T_H,Z_LH)`.

Prime Circle and Visual Exploration sharpen the observation-scale bill. PC-274 proves the raw ordered near-resonance law prime-blind at **every** bandwidth, and PC-275 identifies the first centered fixed-bandwidth correction as the classical logarithmic-integral source bias. VIS-196 removes opposite-sign collisions from the quadratic dephasing frontier and reduces the remaining same-sign crowding to taper-weighted one-parameter affine prime-gap systems. Information content and target sensitivity must be priced at the normalized residual actually consumed.

## Which residual cancellation theorem survives after generic recurrence, coding and matched-control bounds are exhausted?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md) now places both Farey and Möbius beyond their previous generic barriers.

FD-081 reaches physical horizons with subpower first passage, but FD-082 proves that this recurrence currency can still be far too sparse for fixed-per-hit accumulation. It forces only super-`log log X` counting and is compatible with an integer-ratio chain of size `(log log X)^2` and divergent logarithmic horizon entropy; a fixed gain at every good horizon can still total only `X^(-o(1))`. The next Farey input must strengthen distinct-horizon density, anti-concentrate annular energy, or extract a gap-sensitive/nonlocal gain.

MC-252 removes quadratic reciprocity itself as a generic fix. MC-253 then shows that tight shell localization does create bounded-order Legendre pseudorandomness, but that information is still rank-blind: a logarithmic-rank Walsh control has exact pairwise half-distance and the same qualitative fixed-order parity balance. A quadratic improvement must therefore reach growing parity order, a direct shell-specific rank obstruction, or signed cancellation of the rough Möbius coset.

## Which source-specific inequality prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia continue to close complementary generic escapes. PL-289 turns the actual one-dimensional Weil eigen-equation into a squared-log Fourier moment and an `O(log^-2(1/|delta|))` translation modulus. This removes the ambient regularity barrier but leaves the quantitative constant far too large for the tiny certified endpoint margin; the next input must come from arithmetic cancellation, a much smaller state-specific constant, or interval-wide certification.

WI-264 changes the live Weil-Inertia gate. Certified unrestricted positivity at radius `0.8` already implies that any hypothetical first crossing lies beyond `0.8`; recertifying FP-0.35 is therefore not required merely to activate primes. More importantly, at every finite aperture an endpoint-bump autocorrelation can screen **all** active von-Mangoldt lags simultaneously while keeping the continuous source on favorable signs. Adding finite prime layers cannot force a negative phase defect. The next theorem must use the exact null equation or quantitative firstness lower envelope to forbid that screening geometry.

Weil Positivity supplies the global counterpart: neither inverse construction of a positive host nor an exact pure-point-to-continuum time-covariant bridge provides the required source-specific sign mechanism.

## Which residual channel remains after exact local structure and healthy-control boundary costs are factored out?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md) now has sharper calibrations on both Nyman and Robin sides.

NB-080 shows that a fixed-ratio prediction-volume ledger can diverge linearly on a boundedly invertible outer source with zero stable tail. NB-081 makes variable future depth exact by adding a nonnegative screening charge and computes the matched-control threshold: `J~R rho^(2L)` for fixed invertible `rho<1`, so logarithmic descendant depth removes the false positive, whereas the outer noninvertible endpoint has `J~R/(L+1)`. The Nyman problem is therefore to derive the source-specific screening-depth/conditioning law or a residual that survives arbitrarily deep screening.

RE-083 removes endpoint support mismatch and direct linear timing as standalone repairs. RE-084 goes further: the endpoint prime set reconstructs the entire first-layer event timeline, which is uniformly a half-translation of ordinary primes at late scale; fixed nonlinear one-prime timing statistics add no independent assembly channel. The Robin obstruction has moved strictly to cross-layer/state-selection geometry—mixed threshold ordering, chamber admissibility, tied spans and supporting slopes.

The common question is now stricter than “which residual is large?” After exact cancellation, test the surviving quantity on healthy matched controls **at the depth, normalization and target kernel actually used**. A shallow outer boundary cost, a first-layer coordinate already reconstructible from endpoints, or a source discrepancy that vanishes after normalization is not yet the desired obstruction.

## Can the two genuine flute angles satisfy the required ideal count?

PF-316 removes the last residual-source ambiguity at the endpoint. Sequential elimination still gives the exact block-column representation, but singular-value recovery shows that `T_H` cannot be hidden by `V_H`; attenuation is paid quadratically by the direct angle `Z_LH`.

The next target is therefore exact: determine the compactness/weak-trace singular-value laws of `T_H` and `Z_LH` on canonical tails, then close the compatible infinite realization, symmetric reassembly and signed target composition gates. Failure of either channel is already a full physical-angle obstruction.