# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing back to Möbius or erasing Weil localization?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate source provenance, admissibility, target sensitivity and genuine relational curvature.

Weil Positivity has now closed substantially more than one-channel scalar relocalization. WP-293--WP-294 show that exact scalar recovery, even when hidden behind exact equality on the full Weil square cone, forces Möbius inversion. WP-295 extends this to fixed positive Hilbert-valued pointwise channels: prime-power support leaves one visible direction `u Lambda`, with kernel `u mu`.

WP-296 closes linear multiplicative-equivariant nonlocal networks. Every coefficientwise-continuous equivariant linear map is operator-valued Dirichlet convolution, and finite channel depth collapses to one effective kernel. WP-297 then closes flat site-dependent boundary transport: a multiplicative cocycle is exactly gauge-trivial, and a parallel positive metric becomes site-independent after the same gauge. Exact Weil localization therefore still returns to the visible Möbius direction.

The open architecture must contain **gauge-invariant mixed-prime information before scalarization** or leave that linear source-covariant category. The smallest linear certificate is nontrivial prime-diamond holonomy; alternatives include a source-forced nonparallel metric, extra boundary topology/orbits, nonlinear support-sensitive geometry upstream, or a genuinely global finite--archimedean operation. Any such escape must still distinguish the Riemann system from free generalized-prime clones and supply an independent sign theorem.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) separate exact incidence, conditioning, source supply and destination sensitivity.

Arithmetic Fidelity now has two sharply different endpoints. AF-336 proves that consecutive half-spectrum vanishing `s_1=...=s_floor(n/2)=0` rigidifies the entire unit-circle fibre to a rotated regular polygon; a nonzero prime-time tuple cannot realize it. But the actual even middle-singular target remains weaker. AF-335 shows that `e_m=0` leaves exactly one real product phase missing locally, while AF-333's strongest current nonintersection theorem is still conditional on Schanuel. The unconditional problem is joint algebraic dependence on the one-parameter prime-phase tuple, not endpoint classification.

AF-337 separately turns finite sign complexity into an explicit full-Euler observation theorem. Fixed-order sign regularity survives the Euler harmonic tail once `sigma>(h+1)binom(r,2)` and the tail falls below the first-harmonic determinant margin. This is a real quantitative bridge, but it lies deep in `Re s>1`; the remaining question is whether a source-forced small sign budget can be transported toward a zero-sensitive destination without paying away the information one wants to use.

Nyman--Beurling has now separated conditioning from continuum target access at arbitrary high-zero rank. NB-113 makes every finite common-ordinate packet exactly orthogonal after canonical deflation. NB-114 shows that any finite packet above height `G` has Burnol target mass at most `d/G^2`; actual zeta supplies only `O(G log G)` zeros in a fixed-ratio shell, and the entire high positive tail contributes only `O(log G/G)`. Relative-ordinate shear can therefore be huge while classical target occupation vanishes. Any surviving multi-zero mechanism must create a **finite-section arithmetic target effect** absent from the continuum model-space projection.

Prime Flute has moved from an unresolved local test to a falsified transport architecture. PF-337--PF-340 reduce normalized-insertion translation locality to the seam-scale scalar `Delta_n^-+Delta_n^+`. PF-341 proves both adjacent pant contrasts have the same positive sign and `Delta_n^sym/w_n` diverges. The PF-336 translation route is therefore closed; the independent direct Hilbert--Schmidt completed-angle route remains live.

## Which cancellation theorem remains after local algebra, scalar decoding and generic perturbative shortcuts are closed?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track the relocated endpoint costs.

Farey Discrepancy has now classified the canonical local refinement carrier much more completely. FD-114 proves that the full denominator-truncated Stern--Brocot refinement movie of a unimodular parent, at relative horizon, is a function of one ratio `b/d`; at fixed absolute order it adds only the parent scale. FD-115 shows that every centered descendant discrepancy inside one parent cone is also determined by that local movie. The only discrepancy coordinate left locally is the **common absolute anchor** `D_Q(A)`. A surviving second coordinate must therefore be genuinely global/cross-cone, cross-horizon, source-conditioned, or otherwise not reconstructible from the parent ratio and scale.

Möbius Cancellation has acquired an exact parity split in the arithmetic-image endpoint obstruction. MC-285 shows that even shell weights face a deterministic equal-cell pairing barrier forcing roughly logarithmic coordinate support and conductor `exp((1/log 2+o(1))log y log log y)`. MC-286 proves that the odd support obstruction is exactly affine: an odd endpoint row exists iff `0` lies in the affine hull of the occupied `F_2^d` sign cells, equivalently iff no generated product character stays identically `-1` on the shell. The live arithmetic theorem is therefore to rule out those constant-negative generated directions in growing support, not to re-prove full Boolean pattern realization.

Prime Lattice has narrowed the zero-order Hadamard route to a state-side problem. PL-300--PL-304 rule out generic spectral/ambient forcing-space smoothing into BV. PL-305 proves that once a fixed state is already in `C_0 cap BV`, every lower-order prime-shift and completion term is `C^1` in the aperture, including through prime-power activation. The missing theorem is uniform source-structured BV/derivative-measure control for the actual varying ground branch, followed separately by branch differentiation/boundary stress and a rational-prime-specific sign law.

## Which exact null-mode operator constraint survives sharp support relaxation in Weil Inertia?

[MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track the shift from support geometry to operator-domain information.

WI-284 remains the operator target: complete one-signed screening deletes the prime translation form on the whole supported form-domain subspace, so the prime-deleted gamma-plus-pole form must be PSD there with bottom exactly zero. WI-285 shows every fixed finite prime-base shadow is too weak.

WI-286 calibrates the first growing-family compression. Baker--Harman--Pintz (`theta=0.525`, peer reviewed) and Li (`theta=0.52`, current preprint) both force exponentially thin two-island widths but remain on the wrong side of the strict square-root spectral threshold. WI-287 sharpens that compression to the exact critical-scale endpoint

`lambda_width=(1/2)log x-log F-F+O(1)`, `F_*(x)=W(sqrt(x))`.

The next theorem must either cross that Lambert-`W` width endpoint or exploit what nearest-prime compression discards: simultaneous full-family screening, null-equation amplitudes, richer support topology or another collective source coupling. The current numerical short-interval input is no longer under adversarial uncertainty; its evidence tiers are explicit and do not change the structural conclusion.

## What should Prime Circle do with an observable that RH itself makes non-prime?

PC-285--PC-287 show that the one-profile source-placement law collapses toward the Li model under RH nearly up to rational square-root denominator scale, with the current clipping/transport proof leaving a polylogarithmic loss. Persistent order-one prime-versus-Li separation in that range would contradict RH, while collapse does not imply RH. The residual one-profile frontier is the square-root neighborhood or a different RH signature; otherwise the architecture should move to cross-level, multi-source, directional or noncommuting observables.

## Which Robin recovery obstruction remains after even the moving endpoint ray is shown compatible?

RE-112--RE-114 force finite-edge phase locking but show that every fixed admissible Chebyshev/Mertens edge ray is syndetically realized. RE-115 derives the exact edge-normalized adaptive exponent `b_Y=1-Theta+1/log Y` and an order-one endpoint ray along every nondegenerate near-minimal family.

RE-116 closes the diagonal loophole. With `u=log Y` and `J=F_Theta+G_Theta`, the self-consistent moving-ray residual is exactly `(uJ(u))'/u`; positive weighted critical points occur syndetically with a uniform amplitude floor. Even the actual logarithmically drifting selector ray is therefore spectrally compatible.

The live attained-edge theorem must control **arithmetic selector placement inside those bounded-gap compatibility windows**, use lower-order information finer than the leading edge face, or couple recovery geometry to placement in a way almost periodicity does not preserve. The `Theta=1` branch remains the separate unconditional RE-109--RE-111 problem.

## How large can a genuinely pointwise prime-phase fluctuation be after gauge-invariant frustration is priced?

VIS-211 suppresses the one-cancellation family in strictly subcritical long-start mean square, while VIS-193--VIS-202 control finite averaging horizons. VIS-213 resolves the fixed-dimensional pointwise existence question: the start orbit is dense in the prime torus and the worst start equals the unit-modulus quadratic optimum `max |z^*Az|`; near-maximizers recur arbitrarily late.

VIS-214 identifies the invariant obstruction to saturating the coefficient envelope. Vertex phases are a gauge; cycle holonomy is the surviving quantity. Balance/antibalance is necessary and sufficient for exact envelope attainment, and a weighted frustrated cycle gives an explicit deficit. The live problem is now to compare **heavy cycle frustration**, the Haar RMS scale and the growing-dimensional Diophantine access time. Raw edge-phase disorder and fixed-dimensional recurrence are no longer the right questions.

This also supplies a concrete cross-line structural lesson with WP-297: site/vertex dependence that disappears under a local gauge is not yet a relational arithmetic resource. Closed-loop holonomy is the first invariant worth pricing, although the visual gain graph and the Weil boundary transport are mathematically different objects.