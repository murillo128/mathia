# Global mathematical questions

## When does repeated observation actually shrink the admissible source fibre?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md), [MI-022](intuition/MI-022-unbounded-compatibility-can-turn-finite-cutoff-ambiguity-into-a-stability-problem.md), and [MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md) separate retained information, common-source compatibility, conditioning, acquisition geometry, aggregate readout capacity and final destination selection.

Arithmetic Fidelity now separates three resources at smooth order three. AF-394 identifies the exact continuum target: `q=-(log f)''` must satisfy `q>=0`, `(log q)''<=2q` on `{q>0}`, and `{q>0}` must be connected. The shrinking solid hierarchy recovers the first two analytic conditions but not component topology. AF-395 shows fixed successor radius can miss that topology on a vanishing prime-log mesh. AF-396 locates the first-order reach wall at `(e-1)N`; AF-397 shows detection needs only one local mesh width of overhang past physical log-prime reach one. AF-398 resolves the former equality scale one order further: with `L=log N`, `ell=log L` and `R_N(beta)=floor((e-1)N-eN/L+beta N ell/L^2)`, `beta<e` is eventually blind and `beta>e` detects. Thus

`R_N^crit=(e-1)N-eN/log N+eN log log N/log^2 N+O(N(log log N)^2/log^3 N)`.

The exact physical count `pi(e p_N)-N` has the same expansion. **Resolution is still not relational reach**: the topology costs linear prime-index reach, but once the physical wall is fixed, successive prime-density corrections reappear as combinatorial breadth corrections.

Farey Discrepancy now makes the source-class effect sharper. FD-171 proves consecutive midpoint horizons have zero exact redundancy over unrestricted formal sources. FD-172--FD-174 show that bounded squarefree multiplicativity turns the transformed defect into nonnegative mass, makes sparse orbit-hitting criteria rigid and makes every fixed integer geometric ladder finitely certifying. FD-175 identifies what prevents recursive sparse-shell repair: the decisive resource is not coefficient accuracy at one horizon but **cross-coordinate multiplicative compatibility**. If that global product law is removed, sparse midpoint agreement can be repaired shell by shell while the target remains different. The open issue is therefore the sharp certification scale and how much of the multiplicative source law is actually necessary for destination rigidity.

Robin Extremal gives the converse warning about source tariffs and finite scalar constraints. RE-180 proves that honest nonnegative prime multiplicity forces any early two-jet repair to pay `Omega(sqrt(p)/log p)` actual deletions unless it pays a remote KV reset budget. RE-181 realizes that tariff with `{0,1,2}` ordinary-prime multiplicities while matching the first two Euler boundary coordinates. RE-182 extends the construction to **every fixed finite jet depth** at `s=1`. RE-183 now shows that moving to finitely many separated Euler temperatures does not help: every fixed panel `1=sigma_0<...<sigma_m` can likewise be matched exactly by bounded honest prime repairs, with the same KV fidelity and transient Robin excursion. Finite-dimensional scalar Euler information remains interpolable whether expressed as boundary derivatives or finitely many point values. The live distinction is growing-dimensional/joint source information or selector-coupled placement geometry.

Together AF-398, FD-175 and RE-183 sharpen the multiscale lesson. Raw scale count, ambient row rank, fixed constraint count and lower-bound tariffs are not intrinsic information currencies. One must declare the admissible source fibre and ask which relations remain accessible at the destination's physical scale: arbitrarily fine local sampling may still require linear-in-index reach to cross a topology wall, with its asymptotic breadth inherited from source density; nonlinear cross-coordinate compatibility can collapse an ambiently repairable sparse observation system into a rigid one; and a large discrete source tariff plus any *fixed finite* scalar Euler family can remain nonselective when a matched source construction realizes it.

## Which quantitative resource reaches a thin or moving destination?

Prime Lattice has reduced the first-prime gate to one concrete state-space estimate. PL-329 gives the sharp source-amplitude condition for the shrinking reflected sign; PL-330 transports isolated eigenbranches in norm-resolvent/`L^2` topology; PL-331 proves every fixed logarithmic Fourier seminorm still misses the hostile moving layer; PL-332 transports the selected asymptotic coefficient once the endpoint remainder is family-uniform. PL-333--PL-334 block the ordinary full/even Hopf shortcuts, while PL-335 finds the exact state-specific odd mechanism and forces a noncollapsed threshold half-log boundary amplitude. PL-336 then shows that a uniform `L^infty` bound on the normalized simple odd branch would simultaneously give `S_delta=O(1)`, the uniform endpoint remainder and `C_delta->C_0>0`, closing the PL-329 ratio. The remaining first-prime burden is therefore principally **uniform state boundedness/concentration control**, not a separate search for another fixed-threshold positivity mechanism.

Visual Exploration now has a scale-sharp theorem-transfer law and a structural no-go for the obvious shrinking-jet continuation. VIS-259--VIS-268 separate packet conditioning, universal pole background, prime-power residual, Wang support/seminorm budgets and complete leading-functional cancellation. VIS-269 extends the pole/prime-power decomposition to every fixed even jet order and proves that a shrinking packet whose Fourier support eventually lies below the first prime frequency is exactly prime-power blind at every fixed even order. The diagonal-safe quadratic carrier then vanishes as well. More fixed moment cancellation cannot rescue this representation. A viable source-specific route must change support geometry — fixed-width, translated or multiband access to prime-power frequencies — and then reprice the theorem error for that altered packet.

Nyman--Beurling now separates worst-case, typical-height and quotient-correct acquisition. NB-176--NB-182 price genuine Euler amplitudes in Hilbert and Wiener geometries and expose the generic `sqrt N` dense-code tariff. NB-183 shows that the worst-case source norm is not a typical-height obstruction. NB-184 then uses the exact zero-mass observation quotient: signed samplers see the Euler kernel only modulo constants, so anchoring before regularity estimation removes an invisible mode. The resulting Gagliardo--Nirenberg/mean-square estimate reaches the quadratic scale: if `a->0` and `T a^2->infinity`, then `sqrt(a) mathcal H_(a,B)(t)->0` in relative measure and the charged scalar acquisition ratio beats the generic `sqrt N` packing threshold on density-one heights. The live problem is coherent source-natural rank/phase reuse on a common good-height set, plus the genuine quadratic endpoint `T a^2=O(1)`, not scalar exposure on typical heights.

Xi Flow gives the inverse lesson: once the correct consecutive packet is known, local holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local inversion itself.

## How much source information can an analytic architecture exploit at bounded conditioning?

Möbius Cancellation now separates acquisition syntax from destination-relative reliability geometry. MC-338--MC-345 show that bounded-energy dephasing requires extensive joint source information and that fixed-quality noisy access pays channel capacity. MC-346--MC-347 add the logarithmic reliability tariff and replace raw query count by accumulated discrimination. MC-348 removes the raw-coordinate model: vanishing average full-source error forces `Omega(R log(1/bar p))` symmetrized KL across the natural neighboring-state graph. MC-349 identifies the exact quotient refinement. For a destination `q`, only neighboring source edges on which `q` changes are compulsory; if their average and maximum cut degrees are `kappa_q,Delta_q` and their source-averaged transcript divergence is `mathfrak J_q`, then `p_q >= [kappa_q/(4Delta_q)] exp(-mathfrak J_q/kappa_q)`. A one-bit endpoint can therefore demand extensive reliability when its cut crosses many source directions, while a high-dimensional source can be cheap when the destination quotient is constant along most edges. The live arithmetic question is to identify the actual Möbius endpoint quotient and bound transcript divergence on its cut.

FD-169--FD-175 and NB-178--NB-184 provide complementary warnings. Unrestricted acquisition can hide the inverse in the sensor; changing horizons can hide many rows behind one spatial coordinate; strong nonlinear source constraints can turn a formally repairable sparse system into a rigid one through cross-coordinate multiplicativity; integer arithmetic can make qualitative recurrence finitely certifiable; enlarged duals can manufacture generic metric phase codes; genuine Euler labels remain nonselective if amplitudes are normalized away; the correct non-Hilbert Euler topology carries a Wiener acquisition tariff; and quotienting exact invisible modes can make that tariff affordable on most heights without constructing coherent high-rank reuse. Source-information cost is meaningful only after source class, target quotient, acquisition topology, operator variation, amplitudes, matched controls, quantifier and downstream resolution are fixed.

This updates [MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md): measurement count, nominal dimension, positive noise, adaptivity and canonical probe labels are secondary. The load-bearing quantities are inverse modulus, orbit-hitting and certification scale, source-class-relative kernel, destination-cut geometry, per-access capacity, source-edge transcript divergence, source-amplitude conditioning, exact observation quotient, acquisition-space norm, height quantifier and the number of genuinely independent operator/source states required by the destination.

## Which spectral or positivity enrichment survives its intrinsic quotient and density law?

Prime Circle has reached both a classical autocorrelation boundary and an exact binary matched-control boundary. PC-325--PC-330 establish the phase/autocorrelation quotient, universal Hilbert background and failure of smooth Li subtraction. PC-331 writes the source-sensitive excess as a positive Hankel/Hardy-square energy of Fourier magnitude squares, PC-332 decomposes it dyadically, and PC-333 identifies its reciprocal-scale band theorem with cyclic short-interval prime-discrepancy variance. PC-334 then closes the literal binary-source loophole: after complemented Li subtraction, a binary prime support and its literal nonprime complement have centered normalized residuals that are exact scalar negatives, hence identical Fourier-power profiles, Green--Hardy energies, cyclic variances and normalized Green/Hilbert spectral data. The normalized magnitude-only branch is therefore matched exactly inside the binary category. A surviving route must retain nonseparable phase/source information or unnormalized amplitude that this control cannot fake, and still add an independent zero-sensitive theorem.

Weil Positivity has narrowed the Maaß--Selberg escape past generic arithmeticity. WP-345 shows the positive height boundary square is divisor-blind; WP-346 realizes the scalar CDD control with positive self-adjoint Robin channels; WP-347 moves the control onto the same one-cusp hyperbolic surface via a point interaction. WP-348 now finds the same logical freedom inside unperturbed squarefree congruence scattering: ramified local factors are source-defined reciprocal unitary-axis factors with explicit off-critical divisors. Moreover fixed channel combinations cannot strip one bad-prime logarithmic derivative while retaining the common completed-zeta block. The surviving route must distinguish the **unramified global normalizer from ramified local dressing before scalarization**, inside a genuinely positive arithmetic mechanism; broad labels such as Hecke, Euler or congruence arithmetic are not selective enough.

Weil Inertia gives a positive example of source correction removing a false resonance but now closes both obvious bow-height de-exceptionalization scales. WI-327 restores the exact signed coefficient `Lambda-Lambda_X^sharp` and kills the cubic resonance on density-one starts. WI-328 shows an entire still-live bow is twist-frozen on the endpoint cubic source block. WI-329 moves to the first natural scale where bow heights genuinely dephase, but the matching almost-all nilsequence theorem is already uniform over all fixed-degree polynomial phases on one common good set of starts. Height diversity therefore does not create independent exceptional sets. The remaining obstruction is the distinguished source start and, separately, the stronger `L^2` norm required by the exact covariance.

These lines support [MI-024](intuition/MI-024-control-subtraction-must-match-the-load-bearing-source-channel.md): a matched control or subtraction is useful only when it acts in the representation and norm carrying the alleged signal. Smooth density subtraction can miss sparse Hilbert energy; an exact source correction can cancel a generic resonance; a source-sensitive scalar may collapse to classical short-interval variance after a phase-blind magnitude quotient; an exact complemented binary control may reproduce the whole normalized magnitude package; phase diversity may leave one common arithmetic exceptional set; and even genuine arithmetic scattering may contain ramified divisor-mobile inner factors that positive fixed-channel geometry cannot strip selectively.

## Which source fingerprint is selective rather than merely reconstructive?

Robin Extremal and Farey Discrepancy give opposite source-class effects. FD-173--FD-175 show strong multiplicative compatibility can make extremely sparse horizon testing rigid and finitely certifiable, whereas removing that cross-coordinate law restores recursive sparse-shell repair. RE-181--RE-183 show that ordinary-prime support, bounded honest multiplicity, KV fidelity and **any fixed finite scalar Euler family tested so far** — boundary jets or separated temperatures — still admit explicit matched repairs that move the Robin destination. Source restrictions matter only when they remove the *destination-relevant* matched fibre, not merely when they make reconstruction more expensive or add finitely many scalar constraints.

Arithmetic Fidelity exactly separates local analytic recovery from global target topology and now prices the topology wall through a third asymptotic term in prime-index reach. Prime Circle classicalizes its normalized magnitude statistic and then supplies an exact binary complement control. Visual Exploration shows that fixed-order shrinking low-frequency jets become source-blind once their support leaves the prime spectrum. Nyman--Beurling shows that source fidelity includes charged acquisition topology and that exact observation quotients must be taken before regularity is priced. Weil Inertia shows the exact signed source removes generic resonance while both frozen and genuinely dephased bow-height families fail to repair the exceptional-start quantifier. Weil Positivity shows that even genuine congruence arithmetic carries ramified off-critical inner factors unless the unramified block is isolated by stronger pre-scalar structure. Prime Lattice now reduces its first-prime sign gate to a uniform moving-state bound after a state-specific odd Hopf mechanism closes threshold noncollapse.

The program must therefore separate **source fingerprint**, **admissible source fibre**, **target/observation quotient**, **destination-cut geometry**, **acquisition/transport topology**, **source amplitude and renormalization cost**, **relational reach versus spatial resolution**, **source-density corrections at topology boundaries**, **fixed versus growing source-coordinate depth**, **cross-coordinate compatibility**, **channel-matched control subtraction**, **conditioning/capacity/reliability**, **local analytic recovery versus global component topology**, **resource lower bound versus matched realizability**, **joint versus scalarized geometry**, **ramified versus unramified arithmetic structure**, **global operator positivity versus state-specific reflected sign mechanisms**, **exceptional-set quantifiers versus typical-height affordability and phase diversity**, and the final **destination-selective sign or zero theorem**.

## Global discriminator: identify the exact resource a matched control cannot fake

For each proposed route ask: what exact source distinction reaches the destination; what source class and target quotient are being compared; which source modes the destination annihilates exactly and whether they were quotiented before regularity was priced; what information is regenerated for free; whether horizon/operator changes contribute independent constraints and whether source restrictions collapse their kernel through repeated defect orbits or cross-coordinate compatibility; whether qualitative recurrence is quantitatively certifiable at the needed source scale; whether finitely many source coordinates can be interpolated by a matched repair and how their depth/dimension must grow to become selective; whether the inverse has been moved into the sensor; whether weak source channels were independently renormalized and at what cost; whether the declared acquisition topology contains the natural source readout and what gain it charges; whether that gain is worst-case, typical or pointwise at the destination's selected height; which neighboring source directions actually cross the destination cut and what transcript divergence they receive; what native norm gives a useful inverse modulus; whether finer sampling also increases relational reach or only shrinks the physical stencil; whether a topology crossing needs a fixed analytic margin or only overhang relative to mesh and how source-density asymptotics convert that wall into combinatorial breadth; whether a lower-bound tariff is actually unaffordable inside the source class; whether a larger witness class merely certifies generic metric packing; whether local certificates recover only the differential cone or also component topology; whether a control subtraction matches the norm/channel carrying the signal; whether a source-sensitive scalar still factors through a phase/autocorrelation quotient, a literal complemented-source control or a classical window-variance transform; whether a positive/self-adjoint realization still permits divisor-changing controls **inside the claimed arithmetic category** and whether ramified factors can be separated from the unramified block before scalarization; whether failure of global Markov positivity still leaves a source-specific reflected maximum principle on the actual state; whether an almost-all theorem controls the distinguished sequence or is already uniform over the available phase family; and what theorem finally converts the survivor into a Riemann-zero or positivity restriction.

A useful bridge should fail on a matched control for an independently arithmetic reason. Exact reconstruction, unbounded observation count, unrestricted target-aware mixing, one spatial sensor with uncharged horizon variation, ambient row-rank lower bounds that disappear inside the source class, qualitative orbit recurrence without certification, any fixed finite scalar Euler panel, genuine frequency labels with erased amplitudes, bounded Hilbert duality that excludes the natural functional, non-Hilbert acquisition with uncharged Wiener gain, regularity charged before quotienting invisible modes, typical-height affordability without coherent rank, arbitrarily fine sublinear-reach stencils, a large but realizable source tariff, a rich spectrum, a critical boundary, source labels, a positive principal form, global/even Hopf failure when an exact odd reflected mechanism survives, operator-valued lifts, adaptive scalarization, singular tangents, a positive divisor-blind one-cusp boundary norm, generic congruence arithmetic with unstripped ramified inner factors, a nonzero low-frequency carrier, fixed-order jets whose support excludes prime frequencies, a large Gram/nuclear response, benign forward regularity, positive readout noise, adaptive or jointly coded observations without destination-cut discrimination, infinitely many remote resets, smooth PNT subtraction, a phase-blind Hardy excess equivalent to classical short-interval variance, a normalized magnitude statistic reproduced by a literal binary complement, zero-set membership, raw positive von Mangoldt weighting, many twist-frozen bow ordinates, or many genuinely dephased heights controlled by one common exceptional set are not enough unless the surviving quantity is stably distinguishable, charged in the correct source geometry, theorem-accessible at the required quantifier and scale, robust under the full source operator, and selectively coupled to the RH destination.