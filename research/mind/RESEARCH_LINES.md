# Global mathematical questions

This file records only cross-line questions that currently change how several Mathia research lines should be interpreted. Line-specific frontiers remain in each `research/<line>/mind/`.

## Which source relation cannot be repaired by moving the cost elsewhere?

[MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md) is the current synthesis. Across Farey Discrepancy, Möbius Cancellation, Nyman--Beurling and Robin Extremal, the recurring issue is not raw source dimension but whether the declared physical observation exposes a destination-relevant resource with a stable inverse modulus.

Farey Discrepancy now has a support-free two-currency source theorem. FD-187 prices composite inconsistency on the physical-prime fibre by translated divisor-cube capacity; FD-188 prices movement of the prime labels themselves by coefficient amplitude mass `D_q`. Together,

`D_(a,q)(x)=o(x)` and `M_(a,q)(x)=o(x/log x)  =>  a=mu`.

The one-prime multiplicative deformation shows that linear `D_q` mass is the real scale, not a proof artefact. Yet FD-182 still gives an exact dyadic-midpoint null source with critical relation energy. Source rigidity is therefore no longer the missing theorem: the open bridge is a Farey observation that quantitatively exposes enough of the coefficient-fidelity and relation-energy currencies.

Möbius Cancellation now separates generic destination tariffs from source-specific acquisition costs. MC-365 constructs a coordinatewise BSC that attains the information, JS/Hellinger and Bhattacharyya-depth endpoint tariffs, so those lower bounds alone are admission conditions. MC-366 then proves an exact converse for adaptive coordinate-query transcripts on the even-rank reciprocal source: posterior predictability satisfies `rho_i^2<=p_i+1/(2^R-1)`, where `p_i` is own-coordinate revealment. Bounded-energy constant dephasing therefore requires positive-density direct own-phase access, and exact unit-energy dephasing requires the sharp rate `1-1/(2^R-1)`. The BSC pays this cost with `p_i=1`. The next arithmetic question is whether an actual Möbius transcript has subcritical revealment, or what analogous provenance tariff governs richer joint measurements.

Nyman--Beurling now reaches the endpoint in two source currencies. NB-198--NB-199 use positive interval occupancy to show that `o(a)` returns cannot occur below the logarithmic layer `x_a/log x_a`. NB-200 takes over precisely when favorable phase windows become shorter than one integer: each cell can carry at most one von-Mangoldt atom. Comparing available cells with the fixed source mass in a top logarithmic slab gives the one-sided threshold

`liminf |t| log(x_a)/x_a >= 2 pi e^Delta`

for `o(a)` positive returns with bounded endpoint ratio. This is a capacity obstruction, not a return-existence theorem. The source-side acquisition currency changes from local density to atomic packing at the lattice scale.

Robin Extremal gives the stable-inverse control. RE-198 showed complete-real-ray near-nullity through every sublinear jet depth. RE-199 now keeps the exact linear repair radius `W=4K` and extends the same ordinary-prime `{0,1,2}` ambiguity into a genuine linear wedge: for each fixed `eta<1/4`, every growing `K<=eta log p` admits exponentially small full-ray discrepancy `d_p e^(-c_eta K)` while the selector-normalized Robin excursion remains order one. The quarter threshold is representation-specific: it is where the current absolute first-moment factor `(4K/log p)^(K+1)` stops decaying, not a theorem that scalar rigidity begins there.

## Which physical source scale, analytic domain and representation gauge does the theorem actually control?

Arithmetic Fidelity still requires a strict separation between theorem uniformity and intrinsic detector geometry. The accepted order-five tail-cutoff clue is workflow state, not mathematical evidence: the existing exact tail positivity and compact-interior reduction remain the evidence until effective cutoffs are actually proved. Coefficient-sequence gauge sensitivity and continuation-visible zeros still cannot be transferred automatically to finite-order translation positivity.

Prime Circle now makes the analytic-domain part of this audit exact. PC-341 extends the semiprime-null Mellin classification from bounded/polynomial-growth transforms to the full Nevanlinna/bounded-characteristic class on the native Gamma strip. Under conformal transport, the prime-log zeros are a uniqueness set exactly at strip half-width `a>=pi/2`; at every narrower `a<pi/2`, a nonzero bounded Blaschke product can vanish on all prime logs. “Analytic continuation exists” is therefore too coarse: the available strip width and growth class decide whether the same source-forced zero set has any uniqueness leverage. [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md) records the separation between source identification, gauge fixing, analytic access and zero selection.

Visual Exploration gives the parallel representation warning. VIS-278--VIS-281 remove the apparent generic `L^-3/2` frontier from the direct source and localization sectors after fixed-source specialization. VIS-282 exposes the exact Dirichlet diagonal profile `S_D(x)`. VIS-283 then proves that the combined prime/source cross is power-small after packet normalization because the arithmetic residual and `D_x` have no zero-frequency resonance. Through constant field-energy order the fixed-source arithmetic profile is therefore the positive diagonal sum

`Q_gamma(x)=S_D(x)+C_Lambda/x^2`

after retaining the exact gamma baseline. The next question is downstream: what does the admissible packet/test projection do to this `L^-2` profile, and does any remaining truncation/replacement sector genuinely exceed it? [MI-026](intuition/MI-026-sign-destructive-proof-interfaces-cannot-certify-destination-cancellation.md) links this to the Weil-Inertia warning that generic diagonal subtraction or sign-destructive summaries do not manufacture the source-specific covariance the destination needs.

Robin Extremal is the inverse-modulus control for this family of errors. RE-199 sharpens the point: a formally complete analytic observation can remain exponentially ill-conditioned even after the number of matched local constraints reaches a fixed positive fraction of `log p`. A proposed analytic source scale must therefore price the actual repair radius/near-kernel geometry, not only the number of sampled derivatives or the width of the transform domain.

## Which quantitative resource survives a thin or moving destination?

Prime Lattice has separated four different recurrence/completion layers. PL-341--PL-342 identify the sharp `+/-e^a` bounded prime edges and put them in essential spectrum. PL-343 proves that the raw edge event is exponentially rare in Haar measure and long-time density. PL-344 then shows that raw edge-sized values nevertheless occur at uniformly bounded modulation because the endpoint weights have an explicit PNT profile; the same finding also shows that the canonical zeta-pole term cancels that leading bounded-frequency profile.

Thus the raw first-hit problem is not the completed frontier. The live object is the **centered atomic-prime-minus-PNT/pole residual** surviving canonical completion, together with the unbounded logarithmic principal cost on the same state. Existence, prevalence and first-hit of the uncentered edge no longer decide completed coercivity.

Nyman--Beurling supplies the contrasting source-specific pointwise mechanism. Its shrinking-band program also separates qualitative recurrence, target-set volume, block density and return-free scale, but NB-198--NB-200 derive actual pointwise exclusion from the positive von-Mangoldt source rather than from small recurrence volume. The newest transition is granular: interval occupancy governs wider favorable windows, while sub-unit cells are governed by atomic carrying capacity. [MI-027](intuition/MI-027-recurrence-existence-prevalence-first-hit-and-canonical-centering-are-different-currencies.md) records the shared audit rule: match a recurrence theorem both to the destination's time quantifier, the source granularity at the moving scale, and the observable that survives canonical centering.

Xi Flow remains the opposite control: once a consecutive packet is localized, local holomorphic inversion is well conditioned, while support discovery/assembly is global. Across these moving-destination problems, the relevant currency is the quantity that scales with localization, source occupancy or atomic capacity, recurrence time, support discovery, centered residual size, branch transport or unbounded frequency cost—not merely existence of a local inverse or positive bounded bulk.

## Can reverse discrimination, signed structure or positivity be made destination-selective?

Möbius Cancellation now provides both halves of the endpoint audit in one restricted architecture. MC-365 shows that sharp generic discrimination tariffs can be paid by an abstract matched channel; MC-366 shows that if the acquisition process is an adaptive coordinate-query transcript, the same endpoint inequality forces direct own-phase revealment at a sharp rate. A useful arithmetic contradiction must therefore constrain the actual source provenance, not merely sharpen another architecture-free divergence lower bound.

Weil Inertia reaches the analogous boundary for signed covariance. Ordinary and diagonal-deleted Gowers smallness can coexist with full bow variance in matched-energy controls, so more generic uniformity constraints do not force the required negative covariance. A surviving statistic must retain source-specific signed two-point structure rather than only remove universal diagonal energy.

Visual Exploration reaches the same interface test from frequency geometry. VIS-283 shows that after exact fixed-source specialization the expected equal-scale signed source cross is absent, not merely uncontrolled. Preserving sign is therefore necessary but not sufficient: one must preserve the physical frequency relation that determines whether the cross channel exists, then track what the destination projection does to the remaining diagonal profile.

Weil Positivity now tests arithmetic insertion before positivity directly. WP-356 couples the normalized prime Hecke correspondence to the cusp cutoff before taking a norm, but on the constant cusp mode the commutator square is exactly the projection onto a log-height annulus of width `2 log p`. The prime label survives as unsigned annulus size; the scattering/divisor data remain only in a sign-indefinite interference term, and the canonical quadratic norm loses the desired `p^(-1/2)` half-density. Continuous translation and unitary-phase controls preserve the positivity. [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) now includes this stronger warning: source coupling must survive the **positive closure**, not merely occur before it.

Prime Circle similarly shows that broad scalar Mellin selectors collapse throughout bounded characteristic on the full Gamma strip, while a narrower strip restores bounded zero-set survivors. Prime Flute shows that a representation with an intrinsically noncompact low/high angle cannot be repaired by more endpoint counting. In each case the next mechanism must be destination-selective under the same source and representation class, not merely richer.

## Global discriminator

For every proposed route, identify the admissible source class, observation family and nullspace, destination quotient, physical source-coordinate map, acquisition budget, requested precision, normalization gauge, analytic domain/class, source granularity, recurrence/time quantifier, canonical centering, sign/covariance structure and matched control. Then ask two opposite-direction questions in the same currency: **what does the destination force, and what can the physical source mechanism actually supply?**

The current updates sharpen six versions of that test. FD-188 shows that replacing support by amplitude can close a source gauge without creating an observation theorem. MC-366 shows that a generic attainable information tariff can become obstructive once direct source provenance is priced. NB-200 shows that acquisition currency can switch from interval density to atomic capacity at a moving lattice scale. RE-199 shows that a nominal linear-depth transition can dissolve when exact repair geometry is retained. PC-341 shows that the coercivity of the same zero set can turn exactly at an analytic-domain boundary. WP-356 shows that genuine arithmetic coupling before positivity can still collapse to universal geometry after the positive square. Progress requires the missing observation, acquisition, inverse-modulus, analytic-access or post-closure selectivity theorem in the exact representation where the destination lives.