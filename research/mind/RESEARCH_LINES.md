# Global mathematical questions

## Which source resources reach the destination before observation becomes target-mixed completion, decoding or classical recoding?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), and [MI-020](intuition/MI-020-incommensurability-removes-collisions-but-may-end-in-decoding.md) separate information survival, source recovery, collision removal and zero selection.

Arithmetic Fidelity separates relation type, relational arity and actual sampling geometry. AF-374--AF-376 show that killing the continuous separable gauge does not imply determination: a uniformly discrete actual row set admits compact-support false positives that satisfy every sampled total-positivity minor while failing global Pólya-frequency structure. The open AF-376 adversarial review is workflow state, not a replacement theorem, so the current Mind does not promote the objection as evidence.

Prime Circle now supplies an even stronger destination warning. PC-311--PC-318 range from classical finite-Fourier packets to exact source decoding. PC-319 then tests the genuinely coupled canonical Mordell--Tornheim carrier itself: for the smallest canonical pair, zeros in `Re s>1` of the periodic factor persist under every sufficiently small positive coupling. Thus **source recovery, multivariable nonseparability and exact canonical coefficients remain orthogonal to coordinatewise zero selection**.

The live question is: what exact source class and retained relation identify the intended discriminator without reconstructing the source; what relation type, arity, sampling geometry, conditioning and stability are paid; and what independent theorem controls the divisor or sign after identification? A useful destination theorem must exclude the PC-319 coupled off-critical regime by a real structural hypothesis, not merely by changing notation or preserving more source information.

## Which target-conditioned thin channel controls exceptional geometry rather than only typical populations?

[MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) track channels left after bulk controls close.

Nyman--Beurling has now closed the fixed-finite polynomial-height signed-sampler repair. NB-157 shows that the leading macroscopic completed-Gamma term is one signed log-height moment; NB-158 shows three polynomially separated heights can cancel it while keeping bounded target conditioning. NB-159 generalizes the failure: with any fixed finite number of polynomially separated heights, zero mass forces a comparable negative lower atom, polynomial separation isolates its lobe, and ordinary zero counting fills that lobe with `Omega(log G)` actual zeros. Finitely many extra Gamma/Stirling moment constraints therefore cannot make the sampler selective.

The surviving signed route must spend a genuinely new resource—growing sampler cardinality, collapsing/continuous height geometry, subpolynomial adverse scale, worse conditioning coupled to signed prime-side cancellation, or source-specific Nyman/von-Mangoldt coupling before sign separation. The common audit is not how many universal moments are cancelled, but where compensating signed mass lands and whether that region is itself exceptional.

Robin Extremal now has a sharp source-side terminal capacity rather than only an order-of-magnitude scale. RE-162 turns exact lower-moment matching into a Chebyshev approximation problem and proves the threshold `L_crit=4 tau_r`, with `tau_r=(r! eta_p)^(1/r)`. Below this exponential-scale radius every vanishing source budget loses leverage; above it exact-moment PNT-small controls can retain fixed Robin ambiguity. RE-161 remains the separate robustness gate: approximate moments must satisfy the factorially weighted precision staircase `sum_(k<r)|mu_k|/k!` at the destination scale.

Farey Discrepancy still separates rank-anchor tails from coordinate-face anchors; its open FD-153 review is not evidence, while FD-154 shows bounded ancestry depth is possible only after spending explicit macroscopic source information.

## Which growing source law is informative rather than merely connected, numerous, zero-free, interval-pseudorandom or reconstructive?

Möbius Cancellation has moved beyond the abstract half-loading minimax ceiling. MC-322--MC-326 show that independent near-quadratic conductor bills plus any nonnegative universal mode weighting cannot beat the quartic common-radical architecture. MC-327 adds an equality-case theorem for the **actual** source package: if `B_R` is the logarithmically weighted squared deviation of source-column occupancy from one half, then

`liminf (1-B_R) log P/log y >= 4`.

Hence a quartic-saturating package must have `B_R->0`; persistent occupancy imbalance forces a strictly larger source exponent. The former escape “perhaps the arithmetic columns avoid the high-loading region” is closed at economical quartic cost. What remains is arithmetic structure *inside* the forced near-half-loaded band, a genuinely joint/signed conductor law, a stronger individual conductor horizon, or a changed endpoint representation.

Farey Discrepancy continues to show that ancestry resources depend on the geometry and provenance of anchored coordinates rather than edge count alone. Across growing representations the next useful theorem must be on the exact population, norm and scale consumed by the destination. Mode cardinality, connectivity, zero-free regions, interval cancellation and source provenance are not scalar resources until their loading and transfer laws are proved.

## Which representation-faithful null remains after nuisance variables, weak topology and universal scalar positivity are quotiented out?

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) and [MI-002](intuition/MI-002-the-surviving-arithmetic-variable-is-relational.md) separate normalization, relational geometry and source-selective sign.

Visual Exploration now has two exact high-dimensional calibration families inside the log-product quotient. VIS-247--VIS-251 give the parameter-free conditional law, exact Gibbs kernel, connected regular level set, irreducibility and dominant-label switching control. VIS-252 proves ordinary coordinate contrasts have exact eigenvalue `rho_m=(m-3)/(m-1)`. VIS-253 derives the general additive-invariant random-block law and applies it to the second conserved coordinate `sum_i log X_i=lambda`, so log-coordinate contrasts have the same `rho_m^t` autocorrelation and integrated time `m-2`. A frozen implementation must pass **both** families before slower nonlinear `bar T` behavior can be interpreted as conditioned geometry rather than scan or product-constraint error.

Weil Positivity has now exhausted the direct scalar source-preserving additive semigroup route across Muraki's five natural products. WP-325 rejects classical two-divisibility; WP-326 rejects free divisibility while showing Boolean divisibility is universal; WP-327 uses the isolated `log 2` atom plus the further `log 3` atom to reject monotone infinite divisibility, and anti-monotone self-divisibility reduces to the same monotone self-powers. Four categories reject the source and Boolean accepts every source. Changing only the natural notion of scalar additive independence therefore cannot supply a selective sign theorem.

Prime Lattice gives a complementary lesson about positive cones. PL-324 proves that odd parity removes the generic archimedean Beurling--Deny obstruction entirely, so standard semigroup positivity survives exactly until `a=(1/2)log 2`; the first active rational-prime translation then has the wrong modulus sign and destroys the criterion. This is genuine arithmetic sensitivity, but still a negative route result: the RH-relevant signed prime interaction cannot be replaced by order positivity precisely where it becomes active.

The common criterion is whether the destination relation survives honest nuisance propagation and is nonautomatic in the matched-control category. Exact nuisance removal, source fingerprints, reversible access, a positive cone, a change of scalar independence notion or a canonical coefficient vector do not by themselves make the final sign/divisor source-selective.

## Which noncompact source channels and adaptive representation costs survive a positivity reduction under the correct structural limit?

Weil Inertia has removed what looked like a separate diagonal source-margin obstruction. WI-313--WI-314 establish complementary moving-floor and fixed-cap counts at fixed aperture. WI-315 observes that the actual compact-support spaces `C_c^infty((-L,L))` are nested restrictions of one global Weil form. A negative finite-dimensional compactly supported subspace therefore keeps a fixed negative margin once it appears.

Consequently **every** cofinal aperture sequence with cap defect `delta_j->0` is sign-complete: strict comparison counts below `-1/2` converge to the full compact-support negative index, with eventual equality when that index is finite. The abstract varying-source countermodel from WI-314 remains valid abstractly but is not an obstruction for these nested Weil windows.

The hard gate is now direct: construct a source-exact cofinal capped-tail diagonal and rule out comparison spectrum below `-1/2`. No additional aperture-uniform negative-depth theorem or per-aperture saturation schedule is required after that estimate. Compact complexity, modulation scale and exact recombination remain real costs; source-margin synchronization no longer is one for the nested-domain passage.

## Global discriminator: identify the exact resource that the matched control cannot fake

The frontier is increasingly a classification of where information is spent. AF-376 separates removal of a separable gauge from actual determination. PC-319 shows that even genuine canonical multivariable coupling can preserve off-critical coordinate zeros. MC-327 forces economical actual source incidence into the same half-loaded band that the abstract minimax argument identified, moving the arithmetic question inside that band. NB-159 closes every fixed-finite polynomial-height moment-cancellation repair because the compensating negative atom lands in a routine zero reservoir. RE-162 pins the exact-moment source capacity to the Chebyshev radius `4 tau_r` while RE-161 keeps precision as a separate currency. VIS-253 upgrades the conditioned Gibbs audit from one additive coordinate to both linear and logarithmic invariants. PL-324 exposes the first rational-prime event as the exact point where odd-sector order positivity stops being compatible with the signed source. WI-315 removes the diagonal shrinking-margin loophole for nested compact Weil windows, leaving the comparison lower bound as the real gate. WP-327 completes the five-natural-product scalar semigroup closure.

For every proposed route ask: what exact object reaches the destination; under which norm, scale, topology, observation population, source class, relational arity and sampling geometry; which aliases are removed and which fibres remain; what source information was prepaid to obtain coercivity; how demanded modes load onto the source columns actually realized; where compensating signed mass is allowed to land; which structural consistency is available across limits; can a matched control reproduce the remaining variable by decoding, completion, averaging, smoothing, conductor inflation, moment matching, routine reservoir filling, universal transform/convolution closure, positive-cone replacement, generic positivity or classical recoding; and does a separate source theorem dominate the destination loss without reconstructing or assuming the endpoint?
