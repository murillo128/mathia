# Global mathematical questions

## Which source resources survive to the destination rather than becoming target-complete, reconstructive, universal or cheaply matched?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate source recovery, retained relation, conditioning, coupling complexity and final sign/zero selection.

Arithmetic Fidelity now separates **determinant order, placement generators, sampled interiority and spatial resolution**. AF-379/AF-381 make vanishing prime-log sampling target-complete. AF-383 removes one spatial tuple at every fixed determinant order. AF-384 collapses the second placement axis under continuum lower-order strict positivity, while AF-385 sharpens the hidden cost: the strictness itself need only be supplied on the same sampled solid-minor hierarchy. At fixed `k`, strict solid generators below order `k` plus nonnegative solid generators at order `k` on any shrinking mesh sequence determine continuum `TN_k`. AF-372 still forces unbounded determinant order for unrestricted `PF_infinity`. A tiny geometric generator family can therefore remain expensive in order, scale, offset range and source-side sign provenance.

Prime Circle supplies the complementary zero-selection warning across the coupling axis. PC-319--PC-320 show that exact canonical multivariable source recovery carries an off-critical local divisor near `u=0`; PC-321 proves that the same `(5,7)` packet is zero-free on the full right polyhalf-plane once `Re u>=5`, because strong additive exponent isolates the unique lowest output mode. PC-322 then tests the most canonical intermediate value: ordinary Green inversion forces `u=1`, exactly the classical Hilbert-matrix transition, and the accompanying `Re s=1/2` square-summability boundary occurs for every nonzero periodic source. Canonically selecting both the coupling and the half-plane boundary still does not select Riemann zeros when the criticality is matched-control universal.

## Which thin, moving or signed channel remains after bulk controls close?

Nyman--Beurling has moved from worst-case compact transport to a **persistence-in-height** discriminator. NB-165--NB-167 use high-ordinate analyticity before absolute values and identify the one-Poisson-width capped transport `Xi_G`; it controls every Poisson response and gives completed conservation cost `O(q_G Xi_G E_G)`. NB-168 then shows that a fixed compact offset template translated through a dyadic height block cannot use the actual Euler phases as a persistent logarithmic rescue: its source defect has mean square `O(T_G^2(T+a_G^(-2)))`, so `q_G log G`-scale rescue is confined to a density-zero set whenever `Xi_G sqrt(1+(G a_G^2)^(-1))=o(log G)`. The surviving compact escape must therefore be height-adaptive, genuinely sparse in center, logarithmic in capped mismatch, or ultra-thin enough that the mean-square remainder loses force.

Robin Extremal now has two orthogonal source-fidelity coordinates. RE-168 turns the KV envelope into a continuous spatial capacity profile with decay length `ell_KV=log T/V(T)`. RE-169 adds the global Mertens normalization: the same Robin kernel measures the difference of Mertens constants, so exact/critical-scale normalization converts a local post-selector displacement into compulsory future compensation. Under a KV-rigid tail that debt cannot survive past an `O(1)` neighborhood of the remote capacity center in `V`-coordinates. Qualitative `Gamma(Q_p)=gamma+o(1)` is still too weak; the critical precision is `o((sqrt p log p)^(-1))`.

Farey Discrepancy now closes the static sparse-hole loophole as well as completed and divisor-downward auxiliary vocabularies. FD-159 gives `P+L/|C|>=h bar f` on divisor-downward core sets. FD-160 shows that arbitrary sparse holes merely add an explicit incoming term `J`; taking the divisor closure of the depth-`r` cores internalizes `J` without losing depth and restores `P+L/|D_r(C)|>=h(2^r-1)`. Even one deep sparse core reproduces the exponential pressure through its principal divisor ideal. The remaining structural escape must be genuinely terminal/migrating support, growing distortion/leakage, a different cut geometry, or a source law that changes the admissible flips.

Prime Lattice now separates spectral transport from sign transport at the first arithmetic activation. PL-329 proves that the exact source equation orients the shrinking first-prime reflection under `(1+S_delta)/(|C_delta| sqrt(L_delta))->0`. PL-330 shows that the raw prime edge flip, despite its norm-one jump, converges strongly; compact resolvent upgrades the full localized-Weil family to norm-resolvent continuity, with graph-continuous simple eigenbranches across the activation. Yet the sharp sign-reversing perturbation can itself vanish in that graph norm. The blocker is therefore not branch continuity but source-amplitude control relative to the much smaller `delta/L_delta` reflection margin.

## Which growing source law is informative rather than merely numerous, low-rank or programmable?

Möbius Cancellation now has four independent joint-coefficient currencies. MC-332 shows unrestricted mode-dependent rows contain the exact conjugate-character matched filter. MC-333 prices rowspace rank against Frobenius energy, and MC-334 prices centered variation away from every common input. MC-335 adds **mode-dependence Fourier degree**: low-degree coefficient columns cannot even approximate uniform endpoint dephasing, while exact programmability returns at the near-maximal threshold `d=R-2`. A viable analytic class must therefore exclude the matched filter in the exact norm and structural complexity actually enforced by the theorem, not by nominal rank alone.

The common lesson from FD-160 and MC-335 is that structural restriction matters only when it changes the effective carrier after the natural closure operation. Sparse holes that disappear under divisor closure leave the same cut pressure; mode dependence that retains near-maximal Fourier degree leaves enough capacity for a matched filter. “Sparse,” “truncated,” “low-rank” and “structured” are not resources until the restriction survives the completion/closure intrinsic to the destination geometry.

## Which representation-faithful null survives nuisance removal, weak convergence and universal positivity?

Visual Exploration now has an exact cusp-adapted tradeoff. VIS-256 shows fixed polynomial moments cannot beat the sine-kernel `|q|` cusp. VIS-257 kills that cusp exactly but proves the sharp price `|m_2(nu)|<=(b^2/8)||nu||_TV`; fixed quadratic signal costs `Theta(b^(-2))` variation, and the finite-window CUE null itself has a quadratic channel. The route must therefore either calibrate that channel to `o(b^2)` under amplification or cancel it and move to a quartic/higher conditioning gate.

Weil Positivity now has a sharp moving-observable stability boundary. WP-331--WP-332 give exact fixed-cutoff multiplicative-domain rigidity. WP-333--WP-335 show weak, finite-divergence, max-divergence and bounded-channel convergence can miss a fixed variance defect when the logarithmic observable grows. WP-336 identifies the exact correction in the commutative martingale/UCP lane:

`||Q_R|| <= (1/2)||Psi_R-I|| sup_x L_(-,R)(x)L_(+,R)(x)`.

WP-335 saturates this inequality. Thus channel-norm convergence recovers rigidity whenever its error times bidirectional propagation is `o(1)`; a fixed defect requires critical or supercritical nonlocal propagation, or a change of category/observable. The missing resource is no longer an unspecified stronger topology but a concrete scale coupling between escape probability and source-coordinate propagation.

Prime Lattice's PL-330 gives the complementary warning: even a topology strong enough to transport the actual spectral branch can remain too weak for a shrinking sign observable. The crucial issue is not whether a norm is abstractly strong, but whether it controls perturbations relative to the destination margin that must be oriented.

## Which noncompact source channels survive positivity reduction once complexity is separated from information?

Weil Inertia now closes scalar-localization self-bootstrap under **arbitrary Hilbert-valued diagonal amplification**, not merely finite channelization. WI-320 supplies the fixed short prime-free witness. WI-321 shows the scalar semibound amplifies dimension-free to any complex Hilbert space and, by polarization, already generates the corresponding Loewner Gram inequality. Fixed PSD cross-channel metrics are therefore consequences of the scalar premise, not new information. A viable vector route must add coupled inequalities outside that scalar-generated cone.

Xi Flow provides the opposite inversion lesson. XF-185--XF-186 show that once the correct consecutive packet is known, linear and local nonlinear holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local invertibility.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is a classification of **source-specific coupling consumed by the destination at its own scale, topology, family quantifier and closure geometry**. AF-385 makes fixed-order placement compression depend only on a sampled strict solid-minor hierarchy while preserving the unbounded-order/source-sign bill; NB-168 distinguishes persistent source mechanism from sparse phase resonance; RE-169 turns global normalization into compensation debt; FD-160 shows static sparse holes vanish as an escape after divisor closure; MC-335 makes mode Fourier degree an anti-programmability resource; VIS-257 turns cusp cancellation into a sharp conditioning bill; WI-321 identifies the scalar-generated Loewner cone; PC-322 shows a canonically selected `1/2` boundary can still be universal Hilbert criticality; PL-330 separates norm-resolvent branch continuity from the finer shrinking sign margin; WP-336 identifies the sharp channel-error × bidirectional-propagation threshold for moving-observable rigidity.

For every proposed route ask: what object reaches the destination and at what moving scale; over what family of centers/parameters the mechanism must persist; what closure/completion of the observation class the destination can perform for free; what conserved fibre, generator cone, induced metric or matched geometry survives that closure; which hypotheses make a compressed test family complete; whether sparsity survives divisor closure rather than only the original presentation; whether a joint class excludes the target-matched filter by degree as well as rank/variation; whether a global normalization converts local gain into later debt; whether an apparent critical axis is source-selective or shared by matched controls; whether the chosen topology controls the destination margin rather than only the ambient state; whether the norm controls an unbounded observable together with its propagation scale rather than only bounded shadows; and whether channel coupling lies outside what scalar polarization already implies. The surviving theorem must expose a resource the destination uses and a matched control cannot reproduce by completion, decoding, truncation, closure, phase matching, rare resonance, thin-tail averaging, channel duplication, universal positivity or classical recoding.
