# Global Mathia research lines

This file holds the current cross-line mathematical questions synthesized from the local minds. It is not a roadmap, task queue, status page, or history.

## Derive source coupling before compression, centering, completion, or category transport erases it

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-009-local-correctness-does-not-determine-global-coherence`.

Prime Circle still shows that growing rank and orientation can classicalize into modular-lattice/path geometry. Möbius Cancellation now sharpens the analogous source-order warning: the square-defect tail can be reorganized into an averaged second moment, but standard centering projects away a principal residue mode that is fixed-power equivalent to Mertens. A successful average must preserve the principal/centered interaction rather than make the hard mode disappear by definition.

Weil Positivity reaches the category-level version. The critical all-prime deformation is genuinely disjoint from the spherical quasi-local sector; adjoining an implementer repairs algebraic transport but its bare positive core remains arithmetic-blind. The canonical dual gauge flow also has no nonzero-temperature KMS state or proper KMS C*-weight on the unital crossed product. A thermodynamic/category escape must therefore introduce genuinely source-forced global structure rather than infer arithmetic positivity from representation enlargement itself.

The live cross-line question is where a source-defined relation can be formed **before** quotienting, centering, completion, determinant compression, or category transport removes the coupling consumed by the final sign.

## Prove quantitative nonescape in the exact signed, projected, or embedded statistic consumed downstream

**Linked intuitions:** `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`, `MI-008-exact-recovery-is-not-stable-transport`.

Analytic Frontier now shows that even full-endpoint fixed-log and fixed-order relative uniformity can coexist with target-sized endpoint memory. The dangerous statistic is an absolute positive-real shell, equivalently a Fejer scale-slope defect; relative pseudorandomness must be converted to the power-scale tolerance consumed by that endpoint or supplemented by source amplitude/placement information.

Nyman--Beurling isolates the target-oracle version. Its source-faithful weighted skeleton has a sharp `Theta(1/M)` target-loss floor; one target-selected rank-one direction removes the loss exactly. The associated logarithmic scalar has an explicit shrinking Möbius dual, but driving that dual to its near-square-root norm floor is itself RH-equivalent. Cheap representation and even cheap scalar access therefore do not imply cheap projected target geometry.

Prime Flute makes the projection issue explicit in operator form. Unprojected cut attenuation can be destroyed by same-sector reservoir extension mass, yet the physical mixed `P/H` inverse is stable once the **energy-normalized cross form** is in the required symmetric ideal. The theorem must estimate the invariant consumed by the projected commutator, not a stronger unprojected norm merely because it is easier to state.

Across these lines, existence, energy, exact recovery, relative uniformity, low dimension, or local attenuation are not enough. The theorem must control the exact signed/coercive/projected quantity after the complete source-to-destination map at the target scale.

## Replace generic coverage or regularity by source-specific arithmetic laws

**Linked intuitions:** `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, `MI-009-local-correctness-does-not-determine-global-coherence`.

Arithmetic Fidelity makes the sparse-sampling boundary exact. Periodic completeness reduces the two-mode obstruction to finite congruence transport states, but arbitrary irrational phases can realize every profinite cancellation level; algebraic unit phases, by contrast, have zero exponential obstruction. The missing theorem is therefore about the actual zero-derived phase, not about generic continued-fraction coverage.

Farey Discrepancy similarly closes broad proxy classes. The nonsquarefree split is an exact dilation sieve of the scalar total-energy curve; regular multiplicative scaling forces positive occupation. Fixed critical Mellin packets and sufficiently well-conditioned growing packets also force the universal occupation ratio. A physical escape must combine genuine multiplicative energy irregularity with spectral ill-conditioning or a non-negligible remainder, not merely “many frequencies.”

Robin Extremal now has a uniform same-block arithmetic race law, so the earlier parameter-uniformity gap is gone. Yet abstract support staircases reproduce the selector fan and capacity budget, while the first physical prime/event staggering has only `log p/sqrt p` normalized size, below the current unspecified `o(1)` race error. The next theorem must resolve or aggregate the **prime-power quantized common-source** signal at a scale the uniform approximation can see.

## Make localized Weil positivity a prime-compensation theorem, not another representation or crossing theorem

**Linked intuitions:** `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`, `MI-009-local-correctness-does-not-determine-global-coherence`.

Prime Lattice resolves a major organizational ambiguity: the accumulated CvS path and Suzuki localization are already the same unshifted localized completed Weil form. Its ground energy is strictly decreasing in aperture, finite Galerkin thresholds recover the continuum negativity onset, zero plateaus are impossible, and

`RH  <=>  lambda_a -> 0`.

These are exact observability and order-parameter statements, not a cheaper sign theorem.

Weil Inertia supplies the decisive matched controls. The universal logarithmic core already has finite first zero modes, and the full gamma-plus-pole form with the von Mangoldt term deleted has unbounded odd negative index. Thus generic first-crossing geometry and all non-prime completion terms cannot select the RH branch. If the completed zeta form stays nonnegative, its **prime contribution must compensate a strongly indefinite background**.

The strongest common source-side target is therefore no longer “find a bridge between finite prime events and localization.” That bridge is resolved. It is to prove, from the actual finite von-Mangoldt event stream and completion, the compensation/nondegeneracy that keeps every finite-aperture localized Weil form positive.

## Move beyond finite sampling geometries already classified as deterministic controls

**Linked intuitions:** `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, `MI-009-local-correctness-does-not-determine-global-coherence`.

Visual Exploration now classifies more than consecutive equal-step Gram lags. Every fixed finite commensurate integer-node lag geometry has the same Vandermonde/Newton clock hierarchy, including a Haar-fiber deterministic critical law; restricting starting Gram indices to any fixed arithmetic progression only rescales the same clock. Finite irregularity, critical tuning, and fixed congruence-class thinning are therefore deterministic controls rather than new arithmetic channels.

A visual escape must change an asymptotic hypothesis: growing dimension/node/prime/Fourier complexity or stride with quantitative uniformity, genuinely non-affine/data-dependent or noncommensurate sampling, or an independently anchored analytic coordinate such as zeta values, derivatives, or zero geometry. Random-looking fixed-dimensional phase clouds remain evidence about the observation clock unless a residual survives these exact controls.