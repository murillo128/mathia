# Global mathematical questions

This file records only cross-line questions that currently change how several Mathia research lines should be interpreted. Line-specific frontiers remain in each `research/<line>/mind/`.

## Which source relation cannot be repaired by moving the cost elsewhere?

[MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md) is the current synthesis. Across Farey Discrepancy, Möbius Cancellation, Nyman--Beurling and Robin Extremal, the recurring issue is not raw source dimension but whether the declared physical observation exposes a destination-relevant resource with a stable inverse modulus.

Farey Discrepancy shows that relation coercivity and observation of that relation are separate. On the physical-prime fibre, prime-extension/divisor-cube energy can rigidly price a composite defect, while sparse dyadic observations remain blind to it. The missing bridge is an observation theorem for the relation capacity actually proved.

Möbius Cancellation now adds a decisive calibration. MC-363--MC-364 force almost-maximal Hellinger separation and logarithmically diverging negative-log-affinity depth near exact dephasing. MC-365 constructs a coordinatewise binary-symmetric product channel that simultaneously attains the information, JS/Hellinger and Bhattacharyya-depth tariffs at that scale, up to the exponentially small puncture correction. The generic lower tariff is therefore an admission condition rather than an obstruction. A Möbius route now needs an independent arithmetic **upper** theorem that excludes or prices this BSC-like source-direction discrimination in the actual transcript architecture.

Nyman--Beurling makes the source side explicit in a different currency. NB-198 replaces source approximation by positive phase-window occupancy to exclude arbitrarily accurate returns throughout every fixed subunit Mellin power. NB-199 then keeps the variable window scale and shows that vanishing positive tolerances are forced into the final logarithmic layer `t~x_a/log x_a`. Abstract recurrence access is not enough; the true source must actually populate the favorable phase cells at the requested accuracy.

Robin Extremal gives the stable-inverse control. RE-198 extends full-real-ray collapse through every sublinear Euler-jet depth `K=o(log p)` using a repair window growing linearly with `K`, while an order-one Robin transient remains. Exact continuum identification therefore does not become a usable source theorem merely by widening the real Euler observation or letting the jet depth grow slowly. The first unresolved continuum scale begins only around `K~log p`.

## Which physical source scale, analytic domain and representation gauge does the theorem actually control?

Arithmetic Fidelity still requires a strict separation between theorem uniformity and intrinsic detector geometry. The accepted order-five tail-cutoff clue is workflow state, not mathematical evidence: the existing exact tail positivity and compact-interior reduction remain the evidence until effective cutoffs are actually proved. Coefficient-sequence gauge sensitivity and continuation-visible zeros still cannot be transferred automatically to finite-order translation positivity.

Visual Exploration gives the parallel representation warning. VIS-278--VIS-281 remove the apparent generic `L^-3/2` frontier from the direct source and localization sectors after fixed-source specialization. VIS-282 exposes the exact Dirichlet diagonal profile `S_D(x)`. VIS-283 then proves that the combined prime/source cross is power-small after packet normalization because the arithmetic residual and `D_x` have no zero-frequency resonance. Through constant field-energy order the fixed-source arithmetic profile is therefore the positive diagonal sum

`Q_gamma(x)=S_D(x)+C_Lambda/x^2`

after retaining the exact gamma baseline. The next question is downstream: what does the admissible packet/test projection do to this `L^-2` profile, and does any remaining truncation/replacement sector genuinely exceed it? [MI-026](intuition/MI-026-sign-destructive-proof-interfaces-cannot-certify-destination-cancellation.md) links this to the Weil-Inertia warning that generic diagonal subtraction or sign-destructive summaries do not manufacture the source-specific covariance the destination needs.

Robin Extremal remains the inverse-modulus control for this family of errors: a formally large analytic observation domain can still carry almost no stable information on the destination scale when the source class contains matched near-kernels.

## Which quantitative resource survives a thin or moving destination?

Prime Lattice has now separated four different recurrence/completion layers. PL-341--PL-342 identify the sharp `+/-e^a` bounded prime edges and put them in essential spectrum. PL-343 proves that the raw edge event is exponentially rare in Haar measure and long-time density. PL-344 then shows that raw edge-sized values nevertheless occur at uniformly bounded modulation because the endpoint weights have an explicit PNT profile; the same finding also shows that the canonical zeta-pole term cancels that leading bounded-frequency profile.

Thus the raw first-hit problem is not the completed frontier. The live object is the **centered atomic-prime-minus-PNT/pole residual** surviving canonical completion, together with the unbounded logarithmic principal cost on the same state. Existence, prevalence and first-hit of the uncentered edge no longer decide completed coercivity.

Nyman--Beurling supplies the contrasting source-specific pointwise mechanism. Its shrinking-band program also separates qualitative recurrence, target-set volume, block density and return-free scale, but NB-198--NB-199 derive actual pointwise exclusion from positive von-Mangoldt occupancy rather than from small recurrence volume. [MI-027](intuition/MI-027-recurrence-existence-prevalence-first-hit-and-canonical-centering-are-different-currencies.md) records the shared audit rule: match a recurrence theorem both to the destination's time quantifier and to the observable that survives canonical centering.

Xi Flow remains the opposite control: once a consecutive packet is localized, local holomorphic inversion is well conditioned, while support discovery/assembly is global. Across these moving-destination problems, the relevant currency is the quantity that scales with localization, source occupancy, recurrence time, support discovery, centered residual size, branch transport or unbounded frequency cost—not merely existence of a local inverse or positive bounded bulk.

## Can reverse discrimination, signed structure or positivity be made destination-selective?

Möbius Cancellation now makes the two-sided requirement explicit: a sharp destination discrimination floor must be paired with a source-side acquisition ceiling in the same architecture. MC-365 shows why sharpening only the destination lower bound cannot close the endpoint branch.

Weil Inertia reaches the analogous boundary for signed covariance. Ordinary and diagonal-deleted Gowers smallness can coexist with full bow variance in matched-energy controls, so more generic uniformity constraints do not force the required negative covariance. A surviving statistic must retain source-specific signed two-point structure rather than only remove universal diagonal energy.

Visual Exploration reaches the same interface test from frequency geometry. VIS-283 shows that after exact fixed-source specialization the expected equal-scale signed source cross is absent, not merely uncontrolled. Preserving sign is therefore necessary but not sufficient: one must preserve the physical frequency relation that determines whether the cross channel exists, then track what the destination projection does to the remaining diagonal profile.

Weil Positivity continues to separate arithmetic orientation from freely chosen positivity geometry: scalar gauge, bi-invariant quadratic energy and non-reversible Finsler orientation do not become source-forced merely because they can store phase direction. Prime Circle similarly shows that broad scalar Mellin selectors collapse inside the current Gamma-strip-admissible measure class. Prime Flute shows that a representation with an intrinsically noncompact low/high angle cannot be repaired by more endpoint counting. In each case the next mechanism must be destination-selective under the same source and representation class, not merely richer.

## Global discriminator

For every proposed route, identify the admissible source class, observation family and nullspace, destination quotient, physical source-coordinate map, acquisition budget, requested precision, normalization gauge, analytic domain/class, recurrence/time quantifier, canonical centering, sign/covariance structure and matched control. Then ask two opposite-direction questions in the same currency: **what does the destination force, and what can the physical source mechanism actually supply?**

The current updates sharpen three failure modes. MC-365 shows that a large sharp destination tariff can still be attainable by a matched abstract source channel. PL-343--PL-344 show that exponential recurrence rarity can coexist with an early raw hit that is subsequently removed by canonical completion. VIS-283 shows that two coefficient-scale diagonal energies need not possess an equal-scale signed cross because frequency orientation forbids resonance. NB-199 and RE-198 show what a genuine source-side obstruction looks like: a quantitative theorem in the physical source class whose precision/depth dependence survives the destination scaling. Progress requires the missing acquisition upper theorem, centered-residual first-hit theorem or destination projection theorem respectively, not another strengthening of an already calibrated intermediate.