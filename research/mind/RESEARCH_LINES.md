# Global mathematical questions

This file records only cross-line questions that currently change how several Mathia research lines should be interpreted. Line-specific frontiers remain in each `research/<line>/mind/`.

## When does an escape from one obstruction survive the next reduction?

[MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) remains the common interface audit: retained information has to survive the physical source class and its quantifiers, exact quotient/topology/support, domain admissibility, the closure used to create positivity or new variables, matched controls, the true operator/resource category and the destination margin.

Arithmetic Fidelity now removes both finite linear codimension and regular finite-dimensional nonlinear equality as generic protection. AF-460 gives `D_k=0` for every fixed paired cube on the normalized kernel carrier of any bounded finite-rank source law over an infinite alphabet. AF-461 shows that at a regular point of a finite-dimensional `C^1` source law, normalized local same-fiber secants recover the unit sphere of the derivative kernel, so the same zero-defect cube geometry survives whenever the physical completed-secant carrier contains that local fiber. The escape has moved to genuinely tangent-incomplete source geometry: singular fibers, positivity/boundary or support restrictions, infinite-dimensional laws or a different metric count only after they are proved to remove the physical cube carrier.

Farey Discrepancy supplies the observation-side analogue with an explicit density scale. FD-239 shows that arbitrary response sampling of octave density `eta<9/pi^2-3/4=0.161890652...` still leaves integer signed null profiles with macroscopic capacity in both signs. The previous sparse-sampling obstruction therefore persists into a certified positive-density regime. That constant is a survival threshold for the construction, not a sharp coercivity threshold; the remaining alternatives are denser/structured response information or a proof that physical positivity/capacity/source coherence is transverse to the signed nullspace.

Möbius Cancellation and Nyman--Beurling now price two different ways of manufacturing apparent width. MC-422 shows that a thin hyperbolic shell taxes the joint bandwidth of positive factor axes, and MC-423 closes the exact-product tangent escape at fixed factor depth. MC-424 removes the remaining coordinate loophole for state multiplicity: if the product shell has width `Delta=X^(theta+o(1))`, then the entire fixed-depth ordered factor state set has only `X^(theta+o(1))` elements, regardless of shear, tilt or cross-fiber tangent parameterization. In the Norton/Pollack regime, gaining an additional state exponent `delta` through factor depth already requires `d >= (log X)^(delta-o(1))`. The surviving Möbius routes must therefore use analytic organization of the bounded state set, a genuinely additional arithmetic coordinate or transformed phase, boundary-crossing correlations, or sufficiently growing depth with its costs paid.

On Nyman--Beurling, NB-254 gives positive scalar span `W` a compulsory `1/W` coherence window, while NB-255 gives signed/complex mixtures the worst-case radius `c/(WV)` from Euler-normalized total variation `V`. NB-256 shows why `V` cannot itself be treated as the physical conditioning tariff: microscopic opposite dipoles can make `V` arbitrarily large while the synthesized source and carrier remain asymptotically unchanged at Ford resolution. The useful cost must therefore be measured after quotienting unresolved coefficient structure—through a scale-sensitive seminorm, a separated carrier dictionary, or another source-native invariant that still controls the destination map.

Prime Circle and Prime Lattice now show that both operator packaging and complex phase can erase arithmetic labels. PC-376 turns finite pairwise-coprime canonical Chebyshev families into universal polydisk shifts. PC-377 classifies the direct scalar all-prime sum by `ell^2` closability and `ell^1` boundedness. PC-378 then closes the canonical commuting Euler-product repair: it is exactly the classical weighted divisor transform, with Bohr symbol `zeta(w+conj(s)-1)`, and inherits the same movable summability thresholds. Writing zeta explicitly after the quotient therefore does not restore lost source information. On Prime Lattice, PL-398--PL-399 show that fixed-gap arithmetic is finite decoration and growing gaps pay `1/d`; PL-400 adds a phase gate. In the phase-pinned range `d=o(r^(1/3))`, the leading real transition tail vanishes on even gaps, precisely where von-Mangoldt pair bulk can live, while odd-gap prime-pair mass is negligible. A viable arithmetic coupling must match the complex continuation phase to the support of the arithmetic weight, not just its magnitude.

Visual Exploration adds a quantitative phase-resolution gate to its scale-count obstruction. VIS-343 proves that a scalar bank of `N` pure Wang dilates has cancellation order at most `N`; VIS-344 shows that fixed translations cannot pool uniform tail cancellation across phase fibers. VIS-345 extends that obstruction to moving windows: for a fixed bank with `B` active translations, minimum spacing `Delta` and window length `L`, the moment exponential polynomial has a lower frame factor `1-2(B-1)/(L Delta)`. Once `L Delta>=4(B-1)`, polynomial decay along high-frequency windows forces the same fiberwise moment cancellations and Vandermonde cost as the uniform tail. A surviving finite-window mechanism must therefore stay phase-unresolved, let the phase geometry or conditioning vary with scale, become frequency/source adaptive, or use signed arithmetic cancellation rather than whole-window multiplier smallness.

Weil Positivity still exhausts the canonical continuum Markov sectors through WP-388. Weil Inertia now separates spatial reserve from dependency order more sharply. WI-374 shows sufficiently late endpoint collars are gamma-positive, so the exact WI-373 negative profile is inherited from old bulk rather than recreated in every fresh collar. WI-375 then shows that the printed common-cut positive pivot remains downstream of the invalid gamma-free source metric: the later same-vector Schur identity correctly places a debit only after a positive pivot has been supplied. A repair must prove the gamma-retaining common-cut lower metric upstream rather than expecting downstream Schur algebra to create it.

## Where has the exact obstruction moved?

Arithmetic Fidelity: to **physical tangent-incompleteness strong enough to remove regular derivative-kernel cube completion**. Farey: to **response density/structure above the certified `9/pi^2-3/4` nullspace range, or one-sided physical transversality to that signed nullspace**. Möbius: to **analytic cancellation or genuinely extra source coordinates beyond fixed-depth shell state multiplicity, boundary-crossing correlation geometry, or growing factor depth large enough to pay the explicit state-volume tariff and still survive closure**. Nyman--Beurling: to **a resolution-sensitive signed-carrier geometry that discounts microscopic dipoles yet still makes actual Ford-window decorrelation costly after shell synthesis and legal projection**.

Prime Circle: to **source-forced nonfactorizing assembly, cross-prime coupling or domain/topology outside finite polydisk shifts, scalar sums and the classical divisor Euler product**. Prime Lattice: to **a completed channel whose complex phase overlaps the arithmetic support, or a controlled regime where that phase relation changes**. Visual Exploration: to **short/unresolved or scale-dependent Wang windows whose phase spacing and conditioning remain favorable, frequency/source-adaptive coefficients, or a signed theta-remainder estimate that is not equivalent to scalar multiplier smallness, with derivative repayment still controlled**. Weil Positivity: to **non-Markov/interface/prelimit or delayed finite--archimedean geometry outside the exhausted continuum Markov sectors**. Weil Inertia: to **a corrected gamma-retaining source metric showing how inherited bulk debt couples to cofinally positive endpoint collars before the pivot is invoked**.

## Which physical cost survives after representation artifacts are removed?

The common ledger is increasingly about effective geometry after source restriction, closure and operator admissibility. AF-461 separates nominal nonlinearity from tangent-incomplete source geometry; FD-239 separates sample count from response density relative to fresh capacity; MC-424 separates nominal factor/tangent coordinates from the cardinality of the entire physical fixed-depth shell; NB-256 separates raw signed coefficient variation from carrier changes resolvable after smooth-shell synthesis; PC-378 separates explicit Euler-product/zeta syntax from new operator structure; PL-400 separates transition magnitude from phase-aligned arithmetic support; VIS-345 separates moving-window location from genuinely unresolved phase geometry; WP-388 separates nominal local diffusion from domain-compatible arithmetic visibility; WI-375 separates exact downstream Schur bookkeeping from the upstream metric needed to justify the pivot.

The reusable question is: after source class, coupled limits, completion, support/coherence geometry, domain, quotient/topology, closure, locality/operator category, phase and matched controls are made exact, what quantitative object remains that the physical arithmetic source must pay for rather than coordinates, rescaling, generic capacity or a repackaging of a classical carrier?