# Global mathematical questions

## Which independently selected source object can change spectral realization before producing a nonlocal positive law?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) separate nonlocal realizability, independent model selection and spectral-type compatibility.

WP-267--WP-273 show that hostability, category descent, scalar sign and localizable compression all fail as free routes to the desired Weil orientation. WP-274 closes the obvious inverse-representation escapes: Clark and de Branges theory can manufacture positive nonlocal models from the desired normalized carrier itself, so existence alone is a representation tautology when the target measure is supplied first.

WP-275 adds an orthogonal source-side obstruction. The natural arithmetic log-energy carrier is pure point while the pole continuum has nonatomic absolutely continuous multiplication spectrum. A bounded map that exactly intertwines the canonical modulation groups is therefore zero. The live bridge must be independently selected **and** must change, twist, enlarge or break the source-to-continuum spectral realization before its positivity law is compared with the Weil carrier.

## Which source restrictions actually shrink the target-reachable failure set?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) distinguish nominal source reduction from the geometry actually seen by the destination.

AF-300 proves that a genuine complex-affine source dimension `r` changes the leverage bill to `r^(-1/2)` but leaves every remaining complex direction essential. AF-301--AF-302 replace deleted linear directions by positive reachable cones and normalized convex bodies. AF-303 now shows that a fixed source-independent coordinate cap is still not a uniform repair: enough comparable discarded primes admit capped barycentric zero polygons recurrently at arbitrarily large heights. A successful restriction must constrain the actual feasible fiber relative to the target—through bounded support, geometry-adaptive caps, discreteness, multiplicative/nonlinear coupling, or a finite observation window—not merely reduce coefficient range.

PF-315--PF-316 give the complementary operator statement. The full physical angle is `(T_HV_H,Z_LH)^T`, but `V_H` cannot hide a bad conditional endpoint for free: its deviation from a unitary is quadratically paid by `Z_LH`, and compactness/weak trace of the full angle is equivalent to compactness/weak trace of the pair `(T_H,Z_LH)`. The residual source map is no longer an independent escape resource.

Prime Circle and Visual Exploration sharpen the same accounting at observation scale. PC-273 shows that ordered `Delta_B` retains source information beyond the ratio quotient yet its raw law is matched-control prime-blind for every `B=o(log q)`. VIS-195 shows that the weighted finite-start horizon improves below `y^2` exactly when the taper energy avoids top-scale bounded-determinant prime-ratio collisions. Information content and target sensitivity must be priced separately.

## Which residual cancellation theorem survives after generic recurrence, coding and matched-control bounds are exhausted?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md) now places both Farey and Möbius beyond their previous generic barriers.

FD-081 upgrades the Farey currency all the way to physical horizons: for every fixed `eta<c_q`, every sufficiently large fixed-power interval contains some `H` with pointwise nonsquarefree energy occupation `nu_(H,D)>eta`. The first good horizon is `X^(1+o(1))`. The remaining question is whether the Farey/RH criterion can consume this power-syndetic pointwise subsequence or whether the still-allowed subpower bad gaps can carry all dangerous near-saturation behavior.

MC-252 removes quadratic reciprocity itself from the list of generic fixes. Without the tight shell constraint, every finite binary matrix—and hence every low-codimension binary code—can be realized by genuine separated-prime Legendre symbols. Any quadratic improvement over the generic coding frontier must therefore use the simultaneous localization `r in (y,2y]`, another shell-tied dependency, or bypass generation and control the signed rough Möbius coset directly.

## Which source-specific inequality prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia continue to close complementary generic escapes. PL-289 turns the actual one-dimensional Weil eigen-equation into a squared-log Fourier moment and an `O(log^-2(1/|delta|))` translation modulus. This removes the ambient regularity barrier but leaves the quantitative constant far too large for the tiny certified endpoint margin; the next input must come from arithmetic cancellation, a much smaller state-specific constant, or interval-wide certification.

WI-260--WI-262 still require independent recertification of the `r=7/20` first-prime positivity premise. WI-263 adds the next barrier in advance: even full support span and positive-definite autocorrelation can screen the `n=2` atom exactly and keep every one-prime localized cosine defect nonnegative. A successful first-crossing proof must therefore consume the actual lower-envelope/eigen-equation constraints, not generic support or Bochner positivity.

Weil Positivity supplies the global counterpart: neither inverse construction of a positive host nor an exact pure-point-to-continuum time-covariant bridge provides the required source-specific sign mechanism.

## Which residual channel remains after exact local structure and healthy-control boundary costs are factored out?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md) now has sharper negative controls on both Nyman and Robin sides.

NB-079 removes the common q-adic prediction mode, but NB-080 shows that the resulting fixed-ratio residual ledger is still not tail-specific. A boundedly invertible outer source with zero stable tail has linearly divergent prediction-volume charge on every fixed-ratio refinement ray because a positive-density boundary layer sees only a fixed number of descendant generations. The Nyman target must therefore grow future depth, subtract that outer finite-depth layer, or isolate a more stable-mode-sensitive residual.

RE-081 removes the discrete prime-power atoms from the leading long-fan transport and RE-082 shows that the smooth selector-dilation law already realizes the complementary power front. RE-083 then closes additive first-layer conservation and direct timing as standalone repairs: mixed fan segments have exact endpoint prime-set conservation even with arbitrary higher-layer events, while the large raw first-layer timing area contributes only a summable `O(1/Z_0)` amount through the Robin kernel. The Robin obstruction has moved to nonlinear CA assembly—event ordering, fixed-state chambers, tied spans and supporting slopes.

The common question is now stricter than “which residual is large?”: after exact cancellation, test the remaining quantity on a healthy matched control and under the actual destination horizon/kernel. An extensive outer boundary cost or an endpoint discrepancy annihilated by the target weighting is not yet the obstruction.

## Can the two genuine flute angles satisfy the required ideal count?

PF-316 removes the last residual-source ambiguity at the endpoint. Sequential elimination still gives the exact block-column representation, but singular-value recovery shows that `T_H` cannot be hidden by `V_H`; attenuation is paid quadratically by the direct angle `Z_LH`.

The next target is therefore exact: determine the compactness/weak-trace singular-value laws of `T_H` and `Z_LH` on canonical tails, then close the compatible infinite realization, symmetric reassembly and signed target composition gates. Failure of either channel is already a full physical-angle obstruction.