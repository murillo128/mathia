# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Compute the complete control cone before increasing complexity inside a carrier

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Several branches now show that a few finite controls badly understate the true boundary. Analytic Frontier is the sharpest example: high-multiplicity conjugate combs force nonnegative band spectrum, and all-scale duplicated lattices plus Mellin averaging leave only a tiny scalar defect budget. Visual Exploration similarly turns radial zero-entry complexity into exact Jensen zero-counting, while Prime Circle turns an exact half-reflection into universal Bloch time reversal.

A decisive advance must identify **what survives the strongest admissible family of matched controls**, not merely what survives one-point, finite-rank, fixed-scale, or visually natural tests. A decisive negative is an exact classification of the complete control cone or quotient.

## Distinguish source identification, readout quotient, divisor orientation, and zero selection

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Prime Lattice now gives all four separations. Strong Selberg data can identify ordinary zeta, exact coprime power values can recover phase, and integer reflection phase can finitely identify low generator prefixes. Yet real-part/chord power metrics remain reflection-blind even with all powers, and Möbius inverse small-sum theory naturally tracks zeros of `1/zeta` while the nontrivial Riemann divisor appears as poles.

A decisive RH mechanism must state separately what identifies the rational-prime source, what quotient the readout imposes, which analytic divisor the theorem acts on, and what independent sign/unitary/coercive theorem localizes that divisor.

## Make recovery margins survive the source-forced coefficient category

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Arithmetic Fidelity now provides an exact model. Algebraic nonlinear repair is an operator-extension quotient and robust repair is distance from the norm closure of extendable data. But AF-096 shows that making the coefficient a dual Banach space does not add a natural normal-witness layer: dual coefficients are ultrasummands, so the complete defect vanishes.

The same discipline should be enforced in arithmetic realizations: specify the topology, coefficient category, domain, and stability margin that the downstream theorem actually admits. A defect existing only before the source-forced category change is not a surviving arithmetic invariant.

## Require quantitative inverse transfer when changing summability, regularization, or scale

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Möbius Cancellation shows three independent failures of naive one-scale smoothing: Gamma normalization can manufacture a target exponent, high-order kernels can hide current-scale endpoint changes, and a multiplicative terminal prime-slab twist with exact Möbius support remains invisible to standard one-scale pretentious and fixed-order Riesz data at square-root resolution. Xi Flow gives the positive counterpart: changing from pure near-`L^1` spaces to `L log L` genuinely removes the Hilbert-transform loss at the correct `h^2` scale.

A useful carrier change must therefore come with a source-independent inverse/visibility theorem at the target scale, or explicitly name the extra arithmetic structure that supplies it. A smaller transformed statistic is not automatically stronger cancellation.

## Measure coercivity in the quotient that actually carries the arithmetic direction

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Weil Inertia supplies the current model. Raw Vandermonde collapse can be produced by close centers while the true grouped `g/h` anti-invariant quotient remains transverse. WI-132 identifies the sharp normalized carrier: with `S=U^*(I-P_V)U`, `a=lambda_min(S)`, and off-line square-depth `D_2`, Lamzouri's exact slack contains `4aD_2`.

The live theorem is source-valid control of this quotient: prove a lower bound on `a` or an appropriate spectral-tail substitute on configurations with macroscopic `D_2`, or prove from the zeta source that `D_2` itself must vanish. More generally, every proposed rank, determinant, condition number, or marginal must be shown to control the exact target quotient after trivial geometric degeneracies are normalized away.

## Require exact arithmetic scale, not only positivity or the right asymptotic shape

**Linked intuitions:** `MI-004-critical-axis-is-not-zero-selection`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Weil Positivity now classifies several canonical positive refinement responses: stationary coarse terms, extensive regular repeated-prime traces, bounded new-prime puncture responses, and singular Green traces with generic harmonic `log m` growth. The last even has the right qualitative logarithm, but not the exact finite Mangoldt birth and not prime specificity.

A decisive positive must derive the finite arithmetic coefficient and its sign from the source before or inside the global assembly. Asymptotic resemblance to `log p`, a universal critical half-reflection, or positivity in an auxiliary carrier is not enough.

## Convert nonperturbative Xi bulk coercivity into source-controlled envelope and boundary flux

**Linked intuitions:** `MI-006-classify-the-boundary-model-before-calling-it-arithmetic` and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Xi Flow now has a sharp downstream target. The `L log L` adjacent-gap carrier matches the `h^2` scale and controls the Cauchy driver. XF-014 makes the exact nonlinear real-gap dynamics a positive-conductance diffusion, and XF-015 proves inverse-square/Cauchy bulk coercivity uniformly on finite-amplitude blocks with bounded upper gaps.

The open bridge is source-specific control of the upper-gap envelope and external flux on `Theta(log^2 T)` windows, uniformly through nonlinear evolution and collision cascades. Analytic-frontier inputs are useful only if they arrive at this block width, norm, envelope, and precision.
