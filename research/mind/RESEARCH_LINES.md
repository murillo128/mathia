# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to Möbius, invisible curvature, universal support geometry, fixed-kernel projection, homogeneous rigidity or programmable motion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate source provenance, target sensitivity, relational structure, reachability, readout fidelity, motion law and source specificity.

Weil Positivity has closed scalar recovery, fixed positive multi-channel recovery, finite multiplicative-equivariant linear depth, flat site transport, faithful pointwise metrics and ambient output curvature invisible to the commuting source image. WP-300 closes the prime-symmetric scalar-projective upstream escape: full prime-relabeling symmetry leaves only flat or universal fermionic commutator holonomy.

WP-301 closes the direct non-scalar frustration-free continuation. For arbitrary noncommuting local defects, a positive sum of squares has kernel equal to the intersection of the local kernels. Exact mixed-prime rejection is therefore already a two-prime fixed-space condition, higher matrix coherence does not enrich the zero-mode selector, and generalized-prime controls reproduce the same common-kernel mechanism. A positive archimedean operator included in the same frustration-free sum is read only through its kernel, erasing its nonzero spectral data.

WP-302 now closes the two most immediate moving-kernel implementations. Finite-dimensional fixed-generator unitary motion has constant intrinsic local geometry and only bounded regular relative readouts, incompatible with the logarithmically growing archimedean digamma symbol. But a freely chosen projector path is too flexible: its positive Grassmann metric can realize any prescribed smooth positive profile by choosing the path speed. The live curvature route must therefore be **source-forced before target comparison**: nonhomogeneous/infinite-dimensional/singular/nonlocal enough to escape homogeneous rigidity, yet constrained enough that the Gamma profile cannot be programmed by hand, with the informative spectrum surviving the final observable.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) keep conditioning, source supply and target sensitivity distinct.

Analytic Frontier now has two sharp source-side interfaces. On the same-profile cascade, ANF-184 locks odd-dilation return density, integer activation and local source resolution to the frozen-stage clock `A~ell^2`, leaving source-energy non-excisability plus depth-uniform constants. On bow endpoint memory, ANF-185 removes the adaptive sample from the critical `X/a_X` aperture: triangular twist averaging becomes one deterministic one-height high-pass with transition width `a_X` that detects every sector-aligned dangerous packet. The remaining theorem is absolute source control of that weighted prime-minus-Ramanujan residual, or a proof that the actual source cannot realize the matched packet.

Arithmetic Fidelity has now separated four currencies. AF-338 reduces full-Euler fixed-order sign-regular transport from quadratic to linear depth by preserving harmonic collision geometry: order `r` is eventually available once `sigma>r-1`. AF-339 shows that the pole-cancelled zero-sensitive discrepancy `Lambda-1` has ordered sign variation `~2(A-1)P/log P` on `[P,AP]`. AF-340 then shows that zero sensitivity itself is not the cause: every `Lambda-c` retains the zeta-zero poles, uncentered `Lambda` is sign-simple but retains the main pole, and `c=1` is the unique constant background cancelling that pole while lying in the growing-sign regime. The live bridge must therefore quarantine/remove the main background by a genuinely structured recoding with audited zero-divisor recovery, prove a growing-order observation theorem, or use another finite source certificate; retuning a constant baseline cannot buy both resources.

Nyman--Beurling now has a two-layer source restriction on finite high-zero target rescue. NB-117 identifies finite target access with weighted interpolation of the harmonic minimizer `C+D/n` by confluent actual-zero power sums. NB-118 adds exact finite Nyman orthogonality: once the finite capture exceeds a fixed residual baseline `d_M^2`, a target-bearing witness must retain only `O_M(1/R)` of its continuum norm in the first `R` natural cells, independent of zero height. The `M=3` baseline is already about `9.6%`. The live theorem is to exclude such strongly target-aligned actual zero-power packets at inverse-section retention, or construct one and understand its consequences.

Prime Flute's current sharp-cutoff completed-angle branch remains noncompact: adjacent Fourier modes straddling the hard cutoff generate order-one canonical correlations on infinitely many separated modules. A viable continuation must change splitting/observable rather than sharpen the same compactness estimate.

Prime Lattice has now removed both fixed-order Pohozaev admissibility and mixed Hadamard shape calculus from the scalar-stress gate. PL-308 applies Grubb transmission theory to the actual completed-Weil right-hand side and gives, for every fixed `s>0`, the exact weak virial identity

`Gamma(1+s)^2/(2s)(|tau_+|^2+|tau_-|^2)=E_s(w)+a^(2s)[b_a(w)+V_a(w)]`.

The translated source fronts are therefore not a fixed-order obstruction. The live analytic currency is uniform `s->0` control of the weak virial or source-structured derivative compactness strong enough to identify the zero-order aperture derivative. The rational-prime sign of that derivative remains a separate gate.

## Which cancellation theorem remains after representation enlargement and single-horizon source laws stop adding endpoint force?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now has both an information classification and a progressively stronger extremizer test for its cumulative field. FD-117 makes the whole discrepancy field invertibly equivalent to the quotient-Mertens staircase; FD-118 compresses the same-horizon Fourier carrier to the divisor-closed floor-quotient skeleton `Q_N` of size `2 sqrt(N)+O(1)`. FD-119 shows that its Möbius equality ray survives any fixed true Mertens prefix plus the exact global floor-quotient identity at bounded extra energy. FD-120 then imposes the full single-horizon squarefree/unit-amplitude increment law, terminal normalization and the same exact divisor identity, yet still constructs horizon-dependent nonmultiplicative witnesses with `E_N^Q=Y_N(N)^2+o(N)` at critical amplitude. The missing force is therefore not constraint depth alone but **source correlation and coherence**: actual Möbius multiplicativity/sign correlation, cross-horizon compatibility, one horizon-independent sign sequence, or another law whose violation has critical-scale energy cost.

Möbius Cancellation has sharpened its sparse odd-support escape from one-character bias to exceptional-family rigidity. MC-289 shows that non-negligible effective occupancy forces a substantially negative generated quadratic product character. MC-290 proves that if analytic input controls all but `J` characters above `-delta`, the exceptional set must absorb essentially the entire forbidden odd moment; when `epsilon_m=delta^(m-2)(eta^-1-1)` is small and `log J=o(m)`, one exception becomes nearly the exact affine character. The remaining theorem can attack effective occupancy, conductor, exceptional-family size/bias, or rule out this near-affine concentration for the actual Legendre shell.

## Which exact null-mode constraint survives support relaxation in Weil Inertia?

WI-288 reduces complete one-signed symmetric two-island screening to the nearest-prime-power Lambert gap, WI-289 identifies prime squares as the unique critical composite layer, and WI-290 proves that the composite-safe residual contains at least `X^(3/8-o(1))` positive-length components on `[X,2X]` from the published prime-gap second moment.

WI-291 now quantifies the ordinary-prime side. The Lambert threshold forces any eligible upper host to be a `gg sqrt(X) log X` prime gap. Heath-Brown's published large-gap theorem confines the eligible center set to total length `O(X^(3/5+epsilon))` and at most `O(X^(1/10+epsilon)/log X)` host gaps; in logarithmic separation coordinate its dyadic measure is summable.

The bottleneck is therefore a sharply asymmetric cross-scale incidence theorem, not abundance. A polynomially fragmented safe reservoir generated largely by prime gaps near `sqrt(x)` must be localized relative to a much rarer selected family of giant gaps near `x`. The exponents `3/8` and `1/10` do not yield a pigeonhole intersection. Null-equation amplitudes/signs or a different support topology remain separate escapes.

## What should Prime Circle do after the first square-root rational resonance is spectrally negligible?

PC-285 is the unconditional matched-control boundary. PC-286--PC-290 show conditionally on RH that, once total source count is matched, the exact one-profile normalized boundary-log singular spectrum collapses toward the control for every `B=o(sqrt(q))`.

PC-291 removes the most immediate square-root escape. PC-271 guarantees an exact or bounded-residue denominator relation at `B~sqrt(q)`, but the corresponding microscopic residue band has normalized operator norm `o(1)` and therefore perturbs every ordered singular value by `o(1)`. The first rational relation is a genuine geometric resolution threshold, not an order-one amplitude threshold for this one-profile statistic.

The surviving one-profile question is whether a **mesoscopic** portion of the log-sine Green remainder can couple to exact prime placement at order one at square-root bandwidth. Otherwise the architecture must retain information discarded before singular-spectrum scalarization: cross-level, multi-source, directional, phase-sensitive, raw-resonance or noncommuting data. The scalar source count remains a separate resource because the PC-290 control is explicitly given `M_q`.

## What survives after regular-CA placement and every fixed algebraic height correction collapse to the same edge profile?

RE-119 shows the regular CA mesh samples the full leading stable profile `J`; RE-120 proves that, with a fixed horizontal zero gap, every coefficient in the `1/log log C` hierarchy is a stable-filter descendant of that same profile. RE-121 now weakens the gap hypothesis sharply: after retaining the full subedge strip, the same fixed-order hierarchy survives whenever the weighted near-edge zero mass is superpolynomially flat, even if real parts accumulate at the edge.

Conversely, an algebraic deviation of size `nu^-A` requires polynomially detectable weighted zero mass inside a layer of width about `(A+1)log(nu)/nu`. The live finite-edge source is therefore the **rate and signed phase structure of near-edge spectral mass**. Absolute mass gives a necessary envelope but not a signed anomaly because phases may still cancel. The nonattained-edge and `Theta=1` branches remain separate.

## How large can a pointwise prime-phase fluctuation be after distributed gauge-invariant frustration is priced?

VIS-213--VIS-216 reduce fixed-dimensional worst starts to unit-modulus torus optimization and give cycle-holonomy, fractional-packing and normalized magnetic certificates. VIS-217 proves that the normalized magnetic relaxation can reach Haar-RMS scale only if effective edge count is `O(n)`.

VIS-218 now computes the actual prime graph in the whole strictly subcritical regime `H log y/y->0`: `W_1~y/(H log y)`, `W_2~1/H`, so `N_eff~y^2/(H(log y)^2)` and `N_eff/n >> y/(H log y)->infinity`. The normalized magnetic certificate therefore misses the RMS scale by a diverging factor regardless of edge phases. That spectral relaxation is closed as a standalone route in this regime; the remaining static possibilities are stronger fractional cycle packing or the exact fixed-modulus torus constraint. Access time remains separate.

The cross-line lesson with Weil Positivity is now sharper. VIS-218 exhibits **quantitative relaxation loss**: genuine gauge-invariant magnetic geometry survives, but its normalized spectral certificate has the wrong effective dimension. WP-301 exhibits **categorical readout loss**: a frustration-free zero-mode projection erases nonzero local/archimedean spectral information. WP-302 adds **motion-law loss**: a homogeneous moving geometry is analytically too rigid, while an unconstrained path can fit the target by construction. In all three cases the question is not whether nontrivial geometry exists, but whether a source-forced observable retains the information, freedom and scale consumed by the destination theorem.