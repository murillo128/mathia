# Global mathematical questions

## Can the source-native pole tangent be lifted to a canonical completed operator with the correct sign?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate continuum provenance, spectral layers, model selection and sign.

WP-267--WP-277 rule out a sequence of free constructions: local positive hosts, inverse reconstruction from an already supplied target, exact bounded pure-point-to-a.c. intertwining, and bounded/subcritical mixing of compact-resolvent sectors. WP-278 then gives the surviving infinite-volume category a source-native measure-level realization: the critical Mangoldt half-density has high-energy tangent `cosh(x/2)dx`, exactly the polar continuum of the completed carrier.

WP-279 shows why the same scalar tangent expansion cannot simply continue to Gamma. After the pole term is removed, nontrivial-zero modes lie parametrically above the Gamma layer, even under RH. WP-280 tests the canonical primitive escape: retaining the critical Chebyshev primitive really restores the central Gamma slope, but the normalized logarithmic-primitive limit that isolates it is itself equivalent to RH, as is eventual sign of the same primitive.

The remaining burden is genuinely global: construct an independently selected operator/boundary/cohomological architecture that couples finite and archimedean sectors before scalarization, retains source fluctuations, and proves the Weil orientation without subtracting the nontrivial-zero divisor or importing an RH-equivalent primitive limit/sign condition.

## Which source restrictions actually shrink the target-reachable failure set at the metric the theorem consumes?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) distinguish source reduction, exact failure incidence, non-vacuity and stable distance from failure.

AF-300--AF-305 replace ambient deleted directions by intrinsic source dimension, reachable positive bodies and a sharp discrete saturation boundary. AF-306 proves that every fixed saturated support can approach zero arbitrarily closely. AF-307 classifies the first prime-support exact cases, AF-308 makes the ambient exact-hit cone Lebesgue-null, and AF-309 proves that the same cone is dense with dense complement. The exact-hit predicate is therefore nowhere perturbatively stable. The open `k>=5` prime problem is exact arithmetic incidence of the special log-prime vector in a dense null set; generic measure, topology and near-zero access cannot decide it.

Prime Circle gives a matched-control analogue. PC-272--PC-276 show that ordered information, the raw near-resonance law and every finite fixed-bandwidth logarithmic centering can all be classical source data. PC-277 closes the intrinsically normalized scalar law whenever `log B=o(sqrt(log q))` using a denominator-matched exact-`Li` control, while `(B+1)^2>q` forces universal exact rational resonance. Only the intermediate rational-microscopic scalar window remains unresolved before one must leave that observable.

PF-315--PF-317 give a complementary operator reduction. The full physical angle is weak trace exactly when both `T_H` and `Z_LH` are, and the direct channel is controlled by only three local mixed-frequency pant offsets. PF-318 lowers the sufficient local currency further: logarithmic target rank plus Hilbert--Schmidt leakage `O((1+log P_n)/P_n^2)` already gives weak trace. Worst-direction reciprocal-prime operator norm is not required.

Visual Exploration sharpens the observation-scale bill in another direction. VIS-199 reaches a broad stretched-exponential/subpolynomial dephasing corridor, while VIS-200 proves that changing the sieve/taper cutoff cannot improve its leading four-form pointwise threshold. The residual problem is aggregate local-factor/fiber structure or a different dephasing resource, not a better scalar split point.

## Which residual cancellation theorem survives after generic abundance, exact embeddings and conductor descent are exhausted?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) place Farey and Möbius beyond their previous generic barriers.

FD-083--FD-085 show that full-counting occupation and polynomial additive blocks do not imply a finite-entropy multiplicative chain. FD-086 restores the physical source and makes the normalized Jordan/Schur packet coherent throughout each frontier-length block. FD-087 then separates exact embedding from useful normalized capacity: every integer dilation contains a uniformly conditioned exact lower-state copy, but all anchors forced at or below the additive block length lie in a terminal quotient sector carrying only `o(1)` of normalized frontier energy. The next Farey theorem must reach a larger anchor, a bulk/signed carrier, a destination-energy saving, or a multihorizon composition law that does not rely on small common divisors.

MC-252--MC-256 eliminate nonlocalized fixed-order residue rigidity. MC-257 removes the wild prime-square direction from the actual moving shell, making every blind character factor through the squarefree shell modulus. MC-258 then audits the source projector and finds exact normalization compensation: descent from `R^2` to `R` introduces an exterior `1/R`, leaving the support main exponent unchanged. The remaining Möbius gain must therefore be analytic cancellation at squarefree conductor or genuinely localized tame cross-prime structure, not support recounting at the smaller modulus.

Both lines now distinguish existence of a formal transport/reduction from whether the destination actually assigns it enough normalized mass or cancellation power.

## Can source-specific regularity and source-specific order become quantitative enough to control a Weil crossing?

Prime Lattice and Weil Inertia now separate generic regularity/order structure from what is selected by the actual source equation.

PL-285--PL-291 move from inverse-log ambient transport through graph-domain `log^-2` transport to every fixed logarithmic Fourier moment for actual eigenstates. PL-292 quantifies the hierarchy: the moments grow at most like `exp(C m^2 log m)`, yielding an optimized subpower modulus faster than every fixed inverse-log power. But physical cutoffs have at least `(cm)^m` cost on these spaces, so this bootstrap cannot become Hölder merely by tightening generic cutoff constants. The next gain must be state-specific endpoint cancellation, positive-order regularity, or direct positivity continuation.

WI-264--WI-267 show that finite-lag screening and generic first-crossing substitutes fail to capture Suzuki's source equation; every nonzero localized null mode must be prime-translation support-dominating. WI-268 closes the generic sign route because the unrestricted archimedean cross kernel changes sign before the first prime lag. WI-269 derives a source-specific replacement order: the complete multiplier ground-state form is PSD, finite partitions produce PSD signed-source Laplacians, and an uncancelled nodal tariff forces prime-lag/short-range opposite-sign overlap to be financed by long-range archimedean overlap.

The live Weil-Inertia bridge is whether the whole PSD multiplier family plus first-crossing/support constraints can rule out that long-range compensation and force a source-relevant prime overlap, or whether a source-compatible screened mode realizes the escape. Generic Perron--Frobenius structure remains unavailable.

## Does terminal visible correlation entropy rule out fixed-ray Nyman prediction?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate stationary inverse calibration, radial floors and the actual visible future geometry.

NB-082--NB-085 reduce persistent continuation to finite-future prediction, construct the exact source-specific Toeplitz certificate along integer-dilation descendant rays, and show that every such ray has a strictly positive Szegő prediction floor. The qualitative fixed-ray endpoint is the aggregate floor charge `S_(R,q)`.

NB-086 now gives a finite-window obstruction to bounded charge. On the terminal block `j>=ceil(R/q)`, radial descendants are already beyond the cutoff, while the full causal predictor still sees intermediate innovations. The radial charge therefore dominates the normalized terminal Gram determinant deficit `-log det R_(R,q)`. Bounded charge forces bounded total squared off-diagonal correlation energy across this growing terminal block; divergence of that visible correlation energy forces the radial charge to diverge.

The next theorem should estimate this source-specific terminal correlation entropy. Divergence kills every fixed-`q` radial certificate and redirects the proof toward cross-ray/nonradial cancellation. Bounded terminal entropy is only necessary, not sufficient: the remaining nonterminal contribution to the floor charge would still need control.

## Which low-layer selection constraint survives after deep and same-depth Robin chambers become negligible?

RE-083--RE-087 reconstruct selector geometry, isolate anchored gap costs and show that marginal candidate prime-gap capacity is too abundant to force a contradiction. RE-088 removes sufficiently deep selected boundaries under an off-RH exponent gap.

RE-089 now narrows the finite low-layer carrier above `Theta>3/5`. A chamber whose two adjacent support boundaries come from the same fixed higher layer `j>=2` pulls back to a square-root-sized ordinary prime gap, and Heath-Brown's total large-gap length bound makes the total threshold measure of every fixed `j/j` family vanish. In particular pure second-layer chambers are threshold-negligible in this regime.

Outside vanishing threshold measure, the remaining cells therefore either touch a first-layer atom on at least one boundary or have genuinely different higher-layer depths in a finite range. The next Robin theorem must exploit this joint selection structure: mixed-depth resonances, tied power events, simultaneous boundary constraints, skipped spans or rightmost-maximizer compatibility. More tail counting or same-depth scarcity does not touch the surviving carrier.

## Can the local direct flute leakage and the conditional channel satisfy the required ideal counts?

PF-316 removes the residual-source ambiguity at the endpoint: the physical angle is weak trace exactly when `T_H` and `Z_LH` are. PF-317 makes the direct channel local through positivity, nearest-neighbor topology and explicit seam normalization. PF-318 shows that a Hilbert--Schmidt mean-square bound on each local nonconstant-low/high block is already endpoint-complete because the low target rank is only logarithmic in the prime label.

The remaining flute burden is asymmetric and concrete: prove aggregate local high-frequency leakage `O((1+log P_n)/P_n^2)` in squared Hilbert--Schmidt norm and independently control `T_H`, then close the compatible infinite realization, symmetric side and signed target composition. Constant-mode pant blowup is a required negative control, but worst-case `1/P_n` operator-norm control of the mixed corner is no longer the necessary target.