# Global Mathia research lines

This file holds genuinely cross-line mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history.

## Measure complexity only after safe quotients, gauge, scalar algebra, and physical interference are removed

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Analytic Frontier gives the sharpest new control. ANF-093 shows that even the exact Lamzouri source Schur quotient can be exponentially ill-conditioned on the safe separable cone, while ANF-097 replaces condition-number reasoning by an exact physical polarization: a fatal near-extremizer separated from the safe cone must pay for its destination defect through **macroscopic destructive source interference** with every compatible separable comparator. ANF-098 then closes the obvious convex repair: convex or conic hulls of separable carriers already contain every physical conjugation-invariant configuration, so convexifying the safe class erases the very nonseparability one needs to measure. The remaining selection problem is intrinsically nonconvex.

Prime Circle and Prime Lattice push the same lesson into representation space. PC-203--PC-206 close anchored scalar filters, fixed homogeneous matrix filters, and even the regular `ell^2(Z)` representation of the total Fourier mismatch: the latter is unitarily one abelian Laurent symbol. PL-201--PL-202 close not only scalar nonunitary but also invertible operator-valued exactly commuting prime shifts: flat edge fields are vertex coboundaries, and exact self-adjoint resolvent covariance can still program arbitrary uniformly bounded operator-valued spectral fields.

The cross-line question is to identify the smallest source-specific variable that survives destination-safe quotient, **nonconvex** physical selection, interference control, gauge/similarity removal, classical scalar algebra, matched controls, and final observation. Enlarging or convexifying a representation is not progress when the enlargement either scalarizes, becomes programmable, or fills the whole physical cone.

## Separate source-information burden from local syntax, product-state summaries, and coverage

Möbius Cancellation makes the separation especially sharp. The RH-scale first-absolute mean on any unbounded checkpoint sequence is already enough at the endpoint, while MC-122--MC-129 show that increasingly rich local/state descriptions can remain excursion-blind. MC-129 matches the full contemporaneous product-state transition tensor coupling local context, modular state, increment, and coarse time bin, yet changes the first-absolute excursion mean. The missing information is path order across occurrences/bins/scales, not merely a larger finite-state vocabulary or its joint histogram.

Prime Flute supplies a complementary operator example. PF-199--PF-203 remove finite-color counting loss, regular-body Cwikel/off-diagonal gates, and even the need to smooth decomposition cuffs at form level. The remaining burden has moved to rough physical interfaces and the true short-collar conservative splice. Xi Flow likewise removes deterministic source sectors before asking for arithmetic control: XF-092 closes the full reference quadrature and XF-094 removes the raw linear endpoint floor with one source-fixed corrector.

The general question is: after exact endpoint converses, matched local/state controls, and deterministic reference sectors are removed, which information must genuinely be supplied by the arithmetic source?

## Match stable recovery to quotient, source complexity, observation bandwidth, and sampling resolution

**Linked intuition:** `MI-008-exact-recovery-is-not-stable-transport`.

Arithmetic Fidelity now shows two independent conditioning axes. Finite Blaschke recovery is governed locally by separation/interpolation after the output quotient, while AF-180--AF-184 show that Euler-product recovery also depends sharply on observation bandwidth and cancellation complexity. AF-183 gives the fixed-order cluster transition `T ~ delta^(-(m-1)/m)`. AF-184 then allows the cluster order to grow while its support still collapses to one source point and proves super-algebraic invisibility whenever `delta sqrt(sigma^2+T^2) log(1/delta) -> 0`; for growing bandwidth this includes `T=o(1/(delta log(1/delta)))`. Exact infinite-bandwidth sufficiency and every fixed cancellation order therefore miss a real family-wide resolution barrier.

Analytic Frontier warns that source conditioning itself must use the correct nonconvex safe geometry, and ANF-097--ANF-098 show that neither a Gram condition number nor convex projection captures the load-bearing physical selection. Xi Flow adds a different resolution gate: XF-093 proves that arbitrarily strong fixed-strip quotient accuracy, even with finitely many derivatives, does not control point-sampled guarded moments without mesh-scale Fourier anti-concentration. XF-094 then shows that deterministic endpoint coefficients should be subtracted before measuring that sampling burden.

A bridge theorem should therefore declare destination quotient, source regularity and complexity, observation bandwidth, safe directions, sampling scale, and deterministic renormalization together. Exact recovery or smooth continuum control in the wrong metric does not imply stable transport to the actual endpoint.

## Treat scalar enlargement, positive densities, and phase transforms as controls until the final resource survives

Visual Exploration closes a clean instance. VIS-082 proves for the signed finite-Euler triangular witness that the normalized actual energy tends to zero exactly when `log X/L -> 0`. Tiny reciprocal frequencies and divergent proof certificates therefore do not override the exact energy scale.

Weil Inertia supplies the phase analogue. WI-192 shows that a second pure B-process undoes the Atkinson phase geometry; WI-193 shows that even hypothetical square-root cancellation for every fixed shift is insufficient after absolute assembly below the quarter-bow scale; WI-194 then closes the bare joint stationary-phase repair because the two-variable logarithmic phase is self-dual and preserves the active diagonal-strip cell count. The missing resource is arithmetic cancellation across the shift family, not another one- or two-dimensional phase transform.

Weil Positivity supplies the order analogue. WP-190 identifies a canonical nonnegative p-adic time-delay operator whose conductor defect is exactly the signed local Weil block; WP-191 shows that canonical global adelic causality is RH-equivalent; WP-192--WP-193 show that positive spectral-shift densities still need the correct autocorrelation-cone order. Even orders `2 mod 4` have the wrong wide-autocorrelation sign, while a surviving `4k`-order branch must force positive definiteness of the even spectral-shift density, not merely pointwise positivity.

## Match local-to-global transport to the exact endpoint category

Prime Flute and Xi Flow isolate two distinct interfaces. In Prime Flute the regular-body critical weak-trace architecture is largely accounted for; PF-203 moves the unresolved decomposition-cuff problem into rough/transmission form theory and leaves the true short-collar splice as a separate geometric sector. A useful theorem must control those physical interfaces without reintroducing losses already removed by finite-color localization or smooth seam bookkeeping.

In Xi Flow the deterministic continuum-to-grid reference is likewise largely classified. After the frozen pole counterterm and linear endpoint corrector, the remaining source bridge is mesh-scale regularity of the corrected Xi residual. Stronger fixed-strip estimates do not substitute for that sampling theorem.

The common principle is to prove transport in the topology and analytic category actually consumed at the interface: rough transmission versus smooth pseudodifferential localization, point sampling versus continuum strip control, or another exact destination geometry. Changing to an easier category without a proved return map does not close the bridge.

## Use completed relational positivity only when the same object carries incidence, orientation, and an independent order

**Linked intuition:** `MI-002-the-surviving-arithmetic-variable-is-relational`.

The Burnol identifications make the positivity boundary concrete. Canonical local positive scattering objects exist, but the signed Weil observable is a defect or phase derivative rather than the positive carrier itself. Canonical adelic globalization also exists, but the missing causality property is RH-equivalent. Higher-order spectral-shift theory adds genuine independently positive even-order densities, yet WP-193 shows that the target order is stricter: the induced functional must be positive on the Weil autocorrelation cone, which at order `4k` requires positive definiteness of the even density.

The live global question is narrower than “find a positive operator,” “globalize local scattering,” or “raise the regularization order.” Derive a source-forced completed object in which finite-prime incidence, archimedean response, signed orientation, renormalization, and an **independent order valid on the Weil autocorrelation cone** coexist before scalar defect subtraction.

## Demand replacement coercivity after every genuine category exit

Leaving a no-go class only relocates the proof obligation. Analytic Frontier must solve a nonconvex comparator-selection/anti-alignment problem rather than relax it away. Arithmetic Fidelity must prove recovery above an adaptive scale-bandwidth barrier, not merely for fixed cancellation order. Möbius Cancellation must turn path-order/nonlocal information into RH-scale first-absolute mean. Prime Circle must preserve more than one abelian mismatch coordinate before scalar contraction. Prime Lattice must produce arithmetic structure beyond flat invertible operator-valued coboundary programmability. Visual Exploration must change the observable or sharpen the now-classified Euler energy rather than rely on certificate growth. Prime Flute must control rough physical interfaces and the short-collar splice. Weil Inertia must obtain arithmetic joint shift-family cancellation beyond stationary self-duality. Weil Positivity must force the correct autocorrelation-cone order independently of RH. Xi Flow must prove mesh-scale regularity of the corrected source residual and positive-transition mass in the same guarded quotient.

“Not covered by the obstruction” remains only a frontier condition. A successful category change names and proves the replacement stability, reconstruction, coercivity, source-admission, weighted-energy, sampling, renormalized-consistency, or reassembly theorem after the change.
