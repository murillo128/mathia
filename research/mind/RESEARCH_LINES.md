# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry or programmable completion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity has now closed both the orientation-preserving affine orbit of the canonical Mangoldt carrier and the canonical scalar Herglotz projective gauge. WP-307 proves that the pole-normalized logarithmic coefficient `J=A_0-C_+^2/2` stays positive under every density-normalized positive affine spectral recentering. WP-308 then classifies constant positivity-preserving Möbius recodings of the scalar reflected Herglotz response: non-affine maps compactify the logarithmic end, while every map preserving that end is positive affine and retains the wrong positive `y^-2` curvature.

The remaining positive route must therefore introduce genuinely new finite--archimedean geometry before scalar comparison: an operator/matrix-valued boundary sector, source-derived `z`-dependent transform, quotient/compression with a new positivity theorem, determinant/scattering object or nonseparable finite--archimedean coupling. Neither input-coordinate nor scalar output-coordinate retuning is still a live repair.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) keep conditioning, source supply and destination sensitivity distinct.

Analytic Frontier still requires a joint arithmetic-placement law relative to its distinguished logarithmic carrier; matched one-point statistics are exhausted.

Arithmetic Fidelity now separates an invertible representation from the quotient that actually loses the discriminator. AF-346 shows that sign/gap concentration can be saturated by simple perfect-power recodings, and AF-348 shows that after enough fixed-order Riesz smoothing the sparse weighted `k`-power background is only bounded-error distinguishable from dense unit mass throughout the cutoff axis. AF-349 proves simultaneously that the **complete** finite-order Riesz profile remains injective: coefficients are recovered by differentiating `X^r R_r(X)` and the Dirichlet series by a nonzero Mellin beta factor. The demonstrated loss therefore occurs only when the terminal observer quotients away the bounded residual (or uses an equally coarse asymptotic readout). The live resource must be both source-discriminating and stable in the exact terminal metric.

Nyman--Beurling now has a sharp amplitude bridge as well as a phase modulus. NB-128 gives the full natural prefix local ordinate resolution `Theta(1/log R)` inside a height window `Theta(R)`. NB-129 shows that this phase information becomes target-visible at height `|T|` once the target-angle defect is `O(1/|T|)`, and that this reciprocal-height threshold is sharp for the NB-117 Dirichlet-energy data: an `O(1/|T|)` matched residual can erase an entire phase-sensitive block. The missing bridge has therefore moved to genuinely zero-power structure that rules out that sharp amplitude-hiding control at order-one capture.

Prime Lattice similarly identifies rather than merely localizes its endpoint channel. PL-313 proves that a resolved physical corner reaches the first regular outer correction through one weighted scalar `M_±`, carried by the unique obstructed outer direction. The open theorem is the actual completed-Weil overlap decomposition that makes this scalar finite, matches the singular amplitude and balances it against the source.

Prime Flute's hard-cutoff angle remains noncompact, so progress there still requires a new split or observable.

## Which growing source law is genuinely informative rather than sparse, diluted, under-resolved or circular?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now has three sides of the scale-breadth boundary. FD-128 shows that even infinite exact Möbius ancestry can remain cheap when the active prime set is sufficiently sparse. FD-129 shows that complete prime-prefix ancestry can become circular when paired with a stage-uniform critical envelope, because it reconstructs the physical Möbius prefix and imports the Mertens target. FD-130 shows that the apparent middle ground of **full prime-direction coverage with small unweighted mean ancestry defect** is also insufficient: a fixed-rank gauge cut contributes only `O(H)` defective edges inside a graph with `H omega(1)` edges, so the normalized defect vanishes while coefficient disagreement stays at positive density. The useful source law must therefore be broad, noncircular and sensitive to where defect mass sits—rank-weighted, local, growing-depth, or otherwise destination-coercive.

Möbius Cancellation supplies a different scaling obstruction and a priced arithmetic gain. MC-298 proves that every fixed finite hierarchy of endpoint intersections can be moment-matched while Page parity changes. MC-299 shows that a genuinely near-negative Page exception triggers Deuring--Heilbronn repulsion and enlarges the source-cost band in which all other shell modes are forced balanced by a `log log log y` factor. MC-300 then inverts that same black-box inequality: with only the current coarse exceptional-zero input, the uniform fixed-accuracy conductor envelope is precisely Lambert-W scale `O(log y log log log y/log log y)`. A parametrically wider band requires stronger exceptional-zero information, collective family control or a different carrier; constant retuning cannot supply it.

Together with AF-349 and NB-129, these results sharpen the discriminator: the relevant resource is not “more structure” in the abstract but a law whose **order, density, normalization, resolution and terminal metric match the destination without importing the endpoint itself**.

## Which support geometries survive the one-signed Weil first-crossing constraint?

WI-294 changes the large two-island frontier. Complete screening of the actual nonnegative null mode makes the pole term strictly positive, so zero archimedean energy requires negative gamma energy. Fourier uncertainty then imposes an absolute positive lower bound on support measure. The PNT forces a completely screened symmetric two-island width to vanish at large aperture, contradicting that floor.

Thus the large one-signed symmetric two-island branch is closed without a Cramér-scale prime-gap theorem. The live complete one-signed problem is whether a more complicated support topology can retain fixed positive total measure while avoiding all active prime-power translations and satisfying the null equation. Sign-changing modes, incomplete screening and bounded aperture remain distinct.

## What should Prime Circle retain below the diffuse square-zero defect?

PC-295 preserves the universal Green singular sequence, PC-296 collapses eigenvalues, and PC-297 identifies the universal square-zero operator class. PC-298 quantifies the defect: under the same diffuse regime there is a square-zero approximant with expected operator error

`r_q = delta_q^(1/3) + delta_q^(1/2) log q + (log q)/N`.

Every shrinking pseudospectral scale `epsilon_q` satisfying `epsilon_q >> r_q` remains universal after its natural rescaling. PC-299 now identifies the first intrinsic quadratic object below that scale: the cross-Gram loop defect is exactly a Fejer-smoothed source autocorrelation, with a universal `1/N`-type baseline and prime-specific excess equal to a classical additive exponential-sum power packet. Positive quadratic compression therefore forgets Fourier phase and does not by itself create a new spectral variable.

The surviving one-profile question is no longer merely “does arithmetic live at `O(r_q)`?” It is whether information at that scale survives **outside the autocorrelation quotient**—through cross-Gram phase, higher joint correlations, source-coupled left/right relations, noncommuting profiles or cross-level structure.

## Can the required Robin dominant cancelling doublets persist?

RE-129 merges spacing, comparable magnitude and cancelling phase into one inverse-window companion for a hidden dominant algebraically strong atom. RE-130 removes the diffuse-cloud branch as an independent algebraic escape: the full absolute packet mass is at most `C sqrt(M_K) log(e/M_K)`, so algebraic packet mass always contains an algebraically strong atom.

RE-131 strengthens the geometry under macroscopic hiding. If the dominant atom remains hidden on a fixed relative window, then for every fixed `D>2` a distinct companion must lie at ordinate distance between `c/U` and `C_D(log U)^D/U`, carry at least a `1/log U` fraction of the dominant amplitude, have genuinely cancelling target-demodulated phase, and be horizontally locked within `O(log log U/U)`. The attained finite-edge frontier is therefore an actual two-dimensional zero-configuration question: can an off-critical edge support infinitely many such dominant phase-cancelling doublets? The nonattained edge and `Theta=1` remain separate.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225 supplies the coherent-block lower bound and VIS-226 matches it with the absolute tapered envelope. Throughout `H log y/y ->0`,

`I_abs(B) asy M_abs(A) asy U_abs^R(B) asy y/(H log y)`,

and the Haar-normalized scale is `y/(sqrt(H) log y)` up to constants. The static bounded-versus-divergent question is therefore closed at the full coarse order, not just from below.

The live questions are the critical/supercritical transition, sharp constants and optimizer geometry, faithful matched controls that distinguish arithmetic organization within the known scale, and dynamical access time of the physical prime-log orbit.

## Global discriminator: locate the actual loss, then price scale, transport and noncircularity

The newest evidence strengthens [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md). AF-349 shows that an intermediate transform can be exactly faithful while the later asymptotic observer creates the collision. FD-130 shows that even complete prime-direction coverage can become noncoercive when normalization dilutes a fixed-rank interface. MC-300 turns the current Deuring--Heilbronn gain into an explicit Lambert-W certificate ceiling rather than an open-ended tuning parameter. NB-129 identifies a sharp reciprocal-height target-visibility threshold and proves the ambient energy data cannot beat it. PC-299 pushes the first square-zero residual into a classical Fejer autocorrelation quotient and identifies phase/higher-order information as the remaining escape. PL-313 still isolates a rank-one endpoint channel without the completed overlap theorem. RE-131 upgrades hiding to a tightly constrained two-dimensional zero doublet. VIS-226 closes an entire static scale by matching lower and upper structures. WP-308 closes the standard scalar Herglotz boundary-coordinate gauge without closing genuinely higher-dimensional or `z`-dependent boundary geometry.

The common accounting rule is now stricter: first identify **where information is actually lost rather than merely attenuated**, then ask at what order/density/resolution the surviving source coordinate is visible, whether every intervening map transports it with a quantitative margin in the terminal norm/quotient, and whether the assumptions used to enforce it have already reconstructed the endpoint statement. A route can fail by under-resolution, unstable inversion, averaging/dilution, terminal quotienting, relaxation, universality, certificate exhaustion or circularity. If any transfer between lines is only plausible rather than established, it belongs in a clue rather than in global mathematical knowledge.
