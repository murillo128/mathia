# Global Mathia research lines

This file holds genuinely cross-line mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history.

## Preserve source coupling through the exact operation that exposes the target

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-009-local-correctness-does-not-determine-global-coherence`.

Prime Circle now closes a broad fixed-conductor coordinate category: regular joint coarse/fresh quotient coordinates form a commutative CRT cylinder, while source-forced original-coordinate Bezout winding escapes ballistically only to recenter into the same shift calculus. Prime Lattice closes both finite-dimensional Artin representation functors and a canonical positive global Rankin--Selberg self-correlation: in each case the Riemann zeta contribution remains a factor that richer arithmetic does not constrain. Weil Positivity supplies the marginal version: the Gamma and Bost--Connes critical kernels agree on every prime ray and are both positive, yet correspond to different Cauchy cross-prime couplings, with a continuum of other positive couplings sharing the same marginals.

The common question is no longer merely to retain more source data. Derive the **coupling law that remains nonfactorizing in the exact target-bearing object** before block decomposition, scalarization, positive completion or coordinate functional calculus can erase it.

## Price transport in the exact destination currency, including cancellation coherence

**Linked intuitions:** `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`, `MI-008-exact-recovery-is-not-stable-transport`, `MI-009-local-correctness-does-not-determine-global-coherence`.

Arithmetic Fidelity sharpens the depth problem: the Li root-rate quotient tolerates every subexponential output error, but sublinear source jets are invisible and fixed positive linear prefixes can be worse, manufacturing exponential root-rate violations by interrupting binomial cancellation even after full `xi` completion. Nyman--Beurling shows a different transport failure: target-aware shell mixing can be cheap relative to the lcm, yet if intrinsic shell discrepancy is sublinear the rooted mixed residual converges to zero while the full defect remains large. Prime Flute corrects its physical tangent from the bounded Bessel-four operator to an intrinsic-six unbounded graph, proves a surviving fixed-axis square-root window `1<R<2`, and moves the gate to uniform finite-seam/actual-corridor transport. Xi Flow corrects the old single-threshold picture: the initial outward parabolic boundary is exact, but positive heat can expose a separate edge mode above universal depth `(1/2)log 2`.

The shared theorem surface is a quantitative modulus through the **actual global transform**, not merely source depth, exact recoverability or local regularity. If cancellation or visibility is produced by a global orbit, approximation must preserve that orbit before the final quotient is priced.

## Convert abundant low-energy/localized geometry into source-specific coercivity

**Linked intuition:** `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Analytic Frontier now shows that the bow low spectrum is abundant rather than exceptional: `Omega(K)` positive directions already occur at the sharp `K/N^2` scale, and the positive spectrum is dual to a Fejer Toeplitz finite section plus an explicit positive `O(K)`-rank boundary correction. The missing theorem is projection of the actual demodulated prime source into that sector, not another eigenvalue count. Weil Inertia obtains a complementary depth/localization dichotomy: shallow radial screening is forced back to bow-scale radius `M/log T`, while evasion requires order-`M` weighted mass at macroscopic radial depth. Möbius Cancellation similarly separates centered rough-parity transport from a global zero mode that inherits strong Korobov--Vinogradov cancellation but still misses the required fixed power.

Across these lines, favorable geometry, localization or centered smoothing is not coercivity until the actual arithmetic source is shown to occupy the right sector and the resulting mass/sign is converted into the final count or variance budget.

## Exploit the classified boundary between free interpolation and selected arithmetic rigidity

**Linked intuitions:** `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`, `MI-009-local-correctness-does-not-determine-global-coherence`.

Farey Discrepancy now exactly classifies integer-ratio common-source saturation in its unrestricted relaxation: pointwise saturation fails precisely when bounded squarefree transitions recur, while Cesaro saturation fails precisely when some bounded squarefree family has positive density. The remaining leverage must therefore come from Möbius-specific source constraints or a different horizon geometry, not vague overlap. Robin Extremal supplies a selected-event counterpart: small ordinary-prime envelopes force boundary events onto deep fixed prime-power ladders, and bounded-envelope near-collisions are only `O(log Z/log log Z)` up to height `Z`.

A cross-line question is how to turn these **free-model classifications into source-conditioned exclusion** without escalating to full reconstruction. The decisive input should be the arithmetic restriction that the interpolating/free model violates or the selected-event statistic that makes the sparse exceptional set harmless.

## Resolve the complete stochastic/control null before interpreting a residual

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-008-exact-recovery-is-not-stable-transport`.

Visual Exploration has corrected the bounded-source finite-CUE edge null. The taper must replace the hidden full-circle coordinate window, after which the mean edge profile has regular effective-size sensitivity; the realized diagonal still fluctuates at order `1/L`, but as an exactly removable common mode. The live nuisance is the projected off-diagonal covariance plus finite-height source-transfer error.

This reinforces the general rule that a source-specific residual begins only after the full destination model is frozen at the same scale as the claimed signal. Mean calibration alone is not enough when stochastic covariance can occupy the target amplitude.
