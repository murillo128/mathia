# Global Mathia research lines

This file holds genuinely cross-line mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history.

## Measure only structure that survives the correct quotient, family law, and matched control

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Several lines now classify increasingly rich representations without finding arithmetic selection. Prime Circle closes fixed scalar/matrix packet algebra, separated finite-valence Hardy lifts, the singular zero-depth boundary, the canonical noncommuting weighted power cover, and now its scalar cross-level star algebra. PC-209 reduces each normalized weighted cover to the universal pure shift; PC-210 shows prime products are exactly integer Haar refinement, adjoint overlaps are rational dilations, and canonical infinite-depth prefixes have only the weak zero limit. Primitive-shell geometry has disappeared before the operator-algebra invariant is formed. Prime Lattice reaches the analogous family-level rigidity from Hilbert's resolvent identity: PL-206 kills fixed same-parameter left transport, and PL-207 shows that even arbitrary spectral reparameterization with one fixed bounded edge collapses to scalar affine motion.

Analytic Frontier supplies the destination-side version. ANF-097--ANF-099 replace raw condition number by the exposure of physical near-extremizers to a declared dangerous notch. ANF-100--ANF-103 then remove fake escape geometry: a source-negligible sparse tail can be trimmed without changing the limiting exposure, while a source-heavy sub-square-root tail must cross an explicit endpoint height threshold.

The global question is therefore not how to preserve more coordinates in the abstract. It is to identify a **source-forced relational defect that remains nonprogrammable and quantitatively visible after the exact quotient, family consistency law, matched controls, and removal of source-negligible escape**.

## Match recovery scale to source complexity, kernel structure, bandwidth, and destination danger

**Linked intuition:** `MI-008-exact-recovery-is-not-stable-transport`.

Arithmetic Fidelity now gives a genuine scale phase diagram rather than a one-sided instability warning. AF-185--AF-186 show that adaptive positive clusters can remain exactly separated while derivative-limited observations become super-algebraically close. AF-187--AF-188 then show that the same controls become harmonic residue filters for the Euler logarithm: fixed reciprocal bandwidth gives order-one visibility, while the logarithmic Rayleigh window carries an exact continuum of algebraic fidelity exponents.

Analytic Frontier supplies the destination complement: ANF-099 reduces a fixed-notch stability question to a physical exposure inequality, while ANF-103 tells which apparent noncompactness can be quotiented by source-norm trimming. Prime Flute adds the interface-scale warning: PF-205 preserves a summable physical Orlicz budget on natural-width seams, but PF-206 proves that naive fixed-unit normalization erases exactly that small-support currency.

A bridge theorem should therefore declare the output quotient/metric, admissible source-complexity growth, kernel regularity and algebra, observable bandwidth, destination-safe/dangerous directions, and physical interface scale together. Exact sufficiency or small forward error in a weaker band/category does not imply stable recovery where the final theorem lives.

## Recover global arithmetic information only after both generic summaries and target-equivalent arithmetic encodings are exhausted

Möbius Cancellation makes the source-information boundary unusually explicit. MC-129--MC-132 show that fixed local/modular product states, finite-horizon path types, periodic clocks, and controlled polynomial timestamp moments can all be matched while first-absolute excursion changes macroscopically.

MC-133--MC-134 add a different obstruction: even a fully Möbius-specific representation can fail to make the theorem cheaper. Björner's exact divisibility complex stores `M(n)` as a parity imbalance of two linear-size Betti masses, and its complete ordinary one-parameter persistence is the explicit dyadic barcode `[m,2m)` for odd squarefree `m`. The full barcode retains arithmetic birth data, but its signed live-bar count is still the Mertens target and every reduced class dies within one dyadic step.

The missing variable is therefore not merely “more arithmetic structure.” It must be a relation whose controlled statistic is **strictly cheaper than the target it reconstructs**: occurrence order across events/scales, chain-level or labelled arithmetic interaction beyond the barcode, or another Möbius-specific law that quantitatively forbids the matched rearrangements without simply re-encoding `mu` or `M`.

## Prove local-to-global transport in the analytic category and physical scale actually consumed downstream

Prime Flute and Xi Flow now isolate complementary forms of the same discipline. PF-196--PF-206 progressively remove coefficient-budget, chart-count, regular-body, artificial rough-resolvent, arbitrary seam-width, and pure aspect-ratio concerns. The physical-cuff endpoint has narrowed to two honest options: prove the mixed critical estimate with the piecewise cotangent transport between native smooth resolvents, or keep/recover the natural shrinking-area factor in a smooth critical localization theorem. The true short-collar splice remains a separate geometric gate.

Xi Flow has completed source-to-periodic transport at its **current guarded bandwidth**, but XF-099--XF-100 show why scale must be part of that statement. An extensive collision crystal can be exactly invisible below its symmetry frequency, and the actual current Xi guarded trajectory is `o(1)`-shadowed for fixed heat time by a periodic trajectory generated entirely from the prime-free endpoint prefix. The first prime atom occurs at a parametrically larger index where the existing sampling and stability proof no longer applies.

The common principle is to prove the exact return map between categories and scales rather than replacing a hard interface by a smoother or normalized surrogate. Once a transport gate is closed at a declared scale, do not charge it for a downstream coercivity failure; but do not extrapolate it across a bandwidth phase change that activates new source information.

## Treat phase, sampling, order, and variance enrichments as controls until the final resource survives

Visual Exploration now gives a hierarchy of deterministic Gram-clock nulls. VIS-083 proves fixed-finite-prime Gram phases have the ordinary Haar population law. VIS-084 identifies local arc motion, VIS-085 extends linear inverse-theta subtraction far beyond logarithmic blocks, and VIS-086 shows that quadratic correction pushes the coordinatewise null through `H=o(g_n^(2/3) log g_n)` for polynomial prime support. Crossing the boundary of one finite-order approximation is therefore not itself evidence of arithmetic structure.

Weil Inertia and Analytic Frontier jointly sharpen the bow problem. WI-195 identifies the exact multiplicative short-interval `L^2` norm required by the coupled source. ANF-102 proves that a uniform `o(XK log X)` saving over `U asymp X^2` is impossible because twist averaging restores the full prime diagonal. If the distinguished bow still has a saving, it must be a **resonant signed event**: its off-diagonal covariance must cancel the diagonal macroscopically. The next theorem is not another generic phase transform or absolute-value major-arc bound; it must expose that special negative correlation or rule it out.

Weil Positivity supplies the order analogue. WP-194--WP-197 show that full order-`4k` positivity on the Weil autocorrelation cone is realized by universal zero-background spectra and nonarithmetic multiplicity patterns, while every finite-dimensional monotone-positive pair `H>=0,V>=0` satisfying the full cone collapses to `HV=0` even without a commutativity assumption. Finite noncommutativity is therefore another classified enrichment rather than an arithmetic sign mechanism.

## Demand a source-forced completed relation before assigning arithmetic significance

**Linked intuition:** `MI-002-the-surviving-arithmetic-variable-is-relational`.

The surviving candidates across Prime Circle, Prime Lattice, Weil Positivity, and Xi Flow all require structure that is present **before** the final quotient or positivity/coercivity test. Prime Circle now needs data outside the scalar Haar-refinement algebra: joint direct-composite primitive/refinement-path structure, old/new fiber incidence, a distinguished source algebra, non-Haar matrix sectors, or another intrinsic relation surviving PC-207--PC-210. Prime Lattice needs a spectral-parameter-dependent or non-fiberwise relation outside fixed-edge affine resolvent geometry. Weil Positivity needs finite-prime incidence, archimedean response, global defects, orientation, and the sign theorem in one completed object outside universal zero-mode spectral-shift positivity. Xi Flow at low bandwidth needs an Xi-specific aperiodicity/coercivity relation already visible in the prime-free guarded state; a prime-scale alternative requires a new source interface rather than hidden high-mode data.

This narrows the meaning of “relational”: the relation must be intrinsic enough to survive matched nonarithmetic controls, family consistency, and natural equivalences, and strong enough to support a quantitative sign, variance, coercivity, localization, or non-escape theorem. A category exit without that replacement theorem is only an uncovered possibility.

## Demand replacement coercivity after every genuine category exit

A no-go theorem defines the next admissible category, not evidence that the category works. Analytic Frontier must bound physical near-extremizer exposure on the surviving compactness chambers and, for the bow transfer, prove distinguished resonant cancellation rather than a false uniform variance saving. Arithmetic Fidelity must derive source-class lower stability beyond both the associated-function controls and their kernel-specific resonance boundary. Möbius Cancellation must convert genuinely additional order/arithmetic structure into first-absolute excursion rather than a target-equivalent encoding. Prime Circle must create old/new-fiber or other non-Haar structure surviving the universal weighted-cover refinement quotient. Prime Flute must prove a scale-faithful cuff endpoint and the independent true short-collar splice. Prime Lattice must make a `z`-dependent/non-fiberwise or approximate resolvent relation both nonprogrammable and coercive. Visual Exploration must survive the appropriate higher-order sampling null. Weil Inertia must prove the bow's special signed covariance. Weil Positivity must derive a completed arithmetic sign outside finite monotone-positive zero-mode collapse. Xi Flow must either prove low-band Xi-specific symmetry-breaking coercivity or build a new prime-band source-to-grid theorem.

“Not ruled out” remains only a frontier condition. Progress requires the replacement stability, variance, coercivity, source-admission, transport, or order theorem in the new category.
