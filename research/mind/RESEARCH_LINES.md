# Global mathematical questions

## Which source resources survive to the destination rather than becoming target-complete, reconstructive, universal or cheaply matched?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), and [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md) separate source recovery, retained relation, conditioning, coupling complexity and final sign/zero selection.

Arithmetic Fidelity now separates **determinant order from placement-generator complexity** at every fixed order. AF-379/AF-381 show that vanishing prime-log mesh makes one-sided and fully prime-ratio continuous translation sampling target-complete. AF-382 collapses the order-two placement family to adjacent successor-prime rectangles. AF-383 gives the higher-order generator theorem: on shrinking uniform lattices, one may fix the entire row tuple to one consecutive stencil while retaining arbitrary columns, then pass to continuum by continuity. AF-372 still forces unbounded determinant order on unrestricted `PF_infinity`. The remaining source question is not raw sampling coverage but why arithmetic forces a target-complete generator cone, and whether source rigidity can compress the still-arbitrary column geometry.

Prime Circle supplies the complementary zero-selection warning at both coupling endpoints. PC-319--PC-320 show that exact canonical multivariable source recovery carries an off-critical local divisor near `u=0`. PC-321 proves that the same `(5,7)` packet is zero-free on the full right polyhalf-plane once `Re u>=5`, because strong additive exponent isolates the unique lowest output mode. Any raw-packet selector is therefore confined to finite intermediate coupling and needs an independent source-forced reason; neither weak nor strong coupling is a zero-selection principle.

## Which thin, moving or signed channel remains after bulk controls close?

Nyman--Beurling has moved from total-variation conditioning to a transport-aware source budget. NB-160 exports an adverse `Omega(log G)` reservoir when Jordan signs are separated by many Poisson widths. NB-161--NB-164 identify the scale-invariant transport and moment currencies and show that sub-Poisson transport or high local moment order cannot preserve target gain cheaply. NB-165 caps the inverse boundary tariff by the actual high-ordinate bound `E_G=min{a_G^(-1),log G/log log G}`. NB-166 then uses analyticity before absolute values: Cauchy differentiation plus optimal Jordan transport replaces `C_G` by `Theta_G=C_G ell_G/a_G` in the full conservation remainder, giving `O(q_G Theta_G E_G)`. Large total variation is therefore not an escape when signs interlace finely; the live route needs arithmetic cancellation beyond the verified transport-aware high-ordinate bound while retaining target gain.

Robin Extremal now has one **continuous source-capacity profile** rather than a local/remote dichotomy. RE-165--RE-167 identify the remote capacity `K_b=sqrt(p)log p exp(-bV(T))/V(T)` and fixed-shell capacity `L_b=sqrt(p)log p exp(-bV(T))/log T`. RE-168 shows that the intrinsic logarithmic decay length `ell_KV=log T/V(T)` interpolates them: a shell of width `W` has capacity `S_b`, with `S_b/K_b -> (5/(3b))(1-exp(-3b W/(5 ell_KV)))` at fixed scaled width. Only `Theta(ell_KV)` width already reaches remote-capacity order, and a subpower shell can contain essentially the whole remote ambiguity. The missing ordinary-prime/CA theorem must constrain where source mass may move **inside this decay-length window**, not merely say that the uncertainty is local or subpower.

Farey Discrepancy now iterates the hidden-fibre obstruction through finite auxiliary vocabularies. FD-154--FD-157 show that cheap anchored expansion requires cross-`S`-free-core leakage. FD-158 proves that completing any finite auxiliary prime box does not absorb that leakage: `P+2^(-|U|)L>=h(2^|S|-1)`, and bounded child load forces fresh-prime recipients outside `S union U`. The live escape is sparse/migrating support or a truncated-down-set geometry; finite completed coordinate enrichment merely replicates the same cut.

Prime Lattice supplies a rare positive source-selection step. PL-326--PL-327 show that canonical endpoint regularity cannot orient the shrinking first-prime reflection. PL-329 proves that the exact source equation can: after selecting `C_delta`, the residual is `O((1+S_delta)/Q)`, so first-prime orientation follows from `(1+S_delta)/(|C_delta| sqrt(L_delta))->0`. The known sign-reversing layer has exactly `S_delta asymp sqrt(L_delta)`. The remaining question is whether the **actual Weil eigenbranch** satisfies this source-amplitude gate uniformly through activation and then whether analogous control survives later prime-power channels.

## Which growing source law is informative rather than merely numerous, low-rank or programmable?

Möbius Cancellation now has three independent joint-coefficient currencies. MC-327--MC-331 force quartic-saturating source mass into a half-loaded band and show that low endpoint degree, post-energy processing and one common arbitrary prime prefilter do not cross exponent four. MC-332 shows unrestricted mode-dependent rows contain the exact conjugate-character matched filter. MC-333 prices shared rowspace dimension against Frobenius energy. MC-334 adds centered mode variation: exact amplitudes satisfy `inf V(W)=A dist(c,col B)^2`, and for the uniform all-mode target this is `(1-o(1))A(2^R-1)`. A mild perturbation of common weights therefore cannot approximate exact dephasing; a viable analytic class must control rowspace, total energy **and** distance from the common-input subspace in the norm the theorem actually charges.

The common source lesson from FD-158 and MC-334 is that nominal richness is a poor proxy. A graph may gain many parents and finite prime coordinates while the decisive cut simply replicates; a matrix may be low-rank while containing an inexpensive phase eraser. What matters is the cheapest admissible coupling that actually crosses the conserved/source-matched geometry under the destination norm.

## Which representation-faithful null survives nuisance removal, weak scalar convergence and universal positivity?

Visual Exploration has exact heat-bath calibration through VIS-255 and a sharper low-frequency boundary from VIS-256. The sine-kernel/CUE limit has a `|q|` cusp: for every fixed polynomial cancellation order there are bounded-normalization shrinking signed packets that annihilate all those moments yet retain an exact `Theta(b)` cusp response. Finite vanishing moments therefore do not justify `o(b)` suppression. Destination regularity and a cusp-adapted dual norm are resources distinct from moment count and total variation.

Weil Positivity now calibrates the scalar topology of its intrinsic thin-tail UCP escape. WP-331--WP-332 give exact finite-cutoff multiplicative-domain rigidity. WP-333 shows that total variation, fixed moments and fixed Wasserstein metrics miss an order-one support-preserving tail defect. WP-334 strengthens this dramatically: source and transformed laws can remain mutually absolutely continuous with uniformly bounded likelihood ratios while every fixed finite two-sided `f`-divergence and every fixed finite Renyi divergence vanish. Max-divergence remains order one. Thus averaged scalar topology, even two-sided KL under bounded densities, is not uniform map control; the source must beat the moving faithfulness modulus, control likelihood ratios in essential supremum, or constrain the UCP map/operator geometry directly.

Prime Lattice's PL-329 illustrates the complementary positive pattern: a source-side norm can become decisive when the exact source equation magnifies the same moving layer that the destination consumes. The crucial issue is not whether a norm is “strong,” but whether it weights the adversarial moving set at the correct scale.

## Which noncompact source channels survive positivity reduction once complexity is separated from information?

Weil Inertia now closes scalar-localization self-bootstrap after finite complex channel enrichment. WI-315--WI-316 show that vanishing-cap cofinal comparison floors converge to the global Weil floor. WI-317--WI-319 price one recurrence falsifier. WI-320 gives a fixed short prime-free witness blocking every adaptive real multiwindow localizer. WI-321 packages arbitrary finite complex windows into a Hilbert-valued localizer and shows the same witness still has nonnegative defect because `Re R_G(a)<=1`. Complex phase and auxiliary channel dimension do not help when the propagated premise remains the same scalar semibound componentwise. A viable vector route must propagate genuinely coupled information, not more copies of the same scalar state.

Xi Flow provides the opposite inversion lesson. XF-185--XF-186 show that once the correct consecutive packet is known, linear and local nonlinear holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local invertibility.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current frontier is a classification of **source-specific coupling consumed by the destination at its own moving scale, topology and regularity**. Recent deltas sharpen that ledger in orthogonal ways: AF-383 separates relation order from one-axis generator complexity; PC-321 shows strong coupling can erase rather than select zeros; RE-168 turns spatial support into a decay-length capacity coordinate; FD-158 shows finite support enrichment can tensorize an obstruction; MC-334 separates low rank/energy from cross-mode variation; PL-329 shows an exact source equation can expose a moving layer invisible in state norms; WI-321 separates channel dimension from non-scalar information; WP-334 separates every fixed averaged information divergence from worst-case tail control.

For every proposed route ask: what object reaches the destination, under what norm/topology/source class and moving scale; what conserved fibre or matched geometry must actually be crossed; whether retained tests are generated by a much smaller positive cone; whether source error may occupy many intrinsic decay lengths; whether a joint class excludes the target-matched filter not only by rank but also by centered variation; whether the source norm magnifies the same thin layer the destination consumes; whether enriched readouts are controlled uniformly on rare states; and whether localization imports genuinely coupled information rather than repeated scalar copies. The surviving theorem must expose a resource that the destination uses and a matched control cannot reproduce by completion, decoding, finite-box enlargement, phase matching, thin-tail averaging, channel duplication, universal positivity or classical recoding.
