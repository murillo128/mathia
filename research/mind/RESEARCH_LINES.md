# Global mathematical questions

## Which source resources survive the exact destination quotient, resolution and quantifier?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) separate source supply, stable transport, destination sensitivity and worst-case geometry.

Arithmetic Fidelity now resolves the finite exponential-band ambiguity. AF-361 shows that on `T<gamma<=T exp(v/delta)` with fixed scaled width `v`, normalized Riesz transport has condition number tending to `e^v`; the separate base-height variable `u=delta log T` controls the common attenuation `e^-u`. Finite exponential width can therefore be relatively well conditioned while the absolute signal is severely attenuated, and the surviving normalized envelope is universal rather than prime-selective.

Prime Lattice gives the complementary physical-side warning: PL-316/317 isolate the exact Sobolev margin needed by a moving Green observer. Nyman--Beurling now sharpens its zero-side boundary twice: NB-142 converts a dangerous logarithmic confluence packet into an order-`log G` positive short increment of the zero-count remainder, while NB-143 proves that order-one xi-type entire completion, the exact functional equation and the imported coarse zero laws still permit such target-poor packets. The missing local rigidity must therefore be genuinely arithmetic or directly target-aware.

The common requirement is a **coupled-scale modulus in the topology, quotient, absolute error floor, local source law and worst-case quantifier actually consumed by the endpoint**.

## Which growing source law is informative rather than under-resolved, reconstructive, shareable or generically realizable?

Farey Discrepancy moves the lower information boundary substantially. FD-144 replaces the earlier square-root-rank blind regime by a sublinear-rank one: arbitrary source-independent normalized nonnegative ancestry observations with total complexity `o(r_T)` can still be fooled by one common infinite prime coloring along a subsequence. At central rank this is `o(log log T)`. The opposite boundary remains FD-142: only `Theta(log T)` bits in one exact additive coordinate can already reconstruct the squarefree prefix.

Möbius Cancellation makes the source-scale precision equally explicit. MC-314 turns

`log Z <= ((1+eta_R)/R) log y`

into a generalized-weight defect `R-d_t <= R eta_R/(1+eta_R)+2+o(1)` at the live rank. The exact equal-share scale forces constant defect, while the existing generic rate-one-half matched control is only proved to `O(R/log R)`. This does not prove generic impossibility; it identifies `eta_R=o(1/log R)` as the scale at which arithmetic demands begin to outrun the current generic guarantee.

Prime Circle remains the complementary conditioning example: exact low-dimensional algebraic identification can coexist with weak analytic separation. Across these lines, “number of coordinates” is not a meaningful scalar resource. One must price effective source-state resolution, provenance, precision, cost sharing and whether a generic ambient construction realizes the same invariant.

## Which local zero laws survive after global density, functional equation and transfer arguments are exhausted?

Nyman--Beurling and Robin Extremal now converge on a stronger but still nonidentical lesson. In Nyman, NB-143 constructs an xi-type entire matched control with dangerous logarithmic confluence packets, so generic analytic realizability and global zero laws do not force the needed worst-cell occupancy. In Robin, RE-144 constructs a matched formal spectrum with only one rightward retained mode yet target hiding inside a near-left cluster whose reciprocal scaled radius may diverge arbitrarily slowly.

The two obstructions use different target functionals and cannot be identified, but both eliminate broad “more zero density” repairs. Nyman needs a zeta-arithmetic local occupancy/remainder law or direct Nyman-target projection; Robin needs a target-relative bound on scaled near-left radius/cardinality/phase cancellation for actual zeros. Sparse exceptional geometry remains possible under the current upper population controls.

## Which arithmetic quantity remains after universal source mass is quotiented away?

Weil Inertia now separates total unscreened mass from the signed quantity seen after the explicit formula. WI-301 proves that complete one-signed dyadic screening forces a uniform archimedean reserve that a nonpositive test must repay through arithmetic interaction. WI-241 then shows, parity-free, that the pole cancels the density-one prime main term exactly; the residual source variable is the smoothed signed discrepancy of `d(psi-x)`, together with a bounded nonnegative density-one autocorrelation term.

Therefore “some prime-power mass survives” is not yet coercivity. The live bridge must show that the forced source contribution survives the density-one quotient with the correct sign. This is another instance of the global rule that source abundance and destination sensitivity are separate resources.

## What constitutes a representation-faithful null after nuisance variables are removed?

Visual Exploration now has an exact answer for grid origin. VIS-237 shows that averaging same-cell collisions over the cell origin is exactly the triangular pair-gap functional `sum_(a<b)(1-|x_b-x_a|/ell)_+`. This is a legitimate quotient of nuisance grid phase, not a new stochastic null and not evidence of prime specificity.

Together with VIS-232--VIS-236, the live control must act on a genuinely variable monotone geometric object—ordered gaps, counts, within-cell positions or another increasing-point-process coordinate—and must be frozen independently of the prime residual. Robustness to representation shifts and statistical nondegeneracy are separate gates.

## Which scalar or local reductions remain structurally capable of carrying arithmetic specificity?

Weil Positivity continues to reject universal or fixed finite-degree scalar Julia repairs: WP-314--WP-317 show that the natural empty-side tangent is universal, bounded prime clusters destroy local compactness on the support side, isolated dominance scalarizes, and finite multipole Julia reduction terminates in classical rational Herglotz data. A viable scalar route must therefore produce a genuinely unbounded collective source geometry or leave that category.

Xi Flow remains a different global-local problem: its local packet inversion is well conditioned once the packet is known, but support discovery, multiscale assembly and transport from theta-side control to a zero-sensitive destination remain the unresolved gates.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current corpus repeatedly separates nearby notions that broad prose tends to collapse: absolute attenuation from relative conditioning (AF-361); growing observation count from source coercivity (FD-144); first-order source scale from second-order hereditary defect (MC-314); xi-type analytic realizability from arithmetic source realizability (NB-143); shrinking physical neighborhoods from bounded reciprocal-scale geometry (RE-144); nuisance quotienting from a stochastic null (VIS-237); and total arithmetic reserve from signed density-subtracted discrepancy (WI-241).

For every proposed route ask: **what exact quantity reaches the destination; at what scale and precision; what universal component is quotiented out; what exceptional local configuration still survives; can a matched non-arithmetic object realize the same invariant; and which source-native theorem excludes that matched control?** The productive next theorem is the one that closes one of those explicit gaps without reconstructing the answer or importing the endpoint in disguise.