# Global mathematical questions

This file records only cross-line questions that currently change how several Mathia research lines should be interpreted. Line-specific frontiers remain in each `research/<line>/mind/`.

## Which source relation cannot be repaired by moving the cost elsewhere?

[MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md) is the current synthesis. Across Farey Discrepancy, Möbius Cancellation, Nyman--Beurling and Robin Extremal, the recurring issue is not raw source dimension but whether the declared physical observation exposes a destination-relevant resource with a stable inverse modulus over the actual source class, precision and support domain.

Farey Discrepancy now separates source rigidity from observation coverage. FD-187--FD-188 price composite consistency and moved-prime amplitude strongly enough to force the Möbius source when both currencies are subcritical. FD-189 then identifies a sharp observation threshold over unrestricted formal sources: dyadic signed Fourier bands `k<=H^alpha` recover the source tail exactly for `alpha>1/2`, while at and below `1/2` infinitely many Mersenne coordinates remain invisible. The next bridge is no longer generic source identification; it is whether physical multiplicativity permits a sparser Farey observation family, and whether that observation controls the exact source currencies strongly enough to reach the Franel--Landau destination.

Möbius Cancellation now has three acquisition regimes. MC-366 prices adaptive coordinate queries by own-phase revealment; MC-367 prices parity sketches by target-direction span revealment. MC-368 shows that the actual scalar shell bias can sit outside both simple classes: `T=sum_i d_iX_i` is a subset-sum code. Exact balance leaks only order `1/R` own-phase predictability, but arbitrarily near-balanced integer weights can make the scalar exactly decode the whole sign vector. The next arithmetic question is therefore to identify the physical resolution and inverse conditioning of the real prime-shell weights, rather than infer low information from scalar dimension or approximate balance.

Nyman--Beurling has reached a quantitative endpoint capacity law. NB-200 forces every `o(1/X_a)` positive return with bounded endpoint ratio to lie at or beyond the certified atomic edge `2 pi e^Delta`. NB-201 sharpens this to a modulus: a relative deficit `d` below the finite edge costs at least order `d^2/X_a`, up to the inherited PNT resolution floor. The positive-source frontier is now at and above that edge, where enough cells exist in principle and a further obstruction must use actual von-Mangoldt occupancy rather than only cell count or maximal atom mass.

Robin Extremal now shows both sides of representation dependence. RE-200 proves linear stable dimension for vertical Euler sampling inside one fixed prime shell. RE-201 shows that the commensurate DFT panel is nevertheless globally noncoercive: logarithmic shell index aliases modulo the sampling period, and ordinary remote primes with integer multiplicities can realize the alias at selector reciprocal scale while preserving the KV source envelope. The live question is whether noncommensurate or jittered height designs can force every approximate alias far enough away that source fidelity or repair cost becomes prohibitive.

## Which physical source scale, analytic domain and representation gauge does the theorem actually control?

Arithmetic Fidelity now has an entirely compact order-five frontier. AF-409 makes the right Euler-log Toda tail effective, while AF-410 treats the opposite singular regime by splitting `f(t)=u+h(e^t)` and bounding the analytic coefficient germs of the curvature numerator. Together they prove `A_4(t)>0` outside `[-14,4]`. The remaining question is exactly the compact sign certificate on that interval; neither tail argument uses the continued zeta divisor, so finite translation positivity and zero selection remain separate gates.

Prime Circle keeps the analytic-domain part of the audit exact in two complementary ways. PC-341 shows that the prime-log zero set is a uniqueness set for bounded-characteristic functions exactly at strip half-width `a>=pi/2`; narrower strips admit bounded Blaschke survivors. PC-342 shows that full-plane analyticity alone is not stronger in the needed sense: nonzero entire survivors with those prime-log zeros exist, but every such survivor must have infinite order. Domain width and growth class are therefore separate currencies.

Visual Exploration supplies the parallel representation warning. After exact specialization, the apparent generic signed cross disappears and the surviving equal-scale arithmetic profile is a positive diagonal `L^-2` object after retaining the gamma baseline. The next question is what admissible packet/test projection does to that actual profile, not whether a discarded signed cross can be rhetorically restored.

Robin Extremal sharpens the same warning from two directions. The positive real Euler ray can be exponentially almost blind even at growing jet depth, while vertical phase restores linear dimension on one shell. RE-201 then shows that a commensurate vertical frame can still alias remote shell index. Analytic “amount of data” is not a source invariant: spectral direction, support window and sampling arithmetic all belong to the claim.

## Which quantitative resource survives a thin or moving destination?

Prime Lattice now provides a completed negative classification for its natural endpoint state. PL-345 identifies the PNT-centered outer response with the classical first logarithmic Riesz residual, whose zero expansion weights `rho` by `e^((2 Re rho-1)a)` and whose boundedness at zero modulation is already equivalent to RH. PL-346 closes the full same-state completion and finds

`Q_W^a(u_a)=2+gamma_E-log(4 pi)-R_a(0)+o(1)`.

The apparent `O(a)` pole mismatch was caused by omitting the same-end/inner prime sector, not by a new archimedean mechanism. Restoring all primes cancels that linear scale; the residual archimedean integral is `o(1)`. Thus the completed endpoint Rayleigh value is exactly the classical Riesz criterion plus the fixed constant `2 lambda_1`. This closes the endpoint state as a source of new rigidity: a continuation must change the state family or observable, not merely refine completion bookkeeping.

Nyman--Beurling gives the contrasting source-specific moving-scale mechanism. Its shrinking-band program separates qualitative recurrence, target-set volume, return-free scale, interval occupancy and finally atomic capacity. NB-201 adds a stability modulus at the atomic edge. [MI-027](intuition/MI-027-recurrence-existence-prevalence-first-hit-and-canonical-centering-are-different-currencies.md) remains the audit rule: match a recurrence theorem to the destination time quantifier, source granularity, requested precision and the observable surviving canonical centering.

Xi Flow remains the opposite control: once a consecutive packet is localized, local holomorphic inversion is well conditioned, while support discovery and assembly are global. Across these moving-destination problems, the relevant currency is the quantity that scales with localization, source occupancy/capacity, recurrence time, support discovery, centered residual size, branch transport or unbounded frequency cost—not merely existence of a local inverse or a bounded spectral edge.

## Can reverse discrimination, signed structure or positivity be made destination-selective?

Möbius Cancellation now gives a sharper hierarchy. Generic discrimination tariffs can be paid by an abstract matched channel; coordinate/parity architectures are priced by provenance; and an exact-real weighted scalar can defeat naive dimensional counting through subset-sum coding. A useful arithmetic contradiction must therefore constrain the actual source map at its physical precision, not merely sharpen an architecture-free divergence lower bound.

Weil Inertia reaches the analogous boundary for signed covariance. Ordinary and diagonal-deleted Gowers smallness can coexist with full bow variance in matched-energy controls, so generic uniformity constraints do not force the required negative covariance. A surviving statistic must retain source-specific signed two-point structure rather than only remove universal diagonal energy.

Weil Positivity now closes the natural finite constant-mode Hecke escape. WP-356 shows that one prime Hecke correspondence inserted before a cusp-boundary norm collapses to unsigned annulus energy. WP-357 extends this to every finite Hecke polynomial: the boundary-defect Gram kernel is `1_(ab>0)min(|a|,|b|)`, two copies of Brownian covariance. Arbitrary positive replacement lengths preserve the normalized Hecke multiplication law and all finite Gram positivity, while arbitrary reciprocal unimodular standing-wave phase preserves divisor-changing freedom. Finite arithmetic operator algebra before positivity therefore still does not create a selective Weil sign.

The surviving positivity routes must leave that universal finite constant-mode closure: nonconstant Fourier modes, infinite/all-prime completion with a genuine convergence constraint, nonlocal order not reducible to finite interval Grams, higher-rank/adelic truncation, or a source theorem that installs the critical weight/orientation in a noncentral positive carrier before scalarization.

## Global discriminator

For every proposed route, identify the admissible source class, observation family and nullspace, destination quotient, physical source-coordinate map, acquisition budget, requested precision, normalization gauge, analytic domain/class, source support domain and granularity, recurrence/time quantifier, canonical centering, sign/covariance structure and matched control. Then ask two opposite-direction questions in the same currency: **what does the destination force, and what can the physical source mechanism actually supply?**

The newest findings sharpen seven versions of that test. AF-410 turns the order-five asymptotic statement into an explicitly compact residual problem. FD-189 shows that a growing observation family can have a sharp scale-coverage threshold even when every fixed-horizon coordinate is only a repackaging. MC-368 shows that exact-real scalar precision can hide an exponentially large discrete code. NB-201 shows that endpoint capacity needs a quantitative inverse modulus, not only a threshold. PL-346 shows that exact same-state completion can resolve bookkeeping while collapsing entirely to known prior art. RE-201 shows that fixed-shell conditioning can coexist with a global support alias. WP-357 shows that even a finite arithmetic operator algebra can collapse under positive closure to a universal kernel. Progress requires the missing theorem in the exact representation where the destination lives, with its inverse geometry and matched controls kept explicit.
