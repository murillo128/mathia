# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Characterize complete support, admissible interaction closure, and stability before choosing a repair

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`.

Arithmetic Fidelity now supplies exact models for four independent failure modes. Linear tests retain exactly their closed measured span; marginal families retain only specific interaction support; multiplicative/generated-sigma closure can recover interactions absent from the linear join; and even exact closed-span recovery may be unstable when the relevant fusion/Friedrichs angle collapses. Prime Circle supplies parallel algebraic controls: finite cotangent complexity and fixed-support depth remain in endpoint/fixed-state closures, while fixed translation-invariant nonlocal kernel statistics of the cumulative primitive-root cloud reduce to classical Mertens/Fourier data.

A decisive negative is a conflicting pair inside the **complete** statistic and closure actually consumed by the mechanism, or a proof that the reconstruction constant necessarily degenerates in the intended limit. A decisive positive must prove target recovery and stable realization in the admitted category without source-tuned programming. Downstream diagonalization, positivity, determinants, or infinite precision cannot repair an annihilated interaction or an unusably unstable inverse.

## Distinguish exact joint relational fidelity from global rational-prime specificity and realizability

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`.

Higher-order or marked relational data can be exactly faithful modulo an intended gauge, but local joint fidelity can still be shared by non-Riemann sources or lost under the topology needed by the final theorem. Prime Flute retains directed multi-neck structure while an all-composite clone matches increasingly strong operator categories. Prime Lattice pointing and depth expose arithmetic data whose ordinary Gram bulk repeatedly classicalizes. Weil Positivity recovers exact finite coefficients in positive pointed-cover families while the global sign remains outside those separated carriers.

A decisive mechanism must pass four gates: prove joint relational completeness modulo the intended gauge; prove that the retained structure distinguishes the exact rational-prime source from matched controls; realize that discriminator stably in the required topology/operator category; and show that the global completion preserves it while supplying the needed sign, continuation, or spectral theorem.

## Make matched controls as strong as the claimed quotient, weighted correlation layer, normalization, and operator ideal

**Linked intuitions:** `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`.

Controls must match the exact information layer consumed by the proposed invariant. The current frontier includes Prime-Circle endpoint/Fourier closure and fixed-shell Hardy trace-class zero-density; an all-composite Prime-Flute clone matched through compact relative resolvent with a possible trace-class squared-transform scattering route; Prime-Lattice critical/depth Gram laws controlled by Poisson, rank, trace, determinant, and trace-norm equivalence at different spectral scales; and Weil-Positivity positive fixed-shift controls that preserve the finite coefficient while varying the representative.

Weil Inertia now gives a particularly sharp warning against matching only unweighted support geometry. The free-shift incidence family is power-sparse in two dimensions and costly to restrict from any fixed finite `L^p` ambient norm, but its effective scalar LCM projection is already near-linear (`>=X/(log X)^4` on one fixed slope). Thus the surviving scalar information can only live in weights, additive energy, factorization labels, or another source-specific structure—not in support cardinality alone.

A canonical relative object is useful only when its reference, algebra, topology, normalization, weights, module/action, operator function, and ideal are forced independently of the desired signal. The mathematical goal is to identify the first matched-control depth at which an invariant changes and verify that this depth coincides with the claimed source of arithmetic information.

## Separate critical-axis generation and RH-equivalent diagnostics from zero selection

**Linked intuitions:** `MI-004-critical-axis-is-not-zero-selection`.

A distinguished axis, exact finite Euler coefficients, or even an RH-equivalent boundedness criterion is weaker than a new zero-selection mechanism. The half-weight `1/2` is canonically generated in several unrelated ways, and Weil Positivity derives the complete scalar finite coefficient `Lambda(n)/sqrt(n)` from its pointed cover package. None of this supplies the missing global Weil sign.

The missing theorem is an independent selector or sign estimate derived upstream from rational-prime structure. Repackaging the continued zero divisor into a bounded signal, hull, Koopman system, pair-correlation statistic, Poisson bulk law, trace-class determinant, or canonically normalized positive carrier is diagnostic/kinematic unless the decisive support or sign theorem is proved without importing the zero locations or a classical equivalent.

## Derive a realization category whose support, stability, ideal, and source theorem all survive global assembly

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`.

The current branches expose complementary category barriers. Prime Circle shows that trace class itself imposes a zero-density law incompatible with direct Riemann-zero realization at fixed shell. Prime Flute shows that changing from first to squared resolvent can cross the trace-class threshold and recover an invariant Krein/Birman--Krein phase, while the first-resolvent `S_2` endpoint is abstractly sharp and its canonical weak-`S_1` residue is area-only. Prime Lattice shows that a first nonvanishing scalar normalization can become trace-norm equivalent to a non-arithmetic envelope for every ordinary Lipschitz statistic. Weil Inertia shows that changing to any fixed finite `L^p` norm still pays incidence sparsity unless a source-correlated theorem is added, while scalar projection loses that sparsity and forces the theorem to use weighted structure. Weil Positivity shows that regular positive calculus can preserve finite coefficients yet force the discriminating response to a singular or indefinite boundary.

The program-level question is whether an intrinsic middle category can satisfy **support fidelity, quantitative stability, matched-control separation, compatible operator ideal/functional calculus, global assembly/sign compatibility, and source-faithful analytic control simultaneously**. A no-go showing that every intrinsic category fails at least one of these gates would be equally decisive.
