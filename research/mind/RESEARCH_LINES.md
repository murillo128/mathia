# Global mathematical questions

This file records mathematical questions that only become visible by comparing several current Mathia research lines. Line-specific frontiers remain in each `research/<line>/mind/`.

## When does a quantitative budget become a genuine source discriminator rather than a repackaged observable?

Across the current lines, the useful budget is increasingly the one induced by the **exact quotient or discriminator that the destination actually consumes**. Coarse support, ambient norm, matrix rank, compactness, selector sparsity or representation dimension can all be large while the destination-visible source channel is unchanged, universal, source-addressing rather than averaging, or fully reproducible by a matched control.

Arithmetic Fidelity makes the topology and nuisance-quotient issue explicit. AF-542 shows that a value-level accurate carrier-to-curvature approximation can fail catastrophically after the shrinking transverse contrast is applied. AF-543--AF-544 construct an exact curvature-native inverse using the additional reference coordinate `r(t)` and prove that calibration precision is itself part of the theorem. AF-545 identifies the common-mode field governing shared-calibration error, and AF-546 gives the finite translation/affine/projective hierarchy. AF-547 now shows that this hierarchy cannot be extrapolated by polynomial degree: finite relational invariants are controlled by the **Lie closure** of the nuisance family and its evaluation rank on the retained configuration. Unrestricted cubic-or-higher polynomial calibration generates all polynomial vector fields and has full evaluation rank on every finite distinct branch set, so no extra finite branch count restores a universal scalar invariant. A single fixed cubic field is a counter-control: it is one-dimensional and straightenable. The arithmetic burden is therefore to identify the actual source-induced nuisance Lie algebra, not to count generators or nominal polynomial degree.

Möbius Cancellation reaches an analogous information-capacity boundary in a different geometry. MC-505--MC-506 show that the varying start/profile table is seen only through the physical pushforward `t=x+j`, whose excess-collision law controls the centered conductor bias. MC-507 proves that broad marginals and matrix complexity do not control that law. MC-508--MC-509 show that finite mask coordinates are programmable without localization while a sufficiently long exact localized root tuple becomes source-injective. MC-510 survives the gauge repair and now quantifies the approach to rational reconstruction: for the relative state `q r^(-1) mod ell`, exact fibres are charged by the remaining determinant uncertainty `B/(pM_z)`, with `B=H(2Q+H)`, before becoming pair-injective once `pM_z>B`. Thus finer exact state can progressively destroy the repeated fibres needed for averaging even before full reconstruction.

These two lines expose complementary failure modes for “add more relational information.” In Arithmetic Fidelity, enlarging the finite branch configuration can become useless because the nuisance orbit already has full evaluation rank. In Möbius Cancellation, enlarging the exact mask can become counterproductive because the state approaches an address for the source pair. The common audit is not dimension but the surviving fibre after the actual quotient: determine whether extra coordinates create a transverse invariant, merely reconstruct nuisance/source identity, or leave the destination-visible law unchanged.

Farey Discrepancy shows both the value and the danger of downstream compression. FD-305--FD-307 reduce a near-capacity block-Mertens anomaly to a sharply low-frequency fixed-block witness, while FD-308--FD-309 show that the available principal-arc and classical GRH pointwise estimates become contradictory only at or above the earlier source ceiling. FD-310 quantifies the missing pointwise-frequency exponent: source-uniform closure needs `theta<c=0.415037...`, while balanced depth needs `theta<0.273924...`. FD-311 supplies a genuinely different destination-visible budget before Fourier compression: the same source forces an inflated sliding second moment, and balanced depth is killed by any source-window bound `J_mu(X,N)<<X N^(1.7451125-epsilon)`, a fixed power saving `eta>0.2548875...` from the trivial scale. A useful theorem can therefore be weaker pointwise but stronger in exactly the physical `L^2` quotient that the reduction already forces.

Visual Exploration supplies the selector analogue. VIS-419--VIS-425 identify the exact base-prime Haar self-null, finite-window character tariff, fixed-moment normalized-amplitude tariff and fixed-radius persistence tariff for Wang shells at Gram points. VIS-427 shows that normalized fixed-power path averages pay no additional growing-radius tariff. VIS-428 then shows that fixed-shell growing-window suprema also become trivial at every fixed submaximal threshold: minimality plus compactness gives a finite cover radius after which every starting phase has hit the high-amplitude target. The surviving supremum resource is therefore a **changing-shell pre-saturation hitting-time budget**, a shrinking target, a tariff failure or a genuinely non-Haar selector, not path length by itself.

Prime Lattice provides the same discipline through exact covariance geometry. PL-437--PL-441 reduce the unresolved mod-5 defect to reflection/start-residue anisotropy plus one quadratic-character lag channel and solve the lag-stationary dephasing gate as a compact wedge--ellipse with an operator-norm perturbation margin. PL-442 now fixes the surrogate ambiguity: the restricted Frobenius-best stationary fit can leave the covariance cone, whereas zero-extension to `Z/5Z` followed by cyclic Reynolds averaging preserves positivity and supplies an exact anisotropy energy. Positivity of that surrogate is still weaker than the dephasing lens. The arithmetic target is therefore not generic “near stationarity” but a canonical stationary margin together with a quantitative residual norm in the same covariance scale.

The global question is: **for each proposed representation, what is the smallest source-derived object that the destination actually sees, and what fibre rank, norm, seminorm, hitting-time scale, reconstruction tariff or covariance margin on that object is stable under the real compression?** A budget becomes evidence only when it controls that object independently of matched controls, remains below any source-addressing or full-orbit threshold that destroys the intended quotient, and stays nonvacuous after upstream feasibility and representation-capacity gates are imposed.

## Which source dependence survives scalarization, equilibrium, compactness and local-scale gates?

Prime Circle, Weil Inertia and Weil Positivity expose a common danger from different operator categories: sophisticated structure can be universal, too rigid, too programmable, compactly invisible at the critical scale, locally smooth despite global noncompactness, or fail to define a usable infinite operator at all.

Prime Circle PC-419--PC-421 show that the norm-closed bounded-radial/discrete-shift descendants contain no nonzero compacts, while the canonical continuous-scale Hilbert commutator does become Hilbert--Schmidt but merely repackages one vertical Mellin line. PC-422 closes the direct reflected scalar repair by showing that the half-density maximum is universal Mellin/positivity geometry. PC-423 closes the finite reflected bilinear shell-matrix repair as well: on divisor-closed families the Möbius incidence matrix is unit triangular, so determinants discard primitive-shell extraction exactly, and the zero-free matched control has the same factorization, reflection symmetry, full rank and half-line positivity. The remaining Prime-Circle route must retain structure not exhausted by one-shell Mellin amplitudes plus a finite divisor-incidence basis change.

Weil Inertia has the parallel “exact representation is not independent control” obstruction. WI-433--WI-436 show that horizontal transport and the canonical positive/negative Hardy pairing can reproduce the finite Kunik defect exactly without constraining it from source information. WI-437 excludes every fixed compact cross-channel correction on shallow normalized defect packets. WI-438 sharpens this decisively: **global noncompactness is still insufficient**. For a bounded Hankel symbol, shallow-packet response is bounded by its Poisson/Garsia variance at the defect scale; it vanishes at Lebesgue points and uniformly for bounded uniformly continuous symbols, even for noncompact Hankel operators. A viable bounded-symbol family must retain order-one local oscillation at spatial scale `r asymp d`, which in the explicit model means frequencies of order `1/d`. The missing theorem is source-controlled scale-matched roughness, not merely an essential Calkin class.

Weil Positivity sharpens canonicality into a rigidity--programmability--domain trichotomy. WP-429 shows that unrestricted coherent finite-Gibbs sandwiches can realize arbitrary positive functionals; WP-430 shows that thermodynamic-only canonical selection collapses back to zero Bohr degree. WP-431 then tests the most literal arithmetic escape: the one-sided prime semigroup field `sum_p a_p mu_p` has a nontrivial dense Hilbert domain exactly when `a` is square summable. At the critical half-density `a_p=p^(-1/2)` its maximal domain is `{0}`, and bounded normalization of finite-prime cutoffs erases every fixed prime amplitude.

These lines suggest a shared gate without asserting that their operator categories are equivalent. A source coupling has evidentiary value only when it is fixed **before scalarization or target inspection**, survives matched generic controls, has a mathematically valid domain/topology at the critical scale, and retains the required **local-scale** response rather than merely global rank, compactness class or noncompactness. “Compact”, “coherent”, “noncommuting”, “mixed-frequency”, “matrix-valued” and “unbounded” are categories, not evidence. The unresolved mathematics is a source-canonicity theorem for a specific coupling together with a destination estimate at the exact scale consumed by the hypothetical defect.

## Where do apparent arithmetic thresholds disappear once the exact source geometry and proof interface are retained?

Nyman--Beurling supplies a particularly clean witness audit. NB-322--NB-329 show that a `1/sqrt(log n)` rank-one-error resonance survives denominator growth, parity, divisibility and neighboring selector cells while one fixed outer adjacent witness crosses through zero at half-cell drift. NB-330 routes around that zero with an inner adjacent mode, and NB-331 makes the witness index adaptive to cover every external subquadratic sequence `n<=m=o(n^2)`. NB-332 now reaches the critical square scale on the exact lattice `m=dq`: when `d/q->delta`, the adjacent coefficient tends to the sign-definite Diophantine profile `Phi(delta)=-(4delta)^(-1) sum ||kdelta||_Z^2/k^2`, nonzero for every nonintegral `delta` and zero at integer ratios. The old “transition at quadratic scale” is therefore refined into a genuine critical profile whose integer nodes still require an optimized-witness audit; the source-side transition is not a single scalar threshold.

Farey Discrepancy gives the complementary feasibility audit. FD-307 appeared to create principal-major-arc depth thresholds, but FD-308 shows that the upstream finite-support energy ceiling excludes every depth where those thresholds activate, and FD-309 shows classical GRH pointwise theory only touches that ceiling. FD-310 identifies the exact pointwise exponent that would enter the live region. FD-311 then demonstrates why the **theorem class** is part of the threshold: balanced depth can instead be killed by a quarter-power-scale saving in the source-selected sliding variance, without meeting the pointwise Fourier requirement. “Better cancellation” is meaningful only after specifying which compressed object the proof actually needs.

Arithmetic Fidelity gives a third version. AF-544 finds a genuine precision threshold because the leading reference asymptotic fails after the destination contrast while the finite-part correction crosses that downstream calibration barrier. AF-545--AF-546 show how the invariant changes with nuisance geometry and relational arity. AF-547 then adds an adversarial closure test: once the Lie closure has full evaluation rank, a nominal higher-degree finite invariant does not merely require one more branch; it disappears altogether. The relevant transition is orbit-rank loss, not polynomial-degree counting.

Weil Inertia adds a proof-interface version. WI-439 shows that the same `T^lambda L^2` support loss attributed to the short-interval pair-correlation frontier is already generated by zero-window endpoint localization before the prime-polynomial mean-square step. Improving coefficient-weighted prime dephasing alone therefore leaves the support gate untouched. A proposed theorem improvement must be checked against every unchanged upstream interface that already carries the same exponent loss.

The reusable question is therefore: **is a proposed transition intrinsic to the source/destination coupling, or only to the witness, state resolution, operator category or proof interface currently being used?** Stress-test every threshold by optimizing over competing source directions, closing the actual nuisance algebra, quotienting genuine gauge, checking whether finer state becomes source-addressing, comparing against upstream feasibility ceilings, and tracing the claimed gain through every earlier interface that can already saturate the destination budget. If the transition disappears under those controls, it should not be promoted; if it survives them, identify the exact source quantity that enforces it.
