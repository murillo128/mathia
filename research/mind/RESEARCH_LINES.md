# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry, singular source calculus or programmable representation?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate source provenance, target sensitivity, relational structure, reachability, readout fidelity and motion law.

Weil Positivity has closed scalar recovery, fixed positive multi-channel recovery, finite multiplicative-equivariant depth, flat transport and several curvature/readout escapes. WP-300 leaves prime-symmetric scalar projective curvature only flat or universally fermionic. WP-301 shows that arbitrary noncommuting local defects in a positive frustration-free model collapse under the zero-mode readout to common-kernel selection, screening nonzero archimedean spectrum.

WP-302 traps ordinary moving kernels between homogeneous rigidity and free programmability. WP-303 closes the most canonical source-forced spectral interpolation between them: every moving spectral cut of one fixed source operator is a pairwise-commuting projector family, hence norm-discrete and strongly derivative-zero wherever differentiable. The canonical source threshold is therefore a singular filtration, while freeing the spectral realization/compression reaches the POVM-universal category by Naimark dilation. The surviving curvature route must be forced by a genuinely varying noncommuting or nonlocal source law before target comparison, and its informative spectrum must survive the final readout.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) keep conditioning, source supply and target sensitivity distinct.

Analytic Frontier now has a sharp bow interface beyond source marginals. ANF-185 replaces adaptive critical probing by one deterministic one-height high-pass with transition width `a_X`. ANF-186 then matches the natural prime/rough coefficient alphabet, densities, mean, diagonal variance and local absolute-mass envelope while still producing target endpoint memory by placing signs after reading the exact logarithmic carrier. The remaining theorem is a **joint arithmetic-placement law relative to the carrier**, or direct `o(a_X)` control of the deterministic weighted residual / signed Fejer defect. One-point source statistics are exhausted.

Arithmetic Fidelity has localized its source-complexity obstruction to the first prime layer and now priced the simplest variable-background escape. AF-338 gives fixed-order full-Euler sign regularity once `sigma>r-1`. AF-339--AF-341 show that after pole cancellation the prime layer alone retains the RH right-half-plane pole discriminator and `Theta(P/log P)` ordered sign variation, while higher prime powers are analytically harmless there. AF-342 proves that an arbitrary baseline cannot reduce that variation below prime scale unless it itself reacts on a prime-scale set of prime sites or prime-adjacent composites. If its transform remains zero-pole-blind, the residual still carries the RH discriminator. The live bridge must now show that such exceptional structure is generated by genuinely cheaper retained data, prove a growing-order observation theorem, or replace ordered sign variation by another finite source certificate; merely moving the prime pattern into a nonconstant baseline is not compression.

Nyman--Beurling has pushed its inverse-section matched control beyond fixed source sections and the cheapest cross-cutoff coherence test. NB-118--NB-120 show that even `M=M(R)->infinity` with `M^6(1+log M)/R->0` admits perfect target capture at `(1+o(1))/(kappa_infty R)`. NB-121 proves that any fixed finite nested cutoff ladder adds no new source equation at all: the simultaneous natural-cell/Nyman constraints collapse exactly to the maximal cutoff and maximal section, so one maximal matched control serves every bounded-ratio member. The live theorem must distinguish actual zero-power packets, force a faster source-section transition, or use genuinely non-nested/widening/tail-sensitive cross-scale information rather than ordinary `R,2R` nesting.

Prime Lattice has removed fixed-order regularity and now classified the singular principal boundary geometry. PL-308 gives exact weak Pohozaev/virial stress for every fixed `s>0`; PL-309 shows the renormalized trace lives on an `exp(-Theta(1/s))` layer invisible to ordinary bulk convergence. PL-310 computes the stretched Green limit: a Volterra kernel plus a diagonal atom whose nonzero fixed-point cone is exactly `C/sqrt(e^(2r)-1)`. The shape is universal under bounded lower-order perturbations, so the remaining analytic resource is compactness/pass-through for the actual completed-Weil branch plus the scalar amplitude, followed separately by the rational-prime sign/first-crossing theorem.

Prime Flute's sharp-cutoff completed-angle branch remains noncompact: adjacent Fourier modes straddling the hard cutoff generate order-one canonical correlations on infinitely many separated modules. A viable continuation must change splitting/observable rather than sharpen the same compactness estimate.

## Which cancellation theorem remains after representation enlargement and source equations stop adding endpoint force?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy has crossed from one dyadic coherence edge to a finite squarefree network. FD-117--FD-120 show that the whole cumulative field, its minimal floor-quotient skeleton and complete single-horizon squarefree/unit-amplitude admissibility can still support critical near-extremizers. FD-121 gives a fixed dyadic gap; FD-122 extends it to every fixed squarefree dilation and shows that, inside any fixed finite prime cube, sufficiently cheap horizons form a Sperner antichain. The remaining gate is **pointwise globalization**: the constants deteriorate with the prime family and nonsquarefree transitions remain open, so network coercivity must still be transferred to the designated RH-relevant endpoint.

Nyman--Beurling supplies the complementary falsifier for cross-scale reasoning. There, finite nested same-source coherence does not create a network cost because the simultaneous constraints are algebraically redundant with the maximal section. Together with Farey, this isolates a general test: cross-scale coherence becomes real proof currency only when the joint constraints are nonredundant and make the local extremizer geometries genuinely incompatible.

Möbius Cancellation has upgraded exceptional-character rigidity beyond both one-mode bias and the common-cell union threshold. MC-289--MC-291 force collision-rich shells toward a near-affine exceptional spectrum and control its odd-girth/common-cell geometry while total defect is small. MC-292 adds the threshold-free entropy budget `sum(1-h_2(d_i)) <= log_2(1/eta)` for linearly independent biased characters. The remaining arithmetic theorem must control the actual generated quadratic-character defect/rank/conductor/occupancy data; generic finite Fourier geometry and entropy no longer supply the missing source estimate.

## Which exact source incidence survives support relaxation in Weil Inertia?

WI-288 reduces complete one-signed symmetric two-island screening to the nearest-prime-power Lambert gap, WI-289 identifies prime squares as the unique critical composite layer, and WI-290 gives at least `X^(3/8-o(1))` positive-length composite-safe components on `[X,2X]`. WI-291 confines eligible ordinary-prime upper hosts to a much rarer large-gap family with summable logarithmic measure.

The bottleneck is therefore a sharply asymmetric cross-scale incidence theorem, not abundance. A fragmented safe reservoir generated largely by prime gaps near `sqrt(x)` must be localized relative to rare selected gaps near `x`; the available count exponents do not yield the intersection by pigeonhole. Null-equation amplitudes/signs or a different support topology remain separate escapes.

## What should Prime Circle do after diffuse source averaging kills the local square-root residue sector?

PC-285 is the unconditional matched-control boundary. PC-286--PC-290 show conditionally on RH that, once total source count is matched, the exact one-profile normalized boundary-log singular spectrum collapses toward the control for every `B=o(sqrt(q))`.

PC-291 shows that the first denominator-microscopic relation forced at `B~sqrt(q)` is spectrally negligible. PC-292 proves the worst-case operator threshold sharp: a fixed same-source pair already has order-one normalized norm on a local band `T~N/log q`. PC-293 then shows that this mechanism disappears under the actual source averaging. For any diffuse law with maximal atom `O(log q/q)`, every local band `T=O(N)` at `N~sqrt(q)` contributes `o(1)` to the averaged normalized singular-spectrum law. Both the prime source and the count-matched Li-grid control lie in this class.

The local logarithmic singularity is therefore not the missing source-averaged square-root carrier, even though it has an order-one fixed-pair extremizer. The surviving one-profile question is whether an order-one **prime-specific remainder lives in the nonlocal residue complement** after source count and diffuseness are matched. Otherwise the architecture must retain information discarded before one-profile singular-spectrum averaging: cross-level, multi-source, directional, phase-sensitive, raw-resonance or noncommuting data.

## What survives after regular-CA placement is finer than every fixed algebraic zero-packet scale?

RE-119--RE-122 show that regular CA placement and every fixed algebraic edge hierarchy collapse to a stable profile whenever weighted near-edge mass is sufficiently flat. An algebraic anomaly of order `nu^-A` requires a subedge zero within horizontal width `O(log nu/nu)` at ordinate `O(nu^A log nu)`, so fixed-order visibility already requires polynomial-height approach to the attained edge.

RE-123 removes arithmetic phase placement as another fixed-order escape. Reciprocal-square zero weights make the high-frequency tail algebraically negligible after a polynomial cutoff, while the retained packet has only polylogarithmic phase speed. The exponentially fine regular-CA phase mesh therefore samples the **entire signed subedge packet** superalgebraically well. Favorable continuum excursions of fixed algebraic size cannot hide between CA states. The surviving finite-edge problem is the existence of polynomial-height edge approach together with the packet's **internal signed cancellation/correlation**; nonattained-edge and `Theta=1` branches remain separate.

## How strong must distributed prime-phase frustration be after the simple-cycle certificate is universally capped?

VIS-213--VIS-216 reduce fixed-dimensional worst starts to unit-modulus torus optimization and give holonomy, fractional-packing and normalized magnetic certificates. VIS-217--VIS-218 close the normalized magnetic relaxation in the strictly subcritical prime regime because effective edge dimension is too large. VIS-219 shows that cycle packing would need near-total weighted-envelope saturation to reach Haar-RMS scale.

VIS-220 proves that the exact simple-cycle packing LP can never do so: for support-graph girth `g`, `P_+,P_- <= (2/g^2)B`, hence every simple graph leaves at least `7B/9` residual and `D_pack >= (7/9)sqrt(2N_eff)`. The failure is phase-independent and built into the reward/capacity geometry. The live static object is now the **exact fixed-modulus torus optimization or a genuinely stronger non-packing/nonlinear certificate**; one-parameter access time remains separate.

The cross-line lesson with Weil Positivity is sharper. Visual Exploration exhibits quantitative relaxation loss: genuine gauge-invariant geometry survives but two natural certificates provably lose the destination scale. Weil Positivity exhibits categorical and topological loss: a zero-mode readout erases spectrum, a fixed spectral calculus produces singular commuting motion, and a free positive representation can encode the target. In both cases the question is not whether nontrivial geometry exists, but whether a source-forced observable retains exactly the information, regularity and scale consumed by the destination theorem.