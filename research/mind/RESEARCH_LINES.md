# Global mathematical questions

## Can a source-native signed transformation become admissible before exact Weil positivity is read?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) separate source provenance, admissibility and target sensitivity.

Weil Positivity has closed positive additive preservation and nonlinear target scalarization as easy escapes. WP-290 gives an explicit signed source-native integer-dilation coboundary that cancels the zeta-pole tangent exactly, but its temperedness is already equivalent to RH and its Jordan parts are individually non-tempered.

The open theorem is therefore sharper than “find a signed completion.” Derive an admissible source-native coupling or category descent **without assuming the RH-equivalent growth statement**, then prove that its final degree-two, parallelogram and translation identities identify the exact Weil quadratic form. Matched counting controls should distinguish generic pole removal from arithmetic zero information.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) separate source-resource pricing, component globalization, representation resolution and relaxation sharpness.

Arithmetic Fidelity now separates quantitative conditioning from exact incidence even more sharply. AF-327 proves that the retained `{1,z,z^2,z^4}` moment Gram of every nonzero eight-prime common-time orbit stays quantitatively inside its rank boundary, with a support-height/time-height Baker--Wüstholz envelope transferred to determinant/eigenvalue slack. AF-328 then proves that the remaining middle-singular target is a common-zero problem for an irreducible exponential sum and a coprime partner. Common-factor theory can control recurrence or finiteness but cannot exclude one isolated hit. The missing theorem is pointwise prime-log incidence, followed—if hits exist—by downstream sufficiency of the retained Gram margin.

Prime Flute now separates operator norm from the endpoint norm. PF-333 shows that generic positive relative square-root perturbations can pay a logarithmic operator-norm penalty, while PF-334 proves dimension-free Hilbert--Schmidt control and a nonlinear `S_2` transfer under a uniform normalized-insertion envelope. The open theorem is localized reciprocal-prime `S_2` control of the actual pant/core insertion and its resolvent-dressed low/high geometry, not an unnecessary `S_infinity` bound.

Nyman--Beurling has now optimized the finite-window resolution currency under a terminal natural-index budget. NB-103 gives fixed-multiplicity confluent volume as a Laguerre law; NB-104 exposes the ordinate-resolution delay; NB-105 proves that moving the band start cannot remove that delay for free. The unique radial-scale optimum starts at the ordinate scale and uses the terminal budget completely, leaving the single resource `L=2a log(B/G)`. The open theorem is whether this **terminal resolution depth relative to the zero ordinate** is large enough in actual Nyman sections and whether its `Delta_d(L)` volume couples to the target norm or dual certificate.

## Which cancellation theorem remains after transport and normalization shortcuts are closed?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track the relocated endpoint costs.

Farey Discrepancy has now identified the branch-sensitive transport currency. FD-106 gives `R_C >= a_epsilon(C/H)^epsilon R_H` for critical excess `R_H=U_H/H`; the positive power is needed only in the repaired terminal family `C_p/H~1/p`, while odd-square, dyadic half-scale and head branches are critical-lossless. The exact FD-102 potential spends the same source-neutral contraction with coefficient one, so geometric contraction is no longer an independent loss. The endpoint is the harmonic terminal fan: exploit its least-prime-factor/source coherence to remove `epsilon`, or force enough source descent/head activity that the repeated selection tax cannot survive.

Möbius Cancellation has separated representation loss from aggregation loss. MC-275 shows pointwise normalization reconstructs OR; MC-276 shows L2 normalization is cheap because it subtracts the blind atom. MC-277 proves that the raw degree-`m` Christoffel row scalar still carries the blind bit with exact margin `delta_(k,m)~2m/k`. The surviving theorem is therefore arithmetic control at that rowwise margin before provenance is discarded, or signed cross-row cancellation that cannot mask one blind contribution. The representation itself is no longer the missing resource.

Prime Lattice has removed both the pure-branch singular derivative obstruction and the generic perturbation shortcut. PL-299 gives pure-branch `C^1` shift convergence through uniform BV even though `H^(1/2)` diverges. PL-300 constructs norm-vanishing rank-two perturbations with a stable simple ground branch and uniform state convergence but divergent BV and fixed-lag autocorrelation derivative. The residual theorem must derive BV/derivative-measure compactness from the **actual completed-Weil remainder**, not from Kato or norm-resolvent stability; the independent source-specific sign gate remains open.

## Can the source remain coherent across the forced Robin normalization corridor?

[MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) records the shift from intra-fan transport to inter-block source control.

RE-107 closes the whole-fan reset problem: every sufficiently late compact positive fan carries a positive power-scale raw Chebyshev debt across its threshold sweep. RE-108 then prices the interval before the process can even return to a nonpositive colossally abundant state. If `Y=log C` is a quantitative positive state, `V=log D` the next nonpositive CA scale and `R_C=e^(h(C))-1`, then

`V>Y^(1+R_C)`, hence `V-Y>=R_C Y log Y`.

On the false-RH quantitative block family this yields block-start spacing

`X_(k+1)-X_k >>_J X_k^(1-b_0) log X_k`

and `N_J(T)<<_J T^(b_0)/log T`. This does not contradict recurrence: the matched normalization-only control saturates the exponent. More importantly, because `b_0<b_1`, the forced normalization corridor is polynomially longer than the intra-fan Chebyshev debt scale from RE-107.

The open theorem is therefore no longer just “compare different blocks.” Control the **source phase inside the corridor** while the Robin excess falls through zero. A viable contradiction must couple the reciprocal-Mertens coordinate, a Chebyshev deficit or another source-sensitive invariant strongly enough to show that the standard prime source cannot both traverse the required corridor and regenerate the next quantitative positive witness. Merely summing positive fan increments at block endpoints cannot close the current gap.

## What remains after the exact visual pair-difference cancellation taxonomy is exhausted?

Visual Exploration retains the exact VIS-209 coupled anchor-Gram reduction. VIS-211 proves `E_1=o(R_v^2)` throughout every diverging strictly subcritical long-start regime, and VIS-204 leaves no further nonzero higher-cancellation shell in the pair-difference representation.

VIS-212 now removes the need for an unspecified infinite Cesaro limit: the deterministic `y^-2` separation of distinct prime-ratio frequencies gives the long-start mean square uniformly on every sliding interval of length `L>=c y^2`. This is a finite-start density-one theorem, not pointwise cancellation. The frontier is to compress the quadratic worst-spacing window using coefficient-weighted small-determinant geometry, decide genuinely pointwise coherent starts, and separately handle the critical scale `H asymp y/log y`. A new higher-cancellation hierarchy would still require a new observable.

## Which exact null-mode amplitude constraint can lower the sharp one-signed Weil packing budget?

[MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) captures why the current screened relaxation is exhausted.

WI-281 shows that complete one-signed screening forces `log 2` support packing and lowers the positive archimedean budget to `K_pack<K_+`. WI-282 proves this remains sharp after all prime-power lags and qualitative full span. WI-283 goes further: even the prime-translation **support domination** satisfied by actual Suzuki null modes can be imposed while approaching the same `K_pack` extremizer, because support topology can be saturated with arbitrarily small `L^2` mass.

The next theorem must therefore distinguish actual first null modes through amplitude-bearing information: the full equation `A_a v=0`, quantitative mass in translated regions, regularity/nodal/boundary-flux estimates, source-balance identities, a spectral-area margin, or simultaneous-cut constraints that couple amplitudes rather than only support and autocorrelation zeros. The sign-changing branch remains logically separate.

## Where does classical reproducibility finally stop in Prime Circle?

PC-282--PC-283 show that the boundary selector and selector-subtracted scalar symbol are classical. PC-284 proves that even the exact logarithmic finite-section normalized singular-spectrum law is Li-grid reproducible whenever `log B=o(sqrt(log q))`.

The one-profile source-placement frontier therefore begins only in the rational-microscopic regime at or beyond `sqrt(log q)` bandwidth, in a simultaneous three-parameter scaling, or after enriching the observable beyond one finite section and its ordered singular spectrum. A candidate effect inside the PC-284 transport window is a control artifact, not a new arithmetic carrier.
