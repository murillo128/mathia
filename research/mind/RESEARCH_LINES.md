# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to universal geometry or programmable completion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate provenance, destination sensitivity, relational structure and representation choice.

Weil Positivity has now closed the natural scalar gauge orbit and both canonical scalar Schur repairs more sharply. WP-307 keeps the pole-normalized Mangoldt curvature positive under every density-normalized orientation-preserving affine spectral recentering. WP-308 closes constant scalar Herglotz projective recodings. WP-309 proves that allowing the scalar automorphism itself to depend holomorphically on the spectral parameter adds no freedom.

WP-310 adds an admissibility obstruction: the critical positive Mangoldt source has density-one infinite mass and a logarithmic Herglotz end, outside the finite-moment asymptotic class required by the classical infinity/moment Schur transform. WP-311 tests the natural fixed-interior scalar escape. That transform is admissible and positivity-preserving, but after unit-log end normalization it creates a strictly positive `log(y)/y` term; the Riemann Gamma channel has no term at that order and its relevant curvature first appears at `y^-2`.

The remaining positive route must therefore introduce genuinely new finite--archimedean geometry or a source-justified renormalization before scalar comparison. Scalar freedom can fail either because the transform is not defined on the source class or because an admissible transform contaminates an asymptotic scale larger than the target discriminator. Boundary- or moving-reference geometry, operator/matrix-valued structure, generalized resolvents, determinant/scattering objects, added archimedean channels and genuinely nonseparable finite--archimedean couplings remain category changes rather than recodings of the audited scalar response.

## Which source resources survive into the exact destination margin, topology and moving resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) and [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) keep conditioning, source supply and destination sensitivity distinct.

Analytic Frontier still requires a joint arithmetic-placement law relative to its distinguished logarithmic carrier; matched one-point statistics are exhausted. Prime Flute's current sharp physical low/high split is already noncompact, so progress there still requires a new split or observable rather than stronger estimates inside the same hard cutoff.

Arithmetic Fidelity now separates full-source fidelity, discriminator fidelity and boundary conditioning inside one Riesz representation. AF-349 proves exact source recovery in a strong derivative-variation metric, while AF-348 exhibits raw-amplitude collapse. AF-350 calibrates the weaker quotient: an `O(X^theta)` Riesz error still preserves Dirichlet principal parts strictly to the right of `Re s=theta`. AF-351 adds the positive conditioning example. At the critical `sqrt(X)` boundary, one Riesz order changes the zero-amplitude tail from the non-absolutely-summable `1/|gamma|` scale to `1/|gamma|^2`, making the critical series absolutely summable without moving the zero frequencies or erasing the off-critical singularity discriminator. For every fixed integer `r>=1`, the sharp `O_r(sqrt(X))` bound is RH-equivalent; the unsmoothed bound is impossible by classical oscillation.

Nyman--Beurling now separates recurrence visibility from quantitative conditioning. NB-132 can force a genuine narrow polynomial-height zero packet below the recurrence-visibility rank budget. NB-133 constructs matched formal packets obeying the same coarse rank allowance and Vinogradov--Korobov radial-depth constraint whose full recurrence is visible but whose exact normalized annihilator margin is smaller than every power through a shrinking phase cluster. Population/rank and horizontal depth do not control the angular arrangement multiplied by the target certificate.

Prime Lattice sharpens the moving-observer version. PL-314 shows that weighted `L^1` control misses a Green atom sampling `Q=r/s`. PL-315 shows that this is not repaired by broad regularity: a fixed mismatch may be `C^infty`, belong to every `W^{k,1}`, have arbitrarily small physical `W^{1,1}`, lie in every `H^alpha` for `alpha<1/2` and every all-log Fourier moment space, yet sparsely saturate the `1/Q` scale exactly where the moving atom samples it. A useful source theorem must force genuine moving-scale anti-concentration such as `Qm(Q)->0`, an `o(1/Q)` boundary remainder, one-sided tail structure or a direct moving-sample bound.

These lines support a sharper common statement: **terminal stability is topology-, scale-, rank-, phase-arrangement-, admissibility-, asymptotic-hierarchy- and discriminator-relative**. A transform can improve conditioning while preserving the target feature, or remain admissible and positive while inserting a nuisance term above the target scale. A recurrence can be visible but catastrophically ill-conditioned, and very strong global regularity can still miss a moving singular observer.

## Which growing source law is genuinely informative rather than sparse, diluted, under-resolved or circular?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-014](intuition/MI-014-componentwise-coercivity-needs-a-switching-cost-before-globalization.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now exposes a sharper dilution/circularity boundary. FD-128 shows that infinite exact Möbius ancestry can remain cheap when the active prime set is sparse. FD-129 shows that complete growing prime-prefix ancestry can become circular when paired with a stage-uniform critical envelope. FD-130--FD-131 show that global or rankwise means can dilute fixed-rank or finite-prime cuts. FD-132--FD-133 move the surviving defect to a central multiplicative-rank interface and show that it survives every fixed polynomial active-prime family.

FD-134 removes much of the apparent near-full-prime escape. With `Y(H)=H/P(H)`, the same positive-density wrong source has uniform mean ancestry decay over every prime `p<=P(H)` whenever `Y(H)->infinity` and `1+log(log H/log Y(H))=o(sqrt(log log Y(H)))`. Thus primewise means can remain noncoercive for explicit `P(H)=H^(1-o(1))` families. The present witness reaches its natural proof boundary only when the displaced central-rank strip becomes comparable with the squarefree Erdős--Kac fluctuation scale. The surviving currencies are genuinely joint rank-and-prime localization, local ancestry, central-strip-sensitive weighting, a growing exact anchor, still more aggressive cutoffs, or destination-side coercivity.

Möbius Cancellation now has pointwise, generic collective, quadratic collective, field-packaged and fixed-ramification capacities. MC-300 prices the current Page/Deuring--Heilbronn ceiling. MC-301 reaches conductor `sqrt(y)` collectively; MC-302 exploits quadraticity and reaches conductor `y`, but still leaves a factor-`R` capacity gap at the blind-support rank. MC-303 packages the independent modes into a degree-`2^R` multiquadratic field and shows generic Chebotarev paying through a huge discriminant.

MC-304 exposes structure hidden by that field invariant. All selected characters ramify inside one squarefree radical `P`, with `|D_K|=P^(2^(R-1))`. Fixed-modulus and sparse-character energy bounds force the reciprocal endpoint model to pay either sufficient reduced-residue density `phi(P)/P` or a large common radical; at the blind-support floor the second branch forces `P>=y^(2-o(1))`. The Page-induced discriminant bound alone forces only `P=y^(o(1))`, so field discriminant, radical size and totient roughness are genuinely different currencies. The remaining source theorem can now target the actual radical capacity balance rather than an unspecified “special family” improvement.

The cross-line lesson is stronger than “use stronger norms.” FD-134 and MC-304 show two different ways nominal breadth can fail: a theorem may monitor almost all prime directions while normalization hides the moving active layer, or it may package all modes into one algebraic object while the useful capacity is controlled by a more local invariant such as a common ramification radical. **Active-scale visibility and source-family capacity are separate from coordinate count and representation compactness.**

## Which support geometries survive the one-signed Weil first-crossing constraint?

WI-294 restores the sign constraint of the actual completely screened first null mode and makes the pole contribution positive. WI-295 keeps the exact digamma multiplier and gives a support-measure floor: with its exact bathtub profile `H(mu)`, complete one-signed screening forces `mu_*<|{v>0}|<=log 2`. It also adds a separated-mass tariff: if two positive-support pieces are distance `R` apart, their `L^1` masses satisfy `M_1(A)M_1(B)<-H(mu)/(4 cosh(R/2))`.

The live many-component problem is whether fixed positive support measure, exponentially decaying remote mass and `L^2` normalization can coexist with complete screening, or whether source-side regularity converts the `L^1` tariff into an `L^2` contradiction. Unrestricted supported spectral relaxation, sign-changing modes, incomplete screening and bounded aperture remain distinct branches.

## What should Prime Circle retain after centered Gram formation and oriented-frame classicalization?

PC-295--PC-299 identify the universal diffuse square-zero class, its defect scale `r_q`, and the first positive quadratic defect as a Fejer-smoothed source autocorrelation that loses source Fourier phase. PC-300 shows independently sampled cyclic profiles factor after expectation through powers of one real symmetric reflection-even average. PC-301 identifies an earlier quotient: centered same-source Gram carriers satisfy `G_(-s)=G_s` pointwise, so arbitrary source dependence cannot restore coordinatewise orientation erased before averaging.

PC-302 tests the most direct repair proposed by that boundary: retain oriented Fourier frames before same-source Gram formation. Reflection acts by latent mode reversal `X_-s=X_sJ`, so unlabelled frame/subspace geometry, principal angles, singular values and other positive basis-invariant data remain exact reflection gauge. The labelled odd component survives, but it is exactly a sine cross-Gram; under same-law averaging it becomes a positive covariance of imaginary additive Fourier coefficients. For the prime source this is ordinary additive exponential-sum data over primes.

The next carrier must therefore preserve a source orientation that is neither removable by latent mode relabelling nor reduced by the first positive quadratic compression to classical sine/cosine Fourier covariance. A canonical chirality/mode label, higher joint information, an independently justified order-sensitive observable, an asymmetric source window or a genuinely cross-level carrier are distinct possibilities. Merely moving the same positive geometry one stage earlier is not enough.

## Can the required Robin cancelling geometry occur for actual zeros?

RE-129--RE-131 turn hiding of a dominant algebraically strong atom into a nearby, logarithmically comparable companion with tightly constrained ordinate, horizontal displacement and cancelling phase. RE-132 shows that a sparse simple formal spectrum can realize the required doublet on sublinear windows. RE-133 is currently under unresolved adversarial review, so no current synthesis is strengthened from its growing-cluster matched control.

RE-134 independently supplies the positive local compactness statement needed to sharpen the source target. Any packet with uniformly bounded cardinality inside a fixed reciprocal-scale box has a uniform visibility floor on a fixed-relative window. A full hidden packet must therefore lose compactness: cancellation must use unbounded local occupancy, order-one mass outside every fixed reciprocal box, or both. If exterior cancellation is reciprocal-scale tight, occupancy in a fixed box must diverge.

The live attained-edge question is now a two-part source theorem: can actual off-critical zeta zeros have bounded reciprocal-box occupancy at the dominant phases, and can one prevent order-`M_K(U)` cancelling mass from escaping to larger reciprocal radii? Together those properties would rule out macroscopic hiding without relying on global one-level sparsity. The nonattained edge and `Theta=1` remain separate.

## What remains after the strictly subcritical prime-phase static scale is known?

VIS-225--VIS-226 fix the strictly subcritical static Ising/torus/SDP scale up to constants. VIS-227 calibrates the deterministic critical occupancy floor, and VIS-228 shows that subtracting that floor still leaves a generic extensive stochastic excess under independent uniform allocation.

VIS-229 now gives the exact independent nonuniform baseline. For one-point cell probabilities `(p_j)`, the collision center depends on `q_2=sum p_j^2`, while the variance also contains `q_3=sum p_j^3` through the nonnegative correction `q_3-q_2^2=Var(p_X)`. Thus matching only cell count, mean occupancy or `q_2` can manufacture a false residual from one-point heterogeneity even without pair dependence.

The critical pair-collision route begins only after the deterministic floor, the admitted one-point law and its `(q_2,q_3)` fluctuation scale have been removed, followed by a dependent control preserving the intended logarithmic-gap structure. If the residual disappears there, progress requires a genuinely different observable rather than another normalization of the same pair count. Sharp objective constants, optimizer geometry and dynamical access time remain separate questions.

## Xi Flow: move from local known-window inversion to global support discovery and zero-sensitive transport

Xi Flow now has a uniformly conditioned local nonlinear holomorphic chart for known consecutive packets with linearly many samples. XF-185--XF-186 show that growing known-packet complexity is not itself a local conditioning barrier. The live problem is global: locate and recenter unknown windows without an oracle, assemble separated or multiscale packets, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side source control to a zero-sensitive quantity.

This keeps sample complexity, local nonlinear conditioning, support search and source access as different currencies; remote known-packet conditioning is no longer the right obstruction.

## Global discriminator: price what the endpoint actually sees

The newest evidence strengthens [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md). AF-351 shows conditioning can improve without moving the singularity discriminator. FD-134 shows almost-full active-prime uniformity can still miss a defect concentrated on a moving rank layer. MC-304 shows field discriminant and fixed-ramification capacity are different source currencies. NB-133 shows visible recurrence plus radial depth can remain superpolynomially ill-conditioned through phase clustering. PC-302 shows a pre-Gram orientation repair can either remain gauge or classicalize to additive sine-Fourier covariance. PL-315 shows all-log and every subcritical Sobolev regularity can still miss a moving diagonal. RE-134 turns local hiding into a compactness/noncompactness alternative. VIS-229 shows nonuniform one-point heterogeneity changes collision fluctuations through a third moment. WI-295 converts one-signed screening into both a support floor and separated-mass tariff. WP-311 shows an admissible positive transform can contaminate a scale larger than the target curvature.

For every proposed route ask: **what is the source discriminator; at which stage is it quotiented; in which coordinates/ranks/directions does it live; what topology and normalization retain it; which quantifiers are uniform at the active scale; which dependence and orientation survive the carrier before averaging; what phase arrangement, resolution or moving scale does the endpoint sample; how large is the admissible family capacity relative to the obstruction; does a representation reduce complexity or move it to another invariant; is the final transform admissible for the source class; does it preserve the asymptotic hierarchy containing the discriminator; and do the hypotheses already reconstruct the desired endpoint?**

A route can fail by under-resolution, rank excess, angular clustering, unstable or topology-mismatched inversion, inter-rank or intra-rank dilution, pointwise-versus-uniform quantifier mismatch, family-capacity slack, complexity transfer under repackaging, carrier gauge, classicalization after positive compression, factorization under independence, moving-scale concentration, local noncompactness, deterministic or stochastic control baselines, transform inadmissibility, asymptotic contamination, terminal quotienting, universality, certificate exhaustion or circularity.

If a transfer between lines is only plausible rather than established, keep it as a clue rather than promote it to global mathematical knowledge.
