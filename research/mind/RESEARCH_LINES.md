# Global mathematical questions

## Can a source-native signed transformation become admissible without collapsing to Möbius, invisible curvature, universal support geometry, fixed-kernel projection, or programmable support deletion?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-016](intuition/MI-016-genuine-relational-information-first-appears-as-holonomy-after-gauge-quotient.md) separate source provenance, target sensitivity, relational structure, reachability, readout fidelity and source specificity.

Weil Positivity has closed scalar recovery, fixed positive multi-channel recovery, finite multiplicative-equivariant linear depth, flat site transport, faithful pointwise metrics and ambient output curvature invisible to the commuting source image. WP-300 closes the prime-symmetric scalar-projective upstream escape: full prime-relabeling symmetry leaves only flat or universal fermionic commutator holonomy.

WP-301 now closes the direct non-scalar frustration-free continuation. For arbitrary noncommuting local defects, a positive sum of squares has kernel equal to the intersection of the local kernels. Exact mixed-prime rejection is therefore already a two-prime fixed-space condition, higher matrix coherence does not enrich the zero-mode selector, and generalized-prime controls reproduce the same common-kernel mechanism. If a positive archimedean operator is included in the same frustration-free sum, the harmonic projection sees only its kernel and erases all nonzero spectral data.

The live curvature route must therefore make arithmetic information survive the readout: canonically moving kernels, a boundary/cohomological observable retaining nonzero spectrum, or a genuinely non-frustration-free finite--archimedean coupling with an independent sign theorem. “Non-flat, source-reachable and non-scalar” is still insufficient if the final projector discards the informative geometry.

## Which source resources survive into the exact destination margin and resolution?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) keep conditioning, source supply and target sensitivity distinct.

Analytic Frontier gives the same-profile cascade a sharp frozen-stage physical clock: odd-dilation return density is `Theta_G(ell^-2)`, while integer activation and local source resolution both turn on at `A~ell^2`. The remaining problem is source-energy non-excisability plus enough depth-uniform control to iterate those clocks.

Arithmetic Fidelity has now separated the observation-side and source-side bills. AF-338 reduces full-Euler fixed-order sign-regular transport from quadratic to linear depth by preserving harmonic collision geometry: order `r` is eventually available once `sigma>r-1`. AF-339 then shows that the canonical zero-sensitive discrepancy `Lambda-1` has ordered sign variation `~2(A-1)P/log P` on `[P,AP]`. Thus direct zero sensitivity does not supply the bounded source complexity consumed by the fixed-order theorem. The live bridge must compress effective source complexity while provably retaining the zero divisor, prove a genuinely growing-order observation theorem, or use another finite certificate that survives toward the zero-sensitive destination.

Nyman--Beurling now has a two-layer source restriction on finite high-zero target rescue. NB-117 identifies finite target access with weighted interpolation of the harmonic minimizer `C+D/n` by confluent actual-zero power sums. NB-118 adds exact finite Nyman orthogonality: once the finite capture exceeds a fixed residual baseline `d_M^2`, a target-bearing witness must retain only `O_M(1/R)` of its continuum norm in the first `R` natural cells, independent of zero height. The `M=3` baseline is already about `9.6%`. The live theorem is to exclude such strongly target-aligned actual zero-power packets at inverse-section retention, or construct one and understand its consequences.

Prime Flute's current sharp-cutoff completed-angle branch remains noncompact: adjacent Fourier modes straddling the hard cutoff generate order-one canonical correlations on infinitely many separated modules. A viable continuation must change splitting/observable rather than sharpen the same compactness estimate.

Prime Lattice has now removed both fixed-order Pohozaev admissibility and mixed Hadamard shape calculus from the scalar-stress gate. PL-308 applies Grubb transmission theory to the actual completed-Weil right-hand side and gives, for every fixed `s>0`, the exact weak virial identity

`Gamma(1+s)^2/(2s)(|tau_+|^2+|tau_-|^2)=E_s(w)+a^(2s)[b_a(w)+V_a(w)]`.

The translated source fronts are therefore not a fixed-order obstruction. The live analytic currency is uniform `s->0` control of the weak virial or source-structured derivative compactness strong enough to identify the zero-order aperture derivative. The rational-prime sign of that derivative remains a separate gate.

## Which cancellation theorem remains after representation enlargement and fixed-depth source constraints stop adding endpoint force?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md) and [MI-015](intuition/MI-015-relaxations-that-retain-an-endpoint-extremizer-cannot-prove-the-endpoint.md) track relocated endpoint costs.

Farey Discrepancy now has both an information classification and an extremizer test for its cumulative field. FD-117 makes the whole discrepancy field invertibly equivalent to the quotient-Mertens staircase; FD-118 compresses the needed same-horizon Fourier carrier to the divisor-closed floor-quotient skeleton `Q_N` of size `2 sqrt(N)+O(1)`. FD-119 then shows that the skeleton's Möbius equality ray survives any fixed true Mertens prefix plus the exact global floor-quotient identity at only `O_R(1)` additional energy. At `M(N)`-critical amplitude `~sqrt(N)`, fixed-depth physical truth therefore cannot create a fixed relative coercivity gap. A surviving theorem must use growing-depth arithmetic structure whose exclusion cost is itself critical-scale, or leave cumulative Farey discrepancy as the source object.

Möbius Cancellation has sharpened its sparse odd-support escape from one-character bias to exceptional-family rigidity. MC-289 shows that non-negligible effective occupancy forces a substantially negative generated quadratic product character. MC-290 proves that if analytic input controls all but `J` characters above `-delta`, the exceptional set must absorb essentially the entire forbidden odd moment; when `epsilon_m=delta^(m-2)(eta^-1-1)` is small and `log J=o(m)`, one exception becomes nearly the exact affine character. The remaining theorem can attack effective occupancy, conductor, exceptional-family size/bias, or rule out this near-affine concentration for the actual Legendre shell.

## Which exact null-mode constraint survives support relaxation in Weil Inertia?

WI-288 reduces complete one-signed symmetric two-island screening to the nearest-prime-power Lambert gap, WI-289 identifies prime squares as the unique critical composite layer, and WI-290 proves that the composite-safe residual contains at least `X^(3/8-o(1))` positive-length components on `[X,2X]` from the published prime-gap second moment.

WI-291 now quantifies the ordinary-prime side. The Lambert threshold forces any eligible upper host to be a `gg sqrt(X) log X` prime gap. Heath-Brown's published large-gap theorem confines the eligible center set to total length `O(X^(3/5+epsilon))` and at most `O(X^(1/10+epsilon)/log X)` host gaps; in logarithmic separation coordinate its dyadic measure is summable.

The bottleneck is therefore a sharply asymmetric cross-scale incidence theorem, not abundance. A polynomially fragmented safe reservoir generated largely by prime gaps near `sqrt(x)` must be localized relative to a much rarer selected family of giant gaps near `x`. The exponents `3/8` and `1/10` do not yield a pigeonhole intersection. Null-equation amplitudes/signs or a different support topology remain separate escapes.

## What should Prime Circle do with a one-profile observable whose prime locations RH makes invisible once total count is fixed?

PC-285 is the unconditional matched-control boundary. PC-286--PC-289 show conditionally on RH that the exact one-profile normalized boundary-log spectrum collapses toward Li controls almost to square-root scale. PC-290 removes the residual `log^2 q` gap by matching the total source count and pairing RH mean-square prime discrepancy with the existing `L^2` spectral path energy.

Thus for every `B=o(sqrt(q))`, exact prime positions produce no order-one separation from the count-matched control in this statistic. The remaining boundary is the actual denominator-microscopic `B~sqrt(q)` regime, the scalar source count itself, or a richer observable that retains cross-level, multi-source, directional, phase-sensitive or noncommuting information before singular-spectrum scalarization.

## What survives after regular-CA placement and every fixed algebraic height correction collapse to the same edge profile?

RE-117--RE-119 show that first recovery, block maximality and every sufficiently large regular CA state are governed at leading order by the same stable profile `J`, with a regular-CA phase mesh finer than every inverse power of `nu=log log C`.

RE-120 now closes the obvious lower-order escape under a horizontally isolated attained edge. For every fixed `K`, the complete `1/nu` expansion of the normalized CA height has coefficients `J_k` obtained by repeated stable filtering of the same `J`. Hence no fixed algebraic order in `1/log log C` supplies an independent discrete selector when the edge has a fixed spectral gap below it.

The live finite-edge source is therefore either a near-edge zero population inside the shrinking boundary layer `Theta-Re(rho) <=~ K log(nu)/nu`, or information outside the stable-filter closure. The nonattained-edge and `Theta=1` branches remain separate.

## How large can a pointwise prime-phase fluctuation be after distributed gauge-invariant frustration is priced?

VIS-213--VIS-216 reduce fixed-dimensional worst starts to unit-modulus torus optimization and give cycle-holonomy, fractional-packing and normalized magnetic certificates. VIS-217 proves that the normalized magnetic relaxation can reach Haar-RMS scale only if effective edge count is `O(n)`.

VIS-218 now computes the actual prime graph in the whole strictly subcritical regime `H log y/y->0`: `W_1~y/(H log y)`, `W_2~1/H`, so `N_eff~y^2/(H(log y)^2)` and `N_eff/n >> y/(H log y)->infinity`. The normalized magnetic certificate therefore misses the RMS scale by a diverging factor regardless of edge phases. That spectral relaxation is closed as a standalone route in this regime; the remaining static possibilities are stronger fractional cycle packing or the exact fixed-modulus torus constraint. Access time remains separate.

The cross-line lesson with Weil Positivity is now sharper and concrete. WP-301 exhibits **categorical readout loss**: a frustration-free zero-mode projection erases all nonzero local/archimedean spectral information. VIS-218 exhibits **quantitative relaxation loss**: a genuine gauge-invariant magnetic geometry survives, but its normalized spectral certificate has the wrong effective dimension. In both cases the right question is not whether nontrivial geometry exists, but whether the final observable retains the source-specific information and scale consumed by the destination theorem.