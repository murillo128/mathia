# Global mathematical questions

## Which independently selected source object turns critical mixed-prime data into a nonlocal positive law?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) separate nonlocal realizability from independent model selection.

WP-267--WP-273 show that hostability, category descent, scalar sign and localizable compression all fail as free routes to the desired Weil orientation. WP-274 then closes both obvious inverse-representation escapes. Pole normalization gives the positive Herglotz measure with masses `Lambda(n)/(n+1)`; Clark inversion can manufacture a model space with exactly that sampling law, and de Branges inverse spectral theory can manufacture a positive trace-normalized Hamiltonian from the corresponding Herglotz function. Both constructions work for generic Poisson-finite target measures and are therefore representation tautologies when the target carrier is supplied first.

The live bridge is independent selection: derive the inner function, Hamiltonian, canonical system, matrix/noncommutative coupling, or equivalent nonlocal object from source/Mathia geometry **before** using the desired Mangoldt carrier as spectral data, then prove that its pre-existing law reproduces the finite-prime, Gamma and polar sectors with the correct Weil orientation.

## Which source restrictions actually shrink the target-reachable failure set?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) now distinguish ambient dimension, one-sidedness, normalization and complementary physical channels.

AF-300 proves that a genuine complex-affine source dimension `r` changes the leverage bill to `r^(-1/2)` but leaves every remaining complex direction essential. AF-301 replaces deleted linear directions by a positive reachable cone. AF-302 then gives the normalized geometry exactly: the simplex fiber over retained state `y` is `r_S(y;z)+M(y)K_D(z)`. Total mass bounds the correction body, but recurrent multiplicatively independent discarded phases can still put zero inside the hull and create same-fiber safe/failing pairs. Remaining escapes must constrain the actual reachable body through discreteness, caps, sparsity, multiplicative relations, nonlinear source geometry, support restrictions, or fixed observation windows.

PF-315 supplies a complementary operator version. The full physical low/high angle is unitarily `(T_HV_H,Z_LH)^T`, with `V_H*V_H=I-Z_LH*Z_LH`. Attenuation before the conditional angle is therefore paid for by direct-channel saturation; under a uniform direct-channel gap, `V_H` is uniformly invertible. The physical task is singular-value/ideal control of these two exact channels, not an arbitrary source map or raw Green norm.

## Which residual cancellation theorem survives after generic abundance and generic coding bounds are exhausted?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md) now places both Farey and Möbius beyond their previous generic barriers.

FD-080 upgrades Farey recurrence from positive logarithmic density to subpower first passage. For every fixed `eta<c_q`, constant annular occupation reappears on an exact dilation descendant after `o(log X)` steps, within scale `X^(o(1))`; fixed-power bad deserts are impossible. The remaining issue is whether the downstream criterion is stable under that subpower enlargement or requires a stronger horizon-level/pointwise currency.

MC-251 upgrades the Möbius low-order multiplicity squeeze from Singleton to q-ary Hamming sphere packing on every primary torsion level. The generic rank deficit gains a logarithmic factor but remains sublinear in the wide fixed-order shell, so a near-full-dimensional blind code is still possible. The next theorem must use arithmetic dependence beyond minimum distance/general coding theory, or control the signed rough Möbius coset directly.

## Which source-specific inequality prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia continue to close complementary generic escapes. PL-285--PL-288 show that extraordinary ordinary `L^2` tail localization still does not transport the tiny endpoint positivity margin without weighted Fourier regularity or source-specific cancellation. WI-259--WI-262 reduce first-prime activation to an exact positivity certificate and show that both object assembly and the public LDL judge need independent repair before numerical positivity counts as theorem input.

Weil Positivity supplies the global counterpart: even a mathematically valid nonlocal positive representation carries no Riemann-specific sign information when it is reconstructed from the desired carrier itself.

## Which residual scale channel remains after exact local structure is factored out?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md) now has sharper residual decompositions on both Nyman and Robin sides.

NB-079 removes the common q-adic prediction mode. At each refinement `ell=G+P` and `c=G+S`, so the stable-tail charge changes only by `P-S`: residual fine-past discovery after coarse-future conditioning versus screening by future contrasts. Large common prediction `G` is irrelevant to the destination and should not be estimated.

RE-081 removes the discrete prime-power atoms from the leading long-fan transport; RE-082 then shows that the remaining smooth common-amplitude selector law already realizes the complementary power front with native exponent `1-b`. The Robin obstruction has therefore moved from continuous density shape to **assembly by genuine CA states**: fixed-state cells, tied spans, supporting slopes and prime-support constraints must prevent the required long concatenation if the branch is to close.

The common question is no longer where a large aggregate lives, but which residual channel still affects the target after exact cancellation, shorting or asymptotic removal of everything else.

## Can the complete flute angle satisfy the required ideal count after its source ambiguity is removed?

PF-315 resolves the immediate representation ambiguity. Sequential physical-band elimination gives the exact block-column factorization `(T_HV_H,Z_LH)^T` and nested defect identity `I-T_(P/H)*T_(P/H)=V_H*(I-T_H*T_H)V_H`.

The next target is therefore direct: determine the compactness/weak-trace singular-value law for `Z_LH` and `T_HV_H` on canonical tails, then close the symmetric reassembly and signed target composition gates. If `Z_LH` is uniformly gapped, the conditional angle `T_H` is fully visible through a uniformly invertible `V_H`; if it is not, the direct channel itself is part of the obstruction.