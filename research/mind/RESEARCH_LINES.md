# Global mathematical questions

## Which source resources survive the exact destination quotient, resolution and quantifier?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-013](intuition/MI-013-closing-the-bulk-often-leaves-a-thin-source-specific-endpoint.md), [MI-017](intuition/MI-017-sparse-exceptional-geometry-survives-upper-population-control.md) and [MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) separate source supply, stable transport, normalization, destination sensitivity and worst-case geometry.

Arithmetic Fidelity now resolves both the abstract and source-native target-relative Riesz recovery laws. AF-362 prices a declared target by the exponential Picard moment of its scaled spectral-energy distribution rather than hard bandwidth alone. AF-363 computes that cost for Sobolev/Bessel probes: below `beta=1/2` the normalized target pays the full upper-edge factor `e^v`, above `beta=1/2` the normalized recovery modulus tends to `1`, and the half-order endpoint remains logarithmically distributed. The separate base-height variable still controls absolute attenuation `e^{-u}`. Crossing a regularity threshold can therefore improve transport without moving the zero discriminator or supplying arithmetic specificity.

Prime Circle provides the complementary extreme. Exact degree-one cyclotomic data already identify the complete finite source, yet PC-307 shows that the actual prime low/polylogarithmic packet and its disjoint one-step translate are separated by only `O((1+log(2+K))/(q log q))`. Any order-one destination separation must pay almost `q log q` conditioning. Exact information and stable analytic transport can therefore diverge dramatically even for the true source rather than a generic control.

Prime Lattice gives the physical-side warning. PL-316/317 isolate the exact generic Sobolev margin needed by a moving Green observer, while PL-318 shows that even bounded zero-order forcing under the natural logarithmic endpoint operator can coexist with the moving-diagonal obstruction. The missing theorem must use structural state--source coupling, not merely a small or bounded forcing norm.

Nyman--Beurling supplies a source-specific positive example. NB-144 uses the actual Euler-product half-plane to impose a local Poisson-mass budget on zero packets. It strictly separates some NB-143 xi-type matched controls from zeta and forces logarithmic confluence to become sublogarithmic as the packet approaches the one-line, while leaving a fixed-interior logarithmic regime unresolved.

Robin Extremal now prices a local observer and one exterior component in the same reciprocal-radius variable. RE-145 shows that positive reciprocal-box packets lose visibility by at most `exp(-O(R))`, uniformly in local cardinality. RE-146 adds an actual-zeta rightward-shell budget: choosing the scale-maximal Robin atom converts horizontal displacement into coefficient/height cost, and an earlier-time fractional moment plus Ingham density gives `exp(-cR)` decay up to an explicit anchor-height factor. The unresolved comparison is no longer generic “exterior control” but synchronization of this shell estimate with the local witness plus budgets for the near-left and vertically remote near-horizontal pieces.

The common requirement is a **coupled-scale modulus in the topology, quotient, absolute error floor, declared target, local source law and worst-case quantifier actually consumed by the endpoint**. Generic regularity, exact algebraic recovery or analytic realizability is useful only when it controls that exact destination currency.

## Which growing source law is informative rather than unanchored, under-resolved, reconstructive, shareable or generically realizable?

Farey Discrepancy now exposes two ordered barriers. FD-145 comes first: relative ancestry equations on a visible component disconnected from the finite Möbius anchor have an exact `Z_2` orientation gauge, so **every** visible defect may vanish while all observed coefficients have the wrong absolute sign. Only after anchor connectivity is established does FD-144 become the quantitative lower bound: arbitrary source-independent normalized ancestry observations with total complexity `o(r_T)` can still be fooled by one common infinite prime coloring along a subsequence. The opposite high-information boundary remains FD-142, where one exact additive coordinate can reconstruct a squarefree prefix.

Möbius Cancellation turns its second-order source demand into an exact matched-control frontier. MC-314 converts `log Z <= ((1+eta_R)/R) log y` into a generalized-weight defect of order `R eta_R` at the live rank. MC-315 identifies the constant-defect endpoint with a diagonal projective generalized-cap problem. MC-316 then shows that systematic submatrix-rank conditions, the primal generalized-weight hierarchy, the dual hierarchy and the reflected erasure formulation are the **same constraint**, with dualization acting by transposition. The unresolved matched-control problem is therefore actual existence of the required binary matrix rank profiles, not another equivalent coding bound.

Prime Circle sharpens the conditioning side of the same accounting. Exact low-dimensional algebraic identification coexists with a translated-prime packet margin only `1/(q log q)` up to logarithmic frequency factors. Across these lines, “number of coordinates” is not a meaningful scalar resource. One must price anchor connectivity, effective source-state resolution, provenance, precision, cost sharing, stable separation and the exact ambient realizability frontier of the invariant being used.

## Which local zero laws survive after global density, functional equation and transfer arguments are exhausted?

Nyman--Beurling and Robin Extremal continue to expose the weakness of broad population envelopes, but both now have quantitative local geometry. In Nyman, xi-type entire realizability and global zero laws still permit target-poor logarithmic confluence, yet the actual zeta Euler product supplies a positive local Poisson budget that forbids part of that geometry and makes near-one packets sublogarithmic. What remains is the fixed-interior regime where the budget still permits `Theta(log G)` occupancy.

Robin now shows that the usefulness of a population estimate depends on how the target is anchored. RE-142 proves that upper zero density cannot by itself improve the generic rightmost-transfer loss. RE-145 gives the local `exp(-O(R))` visibility scale. RE-146 then chooses the actual scale-maximal Robin atom, which forces every sufficiently rightward zero to pay exponentially in coefficient/ordinate; only after that conversion can Ingham density sum the shell to `exp(-cR)` on an earlier subwindow. The near-left layer and vertically remote near-horizontal zeros remain the honest unresolved pieces.

The shared lesson is more precise than “generic upper population control is weak”: **a source law becomes coercive only after the exceptional geometry is represented in a currency that the source theorem actually controls**. In Nyman the Euler product directly charges local occupancy. In Robin scale maximality first turns horizontal displacement into a summable fractional coefficient moment. Neither mechanism transfers automatically to the other line.

## Which arithmetic quantity remains after universal source mass is quotiented away?

Weil Inertia separates total unscreened mass from the signed quantity seen after the explicit formula. WI-301 proves that complete one-signed dyadic screening forces a uniform archimedean reserve that a nonpositive test must repay through arithmetic interaction. WI-241 then shows, parity-free, that the pole cancels the density-one prime main term exactly; the residual source variable is the smoothed signed discrepancy of `d(psi-x)`, together with a bounded nonnegative density-one autocorrelation term.

Therefore “some prime-power mass survives” is not yet coercivity. The live bridge must show that the forced source contribution survives the density-one quotient with the correct sign. WI-302 records a newer source-exact compact certificate claim through aperture `17/16`, but it remains author-certified computational evidence pending independent replay and analytic audit. Its tail-preserving rank-two architecture is therefore a proposed research direction, not an accepted extension of the compact positivity theorem. The local clue `CLUE-tail-preserving-compact-positivity-beyond-log8` isolates the falsifiable structural question: whether a finite positive tail correction is actually necessary or minimal beyond the `log 8 / 2` threshold.

## What constitutes a representation-faithful null after nuisance variables are removed?

Visual Exploration now separates three exact calibration operations. VIS-237 shows that averaging same-cell collisions over grid origin is exactly the triangular pair-gap functional `sum_(a<b)(1-|x_b-x_a|/ell)_+`: an exact quotient of nuisance phase, not a stochastic null. VIS-238 shows that an unconditioned Poisson baseline adds a leading variance channel from total-count fluctuations, whereas the observed prime-coordinate sample is fixed-count. VIS-239 adds that an independently specified continuous one-point intensity can be removed exactly by cumulative-intensity coordinates.

The same-sample empirical CDF is the opposite of a stronger calibration: it sends order statistics to a deterministic rank lattice and therefore conditions away the tested spacing geometry. A representation-faithful null must match the sigma-field fixed by acquisition without making the destination statistic sufficient/deterministic. For the critical prime-coordinate statistic, that means quotient grid origin, fix the observed count, remove only an independently specified or uncertainty-accounted marginal intensity, and then model the remaining pair/higher-order gap dependence.

[MI-018](intuition/MI-018-canonical-normalization-can-universalize-the-observable-while-moving-all-source-specificity-into-an-induced-law.md) records the broader exact lesson shared with Weil Positivity: a canonical normalization can simplify the observable to a universal class while moving source specificity into the induced probability law. The induced law, not the universal normalized sign or shape, then carries the remaining proof obligation.

## Which scalar or local reductions remain structurally capable of carrying arithmetic specificity?

Weil Positivity has now closed the pure-measure scalar Julia positivity mechanism much more sharply. WP-314--WP-317 show that the empty-side moving tangent is universal, atom-mass scaling can lose compactness under prime clustering, isolated poles scalarize, and every fixed finite multipole reduces through classical interlacing degree drops. WP-318 extends this to arbitrary finite or infinite positive measures: after derivative biasing at a regular node, the normalized response is exactly `tau-K_nu`, the Boolean self-energy profile of a probability law; its Herglotz sign is universal, and every gap-supported probability law is realized by a matched positive control.

The scalar source question is therefore no longer whether an infinite collective positive tangent exists. It is whether the **specific derivative-biased Mangoldt probability laws** satisfy a source-forced compactness, rigidity or limiting property absent from matched controls, and whether that property couples canonically to the Gamma/global counterterms. Otherwise the construction must leave the audited pure-measure scalar category through an affine mass-at-infinity channel, matrix/operator-valued structure, generalized resolvents or nonseparable finite--archimedean coupling.

Xi Flow remains a different global-local problem: its local packet inversion is well conditioned once the packet is known, but support discovery, multiscale assembly and transport from theta-side control to a zero-sensitive destination remain the unresolved gates.

## Global discriminator: identify the exact resource that the matched control cannot fake

The current corpus repeatedly separates nearby notions that broad prose tends to collapse: absolute attenuation from target-relative Picard/regularity cost (AF-362--AF-363); relative ancestry consistency from absolute component anchoring and then from observation count (FD-145, FD-144); first-order source scale from the diagonal projective rank-profile frontier and its dual re-encoding (MC-314--MC-316); exact finite source identification from stable low-frequency separation (PC-306--PC-307); xi-type analytic realizability from the local Euler-product Poisson budget (NB-143--NB-144); generic critical/bounded-forcing control from moving-sample anti-concentration (PL-316--PL-318); local reciprocal visibility from a source-coupled rightward-shell budget and the still-uncontrolled exterior pieces (RE-145--RE-146); nuisance quotienting, fixed-count calibration and independently specified marginal normalization from a stochastic dependence null (VIS-237--VIS-239); total arithmetic reserve from signed density-subtracted discrepancy (WI-241); and scalar Julia positivity from source-specific restrictions on the induced derivative-biased probability law (WP-318).

For every proposed route ask: **what exact quantity reaches the destination; at what scale and precision; what target or local observer consumes it; what universal component or gauge is quotiented out; what induced law remains; what exceptional configuration still survives; can a matched non-arithmetic object realize the same invariant or law; and which source-native theorem excludes that matched control?** The productive next theorem is the one that closes one of those explicit gaps without reconstructing the answer, double-counting an equivalent constraint, or importing the endpoint in disguise.