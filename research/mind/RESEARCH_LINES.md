# Global mathematical questions

## When does repeated observation actually shrink the admissible source fibre?

[MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md), [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md), [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md), [MI-019](intuition/MI-019-source-identification-and-zero-selection-are-orthogonal-gates.md), [MI-021](intuition/MI-021-representation-enrichment-only-buys-leverage-where-source-specific-coupling-actually-lives.md), [MI-022](intuition/MI-022-unbounded-compatibility-can-turn-finite-cutoff-ambiguity-into-a-stability-problem.md), [MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md), and [MI-025](intuition/MI-025-combinatorial-breadth-must-be-priced-through-the-source-coordinate-map.md) separate retained information, common-source compatibility, conditioning, acquisition geometry, aggregate readout capacity, source-coordinate reach and final destination selection.

Arithmetic Fidelity now separates the invariant physical locality wall from the combinatorial depth used to realize it. AF-394 identifies the exact smooth order-three target: `q=-(log f)''` must satisfy `q>=0`, `(log q)''<=2q` on `{q>0}`, and `{q>0}` must be connected. AF-395--AF-398 show that the topology wall occurs at physical prime-log reach one and that one mesh-width of overhang suffices, yielding the three-term prime-index threshold. AF-399 generalizes the resource conversion: for any increasing regularly varying scale `a_N` of index `rho>0`, a fixed logarithmic wall `h` has successor depth `B_N(h)/N -> exp(h/rho)-1`, with lower-order index corrections inherited from the source-density expansion. **Physical reach is invariant; successor breadth is a source-coordinate law.**

Farey Discrepancy makes the source-class effect sharp. FD-171 proves consecutive midpoint horizons have zero exact redundancy over unrestricted formal sources. FD-172--FD-174 show that bounded squarefree multiplicativity turns the transformed defect into nonnegative mass, makes sparse orbit-hitting criteria rigid and makes every fixed integer geometric ladder finitely certifying. FD-175 identifies what prevents recursive sparse-shell repair: the decisive resource is not coefficient accuracy at one horizon but **cross-coordinate multiplicative compatibility**. If that global product law is removed, sparse midpoint agreement can be repaired shell by shell while the target remains different. The open issue is therefore the sharp certification scale and how much of the multiplicative source law is actually necessary for destination rigidity.

Robin Extremal pushes the opposite direction beyond finite-dimensional scalar constraints. RE-180 proves that honest nonnegative prime multiplicity forces any early two-jet repair to pay `Omega(sqrt(p)/log p)` actual deletions unless it pays a remote KV reset budget, and RE-181 realizes that tariff with `{0,1,2}` ordinary-prime multiplicities. RE-182--RE-183 show that every fixed finite boundary-jet depth or separated Euler-temperature panel can still be matched exactly. RE-184 now makes the dimension itself grow: whenever `K(p) log(K(p)+2)=o(V(p))`, `V(p)=(log p)^(3/5)/(log log p)^(1/5)`, the same honest source class can match all Euler derivatives through depth `K(p)`, preserve every fixed KV envelope and retain an order-one transient Robin excursion. The live boundary is a quantitative crossover toward the much larger `log p/log log p` capacity scale, not finite versus infinite scalar dimension.

Together AF-399, FD-175 and RE-184 sharpen the multiscale lesson. Raw scale count, ambient row rank, finite or slowly growing constraint count and lower-bound tariffs are not intrinsic information currencies. One must declare the admissible source fibre and ask which relations remain accessible at the destination's physical scale: a fixed physical wall can cost linear index breadth with source-density corrections; nonlinear cross-coordinate compatibility can collapse an ambiently repairable sparse system into a rigid one; and even an unbounded scalar Euler family can remain nonselective when the source class realizes its interpolation cost.

## Which quantitative resource reaches a thin or moving destination?

Prime Lattice has reduced the first-prime gate to one concrete state-space estimate. PL-329 gives the sharp source-amplitude condition for the shrinking reflected sign; PL-330 transports isolated eigenbranches in norm-resolvent/`L^2` topology; PL-331 proves every fixed logarithmic Fourier seminorm still misses the hostile moving layer; PL-332 transports the selected asymptotic coefficient once the endpoint remainder is family-uniform. PL-333--PL-334 block the ordinary full/even Hopf shortcuts, while PL-335 finds the exact state-specific odd mechanism and forces a noncollapsed threshold half-log boundary amplitude. PL-336 then shows that a uniform `L^infty` bound on the normalized simple odd branch would simultaneously give `S_delta=O(1)`, the uniform endpoint remainder and `C_delta->C_0>0`, closing the PL-329 ratio. The remaining first-prime burden is therefore principally **uniform state boundedness/concentration control**, not a separate search for another fixed-threshold positivity mechanism.

Visual Exploration has moved past the obvious support-design obstruction. VIS-259--VIS-269 separate packet conditioning, universal pole background, prime-power residual, Wang support/seminorm budgets and complete leading-functional cancellation; VIS-269 proves that any fixed-order shrinking packet eventually supported below the first prime frequency is exactly prime-power blind. VIS-270 then constructs a fixed two-band packet whose leading Wang diagonal/cusp functional vanishes while one band still hits exactly the first prime frequency and carries a nonzero pure first-prime quadratic coefficient. The representation can therefore be simultaneously leading-null, source-sensitive and fixed-conditioned. The live problem is now **lower-order theorem transfer**: the available Wang remainder is too large and does not identify that Rodgers arithmetic coefficient in the short-interval statistic.

Nyman--Beurling now separates worst-case, typical-height, quotient-correct and rank-growth acquisition. NB-176--NB-182 price genuine Euler amplitudes in Hilbert and Wiener geometries and expose the generic `sqrt N` dense-code tariff. NB-183 shows that the worst-case source norm is not a typical-height obstruction. NB-184 quotients the exact invisible constant mode before pricing regularity and reaches the quadratic density-one regime: if `a->0` and `T a^2->infinity`, then `sqrt(a) mathcal H_(a,B)(t)->0` in relative measure and the charged scalar acquisition ratio beats the generic `sqrt N` threshold. NB-185 now shows that every **fixed finite** Hadamard prime-phase code still occurs inside that same good-height set. Finite prime-phase algebra is not the rank obstruction; the live question is quantitative growing-dimensional phase hitting with true Euler amplitudes and the correct acquisition charge, plus the genuine endpoint `T a^2=O(1)`.

Xi Flow gives the inverse lesson: once the correct consecutive packet is known, local holomorphic recovery can be uniformly conditioned with linear measurement complexity even far out. The bottleneck is global support discovery, multiscale assembly, source-valid complex access and the bridge to a zero-sensitive Xi quantity, not local inversion itself.

AF-399, VIS-270 and WI-330 support [MI-025](intuition/MI-025-combinatorial-breadth-must-be-priced-through-the-source-coordinate-map.md): count indices, heights or packets only after mapping them into the source coordinate that the destination actually consumes. The same numerical breadth may represent a fixed physical wall, a single source cell, or genuine access to a new arithmetic frequency.

## How much source information can an analytic architecture exploit at bounded conditioning?

Möbius Cancellation now separates acquisition syntax, exact channel distinction and destination-relative reliability geometry. MC-338--MC-345 show that bounded-energy dephasing requires extensive joint source information and that fixed-quality noisy access pays channel capacity. MC-346--MC-347 add the logarithmic reliability tariff; MC-348 replaces raw coordinates by symmetrized KL on the natural neighboring-state graph; MC-349 restricts the compulsory reliability budget to source edges crossing the destination quotient. MC-350 now proves that the transcript/channel-law quotient required for bounded-energy dephasing itself has an **extensive exact cut**: its average cut degree is bounded below by its quotient entropy, hence linearly by the information floor. Near the matched-filter limit almost every local source direction must cross that exact quotient boundary. But crossing laws can still be arbitrarily close, so the remaining bridge is quantitative transcript divergence across the forced cut and its relation to the actual Möbius endpoint quotient.

Farey Discrepancy, Nyman--Beurling and Robin Extremal provide complementary warnings. Unrestricted acquisition can hide the inverse in the sensor; changing horizons can hide many rows behind one spatial coordinate; strong nonlinear source constraints can turn a formally repairable sparse system into a rigid one through cross-coordinate multiplicativity; genuine Euler labels remain nonselective if amplitudes are normalized away; quotienting exact invisible modes can make a natural tariff affordable on most heights; fixed prime-phase rank can survive that good-height filter without giving quantitative growing rank; and even a substantial unbounded scalar Euler jet can remain interpolable inside an honest source class. Source-information cost is meaningful only after source class, target quotient, acquisition topology, operator variation, amplitudes, matched controls, quantifier, dimension-growth law and downstream resolution are fixed.

This updates [MI-023](intuition/MI-023-source-information-is-priced-by-inverse-distinguishability-not-forward-smoothness.md): measurement count, nominal dimension, positive noise, adaptivity and canonical probe labels are secondary. The load-bearing quantities are inverse modulus, orbit-hitting and certification scale, source-class-relative kernel, destination-cut geometry, channel-quotient boundary, per-edge transcript divergence, per-access capacity, source-amplitude conditioning, exact observation quotient, acquisition-space norm, height quantifier and the number of genuinely independent operator/source states required by the destination.

## Which spectral or positivity enrichment survives its intrinsic quotient and density law?

Prime Circle has reached both a classical autocorrelation boundary and exact scalarization no-go boundaries. PC-325--PC-330 establish the phase/autocorrelation quotient, universal Hilbert background and failure of smooth Li subtraction. PC-331 writes the source-sensitive excess as a positive Hankel/Hardy-square energy of Fourier magnitude squares, PC-332 decomposes it dyadically, PC-333 identifies its reciprocal-scale theorem with cyclic short-interval prime-discrepancy variance, and PC-334 shows a literal binary complement reproduces the normalized magnitude package exactly. PC-335 then closes bounded indefinite Mellin quadratic scalarization. PC-336 closes the most intrinsic point-supported singular escape as well: every finite-order distribution at `s=1` is exactly a finite generalized-von-Mangoldt jet. A surviving route must retain pre-scalar relational structure, use genuinely non-endpoint/cross-shell information, or provide an independent zero-sensitive theorem rather than increasing endpoint singular order.

Weil Positivity has narrowed the Maaß--Selberg escape past generic arithmeticity. WP-345 shows the positive height boundary square is divisor-blind; WP-346 realizes the scalar CDD control with positive self-adjoint Robin channels; WP-347 moves the control onto the same one-cusp hyperbolic surface via a point interaction. WP-348 finds the same logical freedom inside unperturbed squarefree congruence scattering: ramified local factors are source-defined reciprocal unitary-axis factors with explicit off-critical divisors. Moreover fixed channel combinations cannot strip one bad-prime logarithmic derivative while retaining the common completed-zeta block. The surviving route must distinguish the **unramified global normalizer from ramified local dressing before scalarization**, inside a genuinely positive arithmetic mechanism; broad labels such as Hecke, Euler or congruence arithmetic are not selective enough.

Weil Inertia gives a positive example of exact source correction removing a false resonance but closes three height-based de-exceptionalization ideas. WI-327 restores the exact signed coefficient `Lambda-Lambda_X^sharp` and kills the cubic resonance on density-one starts. WI-328 shows an entire still-live bow is twist-frozen on the endpoint cubic source block. WI-329 moves to the first natural scale where bow heights genuinely dephase, but the matching almost-all nilsequence theorem is already uniform over all fixed-degree polynomial phases on one common good set of starts. WI-330 then shows that even the natural Riemann--Siegel source coordinate contracts the whole surviving sub-square-root bow into at most two adjacent source cells, with resonant families occupying one cell. Height diversity therefore supplies neither independent exceptional sets nor independent arithmetic source starts. The remaining obstruction is pointwise signed control or a genuinely independent source-position parameter, together with the stronger `L^2` norm required by the exact covariance.

These lines support [MI-024](intuition/MI-024-control-subtraction-must-match-the-load-bearing-source-channel.md): a matched control or subtraction is useful only when it acts in the representation and norm carrying the alleged signal. Smooth density subtraction can miss sparse Hilbert energy; an exact source correction can cancel a generic resonance; a source-sensitive scalar may collapse to classical short-interval variance or a literal complemented-source control; endpoint singularity may collapse to a classical arithmetic jet; phase diversity may leave one common arithmetic exceptional set and even one source cell; and genuine arithmetic scattering may contain ramified divisor-mobile inner factors that positive fixed-channel geometry cannot strip selectively.

## Which source fingerprint is selective rather than merely reconstructive?

Robin Extremal and Farey Discrepancy give opposite source-class effects. FD-173--FD-175 show strong multiplicative compatibility can make extremely sparse horizon testing rigid and finitely certifiable, whereas removing that cross-coordinate law restores recursive sparse-shell repair. RE-181--RE-184 show that ordinary-prime support, bounded honest multiplicity, KV fidelity and even an **unbounded sub-KV-scale boundary Euler jet** admit explicit matched repairs that move the Robin destination. Source restrictions matter only when they remove the destination-relevant matched fibre, not merely when they make reconstruction expensive or increase scalar dimension.

Arithmetic Fidelity exactly separates local analytic recovery from global target topology and turns a fixed physical wall into a source-dependent index resource law. Prime Circle classicalizes its normalized magnitude statistic, supplies an exact binary complement control and now classifies endpoint-supported Mellin singularities as generalized von Mangoldt jets. Visual Exploration shows that source-blindness is a support-location effect rather than a necessary price of leading-functional cancellation: a fixed translated packet can null the universal term while retaining the first prime frequency. Nyman--Beurling shows that exact observation quotients must precede regularity pricing and that fixed finite prime-phase rank survives the resulting density-one acquisition filter. Weil Inertia shows that many heights can still correspond to one arithmetic source cell. Weil Positivity shows that even genuine congruence arithmetic carries ramified off-critical inner factors unless the unramified block is isolated by stronger pre-scalar structure. Prime Lattice reduces its first-prime sign gate to a uniform moving-state bound after a state-specific odd Hopf mechanism closes threshold noncollapse.

The program must therefore separate **source fingerprint**, **admissible source fibre**, **target/observation quotient**, **destination-cut geometry**, **exact channel boundary versus quantitative discrimination**, **acquisition/transport topology**, **source amplitude and renormalization cost**, **physical source reach versus combinatorial breadth**, **source-density/coordinate conversion**, **fixed versus growing source-coordinate depth**, **cross-coordinate compatibility**, **channel-matched control subtraction**, **conditioning/capacity/reliability**, **local analytic recovery versus global component topology**, **resource lower bound versus matched realizability**, **joint versus scalarized geometry**, **endpoint singularity versus genuinely new carrier structure**, **ramified versus unramified arithmetic structure**, **global operator positivity versus state-specific reflected sign mechanisms**, **exceptional-set quantifiers versus phase and source-start diversity**, and the final **destination-selective sign or zero theorem**.

## Global discriminator: identify the exact resource a matched control cannot fake

For each proposed route ask: what exact source distinction reaches the destination; what source class and target quotient are being compared; which source modes the destination annihilates exactly and whether they were quotiented before regularity was priced; what information is regenerated for free; whether horizon/operator changes contribute independent constraints and whether source restrictions collapse their kernel through repeated defect orbits or cross-coordinate compatibility; whether qualitative recurrence is quantitatively certifiable at the needed source scale; whether fixed or growing scalar source coordinates can be interpolated by a matched repair and where their depth crosses a genuine capacity threshold; whether the inverse has been moved into the sensor; whether weak source channels were independently renormalized and at what cost; whether the declared acquisition topology contains the natural source readout and what gain it charges; whether that gain is worst-case, typical or pointwise at the destination's selected height; which neighboring source directions cross the exact channel and destination cuts and what transcript divergence they receive; what native norm gives a useful inverse modulus; whether the count of indices/heights/samples corresponds to genuinely different source coordinates or only to one physical cell; whether finer sampling increases relational reach or only shrinks the physical stencil; whether a topology crossing needs a fixed analytic margin or only overhang relative to mesh and how the source-coordinate map converts that wall into combinatorial breadth; whether a lower-bound tariff is actually unaffordable inside the source class; whether a larger witness class merely certifies generic metric packing; whether local certificates recover only the differential cone or also component topology; whether a control subtraction matches the norm/channel carrying the signal; whether a source-sensitive scalar factors through a phase/autocorrelation quotient, literal complemented-source control, classical window-variance transform or generalized-von-Mangoldt endpoint jet; whether a positive/self-adjoint realization still permits divisor-changing controls **inside the claimed arithmetic category** and whether ramified factors can be separated from the unramified block before scalarization; whether failure of global Markov positivity still leaves a source-specific reflected maximum principle on the actual state; whether an almost-all theorem controls the distinguished sequence, is already uniform over the phase family, or all those phases map to the same arithmetic source start; and what theorem finally converts the survivor into a Riemann-zero or positivity restriction.

A useful bridge should fail on a matched control for an independently arithmetic reason. Exact reconstruction, unbounded observation count, unrestricted target-aware mixing, one spatial sensor with uncharged horizon variation, ambient row-rank lower bounds that disappear inside the source class, qualitative orbit recurrence without certification, finite or subcritically growing scalar Euler panels, genuine frequency labels with erased amplitudes, bounded Hilbert duality that excludes the natural functional, non-Hilbert acquisition with uncharged Wiener gain, regularity charged before quotienting invisible modes, typical-height affordability plus fixed-rank phase recurrence without quantitative growing-rank acquisition, arbitrarily fine sublinear-reach stencils, a large but realizable source tariff, a rich spectrum, a critical boundary, source labels, a positive principal form, global/even Hopf failure when an exact odd reflected mechanism survives, operator-valued lifts, adaptive scalarization, singular endpoint jets that classicalize to generalized von Mangoldt data, a positive divisor-blind one-cusp boundary norm, generic congruence arithmetic with unstripped ramified inner factors, a nonzero low-frequency carrier, fixed-order jets whose support excludes prime frequencies, a large Gram/nuclear response, benign forward regularity, positive readout noise, adaptive or jointly coded observations without destination-cut discrimination, infinitely many remote resets, smooth PNT subtraction, a phase-blind Hardy excess equivalent to classical short-interval variance, a normalized magnitude statistic reproduced by a literal binary complement, zero-set membership, raw positive von Mangoldt weighting, many twist-frozen bow ordinates, many genuinely dephased heights controlled by one common exceptional set, or many heights occupying one Riemann--Siegel source cell are not enough unless the surviving quantity is stably distinguishable, charged in the correct source geometry, theorem-accessible at the required quantifier and scale, robust under the full source operator, and selectively coupled to the RH destination.