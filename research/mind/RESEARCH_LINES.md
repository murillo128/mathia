# Global mathematical questions

## Which source identity fixes the mixed-prime kernel?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) gives explicit positive `r`-prime perturbations invisible to all proper marginals. What identity of the actual source excludes those centered-product directions for arbitrary `r`, and what joint kernel does it select? Independence is one exact answer for the Bost-Connes source, but [MI-004](intuition/MI-004-critical-axis-is-not-zero-selection.md) shows that the common critical ray law does not select it. Selecting the product law also does not establish equality to the completed Weil pairing.

[MI-002](intuition/MI-002-the-surviving-arithmetic-variable-is-relational.md) provides a constructive comparison: a bispectral multiplicativity equation reduces residual phases to one translation. A transfer would need an identified arithmetic analogue of that equation and its nondegeneracy hypotheses. Merely increasing a fixed test order is already excluded by the centered-product controls.

The Weil-positivity Fock branch adds an operator-category restriction. A common finite-dimensional boundary can create cross-prime coherence while preserving the exact critical Mangoldt diagonal on finite truncations, but the infinite boundary map is nonclosable at the Weil exponent; retaining orthogonal prime labels restores boundedness and prime separability. A viable mixed-prime kernel therefore needs a source-forced closable infinite-dimensional correspondence, or a separately justified different category, rather than finite-dimensional coherent scalarization.

## What readout retains a source-dependent quantity after the matched control?

For the flute hierarchy, can an observable retain the resolved `w_j^2/w_(j-1)` correction through its final aggregation and distinguish a matched nonprime gap geometry? Its low-mode product cannot: [MI-001](intuition/MI-001-preserve-structure-before-compressing.md) identifies the exact determinant cancellation. Realization of the resolvable prime hierarchy is separately conditional.

For Prime Circle, [MI-003](intuition/MI-003-arithmetic-significance-begins-after-universal-control-subtraction.md) now removes both obvious finite labels and the canonical total-winding reformulation. Connected components have classical simple oscillatory spectra; central reflection parity is fixed by eigenvalue order; and the cumulative Schur/Pruefer/Maslov rotation is exactly the eigenvalue-counting function whose leading nonresonant limit is the already-classical torus IDS. A surviving statistic must therefore use finer placement, shrinking-window or mesoscopic data, subleading winding, individual matrix-valued phases, transfer-norm/Lyapunov behavior, or near-resonant structure.

For Nyman--Beurling, [MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md) turns the collective late-span loophole into an exact localization question. The late quotient tail is the range of the compressed natural shift on the arithmetic defect space, and its orthogonal complement consists exactly of finite-Paley--Wiener-support defect vectors. The target question is whether those finite-time arithmetic defects approximate the canonical residual, not whether individual late columns are small.

## Which quantitative observation family reaches the required source aperture?

With local Euler degree bounded by `d`, [MI-005](intuition/MI-005-topology-is-part-of-the-arithmetic-claim.md) separates finite depth per prime from unbounded prime breadth and supplies exact finite-jet collisions. For a target requiring coefficients through `N`, can its source geometry provide a uniform absolute-convergence budget and observation accuracy/horizon strong enough for the actual target metric? Qualitative coefficient recoverability is not enough.

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) now separates continuous time breadth, cadence aliasing, channel coupling, and finite-window conditioning. A finite family of complete regular lattices is faithful on the unrestricted ordinary-Dirichlet `ell^1` class only if one cadence is already nonresonant; several individually aliased lattices have an explicit common interaction kernel. Even an injective tuple of phase labels can lose information if acquisition keeps only separate marginals rather than their joint coupling. Every fixed finite cadence family still has remote simultaneous near-aliases on bounded windows.

The observation count is therefore not the source dimension. The same warning appears in Robin Extremal: inside one fixed CA threshold cell, the endpoint-corrected standard and reciprocal prime sources are exactly frozen, with at most one binary raw fringe update. A continuum of tangent parameters is a reparameterization of one source state, not a continuum of independent arithmetic samples. A proposed acquisition or enrichment mechanism must identify where genuinely new source coordinates or joint relations enter.

Visual exploration supplies a distributional calibration of the same principle. For weighted continuous prime-log phases, fixed moments transfer to the matched weighted Haar null under the explicit horizon condition `y^k D_2(y)^(k/2)/H -> 0`. With `1/p` weights, non-Gaussian low-prime shape persists while all preassigned fixed moments transfer for polylogarithmic support. Statistical complexity already reproduced by the representation-matched deterministic/Haar control is not source evidence.

## Does the complete flute response preserve the weighted source budget?

Can the physical mixed response be factored as `G_n=T_nY_n(a_n)+R_n`, with uniformly positive-Sobolev-bounded transport, `sup_n sum_i q_i|a_(n,i)|<infinity`, and vanishing physical-high projection of `R_n`? The known canonical synthesis/high-pass estimates would then exclude persistent local shorting-deficit witnesses.

PF-297 sharpens the missing mechanism. The canonical fixed-axis Robin inverse is `ell^1(q)`-nonexpansive and preserves positive `q`-mass in the complete system. Finite-section strict contraction is only omitted-tail leakage and its norm tends to one with depth. The unresolved theorem must therefore derive the actual `P/H`-split return/reassembly equation and obtain summability from the physical forcing/escape balance, support loss, a source-adapted excessive potential, signed cancellation, or another factor outside the conservative Robin normalization.

Even successful local annihilation would leave the global heavy-range projection product and its singular-value counts to estimate; those remain a separate Prime-Flute question.