# Global mathematical questions

## Which source identity keeps a critical mixed-prime geometry canonical and closable across regulators?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) shows that proper marginals, exact diagonal coefficients, positivity, closability, and even a sharp global signature do not by themselves determine a unique arithmetic coupling or scale. The Weil-positivity Fock branch successively closes finite-dimensional coherent collapse, programmable locally finite incidence, and normalized complete-Gibbs incidence as sufficient selectors.

WP-264--WP-265 exposed a nontrivial complete-incidence finite part but also a real regulator defect: leading-only sharp and Gibbs source-Hamiltonian regularizations land in different inertia classes. WP-266 now removes that particular ambiguity intrinsically. The spectral-diagonal conditional expectation of the simple prime Hamiltonian deletes the full one-body diagonal before the limit, so sharp and Gibbs connected families both converge in trace norm to

\[
K=aa^*-D,
\qquad d_p=\frac{\log p}{p^{3/2}},
\]

with exactly one positive direction and strict primitive negativity.

The obstruction has therefore moved. The source-canonical Hamiltonian transport `e^{H/2}` restores the critical Weil prime amplitude on every finite compression but globally turns the rank-one coefficient vector non-square-summable, so the critical form is nonclosable. The connected remainder also does not generate the Gamma/polar term. A viable global kernel must reconcile **regulator covariance, critical scaling, closability, and archimedean completion in one source-derived object**; another finite scalar counterterm does not address the remaining problem.

## What readout retains source-dependent information after the relevant orbit closure and local structure are matched?

Prime Circle now has a stronger finite-lift boundary. [MI-003](intuition/MI-003-arithmetic-significance-begins-after-universal-control-subtraction.md) includes PC-269--PC-270. Bounded-mode nonresonant prime sequences approaching a rational torus point converge to periodic-Haar bulk laws. If instead one fixed primitive exact resonance persists, primality forces it to be the only fixed independent resonance, the orbit converges to Haar measure on the corresponding one-dimensional subtorus, and the complete normalized bulk spectrum converges to the induced one-circle IDS. Matched controls carrying the same resonance reproduce that law.

Thus a fixed finite lift may select the orbit closure, but **fixed-complexity bulk averaging is classical once that closure is fixed**. The surviving Prime-Circle channels are growing resonance vectors, inverse-near-resonance mesoscopic windows, microscopic/shrinking spectral statistics, singular cocycle functionals, subleading transfer data, or arithmetic statistics of resonance-sector occurrence.

Nyman--Beurling has moved from defect existence through rank to conditioning. [MI-007](intuition/MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure.md) uses NB-061--NB-062: every finite log window has an infinite-dimensional defect sector, while the visible natural space has **exactly** rank `R-1` and receives one nonzero causal innovation per newly opened logarithmic cell. Global orthogonality turns the canonical target leakage into `b_R^*H_R^{-1}b_R`, with `b_R` induced by tails and `H_R` the causal-basis Gram matrix. The target question is therefore forward-memory conditioning and alignment, not defect abundance or hidden algebraic dependence.

Robin Extremal has similarly narrowed. RE-064 rules out bounded `1 -> 1` fringe packets and shows that an Egyptian `0 -> 1` cancellation followed by another bounded switch forces an order-one negative rebound. An extended bounded near-zero-charge fan can therefore use only the fringe-free `0 -> 0` deep-layer escape; the separate remaining escape is unbounded packet cardinality.

Visual Exploration now calibrates polynomial support by test complexity. VIS-178 shows that for prime-power weights the `k`th continuous moment remains matched to Haar on `y=H^delta` throughout `delta<=1/e_k`, `e_k=floor(k/2)+k/2`, including equality. Positive-power support alone is therefore not a source-sensitive regime; the statistic must exceed its order-matched wedge, grow in complexity, change sampling/functional class, or introduce genuinely source-bearing information.

## Which quantitative observation family reaches the required source aperture at the right resource cost?

With bounded local Euler degree, [MI-005](intuition/MI-005-topology-is-part-of-the-arithmetic-claim.md) separates finite prime-power depth from unbounded prime breadth. [MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) now has a complete finite-prefix sample-order calibration from AF-282--AF-285. For the two-prime mixed channel on the known prefix `1,...,N`, phase labels have spacing `Theta(1/N)`, stable coordinatewise mixed degree is sharply `Theta(N)`, and a deterministic unweighted subset of at most `8N` distinct raw mixed moments already gives a breadth-uniform stable inverse under adversarial per-sample absolute noise.

Rank requires at least `N` complex observations, so the raw sample-count order is now exactly `Theta(N)`. The old `N` versus `N log N` gap is closed. Explicit arithmetic subset geometry, maximal time horizon, timing precision, unknown support, source/target norm conversion, and infinite-source stability remain independent costs even though degree and sample count happen to share linear order.

Analytic Frontier gives a different aperture law. ANF-183 shows that endpoint memory can remain invisible to every physical twist window `o(X/a_X)` while a rephased three-point curvature at separation `Theta(X/a_X)` must detect the sector-aligned matched packet. The remaining difficulty is source access with absolute `o(a_X)` error at that aperture, not another source-blind smoothness statistic.

Xi Flow gives the converse calibration: once a consecutive packet window is known, XF-186 provides an `O(m)` holomorphic measurement bank uniformly bi-Lipschitz on a constant nonlinear cell for `m<=N`. Local inverse conditioning is not the bottleneck there; support discovery, global assembly, source-side access, and transport to a zero-sensitive quantity are.

## Which source cancellation theorem crosses the exact selected-family budget?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md) compares two Möbius-facing frontiers without identifying their operators.

Farey discrepancy now gives a stronger positive calibration. FD-062 uses all-short-interval Möbius cancellation to push a growing packet below the deterministic host scale. FD-063 removes the growing CRT host-selection cost altogether: the same source transfer and a trivial four-adic nonsquarefree density give a packet lower bound **uniformly across an entire below-critical host corridor**. The analytic budget and `vartheta>0.55` range do not improve, but host entropy is now available for averaging or selecting a horizon by its denominator.

Möbius cancellation gives the complementary amplitude calibration. MC-232--MC-233 reduce the hard first-shell coordinates to ordinary source-selected reduced-residue Möbius progression sums with fixed-power deficit `1/2-2alpha-beta` below `2alpha+beta=1/2`. MC-234 shows that the 2026 Linnik-scale sign theorem already puts both Möbius signs in every strict hard component, and CRT amplification gives polynomially many representatives of each sign. Yet those representatives are a vanishing fraction of the square-free support. The missing theorem is **near-balance at the exact deficit**, not qualitative parity breaking.

The reusable question is therefore not whether a representation looks simpler or whether both signs occur, but whether an available source theorem matches the exact selected family, quantifiers, and amplitude budget consumed downstream.

## Which source-specific inequality prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia agree on a concrete localization boundary without asserting the same operator. PL-280--PL-281 prove that the canonical support-localized negative Morse index is monotone and exactly exhausts the global compact-support negative index. A finite negative sector cannot hide beyond every finite aperture.

WI-247--WI-251 attack the complementary first-crossing witness. Under RH failure the odd sector has an attained first zero mode at finite radius; layer-cake averaging removes smooth-collar cost and compresses the nested localization family to one signed radial von-Mangoldt flux inequality with a finite mean-side budget and a strictly positive integrated interior gap term.

WI-252 closes the generic positive-ground-state shortcut. Odd reflection of the first prime atom produces a positive Hankel cross interaction on the half interval; once `a>log2/2`, the odd localized form fails the first Beurling--Deny criterion and its semigroup is not positivity preserving. Certified positivity through radius `0.8` places every hypothetical first odd crossing inside that obstructed regime. Any constant-phase/nodal control must therefore come from zeta-specific source structure rather than Perron--Frobenius/Krein--Rutman theory.

WP-266 supplies a parallel analytic-category warning from the positivity branch: source-Hamiltonian centering can make the finite part regulator-covariant and index-one, yet canonical transport to the critical prime scale becomes globally nonclosable. In both lines, arithmetic activation invalidates a generic positivity mechanism that looked available before the source was inserted. A completed-Weil argument must obtain its sign/coercivity from the actual source without leaving the correct global analytic category.

## Does the complete flute response preserve the weighted source budget?

PF-297 shows that complete Robin factors preserve positive `q`-mass, and PF-298 shows that the tight-pant scalar constant channel has only `O(s_n)` relative killing. PF-299--PF-300 identify the exact accumulation law of the prime seam geometry. With `t_n=F(p_n)`, the seam weights are an intrinsic Riemann quadrature, so for decreasing `phi`, `sum s_n phi(t_n)` converges exactly when `int phi(t)dt` does.

In particular

\[
s_n(\log p_n)^{-\beta}
\]

is nonsummable for `beta<=1` and summable for `beta>1`. At the critical law `epsilon_n\sim a s_n/(1+F(p_n))`, the isolated scalar transmission decays like `(\log p_n)^{-a+o(1)}`. The next scalar theorem should therefore identify the slowly varying factor in the actual pant killing, not merely improve `O(s_n)` to an unspecified `o(s_n)`.

This still does not settle the physical mixed response. The scalar compression is not known to be invariant under the complete `P/H` Schur map; nonconstant-mode damping, support loss, source-adapted forcing/cancellation, physical-high remainders, and the global heavy-range singular-value problem remain separate gates.