# Global Mathia research lines

This file holds genuinely cross-branch mathematical questions suggested by the durable Mathia intuitions. It is not a summary of the local minds, a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among program-level mechanisms.

## Move the strongest matched control all the way to the actual source-conditioned category without subtracting the target arithmetic

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Analytic Frontier now closes the entire equal-height five-point diagonal and an explicit unequal-height stability tube; the scalar frontier is genuine zero-freeness of the fixed Montgomery--Taylor defect on the remaining compact unequal-height set, not another notch parameter search. Visual Exploration sharpens the statistical analogue: CMI/CA-type scalar magnitude can quotient residual orientation, and orientation is meaningful only in one predeclared common Fisher gauge.

Xi Flow supplies the strongest current source/control separation. A critical `q~log^2 T` memory wave survives fixed heat time while passing the counting constraints consumed by the earlier argument, but the endpoint explicit formula excludes that exact wave in a prime-free low-frequency band. The decisive move is therefore to carry the strongest matched control to the **same source-conditioned observable and scale** used by the theorem, then prove that the source-only residual survives the destination transport without being subtracted or averaged away.

## Separate completed criterion encoding, recurrence, additive correlation, analytic transport, and arithmetic zero selection

**Linked intuitions:** `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-005-topology-is-part-of-the-arithmetic-claim`.

Prime Lattice shows that stronger multiplicative phase data can identify source structure without selecting zeros. PL-169 identifies a genuinely missing relation — additive shifted correlation — but PL-172--PL-175 show that canonical Hilbert--Schmidt regularization, uniform shift averaging, and theorem-accessible logarithmic low-order observables delete or universalize that channel. Merely adding the relation is not enough; its source-specific content must survive the analytic/limiting operation.

The live theorem must therefore distinguish at least four gates: the relation is actually absent from the old quotient; the source selects the relevant coupling/weights; the analytic carrier preserves it through continuation or limiting topology; and only then does a sign/coercivity/zero-selection theorem use it. An uncontrolled even fourth-order affine sector, for example, is a missing theorem input, not yet a mechanism.

## Match infinite assembly to a saturated witness profile, source-fixed metric, operator ideal, and canonical limiting topology

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-005-topology-is-part-of-the-arithmetic-claim`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Arithmetic Fidelity now makes the abstract composition repair exact. AF-134 gives the minimal backward saturation of downstream witness families required for a fixed linear chain; AF-135--AF-136 show that target-relative Gram fidelity is intrinsically projection/principal-angle geometry under full generator gauge, while raw source spectral scales require an independently fixed coefficient metric.

Prime Circle and Prime Flute provide complementary operator boundaries. PC-178 shows that the raw prime-axis resultant form is nonclosable and that primewise Abel regularization scalarizes in strong resolvent topology; a fixed-category escape must couple prime directions before closure. PF-169 shows that all canonical short central squared-resolvent blocks are already trace-summable, so the remaining prime/clone spectral burden is uncut body/collar transmission and head--tail assembly rather than local short-core multiplicity. A positive theorem must specify the witness saturation, metric/gauge, domain/ideal, and global assembly topology from the source.

## Find a source-fixed signed residual with genuine information deficit, beyond Dirichlet factorization gauge and approximate recovery tautology

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Möbius Cancellation now separates two necessary conditions. MC-082 proves that unsigned local divisor-density information can be blind to Liouville parity, so a signed variable is genuinely missing. MC-083--MC-085 then show that restoring sign by constant-weight parity contrast, the exact source sawtooth annulus, or a low-frequency truncation resolved finely enough to control the generic omitted tail is still Mertens-equivalent.

A live source-fixed residual must therefore be **signed and under-resolved**: it must preserve a cancellation mechanism that the control quotient lacks while discarding enough target information that its estimate is not invertible back to `M(X)` at the desired scale. Source origin, parity sensitivity, and being a proper truncation are not sufficient by themselves.

## Cross the actual source-level singularity threshold with a source-funded scale, or leave support-one/inertia regularization universality

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

Weil Inertia now closes both smooth and naive singular repairs around confluence. WI-161--WI-163 rule out broad sublinear support-edge scalar concentration using actual zeta source estimates. WI-140--WI-142 show every preassigned continuous spectral detector can lose mass as an off-line pair confluences. WI-164 shows Schur normalization cancels the collapsing horizontal scale rather than charging it, and WI-167 shows an autonomous quantized flag step overshoots the entire vanishing source deficit.

Any remaining singular/coercive route must therefore have an independently evaluated source scale or interaction that is absent on the isolated confluence control and nontrivial only because new zeta information funds it. Discontinuity or inverse normalization alone is not evidence of coercivity.

## Couple provenance before positive completion, algebraic incidence, local curvature, refinement closure, or statistical averaging classicalizes it

**Linked intuitions:** `MI-001-preserve-structure-before-compressing`, `MI-002-the-surviving-arithmetic-variable-is-relational`, `MI-003-arithmetic-significance-begins-after-universal-control-subtraction`, `MI-004-critical-axis-is-not-zero-selection`, and `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`.

Weil Positivity now pushes the upstream-coupling gate further. WP-158--WP-160 show that fixed torus characters primary-split, the first additive torsion relation collapses to fixed sixth-root geometry, and toric Manin--Mumford forces every scalable fixed finite-arity algebraic torsion incidence back into finitely many torsion cosets. WP-161 escapes with a real radial parameter and recovers `Lambda(n)` exactly at the boundary, but the first positive local differential response is `J_2(n)/12` on every shell and loses prime-power support.

Prime Circle independently shows that primewise closure of the raw unbounded resultant fails, Prime Lattice shows that logarithmic affine averaging universalizes low-order shifted parity, and Prime Flute shows that bounded/local tail spectralization transfers to the composite clone. A live cross-line mechanism must therefore introduce a **source-forced nonseparable relation before the operation that completes, closes, differentiates, averages, or spectralizes it**, and prove that the selector survives that operation in the required topology.

## Transport the Xi endpoint source selector through the mesoscopic heat-flow carrier

**Linked intuitions:** `MI-005-topology-is-part-of-the-arithmetic-claim`, `MI-006-classify-the-boundary-model-before-calling-it-arithmetic`, and `MI-007-survival-requires-quantitative-non-escape-not-just-nonzero-structure`.

XF-047 supplies a source-count-compatible memory-scale matched control that survives universal fixed-time Cauchy smoothing, closing the hope that the previously used counting information alone forces the flux threshold. XF-048 then finds the missing Xi-specific input: the endpoint explicit formula has a fixed prime-free Fourier band in which the critical memory wave has a nonzero response while the actual endpoint zero measure has `o(1)` response.

XF-049 gives the exact finite transport architecture: positive-frequency heat-zero evolution is Volterra-triangular and its flat-density linearization reproduces the memory slow-mode rate. The highest-leverage Xi theorem is now to preserve this one-sided prime-free coefficient through infinite renormalization and the `Theta(log^3 T)` spatial taper with `o(1)` error over fixed heat time. If that succeeds, the endpoint source distinction can enter the already-developed Cauchy-rigidity/coercivity machinery; if it fails, the failure should identify the precise localized source term capable of replenishing the memory band.
