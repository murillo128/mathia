# Global mathematical questions

## Which source identity keeps a critical mixed-prime geometry canonical in a mathematically admissible positive category?

[MI-009](intuition/MI-009-local-correctness-does-not-determine-global-coherence.md) now separates three gates. WP-267 shows that ordinary linear-relation closure destroys the critical common-vacuum coherence. WP-268 shows that the amplitude row is nevertheless continuous on the source-generated rapid-energy rigging

\[
\Phi_{\exp}=\bigcap_{a>0}\operatorname{Dom}e^{aH}.
\]

WP-269 adds a sharper spectral-category obstruction. The exact positive critical Mangoldt measure is neither tempered nor infra-exponentially tempered; its exponential damping moment is finite exactly for `a>1/2`. Therefore a stationary covariance with the unmodified source spectrum cannot be a positive-definite distribution or Fourier hyperfunction.

The remaining theorem must either derive a source-native finite--archimedean boundary correspondence in a larger exponential-growth category and recover the Weil orientation there, or introduce a source-forced cancellation/quotient before stationary positivity is read so that the spectral growth changes. Category admissibility, mixed-prime coherence, and completed Weil orientation are separate requirements.

## Which information survives the exact destination readout as breadth and complexity grow?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md) and [MI-012](intuition/MI-012-information-content-and-destination-sensitivity-are-separate-resources.md) distinguish exact source information from what the downstream functional, quotient, or cumulative continuation can actually use.

Nyman--Beurling now has a decisive local/global matched control. NB-068 constructs sources with identical finite visible/defect flags, finite bandwidth, and identically zero one-cell memory maps, yet different stable-tail spaces. The hidden boundary mode is invisible to every local `Gamma_R` but is detected exactly by the finite normalized Schur certificates of NB-067. The live theorem is genuinely global continuation control, not refinement of one-step memory.

Robin Extremal supplies the complementary pipeline distinction. RE-068 conserves every additive endpoint prime statistic and RE-069 makes direct prime/event timing summable in the Robin workload, but RE-070 shows that tied selector geometry still survives before that compression: the selector interval expands while its exact Chebyshev mass is fixed, forcing adaptive prime underdensity. A failed readout channel does not imply all source geometry is gone.

Visual Exploration makes frequency crowding quantitative. VIS-184 proves that every predeclared `o(log y)` additive-gap band has vanishing normalized coefficient mass for the full prime prefix with fixed `alpha<1/2`. The logarithmic gap scale is merely the first rate not killed by this bound, not evidence of survival.

Arithmetic Fidelity gives a genuinely zero-sensitive breadth effect. AF-291 shows that raw eta truncations converge uniformly on fixed zero-free contours while their normalized coefficient-space zero-count radius still decays to zero at an explicit power/logarithmic rate. More source breadth can improve analytic approximation and simultaneously worsen robustness in the upstream metric.

Across these lines, exact local richness is not the same as useful target sensitivity. Before treating a surviving coordinate, band, or approximation as evidence, one must price the exact continuation norm, kernel, quotient, equation, or discriminant seen by the destination and track how that price changes with breadth.

## Which observation family reaches the required source aperture at the right target-conditioned cost?

[MI-008](intuition/MI-008-exact-recovery-is-not-stable-transport.md) now contains two explicit normalized middle stages rather than generic conditioning language.

AF-282--AF-290 separate mixed acquisition, common-clock gauge, coefficient-orbit stability, and finite zero-count discriminant distance. AF-291 shows that the natural eta family has a collapsing normalized target margin despite uniformly correct analytic boundary behavior. Any acquisition theorem intended to preserve that zero count must tighten its relative error with breadth or change representation/metric.

Prime Flute supplies the complementary operator factorization. PF-305 writes every genuinely scalar-mediated mixed response as

\[
R_NG_NF_N
=(R_ND_N^{-1/2})\,S_N\,(D_N^{-1/2}F_N),
\qquad
0<S_N\le I,
\quad\|S_N\|=1.
\]

Thus the killing-normalized scalar Green reservoir is neutral for symmetric operator ideals: it neither destroys decay already present at a conversion vertex nor creates the missing compactness itself. The live burden is to control the physical entrance/reassembly maps in the correct energy spaces, or prove that nonconstant modes prevent this scalar factorization.

Analytic Frontier and Xi Flow retain the complementary aperture picture: endpoint memory can be invisible below a source-dependent physical scale, while a known consecutive source window can be encoded stably with linear sample complexity. Their bottleneck remains source access/support discovery and transport to the final target.

## Which source cancellation theorem crosses the exact normalization, recurrence geometry, signed metric, and blind modes?

[MI-010](intuition/MI-010-source-cancellation-budgets-are-family-specific.md) compares the current Farey and Möbius frontiers without identifying their operators.

Farey discrepancy now has three distinct controls. FD-066 gives the coherent-packet threshold `ell>2(1-Theta)`. FD-068 says very low normalized nonsquarefree occupation cannot dominate logarithmic scale for q-subcovariant heads. FD-069 adds an arbitrary-horizon pointwise bridge: a source spike of height `A_X` forces a large nonsquarefree numerator at every sufficiently late admissible horizon once `T` reaches order `X^2/A_X`. For a frontier spike this begins at `T=X^{2-Theta+o(1)}`. The remaining obstruction is no longer absence of pointwise capture but dilution by the same-horizon denominator.

Möbius cancellation has narrowed its blind sector again. MC-239 reduces blindness to cross-prime common-quotient relations; MC-240 rules out every nonprincipal blind character supported on at most six shell primes. In the quadratic sector the Legendre matrix has spark at least seven. The surviving algebraic obstruction is therefore high-support global compatibility, followed still by the fixed-power Fourier/signed-amplitude gate.

The common lesson is that favorable local theorems count only after their quantifiers and gain survive the exact family geometry. Pointwise capture, logarithmic density, source coherence, low-support annihilator exclusion, full subgroup generation, Fourier decay, and signed amplitude are different currencies.

## Which source-specific inequality prevents the first completed-Weil crossing?

Prime Lattice and Weil Inertia now sharpen opposite sides of the first-crossing boundary.

PL-284 strengthens the finite-window positive side. Conditional on a recent computer-assisted source, `A_0.8` has a simple even ground state and an explicit positive spectral gap. This removes endpoint degeneracy as a local obstacle and supplies a clean perturbative base point, but nested-domain monotonicity does not propagate that gap or positivity to all larger apertures.

WI-253--WI-254 show that a hypothetical first crossing has an exact aperture profile rich enough to recover real autocorrelation. WI-255 then closes the first subleading small-aperture bootstrap: after the zero-mode equation is imposed, the complete `O(r)` coefficient becomes universal and firstness reduces only to an old variational trial bound. The first arithmetic correction is algebraically redundant with `Q_W(v)=0`.

The next theorem must therefore use information not already collapsed into fixed-window positivity or the scalar null equation: an aperture-transport inequality for the isolated positive ground branch, a genuinely higher-order/medium-radius first-crossing identity, or another source-specific relation coupling several prime thresholds. Merely improving a finite certificate or computing the first asymptotic correction attacks a closed resource.

Weil Positivity adds the global category gate: any completed-Weil argument must live in a source-admissible category that preserves critical coherence and is large enough for the positive spectral growth, while still forcing the missing finite--archimedean orientation.

## Which global continuation or pre-compression geometry survives locally perfect matching?

[MI-011](intuition/MI-011-exact-local-source-consistency-can-hide-global-continuation.md) now links two distinct method boundaries without identifying their operators.

NB-068 proves that cumulative global incompatibility can be assembled entirely through directions discarded at every one-cell quotient: zero local memory and finite bandwidth do not rule out a stable boundary mode. The finite Schur geometry sees exactly what the local flag forgets.

RE-070 proves the complementary phenomenon that a matched source can remain exactly neutral in additive labels and nearly invisible in the final linear timing readout while still forcing a geometric density defect before that compression. The conserved prime mass must occupy a longer adaptive selector interval.

The reusable question is not “does local matching preserve information?” but **which variable survives after all local quotients and destination compressions have been applied**. In Nyman the survivor is cumulative extension cost; in Robin it is selector-length versus conserved prime mass. Any new local statistic must be tested against those actual surviving global coordinates.

## Does the complete flute response preserve enough source mass once scalar return is normalized away?

Prime Flute remains at the conversion/reassembly frontier. PF-303--PF-304 identify the killing vector as the exact scalar Green currency; PF-305 shows that the entire scalar reservoir becomes a norm-one positive contraction after symmetric killing normalization and is neutral for symmetric ideals.

The unresolved theorem is therefore concentrated at the two physical interfaces: show that the true prime-dependent forcing enters through `D_N^{-1/2}F_N` with the required uniform/ideal control and that `R_ND_N^{-1/2}` reassembles it into the physical high norm, or identify the nonconstant-mode operator that prevents such a factorization. Scalar return depth by itself is no longer the missing compactness mechanism.
