# MI-017 — Thin channels matter only after pullback to the destination-visible source family

**Evidence level:** supported cross-line synthesis from current exact results including NB-147--NB-168, FD-147--FD-160, RE-145/RE-151--RE-158, PL-321--PL-323 and [MC-385](../../mobius_cancellation/findings/MC-385-fixed-power-exceptional-moduli-cannot-feed-rank-linear-source-frame.md)--[MC-386](../../mobius_cancellation/findings/MC-386-welch-energy-makes-source-exception-threshold-sharp.md). The mathematical objects and scales remain line-specific.

A recurring obstruction is quantitatively clearer: **small ambient population is not small destination influence**, but neither is syntactic sparsity automatically a surviving resource. A target may concentrate decisive mass on a thin shell, deep ancestry layer, signed lobe, exceptional height or structured source family that generic population controls underweight. The relevant questions are how much leverage the destination assigns to that set, whether it survives the natural closure/completion, and how an ambient exceptional set intersects the exact source image.

Robin Extremal exposes a moment hierarchy: matching terminal count or first-order source mass need not control the internally placed channel that carries destination leverage. Farey Discrepancy supplies a closure test: sparse divisor supports can regain full cut pressure after the natural divisor closure is taken. Nyman--Beurling adds a family-quantifier test: a phase rescue can survive on isolated target heights while failing on a positive-density translated family. These examples already show that thinness must be measured after the destination's own closure and quantifier are fixed.

MC-385 adds an exact source-incidence version. Endpoint rows are indexed by an additive source group, so exceptional difference modes induce bad-neighbor geometry on a Cayley frame. An ambient exceptional count says little unless it stays sparse after pullback to that structured difference image.

MC-386 sharpens the destination-visible budget and proves its abstract sharpness. For `H` normalized source rows in rank at most `R`, Welch energy forces off-diagonal squared correlation at least `H/R-1` per row on average. If `E` difference modes are exceptional and the rest have correlation at most `epsilon`, then

`H/R-1 <= E+(H-1-E)epsilon^2`.

The natural sufficient scales are therefore `E=o(H/R)` and `epsilon=o(R^(-1/2))`. Annihilator-subgroup Cayley frames realize the `H/R` exceptional scale up to the exact/constant-factor sharpness allowed by the parameters. Thus generic rank/PSD geometry cannot make the source-visible exceptional set substantially thinner; any improvement must exclude those near-extremizers using arithmetic structure.

The reusable diagnostic is destination-weighted, closure-aware, quantifier-aware and **source-pullback-aware at the correct energy scale**. Identify the exact set/mode/shell/depth/sign component the target consumes; compute its leverage; apply the natural closure; state the required family quantifier; pull any exceptional-population theorem back through the source map; and measure the surviving bad geometry in the norm actually consumed by the endpoint rather than an unnecessarily strong pointwise norm.

**Boundary.** The examples use unrelated measures, operators, graph depths and arithmetic families. No common exponent or density conclusion transfers between them. MC-386 is an abstract matched extremizer for the endpoint frame, not evidence that the actual Möbius source realizes annihilator geometry. The synthesis is the requirement to measure thinness in destination-visible source coordinates and destination-relevant energy.