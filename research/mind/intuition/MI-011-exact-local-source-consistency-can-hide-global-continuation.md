# MI-011 — Exact local source consistency can hide a prediction/feedthrough defect or a source-reset bill that survives arbitrary packets

**Evidence level:** supported by NB-066--NB-073 and RE-068--RE-076; no operator or estimate is transferred between the lines.

Nyman--Beurling and Robin Extremal expose complementary failures of “local matching implies global harmlessness,” but both now identify the extra global datum more sharply than a generic continuation or complexity warning.

In Nyman, NB-066--NB-071 make the stable-tail obstruction a global continuation problem and construct an exact-branching, locally indistinguishable, globally well-conditioned matched control with a nonzero stable tail. NB-072 classifies the stationary scalar mechanism: for `psi(V_m)=theta(V_m)g(V_m)`, the stable tail is the model space of the inner factor, while the outer factor controls synthesis/conditioning. The complete global Gram erases the inner factor.

NB-073 shows that the erased information is recovered by comparing two quantities rather than by strengthening the Gram. The future-prediction energy determined by Gram Schur complements is `Pi(psi)^2=|g(0)|^2`, while the causal first-cell feedthrough is `|psi(0)|^2`. Their ratio is `|theta(0)|^2`; equality is exactly the no-stable-tail condition. The actual Nyman problem therefore becomes a nonstationary prediction theorem: derive a canonical future-prediction scale from the descendant Gram and prove that it matches the genuine newest-cell innovation energy.

In Robin, RE-068--RE-073 show that endpoint matching and summable direct timing coexist with a span-free Chebyshev--Mertens source budget along matched runs. RE-075 identifies the reciprocal reset currency: net recovery of the matched Mertens descent can be paid only by **new first-layer support primes**, requiring `>> W^(1-b_1)w` supports and physical logarithmic-state span `>> W^(1-b_1)w log W`, regardless of intervening packet complexity.

RE-076 removes the corresponding standard-source reset ambiguity. The corrected Chebyshev deficit `Z-log rad(C)` is a strict Lyapunov coordinate along the whole sufficiently late selected fan: it increases in cells and across arbitrary exposed-state switches, with higher-layer event mass contributing positively. Thus inserting unmatched, deep-layer, or arbitrarily large packets cannot erase the accumulated corrected Chebyshev cost; raw Chebyshev error differs only by the current fringe atom.

The common discipline is now concrete: **after exact local matching, compare the global quantity predicted by the complete future geometry with the actual causal/source datum, and identify the exact currency capable of closing any gap.** Nyman's survivor is measured by prediction versus feedthrough. Robin's survivor is a monotone corrected source coordinate plus a reciprocal reset bill in new prime supports. Generic conditioning or packet complexity is irrelevant unless it changes those currencies.

**Boundary.** No Nyman prediction identity transfers to Robin prime errors, and no Robin Lyapunov/support-birth law transfers to Nyman continuation. The synthesis is staged source/destination accounting: locally perfect matching can leave a decisive global survivor, and the next theorem must compare it with the actual source capacity rather than add another generic local invariant.