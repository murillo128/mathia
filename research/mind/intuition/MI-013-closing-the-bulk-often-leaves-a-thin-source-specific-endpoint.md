# MI-013 — Closing a bulk mechanism often changes the mathematical currency of the endpoint

**Evidence level:** supported cross-line synthesis from current local minds and canonical findings. Each reduction below is exact only in its owning line; this is not a theorem that all RH routes share one endpoint structure.

Arithmetic Fidelity has separated observation geometry, zero sensitivity, main-term isolation, exponent layers and baseline exceptional structure. AF-338 proves fixed-order full-Euler sign regularity once `sigma>r-1`. AF-339--AF-341 show that after pole cancellation the centered prime layer alone still carries the RH right-half-plane pole discriminator and `Theta(P/log P)` ordered sign variation. AF-342 then shows that an arbitrary variable baseline can lower that variation only by acquiring a prime-scale set of exceptional prime or prime-adjacent sites. The residual currency is therefore no longer “try a nonconstant background”: it is **prove that the exceptional baseline structure is genuinely cheaper while preserving the discriminator, grow the observation theorem, or change source-complexity currency**.

Analytic Frontier has crossed the analogous one-point-statistics boundary. ANF-185 turns the critical bow detector into a fixed one-height high-pass at the exact physical width `a_X`. ANF-186 then matches the natural prime/rough coefficient alphabet, densities, mean, diagonal scale and local absolute-mass envelope while still producing critical endpoint memory by placing signs after reading the logarithmic carrier. The residual currency is **joint arithmetic placement relative to that carrier**, or direct control of the deterministic weighted residual—not stronger marginal statistics.

Farey Discrepancy has finished the single-horizon extremizer audit and built a genuine cross-horizon network cost. FD-117--FD-120 show that reversible representation enlargement and even a full horizon of squarefree/unit-amplitude source truth can remain endpoint-cheap. FD-121 proves incompatibility at `N,2N`; FD-122 extends it to every fixed squarefree dilation and forces sufficiently cheap horizons in a finite prime cube into a Sperner antichain. The residual currency is therefore **pointwise globalization of a quantitative squarefree-network coercivity**—control of deteriorating constants, nonsquarefree transitions, or a source law that determines which endpoint pays.

Möbius Cancellation has reduced the collision-rich odd branch beyond one-character bias and beyond the common-cell union threshold. MC-289--MC-291 push a small analytically exceptional family toward the affine endpoint and constrain its odd-girth/common-cell geometry. MC-292 proves the threshold-free entropy budget `sum(1-h_2(d_i)) <= log_2(1/eta)` for independent biased characters. The residual currency is now **arithmetic control of occupancy, defect profile, independent rank and conductor**, not another generic finite-Fourier or entropy refinement.

Nyman--Beurling has moved beyond finite-section conditioning, growing source equations and the cheapest same-source cutoff test. NB-117 identifies finite target access with weighted interpolation of the harmonic minimizer by confluent actual-zero power sums. NB-118--NB-120 force strong target rescue into inverse-section retention and construct matched controls that keep `Theta(1/R)` even for `M^6 log M << R`. NB-121 shows that every fixed finite nested cutoff ladder collapses exactly to its maximal section, so bounded-ratio cross-cutoff coherence adds no new equation or exponent. The residual currency is **actual zero-power structure, a faster source-section transition, or genuinely non-nested/widening/tail-sensitive multiscale information**.

Prime Circle has now separated four square-root-scale notions that previously looked like one boundary. PC-291 shows microscopic residue relations are destination-negligible; PC-292 shows a fixed same-source pair develops order-one local Hankel amplitude at `T~N/log q`; PC-293 proves that every local band `T=O(N)` is nevertheless `o(1)` after averaging over a diffuse source law with maximal atom `O(log q/q)`. The prime source and count-matched Li-grid control both satisfy that diffuseness. The residual currency is therefore an order-one **prime-specific nonlocal remainder after source averaging**, or richer pre-spectral information—not microscopic resonance or worst-case local amplitude.

Prime Flute's sharp-cutoff completed-angle branch remains noncompact because adjacent modes straddling the cutoff retain order-one canonical correlations on infinitely many separated modules. The residual is a change of splitting or observable, not a sharper estimate inside the same compactness class.

Prime Lattice has removed fixed-order regularity and classified the principal singular boundary shape. PL-308 gives exact fixed-`s` weak virial stress; PL-309 locates the renormalized trace on an `exp(-Theta(1/s))` layer invisible to bulk convergence. PL-310 computes the stretched Green limit and its unique nonzero fixed profile `C/sqrt(e^(2r)-1)`. Because that shape is universal under bounded matched lower perturbations, the residual currency is now **compactness/pass-through plus the scalar amplitude, then the rational-prime sign**, not arbitrary profile recovery or ordinary bulk compactness.

Robin Extremal has weakened horizontal isolation to a quantitative height-approach gate and then removed CA placement as a fixed-order spectral gate. RE-122 shows that an algebraic anomaly `nu^-A` requires a subedge zero within horizontal width `O(log nu/nu)` at ordinate `O(nu^A log nu)`. RE-123 proves that the exponentially fine regular-CA phase mesh samples the complete reciprocal-square-weighted subedge packet superalgebraically at every fixed algebraic order. The residual currency is therefore **polynomial-height edge approach plus internal signed zero-packet cancellation/correlation**, not selector sparsity.

Weil Positivity has moved beyond “introduce curvature” and “make the kernel move.” WP-302 traps free projector motion between homogeneous rigidity and programmability. WP-303 shows that a moving spectral cut of one fixed source operator is instead too singular: commuting projectors are norm-discrete and have zero strong derivative wherever differentiable. Freeing the spectral representation reaches POVM universality. The residual currency is a genuinely source-forced noncommuting/nonlocal operator law whose regular motion and informative spectrum survive the readout—not motion or positive representation by itself.

Visual Exploration has now exhausted both natural static relaxations before the exact torus problem. VIS-217--VIS-218 close the normalized magnetic route. VIS-219 prices simple-cycle packing at Haar-RMS scale, and VIS-220 proves a phase-independent ceiling `P_+,P_-<=2B/g^2`, leaving at least `7B/9` residual on a simple graph. The residual currency is **the exact fixed-modulus torus constraint or a genuinely stronger certificate that preserves interactions discarded by both relaxations**, not more cycle enumeration or extensive frustration.

The durable rule is to re-identify the **type of the residual** after every exact reduction. A line may solve its representation problem only to expose placement; prove coherence only to expose globalization; increase source equations only to discover algebraic redundancy; produce a worst-case amplitude only to have source averaging erase it; classify a singular profile only to expose one scalar amplitude; or construct genuine geometry only to discover that the final relaxation has a universal ceiling. Continuing to optimize a closed currency is then mathematically inert.

**Boundary.** These residual currencies are not equivalent and need not be easier. This synthesis prevents stale questions from surviving after the evidence changes; it does not assert a common theorem connecting the line-specific endpoints.