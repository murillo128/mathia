# MI-021 — Representation enrichment only buys leverage where source-specific coupling actually survives

**Evidence level:** supported cross-line synthesis of current exact results including FD-200--FD-212, MC-382--MC-392, NB-217--NB-226, PC-351--PC-357, PL-361--PL-372, RE-216--RE-224, VIS-302--VIS-313, WI-350--WI-354 and WP-366--WP-370. Quantitative mechanisms remain line-specific.

Across Mathia, nominal enrichment keeps failing when added degrees of freedom do not alter the smallest source-sensitive object actually consumed downstream. Conversely, enrichment is useful when a new relation has an exact source preimage, survives the destination operation and improves a load-bearing bottleneck without repaying the gain elsewhere. The relevant question is not “how much representation is present?” but **what source-specific relation survives acquisition, closure, topology, normalization, inverse recovery, localization, naturality constraints, analytic identification and matched controls?**

Farey Discrepancy now separates fixed and nonautonomous enrichment exactly. Signed source-slab differences and low-resistance graph geometry improve the pre-norm representation, but every common sublogarithmic dyadic depth on a square-root scale window remains invertible at subpower cost. Fixed finite polynomial families have a complete spectral dichotomy: a common root in `sqrt(2)<|rho|<=2` gives physical supercritical blindness; no common annular root makes pointwise square-root control destination-complete. FD-212 then shows why scale variation is a genuinely different representation: the bounded-depth periodic schedule `q=(0,3,3)` has a supercritical Floquet multiplier even though every instantaneous filter is annulus-free and the fixed family has gcd `1`. The remaining price is cross-ray/all-integer source coherence, not filter degree or random benchmark cost.

Prime Circle supplies a two-stage covariance/similarity warning. Full-signature/free-Fock lifts remain related to reciprocal control by second-quantized parity. If a source-derived radial Gram metric is recomputed on the control, PC-356 gives exact metric covariance `G~=J^*GJ` and restores an isometry. If instead one freezes a common parity-breaking metric, PC-357 shows that the whitened source/control operators remain exactly similar through `S_N=direct_sum S^(tensor m)` with `S=H^(1/2)JH^(-1/2)`. Ordinary spectrum is unchanged and singular/resolvent separation can grow like the generic static budget `kappa_H(J)^N`. Breaking isometry is therefore not enough; the route needs a source-specific invariant not explained by transported covariance or fixed nonunitary similarity.

Prime Lattice separates ambient criticality, local source rigidity and global continuation. `ell^p` and Hardy geometry manufacture critical-looking walls. Complete multiplicativity plus pole neutrality and continuation gives a genuine zero-sensitive theorem interface. PL-370 forces local finite prime-harmonic distance to Liouville; PL-371 shows that this local neighbourhood alone still permits arbitrary real continuation obstructions below `1`. PL-372 then extracts the converse scale-by-scale rigidity from actual continuation: the positive logarithm plus Landau forces weighted Liouville closeness at every exponent inside the half-plane. For a finite prime alphabet the correction is holomorphic and zero-free, so the witness is divisor-equivalent to `zeta(2s)/zeta(s)`. A finite-alphabet enrichment therefore changes coefficients without changing the zero obstruction; only conditional infinite-alphabet structure or another genuinely global mechanism remains distinct.

Robin Extremal gives a representation-parameter version of the same audit. The universal separated-prediction floor is one, while the fixed positive-real single-center architecture carries a factor-two interior floor. Exact finite-remainder cancellation does not remove it on compact interior parameter sets. RE-223 excludes collapsing fixed corridors, and RE-224 shows directly from the exact normalized polynomial that bounded-corridor prediction cannot send the real center to infinity or the arc to `pi/2`. Any rate-two escape in that representation is now localized to `alpha->0` or the contraction boundary `r->1`. Parameter enrichment is not new information when it only moves inside a representation whose surviving singular limits are explicitly classified.

Visual Exploration supplies progressively stronger matched controls and then a constructive aggregate representation. Exact prime-power support, magnitudes and same-base multiplicative phase relations still cancel under randomized cross-prime phases; the actual deterministic time orbit has the same long-time quadratic mean as the multiplicative Haar control. Hard truncation plus global/local nearest-neighbour spacing produced quintic and then cubic finite-window horizons, while VIS-312 proved that individual isolation has no cutoff-free meaning on the dense full ratio spectrum. VIS-313 groups first by a fixed near/far ratio split and then by dyadic arithmetic height. Scale-local separation plus summable coefficient energy gives a cutoff-free near-linear sufficient horizon `V >> x log^3 x/(log log x)^2`. The remaining cost is logarithmic and quantifier-level, not a polynomial hard-tail artifact.

Nyman--Beurling gives a positive example and then prices it sharply. Retaining the actual zero spectrum and Bellotti's near-one density beats generic strip analyticity almost to Ford height. A proportional logarithmic source shell reaches the exact Ford denominator. NB-225 shows that every sublinear shell still admits a phase-aligned matched packet under the audited population information. NB-226 then shows that even imposing `O(m^vartheta)` cancellation on every consecutive vertical subpacket for any fixed `vartheta>0` does not rescue the route: the packet moves horizontally until Ford damping compensates the remaining phase bias. The missing coordinate is genuinely joint depth-phase information, not a generic random-phase exponent.

Weil Positivity gives a scale-and-naturality warning. Fixed scale suppresses the tail; homogeneous globalization removes incidence; the unique critical pure-power scale freezes `nY`; and exact diagonal Hecke intertwining forces that same ray. A naturality law can select a critical exponent while simultaneously erasing the mixed coordinate that motivated the representation.

Möbius Cancellation shows that source pullback can change not only the ambient extremizer but the available fourth-moment budget. The projected endpoint simplex excludes the abstract Welch near-extremizer, high dual distance forces macroscopic critical-scale correlations, and MC-391 shows that sparse exceptions require linearly growing normalized genuine quartic collision mass. MC-392 then prices that mass by the independent relation codimension `c=dim W^perp`: `E|g|^4 <= (2c+3)(E|g|^2)^2`, so `K H/R` exceptions at `o(R^-1/2)` scale force `c=Omega(R)`. Additive collision structure is not an independent knob; the source quotient must first carry enough independent relations.

Weil Inertia adds a localization-normalization warning. WI-354 shows that Wang's current short-interval pair-correlation theorem loses fixed Fourier support as the power interval shrinks, while its optimized simple-critical proportion also decreases and every fixed-power window still contains macroscopically many zeros. A more local representation is not automatically a stronger defect certificate when the arithmetic aperture narrows and the output remains normalized by local population.

Arithmetic Fidelity adds a provenance gate before any mesh or shrinking-target audit. AF-433 shows that the amplitude `a=c^2/(4pi^2)` in the current left-tail hierarchy is merely a bijective coordinate on the freely chosen probe path `x_n(c)=c/n`. Residual/null shrinking-target structure in `a` is therefore not source-discriminating until an independent source rule selects `c` or `a`. A mathematically rich transformed coordinate can still be non-informative because it was never acquired from the source in the first place.

These examples refine the audit into one sequence. Identify the minimal source carrier after exact algebraic generation. Ask whether acquisition creates a source-selected coordinate or only transports, tunes or reparameterizes old data. Apply the destination closure, norm, certificate or spectral quotient. State the ambient topology and test whether it already explains the critical scale. Price the physical inverse, nonautonomous coherence and every localization/resolution trade. Solve any covariance, similarity or naturality equation before crediting source-adapted structure. Pull theorem hypotheses, exceptional sets and sharp extremizers back to the exact source image. If continuation is required, test sparse source perturbations and determine whether the continuation itself collapses the apparent source freedom. If finite-time averaging is required, aggregate dense spectrum in a quantity that survives the infinite tail. If phase is invoked, couple it to the horizontal damping actually used downstream. Finally run the strongest matched control at the same pointwise, finite-height or individual-defect quantifier actually consumed downstream.

**Boundary.** This is not a theorem transferring Farey switched-system bounds, Möbius codimension inequalities, Nyman depth-phase controls, Prime-Circle similarity bounds, Robin prediction geometry, zeta-zero density, Wang mean values or Whittaker structure between lines. Each quantitative statement remains line-specific. The cross-line content is the falsifiable audit: enrichment deserves credit only when the newly acquired or retained structure has genuine source provenance, survives exact destination operations, has controlled source/inverse/coherence/localization cost, is not an ambient norm, automorphism, covariance, similarity, naturality, continuation, scale, certificate or averaging artifact, excludes the relevant matched extremizers, and reaches the target quantifier without silently paying the gain elsewhere.
