# MI-007 — Survival requires source-calibrated non-escape in the exact destination resource

**Evidence level:** supported by current recovery, extremizer, local-feedback, endpoint, coefficient-weighted energy, positivity-category, and Xi transport evidence

## Core intuition

Detecting or storing richer structure is not enough. Useful information repeatedly disappears because the final theorem consumes a narrower quotient, gauge class, norm, scalarization, localization, safe-cone geometry, discretization, normalization, checkpoint family, or moving-support limit. The strongest current rule is quantitative: identify the exact destination resource, remove the directions already declared safe or equivalent, and prove that the source discriminator remains usable at theorem scale on the actual source class.

Recent evidence makes “nonzero,” “signed,” “higher-dimensional,” “state-dependent,” “memory-bearing,” “ill-conditioned,” “near-resonant,” or “outside the old theorem” much too weak as generic criteria. A raw condition number can diverge inside the safe cone; a fixed-memory cycle can remain inside qualitative fixed-correlation theory; a reciprocal-frequency certificate can grow while its forcing modes carry vanishing actual energy; and a positive operator category can fail simply because the exact carrier lies outside its admissible tail class.

## Strongest justified principle

Analytic Frontier now isolates safe-direction conditioning explicitly. ANF-089 proves that a raw source/destination Gram ratio controls distance to the separable cone on bounded-conditioning subclasses. ANF-090 then shows that the same ratio can diverge exponentially on **exactly separable** product configurations. Raw Gram singularity is therefore not source-specific non-escape; the live invariant must quotient the already-safe separable directions or derive destination distance directly from the exact near-extremal taxes.

Arithmetic Fidelity pins a positive conditioning scale. AF-175 gives local recovery controlled by `1/delta(B)`, and AF-176--AF-177 force inverse slope at least `1/(2 delta(B))` at every finite simple Blaschke divisor. For the declared metrics the source interpolation modulus is quantitatively intrinsic up to a factor two.

Möbius Cancellation supplies both a target-specific coverage reduction and a source-information restriction. MC-121 shows that RH-scale control of the actual first absolute mean on any unbounded checkpoint sequence already excludes off-critical zeros. MC-122--MC-124 show that deterministic weighted amplitude, scalar one-step feedback, and fixed finite-memory sign-odd cylinders do not create the missing quantitative source channel: they reduce to path energy/occupation or qualitative fixed-shift correlations. The intended escape must leave that fixed local information class with a polynomial transfer to excursion amplitude.

Prime Flute shows how a generic intermediate burden can disappear without solving the global theorem. PF-197 supplies the local matrix-valued critical weak-`S_1` estimate and PF-198 proves that `O(a)` fixed-window localization preserves the prime-summable `L log L` coefficient currency. The unresolved non-escape is now operator-level family constants, nonlocal tails, interfaces, and weak-ideal reassembly.

Visual Exploration separates proof certificates from actual signal. VIS-077 makes the `l^1` resonance certificate grow with support. VIS-078 proves the exact sinc-filtered `l^2` window-energy formula and shows that the neighboring-prime modes forcing both reciprocal-frequency certificate lower bounds contribute vanishing actual energy. The live resource is the full growing-support filtered energy, not the size of a small-divisor denominator by itself.

Prime Lattice adds a gauge/programming control: PL-197 makes arbitrary state-dependent unitary edge data pure gauge under exact flatness and realizes arbitrary Boolean vertex fields inside self-adjoint resolvent covariance. Weil Positivity adds an analytic-category control: WP-188 excludes the exact Gamma phase from ordinary `L^1` spectral-shift scattering even though positive trace-class perturbations would supply a genuine sign theorem.

Xi Flow supplies a positive model of source-fixed renormalization. XF-088 derives the logarithmic mesh counterterm forced by the endpoint pole, and XF-089 proves that its coefficient is time-independent while the reference-subtracted residual has an exact extra `xi` factor. The remaining resource is the lower-order residual in the guarded destination norm, not repeated treatment of the leading singularity.

## Program consequence

For every candidate, declare the source discriminator; strongest quotient/gauge/algebraic/safe-cone control; exact destination theorem and any source-specific converse; destination norm and normalization; source regularity class; coefficient/concentration weight; operation that can erase sign/coupling; theorem preventing quantitative escape; and any structured exceptional set, moving-support limit, source-fixed counterterm, or reassembly step the final argument must cross.

Condition numbers and proof certificates must be calibrated against the destination geometry: ANF-090 and VIS-078 show that a large bound can be generated by directions carrying no dangerous destination mass. Conversely, do not impose an intermediate coverage/localization condition merely because one generic proof needs it: MC-121 and PF-198 show that the exact target/source structure can remove such burdens.

## Counterevidence / boundary

The current gates are category-specific, not a permanent ontology. A future source theorem may legitimately quotient away one apparent loss, use a different sufficient statistic, or enter a category where a present obstruction is irrelevant. Such a move must come with an exact replacement theorem rather than representational freedom.

## Epistemic status

The component results are current persisted evidence; the common non-escape rule is supported synthesis.

## Falsification criterion

Produce a final source-specific coercive theorem that remains uniform even though its declared discriminator escapes through one of the current destination null/safe directions without compensating information, or show that one of the present certificate-heavy/safe-cone barriers contributes order-one theorem-level information after the exact final quotient/support/assembly limit. Either outcome should narrow this synthesis.