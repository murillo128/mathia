# MI-008 — Exact recovery, source restriction, quotient geometry and defect-scale transport are different resources

**Evidence level:** supported by Arithmetic Fidelity through AF-300 and Prime Flute through PF-313; no universal target modulus, arithmetic source model, source-natural quotient, or theorem for the final physical prime-flute mixed response is claimed.

Arithmetic Fidelity separates acquisition from the final analytic target. AF-290 identifies the exact zero-count robustness radius as distance to the boundary-zero discriminant, while AF-295 shows that global complex amplitude/phase are already quotiented intrinsically by projective target geometry. AF-298 optimizes the full-rank anisotropic geometry generated by scalar boundary observations and leaves the sharp ambient `N^(-1/2)` leverage bill.

AF-299 closes the naive reaction “make the representation lower rank” on the unrestricted coefficient class: the boundary-evaluation hyperplanes have trivial common kernel, so every nontrivial additive complex-linear quotient fills with failure representatives and every rank-deficient PSD coefficient metric has zero failure distance.

AF-300 now distinguishes **actual source restriction** from quotient compression. If admissible coefficients are independently known to lie in a complex-affine family `a+U` of intrinsic dimension `r`, the exact source-relative distance uses only the restricted evaluations and the optimal leverage bill really becomes `r^(-1/2)`. But the restricted family still has trivial common evaluation kernel on `U`; any further rank-`<r` complex-linear compression again collapses the failure margin. Source restriction buys exactly the directions removed from the admissible perturbation model and no hidden linear null directions beyond them.

Prime Flute supplies the complementary source-interface picture. PF-312 separates normalized angle from diagonal Green amplification. PF-313 identifies exactly when that amplification is invisible to the physical map: bounded `G_C^(1/2)J_C` is equivalent to `J_CJ_C*<=K^2F_C`, so source overlap with a near-unit singular direction of value `sigma` must vanish like `sqrt(1-sigma^2)`. The destination does not care about a large inverse direction that the actual source misses at the correct defect scale.

The cross-line accounting principle is therefore sharper: **dimension reduction and inverse-direction harmlessness are claims about admissible source fibers, not about coordinate count.** AF-300 says a smaller source model helps only by its proved intrinsic dimension, while every remaining complex source direction stays target-relevant. PF-313 says a large inverse direction is harmless only if the physical source is quantitatively absent at the exact defect scale. In both cases nominal compression is irrelevant until source geometry is proved.

The next Arithmetic-Fidelity escape is categorically non-complex-linear: real/positive/discrete/multiplicative/nonlinear admissibility, a genuine discriminator-saturated symmetry, or a richer observable that changes the failure geometry. That is a source theorem, not another matrix weighting choice.

**Boundary.** The AF and PF operators do not transfer. AF-300 classifies complex-affine source restriction for a finite coefficient-space failure arrangement; PF-313 concerns source maps into a positive Schur complement. Their synthesis is a proof obligation: establish the exact admissible source geometry or defect-scale invisibility before using compression or large inverse geometry as benign.
