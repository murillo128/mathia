# MI-012 — Information content and destination sensitivity are separate mathematical resources

**Evidence level:** supported by AF-290--AF-305, PC-272--PC-275, RE-068--RE-085, WI-254--WI-265, VIS-184--VIS-197, PL-284--PL-289, PF-303--PF-316, MC-244--MC-254, NB-074--NB-082, FD-077--FD-083, and WP-270--WP-276; conditional/computational gates retain their stated evidence boundaries and the operators/targets remain distinct.

Several lines now show that a rich intermediate representation can fail only after an admissible-source restriction, quotient, topology, observation family, continuation map, source/target interface, target-conditioned metric, horizon geometry, scale-composition requirement, parity order, activation gate, representation-selection rule, regularity class, or spectral category is applied.

Arithmetic Fidelity gives the sharpest source-fiber geometry. AF-300 shows that a proved complex-affine source dimension changes the leverage bill but leaves every surviving direction target-visible. AF-301--AF-302 replace deleted directions by positive cones and normalized convex bodies. AF-303 and AF-304 close fixed coefficient and support caps separately; AF-305 now closes their entire strict-slack combination. For normalized nonnegative sources, `k alpha>1` still permits recurrent same-fiber Dirichlet zero/safe collisions, while exact saturation `k alpha=1` collapses the class to discrete uniform `k`-subsets. The relevant distinction is not “more constrained” but whether the source law actually changes the category of the reachable failure set.

Prime Circle separates retained information from normalized arithmetic discrimination. PC-272 proves that ordered finite-window `Delta_B` keeps source placement lost by the ratio quotient. PC-274 proves its raw law prime-blind over every bandwidth sequence, and PC-275 identifies the first centered fixed-bandwidth layer as the classical logarithmic-integral source bias. More information than a quotient is still useless if the destination normalization leaves only a matched-control or prior-art source profile.

Visual Exploration makes the observation-scale bill arithmetic. VIS-194--VIS-196 reduce subquadratic finite-start dephasing to the taper-weighted incidence of one-parameter same-sign affine prime-gap fibers. VIS-197 shows that their local singular-series inflation is only polylogarithmic outside an explicit finite conductor and isolates a finite degenerate three-prime-progression branch. The remaining difficulty is therefore not local admissibility but a coefficient-uniform sieve and weighted aggregate across fibers. Local density, tuple count and destination-weighted incidence are different resources.

Farey Discrepancy now separates abundance from composability. FD-082 proves that subpower first passage alone can be sparse; FD-083 uses the physical atom cap and annular energy to show that the actual constant-occupation set has full polynomial counting exponent, including `X^(1+epsilon-o(1))` witnesses in every fixed-power future window. Scarcity is no longer the barrier. What remains is whether those many horizons possess the multiplicative/transport geometry required to accumulate a power-scale gain.

Möbius Cancellation adds both order and block-selection gates. MC-252 shows that generic quadratic-residue algebra without localization can realize arbitrary finite binary codes. MC-253 shows that moving-shell localization forces fixed-order parity pseudorandomness but that a logarithmic-rank Walsh control has the same qualitative bounded-order balance. MC-254 then shows that even a genuine-prime Rédei matrix of maximal full rank can have a rank-one lower/shell cross-block. Fixed-order pseudorandomness and full class-group `4`-rank can both be large while the destination-visible bipartite interaction remains weak.

Nyman--Beurling makes the horizon-depth gate exact. NB-080--NB-081 show that shallow prediction volume can diverge on healthy outer zero-tail controls. NB-082 identifies the general stationary scalar quantity: the finite-prediction excess `a_(psi,L)`. Outerness only gives `a_(psi,L)->0`; bounded depth-`L` screening at prefix size `R` requires `R a_(psi,L)=O(1)`. Residual magnitude is therefore uninterpretable without the quantitative prediction rate at the depth actually used.

Robin Extremal adds a regularity-class gate to endpoint identifiability. RE-083--RE-084 show that endpoint selector data reconstruct the complete first-layer event set and timeline. RE-085 then proves that, on long stretched fans, the full log-mass-normalized mixed event measure converges in total variation to this endpoint-determined first layer because higher-layer event mass is `o(1)`. Every bounded fixed-order mass statistic is therefore asymptotically first-layer. The only remaining cross-layer channels are singular ones—adjacency, rare-event conditioning, chamber/state selection or other discontinuous effects that can stay order one despite vanishing higher-layer mass.

Weil Inertia separates source cardinality and abstract firstness from source-specific first-crossing rigidity. WI-264 screens every active finite-aperture prime-power lag with a full-span endpoint autocorrelation. WI-265 then makes that same screened vector an exact terminal zero mode of a positive rank-one-defect operator whose every proper centered and translated shorter-window compression remains strictly positive. Generic zero-modehood and first-crossing positivity therefore still do not force the needed autocorrelation mass. The surviving discriminator is the actual Suzuki relation between localization defect and the arithmetic source functional, or the exact arithmetic null equation.

Prime Lattice separates ambient regularity from state-specific quantitative usefulness. PL-289 upgrades the actual one-dimensional Weil eigenstate to a squared-log Fourier moment and an `O(log^-2)` translation modulus, yet its order-one constant is still too large for the tiny endpoint margin. Prime Flute makes the source/target interface exact: PF-315--PF-316 show that the residual source map cannot independently hide bad singular values because attenuation is paid by the direct angle.

Weil Positivity adds continuum provenance to representation selection and spectral compatibility. WP-274 makes inverse host construction tautological when the target is supplied first. WP-275 forbids a nonzero bounded exact modulation intertwiner from the pure-point arithmetic carrier to the nonatomic continuum. WP-276 further shows that finite-rank, trace-class, or trace-class-resolvent self-adjoint source surgery cannot manufacture the missing absolutely continuous sector from a wholly singular source. A viable architecture must either derive that continuum independently at zeroth order or leave the trace-class perturbative category before positivity is invoked.

The common diagnostic is concrete: **after locating source information, identify the exact admissible fibers, reachable body, quotient/bad set, normalization, observation resolution, coefficient mass, recurrence abundance, composition geometry, parity order, block projection, horizon depth, prediction rate, endpoint identifiability, regularity class, target kernel, zero-mode constraint, model-selection direction and spectral category that intervene before the theorem; then quantify what survives at that exact destination scale.** More coordinates, positivity, normalization, tighter caps, sparsity, preserved ordered information, many occupied scales, fixed-order pseudorandomness, large full-matrix rank, a large residual charge, many mixed events, many active source atoms, generic first-crossing positivity, or nonlocal positivity can all be irrelevant when the destination consumes a stronger or singular currency.

**Boundary.** None of the convex-reachability, near-resonance, incidence, occupation, character/code, Rédei-block, prediction, selector-assembly, event-mass, translation-tail, autocorrelation-screening, compression-firstness, Schur-angle, inverse-model or spectral-type theorems transfer between lines. The synthesis is separation and accounting of destination resources, not a shared operator or a new mathematical implication across targets.
