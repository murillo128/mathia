# MI-012 — Information content and destination sensitivity are separate mathematical resources

**Evidence level:** supported by AF-290--AF-304, PC-272--PC-275, RE-068--RE-084, WI-254--WI-264, VIS-184--VIS-196, PL-284--PL-289, PF-303--PF-316, MC-244--MC-253, NB-074--NB-081, FD-077--FD-082, and WP-270--WP-275; conditional/computational gates retain their stated evidence boundaries and the operators/targets remain distinct.

Several lines now show that a rich intermediate representation can fail only after an admissible-source restriction, quotient, topology, observation family, continuation map, source/target interface, target-conditioned metric, horizon geometry, scale-density requirement, parity order, activation gate, representation-selection rule, or spectral realization is applied.

Arithmetic Fidelity gives the sharpest source-fiber geometry. AF-300 shows that a proved complex-affine source dimension changes the leverage bill but leaves every surviving direction target-visible. AF-301--AF-302 replace deleted directions by positive cones and normalized convex bodies. AF-303 closes a fixed coefficient cap; AF-304 closes fixed support cardinality as a standalone repair. For one complex scalar, three unused support slots already recover the full convex hull, while two source terms suffice for recurrent same-fiber zero/safe collisions. The right invariant is the target-relative reachable body, not labels such as positive, normalized, capped or sparse.

Prime Circle separates retained information from normalized arithmetic discrimination. PC-272 proves that ordered finite-window `Delta_B` keeps source placement lost by the ratio quotient. PC-274 proves its **raw** law prime-blind over every bandwidth sequence because small bandwidths are source-equidistributed and large bandwidths collapse geometrically. PC-275 then identifies the first centered fixed-bandwidth layer as the pushforward of the classical logarithmic-integral source bias. More information than a quotient is still useless if the normalization/centering leaves only a matched-control or prior-art source profile.

Visual Exploration makes the observation-scale bill arithmetic. VIS-194--VIS-195 replace the worst prime-ratio gap by the coefficient-weighted spacing horizon and identify subquadratic dephasing with vanishing weighted bounded-determinant mass. VIS-196 removes opposite-sign collisions at only `O(y)` cost and reduces the dangerous same-sign branch to one-parameter affine prime-gap systems `dr-ep=+/-h`. The destination needs a weighted incidence theorem for the actual taper measure, not generic ratio separation or unweighted determinant counts.

Farey Discrepancy adds a scale-density gate. FD-081 reaches a good physical horizon at subpower first passage, but FD-082 proves that this can coexist with only `(log log X)^2` good horizons and divergent logarithmic horizon entropy. A fixed gain per hit may then accumulate to only `X^(-o(1))`. Recurrence frequency and power-saving accumulation are separate resources.

Möbius Cancellation adds an order gate. MC-252 shows that generic quadratic-residue algebra without localization can realize arbitrary finite binary codes. MC-253 shows that tight shell localization does force fixed-order parity pseudorandomness, yet a logarithmic-rank Walsh control has the same qualitative bounded-order balance. Pseudorandomness at every fixed order and global code generation are different resources; the missing theorem must grow the parity order or use a stronger shell-specific invariant.

Nyman--Beurling adds a horizon-depth/conditioning gate. NB-080 proves that a shallow fixed-ratio residual diverges on a healthy outer zero-tail source. NB-081 makes future screening exact and shows that the depth needed to remove this false positive depends sharply on outer conditioning: logarithmic depth for fixed invertible `rho<1`, but order-`R` depth at the noninvertible endpoint. Residual magnitude is meaningless without pricing the depth at which healthy continuation becomes visible.

Robin Extremal adds endpoint identifiability. RE-083 removes first-layer support mismatch and linear timing under the Robin kernel. RE-084 shows that the endpoint selector data reconstruct the entire first-layer timeline, which is asymptotically an ordinary-prime configuration shifted by one half. Even nonlinear first-layer-only timing statistics can therefore be information-rich yet not independent; the live obstruction is cross-layer/state-selection geometry.

Weil Inertia separates source cardinality from first-crossing rigidity. WI-264 uses the already-certified `lambda_0.8>0` to remove prime activation as a live gate, then proves that at any finite aperture all active prime-power lags can be screened simultaneously by a positive-definite full-span autocorrelation while the continuous source has favorable sign. Adding more finite source atoms cannot replace the exact null equation or quantitative firstness envelope.

Prime Lattice separates ambient regularity from state-specific quantitative usefulness. PL-289 upgrades the actual one-dimensional Weil eigenstate to a squared-log Fourier moment and an `O(log^-2)` translation modulus, yet its order-one constant is still too large for the tiny endpoint margin. Prime Flute makes the source/target interface exact: PF-315--PF-316 show that the residual source map cannot independently hide bad singular values because attenuation is paid by the direct angle. Weil Positivity adds causal representation and spectral type: WP-274 makes inverse host construction tautological when the target is supplied first, while WP-275 forbids a nonzero bounded exact modulation intertwiner from the pure-point arithmetic carrier to the nonatomic continuum.

The common diagnostic is concrete: **after locating source information, identify the exact admissible fibers, reachable body, quotient/bad set, normalization, observation resolution, coefficient mass, recurrence density, parity order, horizon depth, conditioning, endpoint identifiability, target kernel, zero-mode constraint, selection direction and spectral representation that intervene before the theorem; then quantify what survives at that exact scale.** More coordinates, positivity, normalization, caps, sparsity, preserved ordered information, subpower recurrence, fixed-order pseudorandomness, a large residual charge, many active source atoms, or nonlocal positivity can all be irrelevant when the destination consumes a stronger currency.

**Boundary.** None of the convex-reachability, near-resonance, incidence, recurrence-density, character/code, boundary-screening, selector-assembly, translation-tail, autocorrelation-screening, Schur-angle, inverse-model or spectral-type theorems transfer between lines. The synthesis is separation and accounting of destination resources, not a shared operator or a new mathematical implication across targets.