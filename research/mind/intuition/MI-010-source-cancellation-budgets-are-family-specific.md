# MI-010 — Source cancellation must be priced after exact observation geometry and exact source inversion are fixed

**Evidence level:** supported by Farey results through FD-074 and Möbius shell results through MC-245; no cancellation theorem or operator is transferred between the two families.

Farey discrepancy now contains a source-selected normalization mechanism with an arithmetic host constraint. FD-071 shows that arbitrary-later persistence of one source episode can be diluted by a later spike. FD-072 repairs that on a source-selected `sigma`-record by choosing a causal horizon whose complete denominator is confined to the record-controlled prefix. FD-073 propagates the protected point to an amplitude-length block using the exact one-Lipschitz law of the source.

FD-074 then uses the exact physical increment rather than only its unit bound. A strict `sigma`-record at `X` forces

`phi(rad X) > X|H(X)|[1-(1-1/X)^sigma] >= sigma|H(X)|`,

and once `|H(X)|>1` its sign equals `mu(rad X)=(-1)^omega(X)`. Along a false-RH supercritical record sequence, `rad X/X^sigma -> infinity`. The remaining Farey currency is therefore recurrence/spacing of amplitude-length protected blocks **subject to the multiplicative arithmetic of their record centers**, with the explicit `1/log` degeneration at the critical endpoint. Large-radical eligibility alone still does not control gaps.

Möbius cancellation has a different information bottleneck. MC-238--MC-243 reduce blindness to a binary kernel, force growing support, and show why minimum distance cannot determine kernel dimension. MC-244 then inverts the whole quadratic blind family to one affine Legendre-cell projector and shows that mode count cancels against source-fiber density, so every positive blind dimension has the same worst-case support power.

MC-245 removes the remaining smooth-weight disguise. The all-powers `Psi` factor is exactly the smooth convolution coordinate already inverted by `g_y=mu*s_y`; because smooth multipliers have trivial quotient charge, the complete blind contribution collapses to one **rough Möbius affine-cell sum**. The support gains only a logarithmic rough-density factor and the fixed-power boundary remains unchanged. Characterwise reciprocal `L`-function difficulty also survives roughing, while aggregate cross-character cancellation remains a logically distinct possible mechanism.

The cross-line lesson is now precise: **Farey progress comes from adapting the observation so normalization is causally subordinated to a source episode, then using the exact coefficient law to constrain which arithmetic hosts can carry recurrent protected blocks; Möbius progress comes from exact source inversion until the remaining signed statistic is exposed without auxiliary mode-count or smooth-weight proxies.** Both routes require the source-specific currency that survives the destination map, but that currency is different in the two constructions.

**Boundary.** FD-074 gives no global density theorem and constrains strict record centers rather than every point of a protected block; FD-073's critical endpoint gap still decays. MC-245 concerns the quadratic blind quotient and gives no signed rough-Möbius estimate. Their scales and operators remain distinct; the synthesis is about preserving the exact source currency consumed by the destination.