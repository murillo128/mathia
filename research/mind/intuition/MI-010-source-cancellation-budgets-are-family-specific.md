# MI-010 — Source cancellation must be priced after recurrence strength, shell occupancy and quotient invariants are fixed

**Evidence level:** supported by Farey results through FD-080 and Möbius shell results through MC-251; the concrete GRH/ERH witness instance remains conditional, and no cancellation theorem or operator is transferred between the two families.

Farey discrepancy has now passed four distinct abundance gates. FD-077 gives frontier-near source values on a set of full polynomial counting exponent; FD-078 removes every fixed-power loss under physical-energy weighting; FD-079 gives fixed positive logarithmic-mean annular occupation; FD-080 then shows that constant occupation is **power-syndetic** along the exact dilation successor. For every fixed `eta<c_q`, the first good descendant occurs after `o(log X)` steps and within multiplicative scale `X^(o(1))`.

Farey's residual problem is therefore smaller than sparse exceptional-scale localization in the old sense. Deep isolated dips can remain, but no dip can generate a bad desert spanning a fixed positive amount of logarithmic scale. The missing theorem must either show the downstream criterion is stable under this subpower enlargement, improve first-passage control, or obtain the actual horizon-level pointwise currency.

Möbius cancellation has an algebraic rather than recurrence bottleneck. MC-244--MC-250 reduce blindness to the exact quotient `Q=G/H` and couple support, torsion order, shell occupancy and primary multiplicity. MC-251 then packs the entire level-`ell^a` code family, replacing the Singleton deficit `D_y-1` by the q-ary Hamming-ball logarithm. At fixed low order this raises the generic deficit to `Omega(log y log log y)` unconditionally and `Omega(sqrt(y))` under the ERH support floor.

That gain is still sublinear relative to the compatible shell `Theta(y/log y)`. The dangerous object is no longer merely “large low-order rank,” but a near-full-dimensional arithmetic code that generic distance information cannot exclude. Closing it requires arithmetic dependence beyond minimum distance or direct signed cancellation of the exact rough Möbius coset.

The cross-line lesson is concrete: once generic abundance or generic coding theory has been pushed to its natural limit, the remaining theorem is dictated by the **destination currency**. Farey needs subpower-to-target transport or stronger first passage; Möbius needs simultaneous arithmetic structure or signed amplitude. Another generic “more cancellation” estimate is unlikely to address either residual.

**Boundary.** FD-080 is not eventual pointwise occupation and gives no bounded first-passage time. MC-251 does not force `Q=1` and leaves fixed-low-order dimension `n_d-o(n_d)` possible. The synthesis compares where cancellation is still missing, not the underlying operators.