# MI-010 — Source cancellation must be priced after exact observation geometry, abundance scale and source inversion are fixed

**Evidence level:** supported by Farey results through FD-077 and Möbius shell results through MC-248; the concrete GRH/ERH witness instance remains conditional, and no cancellation theorem or operator is transferred between the two families.

Farey discrepancy has moved from protected isolated episodes to a quantitative abundance statement. FD-072--FD-075 produce causal record protection, amplitude-length blocks and same-parity large-radical swarms. FD-076 makes frontier records power-dense.

FD-077 transfers Pintz's average absolute Mertens order through the inverse floor-harmonic identity. Frontier-near physical-source values now occur on a set of full polynomial counting exponent, not merely one point per fixed power window. A fixed nonsquarefree causal row turns a full-counting-exponent subfamily into horizons with nonsquarefree occupation and Schur projective defect above every prescribed fixed polynomial decay. The remaining missing currency is stronger than abundance: positive logarithmic/ordinary density, nonvanishing occupation, or an aggregation theorem that can use this near-maximal counting family without requiring strict records.

Möbius cancellation has the opposite shape. MC-244--MC-246 invert the complete blind family until the whole obstruction is one rough Möbius multiplicative coset `-H`; the exact algebraic charge is the quotient `G/H`. MC-247 gives the GRH/ERH `O(log^2 q)` support pressure.

MC-248 proves that the failure is structural across every fixed witness exponent `A>1`. A generic `O(log^A q)` least-witness theorem forces only `y^(1/A)/log y` active shell support, while abstract code controls with that minimum distance can still have redundancy `O(y^(1/A))=o(y/log y)`. Improving generic witness constants or any fixed superlinear log exponent cannot determine quotient triviality. Even linear-scale support would not by itself imply a zero-dimensional kernel.

The cross-line lesson is therefore more precise than “need better cancellation.” **Farey has abundant source amplitude but insufficient density/occupation strength; Möbius has exact source inversion but insufficient quotient control.** One line needs to upgrade how often/strongly a known physical signal survives normalization; the other needs either shell-specific simultaneous generation or signed cancellation after every blind mode has already been compressed to the exact coset source.

**Boundary.** FD-077 gives no positive density or fixed occupation lower bound. MC-248's symbolic `A` theorem is a scale classifier except where literature supplies a concrete witness bound, and no support estimate determines `[G:H]`. The operators, arithmetic families and normalization scales remain distinct; the synthesis is the discipline of identifying the exact final currency before asking for a cancellation theorem.
