# MI-010 — Source cancellation must be priced after recurrence strength, shell localization and quotient invariants are fixed

**Evidence level:** supported by Farey results through FD-081 and Möbius shell results through MC-252; the concrete GRH/ERH witness instance remains conditional, and no cancellation theorem or operator is transferred between the two families.

Farey discrepancy has now passed the generic abundance ladder all the way to physical horizons. FD-077 gives frontier-near source values on a set of full polynomial counting exponent; FD-078 removes every fixed-power loss under physical-energy weighting; FD-079 gives fixed positive logarithmic-mean annular occupation; FD-080 makes that annular occupation power-syndetic; and FD-081 transfers it exactly to pointwise horizons by positive energy-weighted averaging.

For every fixed `eta<c_q` and every `epsilon>0`, every sufficiently large `X` has some `H` with

`X<H<=X^(1+epsilon)` and `nu_(H,D)>eta`.

The first good physical horizon is therefore `X^(1+o(1))`. Farey's residual problem is no longer annular localization or annular-to-pointwise conversion. Deep subpower bad stretches may remain, so the missing theorem must show the downstream criterion is stable on this power-syndetic pointwise set, control those residual gaps, or upgrade to eventual occupation.

Möbius cancellation has an algebraic/localization bottleneck. MC-244--MC-251 reduce blindness to the quotient `Q=G/H`, couple support, torsion order and shell occupancy, and push generic code-family bounds to their natural sublinear frontier. MC-252 then tests the first obvious arithmetic refinement: quadratic reciprocity itself.

That refinement fails without localization. By CRT, reciprocity and Dirichlet, every finite binary matrix can be realized exactly as a Legendre matrix between lower row primes and sufficiently large separated column primes. Thus every low-codimension binary code already occurs inside genuine quadratic-residue arithmetic once the column primes are allowed to leave `(y,2y]`.

The Möbius residual is therefore not “find any arithmetic dependence beyond coding theory,” but **find dependence created by simultaneous shell localization or another source coupling absent from the separated-prime realization**, or bypass generation and control the signed rough Möbius coset directly.

The cross-line lesson is concrete: after generic recurrence or generic code pressure is exhausted, only the exact destination currency matters. Farey needs a theorem about the remaining subpower pointwise gaps; Möbius needs a theorem that genuinely consumes localized arithmetic geometry. Another generic abundance, distance or reciprocity estimate that ignores those residual constraints is unlikely to move either frontier.

**Boundary.** FD-081 is not eventual pointwise occupation and gives no bounded multiplicative return time. MC-252 does not realize arbitrary codes in the active shell, force `Q=1`, or control signed amplitude. The synthesis compares where generic cancellation tools stop, not the underlying operators.