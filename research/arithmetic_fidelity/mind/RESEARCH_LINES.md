# Arithmetic-fidelity research lines

This file holds the current mathematical questions suggested by the durable arithmetic-fidelity intuitions. It is not a roadmap, task queue, status page, or history.

## Treat coefficient-root neutrality as a closed provenance control, not as a surviving cancellation mechanism

**Linked intuition:** `MI-055-derivative-depth-is-a-second-left-tail-scaling-coordinate`.

AF-416--AF-432 isolate the full left-tail determinant hierarchy. Fixed derivative shifts close to a positive Cauchy limit, moving depth introduces `s_n/n`, the complete partition family detects the critical value `2`, and the effective critical coordinate is

`b_n(a)=a n^(s_n/n-2)`.

At square collision fibres, equality of exponential saddle rates is only the first gate: rectangle balance, complete-packet drift and the discrete derivative offset survive at successively finer scales. Above the critical transition, rational constant-density sources automatically hit the first `R_n/n` saddle window, whereas the packet-corrected `R_n/n^2` target is residual but metrically thin.

AF-433--AF-434 separate realization from transport. The reciprocal-order probes `x_n(c)=c/n` leave `c` externally free, although the source has intrinsic singularity shells `r_m=2pi m`; the current determinant then aggregates shell labels into zeta factors before packet labels form, so `c=r_m <=> a=m^2` is not transported shell identity.

AF-435 now closes one natural **source-selection** gate. For any analytic germ with finite radius `R(f)`, coefficient-root neutrality

`limsup |a_j c^j|^(1/j)=1`

selects exactly `c=R(f)` by Cauchy--Hadamard. For the Bernoulli source this gives `c=2pi`, hence `a=1`, before any packet calculation is inspected. That selector is source-native and dilation-covariant, but it does not rescue the determinant route: AF-426 already proves that complete adjacent-packet cancellation fails on the exact-square fibre `a=1`.

The live frontier is therefore no longer “find a favorable `c`” inside the root-neutral class. A continuation must change something substantive: justify a different source-side admissibility notion independently of downstream success, retain shell-resolved provenance in a representation that actually transports it, or use a target mechanism that does not require the adjacent-packet cancellation excluded at the selected scale. AF-435 does not prove that every conceivable canonical selector must equal the radius.

## Keep source selection, source transport and target shrinking scales as separate currencies

The reusable order is now explicit. First ask whether the source selects the asymptotic parameter rather than merely permitting it. Second ask whether the selected datum survives into the representation variable consumed downstream. Only then compare the admissible source mesh with the target aperture at the same asymptotic order.

The present branch supplies a useful negative control because those gates separate cleanly: coefficient-root neutrality selects `c=2pi`; the full-column determinant does not transport the shell index; and the selected amplitude lands on an exact square where the current adjacent-packet cancellation fails. Metric or Baire largeness of exceptional amplitudes, continuous optimization and matching numerical labels cannot replace those source-side obligations.