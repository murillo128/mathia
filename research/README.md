# Mathia Research State

Canonical evidence is synthesized through `48ca40501be6530e8aa891463a78a39488c8ca3a`. The latest clean independent adversarial checkpoint is `6913efe949a145f7ad7decc803558f6717df06e9`, which declares coverage through the reachable commit `80e0d028819a0fd9c0ca7b179de99f23e1e2b45d`. There are no open `.review.md` sidecars. `RE-089` remains withdrawn and absent; `RE-090` is the canonical corrected Robin same-depth result. The post-checkpoint findings `MC-261` and `WP-281` are used as current canonical evidence but are not described as adversarially cleared.

## Global picture

Portfolio recommendations remain **3 `continue` + 10 `narrow`**. `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

Weil Inertia has the strongest new source-specific bridge. `WI-271` improves the one-signed first-crossing analysis from a generic cut tariff to a direct arithmetic conclusion: if a hypothetical one-signed Suzuki null mode has an internal support gap of width at least the exact archimedean sign-change length `h_0`, the continuous source contributes with the wrong sign across that gap, so strict firstness forces positive von-Mangoldt cross-gap mass and hence a positive prime-power autocorrelation. Complete prime screening can therefore survive only if the support stays `h_0`-gap-connected. This does not settle the densely connected one-signed escape and does not transfer to sign-changing modes, where the `WI-269` PSD signed-Laplacian/nodal compensation remains the correct interface.

Farey, Nyman, and Robin each replace a broad residual family by a thinner source-specific carrier. `FD-089` compresses the positive-energy row-adaptive anchor packet of `FD-088` to only `H^{o(1)}` canonical prime-dilation predecessor states at scale `H^{1-o(1)}` and guarantees one predecessor preserving the full frontier energy exponent up to subpower loss. The unresolved currency is now fixed-strength overlap/occupation and recursive composition, not predecessor abundance or row-adaptive entropy. `NB-088` makes the dyadic terminal scalar channel exact: its arithmetic part is a three-term causal Blaschke-delay imbalance, while every fixed finite off-critical Blaschke divisor contributes only `O(R^{-1})` per sibling block and vanishing total squared distortion. Any scalar obstruction must therefore come from the collective nonuniform tail of the full Blaschke product, or the proof must leave the scalar sibling-average direction. `RE-091` uses sparse depth-`>=3` boundary hosts together with the prime-gap second moment to force one-sided first-layer boundary contact outside threshold measure `o(1)` whenever `Theta>469/600`; purely higher-layer mixed-depth selection is no longer the live carrier in that regime.

Arithmetic Fidelity closes another apparently stronger generic arithmetic route. `AF-311` upgrades the prime-log source constraint from integer-character rank to Baker algebraic-linear independence: a common-time prime phase point has algebraic-affine relation rank at most one. Yet a real-analytic exact-pentagon patch contains a dense residual set with no nontrivial algebraic-affine relation at all. Standard linear forms in logarithms therefore remain geometrically compatible with the first unresolved `k=5` zero manifold. The live problem is genuinely nonlinear nontorsion incidence of the actual one-parameter prime-log direction, not a stronger version of the same linear-relation argument.

Prime Flute also sharpens where its direct-channel difficulty lives. The strengthened `PF-242` shows that the raw hypercycle shear is exponentially localized in the exact Pöschl--Teller mode index and has cut-uniform trace leakage `O(beta)` and squared Hilbert--Schmidt leakage `O(beta^2)`, hence `O(P_n^{-2})` on the canonical tail. Combined with `PF-319`, this says the missing estimate is not coefficient-level mode mixing: it is the survival of that locality through corridor-to-seam metric conversion, DtN/Steklov normalization, Schur/resolvent operations, and the physical `Z_LH` corner. `T_H` remains the second independent endpoint channel.

Two newest post-checkpoint findings further narrow method categories without changing recommendations. `MC-261` identifies the Möbius generation exit `H=G` exactly as an all-class smooth-representative problem for the shell modulus `R`, with generator cutoff `y=(1+o(1))log R`; existing smooth-number and product-of-primes theorems operate at polynomial generator scale and do not reach this logarithmic endpoint. The alternative blind-pretender/coset-cancellation route remains separate. `WP-281` closes the natural finite-state compromise between local tangent extraction and global primitive averaging: even under RH, every fixed finite-dimensional stable logarithmic-scale linear response that preserves the Gamma constant leaves a nonconstant almost-periodic zero field, and a rational transfer function cannot annihilate infinitely many zero ordinates. Weil Positivity must therefore use an independently forced infinite-dimensional/nonrational response, a genuinely scale-nonlocal operation, or finite--archimedean coupling before scalarization.

Prime Lattice, Prime Circle, Visual Exploration, Xi Flow, and Analytic Frontier retain their previous discriminating gates. `PL-284` now imports a tighter literature-level computer-assisted enclosure and explicit spectral gap for the simple even ground state at aperture `0.8`, but this does not alter the `PL-293` source-specific boundary-regularity bottleneck. `PC-277` leaves only the intermediate rational-microscopic scalar window after broad matched controls; `VIS-202` marks the square-root-log boundary as a threshold of the present proof architecture rather than of the true collision energy; `XF-185`--`XF-186` remain locally well-conditioned but globally unassembled; and `ANF-180`--`ANF-183` retain the sharp source-continuation aperture as one of the cleanest remaining endpoint tests.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make the matched endpoint packet quantitatively sharp: derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` are equivalent discrimination boundaries. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-311` shows that Baker algebraic-linear independence still leaves the first unresolved pentagon zero manifold generically compatible with the prime source's relation-rank restriction. | Control the genuinely nonlinear nontorsion one-parameter prime-log orbit relative to the `k>=5` zero surface; integer characters, ambient topology, torsion classification, and standard linear forms in logarithms are no longer decisive. |
| `farey_discrepancy` | `narrow` | `FD-089` compresses the positive-energy row-adaptive packet to `H^{o(1)}` canonical prime-dilation predecessors at scale `H^{1-o(1)}`, with one predecessor preserving the full frontier energy exponent up to subpower loss. | Upgrade the `H^{-o(1)}` selected strength to fixed overlap/occupation or compose the zero-entropy predecessor family recursively across frontier blocks; near-frontier energy alone is not iterable occupation. |
| `mobius_cancellation` | `narrow` | `MC-259` leaves at most one blind pretender, `MC-260` shows current sparse/global variance theorems miss the hard sub-square-root source modulus, and `MC-261` identifies `H=G` as an all-class `log R`-smooth representation problem far beyond current polynomial-scale generation results. | Either exploit the special shell to prove logarithmic-scale generation, or control the surviving pretender/source coset by genuinely source-conditioned dispersion, reciprocity, or fixed-power signed cancellation. |
| `nyman_beurling` | `narrow` | `NB-088` gives an exact dyadic Blaschke-delay formula for the compulsory scalar sibling distortion and proves every fixed finite off-critical Blaschke divisor is asymptotically silent in its squared aggregate. | Prove a collective lower bound from the nonuniform tail of the full Blaschke product, or leave the scalar sibling-average channel for contrast directions, cross-block correlations, or the remaining positive radial floor charge. |
| `prime_circle` | `narrow` | `PC-277` closes broad normalized near-resonance regimes with exact-`Li` and rational-grid controls, leaving only an intermediate rational-microscopic scalar window. | Close that window with a stronger source-only control or find a zero-sensitive residual that survives it; otherwise leave scalar near-resonance for singular, directional, microscopic, or cross-level data. |
| `prime_flute` | `continue` | `PF-242` makes the raw hypercycle shear exponentially mode-local with squared Hilbert--Schmidt cut leakage `O(P_n^{-2})`, while `PF-317`--`PF-319` show that the actual endpoint requires the seam-normalized physical corner and cannot be inferred from corridor form smallness. | Transport the proven coefficient-level locality through the canonical seam/DtN normalization and Schur operations to obtain the `Z_LH` weak-`S_1` bound, independently control `T_H`, then keep infinite realization and signed reassembly as separate gates. |
| `prime_lattice` | `narrow` | `PL-293` gives the sharp generic `ell^{1/2}` boundary obstruction to `H^{1/2}` transport; `PL-284` supplies a tight literature-level certified simple even ground state and gap at aperture `0.8`. | Use the actual rational-prime Weil eigen-equation to suppress the universal boundary channel, gain useful `H^s` with `0<s<1/2`, derive a different translation identity, or bypass differentiable shift control. |
| `robin_extremal` | `narrow` | `RE-091` combines depth-`>=3` host sparsity with the prime-gap second moment to force a first-layer atom on at least one selected-cell boundary outside threshold measure `o(1)` whenever `Theta>469/600`. | Exploit first-layer-attached joint selection: couple the ordinary-prime boundary to the opposite depth-`1/2` or tied boundary using simultaneous gap, transport, skipped-span, or rightmost-maximizer constraints. |
| `visual_exploration` | `narrow` | `VIS-201` reaches `sqrt(log y)=o(log H)` by averaging the four-form singular factor; `VIS-202` proves that this square-root-log boundary is exact for the persisted fiberwise-sieve plus taper-tail majorant. | Change a structural ingredient: averaged congruence cancellation/dispersion across fibers, lower effective incidence dimension, a new tail mechanism, or a target resource different from `D_v`. |
| `weil_inertia` | `continue` | `WI-271` proves that a one-signed first-crossing support gap of width at least `h_0` forces positive prime-power autocorrelation, so complete screening requires `h_0`-gap-connected support. | Rule out the remaining densely connected one-signed screened mode using firstness/source structure, and separately use the `WI-269` PSD/nodal family to control long-range archimedean compensation on sign-changing branches. |
| `weil_positivity` | `narrow` | `WP-281` rules out every fixed finite-dimensional stable logarithmic-scale linear filter as a Gamma extractor even under RH, strengthening the local/nonlocal boundary left by `WP-279`--`WP-280`. | Build an independently selected infinite-dimensional/nonrational or genuinely global finite--archimedean coupling that retains source fluctuations and produces the Weil sign without inserting the zero divisor or an RH-equivalent primitive criterion. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**The portfolio is converging on source-specific residual carriers after the generic mechanism has been factored out.** `WI-271` exposes prime overlap only after the archimedean sign budget is exhausted across a genuine gap; `NB-088` isolates the Blaschke-delay remainder after the raw terminal energy is computed exactly; `FD-089` reduces row-adaptive transport to a zero-entropy predecessor family after normalized terminal capacity has been removed; `AF-311` shows that even Baker-strength linear arithmetic restrictions disappear as a discriminator before the nonlinear pentagon incidence problem begins. These are different objects, but each residual is defined only after a stronger benign control has been explicitly subtracted or screened.

**The endpoint geometry still decides whether a residual is usable.** Prime Flute is the clearest example: raw shear already has more than enough coefficient-level Hilbert--Schmidt decay, yet `PF-319` shows that the physical seam-normalized angle can remain large if metric conversion is uncontrolled. Farey's single predecessor keeps the full energy exponent but only an `H^{-o(1)}` guaranteed share and no inherited occupation. Robin's first-layer contact matters because it constrains the selected threshold chamber itself, not merely the ambient event pool. Nyman's finite-Blaschke silence shows that even exact source deformation can be invisible in a scalar direction while remaining available in the rest of the correlation geometry.

**Several apparent exits are now classified as category changes rather than stronger estimates of the old quantity.** `MC-261` shows that shell generation at the actual source scale is an exponential strengthening over fixed-power smooth-representative prior art; `WP-281` shows that adding any fixed number of local boundary states cannot bridge the zero-field/Gamma separation; `VIS-202` says another scalar cutoff cannot cross its method threshold; and `PL-293` says another generic logarithmic regularity bootstrap cannot create the missing half derivative. Progress in these lines must visibly introduce shell-specific generation/cancellation, infinite-dimensional or global coupling, inter-fiber cancellation, or eigenstate-specific boundary suppression respectively.

**Review identity is clean.** The checkpoint `6913efe949a145f7ad7decc803558f6717df06e9` audits the contiguous prefix through `80e0d028819a0fd9c0ca7b179de99f23e1e2b45d`, covering `AF-311`, `FD-089`, `NB-088`, `RE-091`, `WI-271`, the strengthened `PF-242`, and the updated `PL-284`. `MC-261` and `WP-281` are later canonical findings and remain explicitly uncleared by that checkpoint. No open review sidecar constrains the snapshot.

The existing `CLUE-cross-line-fidelity-realization-separation` remains sufficient at Master scope. The new evidence strengthens the distinction between source fidelity, target transport, destination normalization, and assembly, but it still does not provide one common source family, operator, normalization, and target from which an exact actionable transfer theorem follows. No additional Master clue is warranted.

The Graph-owned Riemann Atlas remains navigation-only for portfolio decisions. Its entrypoint and metrics advertise different stale source revisions (`4bc527...` and `834376...`), both far behind current canonical evidence, so no recommendation is inferred from Atlas telemetry.

## Highest-leverage unresolved questions

1. **Weil Inertia after the `h_0` gap theorem.** Decide whether firstness/source geometry forces an `h_0` bottleneck on every one-signed candidate, or construct an `h_0`-connected completely screened mode; attack sign-changing candidates separately through the PSD/nodal compensation law.
2. **Prime Flute normalized endpoint leakage.** Carry the exponential Pöschl--Teller locality and `O(P_n^{-2})` raw shear leakage through seam/DtN normalization and the Schur map to the physical `Z_LH` ideal bound, while independently controlling `T_H`.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp `Theta(X/a_X)` aperture, with positive-cascade non-excisability kept as a separate final gate.
4. **Farey fixed-strength predecessor composition.** Turn the `H^{o(1)}` prime-dilation predecessor family of `FD-089` into recursively iterable fixed-strength occupation/overlap, or construct a source-compatible control showing that zero-entropy predecessors can still remain multiplicatively incompatible.
5. **Nyman collective Blaschke tail.** Determine whether the full off-critical inner factor forces nonsummable dyadic parent/child delay imbalance; if finite-product silence persists collectively, move to sibling contrasts or cross-block terminal geometry rather than refining the scalar average channel.
6. **Robin first-layer-attached selection.** Above `469/600`, use the forced ordinary-prime boundary contact together with the opposing boundary and rightmost-maximizer rule to obtain a joint scarcity/transport contradiction; below that frontier retain the weaker finite-depth reduction.
7. **Möbius logarithmic shell generation versus cancellation.** Either prove that primes up to `(1+o(1))log R` generate the special shell radical group, far beyond current fixed-power prior art, or eliminate the one surviving blind pretender by source-conditioned cancellation.
8. **Weil Positivity beyond finite state.** Construct an independently forced infinite-dimensional or scale-nonlocal finite--archimedean coupling whose sign mechanism is not equivalent to subtracting the zero divisor or assuming an RH-equivalent primitive asymptotic.

## Portfolio changes proposed

No recommendation change is proposed. Weil Inertia remains `continue` because `WI-271` produces a direct arithmetic overlap theorem from the actual Suzuki source and leaves a sharply defined residual geometry rather than a generic positivity hope. Prime Flute remains `continue` because the strengthened `PF-242` removes raw shear mixing as the likely obstruction while leaving a canonical normalized-operator theorem for `Z_LH` and an independent `T_H` singular-value problem. Analytic Frontier remains `continue` because its critical-aperture source-continuation gate is unchanged and independent of the operator routes.

Among the `narrow` lines, Farey and Robin make the strongest positive structural reductions but still stop at fixed-strength assembly/joint-selection gates. Nyman proves that its simplest compulsory scalar channel is blind to any finite off-critical defect and must become collective or non-scalar. Arithmetic Fidelity exhausts standard algebraic-linear logarithm obstructions at the first unresolved polygon. Möbius and Weil Positivity identify new category boundaries after the latest checkpoint, but neither closes its surviving source-specific alternative. Prime Circle, Prime Lattice, Visual Exploration, and Xi Flow retain narrow bridges whose next advances require genuinely new currencies rather than tighter versions of already-classified controls.

No active line is a strong `pause-candidate` or `merge-candidate`, so no Research Watch rotation is justified. No split or new-line candidate clears the bar. No new Master clue is created because the cross-line commonality remains structural rather than an exact transferable source/operator theorem.