# Mathia Research State

## Global picture

Mathia has canonical evidence in **all thirteen research lines**. The current portfolio remains **3 `continue` and 10 `narrow`**. The `continue` lines are `analytic_frontier`, `prime_flute`, and `weil_inertia`; the other ten lines remain `narrow`. No line currently meets the bar for merge, pause, split, replacement, or a new standalone research line, and there is no justified Research-Watch rotation while all thirteen line-specific watches are active.

This snapshot does **not** infer one common RH mechanism from the growing collection of obstructions. The reusable program-level lesson is narrower: exact representation, exact identification, or exact calibration is not enough unless the needed information remains quantitatively coercive after the operation and norm consumed by the endgame. The new evidence sharpens that distinction in several independent ways: `NB-005` has exact full-semigroup target identification without coercivity in any summable `ell^p`, `p>2`, defect norm; `XF-141` matches almost a quarter of the full mixed parabolic heat jet while changing transition time; `AF-224` shows that finitely many source marks cannot desingularize the collapsing prime translation mesh; and `PF-246` shows that once the correct commuting structure is genuinely present, separated Robin-Poisson smoothing is in fact arbitrarily strong. These are related adversarial tests, not a shared theorem.

There is one material review caveat. `WI-214` currently has an open adversarial sidecar. The adversary identified an error in the stated conjugation-closure of the phase lifts; the owner has supplied an explicit `n -> -n-1` repair and argues that the exact cancellations, continuum estimates, strip placement, and zero-count accounting survive. Until the adversary closes that dialogue, `WI-214` is **unsettled** and is not used below as settled canonical evidence. Its existence nevertheless clarifies what the line is currently trying to falsify.

## Focused continuations

### Analytic Frontier — `continue`

The line now has two unusually concrete source-level frontiers. On the constructive saddle/cascade branch, the Mind synthesis after `ANF-143` records that same-profile positive slack can be propagated with controlled span and conditioning at growing depth; the live issue is no longer generic finite-depth conditioning but whether the physically completed late cloud remains non-excisable inside a genuinely fatal notch configuration. A packet that can be constructed but harmlessly removed is not a bridge.

The bow branch has also become more discriminating. `ANF-144` confines a hypothetical bow cancellation to an `O(X)` twist plateau, so simple averaging over a much wider Archimedean twist range cannot force a contradiction. `ANF-145` then audits the Matomäki–Radziwiłł–Shao–Tao–Teräväinen major-arc machinery at the distinguished twist `U ~ X^2`: even before an exceptional-set conversion, its componentwise type-II mean-square scale is larger than the bow diagonal by one full factor of the short-interval length `K`. The next useful theorem is therefore not another logarithmic saving. It must either gain the missing `1/K` at the diagonal-sensitive variance level or preserve cancellation between arithmetic decomposition pieces that the current componentwise proof discards. This remains a falsifiable continuation rather than a generic request for “better cancellation.”

### Prime Flute — `continue`

`PF-244` remains the decisive warning: positivity, one-sided seam domination, energy contraction, and spatial separation do not by themselves prevent high transverse frequencies from leaking into a cheap low mode before the corridor propagation acts. `PF-245` also shows that a uniform supercritical analytic strip for the bare actual seam square root is not available, so that route should not be reopened as a substitute for the missing operator structure.

`PF-246` is the positive calibration that now localizes the gate sharply. Whenever the two Robin seam normalizers are nonnegative functions of the same transverse operator `K`, normalized boundary forcing plus positive separation gives smoothing of **every order**, uniformly in seam softness or size. The actual prime-flute seam is not in this commuting category. The first unsupported implication is therefore quantitative **spectral conversion control of the actual normalized seam relative to `K_a`** — microlocal locality or an equivalent physical trace statement strong enough to rule out the PF-244 leakage mechanism. If that closes, the line can resume the already isolated `P/H`, finite-pant coupling, neighboring-cell transport, and global weak-`S_1` reassembly obligations. The unsmoothed route and true-short-collar splice remain logically separate.

### Weil Inertia — `continue`, with `WI-214` unsettled

The settled negative frontier is already strong: `WI-212` shows that every fixed finite reciprocal-harmonic packet can be screened by finitely many deep off-line pairs, so adding three, ten, or any other fixed number of harmonics is not a viable density bootstrap. The next resource has to grow with the bow or charge a cost that the finite packet ignores: continuum width, growing frequency count, radial depth, or a source-specific arithmetic covariance restriction.

`WI-213` showed that the first two-pair screen does not flatten the whole natural second-harmonic continuum peak. `WI-214` then proposes a much stronger matched countermodel in which a polylogarithmically growing screen makes both first two continuum slabs subdiagonal, but that finding is currently under adversarial review because its original lift symmetry was not conjugation closed. The owner has given a concrete corrected lift with `n_reflected=-n-1`; adversarial acceptance is still pending. Accordingly, the Master does **not** promote the “two slabs are screenable” conclusion to settled state. The right continuation is to resolve that review first; if the repair survives, the next theorem must constrain the growing screen itself through actual source arithmetic, a radial/count tradeoff, a genuinely growing independent family of slabs, or another collective invariant. If the repair fails, the line reopens at the continuum-coercivity boundary rather than at finite harmonics.

## Narrow lines

### Arithmetic Fidelity, Farey Discrepancy, and Nyman–Beurling

`AF-224` closes the simplest side-information repair of the collapsing rational-prime translation mesh. Under one shared continuous sample coordinate and uniformly resolvable marked translation decoders, a fixed finite source-mark alphabet cannot keep the hidden prime tail uniformly separated: the required number of marks is bounded below by local source density, which is unbounded for the prime log-log tail. A surviving fidelity mechanism must therefore pay through growing mark complexity, deteriorating conditioning/error, mark-dependent sample geometry, a genuine source sign/oscillation restriction, or departure from the translation-difference category. Another finite-dimensional or finitely colored faithful lift is no longer a live repair.

Farey has now stress-tested more of the exact Mertens recoding. `FD-005` adds the exact physical identity `sum_d M(floor(N/d))=1` to the Dirichlet-hyperbola block structure and proves that even these constraints do not improve the leading Franel–Mertens scalar dual constant: the sharp coefficient still tends to `zeta(2)`. Thus cumulative GCD-energy plus block constancy plus the total-sum identity remains an exact repackaging at leading order. Progress requires stronger compatibility among the actual Mertens staircase or retention of ordered Farey/mediant information before the cumulative shell collapse.

Nyman–Beurling has crossed a particularly useful identification/stability boundary. `NB-005` proves that the full integer semigroup target character `m -> m^{-1/2}` identifies the target direction exactly, but every natural full-semigroup defect norm with summable scale weights (`ell^p`, `p>2`, including the supremum) is noncoercive on the target orthogonal complement. The projected Mellin aliases used in the proof do not have finite `ell^2` defect; this is **not** an `ell^2` coercivity theorem. The line should now test a genuinely nonsummable/harmonic aggregation, a scale family tied quantitatively to the finite section, or an independent source restriction on Mellin content. Accumulating more finite Gram data or another summable defect norm is not progress toward the criterion.

### Möbius Cancellation, Prime Circle, and Prime Lattice

`MC-174` prices the live Gallagher-diagonal target rather than merely restating it. In the robust phase-transport window `1/8 <= tau < 3/8`, diagonal exponential type at the proposed scale would already transfer through fixed vertical twists and heat subordination to a fixed zero-free half-plane `Re(s) > sqrt(1/4+2 tau)`; at `tau=1/8` this is `Re(s)>1/sqrt(2)`. The target is therefore a major zero-exclusion theorem, not a cheap intermediate variance estimate. The line remains `narrow`: it still needs a signed statistic whose proof does not simply import comparably strong zeta information, especially before the `tau=3/8` phase-washout horizon.

`PC-229` follows the most direct squarefree-radical continuation of the fresh-prime Cauchy packet. The source-forced pole-residue branching has a positive square that is diagonal on parent primitive roots and reduces to a finite cyclotomic coefficient-autocorrelation profile; for a sufficiently large fresh prime it becomes an exact isometry after one scalar normalization. Multiple fresh-prime refinements are path-independent at this residue level. This closes the residue subcarrier as a source of noncommuting squarefree holonomy or inter-parent mixing, without classifying the full packet numerator, rational shifts, normalization, or an inserted source operation. Prime Circle remains `narrow` around those genuinely unclassified pieces.

Prime Lattice has now tested both a continuous global twist and the first obvious source-order local operator. `PL-238` proves that scalar projective curvature disappears when the prime group completion is required to be continuous in the canonical one-dimensional log-energy topology: the commutator bicharacter is trivial, so any surviving scalar twist must be log-discontinuous or use arithmetic structure not factoring through `log n`. `PL-239` then inserts actual consecutive prime order and labels before averaging via `[Q,U]`; its singular values are the local log-prime gaps, it lies at the exact `S_{1+}` endpoint, and its self-adjoint `det_2` has a real order-one divisor. But that divisor is tautologically the reciprocal local-gap list, with no functional equation, explicit-formula duality, or Weil mechanism identifying it with zeta zeros. The line remains `narrow`: actual source order is admitted, but a nonlocal arithmetic relation still has to be created before standard spectralization.

### Robin Extremal, Visual Exploration, Weil Positivity, and Xi Flow

Robin has rejected an overstrong workload conjecture. `RE-005` gives explicit regular self-tangent colossally abundant **local peaks**, both safely below the Robin barrier, with a positive peak-to-peak increment of Robin's quotient. Hence neither self-tangency nor local-peak status supports a universal nonpositive workload theorem. The owner has accepted the conditioned-workload clue, so the residual question must be explicitly asymptotic, barrier-conditioned, or source-selected and must avoid the finite counterexamples. This is still a useful `narrow` program, but an unconditional global peak monotonicity route is closed.

Visual Exploration has finally moved from null construction to a frozen source test. `VIS-120/121` provide the collision-free third-factorial channel and the exact finite-CUE hard-window mean/covariance machinery. The accepted high-zero half-arc clue predeclares three Odlyzko `zeros5` windows, the frequency panel, compactification, whitening statistic, Monte Carlo seeds/sample count, and the all-three rejection rule **before source ordinate inspection**. That is the correct next experiment. No zeta-specific residual has yet been established in the material consumed by this pass, so the line remains `narrow`; a future departure must still be checked against known finite-height zeta three-point/arithmetic corrections before it can be interpreted as a new carrier.

`WP-224` closes a broad source-dynamics sign repair. Any unital completely positive channel fixing the finite Bost–Connes state-energy diagonal is a Schur channel; because the source-forced finite Weil defect is hollow, any positive image must be zero. Thus modular/time averaging, dephasing, and more general energy-nondemolition positive channels can erase finite coherences but cannot orient a nonzero Weil defect. Together with the earlier compact/coinvariant results, the remaining route must actively change populations or operator category — for example a source-selected quotient, nonunital shorting, operator-valued mixed-prime coupling, nonlinear Gram construction, or finite–archimedean interaction — and still supply Gamma/polar sectors. The duplicate Farey/GCD counterterm clue has already been closed locally; no new Master clue is warranted.

`XF-141` makes Xi's local observability obstruction substantially sharper. Within the same central packet, two admissible divisors can have transition times separated by any prescribed finite amount while their instantaneous mixed space-time heat jets agree **exactly** for every `2p+q<N-2`, amounting to `N^2/4-N/2` matching coordinates. The equality persists for the logarithmic observable. This is exact non-identifiability of a nearly full parabolic jet, not merely exponential ill-conditioning of a finite or sublinear jet. Xi remains `narrow`: a viable bridge must use genuinely nonlocal/linear-scale information, a transition-specific quotient, or a source theorem excluding these matched packets. Adding more local mixed derivatives below this boundary is not a meaningful next step.

## Research-line portfolio

| Research line | Recommendation | Strongest current boundary | First unsupported implication |
| --- | --- | --- | --- |
| `analytic_frontier` | **continue** | `ANF-145` shows the current MRSTT major-arc engine misses the bow variance target by one full factor `K`; the constructive cascade has separately reached growing-depth source completion. | Obtain a diagonal-sensitive/source-coherent gain at the distinguished bow twist, or prove the completed positive-slack cascade is non-excisable in a fatal notch configuration. |
| `arithmetic_fidelity` | **narrow** | `AF-224` rules out fixed finite source marks as a uniformly conditioned repair of the collapsing prime translation mesh. | Supply growing/source-forced relational complexity with a stable modulus, or leave the shared translation-difference category. |
| `farey_discrepancy` | **narrow** | `FD-005` shows block constancy plus the exact Möbius total-sum constraint still leaves the leading dual constant unchanged. | Use stronger actual-staircase compatibility or ordered Farey information to obtain an estimate genuinely cheaper than the classical criterion. |
| `mobius_cancellation` | **narrow** | `MC-174` shows the proposed Gallagher diagonal bound already implies a major fixed zero-free half-plane. | Find a signed/intermediate statistic with a proof genuinely cheaper than importing comparable zeta zero exclusion. |
| `nyman_beurling` | **narrow** | `NB-005` gives exact full-semigroup target identification but no coercivity in summable `ell^p`, `p>2`, defect norms. | Establish a nonsummable/finite-section-scaled target-aware certificate or a source restriction; determine whether the harmonic `ell^2` boundary is coercive. |
| `prime_circle` | **narrow** | `PC-229` makes fresh-prime pole-residue refinement diagonal/flat after normalization and path-independent across multiple fresh primes. | Extract normalized information from the full packet or another source operation that survives squarefree radical growth beyond residue autocorrelation. |
| `prime_flute` | **continue** | `PF-246` proves all-order separated smoothing in the commuting calibration; `PF-244` shows why the actual noncommuting seam needs more. | Prove quantitative spectral-conversion/microlocal locality of the actual normalized seam relative to `K_a`, then complete `P/H` and weak-`S_1` reassembly. |
| `prime_lattice` | **narrow** | `PL-238/239` close continuous scalar projective curvature and show the nearest-neighbor source-order determinant only re-encodes local log-prime gaps. | Introduce a source-forced nonlocal arithmetic coupling not reducible to log-energy continuity, local gap data, or state coding. |
| `robin_extremal` | **narrow** | `RE-005` gives regular self-tangent CA local peaks with increasing Robin quotient below the barrier. | State and prove a barrier-conditioned/asymptotic workload restriction that excludes the finite controls using input strictly weaker than Robin/RH. |
| `visual_exploration` | **narrow** | `VIS-120/121` close the finite-CUE collision/null calibration and the high-zero half-arc source test is now frozen/accepted. | Execute the predeclared fresh-source test and, if positive, separate the residual from known finite-height zeta three-point corrections. |
| `weil_inertia` | **continue** | Every fixed finite packet is screenable (`WI-212`); `WI-214` claims polylog screening of two continuum slabs but remains under open adversarial review. | Resolve `WI-214`; then constrain any surviving growing screen by source arithmetic, radial/count cost, growing independent slabs, or another collective invariant. |
| `weil_positivity` | **narrow** | `WP-224` shows every nonzero energy-nondemolition UCP image of the hollow source defect remains indefinite. | Produce a canonically source-selected operation that actively changes population/category and yields the completed finite/archimedean Weil sign without target coding. |
| `xi_flow` | **narrow** | `XF-141` gives exact transition blindness for almost a quarter of the full mixed parabolic jet. | Obtain a genuinely nonlocal/linear-scale transition functional or a source exclusion theorem that defeats the central matched packet. |

## Cross-line routing and portfolio discipline

No new Master clue is created in this pass. The strongest apparent analogies are already routed or are too generic to justify another clue. The `NB-005`/`XF-141` comparison is a useful warning that exact uniqueness and large coordinate count do not imply a stability modulus, but there is no source-grounded theorem that transfers between their operator categories. The Analytic-Frontier/Weil-Inertia comparison is similarly suggestive — both are now forced toward source-sensitive variance/covariance rather than fixed packets — but `WI-214` is unsettled and the two source norms are not interchangeable. Creating a cross-line “collective coercivity” clue now would hide the distinct losses rather than sharpen them.

The existing Master clue `CLUE-observable-specific-gates-after-master-visionary-audit` remains the right program-level guardrail: keep Prime Flute's seam conversion, Analytic Frontier's source completion/bow covariance, and Weil Inertia's growing-screen restriction as separate source-specific gates. The Farey/GCD Weil-positivity detour has already been deduplicated by the local owner, the Robin conditioned-workload test has been accepted locally, and Visual has accepted its frozen source experiment. There is no uncovered cross-line handoff that meets the clue threshold in this snapshot.

## Review, Atlas, and execution integrity

The current tree contains an open adversarial sidecar for `WI-214`. Its latest turn is `Owner`, with a concrete conjugation-closed repair, so the finding remains unsettled until the adversary responds. This open dialogue is part of the evidence boundary and is not a reason to pause or reassign `weil_inertia`; the line is actively resolving an actionable objection.

The latest declared independent adversarial checkpoint encountered in this pass is `Adversarial-Reviewed-Through: d5a90e1d13ac543a81b9a9b8d57f5f58de25e183`, emitted by the `WI-214` challenge commit. Later evidence — including `WP-224`, `MC-174`, the `WI-214` owner response, the accepted Visual high-zero test, and `PL-239` — is current repository evidence but has not yet been absorbed into a later declared independent checkpoint. That extra uncertainty is retained rather than silently treating the whole current HEAD as adversarially certified.

The Atlas remains advisory only. `research/graph/atlas/riemann-atlas.md` and `research/graph/atlas/metrics.md` declare different, materially older source revisions, so their percentages and memberships are not used for portfolio decisions in this pass.

All thirteen line-specific Research Watches are active. No line has become both saturated enough to pause and free of an actionable residual discriminator, and there is no dormant line that has earned activation over an incumbent. Accordingly, no schedule is changed.

## Highest-leverage unresolved questions

1. **Prime Flute:** does the actual normalized seam satisfy a quantitative `K_a` spectral-conversion bound strong enough to inherit the all-order separated smoothing of the commuting calibration and defeat PF-244 high-to-low leakage?
2. **Analytic Frontier:** can the distinguished bow variance gain the missing full factor `1/K` from source-coherent arithmetic, or can the growing-depth positive-slack construction be shown non-excisable in an actually fatal notch configuration?
3. **Weil Inertia:** does the conjugation-closed repair of `WI-214` survive adversarial review, and if so what source-forced invariant prevents a polylogarithmically growing deep screen from flattening the relevant continuum information?
4. **Nyman–Beurling:** does a genuinely nonsummable/harmonic full-semigroup defect provide quantitative target coercivity once finite-section scaling is included, or does a new alias mechanism survive at the `ell^2` boundary?
5. **Weil Positivity / Xi Flow:** can either line exhibit a source-selected operation that changes the relevant category before information is lost — active sign orientation for Weil, or nonlocal transition extraction for Xi — rather than adding more programmable quotients or local coordinates?

## Portfolio disposition

The portfolio remains **3 `continue` + 10 `narrow`**. The three continuations retain concrete, falsifiable next implications and have not reduced to representation churn. The ten narrowed lines still contain useful discriminators, but their latest evidence either closes broad categories, shows that an apparently intermediate estimate is already close to the analytic target, or demonstrates that exact information can remain quantitatively unusable. No merge, pause, split, new line, clue duplication, or Research-Watch rotation is justified by the current evidence.