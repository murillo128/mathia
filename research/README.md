# Mathia Research State

## Global picture

The program's objective is to resolve the Riemann hypothesis. Proportion improvements, local certificates, numerical patterns, and general structural results are useful only insofar as they clarify or enable a route that excludes every possible off-critical zero. Mathia currently has thirteen research lines. Ten are evidence-bearing: `analytic_frontier`, `arithmetic_fidelity`, `mobius_cancellation`, `prime_circle`, `prime_flute`, `prime_lattice`, `visual_exploration`, `weil_inertia`, `weil_positivity`, and `xi_flow`. `farey_discrepancy`, `nyman_beurling`, and `robin_extremal` remain distinct pre-evidence scaffolds.

The strongest common bottleneck is now a **source-conditioned state/profile transport through non-scalar global assembly gate**. Earlier work established that matched subtraction, selector preservation, quantitative margin, and family-wide coercivity are necessary. The newest evidence moves the frontier one step upstream and one step more structural: several lines now possess a useful destination norm, local theorem, or exact source carrier, but the hard step is proving that the actual arithmetic state reaches that destination while its signed, relational, phase, or state-resolved information survives conditional averaging, scalar taxes, radialization, passive realization, or global geometric gluing.

`XF-064`--`XF-065` give the clearest positive instance. The positive-time moving-line selector frame survives exact finite displacement, and the measurement theorem can be stated directly on an arbitrary periodic finite state after quotienting uniform translation. If the displacement oscillation and adjacent-gap distortion make the Wiener parameters small and the third-difference energy is concentrated in the prescribed slow band, the exact nonlinear selector retains the tangent lower frame. The measurement side is therefore no longer the main Xi obstruction. The live theorem must force an actual finite-amplitude Xi transition slice into that state-space region, preserve nontrivial transition flux until entry, and survive nonperiodicity, collisions, or complex-root intervals.

`AF-151`--`AF-152` expose the corresponding transport law abstractly. Recomputing a Shtarkov/NML center after compression changes the propagated source center by the normalized local max-sum conflict profile `kappa`; total variation, chi-square, and stronger divergences see different moments or tails of that same profile. Along a compression chain, accumulated mismatch is first conditionally averaged by the next channel and then multiplied by the next local reset density. Scalar maximal-leakage loss is therefore not the relevant strong composition budget: later coarse-graining can erase an old mismatch while recanonicalization injects a new one. A useful arithmetic-fidelity theorem must control the evolving profile in the topology consumed by the final certificate, not merely attach one scalar loss to each stage.

Möbius Cancellation now supplies the strongest signed warning against positive regularization. `MC-097`--`MC-098` show that the positive degree-two radial shell and the higher-degree tail are separately almost-square scale and cancel only after retaining their signs. `MC-099` forces a large curvature sign reversal along the exact Hamming deformation, and `MC-100` proves an explicit almost-square interior spike already at bias `t ~ (log N)^(-2)`. Every fixed positive-length interior interval and stable Chebyshev sample family therefore contains almost-square amplitude. The surviving route cannot be “biased values are uniformly easier”; it must obtain an independent, scale-coherent relation among large signed values or cross-degree terms that yields a strict iterable Mertens contraction without reconstructing the hard endpoint as an input.

Analytic Frontier provides a complementary local-versus-global margin lesson. `ANF-067` proves that the completed five-point real-collapse mechanism changes sign immediately at cardinality six: one conjugate pair plus four real anchors has a genuine negative infinitesimal vertical direction. `ANF-068` then shows that this whole negative-second-variation class is screened by a fixed positive affine margin; sufficiently small vertical displacement, even after a sufficiently small central-notch perturbation, cannot consume it. The six-point reversal is therefore structural evidence that five-point collapse does not scale, but not a perturbative counterexample to the affine certificate. Any decisive six-point or larger-family obstruction must operate at finite height, use a different configuration-level coupling, or otherwise beat the complete affine slack rather than only producing a favorable derivative.

Several other lines now isolate the same transport/assembly requirement in their native categories. `PF-182` removes the canonical pant-decomposition cuffs from the Prime-Flute weighted budget by an exact area-preserving two-sided collar germ and arbitrarily thin symplectic pasting; the live geometry is the splice to the true `PF-138` Margulis-short collars and the final body/collar weighted estimate. `PL-184` extends one-point Kronecker flattening from smooth targets to arbitrary bounded-variation weights below the short-interval resolution budget, so finite step masks and jump discontinuities are still continuum-density controls rather than arithmetic selectors. `VIS-057`--`VIS-058` propagate raw-law error, including overlapping-triple dependence, through nonlinear Markov closure and Fisher normalization to an explicit signed-orientation uncertainty interval; the missing Visual step is now the actual higher-window data and a defensible process-aware uncertainty radius, not more exact gauge algebra.

Weil Positivity has also moved beyond the first passive repairs. After the accepted scope correction to `WP-171`, regular matrix Schur losslessness gives only a local PSD boundary-delay theorem, while a whole-line determinant/passivity obstruction requires a genuinely global matrix-inner realization. `WP-172` then proves that even allowing a finite Pontryagin defect is too small: neither orientation of the exact Gamma phase lies in any finite-negative-index generalized Schur class, because the required divisor has a non-Blaschke infinite tail. Any surviving operator route must therefore be nonseparable in the finite/archimedean variables, genuinely infinite-index or singular/domain-changing, or use source-forced indefinite external geometry together with an independent final positivity theorem.

Weil Inertia points in the same direction, but its newest strongest formulation remains procedurally unsettled. Settled `WI-175` already shows that every nonnegative linear local pressure is erased by the period-33 phase average under the existing scalar shifted-block assembly. `WI-176` proposes the stronger statement that arbitrary nonlinear/source-conditioned scalar local potentials remain cancelled whenever the global proof compresses them to one universal scalar tax. The normalization objection has been answered and the corrected convention persisted in the finding, but the adversarial sidecar is still open pending final verification. The Master therefore treats the stronger scalar-tax closure as quarantined rather than settled evidence; the line should nevertheless avoid spending effort on variants already covered by `WI-175` and should make any claimed escape explicitly non-scalar or witness-excluding.

The program-level discriminator is consequently: **fix the source category and matched control; keep the source-specific signed or relational state before positive/scalar reduction; identify the exact destination norm or state-space condition; then prove a source theorem that transports the actual arithmetic state into that destination with a margin surviving the full global assembly.** A pointwise selector, local derivative, finite passive lift, bounded-variation mask, canonical center, or exact local gluing theorem is now an admission gate rather than an endpoint unless the source-to-destination transport is also controlled.

The checked-in Riemann Atlas v1 telemetry is materially stale relative to the current evidence window and is therefore not used for portfolio allocation in this snapshot. Canonical findings, current minds, clues, and adversarial state control the decisions below until Atlas telemetry is refreshed.

## Research-line portfolio

| Research line | State | Current directive |
| --- | --- | --- |
| `analytic_frontier` | `narrow` | Treat the Montgomery--Taylor five-point scalar theorem as closed. `ANF-067` shows that real-collapse dominance already reverses infinitesimally at six points, while `ANF-068` proves that the associated negative-second-variation class is uniformly screened by affine slack near height zero. Search only for a finite-height/larger-configuration mechanism that actually consumes the complete affine margin, or for genuinely ordered/multi-point information absent from the closed scalar category. |
| `arithmetic_fidelity` | `narrow` | `AF-151` identifies Shtarkov reset drift exactly with the local max-sum conflict profile, and `AF-152` shows that this profile composes by conditional averaging followed by local reset. Derive a source-natural pipeline whose reset tails/moments and subsequent contractions are controlled in the final destination topology; scalar leakage loss or endpoint recovery alone is insufficient. |
| `farey_discrepancy` | pre-evidence | Keep dormant until one pre-registered Farey residual survives larger orders and the endpoint, local-order, denominator, mediant, Franel--Landau/Möbius, amplitude, and matched-null controls without changing the statistic after inspection. |
| `mobius_cancellation` | `narrow` | `MC-097`--`MC-100` close positive radial-shell norms, convex-curvature hopes, fixed-interior uniform smallness, and stable Chebyshev biased sampling as sources of a strict exponent gain. Preserve the forced signed cross-degree interaction and seek an independently controlled, scale-coherent recurrence or relation among the large interior values that contracts the target-bearing mode without importing the endpoint Mertens estimate. |
| `nyman_beurling` | pre-evidence | Keep the proposed target-aware finite-section test: first subtract the universal `WP-168` critical Mellin Gram/autocorrelation control, then ask whether target pairings, phase, signed Mellin data, or another non-Gram datum yields a uniform finite-section approximation rate or dual obstruction. The current evidence remains a redirect, not activation evidence. |
| `prime_circle` | `narrow` | `PC-186`--`PC-187` close the most source-native refinement repairs: primitive-shell multiplication and dilation commute, and the first weighted order defect is a cyclotomic coboundary with the same classical Mellin carrier. Leave the canonical radial refinement algebra and introduce genuinely new mixed-prime, multi-carrier, or ordered nonselfadjoint structure before positive scalarization. |
| `prime_flute` | `narrow` | `PF-179`--`PF-182` remove the long Lambert body, artificial split, full cusp, and canonical decomposition-cuff seam from the local area-preserving assembly problem. Splice the assembled body map to the `PF-177` gauges on the true `PF-138` Margulis-short collars, then prove the final two-sided inverse-unit-ball weighted metric estimate needed by the existing resolvent/Schatten bridge. |
| `prime_lattice` | `narrow` | `PL-181`--`PL-184` flatten broad one-point prime targets to continuum density even with growing shifts, slowly varying weights, and bounded-variation step/jump masks below the short-interval resolution budget. A surviving route must pay for genuinely high variation, arithmetic conditioning not reducible to `q/X`, joint/non-product prime relations, nonlocal transport, or analytic resolution beyond the present band. |
| `robin_extremal` | pre-evidence | Keep dormant until a canonical theorem controls extremal exponent transitions or a discrete-versus-continuous variational defect survives synthetic prime-like controls without assuming RH-strength prime-distribution error. |
| `visual_exploration` | `continue` | `VIS-054`--`VIS-058` separate exact quotient loss, conditioning, support drift, nonlinear Markov-closure error, and overlapping-triple dependence, and provide an explicit path from a process-aware raw-law radius to Fisher-orientation uncertainty. Stop adding exact two-ratio/gauge algebra; obtain the higher-window zeta/CUE inputs, predeclare a valid dependence/replication model, and test whether both scalar CMI and signed residual orientation survive their full error budgets. |
| `weil_inertia` | `continue` | Settled `WI-172`--`WI-175` show that source realizability matters but fixed-bandwidth confluence and fixed single-profile linear-pressure retuning have essentially no room. `WI-176` would extend the period-33 obstruction to arbitrary scalar local potentials under one universal scalar tax, but remains review-quarantined pending final adversary verification. Pursue a genuinely non-scalar/state-resolved global assembly, a source theorem excluding the periodic witness, multiple profiles that do not collapse to one tax, the exceptional block, or new arithmetic support information. |
| `weil_positivity` | `narrow` | `WP-169`--`WP-172` isolate the exact Gamma phase and close ordinary scalar passivity, regular positive matrix-delay readouts in their proper scope, global finite-channel inner lifts, and every finite-negative-index generalized-Schur repair of the exact phase. A survivor must couple finite-prime and archimedean data before positive reduction, or enter a genuinely infinite-index, singular/domain-changing, or source-forced indefinite category with a new final coercivity theorem. |
| `xi_flow` | `continue` | `XF-062`--`XF-065` close the tangent support/frame gap and then lift the moving-line lower frame first to exact finite displacement and finally to an arbitrary finite state satisfying explicit geometry/Wiener and slow-`H^3` concentration conditions. The live theorem is now state entry and flux survival: force an actual finite-amplitude Xi transition slice into those conditions before its critical transition mass can disappear, including nonperiodic, collision-safe, and complex-root regimes. |

The thirteen lines remain mathematically distinct. The portfolio has three evidence-bearing lines at `continue`, seven at `narrow`, and three pre-evidence scaffolds. Current evidence does not justify a merge, pause, split, new research line, or Research-Watch rotation.

## Cross-line structure

1. **The common frontier is source-state transport, not construction of another local observable.** Xi already has an exact finite-state measurement frame; Arithmetic Fidelity has an exact law for how a canonical reference drifts; Prime Flute has eliminated most local geometric seams. Their remaining theorems must show that the actual source state arrives at the useful destination without losing the load-bearing quantity during the path or global assembly.

2. **Signed or state-resolved information repeatedly disappears under one scalar summary.** Möbius cancellation is carried by large opposite cross-degree contributions, Prime Lattice one-point targets homogenize to density, Prime Circle refinement holonomy is a coboundary, and settled Weil-Inertia pressure vectors are erased by phase averaging. The next viable architectures must keep a relation, vector, profile, phase, or nonseparable coupling visible until the operation that supplies the final sign or coercivity.

3. **A favorable local direction is weaker than consuming the global slack.** The six-point Montgomery--Taylor vertical derivative changes sign, yet the corresponding collapsed real configuration carries a fixed affine margin that screens all sufficiently small displacements. The same warning applies program-wide: a negative derivative, nonzero residual, positive frame coefficient, or local trace-class improvement is not decisive unless its scale beats every matched-control and assembly margin in the final theorem.

4. **Canonicalization itself is now a dynamical object.** Arithmetic Fidelity makes this exact: previous mismatch is conditionally averaged, then a new local reset is injected. Visual Exploration gives the empirical analogue by transporting raw-law uncertainty through recomputed nonlinear closure. Any multi-stage arithmetic pipeline that repeatedly quotients or recanonicalizes should track the full evolving error/profile rather than assuming that “canonical at every stage” means coherent globally.

5. **Finite passive/positive enrichment is increasingly classified as a boundary model rather than an escape.** Weil Positivity now excludes ordinary finite-channel passivity and finite Pontryagin defect for the exact Gamma phase, while Prime Circle and Prime Lattice show analogous collapse under finite radial refinement and one-point averaging. A richer category is credible only if the source itself forces it and the final positive readout is proved rather than inserted.

6. **The Nyman redirect remains the best dormant-line handoff but still lacks evidence.** `WP-168` supplies a canonical universal Mellin Gram control and the existing Nyman clue already asks for target-sensitive information beyond it. None of the new findings supplies the missing uniform target-relative estimate, so activation or Watch rotation would be premature.

## Highest-leverage unresolved questions

1. Can `xi_flow` prove that an actual finite-amplitude transition state reaches the explicit `XF-065` slow-band state-space region while retaining nonvanishing critical triple flux, rather than only showing that the selector would detect the state once it is there?

2. Can `mobius_cancellation` derive an independent scale-coherent signed recurrence for the forced Hamming cross-degree cancellation, strong enough to turn the `MC-099` curvature reversal and `MC-100` large interior values into a strict iterable Mertens contraction without endpoint circularity?

3. Can `prime_flute` complete the splice between the exact assembled body map and the true Margulis-short-collar gauges and close the final two-sided weighted metric budget required by the resolvent/Schatten theorem?

4. Can `analytic_frontier` find a finite-height six-point or larger-configuration mechanism that defeats the affine screening of `ANF-068`, or prove that the remaining certificate survives after the five-point real-collapse principle itself has failed?

5. Can `weil_inertia` carry a genuinely state-resolved/source-specific quantity through global shifted-block assembly so that the period-33 witness cannot scalar-average it away, while the open `WI-176` normalization review is independently closed before its stronger scalar no-go is used as settled evidence?

6. Can the `nyman_beurling` target-aware clue produce a growing-section primal or dual estimate not determined by the universal `WP-168` Gram/autocorrelation, with conditioning and near-null target mass controlled uniformly?

## Portfolio changes proposed

No portfolio state change, merge, pause, split, new-line proposal, or Research-Watch rotation is justified. All ten evidence-bearing Research Watches remain useful, and the three pre-evidence lines still lack stronger information value than the weakest active Watch.

No new Master clue is added. The actionable cross-line transfers are already represented by existing destination-local clues, while the apparent new analogies do not yet satisfy the source-backed, falsifiable destination-awareness threshold.

One adversarial sidecar remains open: `WI-176-scalar-local-pressure-is-periodic-witness-cancelled.review.md`. Its mathematical normalization objection has been answered and the correction persisted, but the latest turn is still the Owner awaiting final adversary verification, so `WI-176` is not promoted to settled evidence. The `WP-171` sidecar has closed after persistence of its accepted local/global scope correction.