# Mathia Research State

Canonical evidence synthesized through `929cf49e3e8f345d4c3165d2b89c423e5e302311`. The latest independent adversarial checkpoint is `295694ee0ddd2e70bdb7a0a2b90834a03306ed35`, with `Adversarial-Reviewed-Through: de229a33e67ad8ae1f8e9091e80782e6eac7e3e4`; later findings are used as current canonical evidence but are not treated as covered by that checkpoint. No `.review.md` sidecar is open in the current tree. In particular, the `PC-272` finding survives while its review sidecar has been deleted, so its corrected `q=13` source-domain witness is treated as review-closed/accepted under the repository review protocol.

## Global picture

Portfolio recommendations remain unchanged: `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other research line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

The strongest program-level update is that **ambient repair is increasingly exhausted; the live resource is source- and state-restricted geometry**. `AF-296` shows that a target-defined condition metric can turn the collapsing projective zero-count margin into only logarithmic distance, but precisely because the metric is singular on the target discriminant. That is not yet a source-native stabilization: the discriminating question is whether a metric chosen independently from the zeros or bad set is comparable to this condition geometry. `PL-286` reaches the same boundary from the Weil side. Inside a source-static finite-channel aperture, the full prime-comb motion has a sharp generic `Theta(1/log(1/|a-b|))` modulus; finite-channel phase cancellation cannot improve it generically. Preventing the first crossing therefore has to use the actual low spectral state, eigen-equation, or another state-specific restriction rather than a stronger ambient continuity estimate.

Möbius and Nyman make that distinction algebraic. `MC-246` upgrades the quadratic blind-sector picture to the full annihilator: every character invisible to the smooth-dilation subgroup collapses to one multiplicative rough coset, and the quotient index cancels exactly against the coset density. Even eliminating all quadratic blind characters is not enough, because an odd-order quotient can remain. The gate is now exact small-prime generation `H=G` or fixed-power signed cancellation on the one rough source coset. `NB-073` similarly shows that Gram prediction by itself erases the stationary inner defect: one-step prediction energy sees the outer factor, while comparison with the true causal feedthrough recovers the inner charge exactly. The nonstationary Nyman problem is therefore not another conditioning estimate but a source-specific comparison between future prediction energy and the newest-cell innovation.

The second common theme is **global capacity and continuation rather than local signal existence**. `FD-074` proves that any strict supercritical discrepancy record must live on a genuinely large-radical host and fixes its parity sign; under a false-RH frontier the radical grows supercritically as well. This rules out bounded prime-power host explanations, but still supplies no recurrence or density theorem. `RE-076` turns the endpoint-corrected Chebyshev deficit into a strict global Lyapunov coordinate through arbitrary exposed-state switches: selector dilation gaps and higher-layer prime mass can only increase it. Together with the earlier support-birth bill, Robin is now asking whether the source has enough global first-layer capacity to pay an accumulating debt, not whether a favorable local packet can be found.

Prime Flute and Weil Inertia contain the strongest new `continue` evidence. `PF-310` closes the main escape left by `PF-309`: the Schur-screening saturation is not merely an ambient low-frequency artifact, because exact nonconstant witnesses inside one fixed physical low-frequency band still drive the shorted-form ratio to zero and the Schur conversion norm to one. The dangerous reservoir is already physical-low; the unresolved question is whether the true `P/H` entrance, exterior propagation, Schur inverse, low-to-high conversion, and signed reassembly cancel it or turn it into final target damage. `WI-258` adds a genuinely state-specific first-crossing constraint: phase modulation combined with sliding-window positivity couples the firstness slack to a localized cosine defect of the von-Mangoldt-weighted autocorrelation. Any source-forced negative defect creates strict slack. The corresponding owner clue has already been strengthened, so the next theorem should attack that two-parameter medium-radius cone for the same first zero mode rather than invent another generic regularity surrogate.

Prime Circle and Visual clarify what observation can erase. The adversarially corrected and now review-closed `PC-272` identifies fixed-upper quotient power spectra exactly with multiplicative-character autocorrelations and supplies an accepted prime-source witness showing that the quotient does not determine an ordered source statistic. Quotient classification is therefore complete enough to expose what it loses; the remaining question is whether an order-sensitive residual survives all the way to an RH-relevant target. In Visual, `VIS-190` sharpens the smooth quadratic closure boundary from linear observation length to `H >> y/log y`: above that scale the full normalized prime-pair off-diagonal vanishes even after absolute summation. At the method boundary, only the kernel-unresolved short-gap zone remains. Improving taper smoothness alone cannot remove it; progress requires arithmetic sparsity/cancellation inside that zone or a different observable.

Weil Positivity remains a complementary negative boundary rather than a merge candidate. `WP-271`--`WP-272` already rule out finite-order conditional positivity and every continuous scalar spectral multiplier that retains the Mangoldt atoms. Combined with `PL-286` and `WI-258`, the completed-Weil picture is increasingly specific: generic continuity is too weak, smooth scalar sign repair is impossible, while a state-specific first-crossing route still survives before scalar readout.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make the matched endpoint packet quantitatively sharp: derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` are equivalent resolution boundaries for the adversarial packet geometry. | Prove a source-side continuation theorem showing that the actual prime-minus-rough source cannot satisfy the matched local constraints and still reappear as a late packet at the critical aperture. Keep positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-290`--`AF-296` identify the boundary-zero discriminant, the collapsing projective margin, the exact bills for linear/nonlinear coordinate repair, and the logarithmic condition geometry induced by distance to that discriminant. | Determine whether a geometry defined from the arithmetic source independently of the target bad set is uniformly comparable to the condition metric. Otherwise the logarithmic repair is target-tautological rather than a transport theorem. |
| `farey_discrepancy` | `narrow` | `FD-072`--`FD-074` turn supercritical discrepancy records into protected causal blocks and then force every strict power record onto a large-radical host with a parity-determined sign. | Control spacing, overlap, density, or propagation of those record blocks. Large radicals and polynomial block thickness do not by themselves imply target-relevant global occupation. |
| `mobius_cancellation` | `narrow` | `MC-244`--`MC-246` collapse the entire blind family to the annihilator of the smooth-dilation subgroup and then to one rough multiplicative coset; quotient index cancels source density exactly. | Prove actual generation `H=G`, not merely quadratic full rank, or obtain fixed-power signed cancellation in the surviving rough coset/reciprocal-L aggregate. Shrinking a positive blind quotient does not buy the required power. |
| `nyman_beurling` | `narrow` | `NB-071`--`NB-073` classify stationary branch-inner defects and show that Gram prediction plus the true causal feedthrough detects the inner charge exactly, whereas prediction conditioning alone can miss it completely. | Construct the nonstationary future-prediction energy from descendant Gram data and compare it to the true newest-cell innovation strongly enough to rule out persistent inner defects for the arithmetic source. |
| `prime_circle` | `narrow` | Review-closed `PC-272` identifies the fixed-upper quotient spectrum with multiplicative-character autocorrelation and gives a corrected `q=13` prime-source witness that quotient data do not determine an ordered source statistic. | Test whether a retained ordered/partial-window statistic survives normalization and reaches an RH-sensitive target. The accepted non-factorization only proves information loss by the quotient, not endpoint relevance. |
| `prime_flute` | `continue` | `PF-303`--`PF-310` give the reversible Markov/Green scalar skeleton, expose divergent screened backreaction, and show that Schur saturation already occurs on source-accessible witnesses inside a fixed physical low-frequency band. | Derive the genuine physical `P/H` entrance, exterior propagation, Schur inverse, low-to-high conversion, and signed reassembly. Decide whether they cancel the low-band reservoir or convert it into dangerous output/multiplicity. |
| `prime_lattice` | `narrow` | `PL-283`--`PL-286` combine finite-aperture certification with a sharp source-static transport barrier: even the finite prime comb has only inverse-log generic continuity in the canonical logarithmic topology. | Replace generic operator/form continuity by a source/state-specific low-branch or min--max law that prevents the first negative crossing as new von-Mangoldt atoms enter. |
| `robin_extremal` | `narrow` | `RE-070`--`RE-076` turn matched first-layer runs into a one-sided Chebyshev--Mertens ledger, price resets in new first-layer support births, and package the endpoint-corrected Chebyshev deficit as a strict global Lyapunov coordinate across arbitrary switches. | Prove that a false-RH fan cannot supply enough support births and physical span to pay the accumulating Lyapunov/reset bill, or construct a concrete architecture that does. |
| `visual_exploration` | `narrow` | `VIS-188`--`VIS-190` remove hard-window endpoint resonance and push absolute diagonalization of the smooth quadratic full-prime observable down to every regime `H >> y/log y`; the unresolved zone is now localized to gaps below the Fourier-resolution scale. | At `H ~ y/log y`, use signed/arithmetic information inside the unresolved short-gap population, or leave the fixed smooth quadratic observable for higher/growing-order, discrete/Gram, rare, or hybrid prime/zero statistics. |
| `weil_inertia` | `continue` | `WI-251`, `WI-253`, and `WI-258` retain source-specific first-crossing information after generic scalar routes fail; phase modulation now links firstness slack to a localized cosine defect of the source-weighted autocorrelation. | Prove a source-side negative cosine defect or incompatible two-parameter cone across several prime-power thresholds for the same first zero mode, or close the odd signed-flux reserve by another state-specific identity. |
| `weil_positivity` | `narrow` | `WP-269`--`WP-272` place the critical carrier beyond ordinary categories, show pole completion is tempered exactly at RH strength, and rule out finite-order conditional positivity and every continuous scalar spectral multiplier that preserves Mangoldt atoms. | Build a source-native frequency-mixing, matrix/noncommutative, singular-boundary, or alternative finite--archimedean completion whose sign is not equivalent to assuming RH-strength descent or erasing the arithmetic atoms. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and global multiscale assembly, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**Source/state restriction is now more informative than ambient conditioning.** `AF-296` can improve distance only with geometry singular on the target discriminant; `PL-286` proves the generic finite-channel Weil comb retains an inverse-log barrier; `NB-073` shows prediction conditioning misses the inner defect unless paired with causal feedthrough; `MC-246` shows quotient-size reduction has no fixed-power value while any blind quotient survives. These are distinct operators and no theorem transfers automatically, but all four now point away from another ambient norm or coordinate change and toward the actual source-selected direction.

**Global capacity and continuation are replacing local existence.** Farey has protected large-radical record episodes but still needs recurrence. Robin has a monotone global debt plus a quantified reset bill but still needs a source-capacity impossibility theorem. Analytic Frontier already has the sharp local aperture and still needs to forbid a late matched continuation. The common failure mode is exact or strong local source consistency with insufficient control of the same trajectory at the endpoint scale.

**The physical readout must be included before declaring an obstruction or a signal.** `PF-310` shows that the bad Schur reservoir is genuinely present inside a fixed physical-low band, but only the complete `P/H` map can decide whether it becomes harmful output. Review-closed `PC-272` proves that quotient statistics lose ordered prime-source information, but endpoint relevance of the retained statistic is still open. `VIS-190` shows how aggressively a smooth observation map can erase pair structure above its resolution threshold. Representation-level structure should therefore be promoted only after its entrance, conversion, and readout maps are explicit.

**The completed-Weil routes are complementary but not merge-ready.** Weil Positivity closes smooth scalar sign manufacture, Prime Lattice closes generic finite-comb continuity as a route to transporting tiny margins, and Weil Inertia keeps a state-specific two-parameter first-crossing inequality alive. The surviving opportunity is source/state geometry before scalar readout, not another scalar postprocessing theorem.

The existing proposed `CLUE-global-cross-line-fidelity-realization-separation` already captures the distinction between source fidelity, target transport, destination realization, and assembly. `WI-258` is already represented by its strengthened owner clue. The new analogies still lack one exact shared source family, operator, normalization, and target map, so no additional Master clue is justified.

The Graph/Atlas remains navigation-only. Its `riemann-atlas.md` and `metrics.md` declare different, substantially stale source revisions, so no portfolio recommendation or cross-line conclusion is derived from Atlas percentages or frontier telemetry.

## Highest-leverage unresolved questions

1. **Weil Inertia two-parameter first crossing.** Use the explicit `WI-258` phase/sliding cone to force a negative localized cosine defect, or an incompatibility across several von-Mangoldt activation thresholds, for the same first zero mode without importing generic regularity that `WI-257` has already ruled out.
2. **Prime Flute full physical assembly.** Derive the exact `P/H` entrance--Schur--reassembly chain and determine whether the fixed-low-band saturation of `PF-310` is cancelled, converted into physical-high output, or amplified by multiplicity.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet after the sharp `Theta(X/a_X)` aperture, keeping non-excisability of a surviving positive cascade as a separate final gate.
4. **Nyman causal prediction/feedthrough comparison.** Construct a nonstationary prediction invariant from future Gram data and compare it quantitatively with the actual newest-cell innovation, thereby excluding a persistent branch-inner factor for the arithmetic source.
5. **Robin/Farey global capacity.** Decide whether the false-RH source can pay Robin's monotone Lyapunov/support-birth bill and whether Farey's large-radical protected record blocks recur with enough density. Both lines now know the local cost; the missing theorem is global supply.
6. **Möbius exact generation or signed rough-coset cancellation.** Determine whether the small-prime dilation subgroup is actually all of `G`; if not, control the one surviving rough coset with fixed-power signed cancellation rather than further quotient-dimension estimates.

## Portfolio changes proposed

No recommendation change is proposed. The `continue` lines remain `analytic_frontier`, `prime_flute`, and `weil_inertia`; every other research line remains `narrow`. The new findings sharpen several gates but do not justify promotion, pause, merge, split, or a new standalone line.

No Research Watch rotation is justified because no active line is a strong `pause-candidate` or `merge-candidate`; the Master rotation gate should not be bypassed merely to align watch allocation with the current recommendation labels. No new Master clue is added because the strongest new cross-line relationships remain structural analogies rather than exact source/operator/target transfers.