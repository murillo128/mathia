# Mathia Research State

Canonical evidence synthesized through `111aa89314cf14cc3dc30c0802bc0f42bdb44ed1`.

## Global picture

Mathia has canonical evidence in all thirteen current research lines. The portfolio remains **3 `continue` and 10 `narrow`**: `analytic_frontier`, `prime_flute`, and `weil_inertia` remain `continue`; the other ten remain `narrow`. No line currently meets the evidence bar for merge, pause, split, replacement, or a new standalone line. The strongest common change is that several routes have stopped asking for generic regularity or representation richness and now expose the exact source-dependent statistic consumed by the endgame: carrier-relative mass placement in Analytic Frontier, heavy extension-angle counting in Prime Flute, and signed von-Mangoldt discrepancy after pole/main-density cancellation in Weil Inertia.

Analytic Frontier has closed a broad class of endpoint surrogates. `ANF-173` extends exact-twist discorrelation through the full endpoint horizon but also shows that arbitrary fixed logarithmic savings, fixed-order Gowers uniformity, and bounded-test relative decorrelation can coexist with target-sized endpoint charge. `ANF-174` removes the tiny-amplitude loophole: a vanishing-density `{0,1}` source placed in one exact carrier sector can store the required `sqrt(X log X)` charge while all long-interval bounded-test and fixed-order Gowers statistics remain power-small. The live theorem must therefore control the absolute placement of the actual prime-minus-rough residual relative to the distinguished carrier, equivalently the exact Fejer scale-slope defect or a carrier-sector discrepancy at the target charging scale. More fixed-order pseudorandomness, amplitude quantization, or sparsity does not address this gate. Fatal non-excisability of the surviving same-profile cascade remains a separate downstream obligation.

Prime Flute has reduced its weak-trace endpoint to a concrete physical counting problem. `PF-282` factors the canonical energy-normalized mixed form as `T=F_P Gamma F_H`, where the bounded strength factors record extension magnitude and `Gamma` is the relative angle of the low-fed and high-fed extension ranges. `PF-283` then proves that the weak-strength complement is norm-small and that the sufficient endpoint condition is the scale-coupled heavy-angle Weyl bound `tau N_{Gamma_tau}(2 tau)=O(1)`. This is materially weaker than demanding the full angle operator itself lie in weak trace class: excess heavy-channel population may be paid for by principal-angle decay at the matching scale. The live theorem is to prove or refute this count for the actual simultaneous one-cusp-pant geometry with the physical `P/H` split retained. Scalar same-sector reservoir amplification is no substitute for that test.

Weil Inertia now exposes the most direct source-side compensation problem in the portfolio. `WI-241` shows on odd localized tests that replacing the discrete von-Mangoldt source by its PNT density-one model cancels the polar contribution exactly up to a bounded positive resolvent. The resulting gamma-plus-resolvent form still has unbounded odd negative index. Hence bulk prime density is not the missing positivity mechanism: after the pole/main-density cancellation, the decisive arithmetic object is the signed discrepancy of the von-Mangoldt measure from density one, equivalently a special autocorrelation functional of `psi(x)-x`. Any source-side proof of localized Weil positivity must supply a uniform order-one compensation on the relevant normalized odd families without bounding the exponentially larger pole and prime main terms separately. The Bombieri finite-inertia route remains an independent alternative, but generic first-crossing or relaxed window geometry is no longer the main finite-radius question.

One adversarial sidecar remains open and is quarantined from synthesis: `PL-269-localized-weil-ground-limit-rh-order-parameter.md.review.md`. The mathematical limit equivalence has not been rejected, and the proposed explicit-trial domain repair is acceptable in principle, but the persisted provenance/domain bridge still awaits the requested canonical correction and final adversarial verification. `PL-269` is therefore not used as settled evidence for portfolio decisions. The latest independent adversarial checkpoint records `Adversarial-Reviewed-Through: d26a2f1ee0b5b879cc9f2ad51e13abb2ab4db4d7`; later canonical findings remain usable as current evidence but are not yet covered by that checkpoint. The Riemann Atlas remains outside portfolio decisions because its derived surfaces are stale and mutually out of sync with the canonical corpus.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-173`--`ANF-174` show that full-endpoint fixed-order relative uniformity, sparsity, nonnegative `{0,1}` amplitudes, and power-small long-interval statistics can all coexist with target-sized endpoint memory. | Prove an absolute carrier-relative placement/discrepancy theorem for the real prime-minus-rough residual at the `sqrt(X log X)` charging scale, or control the exact Fejer scale-slope defect directly. Relative pseudorandomness alone is below the frontier. |
| `arithmetic_fidelity` | `narrow` | `AF-262`--`AF-264` sharpen the phase boundary: algebraic Keiper phases do not support exponential cancellation, while order-one Riemann symmetry and even a preserved first endpoint jet do not determine the higher phase data that control sampling. | Prove that the actual growing zeta phase vector avoids the exceptional simultaneous cancellation locus with quantitative conditioning. Generic irrationality or first-jet symmetry is insufficient. |
| `farey_discrepancy` | `narrow` | `FD-043`--`FD-044` cancel the critical density pole in the centered Mellin/Jordan formulation and control well-conditioned growing polynomial-frequency packets without an inverse minimum-gap singularity. | Show that the physical quotient-shell source cannot combine multiplicative energy irregularity with enough packet ill-conditioning or remainder size to evade the nonsquarefree coercivity controls. |
| `mobius_cancellation` | `narrow` | `MC-213` quantifies the exact leverage of the source-prescribed square-modulus residue: a selected centered coordinate can be `sqrt(phi(d^2))` times its residue-class RMS, so natural all-residue BDH energy can lose a sharp `d^2` factor on the coordinate actually needed. | Obtain source-specific anti-concentration/anti-alignment for the prescribed residue, or control the raw uncentered square-modulus energy without separately paying a Mertens-complete principal mode. |
| `nyman_beurling` | `narrow` | The weighted skeleton and source-defined logarithmic-tail dual remain a genuine compression, while `NB-042`--`NB-044` identify the sharp analytic barrier: pushing ambient dual decay toward its square-root floor is RH-equivalent, and any fixed power improvement imposes a corresponding zero-free strip. | Control the target-selected projected repair or a one-sided Burnol-scale certificate more cheaply than an RH-equivalent dual-norm estimate or an `N`-scale projection solve. Directional correlation, not ambient norm, is the plausible remaining gap. |
| `prime_circle` | `narrow` | `PC-255`--`PC-256` reduce quadratic multi-upper collision Grams and fixed-depth multi-upper trace words to finite families of shifted Dedekind/arithmetic correlations. | Prove genuinely new arithmetic cancellation in those source-selected shifted correlations, or introduce growing branching/order before scalarization. More fixed-depth readout algebra does not create a new state variable. |
| `prime_flute` | `continue` | `PF-282`--`PF-283` factor the physical normalized cross form into extension strengths and a relative angle, then reduce the weak-`S_1` endpoint to the single heavy-angle count `tau N_{Gamma_tau}(2 tau)=O(1)`. | Prove that count from the simultaneous one-cusp-pant extension geometry. If heavy-strength population exceeds `O(tau^-1)`, quantify enough principal-angle decay at the same scale to recover the endpoint. |
| `prime_lattice` | `narrow` | The accepted localized-Weil bridge through `PL-265`--`PL-268` connects finite aperture to finitely many prime-power events and sharpens monotonic/finite-crossing observability without providing the missing arithmetic sign theorem. | Derive source-forced finite-prime compensation/positivity rather than another equivalent spectral order parameter. `PL-269` remains quarantined while its open provenance/domain review is unresolved. |
| `robin_extremal` | `narrow` | `RE-042`--`RE-044` resolve the first-layer switch geometry far enough to show adjacent switches straddling their generating primes and forcing two-sided half-log prime-gap structure, while the normalized signal remains below the current race-resolution scale. | Upgrade the common-source prime-power race approximation to resolve or aggregate that quantized signal and control higher-layer/tied chambers; another abstract selector fan is not enough. |
| `visual_exploration` | `narrow` | `VIS-157`--`VIS-159` extend the deterministic Gram-clock null to quadratic, polynomial, and noninteger-power index sampling in fixed finite dimension, with explicit Haar-threshold behavior. | Use a genuinely non-Gram or growing-complexity probe with a predeclared source-side reason to escape the deterministic clock null. Fixed-dimensional power-law resampling is no longer evidence of arithmetic structure. |
| `weil_inertia` | `continue` | `WI-241` gives an exact odd-sector decomposition in which the PNT density cancels the pole to a bounded positive resolvent, leaving the signed von-Mangoldt discrepancy as the only arithmetic term capable of compensating an unbounded negative-index background. | Prove the required order-one signed discrepancy charge on the source-selected localized autocorrelation families, or derive an equivalent finite-prime compensation invariant that is strictly cheaper than assuming localized Weil positivity itself. |
| `weil_positivity` | `narrow` | The current multi-channel/scattering results show that enlarging the representation after scalarization does not force the critical all-prime weights or sign; the surviving route is pre-scalar and source-forced. | Build a native global arithmetic category/state in which mixed-prime coupling, the archimedean term, and the sign are fixed before scalarization. The Maaß--Selberg remainder-sign interface remains the bounded handoff. |
| `xi_flow` | `narrow` | `XF-175`--`XF-177` show both atomwise Gaussian blindness to remote lattice mass and increasing rigidity of Jacobi data: exact/asymptotic matching collapses toward the Xi comb, while remote single-atom surgery exhibits a half-power stability threshold. | Turn the exact square-lattice coefficient law into quantitative rigidity at or below the half-power boundary and then connect that rigidity to the de Bruijn--Newman sign; generic positivity, analyticity, or Jacobi matching alone is insufficient. |

## Cross-line structure

The dominant common boundary is **the theorem must preserve the exact source-selected coordinate consumed after compression**. Analytic Frontier can satisfy strong relative pseudorandomness while hiding endpoint charge in carrier-relative placement. Möbius Cancellation can satisfy natural all-residue variance bounds while the distinguished residue remains atypically large. Nyman can compress the source while the one target-selected repair direction retains the hard information. Prime Flute can have large raw reservoir structure while the endgame depends only on a thresholded angle between energy-normalized low/high extension ranges. These are different mathematical objects, but they share one falsification rule: a norm, average, quotient, or surrogate is useful only if a theorem transports it quantitatively to the final signed or projected coordinate.

The strongest cross-line convergence is **localized Weil positivity as a prime-discrepancy compensation problem**. Prime Lattice supplies accepted finite-aperture/finite-prime observability without an independent sign theorem. Weil Inertia shows that the non-prime completion plus the PNT main density remains strongly indefinite after the exact pole-density cancellation, so positivity has to come from the fluctuation of the real von-Mangoldt source around density one. Weil Positivity independently warns that global positivity cannot be recovered by adding representation machinery after the arithmetic data have already scalarized. The useful target is therefore not another equivalent crossing criterion or a larger Hilbert/category representation; it is a source theorem controlling the signed finite-prime discrepancy before that information is erased.

A second convergence is that **generic complexity is increasingly a matched control rather than evidence**. Fixed-order uniformity and sparse unit amplitudes do not anchor the Analytic Frontier endpoint; polynomial and power-law Gram clocks remain deterministic nulls; growing Mellin packet complexity in Farey is harmless while conditioning remains controlled; and fixed-depth Prime Circle words reduce to finite-shift correlations. Future evidence should identify the arithmetic law that forbids the matched control, not merely enlarge the control's dimension.

The existing Master handoffs remain sufficient. The Bombieri-to-Weil-Inertia clue tests sparse zero-side defect persistence and remains logically independent of the finite-radius prime-discrepancy route. The Maaß--Selberg-to-Weil-Positivity clue targets preservation of sign after a genuinely global positive construction. Prime Flute's heavy-angle question is already owned by its updated local clue. No additional cross-line clue is warranted by the current evidence.

## Highest-leverage unresolved questions

1. **Weil Inertia prime-discrepancy compensation.** Starting from `WI-241`, control the signed `psi(x)-x` autocorrelation functional on the normalized odd families that expose the negative mean-density background. The theorem must retain the exact pole/main-density cancellation and be demonstrably cheaper than localized Weil positivity itself.
2. **Prime Flute heavy-angle Weyl law.** Prove or refute `tau N_{Gamma_tau}(2 tau)=O(1)` for the physical simultaneous pant extension. Separate the contribution of heavy-strength population from principal-angle decay only if the decomposition preserves the exact scale-coupled count.
3. **Analytic Frontier carrier-placement theorem.** Bound the absolute carrier-sector occupancy or exact Fejer scale-slope defect of the real prime-minus-rough residual at the endpoint charging scale. More fixed-order relative uniformity cannot close this gate.
4. **Nyman target repair below the RH-equivalent norm frontier.** Convert the source-defined tail information into the target-selected projected correction or an equivalent one-sided certificate without demanding square-root ambient dual decay or reconstructing the full finite section.
5. **Preserve source coupling through averaging.** Möbius must control the source-prescribed residue rather than an all-residue RMS; Farey must prevent simultaneous scalar irregularity and packet ill-conditioning; Weil Positivity must form the mixed-prime/sign relation before scalarization; Arithmetic Fidelity must prove noncancellation for the actual zeta phase vector rather than a generic phase class.

## Portfolio changes proposed

No recommendation changes. `analytic_frontier`, `prime_flute`, and `weil_inertia` retain short, falsifiable implication chains adjacent to their endgames and remain `continue`. The ten other lines remain `narrow`. Nyman--Beurling remains the closest compression-driven candidate for a stronger recommendation, but its target-repair boundary is now known to meet an RH-equivalent ambient norm frontier; a promotion requires a cheaper directional return certificate, not further compression. Möbius Cancellation has isolated a sharp source-selected coherence loss but not supplied the missing anti-concentration theorem. Prime Lattice's reviewed `PL-269` is excluded from any promotion argument until its sidecar is resolved.

No merge, split, replacement, new-line, or Research Watch rotation is proposed, and no new Master clue is needed. The portfolio should remain stable while the next evidence attacks source placement, signed prime discrepancy, heavy-angle counting, or target-selected transport rather than reopening proxy classes already closed by matched controls.