# Mathia Research State

Canonical evidence is synthesized through `e12e4c84a11ae46c19cdf644c1e4e63b8e0eba90`. The latest clean independent adversarial checkpoint is `14541823bf5dceadc7f75a32ff1cee5c117a2451`, which declares coverage through the reachable commit `22231d112589edadf06224293ec227257c76f30f`. There are no open `.review.md` sidecars. `MC-265`, `VIS-205`, `WP-283`, and `PF-322` are substantive current evidence after that checkpoint and are not described as adversarially cleared.

## Global picture

Portfolio recommendations remain **3 `continue` + 10 `narrow`**. `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other line stays at `narrow`. No line currently clears the evidence and noncircularity bar for `pause-candidate`, `merge-candidate`, `split-candidate`, or a distinct `new-line-candidate`.

Nyman--Beurling has the strongest new positive source-sensitive bridge. `NB-091` showed that every fixed multiplicative-depth Gram band can remain source-silent after universal calibration. `NB-092` changes the regime: a canonical weighted-Möbius coefficient vector synthesizes an exact flat logarithmic annulus, false RH forces the raw growing-depth Gram toward a zero-frontier near-kernel, and the same off-critical Blaschke zero amplifies that annulus by `q^(2beta-1)`, forcing generalized determinant gain `(2beta-1)log q+O(1)`. Source sensitivity at growing depth is therefore established. The remaining theorem is destination-specific: prove that this relative raw/deflated gain survives diagonal correlation normalization strongly enough to force normalized radial entropy, or enters an inter-scale statistic with a proved lower bridge to the Nyman obstruction.

Farey receives a material correction rather than an incremental refinement. `FD-092` is withdrawn because its quarter-scale identity omitted the `m=1` image of the parent row `r=4`, whose missing contribution `(3/4)|H(B)|^2` can itself carry the full zero-frontier exponent. `FD-093` gives the corrected decomposition: apart from that one row-two/source-head atom, every dyadic terminal coordinate has an exact lower nonsquarefree repair, just as the odd repeated-prime channels do. The live Farey problem is now sharply split between excluding persistent frontier concentration on one source atom and upgrading repaired frontier-sized masks to fixed occupation of complete lower states.

Weil Inertia and Prime Flute each identify a proof-category boundary at an otherwise attractive endpoint. `WI-273` replaces the scalar positive archimedean gap budget by the exact positive-source Hankel norm and visible collar masses, strictly sharpening every positive-width one-signed gap. But that norm tends exactly to `pi/2` as the gap closes, so no further mass-only `L^2` operator-norm refinement can give a uniform gapless gain; the next theorem must use simultaneous cuts, local null-mode structure, or the full distributional equation. In Prime Flute, `PF-321` still makes any fixed superlinear decay of the shear-deleted intrinsic angle sufficient for trace class, but `PF-322` constructs explicit zero-mean physical-low packets for which the compactified transverse energy remains critical after the nominal `s_n^2` factor. A proof can no longer apply the small-argument transfer expansion uniformly before the physical `L/H` projection, seam normalization, and common pant completion; any superlinear gain must be earned downstream in the actual intrinsic angle.

Arithmetic Fidelity and Robin further isolate what information is genuinely missing. `AF-315` shows that six-point centered zero shapes remain exactly recoverable from `(s_2,s_3)`, but the generic quotient by common rotation is real three-dimensional and needs one complex invariant plus one real modulus; distinct-prime hypothetical hits automatically avoid the low-harmonic singular strata. This sharpens representation cost without touching first nontorsion incidence. `RE-095` gives an exact source-conditioned Robin bill: if bad prime hosts `q~X^(1/2)` occur with exponent `kappa(vartheta)`, then the residual frontier is `Theta>max(1/2+vartheta,123/200+kappa(vartheta)/2)`. Any fixed saving below the trivial `kappa=1/2` improves `173/200`, while repackaging the same ambient exceptional-set measure cannot supply that saving.

Three later findings sharpen the statistic that must be controlled without yet supplying the missing estimate. `MC-265` shows that even perfect first-order Legendre-sign balance leaves an even fixed-weight Krawtchouk coefficient parametrically too large for a termwise absolute-value proof of the support-matched high moment from `MC-264`; the live object is the signed aggregate Krawtchouk distance distribution, not pairwise bias. `VIS-205` identifies the dangerous fixed-scale shell energy, up to a factor four, with the two-sample weighted pair-crowding probability `Q(A)`; this is strictly cheaper than the previous one-sided nearest-neighbor criterion and creates a direct weighted-incidence target. `WP-283` closes every fixed bounded finite-memory scalar linear extraction of the Gamma/DC mode: compact memory gives only linear transfer-zero density, whereas the critical zero-frequency screen has `T log T` distinct-frequency density. Any linear escape must therefore become scale-nonlocal, or the architecture must become nonlinear or couple finite and archimedean sectors before scalarization.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` equivalent sharp discrimination boundaries for the matched endpoint packet. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at that aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-315` keeps exact six-point recovery in the two low harmonics but proves that the generic rotation quotient needs three real coordinates; rational-prime hypothetical hits avoid both singular harmonic strata. | Decide first nonlinear nontorsion incidence of the actual prime-log orbit with the centered polygon-zero manifold; more post-hit recovery coordinates do not address that gate. |
| `farey_discrepancy` | `narrow` | `FD-093` corrects the withdrawn `FD-092`: every nonhead dyadic terminal coordinate is exactly repairable at lower nonsquarefree scale, leaving only one row-two/source-head atom plus the lower-state composition problem. | Rule out a fixed frontier share concentrating on `|H(B)|^2`, or upgrade repaired frontier-normalized masks to fixed occupation of complete lower states. |
| `mobius_cancellation` | `narrow` | `MC-264` shows that support-matched moment order `2(t+1)` can cross the single-blind threshold, while `MC-265` shows that first-order shell balance is still far too coarse for a pointwise absolute-value proof of that estimate. | Control the signed aggregate fixed-weight Krawtchouk distance distribution, localize weighted pair mass near Krawtchouk zeros, or prove the shell high moment directly without absoluteizing off-diagonal terms. |
| `nyman_beurling` | `narrow` | `NB-092` constructs an exact growing-depth Möbius annulus direction on which false RH simultaneously creates a raw Gram near-kernel and polynomial Blaschke amplification, forcing generalized determinant gain. | Prove that the relative raw/deflated gain survives diagonal normalization into visible entropy/radial charge, or embed it in an inter-scale statistic with a lower bridge to the Nyman criterion. |
| `prime_circle` | `narrow` | `PC-277` closes broad normalized near-resonance regimes with exact-`Li` and rational-grid controls, leaving only an intermediate rational-microscopic scalar window. | Close that window with a stronger source-only control or find a zero-sensitive residual that survives it; otherwise leave scalar near-resonance for directional, microscopic, or cross-level data. |
| `prime_flute` | `continue` | `PF-321` makes any fixed superlinear seam-scale decay of the shear-deleted intrinsic angle trace class, while `PF-322` proves that such decay cannot be inferred from uniform smallness of the unprojected compactified physical-low cell form. | Propagate the explicit compactification-critical low packet through the actual pant quotient, seam normalization, physical `L/H` projection and common completion; prove the superlinear intrinsic-angle bound or the `PF-318` Hilbert--Schmidt fallback, and independently control `T_H`. |
| `prime_lattice` | `narrow` | `PL-293` gives the sharp generic `ell^(1/2)` boundary obstruction, and `PL-294` proves ordinary Beurling--Deny positivity fails before prime-power translations enter. | Use the actual rational-prime Weil eigen-equation to suppress the universal boundary channel, prove a source-specific sign theorem, derive a different translation identity, or bypass regularity/Hopf entirely. |
| `robin_extremal` | `narrow` | `RE-095` converts source-conditioned bad-host sparsity into the exact frontier `Theta>max(1/2+vartheta,123/200+kappa(vartheta)/2)`; any polynomial saving below the trivial host count lowers `173/200`. | Prove a prime-indexed/deformed-sample bad-host saving at some `vartheta<73/200`, using source or selector structure rather than another ambient exceptional-set estimate. |
| `visual_exploration` | `narrow` | `VIS-205` makes dangerous fixed-scale exact-shell energy equivalent up to factor four to the two-sample coefficient-energy crowding mass `Q(A)`, a strictly weaker target than one-sided nearest-neighbor crowding. | Prove `Q(A)->0` directly from the explicit prime-ratio taper weights at or below the current square-root-log boundary, or construct a source-valid lower-bound mechanism showing macroscopic pair crowding persists. |
| `weil_inertia` | `continue` | `WI-273` replaces the one-signed positive-tail mass bound by the exact Suzuki Hankel norm and collar masses, sharpening every positive-width gap while proving a sharp `pi/2` zero-gap Carleman wall. | Use simultaneous cuts, local null-mode structure or the distributional null equation to rule out the short-gap/gapless screened branch; separately use the `WI-269` PSD/nodal law against long-range compensation on sign-changing branches. |
| `weil_positivity` | `narrow` | `WP-283` extends the extraction no-go to every fixed bounded finite-memory scalar linear response: compact support has only linear notch density and cannot cancel the `T log T` critical zero-frequency screen while retaining DC. | Build an independently forced oscillatory infinite-memory/non-Stieltjes, nonlinear, scale-nonlocal, or pre-scalar finite--archimedean coupling whose sign does not encode the zero divisor or an RH-equivalent primitive criterion. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**A source-silent observable can become informative when its scale is changed, but the destination normalization can still erase the gain.** `NB-091` versus `NB-092` is now the cleanest example: every fixed-depth band can be calibrated away, while growing depth exposes an exact Möbius near-kernel and a false-RH Blaschke gain on the same direction. Prime Flute gives the complementary metric warning: a formally small geometric parameter does not imply a small physical angle until projection and normalization are applied. These results do not share an operator, but together they make the first downstream normalization step the correct place to test whether a new source signal survives.

**Closing a bulk mechanism is increasingly leaving a very thin carrier whose mathematical currency has changed.** Farey's broad dyadic terminal sector collapses to one source-head atom plus a composition modulus. Weil Inertia's positive-gap screening problem collapses, at zero width, to information discarded by every global mass-only Hankel norm. Robin's ambient exceptional set collapses to sparsity on prime-indexed deformed hosts. In each case, improving the exhausted bulk estimate is no longer the live question.

**Exceptional-event detection must match both combinatorial order and the statistic actually summed.** `MC-264` shows that high enough moment order can in principle detect one blind support, while `MC-265` shows that pairwise first-order balance still does not control the needed high-order transform term by term. `VIS-205` moves in the opposite direction by weakening the target from one-sided neighbor incidence to the exact quadratic pair mass consumed by shell energy. `AF-315` adds a representation analogue: exact post-hit recovery can remain available while the minimal quotient dimension grows, and none of that decides first incidence. The common discipline is to price the terminal event at the order and normalization actually consumed, not to substitute a stronger but mismatched proxy.

**Local scalar extraction is becoming a clearly bounded category rather than a generic architecture.** Weil Positivity has now excluded finite-state response, positive completely-monotone relaxation, and fixed compact-memory signed/oscillatory linear filters as ways to keep the Gamma/DC component while removing the zero field. `WP-283` shows that the obstruction is no longer positivity but frequency-density capacity. The remaining routes must pay explicitly for growing memory or leave scalar linear extraction altogether.

The existing `CLUE-cross-line-fidelity-realization-separation` remains the appropriate global handoff for the recurring distinction among source fidelity, target transport, destination realization and assembly. None of the new connections above shares one source family, operator, normalization and target strongly enough to justify a new transfer clue. The Graph-owned Riemann Atlas is also not used for current recommendations: its entrypoint and metrics advertise different stale source revisions (`4bc527...` and `834376...`), both far behind the canonical evidence synthesized here.

**Review identity is clean.** The independent checkpoint `14541823bf5dceadc7f75a32ff1cee5c117a2451` audits the contiguous research prefix through the reachable commit `22231d112589edadf06224293ec227257c76f30f`. `MC-265`, `VIS-205`, `WP-283`, and `PF-322` are later canonical evidence and remain explicitly uncleared by that checkpoint. No open review sidecar constrains the snapshot.

## Highest-leverage unresolved questions

1. **Weil Inertia gapless screening.** Go beyond the sharp `WI-273` mass-only Hankel wall and use the full first-crossing cut/null-equation family to decide whether a one-signed mode can remain gapless or short-gap-connected while screening every prime lag; treat sign-changing modes separately through the PSD/nodal compensation law.
2. **Prime Flute projected physical angle.** Resolve the accepted `research/prime_flute/clues/CLUE-direct-angle-dtn-principal-symbol-decoupling.md` downstream of `PF-322`: propagate the explicit seam-centered low packet through the completed physical angle and either prove any fixed superlinear seam-scale decay, obtain the `PF-318` mean-square fallback, or exhibit a genuine mixed-angle witness; independently close `T_H`.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp `Theta(X/a_X)` aperture, keeping positive-cascade non-excisability separate.
4. **Nyman normalized growing-depth gain.** Determine whether the `NB-092` Möbius-annulus generalized Gram gain survives diagonal normalization into a divergent source-specific entropy/radial charge, or construct an inter-scale target that provably consumes it.
5. **Farey head atom versus composition.** Exclude fixed frontier concentration on the single `FD-093` row-two/source-head value, or turn the exactly repaired lower nonsquarefree packets into fixed-strength iterable lower-state occupation.
6. **Möbius signed Krawtchouk high moment.** Prove or falsify the support-matched `2(t+1)`-moment estimate through the signed weighted distance distribution rather than first-order shell balance or termwise absolute values.
7. **Robin source-conditioned host sparsity.** In the residual depth-two channel, prove a polynomial saving in the number of bad prime-indexed deformed sample hosts at some `vartheta<73/200`.
8. **Visual weighted pair crowding.** Bound the two-sample dangerous pair mass `Q(A)` directly from the explicit coefficient profile, or show by a source-valid construction that it cannot vanish at the required scale.
9. **Arithmetic Fidelity first incidence.** Decide whether the actual prime-log one-parameter orbit can meet the centered five-/six-point polygon-zero manifolds; post-hit harmonic recovery and quotient compression are no longer the missing information.
10. **Weil Positivity beyond bounded-memory scalar extraction.** Produce an independently selected infinite-memory oscillatory/non-Stieltjes, nonlinear, or pre-scalar finite--archimedean architecture that retains zero-sensitive source information and supplies the sign without target insertion.

## Portfolio changes proposed

None. The new findings materially sharpen several terminal carriers and close additional proof subclasses, but none establishes saturation of an entire current line, mathematical redundancy between lines, a natural split, or a distinct question that no existing line can own. The three `continue` lines still have direct source-specific falsifiable gates; every `narrow` line retains at least one mathematically different residual carrier beyond its latest no-go result.