# Mathia Research State

Canonical evidence is synthesized through `2fea3e940ed55af648e54e39206a40d9a9a3cfb3`. The latest clean independent adversarial checkpoint is `f84f807889c2117d8e74cc87ecf36a8f843bc739`, which declares coverage through the reachable commit `992befdae1913c1dfed71be0492f6f017429a7fa`. There are no open `.review.md` sidecars. `MC-263` and `VIS-204` are substantive findings after that checkpoint and are used as current canonical evidence without being described as adversarially cleared.

## Global picture

Portfolio recommendations remain **3 `continue` + 10 `narrow`**. `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

Prime Flute makes the cleanest positive endpoint simplification. `PF-319` correctly showed that corridor form-smallness need not control the raw seam-normalized mixed precision block, but `PF-320` proves that the stronger raw estimate is unnecessary: multiplicative closeness of the whole positive forms controls their **intrinsic low/high angle** after each form normalizes by its own diagonal energies. With `beta_n=O(P_n^{-1})` and logarithmic retained-low rank, the actual hypercycle-shear correction already has squared Hilbert--Schmidt mass `O((1+log P_n)/P_n^2)`, exactly the `PF-318` endpoint budget, and positive local forms reassemble by contraction sandwich. The direct channel is therefore reduced to the shear-deleted local pant-angle family with actual seam energy and common finite-pant completion; `T_H` remains an independent channel.

Weil Inertia sharpens its source-specific one-signed gate rather than adding another generic positivity surrogate. `WI-272` extends the `WI-271` hard `h_0` support-gap theorem below the archimedean sign-change scale. For an internal gap of width `L`, the positive continuous-source budget is exactly priced by `B(L)=int_L^{h_0}w(h)dh`; complete prime screening requires `B(L)^2>=lambda_L lambda_R` together with a compatible left/right mass ratio. Because `B(L)` vanishes quadratically as `L` approaches `h_0`, every proper-aperture firstness margin excludes a nontrivial collar of screened gaps below `h_0`. The surviving one-signed escape is genuinely short-gap or gapless, where the singular short-range archimedean network may still pay every cut; sign-changing modes remain a separate PSD/nodal-compensation problem.

Farey also moves closer to a source-specific terminal obstruction. `FD-091` shows that every **odd** repeated-prime channel can strip the second prime copy and reembed four-adically at `C_p~4H/p^2`, preserving the floor quotient exactly and restoring a wholly nonsquarefree mask with only universal Jordan-weight distortion. For `p>=3` this is a genuine descent, and the normalized odd family has weights `p^{-4 sigma}`, summable already for `sigma>1/4`; the critical `sigma=1/2` normalization therefore has no predecessor-entropy loss. The obstruction is now split sharply between upgrading the odd full-frontier-exponent mask to fixed occupation/composable recurrence and understanding the dyadic channel, where the universal square repair consumes the entire scale gain.

Several `narrow` lines receive strong negative clarification rather than promotion. `NB-090` closes the entire current dyadic terminal-Gram escape under summable horizontal off-critical mass: every terminal correlation is `O_A(R^{-1})`, causal inner order supplies a uniform spectral floor, and the normalized determinant deficit stays `O_A(1)`. A Nyman obstruction must therefore exclude that source regime and become visible outside the exhausted terminal block, or move to nonterminal/inter-scale charge. `RE-093` shows that exact depth-`1/2` cross-scale incidence, common-source membership, orientation and full generalized event adjacency can all coexist with the same `173/200` prime-gap-moment frontier in a PNT-dense integer control; Robin now needs ordinary-prime arithmetic or actual CA selector information at the sampled points, not a richer abstract incidence ledger. `AF-313` proves that after a hypothetical five-prime zero, the pair of second and third harmonics reconstructs the entire unordered phase multiset except for a regular-pentagon fiber that rational-prime phases exclude; this is strong post-incidence fidelity, but it still does not decide the **first** nonlinear nontorsion incidence and becomes ill-conditioned near the exceptional fiber.

The two newest uncleared findings expose complementary method ceilings. `MC-263` shows that even an ideal sparse-family second moment at its natural family-size scale cannot certify blind-kernel triviality: one exact blind support contributes only one atom `k_y^2`, while at the live support `t~log log y` a scalar L2 argument would need normalized mean square below `1/binom(N_y,t)=y^{-Theta(log log y)}` merely to exclude that single exception. The Möbius exit must therefore become an exceptional-set/weight-enumerator/rank theorem, amplify one blind relation into detectable mass, prove logarithmic shell generation, or cancel the surviving blind mode directly; merely replacing the ambient large sieve by a natural sparse product-family L2 estimate is no longer enough. `VIS-204`, following `VIS-203`, proves that the dangerous `|xi|=O(y^{-2})` exact-difference shells have multiplicity at most four: the only high-multiplicity one-cancellation shells are prime/prime quotients separated from zero at scale `Omega(1/y)`. Thus Visual's remaining shell-energy problem is the aggregate mass of **many distinct** four-prime shells, cross-shell interference, or a genuinely signed taper, not hidden reinforcement by large exact-shell multiplicity.

Weil Positivity, Prime Lattice, Prime Circle, Analytic Frontier, and Xi Flow retain their previous discriminating gates. `WP-282` still closes every positive completely-monotone relaxation continuum as a Gamma/zero separator; `PL-293`--`PL-294` leave actual-eigenbranch cancellation/sign as the only way past the sharp generic boundary and semigroup-positivity controls; `PC-277` leaves only the intermediate rational-microscopic scalar window; `ANF-180`--`ANF-183` retain the sharp source-continuation aperture; and `XF-185`--`XF-186` remain locally well-conditioned but globally unassembled.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` equivalent sharp discrimination boundaries for the matched endpoint packet. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at that aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-313` proves exact recovery of a hypothetical five-prime zero configuration from `(F_P(2t),F_P(3t))`, after excluding the regular-pentagon torsion fiber for distinct rational primes. | Decide the **first** nonlinear nontorsion incidence of the actual prime-log orbit with the `k>=5` zero surface; post-hit recovery, harmonic non-recurrence, Baker-linear restrictions and topological rarity do not decide it. |
| `farey_discrepancy` | `narrow` | `FD-091` restores a genuine lower nonsquarefree frontier-sized mask in every odd repeated-prime channel and moves the normalized predecessor summability threshold to `sigma>1/4`, isolating `p=2` as scale-critical. | Upgrade the odd full-exponent mask to fixed occupation/composable recurrence, or extract a contradiction from the dyadic terminal carrier where universal square reembedding loses all multiplicative descent. |
| `mobius_cancellation` | `narrow` | `MC-263` proves that natural sparse-family L2 balance cannot exclude even one blind support at `t~log log y`; the zero-exception threshold is `1/binom(N_y,t)=y^{-Theta(log log y)}`. | Prove logarithmic shell generation, a max/large-deviation or weight-enumerator theorem strong enough to make the blind count `<1`, arithmetic amplification of one blind relation, direct rank control, or signed cancellation of the single surviving blind mode. |
| `nyman_beurling` | `narrow` | `NB-090` shows that under summable horizontal off-critical mass the **full** dyadic terminal correlation matrix remains uniformly nondegenerate and has bounded determinant entropy, closing scalar and terminal-matrix rescues together. | Exclude that source regime and make nonsummability visible outside the terminal block, or move deliberately to earlier indices, nonterminal/inter-scale correlations, aliased-floor excess, or another radial charge. |
| `prime_circle` | `narrow` | `PC-277` closes broad normalized near-resonance regimes with exact-`Li` and rational-grid controls, leaving only an intermediate rational-microscopic scalar window. | Close that window with a stronger source-only control or find a zero-sensitive residual that survives it; otherwise leave scalar near-resonance for directional, microscopic, or cross-level data. |
| `prime_flute` | `continue` | `PF-320` proves that the real hypercycle shear is endpoint-small in the **intrinsic physical angle** despite possible raw seam-block amplification, and positive local angle errors reassemble contractively. | Prove the weak-`S_1`/Hilbert--Schmidt envelope for the shear-deleted local pant angles with actual seam normalization and common completion, independently control `T_H`, then close infinite realization and signed reassembly. |
| `prime_lattice` | `narrow` | `PL-293` gives the sharp generic `ell^{1/2}` boundary obstruction, and `PL-294` proves ordinary Beurling--Deny positivity already fails after `a_BD≈0.1406`, before primes enter. | Use the actual rational-prime Weil eigen-equation to suppress the universal boundary channel, prove a source-specific sign theorem, derive a different translation identity, or bypass regularity/Hopf entirely. |
| `robin_extremal` | `narrow` | `RE-093` shows that exact depth-`1/2` incidence, orientation, common-source labels and complete generalized staircase adjacency remain compatible with the `173/200` moment barrier. | In `469/600<Theta<=173/200`, exploit **ordinary-prime** arithmetic at `eta_1^{-1}(eta_2(q))`, prime-race/height/skipped-span coupling, or genuine rightmost-maximizer compatibility rather than more abstract incidence bookkeeping. |
| `visual_exploration` | `narrow` | `VIS-203` shows autocorrelation tapers make exact shells noncancelling, while `VIS-204` caps every dangerous `O(y^{-2})` shell at four representations and removes high multiplicity as the source of the remaining energy. | Bound the aggregate energy/packing of many distinct dangerous shells, obtain uniform cross-shell interference, or use a genuinely signed/complex taper; scalar cutoff optimization and equal-shell multiplicity are closed. |
| `weil_inertia` | `continue` | `WI-272` prices sub-`h_0` one-signed support gaps by the exact positive archimedean tail and forces a width--mass compatibility law under complete prime screening. | Rule out the remaining short-gap/gapless one-signed screened network using the full cut/null-equation family, and separately use the `WI-269` PSD/nodal law against long-range compensation on sign-changing branches. |
| `weil_positivity` | `narrow` | `WP-282` extends the zero-screen no-go to arbitrary positive completely-monotone infinite-dimensional relaxation continua: their Stieltjes transfer cannot notch even one finite zero frequency while retaining DC. | Build an independently forced oscillatory/non-Stieltjes, scale-nonlocal, nonlinear, or pre-scalar finite--archimedean coupling whose sign does not copy the zero divisor or assume an RH-equivalent primitive criterion. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**Exact information after an event is not a theorem that the event cannot occur.** `AF-313` can reconstruct a five-prime zero configuration after the hit without excluding the first hit. `MC-263` gives the complementary exceptional-set warning: extremely strong average parity balance can coexist with one exact blind support unless the theorem crosses the single-exception threshold. `VIS-204` similarly removes large multiplicity from each dangerous shell without making the sum over many distinct shells small. These are different source families, so no theorem transfers directly, but they all forbid replacing an exact exceptional-event question by post-event fidelity, typical balance, or bounded local multiplicity.

**The destination-normalized metric continues to matter more than a stronger proxy.** `PF-320` is the cleanest example: the raw seam-normalized block may amplify, yet the actual intrinsic angle is Lipschitz under the whole-form sandwich and already meets the endpoint budget. Farey obtains the analogous benefit only after normalizing transported energy at the lower physical scale; `FD-091` then shows that even perfect quotient transport still needs recursive occupation in the destination state. Weil Inertia prices a cut by the proper-aperture ground-state margin rather than by support geometry alone. This reinforces the existing separation between source fidelity, target transport, destination realization and final assembly rather than suggesting a new common operator theorem.

**Several routes now have explicit information-class ceilings.** Nyman's whole terminal Gram is silent under summable horizontal zero mass; Möbius natural second moments cannot certify zero blind exceptions; Visual equal-shell reinforcement and multiplicity are both classified; Prime Lattice has sharp generic boundary/positivity controls; and Weil Positivity has exhausted positive relaxation even with infinite state. These are scoped no-go results, not evidence that the underlying RH-facing mechanisms are impossible. The next useful result in each case must change carrier or use source-specific structure, not optimize the already exhausted class.

**The existing global clue remains sufficient.** `CLUE-cross-line-fidelity-realization-separation` already asks for theorem-level separation of source fidelity, target transport, destination realization and assembly. The new similarities are strategically useful but do not share one source family, operator, normalization and target from which a destination-specific actionable transfer follows, so no additional Master clue is warranted.

**Review identity is clean.** The independent checkpoint `f84f807889c2117d8e74cc87ecf36a8f843bc739` audits the contiguous research prefix through `992befdae1913c1dfed71be0492f6f017429a7fa`, covering the new `AF-313`, `FD-091`, `NB-090`, `PF-320`, `RE-093`, `VIS-203`, and `WI-272` corpus together with earlier current evidence. `MC-263` and `VIS-204` are later canonical findings and remain explicitly uncleared by that checkpoint. No open review sidecar constrains the snapshot.

The Graph-owned Riemann Atlas remains navigation-only for portfolio decisions. Its entrypoint and metrics advertise different stale source revisions (`4bc527...` and `834376...`), both far behind current canonical evidence, so no recommendation is inferred from Atlas percentages or telemetry.

## Highest-leverage unresolved questions

1. **Weil Inertia short-range screening.** Use the full first-crossing cut/null-equation family to decide whether a one-signed mode can remain gapless or sufficiently short-gap-connected while screening every prime lag; treat sign-changing modes separately through the PSD/nodal compensation law.
2. **Prime Flute reference-angle endpoint.** Prove the shear-deleted local pant-angle singular-value envelope with actual seam energy and full common completion, then independently close `T_H` and the infinite/signed assembly gates.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp `Theta(X/a_X)` aperture, keeping positive-cascade non-excisability separate.
4. **Farey recursive eligibility.** Turn the odd-channel frontier-sized nonsquarefree mask into fixed-strength iterability or a composition theorem, and isolate whether the remaining dyadic terminal channel is genuinely fatal.
5. **Möbius exact blindness.** Replace natural L2 balance by a theorem that can detect a single blind support: logarithmic all-class generation, exceptional-set/weight-enumerator control, arithmetic amplification, direct rank, or signed cancellation of the surviving mode.
6. **Nyman beyond terminal Gram entropy.** Determine whether false RH forces a nonsummable effective horizontal zero statistic and make it visible in a nonterminal/inter-scale charge; otherwise abandon the exhausted terminal carrier.
7. **Robin ordinary-prime sampled selection.** In the `469/600<Theta<=173/200` band, control ordinary-prime gaps at the actual depth-2 sampled points with selector/rightmost-maximizer information that the PNT-dense integer matched control cannot reproduce.
8. **Visual dangerous-shell packing.** After `VIS-204`, bound the total weighted mass over many distinct `O(y^{-2})` four-prime shells, prove usable cross-shell interference, or construct a signed taper with arithmetically controlled shell phases.
9. **Weil Positivity beyond positive relaxation.** Produce an independently selected oscillatory/non-Stieltjes or genuinely global finite--archimedean architecture that retains zero-sensitive source information and supplies the sign without encoding the zero divisor.
10. **Arithmetic Fidelity first incidence.** Decide whether the actual prime-log one-parameter orbit can meet the five-point polygon-zero manifold at all; low-harmonic reconstructability and algebraic-linear relation theory are now known to act on the wrong side of that first-incidence gate.

## Portfolio changes proposed

No recommendation change is proposed. Weil Inertia remains `continue` because successive matched controls have been replaced by increasingly source-specific first-crossing constraints, culminating in a quantitative sub-`h_0` screening tariff. Prime Flute remains `continue` because `PF-320` removes a false metric obstacle and leaves two canonical angle problems rather than a representation ambiguity. Analytic Frontier remains `continue` because its sharp source-continuation aperture is unchanged and still gives a direct falsifiable endpoint test.

Among the `narrow` lines, Farey has the strongest positive movement but still lacks fixed-strength recursive occupation or an alternative composition theorem. Möbius, Nyman, Robin, Visual, Arithmetic Fidelity, Prime Lattice and Weil Positivity all gained useful method boundaries that make their next tests more precise without justifying pause: each retains a source-specific carrier not covered by the negative control. Prime Circle and Xi Flow remain narrower exploratory routes with explicit unresolved windows.

No Research Watch rotation is proposed. None of the currently active research lines meets the strong `pause-candidate` or `merge-candidate` gate needed to free capacity safely, and the dormant valid lines do not justify violating that gate merely because one of them carries a `continue` recommendation. No new Master clue is proposed.