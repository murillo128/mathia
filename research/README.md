# Mathia Research State

## Global picture

This snapshot is reconciled against canonical research evidence through `57d54f23697237dc05c9c542b50444db722842f9`. Mathia has canonical evidence in all thirteen research lines. The portfolio remains **3 `continue` and 10 `narrow`**: `analytic_frontier`, `prime_flute`, and `weil_inertia` remain `continue`; the other ten remain `narrow`. No line currently meets the evidence bar for merge, pause, split, replacement, or a new standalone line, and no Research Watch rotation is justified.

The strongest program-level change is that several formerly qualitative gates have become exact destination-space or source-cost statements. Analytic Frontier now identifies the bow target with near-nullspace alignment of the actual post-sieve innovation in a normalized moving-window Gram operator. Prime Flute has crossed the actual critical Jacobi kernel gate: the growing zero mode has Bessel-four scaling and the exact parity chains retain a sharp square-root separated window `R<3`; the unresolved fixed-axis issue has moved to the actual relative seam and its polar transport. Weil Inertia has converted free remote completeness into a source tariff: super-bow displacement costs superpolynomial weighted mass, and Gevrey detectors force an order-`M` dyadic screen into polylogarithmic bow distance. These are genuine contractions of the three `continue` frontiers.

Among the `narrow` lines, Farey Discrepancy now has a real source-coherence gain across `N` and `2N`, but only at constant level; Nyman--Beurling has proved that both linear normal-equation amplification and arbitrary positive multichannel lifts are saturated and has calibrated fixed-scale visibility as essentially closure-strength; Möbius Cancellation has found a squarefree-specific logarithmic prime split that removes the smooth-support obstruction but relocates the problem to short-interval parity cancellation of a logarithmically rough Möbius block. Xi Flow closes the derivative-jet escape all the way through full polynomial degree away from the antipodal visibility layer, while Weil Positivity closes not merely Dixmier-style residues but every finite nonzero scalar singular-trace readout of the exact linear critical carrier.

There is no open `.review.md` sidecar in the current tree. The latest adversarial checkpoint on the branch declares `Adversarial-Reviewed-Through: 963934159da1e043f64b568bd23288d9a657ebc4`; that prefix includes `PF-253`. `VIS-126`, `PF-254`, and `MC-180` are later canonical evidence and should not yet be described as independently checkpoint-covered. The Graph Curator's recent structural migration changes no mathematical claims, and Atlas metrics remain non-authoritative for portfolio decisions.

## Focused continuations

### Analytic Frontier — `continue`

`ANF-150/151` already showed that correct prime support, natural magnitude, the exact logarithmic carrier, finite Ramanujan data, and even a slowly growing Hardy--Littlewood local hierarchy can coexist with full diagonal bow variance. `ANF-152` now gives the exact destination geometry. After diagonal normalization the moving-window form is `z^*Cz` with `C` positive semidefinite and unit diagonal, while the diagonal obstruction is `z^*z`. Achieving the bow target therefore forces `1-o(1)` of the weighted actual post-sieve innovation into eigenspaces of `C` whose eigenvalues tend to zero; equivalently it must asymptotically saturate the `-1` floor of the off-diagonal part.

The first unsupported implication is no longer generic negative covariance or additional local prime statistics. Either prove a source-specific mechanism forcing this distinguished-twist near-kernel alignment, preserving the exact `Lambda-Lambda^sharp` relation, or prove any fixed positive spectral delocalization and thereby kill the bow branch. Separately, the positive-slack cascade still has to be shown fatally non-excisable rather than removable from a near-extremizer. Both are source-facing, falsifiable gates, so the line remains `continue`.

### Prime Flute — `continue`

The previous uncertainty about the actual inverse-square Jacobi edge has been substantially resolved. `PF-252` uses the exact growing zero solution to perform a ground-state transform; the resulting weighted path has mass and conductance of order `j^3` and Bessel-four diffusive scaling. `PF-253` then proves two-sided separated fractional-kernel decay for the actual parity chains and the sharp criterion `E_d J_epsilon^sigma N^R in S_2` exactly for `R<2+2 sigma`. For the square root this is the actual-chain window `R<3`, comfortably above the `R>1` threshold needed by `PF-243`.

`PF-254` isolates what is still genuinely noncommutative. On bounded positive regularizations the normalized Robin injection factors exactly as `K^{-1/2} h(R) U`, with `h(lambda)=sqrt(lambda)/(1+lambda)`, relative seam `R=K^{-1/2}QK^{-1/2}`, and polar partial isometry `U`. The positive amplitude therefore always has the favorable square-root edge; the remaining high-to-low risk is the actual complete-lift relative-seam/Jacobi comparison and the spectral locality of `U`, together with passage to the unbounded form realization. If that combined factor preserves any uniform `R>1`, the frontier moves to `T_a`, hypercycle/Hardy corrections, physical `P/H` recoupling, finite pants, neighboring cells, and global weak-`S_1` reassembly. This is currently the clearest positive local theorem chain in the portfolio.

### Weil Inertia — `continue`

`WI-218/219` established that a fixed power-scale band costs order-`M` screen complexity and that this tariff is sharp in the free co-localized linear class; `WI-220` showed that support separation alone cannot localize arbitrary-coefficient screens. `WI-221` supplies the missing quantitative cost for genuinely remote screens: if all normalized frequencies are at distance `R_M -> infinity`, cancellation of the bow forces weighted coefficient/source mass larger than every fixed power of `R_M`; at bounded radial depth the required screen count is correspondingly superpolynomial in the displacement.

`WI-222` strengthens this with Gevrey detectors. Under the dyadic zero-screen hypothesis, order-`M` radial-weighted source is forced into physical ordinate radius `O_epsilon(M (log T)^epsilon)` for every fixed `epsilon>0`, reducing the source-transport gap from subpower width to polylogarithmic bow distance. The remaining implication is now sharply source-specific: turn that weighted localization into a raw admissible zero-count/multiplicity/radial-cost contradiction, or show that non-dyadic tails or depth can still pay the bill. The co-localized free `Theta(M)` companion remains a control, so localization alone is not yet a proof.

## Narrow lines

**Arithmetic Fidelity.** `AF-231` shows that a power-decaying downstream observable can have a strictly cheaper first-order information price under absolute target error, even when the target is exactly injective. `AF-232` then removes that apparent power discount under relative/logarithmic target error. The information budget is therefore a property of the target together with its operational fidelity geometry, not of prime identity or damping alone, and the same phase laws occur in matched non-prime sources. The live route is an RH-facing sufficient statistic whose lower information price is derived in the metric actually strong enough for the final analytic conclusion.

**Farey Discrepancy.** `FD-013` shows that reflection symmetry, the complete one-gap multiset, and every adjacent gap-pair count can still leave cumulative quadratic discrepancy differing by an unbounded factor, so first-order local chronology is not coercive. `FD-014`, resolving the accepted coherent-horizon clue, gives the first clean positive multiscale constraint: one cumulative sequence cannot jointly saturate the sharp GCD dual bound at both `N` and `2N`, yielding a uniform constant deficit and additive dyadic-block control. That does not improve a Mertens exponent. The live question is whether a compressed growing hierarchy of coherent horizons can convert this genuine source coherence into scale-sensitive Franel/Mertens control before it becomes the lossless source reconstruction of `FD-009`.

**Möbius Cancellation.** `MC-178/179` show that the Chinis one-large-prime architecture gives only subpower improvement at polynomial windows and that support-only treatment of a smooth remainder forces a growing extraction depth. `MC-180` finds a Möbius-specific escape from that method obstruction: at cutoff `y=c log X`, squarefree support makes the small-prime factor live on only `X^{o(1)}` primorial divisors and removes the pure smooth sector from `[X,2X]`, while all larger primes form one logarithmically rough Möbius block and the variance transfer back to full Möbius loses only subpower. The hard currency is now explicit rough prime-factor parity. A useful theorem must prove a fixed-power short-interval second-moment gain for that rough signed block by parity-sensitive source arithmetic; a standalone global power bound would transfer almost directly to Mertens and is not a cheaper intermediate theorem.

**Nyman--Beurling.** `NB-012` exactly diagonalizes arbitrary linear combinations of the normal equations by Ramanujan sums and proves that the prime ensemble of `NB-011` already saturates the leading logarithmic target-load efficiency; adding composite equations cannot buy another unbounded factor. `NB-013` extends the ceiling to arbitrary positive-semidefinite multichannel lifts. `NB-014`, resolving the accepted visible-defect clue, gives the exact finite-section enrichment conversion but also shows that any uniform positive visibility fraction at arbitrarily large fixed probe depths is already closure-strength and that the full frame norm is exactly `M-1` throughout a broad lcm regime. The surviving route must therefore be `N`-dependent and residual-specific: a scale-coupled visible fraction plus compressed-frame bound on the genuinely new quotient that yields a quantitative distance decrement without assuming density.

**Prime Circle.** `PC-235` closes the most natural fixed-base escape from the Cauchy-packet algebra. The fresh-fiber coordinate acts exactly as a Laurent shift, the common-anchor chord observable becomes the discrete Laurent Laplacian, smooth multiplier tangents are unbounded on the source packet, and the bounded renormalized commutators classicalize to ordinary polynomial-coefficient shift calculus. A survivor must use genuinely non-Laurent or simultaneously growing multi-prime coupling, not another fixed-base scalar/mode-shift refinement.

**Prime Lattice.** `PL-245` shows that affine Weyl symmetry leaves only one-variable null-root freedom. `PL-246` audits the explicit affine selector: it canonically fixes a scalar Pochhammer factor, while the zero-bearing function-field block becomes invariant only after Weil purity is supplied as input. `PL-247` locates the first genuine dimensional escape in indefinite Kac--Moody type, whose canonical correction has full-rank imaginary-root support, but exactly there the simple affine coefficient positivity fails and no canonical map from rational-prime exponent space to the root system is supplied. The line therefore needs both a source-derived arithmetic dictionary and a sign/purity/positivity mechanism surviving the same principal Riemann readout; multivariable support alone is insufficient.

**Robin Extremal.** `RE-012` proves that inside a clean prime-free chamber the entire local mixed-race profile is determined by one value plus the CA selector, so higher local derivatives provide no independent arithmetic hierarchy. `RE-013` then prices the selector itself: no fixed prime-power depth resolves it at logarithmic accuracy, the maximal active depth is `log_2 X + log_2 log X + O(1)`, and the deepest tail is quantized by powers of two. The next theorem must therefore control the nonlocal CA-selected boundary primes/events or rule out concentration in the exceptional higher-layer/tied chamber, using input genuinely weaker than a global RH-equivalent prime-race bound.

**Visual Exploration.** The support-edge thread is now calibrated through three additional universal effects. `VIS-124` makes Montgomery's pair weight an exact Laplace convolution and exposes a universal `1/log T` edge rounding; `VIS-125` shows that a hard source window adds squared-sinc leakage of order `log M/M`, while a sufficiently smooth taper removes the logarithmic penalty; `VIS-126` shows that smooth Riemann--von Mangoldt density drift can simultaneously be made negligible, with a broad polylogarithmic-height design corridor. The accepted clue is therefore correctly narrowed to finite-CUE effective-size/arc matching, counting-function fluctuations, exact covariance/diagonal conventions, and only then a preregistered arithmetic residual on fresh zeros. There is still no zeta-specific positive signal, so the line remains `narrow`.

**Weil Positivity.** The strengthened `WP-229` closes the scalar singular-trace shortcut much more generally than its earlier Dixmier formulation. The exact linear critical carrier has singular values asymptotic to `sqrt(log n/n)`, and every positive singular trace finite on it must assign mass zero because direct-sum scaling conflicts with trace additivity. Squaring yields a singularly traceable positive bath, but every finite scalar singular trace on it kills the trace-class repeated-prime-power tower and is therefore indistinguishable from a primes-only control. The surviving category must preserve finite-prime, repeated-prime-power, mixed-prime, and archimedean relations before scalarization through a genuinely operator-valued/modular/semifinite/distributional construction, and still prove the Weil sign.

**Xi Flow.** `XF-149` shows that root-scale derivatives merely trade derivative depth against antipodal reach. `XF-150` closes the apparent full-jet loophole: an exact binomial frequency bound combined with the aperture bound makes even the complete normalized real spatial jet through degree `N-2`, observed through the entire future, exponentially transition-blind on every fixed proper aperture and superpolynomially blind until support reaches the same `O(L sqrt(log N/N))` antipodal layer. More derivatives or same-slice local richness therefore do not alter the target-side geometry. The line needs source-faithful access to that layer without global/exponential inversion, or a source theorem excluding the maximal-contact matched packet.

## Research-line portfolio

| Research line | Recommendation | Strongest current boundary | First unsupported implication |
| --- | --- | --- | --- |
| `analytic_frontier` | **continue** | `ANF-152` turns the bow target into near-nullspace alignment of the actual post-sieve innovation in the exact moving-window Gram form. | Prove source-forced near-kernel alignment or quantitative delocalization, and separately settle fatal non-excisability of the surviving cascade. |
| `arithmetic_fidelity` | **narrow** | `AF-231/232` show that the information phase changes with the target fidelity metric and is not intrinsically prime-specific. | Derive a lower-information RH sufficient statistic in its natural final distortion geometry. |
| `farey_discrepancy` | **narrow** | `FD-014` proves a uniform coherent `N`/`2N` GCD-duality saturation gap, while `FD-013` kills first-order local-gap coercivity. | Turn a compressed growing coherent-horizon hierarchy into scale-sensitive Franel/Mertens control before source reconstruction. |
| `mobius_cancellation` | **narrow** | `MC-180` removes the smooth-support obstruction by a logarithmic prime split and isolates a rough Möbius parity block with subpower transfer cost. | Prove a genuinely parity-sensitive fixed-power short-interval variance theorem for the logarithmically rough block. |
| `nyman_beurling` | **narrow** | `NB-012/013` saturate linear and PSD multichannel amplification; `NB-014` calibrates fixed-scale visibility as closure-strength and the generic frame cost as sharp. | Obtain an `N`-dependent residual-specific visibility/compressed-frame return inequality yielding real distance decrement. |
| `prime_circle` | **narrow** | `PC-235` classicalizes the canonical fresh-scale mixer/tangent as Laurent-shift calculus. | Produce source-forced non-Laurent or simultaneous multi-prime coupling that survives packet scalarization. |
| `prime_flute` | **continue** | `PF-252/253` prove Bessel-four actual-chain smoothing with sharp square-root window `R<3`; `PF-254` isolates all remaining orientation risk in relative-seam polar transport. | Compare the actual relative seam with the Jacobi edge and control the polar transport strongly enough to retain some `R>1`, then complete physical/global reassembly. |
| `prime_lattice` | **narrow** | `PL-245/246` close affine null-root selection as scalar/purity-dependent; `PL-247` finds multivariable indefinite support but loses simple positivity and lacks a prime dictionary. | Couple rational-prime data canonically to a nonseparable target channel and prove an independent sign/purity/positivity law there. |
| `robin_extremal` | **narrow** | `RE-012/013` close the local jet and price clean-selector depth to essentially the full active prime-power stack. | Control the selected boundary-prime/event process or exceptional chamber with weaker-than-RH input. |
| `visual_exploration` | **narrow** | `VIS-124/125/126` calibrate weight smoothing, source-window leakage, and smooth-density drift and expose a common mesoscopic test corridor. | Complete the finite-CUE/window/counting-fluctuation covariance transfer before any fresh zeta residual test. |
| `weil_inertia` | **continue** | `WI-221/222` turn remote completeness into superpolynomial source cost and localize any dyadic weighted screen to polylogarithmic bow distance. | Convert weighted polylog localization into an admissible zero-count/multiplicity/radial contradiction, including remaining source tails. |
| `weil_positivity` | **narrow** | strengthened `WP-229` rules out every finite nonzero scalar singular trace on the linear carrier and shows all finite scalar singular traces on the positive bath forget prime powers. | Build a relational finite--archimedean completion before scalarization and derive the Weil sign in that same channel. |
| `xi_flow` | **narrow** | `XF-149/150` show even full-degree root-scale derivative jets remain transition-blind away from the antipodal visibility layer. | Obtain source-faithful antipodal access without unstable tomography, or exclude the matched packet source-side. |

## Cross-line routing and portfolio discipline

No new Master clue is created in this pass. The new actionable questions are already routed where they belong: Prime Flute's accepted critical-Jacobi clue has been updated through `PF-254` to target the actual relative seam and polar transport; Farey's accepted coherent-dyadic clue is resolved by `FD-014` and explicitly leaves the growing-horizon problem; Nyman's accepted visible-defect clue is calibrated by `NB-014`; Visual's accepted Montgomery support-edge clue now incorporates `VIS-124`--`VIS-126`. The similarities among near-nullspace alignment, parity-sensitive rough variance, coherent-horizon compatibility, residual visibility, and weighted zero-screen localization are useful program-level controls, but there is no theorem-level dictionary strong enough to justify a new cross-line handoff.

All thirteen line-specific Research Watches remain enabled. No active line is a strong `pause-candidate` or `merge-candidate`, and no dormant initialized line has evidence strong enough to displace one of them. The task portfolio therefore remains unchanged.