# Mathia Research State

Canonical evidence synthesized through `49f09491188052599b9eca5964d8a7f53fb3fccd`.

## Global picture

Mathia remains a thirteen-line portfolio with **3 `continue` and 10 `narrow`** recommendations. The three `continue` lines are `analytic_frontier`, `prime_flute`, and `weil_inertia`; the other ten remain `narrow`. No current line clears the evidence and noncircularity bar for `promote`, `pause-candidate`, `merge-candidate`, `split-candidate`, or a new standalone line.

The strongest program-level update is that several remaining finite or local nuisances can now be cleanly separated from the real endpoint burden. `AF-288` shows that a common clock shift acts as a vertical-translation gauge and therefore does not change the RH yes/no statement; multiplicatively independent source anchors can also pin the origin when an absolute phase is genuinely needed. `NB-065` strengthens the target-side picture in the opposite direction: persistent Nyman leakage has only a **finite target-weighted forward-transmission budget**. If a fixed fraction of the distinguished target repeatedly lies above singular floors `eta_R` with divergent `sum eta_R^2`, persistence is impossible. The live obstruction is therefore not finite observability or generic bad conditioning, but repeated alignment of the canonical target with source-generated sectors whose transmission collapses fast enough.

Prime Flute has also moved from qualitative scalar attenuation to an exact finite Green-response calculus. `PF-303` proves that the assembled scalar Robin killing vector is the unit Green-potential currency, `G_N d = 1`. `PF-304` upgrades this to `P_N=G_ND_N`, a reversible Markov kernel with invariant weight `d`, hence a contraction on every weighted `ell^p(d)`. Scalar forcing controlled in the natural killing-weighted currency therefore has depth-uniform response without any spectral-gap theorem. This is a real bridge, but not yet the physical result: the decisive question is whether the actual mixed `P/H` forcing and nonconstant-mode reassembly fall inside a comparable norm or regenerate a noncompact escape.

A second cluster of findings makes source aggregation itself the issue. `FD-066` gives the current direct Farey packet a sharp coherence requirement: under the existing normalization ledger, the source-coherence exponent must beat `2(1-Theta)` to improve the packet route. `MC-238` shows that the proposed smooth-dilation averaging in Möbius has an exact character blind spot: all selected residues stay in the coset generated by small primes, so every character in the annihilator sees no Fourier cancellation at all. `VIS-182` proves that any fixed bounded-gap prime family is diluted in the full prime prefix, while `RE-067` shows that bounded first-layer-only Robin packets are charge-silent unless they touch a lacunary fringe host. These are different operators, but the same methodological warning recurs: a finite or locally coherent source fragment is not enough unless the proposed readout sees the aggregate source in the exact family selected by the target.

The completed-Weil side remains the most endpoint-adjacent. `WI-253` still forces one first global zero mode to satisfy a continuum of sliding source-symbol inequalities across all smaller apertures, while `WI-251` supplies the complementary signed radial-flux budget for the first odd mode. Prime Lattice now moves the rigorously transferred finite frontier: `PL-283` shows that the literature's computer-assisted support-`0.8` lower bound transfers directly to Suzuki's canonical Friedrichs operator, so any first strict-negative aperture satisfies `a_->0.8`. The numerical certificate is external, non-peer-reviewed, and not independently reproduced by Mathia; its value is to advance the finite boundary, not to supply the missing aperture-uniform mechanism.

The common obstruction is therefore sharper than “preserve arithmetic information.” Exact finite recovery, quotienting a nuisance gauge, abundant local structure, fixed-window positivity, or a well-controlled scalar response can all hold while the decisive target relation is still lost at global assembly. The live problems are increasingly **source-selected and target-conditioned**: stable infinite transport, aggregate signed amplitude in the exact selected family, canonical critical topology, full operator reassembly, and incompatibility of one distinguished target with the matched controls.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make the matched endpoint packet quantitatively sharp: derivative order `Theta(K/a_X)` and physical aperture `Theta(X/a_X)`, with `a_X ~ sqrt(X log X)`, are equivalent resolution boundaries for that adversarial geometry. | Prove that the actual prime-minus-rough source cannot occupy the matched carrier geometry at the critical aperture, or obtain an equivalent source-forced phase/curvature observable. Fatal non-excisability of any surviving positive cascade remains a separate gate. |
| `arithmetic_fidelity` | `narrow` | `AF-282`--`AF-287` close finite known-prefix label spacing, mixed degree, raw row count, and timing-jitter scale. `AF-288` further shows that a common-clock offset is an RH-invariant vertical-translation gauge and can be pinned by source anchors if absolute phase is needed. | Obtain a uniform inverse modulus for unknown or infinite sources and show that the downstream target is stable under the actual source-compatible acquisition map. Common clock origin itself is no longer an RH information bottleneck. |
| `farey_discrepancy` | `narrow` | `FD-062`--`FD-065` cross host scarcity and close polynomial Schur-head enlargement as an exponent fix. `FD-066` identifies the direct coherent-packet threshold: the source-coherence exponent must exceed `2(1-Theta)` under the current normalization ledger. | Beat that source-coherence threshold in the actual family, or change currency to a source-coupled same-horizon denominator, corridor correlation, or row interaction. More scalar head optimization does not move the boundary. |
| `mobius_cancellation` | `narrow` | `MC-232`--`MC-237` reduce the hard coordinates to selected Möbius progressions and close separate-sign dense-model and generic smooth-modulus variance as fixed-power solutions. `MC-238` adds an exact small-prime subgroup obstruction to smooth-dilation averaging. | Determine the annihilator of the small-prime-generated subgroup for the actual shell moduli and either eliminate its nonprincipal modes, control their signed amplitude separately, or prove quantitative Fourier expansion on the surviving orbit. Averaging the dilations is not automatically equidistribution. |
| `nyman_beurling` | `narrow` | `NB-061`--`NB-064` isolate the forward-memory carrier and show that persistence requires the canonical target to track its moving near-kernel. `NB-065` strengthens this to a finite total target-weighted transmission budget. | Prove an arithmetic lower bound on accumulated target transmission: enough target mass must repeatedly lie above singular floors whose squared sizes have divergent sum. Without that, square-summably collapsing near-kernel tracking remains a viable escape. |
| `prime_circle` | `narrow` | `PC-269`--`PC-271` classicalize rational-limit, fixed-resonance, and first growing-resonance bulk geometry as torus/dual-lattice resolution phenomena. | Show that shortest-vector directions, lift statistics, or another coupled observable along the actual prime triples remain source-specific after the appropriate geometric matched control, or leave this Fourier-resolution class. |
| `prime_flute` | `continue` | `PF-302` rules out indefinite scalar round-trip survival. `PF-303` identifies the Robin killing vector as an exact unit Green potential, and `PF-304` turns `G_ND_N` into a reversible Markov kernel contracting every weighted `ell^p(d)`. | Derive the complete normalized `P/H` Schur return/reassembly equation and prove that its actual mixed forcing is controlled by the scalar killing-weighted currency strongly enough to control the physical shorting deficit. Otherwise the scalar theorem remains calibration only. |
| `prime_lattice` | `narrow` | `PL-280`--`PL-282` give monotone localized Morse index, exact exhaustion, and finite certification at any fixed strictly positive aperture. `PL-283` transfers the literature's computer-assisted support-`0.8` certificate to Suzuki's canonical operator, giving `A_0.8 >= 8.9e-18 I` and hence `a_->0.8`. | Reproduce the tiny external finite certificate independently if it is to be used computationally, but mathematically the main gate remains aperture-uniform source control preventing the first forbidden crossing as infinitely many prime-power atoms enter. Better fixed-window certificates do not scale to RH. |
| `robin_extremal` | `narrow` | `RE-060`--`RE-066` classify bounded local cancellation, quiet capacity, and the sparse positive-charge/debt ledger. `RE-067` shows bounded first-layer-only packets are charge-silent unless they touch a lacunary fringe host. | Find a different invariant for the generic charge-silent `0->0` branch, or prove that negative source charge, sparse fringe events, or growing packet complexity cannot support the required global drift. The selected residual charge no longer sees the generic bounded first-layer takeover. |
| `visual_exploration` | `narrow` | `VIS-179`--`VIS-181` close continuous variance through the natural linear-support critical regime for `alpha>=1/2`. `VIS-182` further proves that every fixed bounded-gap family is diluted in the full prime prefix. | Test expanding near-diagonal/growing-gap aggregate coherence or leave quadratic continuous variance for growing-complexity, discrete/Gram, rare/unbounded, or hybrid prime/zero observables. A fixed finite gap family cannot explain a source-specific residual. |
| `weil_inertia` | `continue` | `WI-250`--`WI-251` reduce the first odd crossing to an exact signed radial source-flux budget and `WI-252` closes generic positivity-preserving ground-state theory. `WI-253` adds a first-global-crossing continuum of explicit sliding source-symbol inequalities against one nonnegative spectral density. | Test whether the joint `r`-dependence of `Sigma_r`, especially its prime-power threshold changes, is compatible with one compactly supported first zero mode and its eigen-equation; in parallel, keep the signed radial-flux route source-specific. |
| `weil_positivity` | `narrow` | `WP-266` gives source-native regulator-covariant connected centering below critical scale but critical transport is nonclosable. `WP-267` proves standard linear-relation closure makes the first prime layer maximally multivalued and erases its canonical mixed-prime coupling. | Introduce a genuinely source-derived critical boundary/cohomological structure **before** closure, compatible with half-density, closability, mixed-prime coupling, Gamma/polar completion, and Weil orientation. Ordinary relation closure is closed as an escape. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform linear conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support search and global assembly across multiple/multiscale packets, justify the complex samples from the physical Xi/Jacobi source, and transport recovered source control to a zero-sensitive quantity. |

## Cross-line structure

**Finite acquisition is separating from target transport.** `AF-288` removes the common-clock origin as an RH-level information obstruction, while `NB-065` shows that a persistent Nyman defect can survive only by spending a finite target-weighted transmission budget on increasingly weak singular sectors. `XF-185`--`XF-186` make the same broad distinction for known finite Xi packets. The useful next theorem is therefore rarely another injectivity result; it is a uniform statement that the distinguished target cannot keep escaping through the source-generated weak directions of the infinite transport.

**Scalar response is no longer the same problem as physical assembly.** `PF-303`--`PF-304` solve the finite scalar Green response much more strongly than a spectral-gap estimate would: the actual Robin killing stochasticizes the Green operator and supplies weighted `ell^p` contractions. This sharply changes the Prime-Flute gate. The question is now whether the true `P/H` Schur forcing is measured in that currency and whether nonconstant reassembly preserves the contraction. A failure there would identify the precise operator component that carries the remaining roughness.

**Aggregate source visibility is becoming an explicit prerequisite.** `FD-066` gives a power threshold for coherent Farey packets; `MC-238` exhibits exact character directions invisible to smooth-dilation averaging; `VIS-182` kills every fixed bounded-gap explanation in the full prime prefix; `RE-067` makes generic bounded first-layer Robin packets invisible to the selected residual charge. These are not interchangeable theorems, but together they rule out a common style of argument in which many locally meaningful pieces are assumed to become a globally informative source statistic automatically.

**First-crossing problems are increasingly about uniform source control, not finite localization.** `PL-283` pushes the canonical certified positive window to `0.8` by an exact transfer of an external computer-assisted result, while `PL-281` already prevents a genuine negative defect from hiding at infinite radius. Weil Inertia attacks the same broad first-crossing frontier from a different operator with the signed radial budget of `WI-251` and sliding family of `WI-253`. No certificate transfers between the lines yet: the operators, normalization, and candidate mode differ. The shared conclusion is only that another better finite window is not the missing asymptotic theorem.

**Matched controls must grow with the readout complexity.** Prime Circle has pushed bulk resonances into classical dual-lattice geometry, Visual now excludes fixed bounded-gap families as an explanation for the full source, and Analytic Frontier has an explicit sharp aperture at which its adversarial packet finally becomes visible. A useful residual must survive the matched representation geometry at the same growing complexity scale as the proposed source discriminator.

## Highest-leverage unresolved questions

1. **Weil Inertia joint first-crossing incompatibility.** Starting from `WI-253` and the proposed [first-global-crossing sliding-autocorrelation clue](weil_inertia/clues/CLUE-first-global-crossing-sliding-autocorrelation-symbol.md), determine whether one compactly supported first zero mode can satisfy the whole `Sigma_r` family across prime-power threshold changes. Keep `WI-251` as the complementary signed first-odd flux route rather than collapsing the two families prematurely.
2. **Prime Flute physical return/reassembly.** Use the exact Markov/Green structure of `PF-303`--`PF-304` inside the accepted [source-weighted return-potential clue](prime_flute/clues/CLUE-source-weighted-return-potential-controls-mixed-response.md): derive the full normalized `P/H` return and decide whether the actual mixed forcing has bounded `D_N^{-1}`-weighted mass in a norm strong enough for the physical shorting deficit.
3. **Analytic Frontier at the sharp source aperture.** Prove a source-specific carrier-placement, phase, curvature, or equivalent signed observable at physical separation `Theta(X/a_X)` that the matched endpoint packet cannot reproduce, and keep non-excisability as a distinct final gate.
4. **Nyman accumulated target transversality.** Turn `NB-065` into a contradiction by proving that the canonical target carries enough mass above singular floors `eta_R` on enough scales that the resulting `sum eta_R^2` budget diverges. This is sharper than proving good conditioning of `Gamma_R` itself.
5. **Möbius/Farey aggregate source gate.** In Möbius, determine the small-prime subgroup annihilator for the source-selected shell moduli before relying on smooth-dilation averaging. In Farey, beat the explicit `2(1-Theta)` coherence threshold or change the denominator/occupation currency. Both lines now have concrete reasons why a stronger version of the current generic averaging step may still miss the target.
6. **Canonical global transport after local classification.** Prime Lattice needs aperture-uniform first-crossing control beyond finite certificates; Robin needs an invariant for the charge-silent branch; Weil Positivity needs source structure before critical closure; Visual needs aggregate or growing-complexity source coherence. These are global source/target compatibility problems, not missing finite witness theorems.

## Portfolio changes proposed

No recommendation changes are proposed. `analytic_frontier`, `prime_flute`, and `weil_inertia` remain the three `continue` lines because each has a sharply falsifiable source-facing gate close to its endpoint. The other ten lines remain `narrow`: the new evidence either closes a tempting finite/local mechanism, sharpens a quantitative threshold, or leaves a more indirect global transport/normalization problem.

No merge, split, pause, or new line is justified, and no new Master clue is needed in this pass. The apparent relationships among target-transmission budgets, source aggregation, first-crossing, and scalar Green response do not yet preserve an exact source family, operator, normalization, and target map strongly enough to constitute a transferable clue. There are no open `.review.md` challenges in the synthesized tree. The latest independent adversarial checkpoint is `310ca5cc1607a88d90e19aeb0e0485b256565fb9`, reviewing through `92ce3508d1cef814393b29e119e40c2cdcc4c4cd`; later findings (`VIS-182`, `MC-238`, `PF-304`, and `PL-283`) are canonical current evidence but are not attributed to that checkpoint. The Atlas remains navigation-only for this pass because its two current surfaces declare stale and divergent source revisions (`4bc527123c549c73b4ce4f04512cb291b342311d` and `834376c2cef6466d9a7835745c20b608b2a692d3`).