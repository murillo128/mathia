# Mathia Research State

Canonical evidence synthesized through `18b860210c48e3f2d5ca7e4d55b9e5a2d7aa0f4f`. The latest independent adversarial checkpoint is `1f13b352ecdc723ec0c771c617bef71be965363b`, with `Adversarial-Reviewed-Through: 375c459e7a7242588dd8fff165eedb229c6c04ca`. `VIS-199` and `MC-256` are later substantive evidence and are used as current canonical findings without being treated as adversarially cleared. No `.review.md` sidecar is open in the current tree.

## Global picture

Portfolio recommendations remain unchanged: `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other research line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

The strongest update is that **source-specific equations are beginning to defeat matched screening controls that survive every generic spectral test**. `WI-264`--`WI-265` had shown that all finitely active von-Mangoldt lags could be hidden inside an autocorrelation gap and that the same endpoint-screened vector could be made an exact abstract terminal zero mode with strict positivity on every proper centered and translated shorter window. `WI-266` now rules out that entire explicit two-endpoint family for Suzuki's actual localized Weil operator: on a nonempty interval where the vector and all prime translations vanish, the exact null equation reduces to convolution with a strictly increasing regular kernel, whose two endpoint contributions have a fixed nonzero sign. Generic firstness still does not solve the problem, but the real arithmetic null equation has now demonstrated genuinely stronger rigidity. The accepted Weil-Inertia clue has already been retargeted from the excluded endpoint family to the harder support-propagation question for arbitrary multi-component null modes.

Farey and Nyman sharpen two different screening costs. `FD-084` upgrades full-counting-exponent occupation to **consecutive additive blocks**: every fixed-power future window contains a constant-occupation block of length `H^(Theta-delta)` for arbitrary fixed `delta>0`. This removes isolated-point geometry as an escape, but additive thickness still does not provide the integer-ratio or multiplicative compatibility consumed by the GCD/Schur accumulation mechanism. `NB-083`, after its normalization correction, gives a sharp stationary calibration: an outer polynomial whose normalized filter has unit-circle zeros has anchored finite-prediction excess `kappa/L+o(1/L)`, so bounded screening requires depth `L=Omega(R)` and therefore an exponential-size future; if all zeros lie strictly outside the closed disk, geometric prediction error allows logarithmic depth and polynomial-size future. The actual Nyman source still needs a nonstationary arithmetic inverse-prediction theorem that beats this boundary-zero control.

Arithmetic Fidelity now separates **exact discriminator fidelity from stable target margin** at the saturation boundary. `AF-306` shows that even when `k alpha=1` removes all continuous coefficient freedom and forces uniform `k`-subsets, saturated prime supports can be recurrently arbitrarily close to cancellation while another support in the same deletion fiber is arbitrarily close to maximal margin. For three equal prime terms on the imaginary axis, exact zeros are absent but the infimum of the normalized margin is still zero. Discreteness can therefore rescue an exact binary zero question while leaving quantitative zero-sensitive transport completely ill-conditioned.

Robin isolates a plausible singular higher-layer carrier rather than another regular moment. `RE-085` had shown that normalized higher-layer event mass disappears on long stretched fans; `RE-086` now proves that if a fixed-layer event actually becomes an adjacent support boundary, its generating base prime must anchor an ordinary prime gap large enough to carry the inverse threshold chamber. The normalized gap cost is asymptotically layer-independent, and tied support boundaries require synchronized gap resonances at power-related prime scales. This does not yet prove such selected events are too rare, but it converts the surviving discontinuous state-selection channel into a deterministic arithmetic condition.

Weil Positivity gets a positive provenance result without a sign theorem. `WP-278` proves from the prime number theorem that the critical Mangoldt half-density, after recentering at high logarithmic energy and scaling by its local mass growth, converges vaguely to `cosh(x/2) dx`—exactly the polar continuum already present in the canonical Weil carrier. This supplies a natural infinite-volume origin for the continuum left open by `WP-277`, but matched generalized-prime and even full-integer controls have the same tangent law. It does not produce the Gamma term, a canonical operator limit, or Weil positivity, and global subtraction of that continuum remains target-strength.

Visual Exploration and Möbius both move their live boundary. `VIS-199` keeps the fourth prime condition in the affine collision sieve and extends `D_v=o(y^2)` from polynomially growing subcritical horizons into a broad genuinely subpolynomial corridor, including `H>=exp((log y)^(1/2+eta))` under the stated subcritical condition. The remaining crowding frontier is now much closer to polylogarithmic/critical scales. `MC-256` shows that for every fixed prime order `ell`, separated-prime higher power-residue matrices can realize arbitrary finite `F_ell` matrices by Kummer theory and Chebotarev; passing from quadratic to fixed-order higher residues therefore creates no generic rank rigidity unless one uses the moving-shell localization, source coupling, growing residue order, or wild prime-square directions.

Prime Flute remains the main constructive operator route. `PF-315`--`PF-316` reduce the physical endpoint to the two genuine channels `T_H` and `Z_LH`; the residual map cannot hide a bad conditional angle without paying through the direct channel. Prime Lattice remains at its sharp source-static regularity boundary from `PL-290`, while Analytic Frontier retains the sharp matched late-packet/source-continuation gate. Neither line received evidence in this interval that changes its recommendation.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make the matched endpoint packet quantitatively sharp: derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` are equivalent discrimination boundaries. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-306` separates exact from stable saturation fidelity: equal-weight prime supports may be exactly zero-free while having arbitrarily small normalized zero margin, and same-fiber saturated supports can realize almost the full margin diameter. | Identify a genuinely arithmetic global coupling or nonlinear/multiplicative reachable-set restriction that gives a quantitative target margin; support size, coefficient caps, positivity, and saturation alone are insufficient. |
| `farey_discrepancy` | `narrow` | `FD-084` thickens constant occupation into consecutive blocks of length `H^(Theta-delta)` inside every fixed-power future window. | Convert additive blocks into multiplicative/integer-ratio compatibility, or prove a transport/accumulation law that composes across additively thick but multiplicatively unaligned horizons. |
| `mobius_cancellation` | `narrow` | `MC-252`--`MC-256` show that quadratic and every fixed-order tame higher-residue matrix are arbitrarily flexible once shell localization is removed, while global class-group ranks can still miss the target cross-block. | Prove a genuinely moving-shell/source-coupled rank theorem, use growing-order or wild prime-square structure, or bypass rank with fixed-power signed cancellation of the exact rough coset. |
| `nyman_beurling` | `narrow` | `NB-083` calibrates stationary future screening sharply: boundary zeros force anchored prediction excess `kappa/L+o(1/L)` and exponential-size future, while an inverse analytic through the circle gives geometric error and polynomial-size future. | Define and estimate the corresponding nonstationary inverse-prediction quantity for the actual Nyman innovations strongly enough to reach `O(1/R)` at an affordable horizon, or avoid finite prediction altogether. |
| `prime_circle` | `narrow` | `PC-274` proves the raw ordered near-resonance law is prime-blind at every bandwidth, while `PC-275` identifies the first centered fixed-bandwidth correction as the classical logarithmic-integral source bias. | Move to a target-sensitive residual: normalized moving/microscopic bandwidth, Li-centered higher correction, singular/directional data, transfer across levels, or another quantity matched controls cannot reproduce. |
| `prime_flute` | `continue` | `PF-315`--`PF-316` give the exact physical two-channel factorization and endpoint visibility: compactness/weak trace of the full angle is equivalent to that of both `T_H` and `Z_LH`. | Prove singular-value counts for `T_H` and `Z_LH`; for the direct channel, test the accepted DtN-remainder clue uniformly through shrinking collars, then address infinite realization and signed reassembly separately. |
| `prime_lattice` | `narrow` | `PL-289`--`PL-290` identify and saturate the graph-domain transport gain: actual eigenstates have squared-log Fourier regularity, but the complete source-static Weil form still has sharp `Theta(log^-2(1/|a-b|))` graph-norm aperture modulus. | Obtain a stronger estimate from the actual low eigenbranch—state-specific cancellation, stronger regularity, a much smaller explicit constant—or bypass transport with a direct finite positivity certificate. |
| `robin_extremal` | `narrow` | `RE-086` turns the surviving singular higher-layer support-boundary mechanism into anchored ordinary-prime-gap constraints, with tied events requiring synchronized resonances at power-related scales. | Prove that the selected rightmost-maximizer fan cannot supply enough anchored gap/event coincidences, handle growing layer depth, or identify another singular non-boundary channel that survives the `RE-085` mass collapse. |
| `visual_exploration` | `narrow` | `VIS-199` extends weighted dephasing below every fixed polynomial horizon: a four-form upper-bound sieve gives `D_v=o(y^2)` throughout a stretched-exponential/subpolynomial corridor. | Push toward polylogarithmic or critical `H`, improve the worst-case local-factor loss, establish necessity of `D_v`, or move to a different source-sensitive observable. |
| `weil_inertia` | `continue` | `WI-266` proves that the explicit all-prime endpoint-screening family surviving generic first-crossing tests cannot satisfy Suzuki's actual localized Weil null equation. | Generalize the source-specific kernel argument to arbitrary multi-component first-crossing modes: prove support propagation/no-long-gap rigidity or characterize the support graphs that can use prime translations to cancel the monotone continuous kernel. |
| `weil_positivity` | `narrow` | `WP-278` identifies `cosh(x/2) dx` as the canonical high-energy tangent of the critical Mangoldt half-density, giving source-native provenance for the pole continuum without a sign theorem. | Lift the measure-level tangent to an independently justified global/infinite-volume operator geometry retaining prime fluctuations and the Gamma term, then prove the missing coupling/sign law without importing RH through the centering. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**Matched controls are now most useful when they reveal the exact missing source law.** `WI-265` showed that abstract first-crossing structure could reproduce the endpoint screen; `WI-266` then found a source-specific null-equation property that the control cannot satisfy. `NB-083` classifies how dramatically healthy future screening changes when an inverse singularity reaches the unit circle. `AF-306` shows that even exact discreteness can leave stable margin destroyed by recurrent phase geometry. The common lesson is not a shared theorem: a proof must isolate the property of the real source that its strongest matched control provably lacks.

**Abundance has strengthened, but composition geometry remains separate.** `FD-084` upgrades many occupied horizons to long consecutive additive blocks, yet the target mechanism still asks for multiplicative/integer-ratio compatibility. `RE-086` likewise shows that a rare higher-layer event becomes useful only when selected at a support boundary and then pays an anchored prime-gap constraint. Count, local thickness, and event mass are not interchangeable with target-visible alignment.

**Spectral category can emerge in a limit without supplying the target sign.** `WP-277` excludes production of the pole continuum by ordinary subcritical coupling of compact-resolvent sectors, while `WP-278` shows that the continuum nevertheless arises canonically as a high-energy tangent of the discrete critical Mangoldt carrier. The category change therefore has a source-native infinite-volume explanation, but first-order density alone is too universal to encode RH. Prime Lattice gives the finite-aperture analogue: ambient graph regularity can be exactly sharp while the useful extra structure, if any, must come from the selected eigenbranch.

**Localization is increasingly the decisive arithmetic resource.** `MC-256` extends separated-prime universality from Legendre symbols to every fixed tame residue order: algebraic reciprocity/Kummer structure alone can encode arbitrary finite codes. What it cannot reproduce is the simultaneous moving-shell localization of the actual Möbius problem. Visual shows the complementary positive effect of retaining localization: the full four-form affine geometry buys enough logarithmic sparsity to push deterministic dephasing into a subpolynomial corridor.

The existing proposed `CLUE-global-cross-line-fidelity-realization-separation` already covers the program-level separation between source fidelity, target transport, destination realization, and assembly. The new results sharpen source-specific rigidity, screening cost, localization, and infinite-volume provenance, but still do not provide one common source family, operator, normalization, and target from which an exact transfer theorem follows. No additional Master clue is warranted; the accepted Weil-Inertia clue was already updated by its owner after `WI-266`, and the accepted Prime-Flute clue continues to route the direct-angle test.

The Graph-owned Riemann Atlas remains stale for portfolio decisions: its entrypoint and metrics advertise different old source revisions (`4bc527...` and `834376...`), both far behind current canonical evidence. Atlas telemetry is navigation context only and supports no recommendation here.

## Highest-leverage unresolved questions

1. **Weil Inertia source propagation.** Starting from a hypothetical first crossing beyond the certified `a_*>0.8`, use Suzuki's exact localized operator before reducing to generic spectral data. Determine whether the monotone regular kernel plus the finite von-Mangoldt translation graph forbids macroscopic screened support geometries or forces quantitative propagation. A valid negative control must now satisfy the actual source equation, not merely abstract firstness.
2. **Prime Flute endpoint singular counts.** Derive weak-`S_1`/compactness counting laws for both `T_H` and `Z_LH`. For the direct channel, isolate the exact translation-invariant local DtN model and control the normalized remainder uniformly through the shrinking-collar exhaustion; keep infinite realization and signed reassembly as separate gates.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet after the sharp `Theta(X/a_X)` aperture, with positive-cascade non-excisability kept as a distinct final gate.
4. **Farey compatible accumulation.** Use the `FD-084` additive occupied blocks to force multiplicative/integer-ratio structure or derive a nonlocal transport law whose gains compose without exact ratios. Additive thickness alone is now known insufficient.
5. **Nyman arithmetic prediction rate.** Identify the nonstationary arithmetic analogue of the anchored prediction excess and prove it reaches `O(1/R)` without the boundary-zero `1/L` cost at an unaffordable future horizon, or build a stable-mode certificate that bypasses finite prediction.
6. **Robin selected-gap capacity.** Turn the deterministic `RE-086` anchored-gap requirement into a selected-density/capacity contradiction on the rightmost-maximizer fan, including tied multi-scale boundaries and any growing-layer escape.
7. **Weil Positivity infinite-volume lift.** Determine whether the `WP-278` tangent correspondence can be lifted canonically to a global operator/boundary construction that retains prime fluctuations, supplies the Gamma contribution, and proves a sign law independently of RH.

## Portfolio changes proposed

No recommendation change is proposed. Weil Inertia remains `continue` and is strengthened: the strongest generic screening control has now failed a genuinely source-specific test, leaving a concrete propagation problem rather than another generic positivity variant. Prime Flute remains `continue` because its endpoint is still reduced to two exact singular-value problems with a direct falsifiable clue. Analytic Frontier remains `continue` because its sharp source-continuation gate is unchanged and distinct from both operator routes.

The strongest movements among `narrow` lines are real but do not yet supply the missing target bridge. Farey now has long additive occupied blocks but not composability; Visual has pushed weighted dephasing into a broad subpolynomial corridor but not to a zero-sensitive bridge; Nyman has a sharp boundary-zero cost calibration but no arithmetic rate; Robin has converted singular selection into anchored gap conditions but not a contradiction; Arithmetic Fidelity has separated exact from stable saturation without finding a stable source law; Möbius has ruled out fixed-order nonlocalized higher residues as a generic rank repair; Weil Positivity has found a canonical pole-continuum tangent but not the Gamma/sign lift. No active line is a strong `pause-candidate` or `merge-candidate`, and no Research Watch allocation change is justified. The current Watch concurrency is left unchanged.