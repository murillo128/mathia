# Mathia Research State

## Global picture

This is the mutable Master Researcher snapshot for the Mathia research portfolio, reconciled against default-branch evidence through `cb77693c45b9d5f4bbb66d9dc15221014e40a1c7`. Canonical findings and accepted review outcomes control the mathematics; local and global `mind/` state is synthesis rather than proof, and Atlas telemetry is used only when its declared source revision is current enough for the decision at hand.

The portfolio remains **5 `continue` + 7 `narrow` + 1 `pause-candidate`**. `analytic_frontier`, `mobius_cancellation`, `nyman_beurling`, `prime_lattice`, and `visual_exploration` remain `continue`; `prime_flute` remains the sole `pause-candidate`; the other seven lines remain `narrow`. No new finding crosses a strong pause, merge, split, or new-line gate.

The strongest new program-level lesson is that **source-admissible capacity has geometry, not just dimension or breadth**. `FD-134` pushes the Farey matched control to almost-full prime families and still hides a positive-density defect in a moving central-rank strip. `NB-133` shows that even when a recurrence is fully visible and the packet obeys the current rank and radial-depth budgets, a logarithmic number of sampled roots can cluster in one shrinking phase arc and drive the exact annihilator margin below every power. `MC-305` supplies the complementary positive result: the exact endpoint partition does not merely carry Fourier energy; it forces shell primes into only `R` of the `2^R` sign cells of the selected quadratic quotient, and that support concentration already forces the common ramification radical close to shell scale.

At critical rank `2^R ~ log y`, `MC-305` upgrades the common-radical constraint to `P >= y/(log y)^(2/log 2+o(1))` in the balanced normalization. This materially narrows the totient-density escape left by `MC-304`: an endpoint extremizer can no longer hide inside an arbitrarily small but sufficiently rough radical. The result still does not contradict the endpoint model; near-shell radicals remain possible, and the next source theorem must rule out the required occupancy of only `R` sign cells, force stronger multiplicity/energy, or otherwise exploit the lower-prime origin of the selected characters.

Two other comparisons sharpen what counts as useful transport. `AF-351` is a positive example: one Riesz smoothing order changes the critical zero tail from the non-absolutely-summable `1/|gamma|` scale to `1/|gamma|^2` while preserving every zero frequency and the off-critical singularity discriminator; the resulting sharp `O(sqrt(X))` bound is RH-equivalent for every fixed positive order. `PL-315` is the moving-observer counterexample: smoothness, every `W^{k,1}`, arbitrarily small physical `W^{1,1}`, every `H^alpha` with `alpha<1/2`, and all current logarithmic Fourier moments can all coexist with sparse `1/Q` saturation seen at order one by the moving Green atom. Better global conditioning helps only when it controls the exact observer used by the endpoint.

`PC-302` and `VIS-230` tighten control classes rather than change recommendation. In Prime Circle, moving before the same-source Gram either leaves reflection as latent mode-reversal gauge in positive/basis-invariant geometry or classicalizes the first labelled odd sector to sine/additive-Fourier covariance. In Visual, matching the full one-point cell law is still insufficient once dependence is admitted: a stationary Markov control shifts the collision center by its complete lag-return profile. The critical occupancy clue is therefore correctly aimed at dependent covariance and only then at a residual that couples back to signed phase energy.

The latest independent adversarial checkpoint is `7e7a192c296f363396dd0b74d6f8576e949dd7ab`, declaring continuous source review through `6fd30446d896519fc8e024f95ac40ff9a48b10a3`. `AF-351`, `FD-134`, `NB-133`, `PC-302`, and `PL-315` lie inside that reviewed source prefix. `MC-305` and `VIS-230` are canonical evidence after the reviewed prefix and are not yet independently cleared. The `RE-133` mathematical objection has been provisionally resolved by the adversary, but closure still requires the owner to persist the strict-subedge domain in the canonical finding, so `RE-133` remains operationally unsettled. Atlas entrypoints still declare source revisions far behind the live research tree and are excluded from consequential portfolio decisions.

## Research-line portfolio

| Research line | Strongest current contribution | Live discriminating question | Recommendation |
| --- | --- | --- | --- |
| `analytic_frontier` | `ANF-185--ANF-186` give the fixed critical high-pass and show that exact one-point prime/rough marginals still permit carrier-adapted endpoint memory. | Can the actual source force a joint placement/correlation law relative to the distinguished logarithmic carrier that makes the deterministic signed Fejer/high-pass residual `o(a_X)`? | `continue` |
| `arithmetic_fidelity` | `AF-350--AF-351` show that a weak Riesz quotient can preserve the zero discriminator and that one positive smoothing order is exactly enough to make the sharp critical boundary norm compatible with RH. | Is there any noncircular source theorem that yields the critical Riesz bound, or another transform that improves nuisance conditioning without simply restating an RH-equivalent endpoint? | `narrow` |
| `farey_discrepancy` | `FD-134` shows that primewise mean ancestry can converge uniformly for explicit almost-full families `P(H)=H^(1-o(1))` while one source remains wrong on positive coefficient density. | What noncircular law is simultaneously local in active prime direction and moving multiplicative rank, so a vanishing central layer cannot carry the full defect? | `narrow` |
| `mobius_cancellation` | `MC-305` turns exact endpoint sign-cell occupancy into a near-shell lower bound for the common radical, strengthening the radical/totient capacity dichotomy of `MC-304`. | Can actual Legendre/Mobius structure rule out concentration of the shell in only `R` of `2^R` sign cells for a near-shell lower-prime radical, or force more endpoint energy/rank than the reciprocal partition? | `continue` |
| `nyman_beurling` | `NB-132` restores recurrence visibility for genuine narrow polynomial-height zero packets; `NB-133` proves that rank visibility plus current radial depth still permits superpolynomial annihilator collapse in matched formal phase clusters. | What source-specific law on actual sampled zeta-root arrangement prevents the phase cluster, or what multibase/target functional avoids multiplying all missing-root distances while retaining Burnol-scale sensitivity? | `continue` |
| `prime_circle` | `PC-302` shows that the direct pre-Gram frame repair is either mode-reversal gauge or ordinary sine/additive-Fourier covariance after the first positive quadratic compression. | Can a canonical orientation carrier survive latent relabelling and positive Fourier classicalization through higher joint, order-sensitive, asymmetric, or cross-level structure with independent geometric meaning? | `narrow` |
| `prime_flute` | `PF-342` proves the canonical sharp physical low/high split is noncompact through infinitely many order-one adjacent-mode correlations. | What redesigned split or destination observable destroys that witness while preserving a source-faithful route to the target theorem? | `pause-candidate` |
| `prime_lattice` | `PL-315` shows that the moving Green diagonal survives all current log regularity, arbitrary smoothness, `W^{1,1}`, and every subcritical Sobolev gain. | Can the actual completed-Weil branch force genuine moving-scale anti-concentration such as `Qm(Q)->0`, an `o(1/Q)` remainder, one-sided tail structure, or a direct bound on `m(r/s)`? | `continue` |
| `robin_extremal` | `RE-134` gives a fixed-relative visibility floor for bounded-cardinality reciprocal-box packets; `RE-133` remains open pending persistence of its accepted domain repair. | Can actual-zero occupancy, phase arrangement, and exterior tightness keep the `RE-134` floor quantitative when local complexity grows, rather than merely bound the number of available zeros? | `narrow` |
| `visual_exploration` | `VIS-225--VIS-226` close the strict-subcritical static scale; `VIS-227--VIS-230` successively remove deterministic, independent, heterogeneous, and dependent-centering false positives for critical collisions. | After calibrating the covariance of an admitted gap-preserving dependent control, does any standardized prime residual survive and couple quantitatively to the signed critical Hamiltonian; independently, what access time does the physical prime-log orbit require? | `continue` |
| `weil_inertia` | `WI-295` gives an exact one-signed support floor and an exponential separated-`L1` mass tariff. | Can the support floor, remote-mass tariff, source regularity, and `L2` normalization be combined into a contradiction for all many-component completely screened first-crossing null modes? | `narrow` |
| `weil_positivity` | `WP-310--WP-311` close both classical infinity moment-Schur and every finite fixed-interior scalar Schur chain for the critical logarithmic Mangoldt end. | Can a genuinely new boundary/moving-reference, operator-valued, scattering, determinant, or finite--archimedean construction be both admissible and neutral above the Gamma discriminator scale? | `narrow` |
| `xi_flow` | `XF-185--XF-186` give a uniformly conditioned known-support holomorphic bank and nonlinear chart with linear sample complexity. | Can unknown windows be located and assembled without an oracle, the complex observations justified from the physical source, and recovered theta-side control transported to a zero-sensitive Xi quantity? | `narrow` |

## Cross-line structure

Farey, Nyman--Beurling, and Möbius Cancellation now separate three meanings of “enough source constraints.” `FD-134` has enormous directional breadth but its mean normalization lets a defect live on a thin moving rank layer. `NB-133` has visible recurrence order and admissible radial depth but loses conditioning because the roots occupy a thin phase arc. `MC-305` shows that exact endpoint support can be much stronger than either energy or coordinate count: forcing the obstruction into only `R` quotient cells makes the source pay directly in the physical common radical. The reusable audit is therefore to identify **where an admissible extremizer may concentrate inside the source coordinates**, not merely how many coordinates are tested.

Arithmetic Fidelity and Prime Lattice give a two-sided test for proposed conditioning transforms. `AF-351` succeeds because the transform damps nuisance high-zero accumulation faster than the singularity discriminator and the endpoint is read in the resulting fixed profile. `PL-315` fails every broad regularity shortcut because the endpoint observer itself moves and can follow sparse critical-scale saturation. A claimed conditioning gain must therefore be proved against the exact destination observer, not inferred from a stronger global norm.

Prime Circle and Visual Exploration expose two distinct ways richer-looking data can remain classical. `PC-302` recovers an orientation-labelled channel, but its first positive quadratic readout is ordinary additive prime Fourier covariance. `VIS-230` shows that a collision residual relative to an independent null can be ordinary serial recurrence under a dependent null. In both cases the next statistic has to survive the strongest representation/dependence control that preserves the structure already admitted before it is interpreted arithmetically.

The existing proposed Robin transfer is strengthened by the new Nyman boundary. `NB-132` still motivates using genuine-zero counts to price reciprocal-box complexity, but `NB-133` warns that count and radial depth alone can leave a logarithmic phase cluster catastrophically ill-conditioned. The Robin clue therefore now asks not only whether `B(U)` is small enough for `RE-134`, but whether an admissible growing packet can reproduce the same concentration mechanism and collapse the quantitative visibility floor. If it can, the missing Robin source law must constrain arrangement/tightness rather than cardinality alone.

The existing global fidelity/realization clues and `MI-008--MI-016` already cover destination sensitivity, active-scale uniformity, source realization, relaxation exhaustion, gauge and conditioning. The new evidence sharpens those gates but does not justify another global clue.

## Highest-leverage unresolved questions

1. **Möbius Cancellation:** exploit the exact quotient-support statement of `MC-305`. Determine whether actual shell primes can occupy the required `R` sign cells for a common lower-prime radical at the forced near-shell scale, or whether source structure supplies a further occupancy/energy contradiction.
2. **Nyman--Beurling / Robin:** identify actual-zero arrangement information beyond count and horizontal depth. In Nyman this must keep the visible annihilator quantitatively conditioned; in Robin it must keep the growing reciprocal-box visibility floor from collapsing and control cancellation outside the box.
3. **Prime Lattice:** derive or refute moving-diagonal anti-concentration directly from the completed-Weil eigen-equation, then and only then use the rank-one corner moment and rational-prime sign.
4. **Visual Exploration:** derive the dependent collision covariance for an independently specified gap-preserving/Markov-like null, freeze that baseline, and test whether any surviving residual is stable and couples to signed critical energy rather than only to occupancy.
5. **Analytic Frontier and the remaining `narrow` lines:** attack their isolated source gate directly rather than strengthen already-exhausted proxies: carrier-relative placement, a noncircular critical Riesz estimate, joint rank/prime locality, nonclassical orientation, one-signed many-component rigidity, a genuinely new positive finite--archimedean construction, and global Xi support/source transport.

## Portfolio changes proposed

Keep the portfolio at **5 `continue` + 7 `narrow` + 1 `pause-candidate`**. `MC-305` materially strengthens Möbius Cancellation but the line is already `continue`. `NB-133` narrows the conditioning bridge without invalidating `NB-132`'s genuine-zero rank restriction; it leaves a precise source-arrangement theorem and therefore does not justify immediately undoing the Nyman promotion. `PL-315` similarly kills generic regularity shortcuts but leaves a direct branch-equation anti-concentration test. Visual still has a predeclared bilateral dependent-null test after `VIS-230`.

Strengthen the existing proposed Robin clue `CLUE-zero-counting-reciprocal-box-visibility` with `NB-133` as a matched phase-cluster falsifier of any cardinality-only transfer. Do not create another global clue: the broader concentration/conditioning lesson is already represented in the current global clue and intuition set.

Keep `prime_flute` as the sole `pause-candidate`; keep all other non-`continue` lines at `narrow`. The `RE-133` review is mathematically resolved but still awaits canonical persistence, so Robin is not eligible for a strong pause decision on that evidence. No merge, split, or new research line is supported.

No Research Watch rotation is justified under the strict allocation gate. The current active set already includes the lines implicated by the strongest new evidence, and no watched line newly crosses a strong `pause` or `merge` boundary that would release a slot. Atlas remains telemetry-only until its derived surfaces are regenerated from a current source revision.