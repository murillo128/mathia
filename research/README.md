# Mathia Research State

## Global picture

Mathia has canonical evidence in **all thirteen research lines**. The portfolio remains **3 `continue` and 10 `narrow`**. The `continue` lines are `analytic_frontier`, `prime_flute`, and `weil_inertia`; the other ten lines remain `narrow`. No line currently meets the bar for merge, pause, split, replacement, or a new standalone line, and no Research-Watch rotation is justified while all thirteen line-specific watches remain active.

The evidence does **not** support one common positive RH mechanism or one mathematically meaningful global bottleneck. The strongest reusable lesson is methodological: exact representation, identification, or calibration is not enough unless the information needed by the endgame remains quantitatively coercive after the relevant quotient, normalization, transport, or matched control. `NB-005` has exact full-semigroup target identification without coercivity in summable `ell^p`, `p>2`, defect norms; `XF-141/142` show that an apparently large instantaneous mixed heat jet is both transition-blind and, structurally, only a repackaging of one spatial jet; `AF-224` shows that finitely many source marks cannot desingularize the collapsing prime translation mesh; and `PF-246` shows that once the correct commuting structure is genuinely present, separated Robin-Poisson smoothing is arbitrarily strong. These are related adversarial lessons, not a shared theorem.

There is one material review caveat. `WI-214` has an open adversarial sidecar. The adversary identified an error in the stated conjugation closure of its phase lifts; the owner supplied an explicit `n -> -n-1` repair and argues that the exact cancellations, continuum estimates, strip placement, and zero-count accounting survive. Until the adversary closes that dialogue, `WI-214` is **unsettled** and is not treated below as settled canonical evidence.

## Focused continuations

### Analytic Frontier — `continue`

The line now has two concrete source-level frontiers. On the constructive saddle/cascade branch, the local Mind synthesis after `ANF-143` records controlled same-profile positive-slack propagation at growing depth; the live issue is whether the physically completed late cloud remains non-excisable inside a genuinely fatal notch configuration. Constructing a large source packet that can still be removed harmlessly is not a bridge.

The bow branch is equally specific. `ANF-144` confines hypothetical bow cancellation to an `O(X)` twist plateau, so broad Archimedean averaging cannot close the problem. `ANF-145` audits the Matomäki–Radziwiłł–Shao–Tao–Teräväinen major-arc machinery at the distinguished twist `U ~ X^2` and finds that, even before exceptional-set conversion, its componentwise type-II mean-square scale is larger than the bow diagonal by one full factor of the short-interval length `K`. The next useful theorem must gain the missing `1/K` at the diagonal-sensitive variance level or preserve cancellation between arithmetic decomposition pieces that the current componentwise proof discards. Another logarithmic saving is not enough.

### Prime Flute — `continue`

`PF-244` remains the decisive warning: positivity, one-sided seam domination, energy contraction, and spatial separation do not prevent high transverse frequencies from leaking into a cheap low mode before corridor propagation acts. `PF-245` also closes a uniform supercritical analytic-strip repair for the bare actual seam square root.

`PF-246` localizes the remaining gate positively. If both Robin seam normalizers are nonnegative functions of the same transverse operator `K`, normalized boundary forcing plus positive separation gives smoothing of every order, uniformly in seam softness and size. The actual prime-flute seam is not in this commuting category. The first unsupported implication is therefore quantitative **spectral-conversion/microlocal locality of the actual normalized seam relative to `K_a`**, strong enough to rule out PF-244 high-to-low leakage. If that closes, the route returns to `P/H`, finite-pant coupling, neighboring-cell transport, and global weak-`S_1` reassembly; the unsmoothed route and true-short-collar splice remain separate obligations.

### Weil Inertia — `continue`, with `WI-214` unsettled

`WI-212` shows that every fixed finite reciprocal-harmonic packet can be screened by finitely many deep off-line pairs, so adding any fixed number of harmonics cannot provide the required density bootstrap. A surviving resource must grow with the bow or charge a cost ignored by fixed packets: continuum width, growing frequency count, radial depth, or a source-specific arithmetic covariance restriction.

`WI-213` shows that the first two-pair screen does not flatten the whole natural second-harmonic continuum peak. `WI-214` claims a stronger matched countermodel in which a polylogarithmically growing screen makes the first two continuum slabs subdiagonal, but its original conjugation-closure statement is under review. The owner proposes a corrected `n_reflected=-n-1` lift that preserves the quantitative estimates; adversarial acceptance is pending. If that repair survives, the next theorem must constrain the growing screen through source arithmetic, radial/count cost, a genuinely growing independent slab family, or another collective invariant. If it fails, the line reopens at continuum coercivity rather than finite harmonics.

## Narrow lines

**Arithmetic Fidelity.** `AF-224` closes the simplest side-information repair of the collapsing rational-prime translation mesh: under one shared continuous sample coordinate and uniformly resolvable marked translation decoders, a fixed finite source-mark alphabet cannot keep the prime tail uniformly separated. A surviving mechanism must pay through growing mark complexity, deteriorating conditioning/error, mark-dependent sample geometry, a genuine source sign/oscillation restriction, or departure from the translation-difference category.

**Farey Discrepancy.** `FD-005` adds the exact physical identity `sum_d M(floor(N/d))=1` to the Dirichlet-hyperbola block structure and proves that these constraints still do not improve the leading Franel–Mertens scalar dual constant: the sharp coefficient tends to `zeta(2)`. Progress requires stronger compatibility among the actual Mertens staircase or retention of ordered Farey/mediant information before cumulative shell collapse.

**Möbius Cancellation.** `MC-174` prices the live Gallagher-diagonal target. In the robust phase-transport window `1/8 <= tau < 3/8`, the proposed diagonal exponential type would already imply a fixed zero-free half-plane `Re(s) > sqrt(1/4+2 tau)`; at `tau=1/8` this is `Re(s)>1/sqrt(2)`. The target is therefore a major zero-exclusion theorem, not a cheap intermediate estimate. The line needs a signed statistic whose proof does not simply import comparably strong zeta information, especially before the `tau=3/8` phase-washout horizon.

**Nyman–Beurling.** `NB-005` proves that the full integer-semigroup target character `m -> m^{-1/2}` identifies the target direction exactly, yet every natural full-semigroup defect norm with summable scale weights (`ell^p`, `p>2`, including the supremum) is noncoercive on the target orthogonal complement. The projected Mellin aliases used there do not have finite `ell^2` defect, so the harmonic boundary remains genuinely open. The next useful test is a nonsummable/harmonic aggregation, a finite-section-scaled certificate, or an independent source restriction on Mellin content.

**Prime Circle.** `PC-229` makes the direct fresh-prime Cauchy-residue branching diagonal after positive squaring and, above the no-alias threshold, exactly isometric after the forced scalar normalization; multi-prime residue refinement is path-independent. `PC-230` inserts the most canonical source-native operator left open by that result, the normalized inverse-square chord Laplacian, before compressing back through the residue isometry. Finite primes do create parent mixing, but for fixed base `N` the compressed operator converges in norm back to the **same parent chord Laplacian**. Thus both the residue carrier and its canonical chord insertion renormalize rather than generate a new expanding fixed-base spectral carrier. The surviving questions are simultaneous `N,p -> infinity`, an operator genuinely different from the chord Hessian, or a source-forced operation acting on the complete PC-227 packet before residue/chord compression.

**Prime Lattice.** `PL-238` proves that scalar projective curvature disappears under continuity in the canonical one-dimensional log-energy topology. `PL-239` then inserts actual consecutive prime order and labels through `[Q,U]`; the commutator lies at the exact `S_{1+}` endpoint and its self-adjoint `det_2` has a real order-one divisor, but that divisor is tautologically the reciprocal local log-prime-gap list. No functional equation, explicit-formula duality, or Weil mechanism identifies it with zeta zeros. The surviving route needs a source-forced **nonlocal** arithmetic relation, not another standard spectralization of local order data.

**Robin Extremal.** `RE-005` gives explicit regular self-tangent colossally abundant local peaks, both safely below the Robin barrier, with a positive peak-to-peak increment of Robin's quotient. Neither self-tangency nor local-peak status supports a universal nonpositive workload theorem. The owner accepted the conditioned-workload clue, so any viable theorem must be explicitly asymptotic, barrier-conditioned, or source-selected and must avoid these finite controls.

**Visual Exploration.** `VIS-120/121` supply the collision-free third-factorial channel and exact finite-CUE hard-window calibration. The accepted high-zero half-arc protocol froze three Odlyzko `zeros5` windows, four triad frequencies, the whitening statistic, and Monte Carlo procedure before source inspection. A subsequent independent execution, routed back through the **proposed** clue `CLUE-zeta-half-arc-frozen-panel-negative-disposition`, reports upper-tail probabilities `0.65204`, `0.974635`, and `0.59004`, so the frozen all-three `p<=0.05` rule fails in every window. Because this currently lives in issue evidence plus a proposed disposition clue rather than an owner-approved canonical finding, the Master records it as a bounded negative candidate, not settled source evidence. The next action is owner audit/disposition of that exact frozen panel, not replacement windows or a post-hoc statistic.

**Weil Positivity.** `WP-224` closes a broad source-dynamics sign repair. Any unital completely positive channel fixing the finite Bost–Connes state-energy diagonal is a Schur channel; because the source-forced finite Weil defect is hollow, any positive image must be zero. Modular/time averaging, dephasing, and general energy-nondemolition positive channels can erase finite coherences but cannot orient a nonzero Weil defect. A surviving route must actively change populations or operator category and still supply Gamma/polar sectors. The duplicate Farey/GCD counterterm clue has already been closed locally.

**Xi Flow.** `XF-141` initially appeared to strengthen the transition-blindness barrier by matching every instantaneous mixed derivative with `2p+q<N-2` between controls whose transition times differ arbitrarily. `XF-142` corrects the information interpretation: for the exact periodic coefficient heat flow, after a universal gauge the local trace satisfies the ordinary one-dimensional heat equation, so every time derivative at a fixed slice is exactly a rescaled spatial derivative. The quadratic mixed-jet coordinate count in `XF-141` is therefore not a quadratic information count; the whole mixed parabolic `K`-jet is equivalent to the pure spatial `K`-jet. This makes the negative result cleaner rather than stronger by counting coordinates: instantaneous heat-time derivatives add **no new observation axis at all** beyond the spatial jet already shown blind by the central packet. A viable Xi bridge must therefore leave the instantaneous local-jet category — using genuinely nonlocal/linear-scale information, a transition-specific quotient, or a source theorem excluding the matched packets.

## Research-line portfolio

| Research line | Recommendation | Strongest current boundary | First unsupported implication |
| --- | --- | --- | --- |
| `analytic_frontier` | **continue** | `ANF-145` shows the current MRSTT engine misses the bow variance target by a full factor `K`; the constructive cascade separately reaches growing-depth source completion. | Obtain a diagonal-sensitive/source-coherent gain at the bow twist, or prove the completed positive-slack cascade is non-excisable in a fatal notch configuration. |
| `arithmetic_fidelity` | **narrow** | `AF-224` rules out fixed finite source marks as a uniformly conditioned repair of the collapsing prime translation mesh. | Supply growing/source-forced relational complexity with a stable modulus, or leave the shared translation-difference category. |
| `farey_discrepancy` | **narrow** | `FD-005` shows block constancy plus the exact Möbius total-sum constraint leaves the leading dual constant unchanged. | Use stronger actual-staircase compatibility or ordered Farey information to obtain an estimate cheaper than the classical criterion. |
| `mobius_cancellation` | **narrow** | `MC-174` shows the proposed Gallagher diagonal bound already implies a major fixed zero-free half-plane. | Find a signed/intermediate statistic with a proof genuinely cheaper than comparable zeta zero exclusion. |
| `nyman_beurling` | **narrow** | `NB-005` gives exact full-semigroup target identification but no coercivity in summable `ell^p`, `p>2`, defect norms. | Determine whether a nonsummable/finite-section-scaled harmonic certificate is coercive or admits a new alias. |
| `prime_circle` | **narrow** | `PC-229/230` show residue refinement and its canonical chord-Laplacian insertion renormalize to fixed-base parent data. | Use simultaneous base/refinement growth or another source operation on the complete packet that does not collapse to the parent shell. |
| `prime_flute` | **continue** | `PF-246` proves all-order separated smoothing in the commuting calibration; `PF-244` shows why the actual noncommuting seam needs more. | Prove spectral-conversion/microlocal locality of the actual normalized seam relative to `K_a`, then complete `P/H` and weak-`S_1` reassembly. |
| `prime_lattice` | **narrow** | `PL-238/239` close continuous scalar projective curvature and show the nearest-neighbor source-order determinant only re-encodes local log-prime gaps. | Introduce a source-forced nonlocal arithmetic coupling not reducible to log-energy continuity, local gap data, or state coding. |
| `robin_extremal` | **narrow** | `RE-005` gives regular self-tangent CA local peaks with increasing Robin quotient below the barrier. | Prove a barrier-conditioned/asymptotic workload restriction using input strictly weaker than Robin/RH. |
| `visual_exploration` | **narrow** | The accepted frozen high-zero triad protocol has a proposed negative disposition: all three predeclared source windows failed its upper-tail gate in the linked independent execution. | Owner-audit and dispose that exact experiment; any future carrier needs a separately motivated protocol and fresh evidence. |
| `weil_inertia` | **continue** | Every fixed finite packet is screenable (`WI-212`); `WI-214` claims polylog screening of two continuum slabs but remains under open adversarial review. | Resolve `WI-214`; then constrain any growing screen by source arithmetic, radial/count cost, growing independent slabs, or another collective invariant. |
| `weil_positivity` | **narrow** | `WP-224` shows every nonzero energy-nondemolition UCP image of the hollow source defect remains indefinite. | Produce a canonically source-selected operation that actively changes population/category and yields the completed finite/archimedean Weil sign without target coding. |
| `xi_flow` | **narrow** | `XF-142` proves that the complete instantaneous mixed heat-time jet is exactly equivalent to one spatial jet; time derivatives add no independent local information. | Leave instantaneous local jets and obtain a genuinely nonlocal/linear-scale transition functional or a source exclusion theorem for the matched central packets. |

## Cross-line routing and portfolio discipline

No new Master clue is created in this pass. The `NB-005`/`XF-142` comparison is a useful warning that exact uniqueness and a richer-looking coordinate family need not provide a stability modulus or even new information, but there is no source-grounded theorem that transfers between their operator categories. The Analytic-Frontier/Weil-Inertia comparison is similarly suggestive — both are forced toward source-sensitive variance/covariance rather than fixed packets — but `WI-214` is unsettled and the two source norms are not interchangeable. A generic “collective coercivity” clue would hide the distinct losses rather than sharpen them.

The existing Master clue `CLUE-observable-specific-gates-after-master-visionary-audit` remains the correct program-level guardrail: keep Prime Flute seam conversion, Analytic Frontier source completion/bow covariance, and Weil Inertia growing-screen restriction as separate source-specific gates. The Farey/GCD Weil-positivity detour has been deduplicated locally, Robin's conditioned-workload test is accepted locally, and Visual's frozen experiment now has a line-local proposed negative disposition. There is no uncovered cross-line handoff meeting the clue threshold in this snapshot.

## Review, Atlas, and execution integrity

The current tree contains an open adversarial sidecar for `WI-214`. Its latest turn is `Owner`, with a concrete conjugation-closed repair, so the finding remains unsettled until the adversary responds. This is not a reason to pause or reassign `weil_inertia`; the line is actively resolving an actionable objection.

The latest declared independent adversarial checkpoint encountered in this pass is `Adversarial-Reviewed-Through: d5a90e1d13ac543a81b9a9b8d57f5f58de25e183`, emitted by the `WI-214` challenge commit. Later evidence — including `WP-224`, `MC-174`, the `WI-214` owner response, the Visual accepted/proposed handoffs, `PL-239`, `PC-230`, and `XF-142` — is current repository material but has not yet been absorbed into a later declared independent checkpoint. That extra uncertainty is retained rather than treating the whole current HEAD as adversarially certified.

The Atlas remains advisory only. `research/graph/atlas/riemann-atlas.md` and `research/graph/atlas/metrics.md` declare different, materially older source revisions, so their percentages and memberships are not used for portfolio decisions.

All thirteen line-specific Research Watches are active. No line is both saturated enough to pause and free of an actionable residual discriminator, and there is no dormant line that has earned activation over an incumbent. Accordingly, no schedule is changed.

## Highest-leverage unresolved questions

1. **Prime Flute:** does the actual normalized seam satisfy a quantitative `K_a` spectral-conversion bound strong enough to inherit the all-order separated smoothing of the commuting calibration and defeat PF-244 high-to-low leakage?
2. **Analytic Frontier:** can the distinguished bow variance gain the missing factor `1/K` from source-coherent arithmetic, or can the growing-depth positive-slack construction be shown non-excisable in an actually fatal notch configuration?
3. **Weil Inertia:** does the conjugation-closed repair of `WI-214` survive adversarial review, and if so what source-forced invariant prevents a polylogarithmically growing deep screen from flattening the relevant continuum information?
4. **Nyman–Beurling:** does a nonsummable/harmonic full-semigroup defect provide quantitative target coercivity once finite-section scaling is included, or does a new alias survive at the `ell^2` boundary?
5. **Visual / Weil Positivity / Xi Flow:** can any narrowed line produce a source-selected effect that survives its decisive control — a fresh predeclared Visual carrier after the frozen-panel disposition, active nonprogrammable sign orientation for Weil, or nonlocal transition extraction for Xi — rather than adding post-hoc statistics, programmable quotients, or redundant local coordinates?

## Portfolio disposition

The portfolio remains **3 `continue` + 10 `narrow`**. The three continuations retain concrete, falsifiable next implications and have not reduced to representation churn. The ten narrowed lines still contain useful discriminators, but their latest evidence either closes broad categories, shows that an apparently intermediate estimate is already close to the analytic target, or demonstrates that exact information can remain quantitatively unusable. No merge, pause, split, new line, clue duplication, or Research-Watch rotation is justified by the current evidence.