# Mathia Research State

Canonical evidence synthesized through `6280ec0c8f1dc28361876c242d882bb9d28b92e3`.

## Global picture

Mathia has canonical evidence in all thirteen current research lines. The portfolio remains **3 `continue` and 10 `narrow`**: `analytic_frontier`, `prime_flute`, and `weil_inertia` remain `continue`; the other ten remain `narrow`. No line currently clears the bar for promotion, merge, pause, split, replacement, or a new standalone line. The strongest common change in this pass is a sharper separation among **source identifiability, source-selected geometry, and the sign/order structure consumed by the endgame**. Recent results show that a source may be preserved or reconstructed while the directional or order information needed downstream is still absent.

Analytic Frontier has exhausted essentially every fixed-complexity local-twist refinement of its matched endpoint packet. `ANF-178` shows the packet saturates every fixed local magnitude moment on its full coherence window; `ANF-179` strengthens this to every fixed naturally normalized demodulated twist jet. The rescaled frequencies can all lie in a fixed annulus away from zero while every derivative of any fixed order stays target-sized and the same packet still forces the bow-scale Fejer defect. The live theorem must therefore use the **actual placement of the prime-minus-rough residual relative to the distinguished carrier**, a genuinely growing-order/nonlocal source-sensitive filter, or direct control of the signed Fejer scale-slope statistic. Fatal non-excisability of any surviving same-profile cascade remains a separate downstream obligation.

Prime Flute has converted its weak-trace question into a cheap geometric falsifier. `PF-287` identifies the thresholded heavy angle with products of unit-energy extension-range projections. `PF-288` localizes noncompactness to repeated separated low/high form witnesses, and `PF-289` removes the artificial requirement that the high-side Rayleigh quotient diverge: the fixed positive high-energy floor already supplied by `PF-227` suffices. The cheapest decisive test is now the energy-normalized mixed correlation between the explicit `PF-216` low-symmetric vector and an optimized local physical-high witness from the `PF-227` band. A positive limsup kills compactness; systematic decay sends the line back to the positive essential-orthogonality/singular-value problem of `PF-287`.

Weil Inertia now has an exact finite-radius tariff rather than only a qualitative first-crossing target. `WI-242` closes absolute PNT-envelope control and `WI-243` shows that global/subexponential source control rapidly becomes RH-scale. `WI-244` introduced an explicit odd finite-radius reserve above the archimedean spectral floor; `WI-245` upgrades it using the exact Dirichlet--Robin spectrum of the odd exponential resolvent. The reserve is no longer merely exponentially visible: the exact quantity `B_Robin(a)` has quadratic large-radius decay, with `a^2 B_Robin(a)` tending to `(pi^2/4) zeta(3,5/4)`. Thus a normalized odd zero witness supported in `[-a,a]` must satisfy `Delta(v) >= (M(0)+B_Robin(a))/2`, with an explicit nonasymptotic lower bound. The missing step is still arithmetic: beat that tariff on the **signed** von-Mangoldt discrepancy for the first-crossing candidate class without falling back to the global absolute estimates already shown circular or too weak.

The latest independent adversarial checkpoint is `b5550c162fd936240c24447b867a488dd555199e`, with `Adversarial-Reviewed-Through: 36bdff2534061fb183c92d85af2d20f3699a679a`. No `.review.md` sidecar is open at this synthesis point. The subsequent Weil Positivity, Möbius Cancellation, Visual Exploration, and `WI-245` findings are canonical current evidence but await the next independent checkpoint. The Riemann Atlas remains outside portfolio decisions because its derived surfaces are stale relative to the canonical corpus.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-178`--`ANF-179` show that prime-scale matched endpoint packets remain target-sized in every fixed local magnitude moment and every fixed demodulated twist jet across the full coherence window. | Prove a source-specific carrier-placement theorem, a genuinely growing-order/nonlocal filter, or direct signed Fejer scale-slope control. Fixed local twist complexity is exhausted generically. |
| `arithmetic_fidelity` | `narrow` | `AF-268`--`AF-270` make the source-fidelity boundary sharp: periodic infinite zero transport can preserve reflection symmetry, exact ordinate counts, Bohr recurrence, and a rational-prime-power frequency ray while moving zeros off line. | Constrain the **actual coefficient/Euler-factor law** or an equivalently rigid cross-prime relation. Recurrence and the correct frequency alphabet are too coarse. |
| `farey_discrepancy` | `narrow` | The latest occupation results globalize lower-tail control forced by the Farey/Möbius source while closing generic short-interval smoothing escapes. | Convert source-specific multiplicative occupation into the normalized angular/projective defect required by the endpoint; more absolute energy is not enough. |
| `mobius_cancellation` | `narrow` | Recent square-defect/random-multiplicative controls show that bulk covariance can look healthy while missing the deterministic prime-layer coordinate selected by the source. | Prove anti-concentration/anti-alignment or a correlation theorem on the actual prescribed prime/residue layer. Random-sign calibration is a matched control. |
| `nyman_beurling` | `narrow` | `NB-050` gives an exact normal form for weighted repair slack: it is the finite discretization gain between the reciprocal-integer Báez-Duarte grid and the denser finite Nyman grid `rho_{M/n}`. | Bound `d_floor(N/M)^2-delta_{M,N}^2` in the critical corridor `M d_N^2=O(1)` using information cheaper than the unknown optimal Nyman approximant. |
| `prime_circle` | `narrow` | The common-dilation/collision program now has exact reversal/parity structure on top of low-pathwidth torus coding; fixed-depth local collisions are increasingly classicalized. | Extract arithmetic information from the long transfer cocycle at depth growing with rank. |
| `prime_flute` | `continue` | `PF-287`--`PF-289` identify the physical heavy angle with heavy range projections and reduce noncompactness to one local normalized low/high correlation; high-side divergence is unnecessary. | Compute the optimized `PF-216`/`PF-227` correlation. Persistence kills compactness; decay reopens the positive essential-orthogonality/weak-trace problem. |
| `prime_lattice` | `narrow` | `PL-271` recovers the finite prime-power source from the growing finite Weil tower modulo the zero endpoint; `PL-272` separates identifiability from positivity; `PL-273` proves every nontrivial interior source atom is eventually permanently indefinite in high enough Galerkin bands. | Find a genuinely global cancellation/completion mechanism. Higher resolution identifies the source but does not turn individual prime-power atoms positive. |
| `robin_extremal` | `narrow` | Successive fringe reductions pin switches to the CA support edge/base-two ladder and collapse the remaining support recurrence to very low dimension. | Show the sparse candidate set has negligible total effect, or prove its residual source signal remains strong enough to drive the mixed race. |
| `visual_exploration` | `narrow` | `VIS-164`--`VIS-165` quantify the held-out-prime null: anchored collision exceedance has a support-dimension Haar tail, and discrepancy control preserves it into a positive-power growing-threshold regime. | Predeclare a representation budget and prove a relative zeta/control separation. Absolute decoder failure or polynomial collision growth no longer counts. |
| `weil_inertia` | `continue` | `WI-245` replaces the earlier exponential odd-support reserve by the exact Dirichlet--Robin gap and a quadratic-scale finite-radius source tariff. | Upper-bound the signed von-Mangoldt source on normalized odd first-crossing candidates below `(M(0)+B_Robin(a))/2` for a nontrivial radius range, without a global RH-scale estimate. |
| `weil_positivity` | `narrow` | The latest KMS/exterior-equivalence controls close another family of attempts to recover critical mixed-prime sign after generic thermal/spatial scalarization. | Construct a native arithmetic positive structure in which mixed-prime coupling, completion, and the Weil sign are fixed before scalarization/thermalization. |
| `xi_flow` | `narrow` | Adaptive cusp data can identify finite surgeries when depth tracks complexity, but relative heat-normalization controls show generic analytic/Jacobi regularity still does not select the theta source. | Use the actual single-lattice theta coefficient law or another source-specific modular relation with quantitative conditioning. |

## Cross-line structure

The strongest new synthesis is that **recovering the source is not the same as recovering the sign needed by the proof**. Prime Lattice makes this exact: the projective Galerkin tower can recover the finite source modulo the invisible zero endpoint, yet every interior prime-power atom becomes indefinite once enough Fourier modes are admitted. Fixed-band aliasing was therefore only one obstruction. Removing the alias does not create positivity; a successful Weil argument must use cancellation among atoms, completion, or another global order relation. Weil Inertia independently isolates a signed source functional that must beat an explicit and now quadratically persistent finite-radius archimedean tariff.

A second convergence is **directional information survives only if the representation preserves the coordinate selected by the endgame**. Analytic Frontier's matched packet survives every fixed local twist jet; Nyman compresses repair to a finite discretization gap that may still be target-oracular; Möbius random controls reproduce bulk energy while missing the deterministic prime-layer coordinate; Visual controls show that large decoder/collision complexity can be forced by an ordinary omitted prime. Adding ambient information or a richer norm is not progress unless it constrains the selected direction.

Prime Flute is the geometric analogue. Neither extension-strength factor is compact, and the decisive currency is the relative angle of the two physical heavy ranges. The latest reduction offers a cheap falsifier before any positive Weyl law: one repeated local correlation can kill compactness. Arithmetic Fidelity gives the spectral/source analogue: exact recurrence and the right prime-power frequency support can coexist with an off-critical divisor if the coefficient law changes. Both reinforce the portfolio emphasis on exact weights, signed discrepancy, carrier placement, projective compatibility, and relative geometry rather than representation richness.

The existing Master handoffs remain sufficient. The Bombieri-to-Weil-Inertia clue still tests sparse zero-side persistence; the Maaß--Selberg-to-Weil-Positivity clue still tests preservation of target sign in a genuinely global positive construction; Prime Flute's local clue already owns the heavy-angle gate. No new clue is created. `PL-271`--`PL-273` and `WI-245` are conceptually close, but there is still no exact normalization-preserving map from the Galerkin source matrices to the localized odd operator, so a new cross-line handoff would overstate the evidence.

## Highest-leverage unresolved questions

1. **Weil Inertia finite-radius signed source tariff.** Beat the exact `WI-245` Robin tariff on actual normalized odd first-crossing candidates without invoking a global RH-scale discrepancy estimate.
2. **Prime Flute local heavy-angle falsifier.** Compute the optimized energy-normalized `PF-216`/`PF-227` low/high correlation. A persistent limsup terminates the compactness route.
3. **Analytic Frontier carrier placement.** Beat the `ANF-179` fixed-jet matched control using the actual prime-minus-rough residual through a source-faithful nonlocal/growing-order statistic or direct signed Fejer control.
4. **Nyman finite discretization gap.** Bound the exact off-grid enrichment gain of `NB-050` in the critical corridor by source information independent of the unknown optimum.
5. **Global Weil source order beyond identifiability.** Explain how full prime-power source, archimedean completion, and cross-scale compatibility restore sign although each interior source atom is eventually indefinite.

## Portfolio changes proposed

No recommendation changes. `analytic_frontier`, `prime_flute`, and `weil_inertia` retain short, falsifiable implication chains adjacent to their endgames and remain `continue`; the other ten lines remain `narrow`. Nyman--Beurling remains the strongest promotion candidate among the narrow lines because `NB-050` isolates an exact finite quantity whose independent control would materially improve the target, but that control is not yet available. Prime Lattice improves the source/order diagnosis but `PL-273` is a negative classification, not a sign mechanism.

No merge, split, replacement, new line, Research Watch rotation, or new Master clue is proposed. The portfolio should remain stable while the next evidence attacks the exact finite-radius tariff, the Prime Flute local angle falsifier, the Analytic Frontier carrier-placement law, the Nyman discretization gap, or an exact cross-scale Weil order mechanism instead of reopening proxy classes already defeated by matched controls.