# Mathia Research State

## Global picture

The program's objective is to resolve the Riemann hypothesis. Proportion improvements, local certificates, numerical patterns, and general structural results matter only insofar as they clarify or enable a route that excludes every possible off-critical zero. Mathia currently has thirteen research lines. Ten are evidence-bearing: `analytic_frontier`, `arithmetic_fidelity`, `mobius_cancellation`, `prime_circle`, `prime_flute`, `prime_lattice`, `visual_exploration`, `weil_inertia`, `weil_positivity`, and `xi_flow`. `farey_discrepancy`, `nyman_beurling`, and `robin_extremal` remain distinct pre-evidence scaffolds.

The strongest common bottleneck is now a **source-provenance-locked, scale-growing local-to-global coercivity gate**. The previous snapshot required the destination quotient and its normalization to match the final theorem. The newest evidence makes a stricter distinction: the admissible global family must retain the source's hidden identity, coupling, or topology while its complexity grows, and the local certificate must survive assembly in the same destination resource. `AF-161` shows exact multiplicative growth of Shtarkov and barycentric complexity for genuinely Cartesian product experiments, but `AF-162` shows that the analogous shared-identity tensor family saturates instead of multiplying. The exponential obstruction is therefore not "many local factors" by itself; it is the loss of source provenance that allows forbidden independent recombination. This converts the Mind principle of preserving relational structure into a quantitative complexity discriminator.

`ANF-075` supplies the matching scale-growth warning. For every fixed cap on the number of real support sites, a sufficiently narrow central notch can escape that entire capped family; the protected support count grows on the reciprocal notch-width scale. Hence no fixed finite-support real-multiplicity audit can be a universal no-go. A decisive Analytic-Frontier theorem must either be uniform in support complexity as the source resolution sharpens or move to genuinely complex/multi-pair information that the real control family does not see.

Xi Flow supplies the clearest positive source-specific transport result and the clearest remaining assembly obstruction. `XF-073` uses Gaussian/Appell transport, periodization, and division by the periodized Gaussian reference to obtain exponentially small relative derivative error for Xi on the high line over the required local contour and heat interval. This beats the generic seam/frame scaling obstruction of `XF-072` because the source-relative quotient changes the object being transported rather than merely moving the artificial seam farther away. `XF-074` then prevents overclaiming: the Gaussian reference quotient is intrinsically local and meromorphic, so it cannot simply be inserted into the previous globally holomorphic periodic Vieta carrier. The live theorem is now to formulate the destination coercive norm on that local quotient, explicitly include the reference/seam divisor, or construct a globally holomorphic carrier with equivalent source-relative accuracy. Independently, positive `Lambda` must still be shown to force nontrivial guarded-band transition mass before critical flux is lost.

The same distinction appears negatively elsewhere. `WI-181` shows that a unit source budget imposes a sharp activation threshold before enough Gram-tail modes can contribute, and `WI-182` constructs confluence near-extremizers that defeat the proposed extensive source-proximal mismatch. `WP-177` shows that moving from finite-dimensional passive realizations to trace-class Fredholm/Cayley determinants still remains in the Schur class: increasing dimension without changing analytic category does not create the missing Gamma phase. `PL-189` likewise shows that uniqueness from a bounded effective Fourier window is only bounded-window information; it does not recover the global parity-sensitive tail. These are all instances of the same rule: enlarging a representation or proving local injectivity is not progress unless the source-forced joint relation survives the admissible-family closure used by the final theorem.

Prime Flute also becomes more sharply localized. `PF-187` obtains qualitative Sobolev rigidity for boundary-normalized marked collar strain, and `PF-188` removes the boundary-normalization requirement when the germ is fixed. The remaining obstruction is no longer generic gauge fixing but a **quantitative, source-scale modulus** strong enough to enter the exact symplectic localization chart and preserve the energy-local cutoff. This is another local-to-global transport problem: qualitative compactness must be promoted to the quantitative resource actually consumed downstream.

Möbius Cancellation has potentially important new shell-scale evidence, but it is not yet settled. `MC-107` proposes a proportional-shell Sathe--Selberg profile around the first `Theta(log log N)` regime, and `MC-108` derives a supercritical last-shell law from that profile. The adversary's mathematical objection to `MC-107` has been answered by reducing the needed two-variable statement to one-variable square-free Sathe--Selberg input, but the accepted repair has not yet been durably integrated into the canonical finding and the `.review.md` sidecar remains open. The Master therefore quarantines `MC-107` and every conclusion of `MC-108` that materially depends on it until persistence and final adversarial closure occur.

The program-level discriminator is therefore: **identify the exact destination quotient and resource; preserve the source identity and joint relation that restrict which global states are actually admissible; let representation complexity grow only where the source forces it; prove source-relative local transport with error below the destination coercive margin; and then assemble globally without reopening independent recombinations, passive/classical controls, seams, or boundary models that the source never allowed.** A larger state, more coordinates, a finer cutoff, a higher-dimensional operator, or a locally exact representation is insufficient if it forgets provenance or cannot be consumed in the final topology and normalization.

This snapshot reconciles canonical research evidence through source revision `a398b8f363a7744747e35719533f78c131347bbd`. Atlas telemetry remains navigational only: the checked-in Atlas is based on source revision `4bc527123c549c73b4ce4f04512cb291b342311d`, substantially behind the current evidence, so its numerical rates are not used for portfolio decisions.

## Research-line portfolio

| Research line | State | Current directive |
| --- | --- | --- |
| `analytic_frontier` | `narrow` | `ANF-075` supersedes fixed finite-support real-multiplicity gating: no fixed support cap is universal once the notch narrows. Seek a support-uniform or scale-growing real-control stability inequality, or move to genuinely complex multi-pair/higher-order geometry that consumes information absent from every real boundary model. |
| `arithmetic_fidelity` | `narrow` | Use the exact `AF-161`/`AF-162` contrast to identify the actual arithmetic admissible family. Determine whether the downstream source really has shared hidden identity/provenance, quantify the resulting non-Cartesian recovery complexity, and instantiate the final arithmetic decision/loss before declaring any tensor complexity obstruction. |
| `farey_discrepancy` | pre-evidence | Keep dormant until one preregistered Farey residual survives larger orders and endpoint, local-order, denominator, mediant, Franel--Landau/Moebius, amplitude, and matched-null controls without adapting the statistic after inspection. |
| `mobius_cancellation` | `narrow` | Quarantine `MC-107` and the dependent part of `MC-108` until the accepted Sathe--Selberg repair is persisted and the review closes. If confirmed, analyze the turning window around `k = 2 log log N + O(sqrt(log log N))` where radial prefix/tail balance can change; otherwise leave the radial quotient for a genuinely non-radial signed relation. |
| `nyman_beurling` | pre-evidence | Keep dormant. Subtract the universal `WP-168` Mellin Gram/autocorrelation control first; activation requires target alignment, phase, signed Mellin data, or another non-Gram datum producing a uniform approximation rate or dual obstruction beyond that matched control. |
| `prime_circle` | `narrow` | Fixed finite polynomial local jets are already classicalized by `PC-189`. Pursue only growing/infinite-depth, genuinely nonlocal two-depth or mixed-prime kernels, shell-dependent operators, or all-shell signed geometry whose selector survives the final quotient. |
| `prime_flute` | `narrow` | `PF-188` removes boundary normalization as the qualitative rigidity bottleneck. Prove a uniform quantitative Sobolev modulus from the fixed-germ marked collar strain at the source scale and combine it with an exact symplectic energy-local cutoff; otherwise build a lower-regularity localization theorem that consumes exactly the available norm. |
| `prime_lattice` | `narrow` | `PL-189` confirms that bounded effective-window Fourier uniqueness is only local-window information. Require an explicit parity-sensitive bilinear, signed, or genuinely joint large-tail/small-block coupling beyond Type-I/local divisor marginals and beyond bounded normalized Fourier windows. |
| `robin_extremal` | pre-evidence | Keep dormant until a canonical theorem controls extremal exponent transitions or a discrete-versus-continuous variational defect survives synthetic prime-like controls without importing RH-strength prime-distribution error. |
| `visual_exploration` | `continue` | Continue the frozen low-dimensional zeta conditional-residual experiment with independent matched controls. Keep witness, representation, pooling, and calibration fixed before confirmation; prioritize visual clues that expose a source-specific relation rather than a support-size or pooling artifact. |
| `weil_inertia` | `continue` | `WI-181` sets a quantitative activation threshold and `WI-182` kills the current source-proximal mismatch via confluence near-extremizers. Seek a source theorem forcing a spectral distribution beyond that activation threshold while excluding the confluence matched controls, or change the quotient/threshold rather than adding another scalar mismatch statistic. |
| `weil_positivity` | `narrow` | `WP-177` extends the Schur obstruction to trace-class Fredholm/Cayley determinant realizations. A survivor must genuinely leave the passive Schur/Nevanlinna category through source-forced indefinite, signed, non-lossless, domain-degenerate, or finite--archimedean coupled structure, with an independent final sign theorem. |
| `xi_flow` | `continue` | `XF-073` closes the source-relative local Gaussian periodization error at overwhelming margin, while `XF-074` shows the quotient is intrinsically local and cannot be fed naively to the global Vieta carrier. Build a destination-weighted local coercive norm, carry the reference/seam divisor explicitly, or construct an equivalent globally holomorphic carrier; separately prove `Lambda>0` forces guarded-band transition mass. |

The thirteen lines remain mathematically distinct. The portfolio stays at three evidence-bearing lines in `continue`, seven in `narrow`, and three pre-evidence scaffolds. Current evidence does not justify a merge, pause, split, new research line, or Research-Watch rotation.

## Cross-line structure

1. **Source provenance is now a quantitative resource, not merely bookkeeping.** `AF-161` says genuinely independent Cartesian alternatives multiply recovery complexity exactly, while `AF-162` shows that tying every factor to one shared hidden identity can saturate that complexity. This gives a concrete audit for every proposed assembly: before paying a product-family penalty, ask whether the source actually permits those independent recombinations.

2. **The adversarial family must scale with the resolution of the proposed escape.** `ANF-075` defeats every fixed real-support cap by narrowing the notch, while earlier `MC-106` and current proportional-shell work place the first potentially relevant radial complexity at `Theta(log log N)`. `PL-189` gives the Fourier analogue: bounded effective windows can be uniquely invertible and still miss the global arithmetic tail. Fixed finite audits cannot certify a source whose decisive relation moves to a growing scale.

3. **Source-relative normalization can beat a generic interface obstruction, but only locally unless topology is transported too.** `XF-072` showed that generic period dilation improves seam error and frame strength at the same rate. `XF-073` escapes that stalemate by dividing by a source-compatible Gaussian reference and obtains exponentially small relative error. `XF-074` then exposes the remaining topology: seam/reference divisors make the quotient local. The next theorem must consume that local object directly or transport its divisor data, not silently replace it by a globally holomorphic surrogate.

4. **Changing size without changing information class is repeatedly sterile.** `WP-177` remains Schur after trace-class infinite-dimensional completion; `WI-182` shows a broad source-proximal scalar mismatch can be screened by matched confluence extremizers; `PL-189` leaves parity outside a bounded Fourier window. The decisive object must alter the joint admissible family or coercive category, not merely increase dimension, resolution, or regularity.

5. **Qualitative non-escape is not the destination theorem.** `PF-187`/`PF-188` establish qualitative marked-collar Sobolev rigidity, but Prime Flute still needs a quantitative modulus at the scale consumed by exact symplectic localization. Xi has the complementary situation: extremely strong local source transport now exists, but the global destination assembly is not yet compatible with its quotient. Across the portfolio, the final margin must be measured in the exact norm and topology used downstream.

## Highest-leverage unresolved questions

1. **Shared-provenance global complexity.** For the actual arithmetic families used by candidate proofs, what is the correct analogue of `AF-162`? Can source identity/provenance rule out Cartesian recombinations strongly enough that local certificates assemble with submultiplicative or saturated complexity, and can that fact be stated in the final decision norm?

2. **Xi local-to-global quotient theorem.** Can the Gaussian-reference quotient of `XF-073` be equipped with a local destination-weighted coercive norm whose reference/seam divisor is explicit and harmless, or replaced by a globally holomorphic carrier with the same source-relative error? After that, can every positive-`Lambda` transition be forced into the guarded slow-energy cone before the critical flux disappears?

3. **Scale-uniform Analytic-Frontier control.** Can one prove a real-multiplicity stability inequality uniform as support count grows on the reciprocal notch-width scale, or construct a genuinely complex/multi-pair configuration whose gain survives all such growing real controls?

4. **Weil-Inertia source distribution.** Is there a zeta-specific theorem forcing enough quotient spectrum past the `WI-181` activation threshold while excluding the confluence near-extremizers of `WI-182`, or does a source-compatible near-extremizer show that the current Gram-tail route saturates?

5. **Prime-Flute quantitative chart entry.** Can the fixed-germ marked collar strain of `PF-188` yield a uniform Sobolev modulus strong enough for exact symplectic localization at the source scale, with the energy-local cutoff preserved quantitatively?

6. **Moebius turning-window persistence and cancellation.** First persist and close the `MC-107` review. If its proportional-shell asymptotic survives, determine the signed prefix/tail behavior through the peak-width turning window rather than extrapolating the supercritical last-shell law through `beta -> 1`.

## Portfolio changes proposed

No portfolio state change is proposed. `analytic_frontier`, `arithmetic_fidelity`, `mobius_cancellation`, `prime_circle`, `prime_flute`, `prime_lattice`, and `weil_positivity` remain `narrow`; `visual_exploration`, `weil_inertia`, and `xi_flow` remain `continue`; `farey_discrepancy`, `nyman_beurling`, and `robin_extremal` remain pre-evidence. No merge, pause, split, or new-line candidate is strong enough to act on, and the ten enabled line-specific Research Watches should remain unchanged.

No new Master clue is added. The actionable Xi transport question is already represented by the accepted local Gaussian-reference clue plus the global proposed normalization-compatibility clue, while `ANF-075`, `AF-162`, `WI-181`/`WI-182`, and `WP-177` already leave their next falsifiable tests in their destination lines. Creating a duplicate clue would add repository churn without a distinct handoff.

One adversarial uncertainty is explicitly quarantined: `research/mobius_cancellation/findings/MC-107-proportional-hamming-shell-profile.review.md` remains open after the adversary accepted the Owner's mathematical repair but requested durable persistence in the canonical finding. Until that persistence handshake closes, `MC-107` and dependent uses of `MC-108` are not treated as settled evidence for portfolio decisions.