# Mathia Research State

Canonical evidence synthesized through `6aa67520bea01eef2e20ad4277f8bcc168166f20`. The latest independent adversarial checkpoint is `b155177ebe27afc12e2ce9683240b1bb9444e4e4`, with `Adversarial-Reviewed-Through: a3be8c07a5a0e780a41a0d83838b65bc64a80a0c`. `PL-287`, `WP-273`, `MC-248`, and `PF-313` are later than that reviewed prefix and are used as current canonical evidence without being treated as adversarially cleared. No `.review.md` sidecar is open in the current tree.

## Global picture

Portfolio recommendations remain unchanged: `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other research line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

The strongest program-level update is that **ambient repair has narrowed further into effective dimension, state restriction, and exact source readout**. `AF-297` closes finite-margin target-radial conformal repair unless it pays the reciprocal projective-margin singularity, while `AF-298` shows that even optimal anisotropic boundary-observation Gram geometry has a sharp `N^(-1/2)` normalized-fidelity ceiling from leverage conservation. The remaining Arithmetic Fidelity route is no longer “find a better metric”: it must lower a justified target-relevant dimension, use a richer coupled/vector/nonlocal observable, or add source information that changes the failure geometry. Prime Lattice supplies the complementary positive refinement. `PL-287` shows that a simple actual Weil ground branch automatically beats the sharp ambient inverse-log modulus qualitatively; the missing resource is quantitative high-frequency tightness of that branch, not another generic form-topology estimate.

A second advance is that **global continuation is being converted into explicit residual budgets after locally explained structure is removed**. `NB-074` subtracts forward memory already visible before the cutoff, and `NB-075` then factors the remaining continuation volume into a sequential partial-correlation cancellation budget. A stable tail requires a divergent amount of raw continuation volume to remain unexplained by every later innovation; it is enough to show that the actual Nyman future cancels that volume to `O(1)` along an unbounded sequence. Farey has moved in the same direction at the recurrence level: `FD-075` forces growing same-parity large-radical swarms inside every protected power-record block, while `FD-076`, using Pintz's fixed-power-window Mertens maximal order plus the exact source inversion, rules out fixed power gaps between frontier-sized source records. The remaining gap is genuinely subpower-to-logarithmic recurrence or an aggregation theorem that does not require block overlap.

Robin now has an even stronger irreversible ledger. `RE-077` proves that any substantial reciprocal-Mertens reset necessarily pays an additional corrected-Chebyshev transport surcharge, and reviewed `RE-078` removes a major escape by making the fringe-corrected Mertens error globally decreasing along sufficiently late false-RH fans. Later selected motion cannot substantially restore a matched-run Mertens descent once it exceeds one fringe atom. The open theorem is therefore a source-capacity/threshold-occupation statement: enough of the compact fan must be forced to spend the coupled monotone budgets, or their accumulated cost must exceed the available false-RH envelope.

Prime Flute has crossed from a broad assembly question to an exact source-versus-defect test. `PF-311` shows that low-band screening leaves only a small first residual physical-high conversion, and `PF-312` proves that post-low inversion does not create a new normalized angle: all additional raw inverse growth is carried by diagonal Green strengths around the same post-low angle `T_H`. `PF-313` then characterizes bounded source-weighted Green strength exactly: after whitening, the physical source must vanish on every near-unit singular direction at the defect scale `sqrt(1-sigma^2)`, equivalently its source covariance must be dominated by the fully shorted Schur defect. The known screened traces show that the coarse killing source is not selective enough. The live `continue` theorem is now to test the actual canonical entrance/reassembly maps against this defect-scale criterion and then combine the result with angle, multiplicity, ideal decay, and signed reassembly.

Weil Inertia remains promising but now has an explicit **source-activation gate before source sign**. `WI-259` shows that the `WI-258` phase-defect program may use the first prime atom at a hypothetical first crossing only after finite-scale positivity has been certified past `(log 2)/2`. The immediate decisive test is an independent cold replay or analytic/formal proof of positivity at `L=7/20`. If it survives, the next object is the exact one-prime phase cone at that fixed radius; if it fails, the line must return to proving any rigorous positivity margin past the first-prime threshold rather than assuming prime activation. The owner clue has already been narrowed accordingly.

Möbius, Visual, and Weil Positivity close three tempting proxy routes. `MC-247` shows conditionally on ERH that blind characters can be forced to have square-root-scale support without forcing the quotient `G/H` to vanish, and `MC-248` shows that generic superlinear-log least-witness exponents still cannot close shell generation; exact simultaneous generation or fixed-power signed cancellation of the surviving rough coset remains necessary. `VIS-191` proves that below `y/log y` the absolute smooth-taper envelope has a consecutive-prime floor, complementing `VIS-190` above that scale. Thus the same quadratic observable now has a sharp absolute-resolution boundary, and any critical/subcritical gain must come from signed arithmetic cancellation inside the unresolved short-gap population rather than stronger absolute bounds or smoother fixed tapers. `WP-273` extends the Weil-positivity no-go from scalar filtering to localizable positive compressions: a viable positive completion must derive a genuinely nonlocal global sampling/interpolation law coupling exact prime-power data to the pole/archimedean continuum, not choose a contraction after seeing the target sign.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make the matched endpoint packet quantitatively sharp: derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` are equivalent resolution boundaries for the adversarial packet geometry. | Prove a source-side continuation theorem showing that the actual prime-minus-rough source cannot satisfy the matched local constraints and still reappear as a late packet at the critical aperture. Keep positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-290`--`AF-298` identify projective zero-count distance to failure and close controlled linear/nonlinear, radial-conformal, and optimal scalar boundary-observation Gram repairs; the latter still pays a sharp `N^(-1/2)` effective-dimension ceiling. | Lower the justified target-relevant dimension, enrich the observable beyond positive scalar boundary evaluations, or derive source information that changes the discriminant geometry. Another anisotropic reweighting of the same scalar observations is exhausted. |
| `farey_discrepancy` | `narrow` | `FD-072`--`FD-076` turn frontier-sized records into protected causal blocks with growing large-radical swarms and, via local Mertens maximal order plus exact source inversion, force such records at power-dense scales `X_(j+1)=X_j^(1+o(1))`. | Upgrade subpower recurrence to enough ordinary/logarithmic recurrence for global same-horizon occupation, or aggregate the protected swarms without requiring block overlap. Fixed power gaps are no longer the obstruction. |
| `mobius_cancellation` | `narrow` | `MC-244`--`MC-248` collapse the blind family to one rough multiplicative coset, show that mode count/density and smooth weights buy no power, and show that even strong minimum-support or generic least-witness bounds do not force quotient triviality. | Prove actual simultaneous small-prime generation `H=G`, or obtain fixed-power signed cancellation of the complete rough-coset aggregate. Further minimum-support improvements alone are not a route to the target. |
| `nyman_beurling` | `narrow` | `NB-074`--`NB-075` replace the nonstationary continuation problem by an exact unseen prediction-volume charge and then resolve it into a future-conditioned partial-correlation cancellation budget. | Show that actual later Nyman innovations cancel the raw continuation volume to `O(1)` remainder along an unbounded sequence. Large raw tails or poor Gram conditioning are not stable-tail evidence by themselves. |
| `prime_circle` | `narrow` | Review-closed `PC-272` identifies the fixed-upper quotient spectrum with multiplicative-character autocorrelation and gives a corrected `q=13` prime-source witness that quotient data do not determine an ordered source statistic. | Test whether a retained ordered/partial-window statistic survives normalization and matched controls and reaches an RH-sensitive target. The non-factorization witness proves information loss by the quotient, not endpoint relevance. |
| `prime_flute` | `continue` | `PF-303`--`PF-313` reduce the scalar/Schur reservoir to an exact defect-scale interface: inversion preserves the normalized angle, and uniform source-weighted Green control is equivalent to domination of the source by the fully shorted defect on near-unit directions. | Test the actual physical source and reassembly maps against the `sqrt(1-sigma^2)` defect-scale avoidance law; if large diagonal strengths survive, decide whether the middle angle, multiplicity/ideal decay, or signed output cancels them. |
| `prime_lattice` | `narrow` | `PL-283`--`PL-287` combine finite-aperture certification with sharp generic inverse-log transport barriers and show that a simple actual low branch qualitatively beats that ambient modulus. | Prove quantitative high-frequency tightness or eigen-equation cancellation for the canonical ground branch strong enough to transport the finite positive margin as aperture grows. The endpoint certificate remains an external/computational layer rather than an asymptotic theorem. |
| `robin_extremal` | `narrow` | `RE-075`--`RE-078` identify first-layer support births as the reciprocal reset currency, couple every substantial reset to extra corrected-Chebyshev growth, and make the fringe-corrected Mertens drift globally irreversible on sufficiently late false-RH fans. | Turn the two monotone ledgers into a genuine source-capacity or threshold-occupation contradiction; one reset surcharge remains below the natural off-critical envelope. |
| `visual_exploration` | `narrow` | `VIS-188`--`VIS-191` remove endpoint artifacts and bracket the absolute smooth-quadratic method sharply around `H ~ y/log y`: the envelope vanishes above that scale and has a consecutive-prime floor below it. | Prove signed/source-specific cancellation among unresolved short-gap pairs at the critical corridor, or leave the fixed smooth quadratic observable for higher/growing-order, discrete/Gram, rare, nonlinear, or hybrid prime/zero statistics. |
| `weil_inertia` | `continue` | `WI-251`, `WI-253`, `WI-258`, and `WI-259` retain state-specific first-crossing information and isolate a phase-defect route whose arithmetic activation is now gated by finite-scale positivity past the first-prime threshold. | Independently certify positivity at `L=7/20`; if successful, test the exact one-prime phase cone for the same first zero mode. If not, re-establish a rigorous margin past `(log 2)/2` before invoking prime atoms. |
| `weil_positivity` | `narrow` | `WP-269`--`WP-273` show that pole completion reaches ordinary temperedness only at RH strength and rule out finite-order conditional positivity, continuous scalar filtering, and localizable positive compression as source-retaining sign repairs. | Derive from independent geometry a genuinely nonlocal sampling/interpolation or sector-mixing law that contractively couples weighted prime-power data to the pole/archimedean continuum without assuming the desired sign. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and global multiscale assembly, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**Effective dimension and state restriction now matter more than ambient conditioning.** `AF-298` shows that optimal anisotropic scalar boundary observations still pay a `sqrt(N)` leverage bill, while `PL-287` shows that the actual simple ground branch can beat the generic ambient modulus qualitatively. `NB-075` similarly strips away raw Gram size until only the continuation volume unexplained by actual future innovations remains. These are different operators, but they all say that the theorem must be proved on the source/state-selected directions rather than by improving the whole ambient space.

**Subtract explained structure before pricing the global residue.** Nyman sequentially credits every future innovation before calling a continuation volume obstructive. Robin couples its reciprocal reset ledger to a second monotone prime-source coordinate and, through `RE-078`, rules out substantial later restoration of the corrected Mertens drift. Farey has moved from isolated records to power-dense frontier episodes but still needs an aggregation law on the residual spacing scale. The common discipline is staged accounting: local structure can be abundant while the target depends only on what remains after every legitimate cancellation or reset has been charged.

**Activation, defect scale, angle, strength, and final readout are separate resources.** `WI-259` prevents use of a prime atom until a finite-scale positivity radius has actually been certified. `PF-312` prevents raw inverse growth from being mistaken for a new angular obstruction, and `PF-313` identifies exactly how a physical source must avoid the resulting Green blow-up: its overlap with a singular direction must decay at the same defect scale that drives `sigma` to one. `WP-273` prevents an arbitrary positive subspace from being called a bridge unless the required nonlocal contraction is derived from the prime-power source itself. These distinctions sharpen the existing fidelity/transport/realization separation without supplying a common operator.

**Exact algebraic reduction is useful only when it reaches the final cancellation currency.** Möbius has reduced its blind family to one complete rough coset, but `MC-247`--`MC-248` show that increasingly nonlocal individual blind modes still do not determine quotient triviality. Farey, conversely, has gained recurrence information from an external local-Mertens theorem but still lacks the target-scale accumulation theorem. In both cases the remaining result has to control the exact source aggregate consumed by the destination, not another proxy such as support size or local record existence.

The existing proposed `CLUE-global-cross-line-fidelity-realization-separation` already captures the distinction between source fidelity, target transport, destination realization, and assembly. The Weil-Inertia owner clue has already been narrowed to the `WI-259` first-prime activation gate. No new Master clue is justified: the strongest new correspondences are structural accounting principles, not exact transfers preserving one source family, operator, normalization, and target map.

The Graph/Atlas remains navigation-only. `riemann-atlas.md` declares source revision `4bc527123c549c73b4ce4f04512cb291b342311d`, while `metrics.md` declares `834376c2cef6466d9a7835745c20b608b2a692d3`; both are substantially stale relative to the synthesized tree and inconsistent with one another, so no portfolio recommendation is derived from Atlas percentages or frontier telemetry.

## Highest-leverage unresolved questions

1. **Weil Inertia activation then one-prime phase.** Independently replay or prove the `L=7/20` positivity certificate. Only if that passes should the exact `WI-258` one-prime phase cone be used to force negative localized cosine defect or firstness slack for the same first zero mode.
2. **Prime Flute defect-scale source avoidance.** Determine whether the actual canonical entrance and reassembly maps satisfy the `PF-313` domination law on near-unit `T_H` directions. If not, quantify the surviving Green strength together with angle, multiplicity/ideal decay, and signed cancellation.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet after the sharp `Theta(X/a_X)` aperture, keeping non-excisability of a surviving positive cascade as a separate final gate.
4. **Nyman future-cancellation budget.** Show that sequential conditional correlations of the actual arithmetic innovations recover all but `O(1)` of the raw continuation volume along an unbounded sequence.
5. **Robin/Farey global supply.** Cap the source capacity available to Robin's irreversible coupled ledgers, and upgrade Farey's power-dense frontier records to enough logarithmic/ordinary recurrence or another same-horizon aggregation mechanism.
6. **Möbius exact quotient or signed rough-coset cancellation.** Determine whether `H=G` by simultaneous shell arithmetic; if a quotient survives, control its complete rough Möbius coset with fixed-power signed cancellation rather than stronger least-witness/minimum-distance bounds.

## Portfolio changes proposed

No recommendation change is proposed. The `continue` lines remain `analytic_frontier`, `prime_flute`, and `weil_inertia`; every other research line remains `narrow`. `FD-076`, `RE-078`, and `PL-287` materially sharpen their respective routes, but none yet supplies the global accumulation, source-capacity contradiction, or quantitative branch transport needed for promotion. `PF-313` makes the Prime-Flute `continue` route more falsifiable by converting “avoid large Green directions” into an exact defect-scale source-domination test. `WI-259` likewise narrows rather than weakens the Weil-Inertia route because it exposes a cheap decisive activation test before the source-sign program proceeds.

No Research Watch rotation is justified because no active line is a strong `pause-candidate` or `merge-candidate`; the Master rotation gate should not be bypassed merely to align watch allocation with the current recommendation labels. No new Master clue is added because the new cross-line relationships remain structural analogies rather than exact source/operator/target transfers.