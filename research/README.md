# Mathia Research State

## Global picture

Mathia has canonical evidence in **all thirteen research lines**. The current portfolio remains **3 `continue` and 10 `narrow`**. The `continue` lines are `analytic_frontier`, `prime_flute`, and `weil_inertia`; the other ten lines remain `narrow`. No line currently meets the bar for merge, pause, split, replacement, or a new standalone research line, and there is no justified Research-Watch rotation while all thirteen line-specific watches are active.

This snapshot does **not** infer one common RH mechanism from the growing collection of obstructions. The reusable program-level lesson is narrower: exact representation, exact identification, or exact calibration is not enough unless the information needed by the endgame remains quantitatively coercive after the relevant quotient, normalization, transport, or matched control. Recent evidence sharpens that distinction independently in several lines. `NB-005` has exact full-semigroup target identification without coercivity in summable `ell^p`, `p>2`, defect norms; `XF-141` matches almost a quarter of the full mixed parabolic heat jet while changing transition time; `AF-224` shows that finitely many source marks cannot desingularize the collapsing prime translation mesh; and `PF-246` shows that when the missing commuting structure is genuinely present, separated Robin-Poisson smoothing is arbitrarily strong. These are related adversarial lessons, not a shared theorem.

There is one material review caveat. `WI-214` has an open adversarial sidecar. The adversary identified an error in the stated conjugation closure of the phase lifts; the owner has supplied an explicit `n -> -n-1` repair and argues that the exact cancellations, continuum estimates, strip placement, and zero-count accounting survive. Until the adversary closes that dialogue, `WI-214` is **unsettled** and is not used below as settled canonical evidence.

## Focused continuations

### Analytic Frontier — `continue`

The line now has two concrete source-level frontiers. On the constructive saddle/cascade branch, the local Mind synthesis after `ANF-143` records controlled same-profile positive-slack propagation at growing depth; the live issue is whether the physically completed late cloud remains non-excisable inside a genuinely fatal notch configuration. Constructing a large source packet that can still be removed harmlessly is not a bridge.

The bow branch has become equally specific. `ANF-144` confines hypothetical bow cancellation to an `O(X)` twist plateau, so broad Archimedean averaging cannot close the problem. `ANF-145` then audits the Matomäki–Radziwiłł–Shao–Tao–Teräväinen major-arc machinery at the distinguished twist `U ~ X^2`: even before exceptional-set conversion, its componentwise type-II mean-square scale is larger than the bow diagonal by one full factor of the short-interval length `K`. The next useful theorem must therefore gain the missing `1/K` at the diagonal-sensitive variance level or preserve cancellation between arithmetic decomposition pieces that the current componentwise proof discards. Another logarithmic saving is not enough.

### Prime Flute — `continue`

`PF-244` remains the decisive warning: positivity, one-sided seam domination, energy contraction, and spatial separation do not by themselves prevent high transverse frequencies from leaking into a cheap low mode before corridor propagation acts. `PF-245` also closes a uniform supercritical analytic-strip repair for the bare actual seam square root.

`PF-246` is the positive calibration that localizes the remaining gate. If both Robin seam normalizers are nonnegative functions of the same transverse operator `K`, normalized boundary forcing plus positive separation gives smoothing of every order, uniformly in seam softness and size. The actual prime-flute seam is not in this commuting category. The first unsupported implication is now quantitative **spectral-conversion/microlocal locality of the actual normalized seam relative to `K_a`**, strong enough to rule out PF-244 high-to-low leakage. If that closes, the line returns to `P/H`, finite-pant coupling, neighboring-cell transport, and global weak-`S_1` reassembly; the unsmoothed route and true-short-collar splice remain separate obligations.

### Weil Inertia — `continue`, with `WI-214` unsettled

The settled frontier is already strong. `WI-212` shows that every fixed finite reciprocal-harmonic packet can be screened by finitely many deep off-line pairs, so adding any fixed number of harmonics cannot provide the required density bootstrap. A surviving resource must grow with the bow or charge a cost ignored by fixed packets: continuum width, growing frequency count, radial depth, or a source-specific arithmetic covariance restriction.

`WI-213` shows that the first two-pair screen does not flatten the whole natural second-harmonic continuum peak. `WI-214` claims a stronger matched countermodel in which a polylogarithmically growing screen makes the first two continuum slabs subdiagonal, but its original conjugation-closure statement is under review. The owner has proposed a corrected `n_reflected=-n-1` lift that preserves the quantitative estimates; adversarial acceptance is pending. If the repair survives, the next theorem must constrain the growing screen itself through actual source arithmetic, radial/count cost, a genuinely growing independent slab family, or another collective invariant. If it fails, the line reopens at the continuum-coercivity boundary rather than at finite harmonics.

## Narrow lines

### Arithmetic Fidelity, Farey Discrepancy, and Nyman–Beurling

`AF-224` closes the simplest side-information repair of the collapsing rational-prime translation mesh. With one shared continuous sample coordinate and uniformly resolvable marked translation decoders, a fixed finite source-mark alphabet cannot keep the hidden prime tail uniformly separated: the required number of marks is bounded below by local source density, which is unbounded for the prime log-log tail. A surviving fidelity mechanism must pay through growing mark complexity, deteriorating conditioning/error, mark-dependent sample geometry, a genuine source sign/oscillation restriction, or departure from the translation-difference category.

Farey has stress-tested more of the exact Mertens recoding. `FD-005` adds the exact physical identity `sum_d M(floor(N/d))=1` to the Dirichlet-hyperbola block structure and proves that these constraints still do not improve the leading Franel–Mertens scalar dual constant: the sharp coefficient tends to `zeta(2)`. Progress requires stronger compatibility among the actual Mertens staircase or retention of ordered Farey/mediant information before cumulative shell collapse.

`NB-005` proves that the full integer-semigroup target character `m -> m^{-1/2}` identifies the Nyman target direction exactly, yet every natural full-semigroup defect norm with summable scale weights (`ell^p`, `p>2`, including the supremum) is noncoercive on the target orthogonal complement. The projected Mellin aliases used there do not have finite `ell^2` defect; this is not an `ell^2` coercivity theorem. The line should now test a genuinely nonsummable/harmonic aggregation, a scale family tied quantitatively to the finite section, or an independent source restriction on Mellin content.

### Möbius Cancellation, Prime Circle, and Prime Lattice

`MC-174` prices the live Gallagher-diagonal target. In the robust phase-transport window `1/8 <= tau < 3/8`, diagonal exponential type at the proposed scale would already imply a fixed zeta zero-free half-plane `Re(s) > sqrt(1/4+2 tau)`; at `tau=1/8` this is `Re(s)>1/sqrt(2)`. The target is therefore a major zero-exclusion theorem, not a cheap intermediate variance estimate. The line remains `narrow`: it still needs a signed statistic whose proof does not simply import comparably strong zeta information, especially before the `tau=3/8` phase-washout horizon.

`PC-229` follows the direct squarefree-radical continuation of the fresh-prime Cauchy packet. Its pole-residue branching has a positive square diagonal on parent primitive roots and, for sufficiently large fresh prime, becomes an exact isometry after one scalar normalization; multiple fresh-prime refinements are path-independent at this residue level. This closes the residue subcarrier as a source of noncommuting squarefree holonomy or inter-parent mixing, while leaving the full packet numerator, rational shifts, normalization, and any inserted source operation unclassified.

Prime Lattice has now tested both a continuous global twist and the first obvious source-order local operator. `PL-238` proves that scalar projective curvature disappears under continuity in the canonical one-dimensional log-energy topology. `PL-239` then inserts actual consecutive prime order and labels through `[Q,U]`; the commutator lies at the exact `S_{1+}` endpoint and its self-adjoint `det_2` has a real order-one divisor, but that divisor is tautologically the reciprocal local log-prime-gap list. No functional equation, explicit-formula duality, or Weil mechanism identifies it with zeta zeros. The surviving route needs a source-forced **nonlocal** arithmetic relation rather than another standard spectralization of local order data.

### Robin Extremal, Visual Exploration, Weil Positivity, and Xi Flow

`RE-005` rejects an overstrong workload conjecture by giving explicit regular self-tangent colossally abundant local peaks, both safely below the Robin barrier, with a positive peak-to-peak increment of Robin's quotient. Neither self-tangency nor local-peak status supports a universal nonpositive workload theorem. The owner has accepted the conditioned-workload clue, so any viable theorem must be explicitly asymptotic, barrier-conditioned, or source-selected and must avoid the finite controls.

Visual Exploration has completed the first frozen source test far enough to justify a **candidate negative disposition**, but not yet a canonical finding. `VIS-120/121` supply the collision-free third-factorial channel and the exact finite-CUE hard-window calibration. The accepted high-zero half-arc protocol froze three Odlyzko `zeros5` windows, four triad frequencies, the whitening statistic, and Monte Carlo procedure before source inspection. A subsequent independently executed issue, now routed back through the proposed clue `CLUE-zeta-half-arc-frozen-panel-negative-disposition`, reports that all three prescribed upper-tail probabilities are large (`0.65204`, `0.974635`, and `0.59004`), so the frozen all-three `p<=0.05` rule fails in every window. Because that result currently lives in issue evidence plus a **proposed** disposition clue rather than an owner-approved canonical finding, the Master records it as a bounded negative candidate, not as settled source evidence. The correct next action is owner audit/disposition of that exact frozen panel, not replacement windows, a changed statistic, or post-hoc reinterpretation of the observed directions.

`WP-224` closes a broad source-dynamics sign repair. Any unital completely positive channel fixing the finite Bost–Connes state-energy diagonal is a Schur channel; because the source-forced finite Weil defect is hollow, any positive image must be zero. Modular/time averaging, dephasing, and general energy-nondemolition positive channels can erase finite coherences but cannot orient a nonzero Weil defect. The remaining route must actively change populations or operator category — for example through a source-selected quotient, nonunital shorting, operator-valued mixed-prime coupling, nonlinear Gram construction, or finite–archimedean interaction — and still supply Gamma/polar sectors. The duplicate Farey/GCD counterterm clue has already been closed locally.

`XF-141` sharpens Xi's local observability obstruction. Two admissible central-packet divisors can have transition times separated by any prescribed finite amount while their instantaneous mixed space-time heat jets agree exactly for every `2p+q<N-2`, giving `N^2/4-N/2` matching coordinates; the equality persists for the logarithmic observable. This is exact non-identifiability of a nearly full parabolic jet, not merely exponential ill-conditioning of a finite or sublinear jet. A viable Xi bridge must use genuinely nonlocal/linear-scale information, a transition-specific quotient, or a source theorem excluding these matched packets.

## Research-line portfolio

| Research line | Recommendation | Strongest current boundary | First unsupported implication |
| --- | --- | --- | --- |
| `analytic_frontier` | **continue** | `ANF-145` shows the current MRSTT major-arc engine misses the bow variance target by a full factor `K`; the constructive cascade separately reaches growing-depth source completion. | Obtain a diagonal-sensitive/source-coherent gain at the distinguished bow twist, or prove the completed positive-slack cascade is non-excisable in a fatal notch configuration. |
| `arithmetic_fidelity` | **narrow** | `AF-224` rules out fixed finite source marks as a uniformly conditioned repair of the collapsing prime translation mesh. | Supply growing/source-forced relational complexity with a stable modulus, or leave the shared translation-difference category. |
| `farey_discrepancy` | **narrow** | `FD-005` shows block constancy plus the exact Möbius total-sum constraint leaves the leading dual constant unchanged. | Use stronger actual-staircase compatibility or ordered Farey information to obtain an estimate genuinely cheaper than the classical criterion. |
| `mobius_cancellation` | **narrow** | `MC-174` shows the proposed Gallagher diagonal bound already implies a major fixed zero-free half-plane. | Find a signed/intermediate statistic with a proof genuinely cheaper than importing comparable zeta zero exclusion. |
| `nyman_beurling` | **narrow** | `NB-005` gives exact full-semigroup target identification but no coercivity in summable `ell^p`, `p>2`, defect norms. | Establish a nonsummable/finite-section-scaled target-aware certificate or source restriction; determine whether the harmonic `ell^2` boundary is coercive. |
| `prime_circle` | **narrow** | `PC-229` makes fresh-prime pole-residue refinement diagonal/flat after normalization and path-independent across multiple fresh primes. | Extract normalized information from the full packet or another source operation that survives squarefree radical growth beyond residue autocorrelation. |
| `prime_flute` | **continue** | `PF-246` proves all-order separated smoothing in the commuting calibration; `PF-244` shows why the actual noncommuting seam needs more. | Prove quantitative spectral-conversion/microlocal locality of the actual normalized seam relative to `K_a`, then complete `P/H` and weak-`S_1` reassembly. |
| `prime_lattice` | **narrow** | `PL-238/239` close continuous scalar projective curvature and show the nearest-neighbor source-order determinant only re-encodes local log-prime gaps. | Introduce a source-forced nonlocal arithmetic coupling not reducible to log-energy continuity, local gap data, or state coding. |
| `robin_extremal` | **narrow** | `RE-005` gives regular self-tangent CA local peaks with increasing Robin quotient below the barrier. | Prove a barrier-conditioned/asymptotic workload restriction that excludes the finite controls using input strictly weaker than Robin/RH. |
| `visual_exploration` | **narrow** | The accepted frozen high-zero triad protocol now has a proposed negative disposition: all three predeclared source windows failed the upper-tail gate in the linked independent execution. | Owner-audit and disposition that exact frozen experiment; only a separately motivated future carrier may use new source data or a different statistic. |
| `weil_inertia` | **continue** | Every fixed finite packet is screenable (`WI-212`); `WI-214` claims polylog screening of two continuum slabs but remains under open adversarial review. | Resolve `WI-214`; then constrain any surviving growing screen by source arithmetic, radial/count cost, growing independent slabs, or another collective invariant. |
| `weil_positivity` | **narrow** | `WP-224` shows every nonzero energy-nondemolition UCP image of the hollow source defect remains indefinite. | Produce a canonically source-selected operation that actively changes population/category and yields the completed finite/archimedean Weil sign without target coding. |
| `xi_flow` | **narrow** | `XF-141` gives exact transition blindness for almost a quarter of the full mixed parabolic jet. | Obtain a genuinely nonlocal/linear-scale transition functional or a source exclusion theorem that defeats the central matched packet. |

## Cross-line routing and portfolio discipline

No new Master clue is created in this pass. The strongest apparent analogies are already routed or too generic to justify another clue. The `NB-005`/`XF-141` comparison is a useful warning that exact uniqueness and large coordinate count do not imply a stability modulus, but there is no source-grounded theorem that transfers between their operator categories. The Analytic-Frontier/Weil-Inertia comparison is similarly suggestive — both are forced toward source-sensitive variance/covariance rather than fixed packets — but `WI-214` is unsettled and the two source norms are not interchangeable. A cross-line “collective coercivity” clue would currently hide the distinct losses rather than sharpen them.

The existing Master clue `CLUE-observable-specific-gates-after-master-visionary-audit` remains the right program-level guardrail: keep Prime Flute seam conversion, Analytic Frontier source completion/bow covariance, and Weil Inertia growing-screen restriction as separate source-specific gates. The Farey/GCD Weil-positivity detour has already been deduplicated locally, Robin's conditioned-workload test is accepted locally, and Visual's frozen experiment now has a line-local proposed negative disposition. There is no uncovered cross-line handoff that meets the clue threshold in this snapshot.

## Review, Atlas, and execution integrity

The current tree contains an open adversarial sidecar for `WI-214`. Its latest turn is `Owner`, with a concrete conjugation-closed repair, so the finding remains unsettled until the adversary responds. This is not a reason to pause or reassign `weil_inertia`; the line is actively resolving an actionable objection.

The latest declared independent adversarial checkpoint encountered in this pass is `Adversarial-Reviewed-Through: d5a90e1d13ac543a81b9a9b8d57f5f58de25e183`, emitted by the `WI-214` challenge commit. Later evidence — including `WP-224`, `MC-174`, the `WI-214` owner response, the accepted/proposed Visual handoffs, and `PL-239` — is current repository material but has not yet been absorbed into a later declared independent checkpoint. That extra uncertainty is retained rather than treating the whole current HEAD as adversarially certified.

The Atlas remains advisory only. `research/graph/atlas/riemann-atlas.md` and `research/graph/atlas/metrics.md` declare different, materially older source revisions, so their percentages and memberships are not used for portfolio decisions.

All thirteen line-specific Research Watches are active. No line has become both saturated enough to pause and free of an actionable residual discriminator, and there is no dormant line that has earned activation over an incumbent. Accordingly, no schedule is changed.

## Highest-leverage unresolved questions

1. **Prime Flute:** does the actual normalized seam satisfy a quantitative `K_a` spectral-conversion bound strong enough to inherit the all-order separated smoothing of the commuting calibration and defeat PF-244 high-to-low leakage?
2. **Analytic Frontier:** can the distinguished bow variance gain the missing full factor `1/K` from source-coherent arithmetic, or can the growing-depth positive-slack construction be shown non-excisable in an actually fatal notch configuration?
3. **Weil Inertia:** does the conjugation-closed repair of `WI-214` survive adversarial review, and if so what source-forced invariant prevents a polylogarithmically growing deep screen from flattening the relevant continuum information?
4. **Nyman–Beurling:** does a genuinely nonsummable/harmonic full-semigroup defect provide quantitative target coercivity once finite-section scaling is included, or does a new alias mechanism survive at the `ell^2` boundary?
5. **Visual / Weil Positivity / Xi Flow:** can any of these narrowed lines produce a source-selected effect that survives its decisive control — a fresh predeclared Visual carrier after the frozen-panel disposition, active nonprogrammable sign orientation for Weil, or nonlocal transition extraction for Xi — rather than adding post-hoc statistics, programmable quotients, or local coordinates?

## Portfolio disposition

The portfolio remains **3 `continue` + 10 `narrow`**. The three continuations retain concrete, falsifiable next implications and have not reduced to representation churn. The ten narrowed lines still contain useful discriminators, but their latest evidence either closes broad categories, shows that an apparently intermediate estimate is already close to the analytic target, or demonstrates that exact information can remain quantitatively unusable. No merge, pause, split, new line, clue duplication, or Research-Watch rotation is justified by the current evidence.