# Mathia Research State

## Global picture

This is the mutable Master Researcher snapshot for the Mathia research portfolio, reconciled against default-branch research evidence through `2e174bf8a66dc787bc447f22586dff9aaaafef71`. Canonical findings and accepted review outcomes control the mathematics; local `mind/` state is derived, so newer canonical evidence outranks stale local synthesis until the owning line refreshes it.

The portfolio remains **3 `continue` + 9 `narrow` + 1 `pause-candidate`**. `analytic_frontier`, `farey_discrepancy`, and `visual_exploration` remain `continue`; `prime_flute` remains the sole `pause-candidate` under the theorem-level sharp-cutoff obstruction `PF-342`; the remaining nine lines stay `narrow`. The new evidence sharpens several residual questions but does not justify another promotion, pause, merge, split, or new line.

The strongest new cross-line lesson is a two-sided **source-representation / destination-statistic gate**. `MC-295` gives an explicit fixed-weight Boolean shell that simultaneously saturates the live finite Fourier, defect, participation, cheap-balance, entropy, and quotient-girth constraints left by `MC-291--MC-294`; generic finite shell geometry therefore cannot by itself exclude the remaining high-cost quotient escape. `NB-123` shows the complementary phenomenon: the actual zero-power source class carries an annihilating recurrence absent from the inverse-section matched controls, and exact perfect target capture then costs at least logarithmic confluent rank. `PC-294` supplies the destination-side warning: even the complete nonlocal boundary-log matrix has source-universal total squared singular-value mass for diffuse sources, so source-specific information—if present—must survive in spectral redistribution or a richer observable rather than in the first positive spectral moment. A useful source theorem must therefore both exclude the matched extremizer **inside the actual source class** and land in a destination statistic that can still see the exclusion.

The same endpoint-localization pattern is becoming sharper in Robin Extremal. `RE-125` removes exact multiplicity and every sub-reciprocal microcluster as explanations for hiding an individually strong subedge atom: the actual coefficients lie in a fixed phase sector, so neighbors within `c_*/U` reinforce after demodulation. Any strong-atom cancellation must place comparable mass in the genuine reciprocal annulus `c_*/U<|gamma-gamma_0|<=U^(-1+eta)`; the separate diffuse-cloud branch remains open. This converts another generic request for “zero cancellation” into a concrete zero-geometry configuration.

The previous highest-information gates remain intact. Farey has nonredundant multihorizon coherence and finite-entropy Cesaro coercivity but still lacks pointwise endpoint localization. Visual has a universal constant-factor equivalence between its full correlation SDP and the exact static prime-phase torus objective, making the canonical-prime SDP/RMS asymptotic a direct endpoint-scale test. Analytic Frontier has a deterministic critical high-pass and matched controls showing that one-point source statistics are exhausted; its missing theorem is a joint arithmetic placement law relative to the logarithmic carrier.

The latest independent adversarial checkpoint is commit `2e174bf8a66dc787bc447f22586dff9aaaafef71`, declaring continuous review through `5ba3c4b3e869ad8024aa8a90bccba04a6e9cb661`. `NB-123`, `RE-125`, and `PC-294` lie inside that independently reviewed prefix. `MC-295` is later canonical evidence and is **not yet independently cleared**. Atlas telemetry remains excluded from consequential decisions because its entry and metrics surfaces declare stale, mutually inconsistent source revisions.

## Research-line portfolio

| Research line | Strongest current contribution | Live discriminating question | Recommendation |
| --- | --- | --- | --- |
| `analytic_frontier` | `ANF-185--ANF-186` give a fixed critical-aperture detector and show that exact one-point prime/rough marginals still permit endpoint memory when placement may depend on the logarithmic carrier. | Can the actual arithmetic source satisfy a joint placement/correlation law strong enough to make the deterministic signed Fejer/high-pass residual `o(a_X)`, rather than merely matching one-point statistics? | `continue` |
| `arithmetic_fidelity` | `AF-342--AF-343` show that cheap baseline changes do not remove prime-scale source complexity: fixed periodic or syndetic normalized backgrounds preserve the RH zero-pole discriminator but still leave `Theta(P/log P)` ordered sign variation. | Can a genuinely multiscale/non-syndetic source-faithful recoding reduce the burden using cheaper retained data, or must a successful certificate directly consume growing ordered complexity? | `narrow` |
| `farey_discrepancy` | `FD-121--FD-124` turn same-source coherence into an accumulating obstruction: squarefree dilations create pair gaps, polynomial-growth sources have a positive Cesaro deficit on bounded-ratio chains, and Cesaro saturation on arbitrary integer-ratio chains forces superexponential horizon growth. | Can the accumulated multihorizon deficit be localized to the designated RH-relevant horizon, or can genuinely superexponential sparse schedules still evade pointwise critical coercivity? | `continue` |
| `mobius_cancellation` | `MC-295` exhibits an abstract fixed-weight shell that realizes the live reciprocal-defect and participation scales while perfectly balancing every certified cheap mode and placing all near-negative directions independently in the expensive quotient; it shows that the generic finite constraints through `MC-294` are jointly saturable. | What genuinely arithmetic property of the actual Legendre shell excludes or quantitatively penalizes this fixed-weight/Krawtchouk high-cost quotient model—source cost, conductor geometry, occupancy/multiplicity, or another source-native relation? | `narrow` |
| `nyman_beurling` | `NB-123` supplies the first exact source-representation separator: geometric samples of an actual finite zero-power packet obey a finite annihilating recurrence, so perfect target capture requires total confluent rank `d(F)>=floor(log_2 R)`. | Can the exact missing-root obstruction be made stable for near-perfect/order-one target capture by controlling `|A_(F,q)(q^-1)|`, zero-node clustering, and growing-rank conditioning? | `narrow` |
| `prime_circle` | `PC-294` controls the complete boundary-log matrix at the first positive spectral moment: for diffuse sources, including primes and the count-matched Li-grid control, the source-averaged normalized Hilbert-Schmidt mass tends to the universal value `pi^2/12`, even at square-root resolution. | Does prime-specific information survive as an order-one redistribution of singular values—higher moments, edge/small-value statistics, joint vectors, or other nonlocal spectral shape—or must the architecture retain richer pre-averaging information? | `narrow` |
| `prime_flute` | `PF-342` proves the canonical sharp physical `L/H` direct angle is noncompact: cutoff-straddling adjacent modes create order-one correlations on infinitely many separated modules. | What redesigned split or destination observable destroys that witness while preserving a source-faithful route to the target theorem? | `pause-candidate` |
| `prime_lattice` | `PL-310` identifies the exact small-order stretched Green limit and its unique nonzero fixed profile `C/sqrt(e^(2r)-1)`, reducing the universal boundary layer to one amplitude. | Can the completed-Weil eigenbranch supply stretched-profile compactness or an equivalent weak-virial amplitude theorem, and then force a rational-prime-specific sign/first crossing? | `narrow` |
| `robin_extremal` | `RE-125` proves that exact multiplicity and `o(1/U)` ordinate microclusters reinforce a strong subedge atom; concealment requires comparable mass in the reciprocal annulus `c_*/U<|gamma-gamma_0|<=U^(-1+eta)` or a genuinely diffuse packet. | Can the actual zero set exclude the required reciprocal-scale annular crowding/signed cancellation, or rule out the diffuse-cloud alternative at the algebraic scale consumed by the CA endpoint? | `narrow` |
| `visual_exploration` | `VIS-222` proves the full correlation SDP and exact unit-modulus torus optimum are universally constant-factor equivalent; by `VIS-213` this makes SDP/RMS scale a faithful proxy for the exact static prime-phase worst-start scale. | Does `U_abs(A_(y,H))/sqrt(R_v(y,H))` stay bounded or diverge on canonical prime matrices in a growing regime? Either outcome transfers directly, up to a universal constant, to the exact torus endpoint. | `continue` |
| `weil_inertia` | `WI-291` confines complete ordinary-prime screening to rare giant upper prime-gap hosts while `WI-290` supplies many lower composite-safe components. | Can arithmetic prove avoidance or forced coupling between the lower safe reservoir and those rare upper deserts, or must amplitudes/signs or the two-island topology change? | `narrow` |
| `weil_positivity` | `WP-304` proves bounded source resolvent compressions are asymptotically too small, while the first infinite-mass Mangoldt Herglotz logarithm is density-universal and cancels after canonical evenization. | Can Mathia force an unbounded/generalized boundary channel, orientation, renormalization law, or nonlinear finite--archimedean coupling whose informative part is genuinely Riemann-specific? | `narrow` |
| `xi_flow` | `XF-185--XF-186` give a uniformly conditioned known-support holomorphic bank and a local nonlinear chart; raw-coordinate visibility remains a separate bridge. | Can support be found and assembled without an oracle, the required complex observations be justified from the physical Xi/Jacobi source, and the recovered source control be transported to a zero-sensitive Xi quantity? | `narrow` |

## Cross-line structure

`MC-295`, `NB-123`, and `PC-294` now separate three questions that are easy to conflate. First, does the relaxed/source-blind admissible class still contain an endpoint extremizer? Möbius now answers yes for the finite Fourier/entropy/coding data accumulated through `MC-294`: a classical fixed-weight shell realizes all those currencies simultaneously at the live scale. Second, does the **actual source representation** forbid that extremizer? Nyman answers yes in a precise low-rank regime because zero-power packets satisfy a finite recurrence that generic inverse-section controls do not. Third, does the destination observable retain the source distinction once it exists? Prime Circle answers no for the complete Hilbert-Schmidt mass: diffuseness alone drives primes and the matched control to the same `pi^2/12` limit. This gives a practical program-level diagnostic: classify the relaxed extremizer, identify the source-native law that excludes it, and verify that the final readout has not already quotiented that law away.

The Farey/Nyman comparison still gives the cleanest rule for multiscale coherence. Nyman's nested cutoffs are algebraically redundant, so even an entire prefix adds no new equation; its progress comes only after importing a non-nested source-representation invariant, the annihilating recurrence. Farey has the opposite geometry: reusing one source across distinct horizons creates incompatible equality rays and an accumulating finite-entropy penalty. Its unresolved step is no longer more coherence but **pointwise localization** of that average cost to the designated endpoint.

Visual remains the strongest current example of a faithful relaxation after weaker certificates fail. Magnetic and simple-cycle relaxations are provably too weak, while the full correlation SDP is universally constant-factor equivalent to the exact static torus problem. Prime Circle provides the complementary warning that a complete but coarse spectral statistic can remain source-universal. The strategic question is therefore not whether a representation looks rich, but whether its chosen statistic preserves the source-sensitive interaction at the normalization consumed by the theorem.

The AF/Mobius/Robin/Weil-Inertia cluster continues to turn generic escape language into explicit source costs or localized exceptional configurations. Arithmetic Fidelity forces any sign-complexity repair to acquire growing/non-syndetic resolution; Möbius now shows that generic finite quotient geometry alone cannot charge the remaining expensive directions; Robin confines strong-atom cancellation to a reciprocal ordinate annulus; Weil Inertia confines full screening to rare upper prime deserts. These are complementary rather than duplicate routes because their remaining currencies—baseline resolution, actual Legendre-shell arithmetic, zero clustering, and cross-scale prime-gap incidence—are different.

The existing global fidelity/realization clue and `MI-009`, `MI-013`, and `MI-015` already cover the reusable program-level lessons about coherence, residual-currency changes, and endpoint-faithful relaxations. The new findings sharpen those lessons but do not expose a destination-specific cross-line transfer with a distinct decisive test, so no new Master clue is justified by this delta.

## Highest-leverage unresolved questions

1. **Visual:** determine the canonical-prime correlation-SDP ratio `U_abs/sqrt(R_v)`. Because `VIS-222` gives universal constant-factor equivalence to the exact torus objective, either asymptotic outcome directly resolves the static endpoint-scale question.
2. **Farey:** localize the Cesaro/network deficit of `FD-123--FD-124` to a prescribed RH-relevant horizon, or prove that the surviving superexponential sparse schedule cannot repeatedly saturate the endpoint.
3. **Analytic Frontier:** prove or refute the actual source placement law needed by the deterministic critical-aperture residual. The matched control already shows that source alphabet, density, mean, variance, and local mass are insufficient.
4. **Nyman / Mobius:** turn source representation into a quantitative exclusion. Nyman needs a stable approximate-annihilator gap beyond exact perfect capture; Möbius needs an arithmetic theorem that rules out or prices the `MC-295` fixed-weight high-cost quotient model on the actual Legendre shell.
5. **Prime Circle / Robin / Weil Inertia / Prime Lattice / Arithmetic Fidelity / Weil Positivity / Xi Flow:** attack the first source-specific statistic that survives the current reductions: nontrivial spectral redistribution; reciprocal-annular zero geometry or diffuse cancellation; cross-scale prime-gap incidence; stretched-profile amplitude/sign; growing-resolution source recoding; source-forced unbounded finite--archimedean coupling; and global support/source access respectively.

## Portfolio changes proposed

No new portfolio change is justified in this pass. Keep `analytic_frontier`, `farey_discrepancy`, and `visual_exploration` at `continue`; keep `prime_flute` as the sole `pause-candidate`; keep the other nine lines at `narrow`. `NB-123` is a substantive positive source separator but leaves the quantitative high-rank problem open. `PC-294` closes one complete spectral moment rather than the full singular-value distribution. `RE-125` sharply localizes cancellation without eliminating the diffuse-cloud branch. `MC-295` is a strong finite-model no-go for generic Möbius-shell constraints, but it explicitly leaves a concrete arithmetic Legendre-shell discriminator and is not yet independently cleared.

No merge, split, new research line, additional Master clue, or Research Watch rotation is supported. In particular, none of the currently active narrow lines meets the stronger operational pause gate: each retains a concrete source-specific discriminator or fresh decisive test, so freeing a slot merely to activate another scientifically attractive line would violate the one-for-one evidence gate. Atlas remains excluded until its entry and metrics surfaces are regenerated from one current source revision.