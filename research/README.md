# Mathia Research State

## Global picture

Mathia currently has thirteen research lines. Ten carry canonical evidence: `analytic_frontier`, `arithmetic_fidelity`, `mobius_cancellation`, `prime_circle`, `prime_flute`, `prime_lattice`, `visual_exploration`, `weil_inertia`, `weil_positivity`, and `xi_flow`. `farey_discrepancy`, `nyman_beurling`, and `robin_extremal` remain pre-evidence scaffolds.

The evidence still does **not** support one common positive mechanism or one mathematically meaningful global bottleneck shared by the program. Progress is audited line by line at the first genuinely unsupported implication. In this snapshot the largest strategic change is negative but decisive: the current Xi Vieta-prefix architecture no longer satisfies the `continue` gate. By contrast, Prime Flute continues to remove concrete terms from a still-live operator comparison, and Weil Inertia retains one exact source-fixed cancellation test.

**Xi Flow is narrowed.** `XF-100` first proves that the existing guarded prefix is asymptotically prime-blind and that its periodic shadow is generated from the archimedean/polar endpoint data alone. `XF-101` then gives a direct nonidentifiability theorem: for every fixed positive transition time in the observed interval there is a periodic backward-heat trajectory with exactly that threshold whose complete visible low-Vieta time history is identical to the current periodic shadow. `XF-102` closes the simplest prime-band repair as well. Even after legally inserting the first prime atoms throughout any fixed physical band, the prefix still has length `K=o(N)` and `o(N)` normalized moment cost, so an invisible collision crystal can again be implanted at an arbitrary positive transition time without changing the complete visible history. The failure is therefore no longer “we have not yet proved Xi-specific coercivity in these coordinates”; **the current sublinear autonomous prefix does not identify the transition even when a fixed band of prime data is present**. A surviving Xi route must change the state consumed downstream: for example a non-prefix Xi-specific analytic constraint, a bandwidth/source interface whose complexity is no longer sublinear in the carrier, or an observable whose heat dynamics are not closed by the triangular prefix system.

**Prime Flute remains the clearest positive proof-interface progression.** `PF-207` shows that dilating the whole critical Dirichlet block restores exactly the `w_n^2` physical scale lost by coefficient-only normalization. `PF-208` proves that scale-matched cutoff commutator errors are strongly trace-summable, and `PF-209` gives the same prime-summable currency for fixed-profile artificial-boundary recoupling. `PF-210` further shows that a remaining logarithmic-rank low-frequency sector would already be globally weak trace class if each prime module carries only `O(1+a_n)` such modes with norm `O(d_n)`; absolute summability of per-module weak norms is not required. These results do **not** prove the native global resolvent has those rank/norm bounds. They move the first unsupported implication to a genuinely global-to-buffer theorem that must reduce the native two-metric hyperbolic resolvent comparison to the scale-correct local calculus without an uncontrolled exterior/low-frequency loss. The true-short-collar splice remains independent.

**Analytic Frontier contracts its own noncompactness problem rather than creating a new global gate.** `ANF-104` shows that a sparse remote tail can remain source-heavy inside a near-extremizer only by maintaining macroscopic negative coupling to the core, which forces an explicit endpoint-height cost as the horizontal gap grows. `ANF-105` extends this to arbitrary profile splittings: if the aggregate source and notch cross-interactions decouple, one component inherits near-extremality and essentially all normalized notch exposure. Hence superlinear horizontal spread created merely by separated profile fragmentation cannot enlarge the fatal exposure envelope. The live own-line question is now whether the remaining interaction-bearing chambers can attain the target notch exposure, not whether generic escape to remote components defeats compactness.

**Arithmetic Fidelity continues to calibrate rather than solve its inverse problem.** `AF-189` makes the first reachable Euler resonance asymptotically sharp for the high-separation parity control, while `AF-190` identifies the finite-overlap regime with a nontrivial discrete-Gaussian/Jacobi-theta fidelity profile controlled by `m/q^2`. This materially improves the phase diagram of one exact matched-control family, but it is not a source-class inverse theorem. The unresolved implication remains a lower stability/converse statement for a declared arithmetic source class at the relevant bandwidth.

Several other lines accumulate strong negative classification. `MC-135` strengthens the dyadic persistence obstruction to space level by factoring every divisor-complex inclusion across a factor-two gap through the prime-2 star; `MC-136` identifies this as the generic minimum-weight coning mechanism of scalar quota complexes; and `MC-137` shows that even any fixed finite-dimensional additive vector-quota filtration has a uniformly bounded ordinary homotopy-persistence horizon along every cofinal positive direction. `PC-211` reduces the same-level primitive/Haar cross-transfer to the scalar `Phi_N(1)=exp(Lambda(N))`, and `PC-212` shows that the canonical common-refinement Gram geometry is ordinary profinite cylinder-function geometry whose centered coupling vanishes for coprime conductors. `PL-208`--`PL-210` show that holomorphic `z`-dependent resolvent transport, exact `log p` rank-one target coupling, and even irreducible noncommuting finite-rank prime channels remain spectrally programmable.

`PL-211` is useful because it finally tests a genuinely source-forced arithmetic relation rather than another programmable prime-channel model. In the canonical Bost--Connes divisibility algebra, the natural finite-prime Möbius observable has an `L^2` threshold at `beta=1/2`, but that threshold is exactly square-free support: arbitrary unimodular prime phases have the same norm transition. The actual Möbius orientation becomes `L^2`-coherent only for `beta>1`. This does not provide an RH bridge, but it creates a precise cross-line test for Möbius Cancellation: find a canonical phase-sensitive KMS statistic near the critical half that defeats matched prime-phase controls and transfers to Mertens excursion, or close that route. The handoff is persisted as `research/mobius_cancellation/clues/CLUE-bost-connes-phase-sensitive-mobius-transfer.md`.

**Weil Positivity is no longer review-quarantined at `WP-194`.** The adversarial sidecar was removed while the finding survived, so under repository review semantics the repaired chain is accepted. `WP-198` then identifies a sharp infinite-dimensional boundary: the finite zero-mode obstruction survives for commuting positive pairs under the stronger `S^(4k-1)` source-variation condition but fails in the natural `S^(4k)` class, where an explicit rough centered bath can manufacture full Weil-cone positivity. `WP-199` strengthens that control: enough copies of the same nonarithmetic bath can stabilize the sign defect of **any** finite monotone-positive payload, commuting or not. Infinite-dimensional roughness is therefore a real category exit, but generic positivity there is even less selective; a viable completion must derive and couple its critical reservoir intrinsically from the Riemann source rather than append a detachable stabilizer.

Visual Exploration also closes a broad family rather than finding a positive signal. `VIS-087` extends the inverse-theta clock null to every fixed sublinear power-law Gram block with polynomial prime support by choosing a sufficiently high but fixed Taylor order. `VIS-088` shows that every observable with polynomially growing Lipschitz sensitivity inherits that collapse, and `VIS-089` does the same for arbitrary discontinuous decisions with an inverse-polynomially robust margin. A local prime-phase visualization on such scales is therefore not revived by dimension, ordinary aggregation, or robust thresholding; a survivor must move to nearly linear/nonlocal sampling, justify genuinely more singular sensitivity, or import an independently informative analytic coordinate.

The Riemann Atlas remains historical/structural navigation only. Its declared `source_revision` is still `4bc527123c549c73b4ce4f04512cb291b342311d`, far behind the evidence reconciled here. Its percentages therefore remain stale derived telemetry and do not influence the portfolio decision.

## Research-line portfolio

| Research line | State | Strongest current contribution and first unsupported implication |
| --- | --- | --- |
| `analytic_frontier` | `narrow` | `ANF-103`--`ANF-105` remove source-negligible remote tails and asymptotically decoupled profile fragmentation, while forcing any source-heavy remote tail to maintain endpoint-amplified negative core coupling. The first unsupported theorem is now the fixed-notch exposure bound on the remaining interaction-bearing near-extremizer chambers, or a physical construction saturating it. |
| `arithmetic_fidelity` | `narrow` | `AF-187`--`AF-190` give an exact bandwidth/complexity phase diagram for the parity controls in the actual Euler logarithm, including the sharp single-resonance regime and finite-overlap theta profile. This calibrates one hard control family; the line still needs a source-class recovery lower bound or converse theorem, not another matched upper obstruction. |
| `farey_discrepancy` | pre-evidence | Remain dormant until a preregistered Farey invariant survives endpoint, local-order, denominator, mediant, Franel--Landau/Möbius, amplitude, and matched-null controls and yields a quantitative discriminator beyond the classical scalar criterion. |
| `mobius_cancellation` | `narrow` | `MC-135`--`MC-137` close ordinary long-range homotopy transport for the Björner divisor filtration, every fixed scalar quota reweighting with a positive minimum, and fixed finite-dimensional additive vector quotas along cofinal positive directions. A topological continuation must leave that class and still be cheaper than reconstructing Mertens. `PL-211` now supplies a separate source-forced KMS test: the critical-half norm threshold is phase-blind support, so any Bost--Connes continuation must produce a genuinely orientation-sensitive statistic that transfers to first-absolute excursion. |
| `nyman_beurling` | pre-evidence | Remain dormant until a target-relative datum beyond universal Gram/autocorrelation geometry yields a falsifiable approximation-rate improvement rather than another equivalent scalar reformulation. |
| `prime_circle` | `narrow` | `PC-209`--`PC-212` classify the normalized weighted covers, their Haar-refinement star algebra, the direct primitive/Haar cross-transfer, and the canonical common-refinement coefficient Gram. Same-level and pairwise-refined linear geometry reduces to universal Haar/profinite structure and classical cyclotomic scalars. A survivor must retain old/new incidence, mixed-composite tensor information, nonlinear structure, or another source relation lost by pairwise Gram contraction. |
| `prime_flute` | `continue` | `PF-207`--`PF-209` recover the physical critical scale in Dirichlet model blocks and make cutoff/boundary model errors prime-summable; `PF-210` shows logarithmic-rank `O(d_n)` low modes would already assemble in weak `S_1`. The first unsupported implication is the native global-to-buffer/exterior reduction with those scale/rank bounds, followed separately by the true-short-collar splice. |
| `prime_lattice` | `narrow` | `PL-208`--`PL-210` close generic `z`-dependent transport, rank-one `log p` target coupling, and abstract irreducible multichannel prime coupling as programmable. `PL-211` reaches a canonical Bost--Connes divisibility relation but separates the phase-blind `beta=1/2` square-free-support threshold from Möbius orientation coherence at `beta>1`. A live route must derive a source-forced phase/global relation whose target consequence is not support, arbitrary spectral programming, or an RH-equivalent encoding. |
| `robin_extremal` | pre-evidence | Remain dormant until a canonical theorem controls extremal exponent transitions or a discrete-versus-continuous variational defect survives synthetic prime-like controls without importing RH-strength prime-distribution error. |
| `visual_exploration` | `narrow` | `VIS-087`--`VIS-089` classify every fixed sublinear power-law Gram/prime-phase block with polynomial prime support under arbitrary fixed-order clock subtraction, including polynomial-sensitivity observables and discontinuous decisions with inverse-polynomial margin. Future work needs nearly linear/nonlocal sampling, quantitatively justified singular sensitivity, or an independent analytic source coordinate. |
| `weil_inertia` | `continue` | `WI-195` gives the exact multiplicative Gallagher `L^2` representation and `ANF-102` rules out the window-uniform saving. The live theorem remains pointwise and signed at the distinguished source twist: prove macroscopic negative off-diagonal cancellation or a canonical subtraction leaving an `o(XK log X)` residual, or close the present bow bridge. |
| `weil_positivity` | `narrow` | The repaired `WP-194` chain is accepted. `WP-197` closes finite-dimensional monotone-positive noncommuting backgrounds to zero-mode support, while `WP-198`--`WP-199` show that rough `S^(4k)\S^(4k-1)` centered baths can manufacture full cone positivity around arbitrary finite payloads. A survivor must derive an intrinsic finite/archimedean/global reservoir and an order theorem that excludes these detachable nonarithmetic stabilizers. |
| `xi_flow` | `narrow` | `XF-101` proves complete transition-time nonidentifiability for the current low Vieta history, and `XF-102` extends the obstruction to every fixed physical prime band encoded as a sublinear autonomous power-sum prefix with `o(N)` moment budget. The current bridge is therefore classified negatively. A continuation must change the source/destination state itself rather than seek another coercivity theorem determined by such a prefix. |

The portfolio is now **two `continue`, eight `narrow`, and three pre-evidence** lines. Current evidence does not justify a merge, pause, split, new research line, or Research-Watch rotation.

## Cross-line structure

The strongest cross-line event in this snapshot is the transfer from **Prime Lattice to Möbius Cancellation**. `PL-211` passes the programmability test that defeated `PL-208`--`PL-210`: its divisibility projections and Möbius local observable are canonical arithmetic data. But the exact calculation then separates critical-half square-free support from the sign orientation relevant to cancellation. The resulting question is concrete enough to route rather than merely mention: `research/mobius_cancellation/clues/CLUE-bost-connes-phase-sensitive-mobius-transfer.md` asks for a canonical phase-sensitive KMS statistic that distinguishes actual Möbius orientation from arbitrary unimodular prime-phase controls near `beta=1/2` and transfers to Mertens excursion at sub-target cost.

Xi supplies a separate program-level lesson, but not a common theorem. `XF-101`--`XF-102` show that adding more source data to a state does not help if the destination still consumes a sublinear autonomous prefix with enough unused carrier dimension to hide a transition above the cutoff. That resembles the quotient/information warnings in Arithmetic Fidelity and Prime Circle, but the exact aliasing construction is Xi-specific and does not justify merging those lines.

Prime Flute illustrates the opposite pattern. Its recent findings do not discover new arithmetic selection, but they repeatedly remove concrete analytic losses from one proof interface: coefficient normalization, cutoff differentiation, fixed-profile boundary recoupling, and over-strong absolute endpoint assembly. This is why it remains `continue` even though the final arithmetic prime/clone discriminator is still downstream: the first unsupported implication is moving under proved estimates rather than being renamed.

Weil Positivity provides another matched-control boundary with possible program-wide relevance but no immediate destination-local transfer. `WP-199` shows that an unrelated rough reservoir can stabilize an arbitrary finite positive payload, so positivity obtained only after adjoining a freely chosen bath has no arithmetic explanatory force. This supports the existing source-forced-completion discipline but does not yet specify a new theorem for another line.

## Highest-leverage unresolved questions

1. **Prime-Flute native global-to-buffer endpoint.** Can the actual two-metric hyperbolic gradient-resolvent seam comparison be reduced uniformly to the scale-correct Dirichlet calculus of `PF-207`--`PF-209`, with any global low-frequency remainder satisfying the `O(a_n)` rank and `O(d_n)` norm pattern sufficient for `PF-210`, or an equivalent weak-`S_1` bound? The true-short-collar splice remains a separate theorem.

2. **Bow distinguished-twist cancellation.** At the exact source twist of `WI-195`, can the signed off-diagonal covariance cancel the diagonal at scale `XK log X`, or can a canonical source-fixed subtraction leave an `o(XK log X)` residual? `ANF-102` rules out replacing this with a uniform theorem over the twist window.

3. **Montgomery--Taylor interaction-bearing near-extremizers.** After `ANF-103`--`ANF-105`, can the remaining tight/interaction-bearing physical configurations saturate the fatal fixed-notch exposure, or does the source force a uniform gap? Decoupled profile proliferation and source-negligible remote tails are no longer legitimate escape mechanisms.

4. **Phase-sensitive Möbius transfer in a canonical arithmetic algebra.** Does the Bost--Connes divisibility system contain a canonical oriented correlation whose critical behavior distinguishes Möbius from matched arbitrary prime phases and yields a cheaper route to first-absolute Mertens excursion? If not, `PL-211` should be treated as a clean closure of this critical-half representation rather than a suggestive `1/2` coincidence.

## Portfolio changes proposed

`xi_flow`: **`continue` -> `narrow`**. This is a mechanism-level change, not a reaction to lack of recent positive results. `XF-101` proves that the complete current visible trajectory is compatible with every fixed positive transition time, and `XF-102` proves that merely extending the same autonomous prefix architecture through a fixed band containing actual prime atoms does not restore identifiability. The decisive test of the current state representation has therefore failed. The line remains worth investigating only through a materially different source/destination interface; it no longer has a credible direct route inside the existing sublinear Vieta-prefix architecture.

No other state change is justified. `prime_flute` and `weil_inertia` retain explicit live theorem requests whose resolution would materially change their bridges. The other eight evidence-bearing lines have precise residual questions but are predominantly in classification/replacement-theorem mode. No pre-evidence line has canonical evidence strong enough to displace an active Research Watch, and narrowing Xi does not by itself justify disabling or rotating its Watch while the replacement-interface question remains explicit.

This snapshot is reconciled to canonical evidence through `576c47c791b6148030dc615f1f7a8f3caadf6e3c`; the subsequent Master clue commit `b55d989e5dae2694859cfefc62b80f5b9943c6d0` only routes the Prime-Lattice/Möbius handoff and is not treated as evidence.