# Mathia Research State

Canonical evidence is synthesized through `76f73305f4ae595c882347eae1aa1ba51007a36c`. The latest clean independent adversarial checkpoint is `a22bc63a65c346c7fe691bdcd598a98e93509ae9`, which declares coverage through the reachable commit `f6cf86f0b7712d6acfeb4d0d0952a6783eccca2c`. There are no open `.review.md` sidecars. `MC-264` is substantive current evidence after that checkpoint and is not described as adversarially cleared.

## Global picture

Portfolio recommendations remain **3 `continue` + 10 `narrow`**. `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other line stays at `narrow`. No line currently clears the evidence and noncircularity bar for `pause-candidate`, `merge-candidate`, `split-candidate`, or a distinct `new-line-candidate`.

Prime Flute receives the strongest positive endpoint relaxation. `PF-320` already moved the direct-channel target from a raw seam-normalized block to the intrinsic low/high angle. `PF-321` now proves that the remaining shear-deleted reference problem does not need reciprocal-prime decay either: because the exact seam scales `s_n` satisfy `sum_n(1+log P_n)s_n^q<infinity` for every fixed `q>1`, any local intrinsic-angle bound `||T_{0,n}^{(r)}||=O(s_n^{1+epsilon})` makes the reference direct channel trace class. The actual hypercycle shear can then be added at the already-established weak-`S_1` cost. The missing theorem is therefore a geometry-native **fixed superlinear seam-scale gain** through the physical split and full common pant completion; `T_H` remains the independent second channel.

Farey sharpens its terminal obstruction rather than merely redistributing predecessor mass. `FD-092` decomposes the dyadic branch exactly: energy already nonsquarefree after the first deflation is recursively eligible at half scale, while the genuinely terminal rows are precisely `s=2m` with `m` odd squarefree and deflate again, without source error, to a quarter-scale physical mask on those odd-squarefree rows. Any universal exact multiplicative repair that makes every such row nonsquarefree needs a nonsquarefree multiplier `d>=4`; the smallest choice returns the horizon to `H+O(1)` and destroys fixed-factor descent. The remaining dyadic carrier is therefore an **adaptive odd-squarefree mask**, not a generic `p=2` transport defect.

Nyman and Robin each close a broad ambient escape. `NB-091` proves that for every fixed multiplicative depth `q`, the raw visible Gram band has exact determinant `m/R` and a universal normalized entropy `R Phi_q+O_q(1)`; under summable horizontal off-critical zero mass the actual Blaschke-deflated entropy differs from that baseline by only `O_{A,q}(1)`. Fixed-ratio determinant growth is therefore source-silent after calibration, so the route must use growing depth, genuinely inter-scale statistics, or first exclude the summable-horizontal-mass regime. `RE-094` gives the corresponding ambient exceptional-set frontier for the residual depth-two Robin channel: a short-interval theorem with length exponent `vartheta` and bad-start exponent `m(vartheta)` only beats the current barrier when `Theta>max(1/2+vartheta,m(vartheta))`. Improving `173/200` by this route requires some `vartheta<73/200` with `m(vartheta)<173/200`, which the currently audited general exceptional-set majorants do not certify. The live Robin input must therefore be source-conditioned at the actual prime-indexed/selector sample points or improve that exceptional-mass technology materially.

Arithmetic Fidelity further separates information cost from first incidence. `AF-314` compresses the complete post-hit five-point phase shape modulo common rotation to the single complex invariant `kappa=s_2^3/s_3^2`, continuously extended by `0` on the regular-pentagon orbit. The generic unordered rotation quotient is real two-dimensional, so one complex coordinate is dimension-tight among continuous Euclidean summaries. This substantially lowers the post-incidence information bill but leaves the actual unresolved question unchanged: whether the one-parameter prime-log orbit can meet the nonlinear five-point zero manifold at all, and with what stable margin.

Möbius materially refines the previous exceptional-set warning. `MC-263` remains a genuine low-moment no-go: a natural second moment cannot exclude one blind support at `t~log log y`. But `MC-264` proves that this is **not** an obstruction to moment methods in general. After tensorization the exact quadratic diagonal is `(2m-1)!! k_y^m`; at the first unresolved support scale, moment order `2(t+1)` would already push one blind atom above the natural fixed-weight shell-family diagonal with large quantitative slack, provided a shell-specific high-moment estimate of the stated form holds. The missing analytic object is explicit: the off-diagonal Krawtchouk-weighted distribution of Legendre-signature distances for `m`-fold lower-prime products. Standard all-moduli quadratic-large-sieve tensorization loses the sparse-family advantage through coefficient length, so the positive route is a genuinely support-matched fixed-weight shell theorem, not another low-moment average.

Visual, Weil Positivity, Prime Lattice, Prime Circle, Analytic Frontier, Weil Inertia, and Xi Flow retain their previous discriminating gates. `VIS-204` leaves aggregate mass across many distinct dangerous shells rather than high exact-shell multiplicity; `WP-282` exhausts positive completely-monotone relaxation continua; `PL-293`--`PL-294` leave actual-eigenbranch cancellation/sign beyond sharp generic boundary and semigroup controls; `PC-277` leaves only the intermediate rational-microscopic scalar window; `ANF-180`--`ANF-183` retain the sharp source-continuation aperture; `WI-272` retains the quantitative short-gap/gapless screening frontier; and `XF-185`--`XF-186` remain locally well-conditioned but globally unassembled.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` equivalent sharp discrimination boundaries for the matched endpoint packet. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at that aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-314` gives a continuous complete one-complex-coordinate invariant for the unordered five-point zero shape modulo common rotation, sharpening `AF-313` post-hit recovery without adding a first-hit theorem. | Decide the **first nonlinear nontorsion incidence** of the actual prime-log orbit with the `k>=5` zero surface; quotient compression, harmonic recovery, Baker-linear restrictions and topological rarity do not decide it. |
| `farey_discrepancy` | `narrow` | `FD-092` splits the dyadic carrier exactly into half-scale nonsquarefree eligibility or a quarter-scale frontier-sized adaptive odd-squarefree mask, and proves universal exact square repair cannot keep fixed-factor descent. | Upgrade the odd branch to fixed-strength recursive occupation/composition, or exploit source-specific/projective structure of the adaptive odd-squarefree terminal mask; another universal fixed multiplier is closed. |
| `mobius_cancellation` | `narrow` | `MC-264` proves that support-matched moment order `2(t+1)` crosses the single-blind threshold at `t~log log y` if a natural fixed-weight shell-family high-moment estimate holds, identifying an exact Krawtchouk off-diagonal kernel. | Prove that shell-specific high-moment estimate uniformly at the live support scale, prove logarithmic shell generation, or directly control/cancel the surviving blind mode; ordinary all-moduli tensorized large sieve loses the required sparse-family scale. |
| `nyman_beurling` | `narrow` | `NB-091` proves that every fixed-ratio contiguous Gram band has a universal raw entropy baseline and only bounded source-specific excess under summable horizontal off-critical zero mass. | Make source information visible at growing depth `q(R)`, in a genuinely inter-scale statistic, or prove false RH forces an effective horizontal-zero statistic outside the summable silent class. |
| `prime_circle` | `narrow` | `PC-277` closes broad normalized near-resonance regimes with exact-`Li` and rational-grid controls, leaving only an intermediate rational-microscopic scalar window. | Close that window with a stronger source-only control or find a zero-sensitive residual that survives it; otherwise leave scalar near-resonance for directional, microscopic, or cross-level data. |
| `prime_flute` | `continue` | `PF-321` proves that any fixed superlinear decay in the exact seam scale makes the shear-deleted local reference direct channel trace class; `PF-320` then adds the actual shear at weak-`S_1` cost. | Prove `||T_{0,n}^{(r)}||=O(s_n^{1+epsilon})` for some fixed `epsilon>0` through the physical split and full common pant completion, or fall back to the `PF-318` mean-square criterion; independently control `T_H`. |
| `prime_lattice` | `narrow` | `PL-293` gives the sharp generic `ell^{1/2}` boundary obstruction, and `PL-294` proves ordinary Beurling--Deny positivity fails already after `a_BD≈0.1406`, before primes enter. | Use the actual rational-prime Weil eigen-equation to suppress the universal boundary channel, prove a source-specific sign theorem, derive a different translation identity, or bypass regularity/Hopf entirely. |
| `robin_extremal` | `narrow` | `RE-094` converts the depth-two ambient short-interval route into the exact max-law frontier `Theta>max(1/2+vartheta,m(vartheta))` and shows current general exceptional-set majorants do not improve `173/200`. | Obtain a prime-conditioned/sample-conditioned short-interval or gap theorem, selector/rightmost-maximizer coupling, prime-race/height information, or genuinely stronger exceptional-set exponents at the required scale. |
| `visual_exploration` | `narrow` | `VIS-203` makes exact autocorrelation shells noncancelling, while `VIS-204` caps every dangerous `O(y^{-2})` shell at four representations and removes high multiplicity as the source of remaining energy. | Bound aggregate energy/packing over many distinct dangerous shells, obtain uniform cross-shell interference, or use a genuinely signed/complex taper; scalar cutoff optimization and equal-shell multiplicity are closed. |
| `weil_inertia` | `continue` | `WI-272` prices sub-`h_0` one-signed support gaps by the exact positive archimedean tail and forces a width--mass compatibility law under complete prime screening. | Rule out the remaining short-gap/gapless one-signed screened network using the full cut/null-equation family, and separately use the `WI-269` PSD/nodal law against long-range compensation on sign-changing branches. |
| `weil_positivity` | `narrow` | `WP-282` extends the zero-screen no-go to arbitrary positive completely-monotone infinite-dimensional relaxation continua: their Stieltjes transfer cannot notch even one finite zero frequency while retaining DC. | Build an independently forced oscillatory/non-Stieltjes, scale-nonlocal, nonlinear, or pre-scalar finite--archimedean coupling whose sign does not copy the zero divisor or assume an RH-equivalent primitive criterion. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**Universal baseline must be removed before apparent growth can count as arithmetic information.** `NB-091` is now the sharpest example: a linearly growing fixed-ratio Gram entropy survives even when the off-critical Blaschke source is replaced by its raw universal control, and only the centered excess can carry source information. `PC-277` reaches the same strategic requirement with denominator-matched exact-`Li` controls, while `WP-278`--`WP-282` similarly separate a universal pole tangent from the genuinely zero-sensitive residual. These are different operators and no theorem transfers directly, but they jointly make uncalibrated divergence a poor research currency.

**Closing the bulk increasingly leaves a thin source-specific terminal carrier.** Farey's dyadic residue is now an adaptive odd-squarefree quarter-scale mask; Robin's remaining depth-two obstruction sits at prime-indexed analytic sample points beyond what ambient exceptional-set measure controls; Weil Inertia is reduced to a short-gap/gapless screened network or nodal compensation; and Prime Flute needs only a fixed superlinear gain in the physically normalized local reference angle. The common strategic lesson is to attack the actual surviving carrier rather than improve a generic property already neutralized by the matched control. This is synthesis, not a shared lemma.

**Exact exceptional events require the statistic to be matched to their complexity, not merely a stronger average.** `AF-314` shows that post-hit five-point geometry can be compressed completely without excluding the first hit. `MC-263` shows why low moments can miss a single blind support, but `MC-264` adds the important qualification: when moment order tracks the possible support size, a natural sparse-family high-moment theorem could detect that single atom. `VIS-204` removes local shell multiplicity but still leaves aggregate mass over many distinct shells. Together these results distinguish post-event recovery, low-moment typicality, support-matched amplification, and global exceptional-set exclusion rather than treating them as one notion of fidelity.

**The destination-normalized metric still determines which estimate is worth proving.** `PF-321` lowers the direct-angle target from reciprocal-prime decay to any fixed superlinear power of the exact seam scale. `FD-092` shows the complementary failure mode: exact transport and preservation of frontier exponent are not enough when the transported mask loses recursive eligibility. The existing `CLUE-cross-line-fidelity-realization-separation` remains the correct global handoff for this distinction; the new results do not share one source family, operator, normalization and target from which a new destination-local transfer follows.

**Review identity is clean.** The independent checkpoint `a22bc63a65c346c7fe691bdcd598a98e93509ae9` audits the contiguous research prefix through `f6cf86f0b7712d6acfeb4d0d0952a6783eccca2c`, covering the substantive `AF-314`, `FD-092`, `NB-091`, `PF-321`, and `RE-094` additions together with the earlier current corpus. `MC-264` is later canonical evidence and remains explicitly uncleared by that checkpoint. No open review sidecar constrains the snapshot.

The Graph-owned Riemann Atlas remains navigation-only for portfolio decisions. Its entrypoint and metrics still advertise different stale source revisions (`4bc527...` and `834376...`), both far behind current canonical evidence, so no recommendation is inferred from Atlas percentages or telemetry.

## Highest-leverage unresolved questions

1. **Weil Inertia short-range screening.** Use the full first-crossing cut/null-equation family to decide whether a one-signed mode can remain gapless or sufficiently short-gap-connected while screening every prime lag; treat sign-changing modes separately through the PSD/nodal compensation law.
2. **Prime Flute superlinear reference angle.** Resolve the accepted `research/prime_flute/clues/CLUE-direct-angle-dtn-principal-symbol-decoupling.md`: prove any fixed superlinear seam-scale bound for the shear-deleted intrinsic local angle through the physical representation and common completion, or identify a genuine nonconstant-mode witness forcing failure; independently close `T_H`.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp `Theta(X/a_X)` aperture, keeping positive-cascade non-excisability separate.
4. **Farey recursive eligibility.** Turn the odd-channel lower nonsquarefree mask into fixed-strength iterability/composition, or extract source-specific structure from the quarter-scale adaptive odd-squarefree dyadic terminal mask.
5. **Möbius support-matched high moments.** Prove or falsify the fixed-weight shell-family `2(t+1)`-moment estimate at `t~log log y`, with its Krawtchouk off-diagonal kernel, before returning to broader generation or direct-cancellation exits.
6. **Nyman beyond fixed depth.** Determine whether false RH forces a nonsummable effective horizontal-zero statistic; otherwise make the Blaschke source visible at growing depth or in a genuinely inter-scale centered charge.
7. **Robin source-conditioned sampled selection.** In the `469/600<Theta<=173/200` band, replace ambient start-set control by arithmetic information conditioned on the actual prime-indexed quadratic sample points or CA rightmost selector.
8. **Visual dangerous-shell packing.** Bound the total weighted mass over many distinct `O(y^{-2})` four-prime shells, prove usable cross-shell interference, or construct a signed taper with arithmetically controlled shell phases.
9. **Arithmetic Fidelity first incidence.** Decide whether the actual prime-log one-parameter orbit can meet the five-point polygon-zero manifold; the post-hit shape is now completely compressed, so further recovery coordinates do not address the live gate.
10. **Weil Positivity beyond positive relaxation.** Produce an independently selected oscillatory/non-Stieltjes or genuinely global finite--archimedean architecture that retains zero-sensitive source information and supplies the sign without encoding the zero divisor or an RH-equivalent primitive.

## Portfolio changes proposed

None. The new findings materially sharpen several endpoints but do not establish saturation of any current line, mathematical redundancy between lines, a natural split, or a distinct question that no existing line can own. The three `continue` lines retain direct falsifiable source-specific gates; every `narrow` line still has at least one materially different residual carrier not covered by its latest no-go result.