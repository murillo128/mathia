# Mathia Research State

Canonical evidence is synthesized through `0677715a1a05eb4733ed97fcc4d7530ce80d0154`. The latest clean independent adversarial checkpoint is `b2aaa48e296e5362c9373aec449c08658c577224`, which declares coverage through the reachable commit `3c1a4484c49fc6472c86d09f8b411305288f62bc`. There are no open `.review.md` sidecars. `MC-262` is the only substantive finding after that checkpoint and is used as current canonical evidence without being described as adversarially cleared.

## Global picture

Portfolio recommendations remain **3 `continue` + 10 `narrow`**. `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

The strongest positive movement is in Farey. `FD-090` removes the predecessor-count entropy loss left by `FD-089`: after normalizing each transported lower mask by its natural scale `A_p^{2\Theta}`, the prime contraction weights are summable for every false-RH frontier `\Theta>1/2`, so one canonical predecessor retains a **fixed positive fraction of the parent frontier-normalized energy** for every fixed admissible contraction band. The remaining obstruction is no longer predecessor multiplicity or frontier exponent preservation. It is recursive eligibility: the selected mask may still occupy a vanishing fraction of the lower state, the lower state need not inherit nonsquarefree occupation, and the raw parent-scale share still pays the unavoidable factor `p^{-2\Theta}`.

Nyman and Robin sharpen two negative controls around similarly relational endpoints. `NB-089` extends the scalar silence of finite Blaschke defects to an infinite false-RH-compatible class: if the total horizontal off-critical mass `\sum_{\Re\rho>1/2}(\Re\rho-1/2)` is finite, the full Blaschke product contributes only `O(R^{-1})` aggregate squared distortion in the compulsory dyadic scalar sibling channel. A scalar proof must therefore force genuinely nonsummable horizontal mass **and** show that it survives the parent/child delay combination, or leave the scalar average direction for contrasts, cross-block correlations, earlier indices, or the remaining positive floor charge. `RE-092` proves that merely pairing the two anchored prime-gap **magnitudes** on a mixed cell does not improve the `RE-091` frontier: the depth-3 scalar boundary remains `469/600`, and the surviving depth-2 channel has scalar barrier `173/200`. Progress in the intermediate regime must preserve exact cross-scale source incidence, adjacency/orientation, or rightmost-maximizer selection rather than add another separate gap moment.

Arithmetic Fidelity and Prime Lattice each close another attractive generic amplification. `AF-312` shows that a hypothetical five-prime exact hit cannot repeat at harmonics `2,3,4,5`, and that any fixed integer time lattice contains only finitely many exact hits by finite-rank unit-equation theory. Those are genuine nonlinear/post-hit restrictions, but they leave the first continuous five-prime incidence untouched. `PL-294` locates the exact first Beurling--Deny positivity threshold of Suzuki's localized Weil form at `a_BD=(1/2)log rho_pl=0.140599787...`, strictly before the first prime-power translation. Ordinary positivity-preserving/Perron--Frobenius continuation is therefore unavailable at the certified `a=0.8` ground state; any useful sign or boundary regularity there must be zeta-specific or bypass the Hopf route.

Weil Positivity now closes the most natural positive infinite-dimensional continuation of its finite-state control. `WP-282` allows an arbitrary positive continuum of exponential relaxation modes, equivalently a nonrational Stieltjes response, but its boundary transfer has strictly positive real part at every finite frequency. It therefore cannot annihilate even one surviving zeta-zero mode while preserving the Gamma DC mode, even under RH. The remaining architecture must be qualitatively different: oscillatory/non-Stieltjes and independently forced, genuinely scale-nonlocal, nonlinear with an auditable sign mechanism, or coupled across finite and archimedean sectors before scalarization.

Möbius supplies the newest method ceiling. `MC-262` uses the 2026 explicit quadratic large sieve to extend fixed-order shell parity balance to `t=o(log^{(4)} y)`, but the same explicit constant dependence shows that the black-box all-squarefree-moduli embedding loses nontrivial normalized control once `t/log^{(4)}y` grows. The live blind-support floor is already of order `log log y`, multiple iterated logarithms larger. Thus generic explicit quadratic-large-sieve uniformity cannot bridge the shell-rank gap; a useful theorem must exploit the sparse shell-product family directly, prove logarithmic-scale shell generation, or attack the surviving source-selected Möbius coset/pretender.

Weil Inertia, Prime Flute, Analytic Frontier, Prime Circle, Visual Exploration, and Xi Flow retain their previous discriminating gates. `WI-271` still gives the clearest source-equation bridge: a one-signed first-crossing support gap of width at least `h_0` forces positive prime-power autocorrelation. `PF-242` plus `PF-319` leave a concrete seam/DtN-normalized transport theorem rather than a raw locality conjecture. `ANF-180`--`ANF-183` retain the sharp source-continuation aperture. `PC-277` leaves only an intermediate rational-microscopic scalar window, `VIS-202` identifies a square-root-log threshold of its current proof architecture rather than of the true collision energy, and `XF-185`--`XF-186` remain locally well-conditioned but globally unassembled.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` equivalent sharp discrimination boundaries for the matched endpoint packet. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at that aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-312` adds low-harmonic exclusion and finite fixed-lattice recurrence after a hypothetical five-prime hit, while `AF-311` already shows standard algebraic-linear logarithm restrictions are compatible with generic pentagon zeros. | Decide the **first** nonlinear nontorsion incidence of the actual prime-log one-parameter orbit with the `k>=5` zero surface; recurrence sparsity and stronger linear-relation arguments no longer decide it. |
| `farey_discrepancy` | `narrow` | `FD-090` removes predecessor-count entropy: one prime contraction preserves a fixed fraction of frontier-normalized masked energy for every fixed admissible contraction band. | Prove fixed mask/occupation inheritance or recursively compose frontier-normalized carriers despite the unavoidable raw factor `p^{-2Theta}`; exponent preservation alone is no longer the issue. |
| `mobius_cancellation` | `narrow` | `MC-261` identifies full shell generation as all-class `(1+o(1))log R`-smooth representation, while `MC-262` shows published explicit quadratic-large-sieve uniformity reaches only `o(log^(4)y)`, far below the `log log y` blind-support scale. | Exploit the sparse shell-product family or shell arithmetic itself to reach the live scale, prove logarithmic shell generation, or eliminate the one surviving blind pretender by source-conditioned signed cancellation. |
| `nyman_beurling` | `narrow` | `NB-089` proves that even an infinite off-critical Blaschke defect is scalar-silent when total horizontal displacement is summable. | Force genuinely nonsummable effective horizontal mass and a nonsummable parent/child delay imbalance, or leave the scalar sibling-average channel for matrix/contrast directions and the remaining radial floor charge. |
| `prime_circle` | `narrow` | `PC-277` closes broad normalized near-resonance regimes with exact-`Li` and rational-grid controls, leaving only an intermediate rational-microscopic scalar window. | Close that window with a stronger source-only control or find a zero-sensitive residual that survives it; otherwise leave scalar near-resonance for directional, microscopic, or cross-level data. |
| `prime_flute` | `continue` | `PF-242` makes raw hypercycle shear exponentially mode-local with squared Hilbert--Schmidt cut leakage `O(P_n^{-2})`; `PF-319` proves corridor form-smallness alone does not control the seam-normalized physical angle. | Transport the proven locality through corridor-to-seam/DtN normalization and Schur operations to the physical `Z_LH` weak-`S_1` bound, independently control `T_H`, then keep infinite realization and signed reassembly separate. |
| `prime_lattice` | `narrow` | `PL-293` gives the sharp generic `ell^{1/2}` boundary obstruction, and `PL-294` proves ordinary Beurling--Deny positivity already fails after `a_BD≈0.1406`, before primes enter. | Use the actual rational-prime Weil eigen-equation to suppress the universal boundary channel, prove a source-specific sign theorem at the aperture of interest, derive a different translation identity, or bypass regularity/Hopf entirely. |
| `robin_extremal` | `narrow` | `RE-091` forces one-sided first-layer boundary contact above `469/600`; `RE-092` proves paired gap magnitudes do not improve that information class and gives the depth-2 scalar barrier `173/200`. | In `469/600<Theta<=173/200`, exploit the exact `eta_1(p)`/`eta_2(q)` incidence, adjacency/orientation, skipped-span, or rightmost-maximizer constraint rather than separate gap-size moments. |
| `visual_exploration` | `narrow` | `VIS-201` reaches `sqrt(log y)=o(log H)` by averaged four-form control; `VIS-202` proves that boundary exact for the persisted fiberwise-sieve plus taper-tail majorant. | Change a structural ingredient: inter-fiber cancellation/dispersion, effective incidence dimension, tail mechanism, or the dephasing resource itself. |
| `weil_inertia` | `continue` | `WI-271` proves that a one-signed first-crossing support gap of width at least `h_0` forces positive prime-power autocorrelation, so complete screening requires `h_0`-gap-connected support. | Rule out the remaining densely connected one-signed screened mode using firstness/source structure, and separately use the `WI-269` PSD/nodal family against long-range archimedean compensation on sign-changing branches. |
| `weil_positivity` | `narrow` | `WP-282` extends the zero-screen no-go from finite-state filters to arbitrary positive completely-monotone infinite-dimensional relaxation continua: their transfer has no imaginary-axis notches. | Build an independently forced oscillatory/non-Stieltjes, scale-nonlocal, nonlinear, or pre-scalar finite--archimedean coupling whose sign does not copy the zero divisor or assume an RH-equivalent primitive criterion. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**The common residual is increasingly relational rather than scalar.** Farey has removed predecessor-count entropy but still needs occupation inheritance/composition; Robin has saturated the available paired gap-magnitude budgets and now needs exact cross-scale incidence; Möbius has pushed generic high-order parity control far enough to see that the live shell relation lies outside the black-box uniformity range. These are not the same theorem, but in all three cases another aggregate count or moment is no longer the missing currency. The unresolved information is which source pieces are paired, eligible, or simultaneously realizable in the target geometry.

**Post-event rigidity is not first-event exclusion.** `AF-312` can forbid low harmonics and infinite integer-lattice recurrence after a hypothetical five-prime zero without touching first incidence. `NB-089` gives the complementary warning: even infinitely many off-critical zeros can be invisible in a compulsory scalar channel when their horizontal mass is summable. Repetition, infinitude, and source nontriviality therefore cannot substitute for a quantitative theorem in the exact direction consumed by the target.

**Several lines now have explicit method ceilings rather than vague shortages.** `PL-294` closes generic semigroup positivity at a precise pre-prime aperture; `WP-282` closes the entire completely-monotone positive relaxation continuum; `MC-262` quantifies the iterated-log ceiling of the current all-moduli quadratic-large-sieve embedding; and `VIS-202` quantifies the square-root-log ceiling of its current fiberwise-sieve/taper architecture. These are scoped no-go results, not evidence that the underlying research objects are impossible. The strategic implication is that further progress must change the information class or use source-specific structure rather than optimize constants inside the exhausted class.

**Weil Inertia remains a useful positive control on that pattern.** Generic first-crossing and positivity substitutes were eliminated, but Suzuki's actual source equation then produced the new overlap and cut constraints in `WI-269`--`WI-271`. Prime Flute has a similar opportunity: coefficient-level locality is already established, so the next theorem must act on the actual seam-normalized operator rather than on a proxy metric. This is why both remain `continue` while several neighboring operator/positivity lines remain `narrow`.

**Review identity is clean.** The checkpoint `b2aaa48e296e5362c9373aec449c08658c577224` audits the contiguous prefix through `3c1a4484c49fc6472c86d09f8b411305288f62bc`, covering `AF-312`, `FD-090`, `NB-089`, `PL-294`, `RE-092`, and `WP-282` together with the earlier current corpus. `MC-262` is later canonical evidence and remains explicitly uncleared by that checkpoint. No open review sidecar constrains the snapshot.

The existing `CLUE-cross-line-fidelity-realization-separation` remains sufficient at Master scope. The new similarities concern residual information classes and method ceilings, but they still do not provide one common source family, operator, normalization, and target from which a destination-specific actionable transfer theorem follows. No additional Master clue is warranted.

The Graph-owned Riemann Atlas remains navigation-only for portfolio decisions. Its entrypoint and metrics advertise different stale source revisions (`4bc527...` and `834376...`), both far behind current canonical evidence, so no recommendation is inferred from Atlas telemetry.

## Highest-leverage unresolved questions

1. **Weil Inertia after the `h_0` gap theorem.** Decide whether firstness/source geometry forces an `h_0` bottleneck on every one-signed candidate, or construct an `h_0`-connected completely screened mode; attack sign-changing candidates separately through the PSD/nodal compensation law.
2. **Prime Flute normalized endpoint leakage.** Carry the exponential Pöschl--Teller locality and `O(P_n^{-2})` raw shear leakage through seam/DtN normalization and the Schur map to the physical `Z_LH` ideal bound, while independently controlling `T_H`.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp `Theta(X/a_X)` aperture, keeping positive-cascade non-excisability as a separate final gate.
4. **Farey recursive eligibility.** Upgrade `FD-090` from fixed-strength frontier-normalized predecessor transport to a lower state/mask that is eligible for the same construction again, or prove a composition theorem that does not require fixed raw occupation.
5. **Nyman beyond summable scalar delay.** Determine whether false RH forces nonsummable horizontal Blaschke mass and whether that mass survives the three-delay parent/child combination; if not, move deliberately to contrast or matrix directions.
6. **Robin source-conditioned depth-2 selection.** Between `469/600` and `173/200`, bound the selected cross-scale `eta_1(p)`/`eta_2(q)` incidence with its adjacency and rightmost-maximizer orientation rather than its two marginal gap sizes.
7. **Möbius at the actual shell scale.** Either prove `(1+o(1))log R` all-class shell generation, obtain a sparse shell-product large sieve reaching the `log log y` support regime, or eliminate the surviving blind pretender by source-conditioned signed cancellation.
8. **Weil Positivity beyond positive relaxation.** Produce an independently selected oscillatory/non-Stieltjes or genuinely global finite--archimedean architecture that retains zero-sensitive source information and supplies the sign without encoding the zero divisor.
9. **Prime Lattice source-specific sign/regularity.** After `PL-293` and `PL-294`, determine whether the actual low Weil eigenbranch suppresses the universal boundary channel or has a fixed-sign property unavailable to generic semigroup theory.

## Portfolio changes proposed

No recommendation change is proposed. Weil Inertia remains `continue` because its latest gains come from Suzuki-specific first-crossing identities after generic controls have failed. Prime Flute remains `continue` because the raw locality problem is solved strongly enough that one canonical normalization/ideal theorem for `Z_LH` and an independent `T_H` singular-value problem remain. Analytic Frontier remains `continue` because its sharp source-continuation aperture is unchanged and still gives a direct falsifiable endpoint test.

Among the `narrow` lines, Farey makes the strongest positive advance in this source window, but `FD-090` still stops before recursive eligibility. Nyman, Robin, Arithmetic Fidelity, Prime Lattice, Möbius, and Weil Positivity each sharpen a method boundary without exhausting the source-specific alternatives named above. Prime Circle, Visual Exploration, and Xi Flow retain distinct narrow residuals whose next progress requires a new information class rather than another refinement of the current scalar/local control.

No active line is a strong `pause-candidate` or `merge-candidate`, so no Research Watch rotation is justified. No split or new-line candidate clears the bar. No new Master clue is created because the cross-line commonality remains structural rather than an exact transferable source/operator theorem.