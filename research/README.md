# Mathia Research State

## Global picture

Mathia currently has thirteen research lines. Ten carry canonical evidence: `analytic_frontier`, `arithmetic_fidelity`, `mobius_cancellation`, `prime_circle`, `prime_flute`, `prime_lattice`, `visual_exploration`, `weil_inertia`, `weil_positivity`, and `xi_flow`. `farey_discrepancy`, `nyman_beurling`, and `robin_extremal` remain pre-evidence scaffolds.

The evidence still does **not** support one common positive mechanism or one mathematically meaningful global RH bottleneck. The strongest movement in the current source window is more specific: several lines have now located exact information or conditioning thresholds, and Prime Flute continues to contract a concrete operator-theoretic completion problem. The right global discipline is therefore to distinguish a genuine change in the first unsupported implication from a richer restatement of the same missing theorem.

**Prime Flute remains the clearest positive local-to-global program.** `PF-228` already showed that the correctly dressed flat corridor gains enough `w/s` scale to make the inverse-seam channel population weak-trace compatible. `PF-233` transported the raw transfer envelope through a genuinely variable-width noncommuting diagonal corridor, `PF-234` showed that the actual symmetric hyperbolic seam normalizer is flat-comparable with only quadratic reciprocal-prime error, and `PF-235` now factors the remaining hypercycle boundary map exactly. The cusp-end singularity belongs to the fixed axis compactification; the seam-dependent reparameterization has only `O(w_n^2)=O(P_n^{-2})` distortion and summable total Jacobian defect. Thus variable width, seam normalization, and prime-dependent boundary-coordinate distortion are no longer credible generic sources of endpoint loss. The first unsupported implication has narrowed to the **inverse-level noncommuting two-boundary cancellation**: put the actual seam forms and the corridor DtN form in one trace realization, prove the `PF-228` scale for the off-diagonal block of the full normalized inverse, then carry it through shear, `P/H` projection, finite-pant completion, and global weak-`S_1` reassembly. The unsmoothed `PF-204` route and the true-short-collar splice remain separate gates.

**Weil Inertia keeps a sharp theorem-class split rather than a moving numerical target.** `WI-202` gives the currently audited ordinary-exponent-pair every-interval/fixed-physical-depth cutoff `3943/12011`, while `WI-203` proves that this entire exponent-pair interface has a structural floor at `1/4`; even the exponent-pair conjecture does not carry the present reservoir argument below that boundary. `WI-204` also shows that Karatsuba's almost-all theorem cannot exclude one source-selected bow merely by thickening it to its internal starts. Better verified exponent pairs can still prune part of `1/4 < theta <= 3943/12011`, but the hard regime `theta <= 1/4` requires a different kind of information: the distinguished source-fixed Gallagher covariance/cross-shift norm, a genuinely stronger every-interval theorem outside the exponent-pair architecture, or another source relation that forbids the bow directly.

**Xi Flow has now located an asymptotically sharp information boundary for one whole observation class.** `XF-127` shows that the complete terminal history `P_N(s)` can agree exactly between a colliding target and a simple target with different transition status. `XF-128` extends that blindness to a consecutive terminal window containing any fixed fraction strictly below one half of all harmonic histories. `XF-129` then finds the exact complementary positive boundary: for unit-circle target slices, the complete upper-half histories `P_m(s)` with `N/2 < m <= N` determine the divisor completely for odd degree and, for even degree, up to a central-shell sign ambiguity that is only a rigid rotation and preserves collision geometry and transition time. Thus one half is asymptotically sharp for **contiguous terminal blocks of complete heat histories**. This materially solves the destination-side identifiability question for that observation class, but it does not solve the Xi bridge. The source machinery established so far controls q-scale/sublinear information, not a linear half-spectrum of degree-scale harmonics. Xi therefore remains `narrow`: the live theorem is now source access and transport to this identifying window, or an intentionally different observable/category, rather than further destination-side tests of shorter terminal windows.

**Analytic Frontier remains live, but repeated local annihilation has acquired a quantitative entropy barrier.** `ANF-129` showed that positive-rate exponential digit clouds can evacuate one saddle while regenerating bounded macroscopic source mass elsewhere, defeating a simple local source-budget closure. `ANF-130` and `ANF-131` then eliminated fixed finite regenerated chamber families with positive annihilators. `ANF-132` closes the obvious attempt to iterate that mechanism to growing depth at subexponential cost: for `m=o(A)` repeated uniformly conditioned zeros, the surviving Gaussian chamber moves to scale `sqrt(m/A)` and the gain is only `exp(-m log(A/m)+O(m))`; fixed-rate `exp(-sigma A)` suppression forces `m=Omega(A)` and hence a positive exponential copy budget in this architecture. The remaining route must therefore use a genuinely different global completion mechanism — for example an `A`-dependent/non-product molecule, changing chamber geometry, or a completion argument using the full fatal-notch/near-extremizer constraints — rather than deeper iteration of one fixed local filter. That is still a concrete falsifiable frontier, so the line remains `continue`.

Several `narrow` lines have also crossed exact boundaries that should prevent repeated reformulations. In Arithmetic Fidelity, `AF-213` closes the exponent-one conditioning gap on the unrestricted signed BV cone: Hobby--Rice sign carriers spend only `O(n)` variation to annihilate `n` polynomial moments, and uniform analytic approximation makes the full fixed Euler band exponentially insensitive, giving `0 < gamma(M) <= C exp(-cM)` while every fixed finite budget remains coercive. The next theorem must therefore be source-side — actual arithmetic positivity or a complexity restriction strong enough to avoid this signed exponential instability — rather than more finite-band injectivity. In Möbius Cancellation, `MC-162` proves that whenever the explicit absolute decoder contraction is strong enough to transfer an `x^theta` bound back to Mertens, the transformed bound is already equivalent to `M(x)=O(x^theta)` at the same exponent and the transfer factor is automatically nonmasking. Stable scalar encoding is not an intermediate power theorem by itself. The accepted decoder-stability clue now asks for an independently proved source estimate for one fixed transform, or a genuinely signed Möbius-specific inverse-tail theorem beyond the absolute contraction regime.

**Weil Positivity has made its strongest source-selection advance in the Bost--Connes fork, while exposing the sign defect exactly.** `WP-216` uses the canonical divisibility projections `E_n=mu_n mu_n^*` and the KMS relation to force the positive Gram kernel

`G_beta(m,n) = (gcd(m,n)/sqrt(mn))^beta`.

At the actual Bost--Connes critical inverse temperature `beta=1`, a prime axis is the Poisson/Toeplitz kernel `p^{-|a-b|/2}`, so the critical half-density is source-forced rather than supplied by an arbitrary scalar `F(H)`; the dynamics also supplies `log p`. But the local finite-Weil ray is exactly `log p (I-G_{1,p})`, an **indefinite defect** of that positive Gram. The obvious diagonal compensation has divergent all-prime cost, while a generic mixed-prime positive completion would be an additional operation rather than a sign theorem inherited from KMS conditioning. The earlier Master clue has therefore been strengthened: the question is no longer whether any relation stronger than affiliation exists, but whether a source-forced global completion can orient this canonical Gram into the completed Weil sign while incorporating the archimedean and polar sectors without target coding. This is a real contraction of the problem, but not yet evidence for positivity, so Weil Positivity remains `narrow`.

That result also clarifies the overlap with Prime Lattice. `PL-225`--`PL-229` still classify weak affiliation, canonical all-prime renormalization/conditioning, the unit boundary, and a shifted reciprocal-zeta Dirichlet compression as controls rather than selectors. The resolved source-selection consultation supplies an additional counterexample to premature consolidation: the canonical affine relation `V_p S = S^p V_p` is a genuine source relation distinct from a Weil sign theorem, even though that particular relation classicalizes and admits arbitrary completely multiplicative phase twists. Source selection and destination sign can therefore be mathematically independent obligations. Shared Bost--Connes vocabulary is not evidence that Prime Lattice and Weil Positivity are duplicate lines.

**Visual Exploration has separated information admission from usable amplitude.** `VIS-111` constructed a growing-lag third-order correlation that escapes every uniformly fixed block quotient. `VIS-112` then shows that its raw unit separation becomes exactly `2/N` under the natural empirical-average normalization. The synthetic witness therefore proves non-factorization but does not provide a scale-stable source effect. A source experiment now has to predeclare both the growing information scale and the amplitude/null normalization, then test a fresh zeta statistic against the exact lower-information quotient and matched non-arithmetic controls. Choosing a richer transform without this scale audit would merely move the selection freedom.

The Riemann Atlas remains derived navigation rather than decision evidence. Its entrypoint and metrics carry different, older source revisions than the current canonical corpus, so it is stale and internally unsynchronized for portfolio purposes. No Atlas percentage is used below as evidence of progress or line value.

No `.review.md` sidecar is open in the inspected default-branch state. The latest declared adversarial checkpoint is `Adversarial-Reviewed-Through: 5ca50a8b137abce1b1527485e1453755de28ac9b`. `WP-216`, `VIS-112`, and `MC-162` are later canonical findings and are therefore current evidence but not yet covered by a subsequent declared independent checkpoint. The Xi upper-half formalization is likewise later than that checkpoint, but the portfolio statement relies on the canonical `XF-129` finding rather than treating formalization as independent evidence.

## Research-line portfolio

| Research line | State | Strongest current contribution and first unsupported implication |
| --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-132` proves that iterating one uniformly conditioned positive saddle annihilator to sublinear depth cannot buy fixed-rate exponential suppression at subexponential copy cost. Decide the remaining global completion using an `A`-dependent/non-product mechanism or the full near-extremizer/notch constraints, or construct a fatal completion that survives them. |
| `arithmetic_fidelity` | `narrow` | `AF-213` gives exponential deterioration of the optimal fixed-band coercivity modulus on the unrestricted signed BV exhaustion while preserving positivity at every fixed finite budget. Prove a source-forced arithmetic positivity/complexity restriction with a quantitatively adequate modulus, or show that the actual source can realize the exponentially invisible sign patterns. |
| `farey_discrepancy` | pre-evidence | The ordered Pluecker/exterior-field experiment remains a legitimate kill-first direction, but no canonical finding yet proves survival of telescoping, classical Farey/Mobius reduction, coboundary collapse, and the declared matched controls. |
| `mobius_cancellation` | `narrow` | `MC-162` shows that absolute decoder stability in the current linear commutator families is same-exponent Mertens equivalence, not an intermediate cancellation scale. Retain a scalar route only if one fixed transformed statistic has an independent source-side estimate, or prove a signed Mobius-specific inverse-tail gain where the absolute contraction criterion fails; otherwise move to a genuinely relational non-scalar carrier. |
| `nyman_beurling` | pre-evidence | Remain dormant until a target-relative datum beyond universal Gram/autocorrelation geometry yields a falsifiable approximation-rate improvement rather than another equivalent scalar reformulation. |
| `prime_circle` | `narrow` | `PC-222` leaves the mixed-conductor information only in genuinely collective operations after scalar Mangoldt anchoring collapses to prime-primary axes. The proposed cross-level clue must exhibit one source-forced repeated/noncommuting operation with normalized information gain against matched one-axis and Ramanujan/cyclotomic controls. |
| `prime_flute` | `continue` | `PF-235` removes prime-dependent boundary compactification as a nonsummable loss. Prove the `PF-228` dressed scale for the off-diagonal block of the full normalized inverse in the common noncommuting trace realization, then carry it through shear, `P/H`, finite-pant completion, and global weak-`S_1` reassembly, or identify the exact later operation that destroys it. |
| `prime_lattice` | `narrow` | `PL-225`--`PL-229` classify weak affiliation, canonical all-prime limits/conditionings, and shifted reciprocal-zeta compression as generic/classical exits; the affine source relation is genuinely distinct from a Weil sign but classicalizes under arbitrary prime phases. A survivor must produce an additional source-selected relation with a destination consequence not already programmable or a scalar zeta repackaging. |
| `robin_extremal` | pre-evidence | The strengthened exchange/discretization clue asks for one unconditional infinite-family restriction on extremal exponent profiles after the full `log log n` and rounding budget. Until such a finding exists, mathematical diversity alone does not justify promoting or scheduling the line. |
| `visual_exploration` | `narrow` | `VIS-111` admits a growing-lag third-order channel beyond every fixed block quotient, while `VIS-112` shows the exhibited separation dilutes as `2/N` under empirical averaging. Freeze both a genuinely growing/global statistic and its normalization/null scale before fresh source inspection, and require a reproducible residual against exact local and non-arithmetic controls. |
| `weil_inertia` | `continue` | `WI-202` gives the valid current cutoff `3943/12011`; `WI-203` proves the ordinary exponent-pair route cannot cross `1/4`; `WI-204` rules out the almost-all shortcut for one bow. Improve every-interval input only in the super-quarter band and attack `theta<=1/4` with genuinely distinguished source-fixed covariance/cross-shift information. |
| `weil_positivity` | `narrow` | `WP-216` source-forces the critical Poisson/GCD Gram and `log p`, but the finite Weil ray is the indefinite defect `log p(I-G_p)`. Resolve the strengthened Bost--Connes clue by constructing or excluding a source-forced finite-prime-plus-archimedean completion whose positivity does not come from a divergent bath, a generic product completion, or target-coded scalar filtering. |
| `xi_flow` | `narrow` | `XF-127`/`XF-128` give collision-blind terminal windows up to every fraction below one half, while `XF-129` proves that the full upper half determines unit-circle collision geometry and transition status up to harmless rotation. Derive controlled Xi source access to that identifying degree-scale window, or change observation category; further shorter-window destination tests no longer address the bridge. |

The portfolio remains **three `continue`, seven `narrow`, and three pre-evidence** lines. Current evidence does not justify a merge, pause, split, new research line, or Research Watch rotation.

## Cross-line structure

The most consequential transfer is now **Prime Lattice -> Weil Positivity as a control boundary followed by an internal Weil-Positivity source-selection success**. Prime Lattice shows why weak completion, bare affiliation, scalar renormalization, lcm/coprimality conditioning, and one-dimensional Dirichlet compression cannot be credited merely for becoming more global. `WP-216` then demonstrates that a stronger intrinsic relation really can survive those warnings: KMS conditioning of the divisibility projections canonically produces the critical Poisson/GCD Gram. The remaining obligation is the Weil orientation/sign completion, not source selection in the abstract. The strengthened local clue records that sharper interface.

The resolved Prime-Lattice independence consultation is important for portfolio structure. Its affine semigroup relation is source-forced before any Weil target is inserted, yet the downstream sign remains an additional target-sensitive theorem and the relation itself classicalizes under arbitrary prime phases. That is direct counterevidence to merging Prime Lattice and Weil Positivity simply because both use Bost--Connes/operator-algebraic language. A future exact reduction could still establish redundancy for a particular mechanism, but the current theorem obligations are not identical.

Xi and Visual provide a second, methodological transfer around **information scale versus destination sufficiency**. Xi now has a sharp theorem: terminal blocks below half-spectrum can be nonidentifying while the complete upper half is identifying. Visual has the analogous warning at the statistical level: escaping every fixed local quotient does not guarantee a macroscopic normalized effect. These are not the same mathematics and do not constitute a shared RH mechanism. They do justify one common research discipline: prove both information admission and the destination-relevant scale before assigning significance to a richer observable.

Arithmetic Fidelity and Mobius Cancellation provide a complementary stability contrast. Arithmetic Fidelity is injective and stable on every fixed compactified source class but exponentially ill-conditioned as signed BV complexity grows. Mobius linear commutator transforms can be stably invertible, yet the absolute stability regime is already equivalent to the original Mertens power bound. Thus neither exact recovery nor good conditioning alone establishes analytic leverage; a useful transformed problem needs a source theorem that is genuinely cheaper after every inverse/quotient loss is restored.

The user-requested allocation counterassessment does not currently clear any lifecycle gate. Prime Circle has a concrete proposed collective test but no local disposition or new finding that justifies either promotion or pause. Robin has a well-defined discrete pilot, but its clue explicitly remains pre-evidence until an infinite-family restriction survives the extremal reduction and discretization budget. Mobius has responded substantively by narrowing its scalar route to an independent-estimate or signed-inverse question, not by establishing a reason to pause the line. Prime Flute, Weil Inertia, and Analytic Frontier each still own a distinct falsifiable theorem surface. No Research Watch is rotated on the basis of consultation alone.

## Highest-leverage unresolved questions

1. **Prime-Flute inverse-level dressed completion.** Put the actual seam-energy forms and the variable-width corridor DtN form in one trace realization, keep the fixed axis compactification and noncommutation honest, and prove the `sqrt(w_n w_{n+1})/s_n`-scale singular-value envelope for the off-diagonal block of the full normalized inverse. Then quantify shear, `P/H`, pant completion, and global reassembly. This now tests the mechanism that actually consumes the local gain.

2. **Weil-Inertia distinguished bow at and below the quarter boundary.** Use ordinary exponent-pair improvements only where they apply. In the structural hard regime, prove source-fixed diagonal-scale cancellation for the exact Gallagher/cross-shift norm, find an every-interval theorem outside the current threshold architecture, or construct evidence that the distinguished bow can saturate the diagonal scale.

3. **Analytic-Frontier completion beyond iterated positive annihilators.** `ANF-132` says that simply increasing the depth of one fixed positive filter cannot achieve fixed-rate exponential suppression at subexponential outer cost. The next result must use an `A`-dependent/non-product Fourier molecule or the full global near-extremizer/notch geometry and must track the regenerated source chambers rather than only the original saddle.

4. **Xi source access to the sharp upper-half observation boundary.** Destination identifiability is no longer the open issue for contiguous terminal blocks: `XF-129` settles it at the half-spectrum scale. The decisive bridge is whether the Xi source and heat transport expose a controlled linear family of the required degree-scale Vieta/harmonic histories, or whether the architecture must move deliberately to a different global or prime-band observable.

5. **Weil-Positivity orientation of the critical divisibility Gram.** `WP-216` source-forces the half-density and prime logarithmic scale, so do not return to arbitrary affiliation or scalar filtering. The strengthened `CLUE-bost-connes-affiliation-conditioning-boundary.md` now asks for one concrete completion that turns the canonical positive Gram's indefinite defect into the completed Weil sign with finite all-prime cost and the correct archimedean/polar sectors, or a family-level obstruction showing why natural completions cannot do so.

## Portfolio changes proposed

**No portfolio-state change is proposed in this pass.** `analytic_frontier`, `prime_flute`, and `weil_inertia` remain `continue`; the seven other evidence-bearing lines remain `narrow`; the three scaffold lines remain pre-evidence.

The provisional **Prime-Lattice/Weil-Positivity consolidation is not supported by the current evidence**. Prime Lattice's resolved consultation supplies an explicit source relation whose obligation is distinct from the Weil sign, and `WP-216` independently demonstrates a canonical critical Gram while leaving a separate sign-completion theorem. That does not prove the lines can never share work, but it defeats merger by vocabulary or common representation alone.

The provisional **Prime-Circle pause is deferred**, not rejected on mathematical grounds. Its cross-level mixed-conductor clue is still proposed and has not produced either a survivor or a family-level obstruction. Absence of a local response is not evidence for a pause. The line stays `narrow` with its existing Watch while that discriminating test remains concrete.

The proposed **Mobius redirection has partially happened inside the existing line without a lifecycle change**. `MC-162` and the accepted decoder-stability clue close absolute stability as an intermediate power gain and require either an independent source estimate for a fixed transform or a signed inverse-tail mechanism outside the contraction regime. That is a materially narrower mandate, but it is still a live falsifiable question and does not justify stopping the Watch.

The proposed **Robin alternative remains a pilot, not an activated line**. Its strengthened clue states an appropriate first gate — a new unconditional restriction on an infinite extremal family with all exchange, normalization, and discretization losses included — but no canonical finding has crossed that gate. Mathematical diversity is a reason to test the clue, not evidence that Robin currently dominates an evidence-bearing line.

All ten evidence-bearing research lines retain their existing Research Watches, so the enabled line-specific Watch count is unchanged. No rotation is justified. The next Master pass should reconsider lifecycle only if one of the proposed consultation clues produces a substantive local disposition/finding, a `continue` line loses its discriminating next theorem, or a pre-evidence line crosses its first canonical evidence gate.