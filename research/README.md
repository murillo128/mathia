# Mathia Research State

Canonical evidence is synthesized through `ca2456bd59fd32903f4908374d395683d0ba7c8a`. The latest clean independent adversarial checkpoint is `9e155fc35541b0ce2d9e491fb815f9063debb7f1`, which declares coverage through the reachable commit `917a6ffa2608da5962f62fce4499dcc5085b709a`. There are no open `.review.md` sidecars. `RE-089` is withdrawn and absent; `RE-090` is the canonical corrected Robin same-depth result. Evidence after the checkpoint, notably `VIS-202` and `MC-260`, is used as current canonical evidence but is not described as adversarially cleared.

## Global picture

Portfolio recommendations remain **3 `continue` + 10 `narrow`**. `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

The strongest new source-specific constraint is `WI-270`. Building on the PSD signed-source Laplacian of `WI-269`, a one-signed hypothetical first-crossing mode that screens all active prime lags must pay a Carleman cut tariff across every admissible cut. In particular, the null mode cannot hide its mass in widely separated islands: every relevant cut needs bilateral short-range mass, yielding median/no-gap consequences before any generic positivity argument is invoked. This does not yet force nontrivial autocorrelation at a prime lag, and sign-changing modes still require the full signed PSD machinery. The live gate is therefore to turn the family of cut tariffs and `WI-269` into a finite effective-resistance/Schur or nodal inequality that makes complete prime-lag screening impossible, or to construct an actual Suzuki-compatible counterconfiguration.

Farey and Nyman now have unusually explicit residual carriers. `FD-088` escapes the terminal-sector obstruction of `FD-087`: repeated-prime deflation moves a fixed positive share of the nonsquarefree frontier energy into exact lower-horizon anchors with unit Jordan-weight distortion and anchor scale `a_r \gg \sqrt{H L_H} \gg L_H`, while the destination horizons remain in one asymptotically coherent Schur cap. The unresolved issue is no longer anchor abundance, size, or intra-block coherence but **multihorizon composition**: the anchors are row-adaptive and no existing GCD identity assembles them into one physical lower-horizon energy. `NB-087` similarly scalarizes fixed-ray screening into the exact `q`-adic branch-energy distortion. Bounded radial floor charge forces the accumulated squared distortion to remain controlled, with the tightened rate requiring RMS distortion on the order of `R^{-1/2}`; estimating that scalar directly on the arithmetic source can now falsify the entire fixed-ray strategy.

Arithmetic Fidelity sharpens the exact-incidence barrier rather than removing it. `AF-310` proves that the normalized common-time prime phase relation lattice is cyclic: at most one independent monomial relation can hold. This unifies the exact `k=3,4` zero exclusions, but it also proves that relation-rank rigidity stops being decisive at `k=5`, where the pentagon zero surface contains a dense residual set compatible with rank at most one. Any surviving exact prime hit must be genuinely nontorsion. The next useful theorem must therefore control the actual one-parameter prime-log orbit relative to the zero surface; root-of-unity classification, ambient density, or integer-character rank alone cannot decide the problem.

Robin is no longer review-blocked. `RE-090`, using the stronger aggregate large-prime-gap estimate now recorded in `SOURCES.md`, replaces the withdrawn `RE-089` frontier. Above the corrected `0.57` scale, same-depth higher-layer selected chambers have vanishing threshold measure, while sufficiently deep layers are already negligible from `RE-088`. The residual geometry is finite-complexity: outside a vanishing exceptional set, the live non-first-layer configurations use distinct nonempty depth sets inside a bounded collection, concretely depths below `20`. This does not prove a contradiction; it converts an infinite higher-layer escape into a finite mixed-depth/rightmost-maximizer problem.

Two post-checkpoint findings identify method ceilings rather than new target theorems. `VIS-202` shows that the current absolute-value, uniform-congruence four-form method naturally stops near `\log H \asymp \sqrt{\log y}`: below that scale, even retaining all four primality conditions cannot prove `D_v=o(y^2)` through uniform per-fiber counting. Further descent requires averaged congruence cancellation, large-sieve/dispersion across the affine family, or a different target currency. `MC-260` finds the analogous obstruction after `MC-259` compresses Möbius blindness to at most one pretentious character: textbook/global BDH averaging does not see the extremely sparse, source-selected prime-shell modulus family at the required strength. The next route must be sparse-modulus/source-conditioned dispersion, reciprocity coupling, or direct cancellation in the surviving affine rough cell.

Prime Flute, Prime Lattice, Prime Circle, Weil Positivity, Xi Flow, and Analytic Frontier retain their previous discriminating gates. `PF-319` still rules out inferring the seam-normalized angle from raw corridor energy; `PL-293` still rules out obtaining the needed half-derivative shift control from generic logarithmic-Laplacian regularity; `PC-277` leaves only a shrinking rational-microscopic window after the universal Li ramp; `WP-279`--`WP-280` isolate a PNT-level tangent and a Gamma first correction without an independent sign law; `XF-185`--`XF-186` remain locally well-conditioned but globally unassembled; and `ANF-180`--`ANF-183` retain the sharp source-continuation aperture as one of the cleanest remaining endpoint tests.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make the matched endpoint packet quantitatively sharp: derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` are equivalent discrimination boundaries. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-310` proves the common-time normalized prime phase relation lattice is cyclic, unifies the `k=3,4` exclusions, and shows that relation-rank rigidity becomes compatible with generic pentagon zero geometry at `k=5`. | Control the genuine nontorsion one-parameter prime-log orbit relative to the `k>=5` zero surface; ambient density, torsion theory, and character-rank bounds are no longer sufficient. |
| `farey_discrepancy` | `narrow` | `FD-088` moves a fixed positive share of frontier energy into row-adaptive exact anchors `a_r \gg \sqrt{H L_H} \gg L_H` with unit repeated-prime deflation weight distortion and coherent destination states. | Prove a multihorizon composition law that assembles these row-dependent anchors into nonvanishing physical lower-horizon control, or show that no such assembly can beat the endpoint. |
| `mobius_cancellation` | `narrow` | `MC-259` compresses rank-one blindness to at most one pretentious character; `MC-260` shows that standard global BDH averaging is too diffuse for the sparse source-selected prime-shell modulus family that remains. | Build sparse-modulus/source-conditioned dispersion or reciprocity strong enough to eliminate the one blind pretender, or prove fixed-power cancellation directly in the surviving source coset. |
| `nyman_beurling` | `narrow` | `NB-087` scalarizes fixed-ray screening into exact `q`-adic branch-energy distortion and gives a quadratic lower bound for radial floor charge; bounded charge requires RMS distortion at the `R^{-1/2}` scale. | Estimate the actual arithmetic branch-energy distortion. Divergence of its square sum would rule out every fixed-ray radial certificate regardless of future depth and force cross-ray geometry. |
| `prime_circle` | `narrow` | `PC-277` isolates the normalized Li near-resonance scale `tP/log^2 P=O(1)` with the next correction on the `log^3 P/P` scale. | Extract a zero-sensitive remainder above the universal Li/PNT ramp inside that shrinking window, or show that normalization makes the residual universal too. |
| `prime_flute` | `continue` | `PF-317`--`PF-319` reduce the direct channel to canonical one-pant leakage, show Hilbert--Schmidt mean-square control would suffice, and prove raw corridor-form smallness cannot imply seam-normalized angle smallness. | Prove normalized DtN/Steklov leakage for `Z_LH` itself and a singular-value law for `T_H`; then keep infinite realization and signed reassembly as separate gates. |
| `prime_lattice` | `narrow` | `PL-293` supplies a theorem-backed matched control with the sharp `ell^{1/2}` Dirichlet boundary law and an exact obstruction to generic `H^{1/2}` after zero extension. | Prove zeta-specific suppression/cancellation of the boundary channel, obtain any useful `H^s`, `0<s<1/2`, for the actual low branch, or bypass differentiable shift control. |
| `robin_extremal` | `narrow` | `RE-090` uses the corrected large-gap mass exponent to make same-depth higher-layer chambers negligible above the `0.57` frontier; with `RE-088`, the residual higher-layer problem has bounded depth. | Attack the remaining first-layer and finite mixed-depth/tie/rightmost-maximizer geometry; the same-depth infinite-layer escape is no longer the live frontier. |
| `visual_exploration` | `narrow` | `VIS-201` reaches `sqrt(log y)=o(log H)` by averaging the four-form singular factor; `VIS-202` shows uniform per-fiber congruence counting cannot push that method materially below the `sqrt(log y)` transition. | Introduce averaged congruence cancellation/dispersion across affine fibers, or demonstrate that a different observable or target currency avoids the `D_v` barrier. |
| `weil_inertia` | `continue` | `WI-269` gives a PSD signed-source Laplacian and `WI-270` turns one-signed first-crossing screening into a cut tariff that forces bilateral short-range mass across every admissible cut. | Combine cut tariffs with the signed PSD structure to force quantitative prime-lag activity for arbitrary modes, or construct a genuine Suzuki-compatible screened mode. |
| `weil_positivity` | `narrow` | `WP-279`--`WP-280` separate the universal PNT-level tangent from the Gamma first correction but do not produce a noncircular sign law. | Build an independently forced global non-scalar/infinite-volume coupling that retains a nonuniversal source term and makes zero-sensitive sign information unavoidable. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**The portfolio is converging on source-specific residual carriers rather than ambient regularity.** `WI-270` measures the unavoidable mass crossing each cut after full prime-lag screening; `NB-087` measures the energy distortion left after exact `q`-adic future completion; `FD-088` identifies exact row-adaptive lower anchors after the terminal destination sector has been removed; `AF-310` reduces exact prime incidence to the arithmetic placement of one nontorsion orbit after all generic relation-rank information is exhausted. These are different mathematical objects, but in each case the residual is defined only after the benign matched mechanism has been factored out.

**A residual must also be expressed in the geometry consumed by the endpoint.** Prime Flute remains the clearest warning: raw form energy can vanish while the DtN-normalized angle stays order one. Farey's new anchors are genuinely large and exact but still require a physical assembly map. Nyman's branch distortion is useful precisely because it lower-bounds the completed radial floor charge rather than a pre-completion Gram statistic. Robin's corrected reduction is useful because it is measured in selected threshold-chamber mass rather than ambient event count.

**Two current analytic frontiers are now explicitly method-specific.** `VIS-202` does not say prime-ratio crowding is intrinsically uncontrollable below the `sqrt(log y)` corridor; it says uniform absolute congruence counting cannot prove the needed aggregate bound there. `MC-260` does not say the final blind character survives; it says ordinary all-moduli BDH averaging wastes the sparsity and source selection of the relevant modulus family. Both lines therefore need structured family cancellation rather than another refinement of the same worst-case average.

**Review identity is currently clean.** `RE-089` is not treated as a corrected theorem: it is withdrawn, and the materially changed statement is `RE-090`. The current checkpoint covers the corrected finding and the other new findings through `NB-087`; `VIS-202` and `MC-260` remain current but post-checkpoint. No open sidecar constrains the present portfolio snapshot.

The existing `CLUE-cross-line-fidelity-realization-separation` remains sufficient at Master scope. The new evidence sharpens source-specific residual currencies but does not produce a common source family, operator, normalization, and target from which an exact actionable cross-line transfer theorem follows. No additional Master clue is warranted.

The Graph-owned Riemann Atlas remains navigation-only for portfolio decisions. Its entrypoint and metrics advertise different stale source revisions (`4bc527...` and `834376...`), both far behind current canonical evidence, so no recommendation is inferred from Atlas telemetry.

## Highest-leverage unresolved questions

1. **Weil Inertia quantitative screening cost.** Combine the cut tariffs of `WI-270` with the signed PSD Laplacian of `WI-269` to force a nontrivial active-prime overlap for arbitrary first-crossing modes, or produce a real Suzuki-compatible sign-changing counterconfiguration.
2. **Prime Flute normalized endpoint leakage.** Prove the one-pant Hilbert--Schmidt/weak-`S_1` estimate for `Z_LH` after canonical seam/DtN normalization, independently obtain the singular-value law for `T_H`, and do not infer either from raw corridor energy.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp `Theta(X/a_X)` aperture, with positive-cascade non-excisability kept as a separate final gate.
4. **Farey multihorizon assembly.** Turn the positive-energy packet of exact mesoscopic anchors in `FD-088` into one endpoint-relevant lower-horizon inequality despite row-dependent anchor scales, or prove that such composition necessarily loses the gain.
5. **Nyman branch-energy law.** Compute or bound the `NB-087` distortion sequence on the actual arithmetic source strongly enough to decide whether its square sum stays bounded or diverges along an unbounded branch.
6. **Möbius sparse-modulus pretender test.** Replace the global BDH route rejected by `MC-260` with dispersion tailored to the source-selected prime-shell modulus family, reciprocity structure, or direct rough-cell cancellation.
7. **Visual below the uniform-sieve threshold.** Cross the `log H \asymp \sqrt{\log y}` transition using averaged congruence cancellation across affine fibers, rather than another uniform per-fiber sieve estimate.
8. **Robin finite-depth selection.** Use the `RE-090` reduction to analyze the remaining bounded-depth mixed chambers, ties, and rightmost-maximizer constraints, with the first layer treated explicitly rather than as another negligible event family.

## Portfolio changes proposed

No recommendation change is proposed. Weil Inertia remains `continue` because `WI-270` adds a source-specific, quantitative family of first-crossing constraints on top of `WI-269`; the next theorem is sharply falsifiable and cannot be reproduced by the earlier generic matched controls. Prime Flute remains `continue` because the normalized `Z_LH` and `T_H` endpoint questions are still canonical after `PF-319` removes an invalid shortcut. Analytic Frontier remains `continue` because its critical-aperture source-continuation gate is unchanged and independent of the operator routes.

Among the `narrow` lines, Farey and Nyman make the strongest positive reductions but still stop at assembly/source-estimation gates; Arithmetic Fidelity identifies the exact arithmetic-incidence problem after generic relation theory is exhausted; Robin replaces a challenged threshold by a stronger corrected finite-depth reduction; Visual and Möbius identify precise ceilings of their current averaging machinery rather than intrinsic no-go theorems. Prime Circle, Prime Lattice, Weil Positivity, and Xi Flow retain narrow, source-specific bridges that remain unresolved.

No active line is a strong `pause-candidate` or `merge-candidate`, so no Research Watch rotation is justified. No split or new-line candidate clears the bar. No new Master clue is created because the cross-line commonality remains structural rather than an exact transferable source/operator theorem.