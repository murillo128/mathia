# Mathia Research State

Canonical evidence is synthesized through `7266899806fc3eb2154fadb3e88afc04467510ee`. The latest clean independent adversarial checkpoint is `cd2dee16a1fc0a93dbf01f8fe3827f187e87e190`, which declares coverage through the reachable commit `f9eb849f1063109323c421d2b5f7f7204e2bb586`. A later adversarial action, `baf6d8ff8011cf72be514abc411bcfa6dca4a9da`, opens a challenge against `RE-089`; its `.review.md` sidecar remains open with the Adversary as the last speaker. The numerical same-depth Robin frontier from that finding is therefore non-decisive until owner resolution. Evidence after the clean checkpoint is used as current canonical evidence but is not described as adversarially cleared.

## Global picture

Portfolio recommendations remain **3 `continue` + 10 `narrow`**. `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

The strongest positive structural update is `WI-269`. At a hypothetical first Suzuki crossing, localization by every bounded real Lipschitz multiplier produces a positive-semidefinite signed-source quadratic form; finite partitions turn it into a PSD signed Laplacian with zero row sums. This is materially stronger than the support propagation of `WI-267` and survives the failure of generic semigroup positivity in `WI-268`: the surviving constraint is genuinely source-conditioned. Its nodal form gives a cancellation-free tariff between prime-lag overlap, short-range positive archimedean overlap, and compensating long-range negative archimedean overlap. The remaining gate is now precise: compress that PSD constraint with source-adapted multipliers or an effective-resistance/Schur argument strongly enough to force arithmetic lag activity, or construct a Suzuki-compatible screened mode showing that long-range compensation can still win.

Prime Flute receives an equally important negative calibration. `PF-317`--`PF-318` reduce the direct physical channel to canonical one-pant low/high leakage and show that logarithmic-rank Hilbert--Schmidt control would already suffice for weak trace closure. `PF-319`, however, proves that small raw corridor energy does **not** imply a small seam-normalized direct angle: canonical Steklov/DtN normalization can turn a form-small coupling into an order-one angle. The live theorem must therefore estimate `Z_LH` in the normalized seam geometry itself, for example after subtracting the local DtN principal symbol, rather than infer it from area, perimeter, or unnormalized corridor energy. `T_H` remains a separate singular-value problem.

Several `narrow` lines sharpen the same distinction between source availability and destination-visible capacity. `FD-087` shows that exact integer-dilation anchors inside Farey's long coherent blocks occupy only a vanishing terminal Jordan sector when the common divisor is limited by the block length; additive thickness and intra-block coherence are therefore insufficient unless one reaches larger anchors, the signed bulk, or a multihorizon composition law. `NB-086` finds a terminal block on every fixed dilation ray whose visible correlation matrix lower-bounds the radial Szegő charge by `-log det R`; bounded charge forces the total squared terminal correlation over a linear block to remain bounded. Full future availability is therefore not the same as radial screening capacity.

The aggregate arithmetic lines now split cleanly between useful averaging and irreducible residuals. `VIS-201` successfully averages the four-form local factor before taking a worst fiber, removes the previous `log log y` loss, and pushes `D_v=o(y^2)` down to windows satisfying `sqrt(log y)=o(log H)`; the live frontier is the transition `log H=Theta(sqrt(log y))`, polylogarithmic horizons, or a proof that `D_v` is not the necessary currency. `MC-259` obtains a complementary compression: smooth-shell variance reduces the entire rank-one pretentious obstruction to at most **one** blind character in the exact source quotient. But the available dispersion remainder is still too coarse to improve the support exponent, so the remaining task is to eliminate/control that single pretender or prove fixed-power cancellation directly in the source coset.

Arithmetic Fidelity and Prime Lattice both close generic ambient shortcuts. `AF-309` shows that equal-weight exact-zero incidence for `k>=3` is simultaneously measure zero and dense in ambient frequency space, with dense complement; the exact-hit predicate is topologically unstable everywhere, so measure/topology/continuity cannot supply a robust margin. The prime-log cases `k=3,4` remain zero-free while `k>=5` is unresolved, making exact arithmetic placement the relevant question. `PL-293` shows that the logarithmic principal Dirichlet geometry naturally supports the sharp boundary law `ell(t)^{1/2}`; after zero extension this is incompatible with `H^{1/2}`. Hence the routine absolute first-frequency-moment/Feynman--Hellmann route is not a generic consequence of the principal operator. Any stronger moving-prime-shift control must come from zeta-specific suppression of that boundary channel, a non-absolute identity, some `H^s` gain with `0<s<1/2`, or a route that bypasses differentiation.

Robin has a promising same-depth scarcity mechanism but its advertised exponent is under active review. `RE-087` already rules out candidate counting as a route, and `RE-088` makes sufficiently deep selected layers negligible while leaving the finite low-depth geometry. `RE-089` attempts to show that fixed same-depth higher-layer selected chambers also have vanishing threshold measure above a large-gap exponent, but the open adversarial review points to stronger modern prime-gap total-length estimates and challenges the stated numerical frontier. Until that sidecar is resolved, the robust conclusion is only that same-depth chambers may be reducible by large-gap mass; the exact threshold and resulting frontier are not portfolio-grade evidence.

Prime Circle, Weil Positivity, and Xi Flow remain useful controls on what counts as genuinely zero-sensitive. `PC-277` confines the Li-grid signal to a shrinking near-resonance window `tP/log^2 P=O(1)` and leaves the zero-sensitive residual unresolved. `WP-279`--`WP-280` identify the leading critical Mangoldt tangent as PNT-level and place Gamma in the first correction without producing an independent sign law. `XF-185`--`XF-186` give a well-conditioned local packet chart but still lack unknown-support discovery, global assembly, and source access. These are not failures of local representation; they are missing bridges from representation to the target.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make the matched endpoint packet quantitatively sharp: derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` are equivalent discrimination boundaries. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-309` shows generic equal-weight exact-zero incidence is dense, measure zero, and frequency-unstable; ambient exact-hit topology gives no stable margin. | Determine the exact prime-log incidence for unresolved support sizes and find a genuinely arithmetic global coupling/nonlinear reachable-set restriction that yields quantitative margin. |
| `farey_discrepancy` | `narrow` | `FD-086` gives long coherent Schur blocks, while `FD-087` shows exact small-divisor dilation anchors from those blocks occupy only a vanishing terminal destination sector. | Reach larger anchors or signed bulk energy, prove a destination-energy saving, or build a multihorizon composition law that does not rely on small common divisors. |
| `mobius_cancellation` | `narrow` | `MC-259` compresses the smooth-shell rank-one pretentious obstruction to at most one blind character in the exact source quotient. | Eliminate or quantitatively control that single blind pretender, or prove a source-coset-scaled fixed-power cancellation estimate strong enough to beat the endpoint. |
| `nyman_beurling` | `narrow` | `NB-086` lower-bounds the radial Szegő charge by terminal visible correlation entropy, making a linear terminal block an explicit obstruction to same-ray screening. | Estimate the terminal correlation determinant/energy for the actual arithmetic source; if it diverges, move to a genuinely cross-ray/nonradial cancellation mechanism. |
| `prime_circle` | `narrow` | `PC-277` isolates the normalized Li near-resonance scale `tP/log^2 P=O(1)` with the next correction on the `log^3 P/P` scale. | Extract a zero-sensitive remainder above the universal Li/PNT ramp inside that shrinking window, or show that normalization makes the residual universal too. |
| `prime_flute` | `continue` | `PF-317`--`PF-319` reduce the direct channel to canonical one-pant leakage, show Hilbert--Schmidt mean-square control would suffice, and prove that raw corridor-form smallness cannot imply seam-normalized angle smallness. | Prove normalized DtN/Steklov leakage for `Z_LH` itself and a singular-value law for `T_H`; then keep infinite realization and signed reassembly as separate gates. |
| `prime_lattice` | `narrow` | `PL-293` supplies a theorem-backed matched control with the sharp `ell^{1/2}` Dirichlet boundary law and an exact obstruction to generic `H^{1/2}` after zero extension. | Prove zeta-specific suppression/cancellation of the boundary channel, obtain any useful `H^s`, `0<s<1/2`, for the actual low branch, or bypass differentiable shift control. |
| `robin_extremal` | `narrow` | `RE-087` rules out candidate counting and `RE-088` removes sufficiently deep selected layers; `RE-089` proposes an additional same-depth reduction but its numerical large-gap frontier is under open adversarial review. | Resolve the `RE-089` review first, then attack the remaining finite low-depth, mixed-depth, tie, and rightmost-maximizer geometry without relying on an unsettled threshold. |
| `visual_exploration` | `narrow` | `VIS-201` removes the pointwise local-factor `log log y` loss by averaging the four-form singular factor and extends `D_v=o(y^2)` to `sqrt(log y)=o(log H)`. | Cross the `log H=Theta(sqrt(log y))` transition/polylog regime using stronger family cancellation, or prove that `D_v` is not the currency the target ultimately needs. |
| `weil_inertia` | `continue` | `WI-269` turns first-crossing localization into a PSD signed-source Laplacian and a nodal overlap tariff, a genuinely Suzuki-specific constraint beyond support propagation. | Use finite source-adapted multipliers/effective resistance/Schur compression to force prime-lag overlap despite long-range archimedean compensation, or construct a genuine source-compatible screened mode. |
| `weil_positivity` | `narrow` | `WP-279`--`WP-280` separate the universal PNT-level tangent from the Gamma first correction but do not produce a noncircular sign law. | Build an independently forced global non-scalar/infinite-volume coupling that retains a nonuniversal source term and makes zero-sensitive sign information unavoidable. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**Source-conditioned destination norms are replacing ambient proxies.** Prime Flute cannot infer the physical angle from raw corridor energy because seam normalization changes the scale; Farey cannot count exact dilation anchors if they land in a vanishing terminal sector; Nyman cannot equate unlimited future depth with visible screening if terminal correlation entropy remains. Weil Inertia is the positive version of the same principle: localization becomes useful only after it is inserted into the exact first-crossing source identity, where it yields a PSD signed Laplacian rather than generic Markov positivity.

**Averaging helps only when it is performed in the quantity the target actually sees.** `VIS-201` gains a full `log log` factor by averaging local arithmetic singular factors over the affine family before taking a worst case. `MC-259` similarly compresses the global pretentious obstruction to one source-compatible character, but the remaining analytic error is still too large. The lesson is not simply “average more”; it is to average after the exact source quotient/normalization has identified the surviving residual.

**Exact existence is repeatedly weaker than stable or composable capacity.** `AF-309` gives dense exact-zero incidences with zero robustness; `FD-087` gives exact dilation embeddings whose destination energy vanishes; `NB-086` allows an arbitrarily rich future while preserving a terminal correlation floor. These examples jointly rule out arguments that promote availability, density, or exact representability directly into target control without measuring the normalized residual.

**Generic regularity and universal baselines are increasingly exhausted.** `PL-293` shows that the logarithmic principal operator itself naturally reaches a boundary law that blocks the routine half-derivative route, so further regularity must be state-specific. `PC-277` and `WP-279`--`WP-280` likewise show that leading Li/PNT structure can survive normalization without carrying the desired zero information. The next useful theorem in those lines must isolate what the arithmetic source adds beyond the universal model.

The existing `CLUE-cross-line-fidelity-realization-separation` remains sufficient at Master scope: it already separates source fidelity, target transport, destination realization, and assembly. The new evidence sharpens the residual currency in several lines but does not provide a common source family, operator, normalization, and target from which an exact cross-line transfer theorem follows. No additional Master clue is warranted.

The Graph-owned Riemann Atlas remains navigation-only for portfolio decisions. Its entrypoint and metrics advertise different stale source revisions (`4bc527...` and `834376...`), both far behind current canonical evidence, so no recommendation is inferred from Atlas telemetry.

## Highest-leverage unresolved questions

1. **Weil Inertia finite compression.** Starting from the PSD signed-source Laplacian of `WI-269`, choose a finite source-adapted multiplier partition and prove an effective-resistance, Schur-complement, or nodal inequality that makes all-prime-lag screening impossible unless a quantitative arithmetic overlap is present. A decisive negative control must satisfy the real Suzuki identity, not just abstract firstness.
2. **Prime Flute normalized endpoint leakage.** Prove the one-pant Hilbert--Schmidt/weak-`S_1` estimate for `Z_LH` after the canonical seam/DtN normalization, explicitly avoiding the invalid inference closed by `PF-319`; independently obtain the singular-value law for `T_H`.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp `Theta(X/a_X)` aperture, with positive-cascade non-excisability kept as a separate final gate.
4. **Farey destination-visible composition.** Convert the long coherent frontier blocks into nonvanishing destination energy through larger exact anchors, signed bulk transport, or a multihorizon composition law that does not collapse into the terminal sector quantified by `FD-087`.
5. **Nyman terminal correlation entropy.** Determine whether `-log det R_(R,q)` or an equivalent terminal correlation energy remains bounded along an unbounded arithmetic subsequence. Divergence would rule out the fixed-ray strategy and force cross-ray geometry.
6. **Möbius single-pretender barrier.** Decide the one remaining blind pretentious character after source projection: prove it cannot persist on the moving active shell, bound it with endpoint-strength cancellation, or show a matched control that survives.
7. **Visual critical-window transition.** Improve the averaged four-form estimate through `log H=Theta(sqrt(log y))` toward polylogarithmic windows, or prove that controlling `D_v` more strongly is unnecessary for the target observable.
8. **Robin finite low-depth selection.** Resolve the open `RE-089` challenge before using its numerical frontier, then attack the residual first/second/mixed-depth selected chambers, ties, and rightmost-maximizer constraints by geometry rather than candidate counts.

## Portfolio changes proposed

No recommendation change is proposed. Weil Inertia remains `continue` because `WI-269` converts the real source equation into a new PSD finite-compression object whose next test is both source-specific and falsifiable. Prime Flute remains `continue` because `PF-319` closes only an insufficient proof route: the exact normalized `Z_LH` and `T_H` endpoint questions remain sharply posed. Analytic Frontier remains `continue` because its critical-aperture source-continuation gate is unchanged and independent of the operator routes.

The strongest movements among `narrow` lines still stop one gate before the target. Farey has source abundance and local coherence but loses exact small-anchor energy at the destination; Nyman has an explicit terminal entropy obstruction but no arithmetic estimate of it; Visual improves the aggregate sieve but stops at a critical subpolynomial transition; Möbius compresses the analytic obstruction to one pretender without removing it; Prime Lattice rules out a generic half-derivative route without deciding the actual zeta state; Arithmetic Fidelity makes exact incidence topologically ubiquitous but unstable; Prime Circle and Weil Positivity isolate universal baselines without a zero-sensitive residual; Xi Flow remains locally identifiable but globally unassembled. Robin is additionally review-blocked on the exact `RE-089` frontier. No active line is a strong `pause-candidate` or `merge-candidate`, no new standalone line is justified, and the current Research Watch allocation is left unchanged.