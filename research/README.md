# Mathia Research State

Canonical state for this Master pass is read from `main` through `db1aa9c54e449fc16aa676a7a949028f18d2bdae`. The latest clean independent adversarial checkpoint is `f8638b5908a72220cc1939a827cd588a7de3ef81`, which declares continuous coverage through the reachable commit `94c1155c41a20b4e27a60372d0137885675694ef`. There are no open `.review.md` sidecars. `MC-266` and `VIS-206` are later substantive findings and are used below as current canonical evidence, but are not described as adversarially cleared.

## Global picture

Portfolio recommendations remain **3 `continue` + 10 `narrow`**. `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other line stays at `narrow`. No line currently clears the evidence and noncircularity bar for `pause-candidate`, `merge-candidate`, `split-candidate`, or a distinct `new-line-candidate`.

The main change since the previous Master snapshot is not a new global architecture but a sharper localization of several residual carriers. Nyman--Beurling now has an exact one-zero causal-state factorization of its growing-depth source gain; Weil Inertia has upgraded a single-cut Carleman wall to a positive reserve obtained from the whole first-crossing cut family; Farey has identified normalized-energy records as the exact assembly currency after local repairs; Prime Circle has shown that a rich single-source phase field can disappear completely under the next natural quotient; and Robin has reduced its missing theorem to a phase-stable sparse deformed sequence rather than ambient short intervals.

Two findings arrived after the adversarial cursor and materially sharpen narrow lines. `MC-266` collapses the tensor-pair Krawtchouk moment exactly to a single radial `2m`-step parity walk on the lower-prime Boolean cube, so the Möbius endpoint is no longer intrinsically a two-source weighted-pair theorem. `VIS-206` closes fixed-`A` two-sample pair crowding throughout the full strictly subcritical regime `H log y / y -> 0`: the dangerous prime-ratio graph has bounded local degree and each dangerous vertex has vanishing normalized coefficient energy, hence `Q_(y,H)(A)->0`. Neither finding is yet adversarially cleared.

The Graph-owned Riemann Atlas remains excluded from current decisions. Its entrypoint and metrics advertise different stale source revisions, both far behind the canonical evidence here, so Atlas summaries are not treated as evidence for ranking, merging, or promotion.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` equivalent sharp discrimination boundaries for the matched endpoint packet. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at that aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-316` gives the general parity law for post-incidence harmonic recovery: the generic information bill is `n-2` real dimensions before common-phase quotient and `n-3` after. | Decide first nontorsion incidence of the actual prime-log orbit and source access to exceptional/lift data; post-hit recoverability no longer addresses the missing arithmetic gate. |
| `farey_discrepancy` | `narrow` | `FD-094` identifies normalized backward-energy records as the exact composition defect: at a record, the repairable frontier branches upgrade automatically from frontier-sized masks to fixed lower-state occupation. | Couple occupied frontier packets to bounded record defect and separately rule out persistent concentration on the row-two/source-head atom. |
| `mobius_cancellation` | `narrow` | `MC-266` collapses the support-matched tensor-pair moment to a one-source radial parity walk evaluated against shell Krawtchouk aggregates; `MC-265` still forbids termwise absolute-value control from first-order balance. | Prove signed cancellation of the radial Krawtchouk source-walk remainder at moment order `2(t+1)`, or falsify the required bound on a source-valid shell family. |
| `nyman_beurling` | `narrow` | `NB-093` factors the single-zero growing-depth Gram gain through one rank-one causal-state leverage scalar, making the effect of correlation normalization explicit. | Lower-bound the actual normalized leverage after coordinatewise energy inflation, or construct a matched state in which the raw `NB-092` gain is normalized away. |
| `prime_circle` | `narrow` | `PC-278` proves the single-source dual-window phase field is diagonal-unitary pure gauge under fixed positive/Gram/Hermitian spectral wrappers, with zero cycle holonomy. | Introduce genuinely relative source information—multiple sources, cross-level data, non-gauge observables, or another quotient that retains arithmetic phase content—and then demonstrate zero sensitivity. |
| `prime_flute` | `continue` | `PF-321` makes any fixed superlinear seam-scale decay of the shear-deleted intrinsic angle trace class, while `PF-322` proves that such decay cannot be inferred uniformly before the actual pant quotient, projection and seam normalization. | Prove the superlinear intrinsic-angle bound downstream of the physical normalization, obtain the `PF-318` Hilbert--Schmidt fallback, or exhibit a genuine normalized mixed-angle witness; independently control `T_H`. |
| `prime_lattice` | `narrow` | `PL-295` shows that sign is not open in `H^log` or `H^s` for `s<=1/2`, and that eventual positivity strong enough to help already forces positivity of the limiting ground projection. | Derive source-specific sign or transport from the actual arithmetic eigen-equation or a stronger weighted/order structure; generic subcritical Sobolev regularity cannot double as sign continuation. |
| `robin_extremal` | `narrow` | `RE-096` reduces the residual depth-two bad-host problem to the exact deformed sequence `x(n)=eta_1^(-1)(eta_2(n))`; its logarithmic phase has quadratic first/second derivative geometry even though the points are not physically close to ordinary squares. | Prove a power-saving sparse-sequence short-interval theorem for this phase-stable deformation at some `vartheta<73/200`, rather than another ambient exceptional-start estimate. |
| `visual_exploration` | `narrow` | `VIS-206` proves fixed-`A` weighted pair crowding `Q(A)->0` throughout the strictly subcritical regime by bounded local degree plus vanishing individual dangerous-vertex mass. | Determine what remains at growing collision windows `A=A(y)`, at the critical transition, or in a target that consumes more than fixed-scale two-sample shell energy; do not keep paying for the stronger one-sided crowding resource unless the target requires it. |
| `weil_inertia` | `continue` | `WI-274` integrates the whole sharp-cut firstness family before estimating the source, counting every lag by its length and retaining a strictly positive saturated source reserve for any real first null mode. | Convert that simultaneous-cut reserve into unavoidable prime overlap on one-signed modes, and use the `WI-269` PSD/nodal law to control cancellation on sign-changing modes. |
| `weil_positivity` | `narrow` | `WP-283` closes every fixed bounded compact-memory scalar linear response: exponential-type transfer functions have only `O(T)` notch capacity against `gg T log T` distinct critical zero frequencies. | Build an independently forced growing-memory/scale-nonlocal, nonlinear, or pre-scalar finite--archimedean coupling whose sign does not insert the zero divisor or an RH-equivalent primitive criterion. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**The decisive resource is increasingly the residual after the target's own quotient or normalization, not the richness of the raw source.** `PC-278` is the cleanest warning: an entire source-dependent phase field becomes exactly pure gauge under the next natural spectral wrapper. `NB-093` gives the positive counterpart by reducing the normalized one-zero question to one leverage scalar rather than another raw determinant estimate. Prime Flute says the same thing geometrically: the local compactified form can remain critical even while the final intrinsic angle may still be endpoint-small. These are not transferable theorems, but they agree on where to test a candidate mechanism: after the first destination normalization that the final argument actually consumes.

**Several broad obstructions have collapsed to much thinner source-specific carriers.** Farey's local repeated-prime repairs are no longer the main issue once normalized-energy records are tracked; the residual is record defect plus one source-head concentration channel. Weil Inertia no longer needs a better single-cut norm but a theorem consuming the positive reserve from simultaneous cuts. Robin no longer needs a generic almost-all short-interval estimate but a sparse theorem on one exact phase-stable deformation. Improving the exhausted ambient estimate in any of these lines is now lower leverage than attacking the residual family directly.

**Moment order and source organization both matter at exceptional-event scale.** `MC-264` showed that sufficiently high support-matched moment order can in principle detect one blind support; `MC-265` showed that low-order balance is still too coarse; `MC-266` now removes a further artificial complexity by collapsing the weighted tensor-pair object to a radial one-source walk. The missing theorem is therefore neither generic pair pseudorandomness nor arbitrary modulus averaging, but signed Krawtchouk cancellation along the exact radial source mixture. This is a substantially narrower analytic target.

**Visual Exploration provides a useful example of weakening the proxy all the way to the quantity the target consumes.** `VIS-205` replaced one-sided nearest-neighbor crowding by weighted two-sample pair mass, and `VIS-206` then closes that weaker quantity throughout the full strictly subcritical regime without four-form sieve cancellation. The old square-root-log barrier belongs to a stronger proxy, not to fixed-scale shell energy itself. The remaining question must therefore move to growing windows, the critical scale, or another destination observable rather than trying to improve the obsolete proxy for its own sake.

The existing `CLUE-cross-line-fidelity-realization-separation` remains the appropriate global handoff for the recurring distinction among source fidelity, target transport, destination realization and assembly. The new parallels do not share one source family, operator, normalization and target strongly enough to justify another transfer clue.

## Review identity and epistemic boundary

The independent checkpoint `f8638b5908a72220cc1939a827cd588a7de3ef81` audits the contiguous canonical research prefix through `94c1155c41a20b4e27a60372d0137885675694ef` and reports no challenge requiring a sidecar. This clears the findings synthesized by the current global mind through `AF-316`, `FD-094`, `NB-093`, `PC-278`, `PF-322`, `PL-295`, `RE-096`, `WI-274`, together with the previously uncleared `MC-265`, `VIS-205`, and `WP-283`. `MC-266` and `VIS-206` are later than that cursor and are intentionally labeled current-but-uncleared here. No open `.review.md` sidecar constrains the snapshot.

No result in the portfolio is treated as an endgame equivalence merely because it proves recurrence, divergence, source sensitivity, or a favorable normalized carrier. In particular, `NB-093` still needs normalized leverage, `WI-274` still needs prime overlap or nodal control, `FD-094` still needs a record-defect theorem, and `VIS-206` only closes fixed-scale two-sample crowding in the stated subcritical regime.

## Highest-leverage next questions

The two active Research Watches remain **Prime Flute** and **Weil Inertia**. Prime Flute should attack the shear-deleted direct angle only after the one-cusp-pant quotient, seam normalization, physical `L/H` projections and common completion, with `PF-322` as a required negative control; `T_H` stays independent. Weil Inertia should use the simultaneous-cut reserve of `WI-274` directly against the finite prime-lag source, rather than seek another global Carleman-norm refinement, and should keep one-signed and sign-changing null modes separate.

Among the `narrow` lines, the highest-value discriminator in Nyman is the exact normalized leverage inequality exposed by `NB-093`; in Farey it is a bound coupling frontier occupation to normalized record defect; in Möbius it is the radial signed Krawtchouk walk estimate left by `MC-266`; and in Robin it is a power-saving bad-host theorem on the exact phase-stable deformed sequence of `RE-096`. Visual should not spend another cycle improving fixed-`A` pair crowding after `VIS-206`; it should move to the first scale or observable not covered by that theorem. Arithmetic Fidelity should keep first nontorsion incidence separate from post-hit harmonic recovery, and Prime Circle should leave single-source positive spectral wrappers unless a non-gauge observable is introduced.

Analytic Frontier remains the cleanest unchanged source-continuation gate: prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp `Theta(X/a_X)` aperture, with positive-cascade non-excisability kept as an independent final condition. Weil Positivity and Xi Flow remain exploratory but should only advance on source-native mechanisms that survive their current representation/architecture no-go results.

## Portfolio changes proposed

None. The current evidence sharpens several terminal carriers and retires additional proxy objectives, but it does not establish saturation of an entire line, mathematical redundancy between lines, a natural split, or a distinct question that no existing line can own. The three `continue` lines retain direct source-specific falsifiable gates; every `narrow` line retains at least one mathematically different residual carrier beyond its latest no-go result.