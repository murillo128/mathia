# Mathia Research State

Canonical evidence synthesized through `b38dfc23c8e05f0a01c4c7bcb3473e9efc80160f`.

## Global picture

Mathia has canonical evidence in all thirteen current research lines. The portfolio remains **3 `continue` and 10 `narrow`**: `analytic_frontier`, `prime_flute`, and `weil_inertia` remain `continue`; the other ten remain `narrow`. No line currently meets the evidence bar for merge, pause, split, replacement, or a new standalone line. The strongest change in this pass is a sharpening of what information must survive the final reduction in the three `continue` lines: Analytic Frontier now requires adjacent-scale phase-faithful control rather than one-scale energy alone, Prime Flute has replaced a raw reservoir-amplification question by a form-native ideal-and-gap theorem for the physical normalized cross coupling, and Weil Inertia retains two genuinely source-side routes, finite-prime Suzuki first crossing and sparse-defect persistence.

Analytic Frontier has corrected the optimistic reading of the moving-window square-function witness. `ANF-171` shows that large one-scale energy can coexist with a tiny endpoint completion because phase may cancel the relevant boundary quantity. `ANF-172` identifies the exact phase-sensitive object: for the Fejer-scale quantities `T_L=L F_L` and `D_L=T_L-T_{L-1}`, the adjacent-scale identity `D_{2H}-D_H=2 Re S_H` recovers the signed endpoint contribution. The live theorem is therefore not to bound `F_H` and `F_{2H}` separately, which destroys the cancellation, but to prove a source-specific adjacent-scale estimate for `D_{2H}-D_H` at the distinguished twist while preserving the prime-minus-rough structure. Fatal non-excisability of the surviving same-profile cascade remains a separate downstream obligation.

Prime Flute has also become more precise. `PF-281` moves the outer `P/H` coupling into the correct energy spaces. For the positive block form, the canonical normalized cross operator `T=A^{-1/2}BC^{-1/2}` is bounded even when the raw cross block is only a quadratic-form coupling. If `T` lies in a compact symmetric ideal and has the resulting strict gap `||T||<1`, the mixed inverse block factors through `T` and belongs to the same ideal; in particular `T in S_{1,infinity}` is sufficient for the weak-trace mixed block needed downstream. A direct-sum positive control shows that positivity and normalization alone do not force compactness. The decisive theorem is now to establish ideal decay and a strict gap for the **physical** energy-normalized cross form after finite-pant completion and neighboring-cell coupling. This subsumes the scalar reservoir concern into a sharper operator-valued criterion rather than reopening the already-refuted normalized-source locality route.

Weil Inertia keeps its `continue` status with two complementary source-side frontiers. The existing `WI-232`--`WI-235` ledger makes same-window screening expensive in zero count, radial depth, and spacing surplus, while the Bombieri handoff asks whether a finite negative direction survives an infinite sparse off-line tail. Independently, `WI-238` reduces every finite Suzuki-style radius to an operator assembled from finitely many prime/power contributions. The actionable finite-prime problem is to prove a first-crossing, monotonicity, coercivity, or nondegeneracy statement that persists with radius while remaining strictly cheaper than the all-radius RH-equivalent criterion. Neither route should be replaced by another relaxed window optimization.

There is no open `.review.md` sidecar in the current tree. The latest adversarial checkpoint is `b193d321f021471db9aaabd1677bfec3b5521128`, with `Adversarial-Reviewed-Through: 7da2217d33ea7f81b4022541778008b79b40b7b6`; later canonical findings remain current evidence but await the next independent checkpoint. All thirteen current research lines retain one enabled Research Watch; no scheduler rotation or duplication repair is warranted. The Riemann Atlas remains outside portfolio decisions because its two derived surfaces declare different, stale source revisions relative to the canonical corpus.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-172` restores the missing endpoint phase through an exact adjacent-scale Fejer-slope identity after `ANF-171` showed that one-scale energy alone can be misleading. | Prove a source-specific bound for the signed adjacent-scale defect `D_{2H}-D_H` at the distinguished twist while preserving prime/rough cancellation. Separate positive bounds for the two scales are not enough. |
| `arithmetic_fidelity` | `narrow` | `AF-259` reduces the remaining periodic/profinite sampling obstruction to an explicit odd-multiplier continued-fraction phase law, leaving only finitely many multiplier classes at fixed detectability. | Exclude the exceptional cancellation locus for the actual growing zeta phase vector, including simultaneous dominant modes and adequate conditioning, without assuming the conclusion through the Li data. |
| `farey_discrepancy` | `narrow` | `FD-040` proves coercivity of the nonsquarefree rows against every fixed finite separated Mellin-frequency packet at fixed `beta>1/2`. | Determine whether a growing, infinite, or increasingly nonseparated frequency packet can make that coercivity collapse in the actual Möbius/Farey source; fixed packets are no longer an escape. |
| `mobius_cancellation` | `narrow` | `MC-210` shows that centered square-modulus/BDH-style progression variance removes the principal residue mean but leaves that principal mode itself fixed-power Mertens-complete. | Control the raw prescribed-residue energy through principal/transverse cancellation or new principal arithmetic. Centered variance plus a separate principal estimate merely reintroduces the Mertens barrier. |
| `nyman_beurling` | `narrow` | `NB-041` constructs an explicit source-defined Möbius/Vasyunin dual for the logarithmic tail coordinate, reducing the oracle objection and placing the optimal scalar in an `O(1/log M)` source-defined window uniformly in `N`. | Improve the projected repair/source-aligned defect to `o(1/log N)` strictly more cheaply than an `N`-scale projection solve or access to `d_N`. The current source-defined precision is still too coarse for arbitrary superlogarithmic `M`. |
| `prime_circle` | `narrow` | `PC-253`--`PC-254` continue to classicalize fixed-base collision and nonlinear operator invariants, including local path-run factorizations. | Introduce a genuinely growing singular or multilinear carrier before scalarization. More auxiliaries or readout algebra on a fixed finite base do not create the missing arithmetic state. |
| `prime_flute` | `continue` | `PF-281` gives a form-native Schur factorization: ideal decay of the physical energy-normalized cross form `T=A^{-1/2}BC^{-1/2}` transfers directly to the mixed inverse block, even when the raw cross term is unbounded. | Prove `T` belongs to the required weak/Schatten ideal and has a strict gap after finite-pant completion and neighboring-cell coupling. Positivity alone admits noncompact matched controls. |
| `prime_lattice` | `narrow` | `PL-264` isolates a genuine local prime-power shock with a universal negative rank-one template carrying von Mangoldt/location weight. | Couple many local prime-power events and the pole/archimedean completion on a growing core without losing the source weights or sign. A single negative event is not a global criterion. |
| `robin_extremal` | `narrow` | `RE-039` shows that source-compatible finite packets can synthesize the dangerous selector fan, so finite spectral geometry and coarse spacing do not exclude the event. | Derive the translated prime-race normal form uniformly across the actual counterexample block and use the real CA selector/infinite spectrum to exclude the surviving cone rather than another finite matched model. |
| `visual_exploration` | `narrow` | `VIS-156` proves that fixed affine prime-index subsampling `a+qn` is only a deterministic clock reparameterization: every fixed finite prime torus retains the same Haar/fixed-node null law. | Move to growing stride/geometry, a non-affine selected subsequence, noncommensurate offsets, or an independently anchored analytic coordinate, with a predeclared source-side reason to escape the Gram/CUE null. |
| `weil_inertia` | `continue` | `WI-238` gives a finite-radius Suzuki zero-mode reduction to finitely many prime/power contributions, complementing the `WI-232`--`WI-235` screen ledger and the Bombieri sparse-tail handoff. | Prove a finite-prime first-crossing/nondegeneracy theorem that persists with radius without restating the global criterion; in parallel test whether a sparse off-line tail can neutralize Bombieri's finite negative directions within the existing zero-budget ledger. |
| `weil_positivity` | `narrow` | `WP-248`--`WP-249` show that bounded critical all-prime-to-spherical intertwiners and crossed-product innerization do not by themselves supply the source-forced critical weights or positive arithmetic core. | Build a native critical/global category whose state or unbounded cycle fixes the all-prime coefficients, archimedean term, and sign before scalarization. The Maaß--Selberg remainder-sign clue remains the bounded handoff. |
| `xi_flow` | `narrow` | `XF-174` rewrites the subcritical edge observable as a central Mellin deficit with an explicit quarter-power tail tax, after broad positive/analytic matched controls have survived generic regularity tests. | Use the single square lattice `pi n^2`, its coefficient law, or complex modular deformation to prove an atomwise/mesoscopic source theorem that the matched positive scale mixtures cannot reproduce. |

## Cross-line structure

The dominant boundary is now **phase and source coupling must survive the exact compression consumed by the endgame**. Analytic Frontier is the clearest phase example: one-scale energy is not enough because the endpoint is an adjacent-scale signed difference. Prime Flute is the operator analogue: raw low/high coupling is not the right invariant, while the energy-normalized cross form is exactly the object that controls the Schur inverse. Weil Inertia similarly moves away from relaxed support geometry toward finite-prime first crossing or persistence of a genuinely negative zero-source direction.

A second boundary is **making the target explicit does not automatically make the theorem cheaper**. `NB-041` is progress because it replaces a free logarithmic-tail oracle by a source-defined dual, but its `O(1/log M)` precision is still too coarse for the Burnol-scale objective and the projected repair remains target-selected. `MC-210` gives the same warning from a different direction: centering exposes a clean transverse variance but the removed principal mode is itself Mertens-complete. Arithmetic Fidelity likewise classifies the exceptional sampling mechanism without yet excluding it for the one arithmetic phase vector that matters.

The Master handoffs already present remain sufficient. The Bombieri-to-Weil-Inertia clue tests sparse-defect persistence and complements, rather than duplicates, the internal finite-prime Suzuki route. The Maaß--Selberg-to-Weil-Positivity clue still targets the sign of the remainder after a genuinely global positive scattering construction. No other current cross-line resemblance has matured into a same-object, same-normalization implication strong enough to justify a new clue.

## Highest-leverage unresolved questions

1. **Prime Flute normalized cross ideal.** Estimate the singular values of the physical energy-normalized `P/H` cross form after finite-pant completion and neighboring-cell coupling, and prove the strict gap needed by the form-level Schur factorization.
2. **Analytic Frontier adjacent-scale phase theorem.** Bound the exact signed Fejer-slope defect `D_{2H}-D_H` for the real demodulated prime-minus-Ramanujan source with a power-scale gain, rather than bounding the two energies separately.
3. **Weil Inertia finite-prime first crossing.** On the `WI-238` finite-radius operator family, prove source nondegeneracy or monotonicity that survives increasing radius and is demonstrably cheaper than the global zero-mode criterion; keep the Bombieri sparse-tail test as the independent alternative.
4. **Nyman source-defined repair at Burnol precision.** Turn the `NB-041` source dual into the target-selected projected repair or an equivalent return certificate at `o(1/log N)` cost without reconstructing the full finite section.
5. **Escape only through source-specific growing structure.** Farey must test growing/nonseparated Mellin packets, Möbius must avoid the centered-principal Mertens barrier, Visual must leave fixed affine clock geometries, and Arithmetic Fidelity must prove deterministic zeta-phase noncancellation.

## Portfolio changes proposed

No recommendation changes. `analytic_frontier`, `prime_flute`, and `weil_inertia` retain short, falsifiable implication chains adjacent to their endgames and remain `continue`. The ten other lines remain `narrow`. Nyman--Beurling has made a genuine source-access improvement in `NB-041`, but it still lacks an independently cheaper return inequality; Möbius Cancellation has exposed rather than solved its principal mode; and Visual Exploration has closed another fixed-geometry null rather than revealed a zeta-specific signal.

No merge, split, replacement, new-line, or Research Watch rotation is proposed, and no new Master clue is needed in this pass. The correct action is to keep the portfolio stable while requiring the next evidence to attack phase-faithful, source-defined, or energy-normalized gates rather than reopen representation classes already closed by matched controls.