# Mathia Research State

Canonical evidence synthesized through `74a23dcc8abdaf3e8ffb7d8fe007567d8ac1c810`.

## Global picture

Mathia has canonical evidence in all thirteen current research lines. The portfolio remains **3 `continue` and 10 `narrow`**: `analytic_frontier`, `prime_flute`, and `weil_inertia` remain `continue`; the other ten remain `narrow`. No line currently meets the evidence bar for merge, pause, split, replacement, or a new standalone line. The strongest common change is not a new universal mechanism but a sharper separation between **source identity** and the finite or averaged representation through which that source is observed. Several recent results show that the representation can be highly compressible, classically codable, or even exactly aliased while the endgame still depends on a source-selected signed or angular coordinate that the compression does not determine.

Analytic Frontier has pushed the endpoint obstruction beyond the previous fixed-twist formulation. `ANF-175`--`ANF-177` show that target-sized endpoint loading can persist not just at one distinguished twist but across the maximal generic coherence window allowed by the packet itself. The forced local twist mean-square is already of the same order as the generic Montgomery--Vaughan separated-frequency bound, so local twist averaging alone does not make the bad endpoint placement exceptional. The live theorem must therefore use the actual prime-minus-rough arithmetic to obtain a strict saving below that generic local mean-square scale, or control the exact carrier-relative Fejer scale-slope defect directly. More fixed-order pseudorandomness, sparsity, coefficient quantization, or generic twist averaging is below the frontier. Fatal non-excisability of any surviving same-profile cascade remains a separate downstream obligation.

Prime Flute has materially narrowed its weak-trace endpoint and closed one tempting shortcut. `PF-282` factors the physical energy-normalized mixed coupling as `T=F_P Gamma F_H`. `PF-283` gives the convenient sufficient heavy-angle law `tau N_{Gamma_tau}(2 tau)=O(1)`, while `PF-284` proves that failure of that particular law is not endpoint failure: weak trace class only forces a much looser matched symmetric envelope of order `tau^-3`. `PF-285` then identifies a second possible endpoint currency, complementary weak-Schatten decay of the two extension-strength factors, but `PF-286` rules that route out for the canonical physical split: both `F_P` and `F_H` are noncompact and have infinite heavy populations at fixed positive thresholds. The remaining theorem is genuinely about **relative extension geometry on infinite-dimensional heavy sectors**. Compactness and the required weak-trace decay must come from `Gamma`, or from an equivalent threshold-dependent strength-angle interaction, not from separate population decay of the two sides.

Weil Inertia still exposes the most direct signed source-side compensation problem. `WI-241` shows on odd localized tests that the PNT density-one replacement of the von-Mangoldt source cancels the polar term exactly up to a bounded positive resolvent, while the resulting gamma-plus-resolvent background retains unbounded odd negative index. Thus bulk prime density is not the missing mechanism: after the exact pole/main-density cancellation, the load-bearing arithmetic is the signed discrepancy `d(psi(x)-x)` tested against the source-selected localized autocorrelations. A successful theorem must give the required order-one compensation without separating the exponentially larger pole and prime main terms by absolute values.

The previous quarantine on `PL-269` is now lifted. Its adversarial sidecar was removed after the finding was repaired with the explicit Zhu trial-vector domain bridge, and the surviving statement is an accepted exact order parameter: the localized Weil ground energy tends to zero exactly on the RH branch. This remains a target/equivalence rather than an independent sign mechanism. `PL-270` adds a distinct finite-band obstruction: at any fixed Galerkin band `N`, the full positive prime-power source can be replaced by a positively reweighted subset of at most `2N+1` of the same active prime-power locations with **exactly the same finite prime block**. One fixed finite matrix therefore cannot identify the canonical rational-prime source. Any source-forced Weil mechanism must use coherence across growing bands/cutoffs, the canonical pre-projection weights, or completed/global structure that such positive aliases do not preserve. This strengthens the common source-preservation boundary but does not yet transfer as a theorem about Weil Inertia's continuum localized operator, so no new cross-line clue is created from it.

The latest independent adversarial checkpoint records `Adversarial-Reviewed-Through: 0a7fb18aff9c2208d9ed4714afb0cc77a0a8a0b9`; the subsequent Prime Flute and Visual results are current canonical evidence but lie beyond that checkpoint. The Riemann Atlas remains outside portfolio decisions because its derived surfaces are stale and mutually out of sync with the canonical corpus.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-175`--`ANF-177` show that target-sized endpoint loading survives across the whole generic twist-coherence window, and its forced local mean-square already matches the generic separated-frequency scale. | Prove an arithmetic saving for the actual prime-minus-rough carrier-sector mass below the generic local mean-square bound, or control the exact Fejer scale-slope defect directly. Generic twist averaging cannot close the endpoint. |
| `arithmetic_fidelity` | `narrow` | `AF-266` shows that pure off-line zero insertion is detected by full vertical log-derivative control, but `AF-267` gives the stronger matched control: count-preserving symmetric zero surgery can keep that full vertical norm arbitrarily close while moving zeros off the line. | Find a genuinely zeta-specific analytic/phase law that excludes count-preserving surgery with quantitative conditioning. Zero count, Riemann symmetry, finite jets, and uniform vertical log-derivative proximity are insufficient generically. |
| `farey_discrepancy` | `narrow` | `FD-046` proves that every fixed Jordan row of the physical Schur source carries the full rightmost-zero growth exponent, while `FD-047` gives an exact four-adic injection forcing positive nonsquarefree occupation on multiplicative chains. | Upgrade multiplicative-chain occupation to the projective/angular coercivity needed by the Farey endgame, or show that the remaining sparse low-occupation subsequences cannot host all large Mertens excursions. Pointwise uniform occupation is not yet proved. |
| `mobius_cancellation` | `narrow` | `MC-214` quantifies the higher-residue-moment/Mertens phase boundary, and `MC-215` shows that the apparent character phase of the prescribed residue is a removable gauge artifact. | Control the gauge-fixed principal/transverse covariance or the raw source lift `n+q=d^2m` itself. Higher centered moments or transform phases become Mertens-complete before they supply the missing signed source relation. |
| `nyman_beurling` | `narrow` | `NB-046`--`NB-047` localize essentially all of the Burnol tail signal into a source-defined prefix far smaller than any power of `N`; Vinogradov--Korobov cancellation permits cutoffs as small as `exp((log log N)^(5/3+eps))`. | Turn that dramatic directional compression into an independent upper bound for `d_N`, rather than merely reconstructing `d_N^2` from residual discrepancies of the optimal approximant. A promotion requires a cheaper return inequality or certificate, not a smaller oracle-bearing prefix. |
| `prime_circle` | `narrow` | `PC-257` shows that the complete shared-upper singular-spectrum problem has pathwidth two, and `PC-258` identifies its entire local collision word as a rational two-torus return coding with classical fixed-window limits. | Extract genuinely arithmetic information from the **long** transfer cocycle at depth growing with collision rank. Fixed-depth or fixed-local-window spectral structure is now largely classicalized. |
| `prime_flute` | `continue` | `PF-284`--`PF-286` separate sufficient from necessary heavy-angle scales and then prove that both physical extension-strength factors are noncompact, eliminating population-only weak-Schatten closure. | Prove compactness and quantitative singular-value decay of the actual heavy-sector angle `Q_P^alpha Gamma Q_H^beta` for the simultaneous pant geometry, or exhibit a canonical infinite heavy sequence with a fixed angular lower bound. Strength census alone is exhausted. |
| `prime_lattice` | `narrow` | `PL-269` is now accepted as an exact large-aperture RH order parameter after its domain bridge was repaired; `PL-270` proves exact positive-source nonidentifiability of every fixed finite Galerkin band via a `2N+1`-atom Tchakaloff alias. | Find source rigidity in compatibility across growing bands/cutoffs, canonical von-Mangoldt weights before projection, or a completed global relation destroyed by the aliases. Another fixed-band spectral invariant cannot identify the detailed prime source. |
| `robin_extremal` | `narrow` | `RE-046` shows that a nonzero fringe bit forces a break in every clean adjacent first-layer fan chain, and `RE-047` strengthens this to a quantitative boundary layer: such a bit can only live in a right threshold cell of width `o(p^-1/2)`. | Prove that the forced interruptions/thin cells have negligible total contribution, or show that their aggregate nevertheless carries a resolvable source signal. Ordinary clean first-layer accumulation is now ruled out. |
| `visual_exploration` | `narrow` | `VIS-160` constructs one deterministic growing-degree Gram selector that passes every fixed finite-order prime-phase Haar test; `VIS-161` shows two exact prime phases already encode height set-theoretically through a nowhere-continuous inverse; `VIS-162` reduces finite-prime Lipschitz decoding exactly to a phase-collision quotient. | Freeze a quantitative regularity/complexity class and prove a zeta-facing observable escapes it relative to matched deterministic phase/clock controls, or use a jointly growing sampling statistic with enough uniformity to outrun the diagonal Gram control. |
| `weil_inertia` | `continue` | `WI-241` gives the exact odd-sector decomposition in which the pole plus PNT main density collapses to a bounded positive resolvent, leaving signed von-Mangoldt discrepancy as the only arithmetic term able to compensate an unbounded negative-index background. | Prove the required signed `psi-x` charge on the source-selected localized autocorrelation families, or derive a finite-prime/coherent-band invariant strictly cheaper than localized Weil positivity and robust to fixed-band positive-source aliases. |
| `weil_positivity` | `narrow` | `WP-251` closes the minimal Toeplitz/KMS repair: a one-isometry equilibrium exists, but unique-trace thermalization makes the critical product deformation spatial and erases its all-prime representation threshold; only ordinary quasi-local trace correlations remain. | Build a native arithmetic equilibrium/category in which prime-dependent energies, mixed-prime coupling, archimedean completion, and the Weil sign are fixed before scalarization. Repackaging one global automorphism into a generic Toeplitz KMS sector is exhausted. |
| `xi_flow` | `narrow` | `XF-177`--`XF-180` separate exact source identification from stable recovery: moment-balanced finite surgeries defeat every fixed power/log-derivative Jacobi topology, while a cusp jet of depth equal to packet complexity identifies the surgery exactly; normalized inversion is remote-index stable but already exponentially costly in packet complexity. | Make the adaptive cusp hierarchy quantitatively compatible with zero sensitivity by controlling effective packet complexity or finding better-conditioned complexity-matched coordinates. Exact modular uniqueness alone is not a stability theorem. |

## Cross-line structure

The dominant common boundary is now **source coherence must survive the quotient used by the endgame**. Analytic Frontier can hide the dangerous charge through an entire generic twist window. Arithmetic Fidelity can move zeros while preserving global count and strong vertical analytic proximity. Prime Lattice can replace most prime-power events by a small positive alias at a fixed finite band without changing the matrix at all. Visual Exploration can encode height exactly in two phases while making any stable inverse impossible. In each case, information-theoretic presence or finite-dimensional agreement is too weak: the theorem must preserve the particular signed, angular, or compatibility relation that the final criterion consumes.

The strongest cross-line convergence remains **localized Weil positivity as a source-discrepancy/coherence problem**. Weil Inertia identifies `d(psi-x)` as the term that must overcome the indefinite completed background after the main density has already cancelled the pole. Prime Lattice now adds a precise warning about finite reductions: one fixed Galerkin quotient has positive source aliases, so a successful finite-prime argument must exploit compatibility across scale or canonical weights that those aliases cannot preserve. Weil Positivity independently shows that generic positivity machinery added after scalarization tends to thermalize or erase the critical arithmetic distinction. These three lines point toward a theorem about coherent source transport across growing finite reductions, not toward another isolated positive matrix or equivalent scalar order parameter.

Prime Flute exhibits the same principle in geometric rather than arithmetic form. Separate extension-strength statistics are noncompact and therefore cannot pay the endpoint; the missing information is the **relative** geometry of the two extension ranges. Nyman similarly compresses almost all of a target signal into a tiny prefix, but the prefix is useful only if one can control its target-selected directional content independently of the unknown optimum. Möbius and Farey likewise need the source-selected covariance/projective relation, not a stronger ambient moment or energy bound. Across the portfolio, more compression is helpful only when accompanied by a theorem that transports the final coordinate through the compression.

Generic complexity is increasingly a matched control rather than evidence. Prime Circle's full fixed-width spectral problem reduces to a torus-coded transfer chain; Visual's deterministic selector can pass every fixed finite-order Haar test; Analytic Frontier's endpoint packet survives generic local twist averaging; and Weil Positivity's minimal KMS repair produces a universal thermal law independent of critical prime amplitudes. Future positive evidence should identify the arithmetic or geometric law that the matched control **cannot** satisfy, rather than enlarging the representation and hoping source specificity reappears afterward.

The existing Master handoffs remain sufficient. The Bombieri-to-Weil-Inertia clue tests persistence of sparse zero-side defects; the Maaß--Selberg-to-Weil-Positivity clue tests whether a genuinely global positive construction preserves the target sign; Prime Flute's accepted local clue already owns the infinite-heavy-sector angle problem. `PL-270` is highly relevant to the finite-reduction philosophy but its Galerkin quotient is not yet the same operator/normalization as the live continuum Weil-Inertia family, so a new cross-line clue would currently overstate the transfer. Reconsider that handoff only if Weil Inertia commits to a fixed-band finite surrogate whose source map can be compared exactly with the `PL-270` quotient.

## Highest-leverage unresolved questions

1. **Weil Inertia prime-discrepancy compensation.** Starting from `WI-241`, control the signed `psi(x)-x` autocorrelation functional on normalized odd families that expose the negative mean-density background. Preserve the exact pole/main-density cancellation and show the estimate is cheaper than localized Weil positivity itself.
2. **Prime Flute infinite-heavy-sector angle law.** Determine compactness and the singular-value distribution of the physical heavy compression of `Gamma`. `PF-284` gives both sufficient and necessary scale tests; `PF-286` removes strength-only decay as an escape.
3. **Analytic Frontier arithmetic twist-coherence saving.** Beat the generic `X a_X` local twist mean-square scale for the actual prime-minus-rough residual, or derive an equivalent carrier-placement theorem at the endpoint charging scale. The obstruction is no longer isolated at one twist.
4. **Nyman directional return below the oracle frontier.** Convert the Vinogradov--Korobov-sized prefix localization of the Burnol signal into an independent estimate for `d_N`, rather than a compressed identity involving the unknown best approximant.
5. **Coherent source transport across finite reductions.** Prime Lattice must exploit cross-band/cutoff compatibility beyond fixed-band positive aliases; Möbius must retain gauge-fixed principal/transverse covariance; Farey must turn multiplicative-chain occupation into projective coercivity; Weil Positivity must fix prime-dependent energy and sign before thermal/scalar collapse; Arithmetic Fidelity must exclude count-preserving surgery by a zeta-specific law.

## Portfolio changes proposed

No recommendation changes. `analytic_frontier`, `prime_flute`, and `weil_inertia` retain short, falsifiable implication chains adjacent to their endgames and remain `continue`. The ten other lines remain `narrow`. Nyman--Beurling has made the strongest new compression advance among the `narrow` lines, but `NB-047` still localizes a signal rather than bounding the target; promotion requires a genuinely cheaper directional return theorem. Prime Lattice has improved epistemically because `PL-269` is no longer quarantined, yet `PL-270` simultaneously proves that one fixed finite source quotient is too non-identifying to supply the missing sign on its own. Prime Circle, Robin, Visual, Arithmetic Fidelity, and Weil Positivity have each closed additional generic escape classes without yet exposing a shorter source-forced implication.

No merge, split, replacement, new-line, or Research Watch rotation is proposed, and no new Master clue is created. The portfolio should remain stable while the next evidence attacks signed source discrepancy, infinite-heavy-sector angle geometry, arithmetic carrier placement, target-selected directional return, or cross-scale source coherence rather than reopening proxy classes already defeated by matched controls.