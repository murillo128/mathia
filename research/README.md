# Mathia Research State

Canonical evidence synthesized through `fe17e6ca48f23532ab2bb2f384e21e26efa34fef`.

## Global picture

Mathia has canonical evidence in all thirteen current research lines. The portfolio remains **3 `continue` and 10 `narrow`**: `analytic_frontier`, `prime_flute`, and `weil_inertia` remain `continue`; the other ten remain `narrow`. No line currently meets the evidence bar for merge, pause, split, replacement, or a new standalone line. The strongest global change is that several formerly plausible representation-level escapes are now ruled out at exactly the point where they would need to carry the target: raw-source regularity in Prime Flute, local/carrier endpoint anchoring in Analytic Frontier, generic compressed target data in Nyman--Beurling, fixed-base nonlinear state growth in Prime Circle, and permutation-invariant source processing in Möbius Cancellation. The surviving questions are correspondingly more source-specific and more falsifiable.

Prime Flute has crossed another useful negative boundary without losing its route. `PF-274` supplies a continuous-Hahn spectral representation with a persisted phase gauge whose adversarial challenge is closed. `PF-275` then shows that the normalized **raw source** itself cannot satisfy the desired supercritical fixed-row high-to-low leakage estimate: the complete-Bernstein continuum reaches the critical mass-one edge and leaves the raw source at the `j^{-1}` boundary rather than any `R>1` decay. This invalidates the upstream estimate, not the endgame. `PF-269`--`PF-271` already established that the physical proof only needs the leakage remaining after positive separated propagation. The live fixed-axis gate is therefore the **normalized-source plus propagator composition**, analyzed in the continuous-Hahn coordinates; further attempts to prove `R>1` for the raw source alone should be rejected as targeting a false intermediate statement.

Analytic Frontier has similarly exhausted increasingly rich local anchors. `ANF-166` shows that stronger-than-any-fixed-log all-interval cancellation above the current activation scale can coexist with full bow-size endpoint completion through a charge-and-hold primitive. `ANF-167` rules out uniform local activity as the missing anchor, and `ANF-168` shows that exact logarithmic carrier-ray structure still does not determine the absolute endpoint primitive. The unsupported implication is now genuinely cumulative and source-specific: the actual demodulated prime-minus-surrogate source must prevent persistent endpoint charge by a power-scale estimate, a direct primitive-square-function bound, or another global relation that cannot be reproduced by the matched controls. Fatal non-excisability of the surviving same-profile cascade remains an independent downstream obligation.

Weil Inertia remains the third `continue` line because its local screen model is already tightly priced and the remaining escape is sharply defined. `WI-232`--`WI-235` couple local zero count, radial depth, and spacing surplus strongly enough that a same-window density-matched screen cannot be rescued by any ordinary small spacing correction once the selected amplitude breaches local capacity. The existing Bombieri handoff from `PL-259`--`PL-260` targets the exact residue: full cofinal Weil truncations eventually detect every finite-total off-line defect even though low moments may miss a zero-density exceptional set. The live question is whether a finite negative direction persists quantitatively against an **infinite sparse remote tail**, or whether neutralizing it forces that tail to violate the existing count/radial/spacing ledger.

There is no open `.review.md` sidecar in the current tree. The latest independent adversarial checkpoint is commit `263da54d2e4196370a2438d0adc28b8256b60a8d`, declaring coverage through `c84e9194ad8dfcea78e144cc2edadc55c4d3823d`; later canonical evidence is current but awaits a later independent checkpoint. All thirteen current research lines have one enabled Research Watch, so no scheduler rotation or duplication repair is warranted. The Riemann Atlas remains outside portfolio decisions because its derived surfaces have not been established as synchronized with the current canonical corpus.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-166`--`ANF-168` show that arbitrarily strong fixed-log interval cancellation, uniform local activity, and exact logarithmic carrier rays can all coexist with an uncontrolled endpoint primitive. | Prove a genuinely cumulative source theorem for the actual demodulated prime-minus-surrogate residual: a power-scale endpoint anchor, primitive-square-function estimate, or equivalent global prime/rough relation. Separately prove fatal non-excisability of the surviving cascade. |
| `arithmetic_fidelity` | `narrow` | `AF-251`--`AF-254` make destructive finite-mode sampling exponentially exceptional in phase space, while `AF-255` shows that Keiper coefficient height couples growth and cancellation scales rather than giving a free sampling theorem. | Prove a zeta-specific deterministic phase/height theorem for the growing Li family and reduce source-access cost. Generic Haar/Baire smallness of the bad set cannot certify the one arithmetic phase vector actually supplied by zeta. |
| `farey_discrepancy` | `narrow` | `FD-036` upgrades the linear-prime-breadth Schur obstruction to a pointwise half-power normalized prefix gap through quotient-shell Lipschitz rigidity. | Convert that projective gap into an actual Mertens/RH-scale contraction, or prove the proposed joint nonsquarefree Jordan-energy occupation needed to prevent corner concentration. A pointwise `H^{-1/2}` deficit is structure, not yet an exponent gain. |
| `mobius_cancellation` | `narrow` | `MC-204` shows full permutation-invariant ternary source processing collapses to signed mass/support statistics. `MC-205` proves that cyclic mixed-shift kernels genuinely retain labelled relational spectrum, but the principal Möbius carrier still splits exactly from it: either it is cancelled or recovered by an explicit decoder plus the relational remainder. | Obtain a new arithmetic theorem for the centred sign/support shift spectrum `K_w`, or for its joint numerator with the principal carrier. Merely changing symmetry or choosing a clever shift kernel does not make the principal mode leak out of controlled transverse data. |
| `nyman_beurling` | `narrow` | `NB-033`--`NB-034` give a source-faithful near-logarithmic, polynomially conditioned skeleton preserving relative Nyman distance and the low Möbius core; `NB-035` then shows its weighted-tail scalar is itself uniformly Möbius-locked. | Resolve the active target-data clue: certify the skeleton target vector from bounded explicit arithmetic data without reconstructing the full section or hiding Mertens/`d_N` in the oracle. Promotion requires a cheaper return theorem, not another representation compression. |
| `prime_circle` | `narrow` | `PC-246`--`PC-252` progressively close fixed-base enlargement: arbitrary finite transfer packets, fresh-prime channels, shared-upper collisions, and row-local rectangular nonlinearities remain finite CRT/weighted-shift or affine periodic residue data. | Make the **state itself** grow through source-forced lower moduli or genuinely global relations. More nonlinear readout or more auxiliaries on a fixed finite base do not create a new arithmetic carrier. |
| `prime_flute` | `continue` | `PF-274` exposes the exact continuous-Hahn spectral transform and phase geometry; `PF-275` proves the normalized raw-source stage is critically limited by the mass-one continuum and cannot itself have supercritical fixed-row leakage. | Prove or refute one `R>1` low-output estimate for the **full normalized-source plus positive separated-propagation composition**, uniformly through the mass-one boundary layer. Do not demand the already-false raw-source estimate. If the composite passes, transport the margin to the physical corridor and `P/H`/pant/reassembly stages. |
| `prime_lattice` | `narrow` | `PL-259`--`PL-261` separate moment observability from cofinal/finite-core form observability; finite off-line packets and finite arithmetic cores are detectable. `PL-262` identifies the remaining quantitative gate as expanding-window Fourier/Weil stability rather than finite detectability. | Prove a shape-preserving lower Riesz/frame or singular-value bound on the growing zeta-relevant test space, or build a countermodel showing that exact finite observability becomes ill-conditioned in the cofinal limit. |
| `robin_extremal` | `narrow` | `RE-031`--`RE-035` show that finite dominant off-critical zero packets can realize the selected adaptive threshold cone/ray, even with bounded logarithmic gaps, so no universal finite-packet sign/spacing obstruction survives. | Use the **actual CA/source selector**, the infinite spectrum, or a growing-packet relation to exclude the bad events. Generic finite spectral geometry and coarse event spacing are now controls, not proof mechanisms. |
| `visual_exploration` | `narrow` | `VIS-149` moves the frozen packet test to a source-side fourth-moment gate. `VIS-150` adds a second deterministic Gram-clock transition: after dephasing, lags below `log^2 N` collapse, `log^2 N` lags trace a deterministic torus curve, and larger lags in its proved range can become Haar purely from clock shear. | Derive a residual that survives the complete prime-phase/Gram-clock null and the post-calibration fourth-moment error, with growing-support uniformity or an independently anchored analytic coordinate. Do not treat superlogarithmic separation or clock-generated Haar appearance as arithmetic mixing. |
| `weil_inertia` | `continue` | `WI-232`--`WI-235` make same-window screening quantitatively expensive in zero count, radial depth, and spacing surplus; `PL-260` supplies the complementary fact that cofinal Weil truncations detect every finite-total off-line defect. | Close the infinite-sparse/nonlocal regime: quantify persistence of a finite negative direction under a remote zero-density tail, or prove that neutralization necessarily exceeds the WI amplitude ledger. The existing Bombieri bridge clue is the correct bounded handoff. |
| `weil_positivity` | `narrow` | `WP-242`--`WP-244` show that squarefree/nonsquarefree congruence and higher-rank spherical scattering still scalarize the Riemann arithmetic; `WP-247` further shows that critical all-prime nonspherical deformation leaves the canonical restricted-tensor sector. | Pursue only a canonical **pre-scalar** finite--archimedean/noncharacter incidence or vector-valued construction that keeps exact all-prime coefficients, the archimedean term, and a forced sign simultaneously. The Maaß--Selberg clue is already narrowed to this interface. |
| `xi_flow` | `narrow` | `XF-168`--`XF-170` isolate the source boundary: semigroup rigidity singles out the square-lattice heat mixture in one restricted class, while positive scale mixtures remain flexible enough to create additional nonreal zeros subject only to an inverse-displacement barrier. | Exploit the exact lattice `pi n^2`, coefficient law, and complex modular/all-time compatibility. Positivity, analytic smoothness, complete monotonicity, finite jets, or generic scale-mixture restrictions remain reproducible controls. |

## Cross-line structure

The strongest cross-line lesson is **target-preserving compression is not yet target control**. Nyman--Beurling now has an unusually efficient low-dimensional skeleton, but `NB-035` shows that the final weighted-tail coordinate remains Möbius-locked; Prime Lattice has exact finite-core detectability but still needs quantitative stability on an expanding Weil window; Arithmetic Fidelity has generic phase nonexceptionality but not a source theorem for the actual zeta phase vector. These are useful compressions precisely because they expose the next missing implication, not because dimensional reduction itself makes that implication cheaper.

A second boundary is **upstream regularity versus downstream effective regularity**. Prime Flute is the clearest case: `PF-275` proves that the raw normalized source sits exactly at the critical leakage edge, yet the endgame only consumes what remains after positive propagation. Analytic Frontier has the dual phenomenon: excellent local increment control does not anchor an endpoint primitive charged earlier. Weil Inertia likewise shows that local window optimization cannot decide an infinite remote compensation problem. In all three `continue` lines the relevant norm has to be taken after the exact transport, accumulation, or complement operation used by the endgame; stronger statements on a convenient intermediate representation can be false or irrelevant.

A third convergence is **breaking symmetry only helps if the new relational spectrum is controlled arithmetically**. `MC-204` closes full permutation-invariant source hierarchies, while `MC-205` shows that cyclic shift sensitivity does retain genuinely new labelled correlations but leaves the principal carrier algebraically separate. Prime Circle reaches the same boundary from the finite-state side: row-local nonlinearities on a fixed base remain affine periodic residue data. Weil Positivity reaches it from automorphic representation theory: adding channels or nonspherical deformation after scalarization either remains scalar or leaves the canonical infinite tensor sector. The needed structure must be source-forced before the target coordinate is quotiented or scalarized away.

The established cross-line transfers remain bounded and useful. `PL-260` feeds the Bombieri cofinal negative-index boundary into the Weil Inertia sparse-defect clue; it does not identify Bombieri truncations with the relaxed WI screens. `PL-253` feeds Maaß--Selberg global positivity into the Weil Positivity sign-interface clue; the destination clue now explicitly rejects fixed-level scalar character decompositions and noncanonical all-prime nonspherical deformations. No newer result supplies a sufficiently distinct cross-line mechanism to justify another Master clue.

Visual Exploration adds a methodological control rather than proof evidence. `VIS-150` shows that point collapse, deterministic curve geometry, and even Haar-looking lag populations can all be generated by the deterministic Gram clock at different scales. Together with the fourth-moment gate in `VIS-149`, this reinforces the portfolio rule that visual or statistical complexity is informative only after the representation, calibration, and sampling nulls have been quotient out explicitly.

## Highest-leverage unresolved questions

1. **Prime Flute source--propagator composition.** In the exact continuous-Hahn/finite-seam normalization, prove or refute one supercritical low-output estimate for the normalized source followed by positive separated propagation. The mass-one raw-source obstruction must be included rather than bypassed.
2. **Analytic Frontier endpoint persistence.** For the actual demodulated prime-minus-surrogate source, exclude the charge-and-hold primitive by a power-scale cumulative estimate, a direct primitive-square-function theorem, or an equivalent source-specific global relation; local activity and carrier-ray structure are no longer candidates.
3. **Weil Inertia sparse negative-direction persistence.** Use the Bombieri handoff to determine whether a finite off-line packet's negative direction survives an infinite remote zero-density tail unless the tail violates the `WI-232`--`WI-235` count/radial/spacing ledger.
4. **Nyman target-data economy.** Decide whether the near-logarithmic conditioned skeleton can be populated/certified from bounded explicit arithmetic data without rebuilding the full section or importing an equivalent Möbius/Mertens oracle. Only then ask for a residual-specific return inequality for `d_N`.
5. **Farey projective gap and Visual source residual.** On Farey, convert the new pointwise half-power linear-prefix gap into a target-scale contraction or close the joint nonsquarefree-energy clue. On Visual, prove the post-calibration fourth-moment/source-error bound and a statistic surviving the full Gram-clock null before inspecting fresh confirmation data.

## Portfolio changes proposed

No recommendation changes. `analytic_frontier`, `prime_flute`, and `weil_inertia` retain short, falsifiable implication chains adjacent to their endgames and remain `continue`. The ten other lines remain `narrow`: several have made substantial structural progress, especially Nyman--Beurling and Farey Discrepancy, but none yet converts its new representation or obstruction into a cheaper target theorem. Robin Extremal, Prime Circle, Möbius Cancellation, Weil Positivity, Xi Flow, and Visual Exploration have each closed broad generic escape classes while retaining at least one source-specific discriminator, so the stronger `pause-candidate` gate is not met.

No merge, split, replacement, new-line, or Research Watch rotation is proposed. The existing Bombieri-to-Weil-Inertia, Maaß--Selberg-to-Weil-Positivity, Nyman target-data, Farey joint-energy, Prime Flute composite-leakage, and Visual clock/source clues already cover the actionable handoffs without duplication. The correct Master action is therefore to keep the portfolio allocation stable and require the next evidence to attack these first unsupported implications rather than reopen representation classes already closed by matched controls.