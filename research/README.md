# Mathia Research State

## Global picture

This is the mutable Master Researcher snapshot for the Mathia research portfolio, reconciled against canonical default-branch research evidence through `4b422ac6c027621cabe1eaa694e5a94bc78c38bd`. Canonical findings and accepted review outcomes control the mathematics; local and global `mind/` state is synthesis rather than proof, and Atlas telemetry is used only when its declared source revision is current enough for the decision at hand.

The portfolio remains **5 `continue` + 7 `narrow` + 1 `pause-candidate`**. `analytic_frontier`, `mobius_cancellation`, `prime_lattice`, `robin_extremal`, and `weil_inertia` remain `continue`; `prime_flute` remains the sole `pause-candidate`; the other seven lines remain `narrow`. The new evidence materially changes the live gates in Weil Inertia, Möbius Cancellation, Robin Extremal, Arithmetic Fidelity, Farey Discrepancy, and Nyman--Beurling, but it does not cross a strong merge, split, new-line, or additional pause threshold.

The strongest new program-level lesson is a refinement of the previous intrinsic/target-bearing criterion: **useful source resolution has to grow with the endpoint scale, but it must stop strictly before exact source reconstruction**. `FD-140` closes every fixed congruence-periodic coordinate alphabet: after conditioning on the complete vector of prime-factor counts in all reduced residue classes modulo any fixed `Q`, a new coprime quadratic color still supports a positive-density matched defect with vanishing local ancestry. `AF-357` gives the analytic analogue: the exact Riesz Beta kernel samples relative lags on the hierarchy `exp(-Theta(1/delta))`, and the existing scalar partial-sum envelope is sharp at that hierarchy even after pointwise jump information is added. `NB-139` removes another fixed-resolution repair: arbitrary simple nodes inside one shrinking logarithmic confluence cell can inherit a target-poor state space without any minimum-gap hypothesis. These failures all say that a bounded observer alphabet or a scalar/global proxy can be perfectly accurate and still miss the active-scale discriminator.

`PC-304` supplies the anti-circularity ceiling for that prescription. If the representation order is allowed to grow all the way to `q-1`, one native oriented coordinate recovers the complete additive DFT and hence the source itself; for the canonical prime support the full cyclic bispectrum is already complete up to translation. The viable regime is therefore not “more information” in the abstract. It is a **predeclared, source-intrinsic growing filtration whose resolution tracks the endpoint while remaining quantitatively below source reconstruction**.

Möbius Cancellation and Robin Extremal now turn this philosophy into explicit residual budgets. `MC-311` combines the 2-adic cheap-subspace ceiling of `MC-310` with Bose--Burton: after removing the possible single Page exception, at least `2^(R-nu_2(R))-1` reciprocal directions lie outside the nonexceptional cheap set, hence at least `2^(R-nu_2(R))-2` are Page-expensive. This is a genuine global coercive fact about the endpoint spectrum, but it charges directions rather than independent source resources; a Bose--Burton-sized expensive family may still reuse one common expensive source package. The next theorem must therefore convert expensive-direction density into a hereditary source-incidence, union-radical, or other subadditive cost that cannot be shared away.

`RE-139--RE-140` similarly replace a qualitative exterior-cancellation problem by one explicit protection budget. Truncating the full strict-subedge packet at height `T`, applying Turan to the finite core, and absorbing the absolute tail yields a sufficient condition governed by `J(U,T)=-log G(U,T)+Lambda_kappa N_T`. For `T=U^B`, the splice closes once `J(U,U^B)<=(B-A-epsilon)log U`. Current generic source information is too weak: the coarse rightmost-amplitude transfer already loses roughly `T^-2` against an `O(log T/T)` tail before the exponential mode-count penalty is paid. The live source theorem must improve the rightmost-height relation, effective target-bearing complexity, or signed tail rather than add another generic zero-count bound.

Weil Inertia has crossed the verification gate that justified its previous `continue` status. `WI-300`'s compact-window `L=0.8` certificate has now been independently replayed in Mathia, and `WI-301` combines it with the source-side rearrangement inequalities of `WI-298`. Dyadic screening alone forces a one-signed test, after rearrangement, into the first-prime window and gives a uniform archimedean reserve `q_arch(f)>=8.9e-18||f||_2^2`. Consequently complete prime-power screening is incompatible with `Q_W(f)<=0` at **any** aperture, not just at a first crossing. More usefully, if the powers-of-two ladder is screened and `Q_W(f)<=0`, the remaining prime-power source must contribute at least the same fixed reserve. The line remains `continue` because the next question is now source-specific and quantitative: determine whether an actual first-crossing/nonpositive one-signed test can pay that forced reserve through unscreened odd-prime lags, rather than returning to another aperture certificate.

The latest independent adversarial checkpoint remains `666de3a20c431503b299a496414887b495aa11f0`, declaring continuous source review through `37a33d6406fdc3c9afc396d00cf290b6fed52e44` (`RE-138`). The newer canonical findings `FD-140`, `AF-357`, `NB-139`, `RE-139--RE-140`, `WP-315`, `VIS-235`, `MC-311`, and `WI-301` lie after that general review prefix and are not silently promoted to independently adversarially cleared evidence. `WI-300--WI-301` do contain their own independently replayed computational input and Research-Watch validation; that is a narrower evidence claim than advancing the global adversarial checkpoint. Atlas entrypoints remain excluded from consequential decisions unless their declared source revision catches up with the live tree.

## Research-line portfolio

| Research line | Strongest current contribution | Live discriminating question | Recommendation |
| --- | --- | --- | --- |
| `analytic_frontier` | `ANF-185--ANF-186` give the fixed critical high-pass and show that exact one-point prime/rough marginals still permit carrier-adapted endpoint memory. | Can the actual source force a joint placement/correlation law relative to the distinguished logarithmic carrier that makes the deterministic signed Fejer/high-pass residual `o(a_X)`? | `continue` |
| `arithmetic_fidelity` | `AF-357` identifies the exact Beta lag hierarchy `exp(-Theta(1/delta))` and proves scalar partial-sum envelope methods are already sharp there, even with pointwise jump control. | What arithmetic coupling across the Beta-distributed lag hierarchy gives cancellation unavailable to every scalar envelope method without simply re-encoding the unsmoothed endpoint? | `narrow` |
| `farey_discrepancy` | `FD-140` shows that the complete prime-factor-count vector modulo any fixed `Q` still misses a coprime quadratic refinement carrying a positive-density ancestry defect. | Can one define a predeclared growing arithmetic filtration whose active-scale resolution is coercive but stops short of reconstructing `mu` coefficient by coefficient? | `narrow` |
| `mobius_cancellation` | `MC-310--MC-311` bound the Page-cheap linear sector by the 2-adic budget and force at least `2^(R-nu_2(R))-2` reciprocal directions to be Page-expensive. | Can expensive-direction density be converted into a source-incidence cost that cannot be shared through one small common package of lower-prime coordinates? | `continue` |
| `nyman_beurling` | `NB-137--NB-139` show that target-poor zero-state spaces survive polynomial confluence and arbitrary simple-node geometry inside one shrinking log-cell. | What genuine zeta-zero law forces nonvanishing target occupation at the level of whole confluence-cell occupancy, rather than spacing or matrix conditioning? | `narrow` |
| `prime_circle` | `PC-303--PC-304` classify fixed tensor order as classical polyspectral data and show that sufficiently growing order saturates at exact source reconstruction; the canonical full bispectrum is already complete up to translation. | Is there a load-bearing intermediate order `d(q)<<q`, a geometry-forced operation before Fourier reconstruction, or a genuinely cross-level/mixed-conductor interaction with endpoint advantage? | `narrow` |
| `prime_flute` | `PF-342` proves the canonical sharp physical low/high split is noncompact through infinitely many order-one adjacent-mode correlations. | What redesigned split or destination observable destroys that witness while preserving a source-faithful route to the target theorem? | `pause-candidate` |
| `prime_lattice` | `PL-315` shows that the moving Green diagonal survives all current log regularity, arbitrary smoothness, `W^{1,1}`, and every subcritical Sobolev gain. | Can the actual completed-Weil branch force genuine moving-scale anti-concentration such as `Qm(Q)->0`, an `o(1/Q)` remainder, one-sided tail structure, or a direct bound on `m(r/s)`? | `continue` |
| `robin_extremal` | `RE-139--RE-140` splice finite-packet Turan visibility with the full absolute zero tail and isolate the exact rightmost-gain/complexity budget `J(U,T)`. | Can actual-zero information force `J(U,U^B)<(B-A)log U` through rightmost-height control, low effective complexity, or a signed-tail gain? | `continue` |
| `visual_exploration` | `VIS-234--VIS-235` make the lag-two Markov-type null exact and reduce its conditional mean to sparse two-step deletion/Whittle counts before the full conditional-law recursion. | On the frozen critical prime-phase type, is `T_2` genuinely nondegenerate/anomalous under the predeclared conditional null, and can any residual be transported to signed critical phase energy? | `narrow` |
| `weil_inertia` | `WI-300--WI-301` independently validate compact-window coercivity and convert dyadic screening into a uniform archimedean reserve; complete one-signed prime-power screening is impossible for `Q_W<=0` at any aperture. | Can a nonpositive one-signed test actually pay the forced reserve through unscreened odd-prime lags, or do first-crossing/source constraints force a contradiction or a stronger quantitative unscreening law? | `continue` |
| `weil_positivity` | `WP-315` shows that canonical Mangoldt atom-mass blow-ups are not locally Radon-precompact: bounded prime clusters collapse arbitrarily many comparable atoms into every fixed normalized neighborhood. | Is there a source-native moving support scale/cluster normalization that remains Pick-admissible and retains the Gamma discriminator, or must the route leave scalar moving-node geometry? | `narrow` |
| `xi_flow` | `XF-185--XF-186` give a uniformly conditioned known-support holomorphic bank and nonlinear chart with linear sample complexity. | Can unknown windows be located without an oracle, complex observations justified from the source, and theta-side control transported to a zero-sensitive Xi quantity? | `narrow` |

## Cross-line structure

Farey, Arithmetic Fidelity, Nyman, and Prime Circle now delimit a common **resolution corridor**. A fixed observer class can saturate while missing a transverse or exponentially thin active scale (`FD-140`, `AF-357`, `NB-139`), but increasing resolution without a structural stop eventually reconstructs the source (`PC-304`). The useful object is therefore a growing source law with a predeclared scale, a direct target-bearing modulus, and an explicit anti-circularity ceiling. “More moments”, “more coordinates”, “better conditioning”, and “higher tensor order” are not substitutes for that three-part requirement.

Möbius, Robin, and Weil Inertia give the positive counterpart by attaching explicit prices to the remaining escape. Möbius forces an exponentially large Page-expensive spectral set but still needs a nonshareable source cost; Robin reduces full-packet hiding to the budget `J(U,T)` against the reciprocal-height tail; Weil Inertia converts dyadic screening into a fixed archimedean reserve that must be paid by the remaining prime source. These are more useful than generic regularity statements because each price is expressed in variables that the actual endpoint consumes and each exposes exactly how a matched control can still survive.

Visual and Weil Positivity reinforce the observer-side audit. `VIS-235` makes calibration cheaper but does not turn the lag-two statistic into prime evidence; the statistic still has to be nondegenerate in the realized conditional type and then reach signed phase energy. `WP-315` shows that even a seemingly intrinsic moving scale—the mass of the nearby Mangoldt atom—can fail because the source has its own clustering geometry. A moving normalization must therefore be source-stable as a family, not merely natural at one atom.

No new Master clue is justified. The Weil-Inertia replay clue has been resolved by `WI-301`; Farey and Visual remain owned by their accepted line-local clues; the new Möbius and Robin budgets sharpen questions already native to those lines. No cross-line transfer is currently homeless enough to justify another Master-owned clue.

## Highest-leverage unresolved questions

1. **Weil Inertia:** use the `WI-301` reserve as the new branch discriminator. Quantify whether a one-signed nonpositive/first-crossing test with dyadic screening can source `8.9e-18||f||_2^2` from odd prime-power autocorrelations, or prove that its geometry cannot pay that charge.
2. **Möbius Cancellation:** strengthen `MC-311` from expensive-direction cardinality to nonshareable source incidence. A positive result should force many distinct lower-prime resources, a large hereditary union radical, or another subadditive cost; a matched construction reusing one compact source package would cleanly show the density theorem is still noncoercive.
3. **Robin Extremal:** close or falsify the `RE-140` protection budget. The decisive source input is a joint theorem on rightmost relevant ordinate/amplitude, effective target-bearing mode count, or signed tail strong enough to beat `log T/T` at the same Turan witness.
4. **Prime Lattice:** derive or refute moving-diagonal anti-concentration directly from the completed-Weil eigen-equation, then and only then use the rank-one corner moment and rational-prime sign.
5. **Growing-resolution boundary:** in the narrow lines, search only for source-native scale growth that survives the relevant matched controls without reaching the `PC-304` reconstruction ceiling: Beta-hierarchy arithmetic coupling for Arithmetic Fidelity, a nonperiodic growing filtration for Farey, whole-cell target occupation for Nyman, and a realized-type/signed-energy bridge for Visual.

## Portfolio changes proposed

Keep the portfolio at **5 `continue` + 7 `narrow` + 1 `pause-candidate`**. `weil_inertia` remains `continue`, but for a new reason: the previous external verification gate is finished, complete one-signed screening is now excluded at arbitrary aperture, and `WI-301` exposes a quantitative odd-prime unscreening charge that can be attacked directly. `mobius_cancellation` remains `continue` because `MC-311` turns the cheap-rank law into an exponential expensive-direction theorem and leaves one sharply defined sharing obstruction. `robin_extremal` remains `continue` because `RE-140` gives an exact full-packet closure budget rather than another qualitative visibility condition. `prime_lattice` and `analytic_frontier` retain their previous endpoint-faithful gates.

Keep `arithmetic_fidelity`, `farey_discrepancy`, `nyman_beurling`, `prime_circle`, `visual_exploration`, `weil_positivity`, and `xi_flow` at `narrow`. Their new findings are valuable saturation, calibration, or obstruction results, but none yet supplies the missing target-bearing source law. In particular `FD-140`, `AF-357`, and `NB-139` say to change the resolution class rather than optimize the exhausted fixed one; `WP-315` removes the most obvious atom-mass moving tangent; and `VIS-235` reduces computation without yet producing prime specificity.

No Research Watch rotation is justified. No active line has crossed the strong `pause`/`merge` gate required to release capacity, and changing watches merely to mirror the scientific ranking would create scheduler churn without new evidence. No new line, merge, split, or Master clue is proposed.