# Mathia Research State

Canonical evidence synthesized through `5d8703d31995d595b5626479f295d83b4097088c`.

## Global picture

Mathia has canonical evidence in all thirteen current research lines. The portfolio remains **3 `continue` and 10 `narrow`**: `analytic_frontier`, `prime_flute`, and `weil_inertia` remain `continue`; the other ten remain `narrow`. No line currently meets the evidence bar for merge, pause, split, replacement, or a new standalone line. The strongest change in this pass is not a portfolio rotation but a tightening of the first unsupported implication in all three `continue` lines: Analytic Frontier now has a positive moving-window square-function witness for endpoint completion, Prime Flute has refuted its former normalized-source locality target and exposed a new outer reservoir-amplification gate, and Weil Inertia has a finite-radius finite-prime zero-mode reduction that offers a second source-side route beyond the Bombieri sparse-tail handoff.

Prime Flute has undergone the sharpest correction. `PF-278` proves that the full normalized Robin source `B_w=(I+R^0_{s,r})^{-1}X_w` itself has only critical fixed-low-row high-input decay, so the previously proposed `q>1` normalized-source locality statement is false. That route should be regarded as closed rather than sharpened. `PF-279` then moves the obstruction downstream: a scalar matched control in the variable-corridor/outer Schur-complement stage exhibits coherent reservoir amplification across many cells even when single-cell leakage is tame. The live gate is now endgame-adjacent: estimate the extension/reservoir Gram form in the actual operator-valued Prime Flute geometry, or prove that the physical `P/H` structure suppresses the scalar amplification. Prime Flute remains `continue` because this is a concrete discriminator for the real weak-`S_1` assembly, but another matched amplification in the physical geometry without compensating structure would materially weaken the route.

Analytic Frontier has acquired a more positive source-side reduction. `ANF-170` reformulates macroscopic endpoint completion as a forced **moving-window square-function witness** for the actual demodulated prime-minus-Ramanujan residual at the distinguished logarithmic twist. This replaces abstract low-spectrum or endpoint-charge language by an explicit positive quantity whose lower bound is forced if the completion is bow-sized. The next theorem is correspondingly sharp: obtain an upper bound below that forced level from genuinely source-specific short-interval, large-sieve, or Type-I/II information while preserving the distinguished twist and the prime/rough cancellation. `ANF-166` still warns that arbitrary fixed-log gains are insufficient, so the new witness is useful only if it enables a true power-scale improvement. Fatal non-excisability of the surviving same-profile cascade remains a separate downstream obligation.

Weil Inertia keeps its `continue` status but now has two complementary source-side frontiers. `WI-232`--`WI-235` already make same-window screening expensive in zero count, radial depth, and spacing surplus, and the existing Bombieri handoff asks whether a finite negative direction survives an infinite sparse remote tail. `WI-236`--`WI-237` show that this coefficient-tightness route is not logically forced. `WI-238` supplies a different exact reduction: at every finite Suzuki-style radius the relevant zero-mode question is encoded by an operator assembled from only finitely many prime/power contributions. The actionable question is therefore whether one can prove a finite-prime monotonicity, coercivity, or nondegeneracy statement that persists with the radius and is genuinely cheaper than the global RH-equivalent criterion. The Bombieri clue remains useful as an alternative sparse-defect route rather than the unique continuation mechanism.

There is no open `.review.md` sidecar in the current tree. All thirteen current research lines retain one enabled Research Watch; no scheduler rotation or duplication repair is warranted. The Riemann Atlas remains outside portfolio decisions because its derived surfaces have not been established as synchronized with the current canonical corpus.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-170` turns bow-sized endpoint completion into a positive moving-window square-function witness for the real demodulated prime-minus-Ramanujan residual. | Beat the forced witness lower bound using source-specific short-interval/large-sieve/Type-I/II input while preserving the distinguished twist and prime/rough cancellation. A merely polylogarithmic gain is already known to be insufficient. |
| `arithmetic_fidelity` | `narrow` | `AF-258` computes the periodic/profinite cancellation exponent for the complete sampling obstruction, sharpening exactly when output sampling can hide the Li growth rate. | Prove that the actual growing zeta phase vector avoids the exceptional cancellation locus with adequate conditioning and source-access cost. Classification of generic sampling pathologies alone is not a source theorem. |
| `farey_discrepancy` | `narrow` | `FD-039` supplies a hyperbola/generic-source countermodel showing that broad regularity, Lipschitz, and energy controls still do not force the required projective angular separation. | Use the exact Möbius/Farey increment law, growing prime breadth, or another genuinely multiplicative constraint to force the angular defect and convert it into target-scale contraction. |
| `mobius_cancellation` | `narrow` | `MC-208` adds a square-defect/level-doubling barrier: further decomposition can isolate the residual parity carrier but still pushes the hard information into a structured sparse tail. | Obtain a parity-sensitive correlation theorem strong enough on the induced polynomially sparse shifts, or find a source identity that avoids near-full level. More decomposition without new parity input merely relocates the difficulty. |
| `nyman_beurling` | `narrow` | `NB-038` identifies an explicit source direction in the conditioned skeleton with pairing `d_N^2+O(d_N/N)`, so the Burnol-scale defect is genuinely present in the compressed source data. | Turn that explicit direction into an independently controlled return inequality. At present the relevant source coordinate is oracle-like: exploiting it can already require essentially the target quantity `d_N^2`, so the compression has not yet made the theorem cheaper. |
| `prime_circle` | `narrow` | `PC-254` factors the collision pseudodeterminant into local path-run contributions, closing another fixed-base nonlinear/operator invariant as a new arithmetic carrier. | Make the state or arithmetic coupling itself grow before scalarization. More collision algebra, nonlinear readout, or auxiliaries on a fixed finite base continue to classicalize. |
| `prime_flute` | `continue` | `PF-278` refutes the former supercritical normalized-source locality target; `PF-279` then exposes coherent variable-corridor reservoir amplification in a scalar outer matched control. | Test the actual operator-valued extension/reservoir Gram form and physical `P/H` geometry. Prove that the true geometry suppresses the scalar amplification, or accept that the outer weak-`S_1` route has a genuine accumulation obstruction. |
| `prime_lattice` | `narrow` | The recent finite-core/cofinal analysis separates exact finite observability from quantitative stability; global arithmetic structure can be present before the scalar zeta readout while finite defects remain detectable. | Prove a shape-preserving lower Riesz/frame or singular-value bound on the growing zeta-relevant test space, or construct a source-faithful countermodel showing cofinal ill-conditioning. |
| `robin_extremal` | `narrow` | Event/collision analysis has reduced finite-packet and bounded-envelope bad events to extremely sparse sets, but source-compatible finite packets can still realize the dangerous cone/ray. | Use the actual CA selector, infinite spectrum, or a growing-packet relation to rule out the surviving events. Generic finite spectral geometry and coarse spacing are no longer discriminants. |
| `visual_exploration` | `narrow` | `VIS-153` classifies the critical finite-dimensional/equal-step Gram lag-product law, closing another apparent order-one residual as deterministic clock/CUE-compatible structure. | Design a source-side statistic that provably escapes the full Gram-clock/CUE null, likely through growing dimension, non-equal-step geometry, or an independently anchored arithmetic coordinate, before inspecting new confirmation data. |
| `weil_inertia` | `continue` | `WI-238` gives a finite-radius Suzuki-style zero-mode reduction to finitely many prime/power contributions, complementing the existing `WI-232`--`WI-235` local-screen ledger and Bombieri sparse-tail handoff. | Prove a finite-prime monotonicity/coercivity/nondegeneracy statement that persists with radius without becoming the global RH criterion in disguise; alternatively close the infinite sparse-tail route through the existing Bombieri clue. |
| `weil_positivity` | `narrow` | Existing finite--archimedean and scattering constructions can preserve substantial global structure, but scalarization or bounded-order marginals still fail to force the exact all-prime critical coupling and sign. | Construct a canonical pre-scalar vector/operator incidence that retains all-prime coefficients, the archimedean term, and a forced sign simultaneously. The Maaß--Selberg remainder-sign clue remains the appropriate bounded handoff. |
| `xi_flow` | `narrow` | Positive, analytic, completely monotone and finite-time/all-local observation constraints remain reproducible by matched controls; generic source regularity still does not select the theta transition. | Exploit the single square lattice `pi n^2`, its coefficient law, and complex modular/all-time compatibility in a way the matched positive scale mixtures cannot reproduce. |

## Cross-line structure

The strongest common boundary remains **source-faithful information must survive the exact operation consumed by the endgame**. Prime Flute now demonstrates this negatively: good single-cell or upstream leakage statements do not prevent coherent outer reservoir amplification. Analytic Frontier demonstrates the complementary positive form: the right object is not another representation of the bow but a square function of the real prime-minus-surrogate source after the precise demodulation and accumulation used by the endpoint. Weil Inertia similarly moves from relaxed screens toward finite-prime operators or sparse-tail persistence, where the actual zero source rather than support geometry must pay the cost.

A second boundary is **compression can expose the target without making it cheaper**. Nyman--Beurling is now the clearest example: the near-logarithmic skeleton contains an explicit source direction carrying the Burnol-scale defect, but the useful scalar is already essentially `d_N^2`. Arithmetic Fidelity has an exact classification of cancellation exponents but still needs a theorem for the one arithmetic phase vector. Prime Circle and Möbius Cancellation likewise show that increasingly elaborate finite-state or decomposed representations do not help when the source-specific coupling/parity information has merely been moved into a hidden coordinate.

The established Master handoffs remain sufficient. The Bombieri-to-Weil-Inertia clue continues to test persistence of a finite negative direction against an infinite sparse off-line tail; `WI-238` is a distinct internal source-side route and does not require a new cross-line clue. The Maaß--Selberg-to-Weil-Positivity clue remains targeted at the sign of the remainder after global positive scattering. Prime Flute's former normalized-source leakage target is now refuted by `PF-278`; the new reservoir-amplification gate is already local to that research line, so duplicating it as a Master clue would add no information.

## Highest-leverage unresolved questions

1. **Prime Flute reservoir suppression.** Determine whether the actual operator-valued variable-corridor extension and `P/H` geometry suppress the coherent reservoir amplification exposed by `PF-279`, or whether the scalar matched control survives into the weak-`S_1` endgame.
2. **Analytic Frontier square-function upper bound.** Use the exact arithmetic source to upper-bound the `ANF-170` moving-window witness below the level forced by bow-sized endpoint completion, with a true power gain and without discarding prime/rough cancellation.
3. **Weil Inertia finite-radius source theorem.** On the `WI-238` finite-prime operator family, prove a radius-persistent nonzero-mode/coercivity statement that is strictly cheaper than the global criterion; in parallel retain the Bombieri sparse-tail persistence test as the alternative route.
4. **Nyman return inequality without an oracle.** Exploit the explicit `NB-038` source direction using independently available arithmetic data rather than the already-encoded `d_N^2`, and only then ask whether the conditioned skeleton improves the Nyman distance estimate.
5. **Source-specific escape from generic controls.** Farey must use exact Möbius/Farey increments rather than generic energy regularity; Möbius needs a parity-sensitive sparse-correlation theorem; Visual needs a statistic outside the classified Gram/CUE lag law; Arithmetic Fidelity needs deterministic zeta-phase conditioning rather than generic exceptional-set estimates.

## Portfolio changes proposed

No recommendation changes. `analytic_frontier`, `prime_flute`, and `weil_inertia` retain short, falsifiable implication chains adjacent to their endgames and remain `continue`. Prime Flute's previous local route has been decisively closed, but `PF-279` immediately supplies a more physical outer discriminator; downgrading before testing that geometry would discard the most informative next experiment. The ten other lines remain `narrow`: Nyman--Beurling in particular has achieved an unusually strong compression, but `NB-038` shows why this is not yet a cheaper theorem rather than merely an explicit encoding of the target defect.

No merge, split, replacement, new-line, or Research Watch rotation is proposed, and no new Master clue is needed in this pass. The correct action is to keep the portfolio stable while requiring the next evidence to attack the newly exposed source-faithful gates rather than reopen representation classes already closed by matched controls.