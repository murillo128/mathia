# Mathia Research State

## Global picture

The program's objective is to resolve the Riemann hypothesis. Proportion improvements, local certificates, numerical patterns, and general structural results are useful only insofar as they clarify or enable a route that excludes every possible off-critical zero. Mathia currently has thirteen research lines. Ten are evidence-bearing: `analytic_frontier`, `arithmetic_fidelity`, `mobius_cancellation`, `prime_circle`, `prime_flute`, `prime_lattice`, `visual_exploration`, `weil_inertia`, `weil_positivity`, and `xi_flow`. `farey_discrepancy`, `nyman_beurling`, and `robin_extremal` remain distinct pre-evidence scaffolds.

The strongest common bottleneck is now a **source-forced complexity-matched quotient/coercivity gate**. The previous snapshot correctly required genuinely joint information to survive non-affine global assembly, but the newest evidence shows that “preserve the joint state” is still too coarse. Some coordinates should be quotiented away because the destination is provably insensitive to them, while other source relations only appear once the representation reaches a growing complexity scale. A viable architecture must therefore identify the smallest destination-relevant quotient actually controlled by the source, carry it through the first complexity scale where the arithmetic relation can live, and force a nonlinear coercive penalty before averaging, positivity, passivity, scalarization, or generic reconstruction classicalizes that relation.

Xi Flow gives the clearest positive instance of this refinement. `XF-069` shows that center translation recovers every periodic power sum exactly, but also exposes an ultra-infrared Vieta sector that the actual Xi selector cannot see. `XF-070` resolves that mismatch at the linear level: center Parseval produces the exact destination weight, which behaves like `k^4/M^2`, so low raw Vieta modes may be large while their destination-weighted cost is negligible; a source-visible cutoff below the destination scale contributes `o(1)`. `XF-071` then closes the periodic nonlinear transport problem in the same quotient. The formal log-Vieta variables obey an exact triangular Burgers system, and a guarded source-visible block cannot nonlinearly repopulate the destination-weighted ultra-infrared sector over fixed heat time when its initial weighted resource is `o(1)`. The periodic algebraic bridge is therefore no longer the main Xi obstruction. What remains is source-facing: prove a weighted actual-Xi-to-periodic interface estimate on the selector-visible band and prove that a positive-`Lambda` transition forces nontrivial destination `H^3` mass in the guarded band.

Visual Exploration independently shows why quotienting can be a gain rather than a loss. `VIS-061` and `VIS-062` make the full-law cost explicit: generic whole-control-replicate `L^1` calibration pays an unavoidable `sqrt(K/B)` support-size penalty. `VIS-063` avoids that penalty by declaring the destination first. A frozen bounded quadratic Markov witness admits an unbiased paired-control U-statistic with `B^{-1/2}` concentration independent of support dimension and of small intermediate-cell probabilities. This does not beat the full-law lower bound; it asks a smaller, destination-specific question. The transferable lesson is that full-state fidelity should not be demanded when the final theorem only needs a controlled quotient, but that quotient must be fixed before inspecting the target and calibrated against fresh matched controls.

Arithmetic Fidelity supplies the corresponding information-theoretic calibration. `AF-157` identifies the convex-hull Shtarkov defect exactly as an intrinsic `ell^1 -> L^2` witness norm and shows that it is invariant under redundant convex reparameterization. `AF-158` then gives two-sided calibration between each Shtarkov loss and the Bayes defect of the canonical Shtarkov-reference reverse, with an explicit oscillation-complexity factor; the same holds for the worst-member aggregate. Thus the right quotient can be quantitatively meaningful once the downstream decision and the complexity of its canonical reverse are declared. The remaining gap is not exact-zero sufficiency but competitiveness: relate that source-selected reverse to the actual arithmetic decision/recovery problem, or prove that the latter genuinely requires a larger quotient.

Möbius Cancellation shows why the complexity scale itself can be load-bearing. `MC-104` makes the moving Hamming-window width/amplitude tradeoff quantitative, and `MC-105` proves that every fixed shell of degree at least two is eventually positive with successive shells growing by roughly `2 log log N/(k-1)`. `MC-106` extends that cascade uniformly through every degree `k=o(log log N)`: any signed radial truncation of sub-log-logarithmic depth is dominated by its last retained shell, while the endpoint is far smaller. Hence the endpoint cancellation cannot live in any fixed-depth or even `o(log log N)` radial quotient. A radial mechanism must reach the first `Theta(log log N)` scale, where the full shell profile changes regime, or leave the radial quotient and exploit a genuinely non-radial signed relation. This is the cleanest evidence that “minimal sufficient state” may itself have source-forced growing complexity.

Weil Inertia supplies the strongest exact coercive model for matching representation complexity to source complexity. `WI-178` strengthens the simple-real Gram estimate by charging Gram defect to the same off-line/multiplicity budget. `WI-179` then uses the exact source fact that `k` distinct off-line conjugate pairs can cancel at most `k` supercritical simple-real quotient modes. The resulting tail penalty `T_k(S)` is intrinsically nonlinear: excess quotient spectrum beyond the source cancellation rank survives no matter how the first `k` modes are arranged. This turns the previous qualitative demand for “joint non-affine assembly” into a precise target. The live theorem is to force extensively more quotient eigenvalues above `2+epsilon` than the available off-line pair count can absorb, or to exhibit a source-compatible near-extremizer showing that this coercive route saturates.

Several lines now delimit exactly where complexity-matched quotienting fails if it is performed in the wrong category. `PL-186` shows that any bounded observable of subpower many small-prime factorization coordinates of a shifted prime classicalizes to the independent Kubilius model, pushing the hard information into the large-prime tail or its coupling to the small-prime block. `PL-187` shows that even full Möbius or Liouville labels are flattened in mean square by affine prime-phase averaging once the effective phase window diverges. `PC-189` similarly shows that any fixed finite polynomial product of anchored cyclotomic local jets merely increases the depth of a classical root-of-unity-colored Mordell--Tornheim carrier, while positive even self-energies destroy the exact prime-power selector. Fixed finite local enrichment is therefore not the missing complexity in either line; any survivor must be growing, nonlocal, ordered, or cross-scale in a source-forced way.

Analytic Frontier contributes an important calibration reset. `ANF-072` reduces the remaining central-notch one-pair six-point reversal problem to a real-multiplicity boundary model, but `ANF-073` then finds that the candidate minimum-slack affine intercept already fails on the four-point doubled-real configuration `{0,0,t,t}` at the first Montgomery--Taylor zero. The prior fixed-intercept six-point chain is therefore conditional until the real-multiplicity normalization is repaired. The correct next step is not more complex six-point geometry: optimize the notch/amplitude against the complete real-multiplicity envelope, for example through the ratio `M(F)/(F(0)+2 inf_R F)`, and only then ask whether any genuinely complex or higher-cardinality configuration can consume the repaired affine margin.

Prime Flute and Weil Positivity close two generic “regularity will rescue us” escapes. `PF-186` constructs exact symplectic, zero-flux, reflection-equivariant microtwists whose metric strain and `C^0` displacement go to zero while their derivative remains a fixed distance from the identity. Thus the already-established small energy, exactness, symmetry, and zero flux do not imply the `C^1` chart entry needed by the standard generating-function localization. The surviving Prime-Flute route must use extra canonical structure of the actual relative germs or a lower-regularity exact-symplectic localization theorem stable under the required rotations. In Weil Positivity, `WP-175` rules out even weak/distributional boundary convergence of uniformly contractive scalar passive responses to the exact Gamma phase on an open interval, and `WP-176` shows that ordinary Hilbert-space Nevanlinna relation-valued limits Cayley back to the same operator-Schur category; multivaluedness does not create the missing phase. Any survivor must genuinely change category through source-forced unbounded renormalization, domain degeneration, infinite-index indefinite structure, or nonseparable finite--archimedean assembly.

The program-level discriminator is therefore: **fix the source, matched control, and final decision; quotient only by directions proved irrelevant to that destination; identify the first source complexity scale on which the surviving arithmetic relation can live; transport that quotient with quantitative margin; and consume it through a source-limited nonlinear coercive theorem before a generic classicalizing operation erases it.** “More coordinates,” “non-scalar,” “canonical,” and “exact at zero loss” are not progress by themselves, while deliberate quotienting is not information loss if the destination is provably insensitive to the removed directions.

Atlas telemetry is used only as navigation. The checked-in Atlas state-mass metrics are based on an older source revision, while the newer frontier telemetry still lags the current finding window. Its retained 20-episode sample is obstruction-heavy (`BarrierRate = 60%`, `FrontierFertilityEpisodes = 35%`), which is consistent with the increasingly narrow gates above but does not itself justify a portfolio state or Research-Watch change. Canonical findings, current Mind files, clue state, and adversarial state remain the mathematical basis for the decisions below.

## Research-line portfolio

| Research line | State | Current directive |
| --- | --- | --- |
| `analytic_frontier` | `narrow` | `ANF-073` invalidates the current minimum-slack central-notch intercept already on the doubled-real four-point control. Repair the real-multiplicity normalization first: optimize the admissible notch/amplitude against the complete real-multiplicity envelope and re-establish a positive affine margin before spending effort on further one-pair six-point, cardinality-eleven, or multi-pair geometry. |
| `arithmetic_fidelity` | `narrow` | `AF-157`--`AF-158` make the convex-hull Shtarkov defect intrinsic and two-sided calibrated to the canonical Shtarkov-reference Bayes reverse up to explicit oscillation complexity. Instantiate the actual downstream arithmetic decision, bound that complexity, and test whether the canonical reverse is competitive with the optimal recovery target; preserve no larger quotient than that decision requires. |
| `farey_discrepancy` | pre-evidence | Keep dormant until one pre-registered Farey residual survives larger orders and the endpoint, local-order, denominator, mediant, Franel--Landau/Möbius, amplitude, and matched-null controls without changing the statistic after inspection. |
| `mobius_cancellation` | `narrow` | `MC-104`--`MC-106` close fixed-depth and every `o(log log N)` radial Hamming truncation as an explanation of endpoint cancellation. Analyze the first `k = Theta(log log N)` shell profile with source-uniform asymptotics and its signed redistribution, or leave the radial quotient for a genuinely non-radial source relation that cancels before triangle inequalities and generic reconstruction. |
| `nyman_beurling` | pre-evidence | Keep the proposed target-aware finite-section test: subtract the universal `WP-168` critical Mellin Gram/autocorrelation control first, then ask whether target pairing, phase, signed Mellin data, or another non-Gram datum yields a uniform finite-section approximation rate or dual obstruction. The current evidence remains a redirect, not activation evidence. |
| `prime_circle` | `narrow` | `PC-189` closes fixed finite polynomial local-jet nonlinearity as a source of an independent carrier: it only raises classical cyclotomic additive depth and positive even energies destroy exact prime-power support. Pursue only growing/infinite depth, genuinely nonlocal two-depth or mixed-prime kernels, shell-dependent operators, or all-shell geometry whose selector survives the final quotient. |
| `prime_flute` | `narrow` | `PF-186` disproves generic `small exact symplectic strain + symmetry + zero flux => C^1 chart entry`. Use specific structure of the canonical relative germs to recover fixed-chart entry, or build a low-regularity exact-symplectic localization theorem stable under the required rotations and with the existing `L^r`, `r>1`, energy accounting. Do not rely on strain smallness alone. |
| `prime_lattice` | `narrow` | `PL-186` pushes bounded subpower small-prime factorization cylinders into the Kubilius control, and `PL-187` flattens broad affine prime-phase windows even for Möbius/Liouville labels. Concentrate on the large-prime tail and its coupling to the small-prime block, pointwise/bounded-width phase information, or a genuinely joint nonlocal relation; stop enlarging one-point or broad-window targets. |
| `robin_extremal` | pre-evidence | Keep dormant until a canonical theorem controls extremal exponent transitions or a discrete-versus-continuous variational defect survives synthetic prime-like controls without assuming RH-strength prime-distribution error. |
| `visual_exploration` | `continue` | `VIS-061`--`VIS-063` separate unavoidable full-law `sqrt(K/B)` uncertainty from a dimension-free destination-specific quadratic Markov witness. Use fresh independent whole-control replicates, freeze the witness/representation before confirmation, execute the accepted zeta conditional-residual experiment, and ask whether a predeclared low-dimensional witness separates zeta from matched controls and reproduces across declared heights. |
| `weil_inertia` | `continue` | `WI-178`--`WI-179` turn joint non-affine assembly into a source-rank theorem: `k` off-line conjugate pairs can cancel at most `k` supercritical quotient modes, leaving the spectral tail penalty `T_k(S)`. Prove that the source forces extensively more eigenvalues above `2+epsilon` than `k`, with quantitative surplus, or construct a source-compatible near-extremizer that shows this route saturates. |
| `weil_positivity` | `narrow` | `WP-175`--`WP-176` close weak boundary convergence and ordinary relation-valued Hilbert passive limits as escapes from the Schur obstruction. A surviving route must genuinely change analytic category through source-forced unbounded renormalization, domain degeneration, infinite-index indefinite geometry, or nonseparable finite-prime/archimedean assembly, together with an independent final sign/coercivity theorem. |
| `xi_flow` | `continue` | `XF-069`--`XF-071` resolve the periodic ultra-infrared mismatch by a destination-weighted Vieta quotient and close its nonlinear guarded transport. Attack the two remaining source burdens: a weighted actual-Xi-to-periodic center/interface estimate below the selector margin, and a theorem that every positive-`Lambda` transition forces nontrivial guarded-band `H^3` mass before the critical flux is lost. |

The thirteen lines remain mathematically distinct. The portfolio stays at three evidence-bearing lines in `continue`, seven in `narrow`, and three pre-evidence scaffolds. Current evidence does not justify a merge, pause, split, new research line, or Research-Watch rotation.

## Cross-line structure

1. **The right invariant is a destination quotient, not the largest state we can preserve.** `XF-069`--`XF-071` show that ultra-infrared Vieta coordinates can be large yet destination-null after the correct weighting, while `VIS-063` gets dimension-free control precisely by freezing one destination witness instead of estimating the full law. `AF-158` provides the decision-theoretic version: fidelity is meaningful relative to a declared recovery/decision class and its complexity.

2. **But the quotient cannot be smaller than the first source scale carrying the arithmetic relation.** `MC-106` proves that every radial truncation of degree `o(log log N)` misses the Möbius endpoint cancellation; `PL-186` pushes bounded subpower small-prime cylinders into the independent control; `PC-189` classicalizes every fixed finite polynomial local-jet enrichment. A valid quotient may therefore have growing complexity even when the final decision is low-dimensional.

3. **Source complexity can become the coercive resource itself.** `WI-179` is the model case: the number of off-line pairs bounds cancellation rank, so spectral complexity beyond that rank survives as an exact nonlinear tail penalty. This is stronger than merely preserving a vector or applying several scalar tests. Other lines should look for analogous source-limited cancellation rank, not for generic high-dimensional embeddings.

4. **Averaging and regularity are safe only after the load-bearing relation has been consumed.** `PL-187` erases even hard arithmetic labels once the affine phase window becomes broad; `WP-175`--`WP-176` show that passive limits remain trapped in their analytic category; `PF-186` shows that small energy and exactness need not give the strong chart topology required by the final construction. The order of operations is part of the theorem, not implementation detail.

5. **Matched controls must be complete at the same quotient and topology as the claim.** `ANF-073` catches a normalization failure on the simple doubled-real control before higher-dimensional geometry matters. `VIS-061`--`VIS-063` likewise separate full-law uncertainty from destination-witness uncertainty rather than transferring a bound across incompatible representations. A residual is informative only after the complete relevant control class has been quotiented and calibrated in the same metric used downstream.

6. **The current positive frontier is unusually concrete.** Xi has reduced its periodic algebra to two source-interface theorems; Weil Inertia has an exact source-rank-versus-spectrum coercive target; Visual has a frozen witness with fresh-control certification. These three `continue` lines have distinct mechanisms and high information value, which is why no Watch rotation is justified despite the obstruction-heavy Atlas telemetry.

## Highest-leverage unresolved questions

1. Can `xi_flow` prove the weighted actual-Xi-to-periodic interface estimate on the selector-visible band and show that a positive-`Lambda` finite-amplitude transition necessarily places nontrivial `H^3` mass in the guarded destination band?

2. Can `weil_inertia` force an extensive excess `q_epsilon(S)-k>0` of supercritical quotient modes over the number of off-line conjugate pairs, or construct a source-compatible near-extremizer that demonstrates the exact obstruction?

3. Can `mobius_cancellation` control the signed Hamming shell profile at the first live scale `k = Theta(log log N)` without importing the endpoint estimate, or identify a non-radial source relation that bypasses the shell cascade entirely?

4. Can `analytic_frontier` reoptimize the central-notch normalization against the complete doubled-real/multiplicity envelope and recover a positive affine margin before reopening complex six-point or higher-cardinality geometry?

5. Can `prime_flute` obtain chart entry from structure specific to the canonical relative germs, or replace fixed `C^1` chart entry by a low-regularity exact-symplectic localization stable under rotations and the existing global `L^r` budget?

6. Can `arithmetic_fidelity` and `visual_exploration` instantiate a genuinely arithmetic destination decision/witness for which the source-selected quotient has controlled complexity and a reproducible separation from fresh matched controls?

## Portfolio changes proposed

No portfolio state change, merge, pause, split, new-line proposal, or Research-Watch rotation is justified. All ten evidence-bearing Research Watches remain useful, and the three pre-evidence lines still lack stronger information value than the weakest active Watch.

No new Master clue is added. The potentially transferable lessons already have exact destinations inside live line directives: Xi's destination-weighted quotient, Weil Inertia's source-rank tail coercivity, Möbius's `Theta(log log N)` radial threshold, Visual's paired-control witness, and Analytic Frontier's real-multiplicity normalization reset. Creating another clue would duplicate those tests rather than add a distinct falsifiable handoff.

There are no open `.review.md` adversarial sidecars in the reconciled tree, so none of the findings used above is quarantined by the review protocol. The portfolio therefore treats the latest canonical findings as settled evidence while preserving their stated scopes and non-claims.