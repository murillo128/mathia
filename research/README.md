# Mathia Research State

## Global picture

This snapshot is reconciled against `main` through `da0e2a5f258de44db009c06ffda83d9e6fbb6c04`. Mathia has canonical evidence in all thirteen research lines. The portfolio remains **3 `continue` and 10 `narrow`**: `analytic_frontier`, `prime_flute`, and `weil_inertia` remain `continue`; the other ten remain `narrow`. No line currently meets the evidence bar for merge, pause, split, replacement, or a new standalone line, and no Research Watch rotation is justified.

The strongest program-level update is again a sharpening of what information must survive the representation actually used at the endgame. Analytic Frontier now shows that even a slowly growing Hardy--Littlewood local hierarchy can be reproduced without buying the missing bow cancellation. Prime Flute has a favorable free separated Jacobi window but an exactly classified zero-edge deformation in the actual chain, so generic trace-class transfer is insufficient. Weil Inertia has pushed the fixed-band screen tariff to positive-density complexity and then shown that frequency separation alone cannot turn that tariff into localization when coefficients are relaxed. Nyman--Beurling and Robin Extremal have both acquired substantially stronger source-selected constraints, but each still lacks the return/exclusion theorem that would make the route program-level credible rather than merely precise.

The `MC-176` adversarial sidecar has now been removed after the persisted nearest-dyadic repair was accepted, so that finding is no longer unresolved on that issue. The latest adversarial checkpoint commit on the branch declares `Adversarial-Reviewed-Through: fa3d274e109a3725696f206ba1c53f5f506572da`; the newer findings synthesized below are canonical current evidence but should not be described as independently checkpoint-covered beyond that prefix. The Atlas remains unsuitable for portfolio decisions because its entrypoint and metrics surfaces declare different, substantially older source revisions.

## Focused continuations

### Analytic Frontier — `continue`

The positive-slack cascade still has a distinct global completion question: whether its late cloud can participate non-excisably in a genuinely fatal notch configuration. The bow branch is now even more source-specific. `ANF-150` constructs a sieved-Cramer matched control reproducing the finite Ramanujan pair profile while leaving the full bow diagonal, and `ANF-151` extends the obstruction through a slowly growing Hardy--Littlewood local tuple hierarchy. Together with `ANF-148`, this means that correct prime location, natural coefficient scale, exact logarithmic carrier, and increasingly rich local prime statistics still do not force the required moving-window cancellation.

The first unsupported implication is therefore a genuinely centered signed theorem: obtain macroscopic negative twisted covariance for the actual post-sieve prime innovation, including the fixed `Lambda-Lambda^sharp` prime/rough-composite relation, or preserve an equivalent cross-piece cancellation that the matched controls cannot reproduce. A local-marginal hierarchy by itself is now a control, not the missing bow mechanism.

### Prime Flute — `continue`

The fixed-axis problem has become sharply spectral. `PF-248` and `PF-249` show that the constant half-line Jacobi square-root model enjoys an image cancellation and, after any fixed positive separation, retains the full-output weighted smoothing window `R<5/2`; in particular the needed regime `R>1` is not lost merely by summing all output modes. `PF-250` extends the free calibration to fractional edge powers.

`PF-251` then blocks the generic transfer shortcut. The actual parity chain has an exact boundary-selected zero-energy solution growing like `j^(3/2)` and an independent subordinate tail like `j^(-1/2)`, so the `O(j^-2)` coefficient perturbation changes the threshold behavior despite being trace class. The first unsupported implication is now to determine the actual low-energy spectral density/Green kernel, or prove a replacement weighted functional-calculus estimate, that preserves **some** separated gain `R>1` for the normalized Robin source map. Only after that fixed-axis theorem should the route spend margin on the `T_a` replacement, hypercycle-coordinate defect, `P/H`, finite-pant coupling, neighboring cells, and global weak-`S_1` reassembly.

### Weil Inertia — `continue`

`WI-218` closes the previous logarithmic rank gap: stacking a logarithmic family of multiplicative slabs forces positive-density, order-`M` screen complexity to flatten the bow across a fixed exponent band. `WI-219` shows that this order is sharp in the relaxed interface by an explicit two-phase `Theta(M)` companion, but that co-localized construction is rejected by the local zero-count budget.

`WI-220` prevents a support-only localization shortcut. On every proper fixed band, arbitrarily remote Fourier tails remain complete, so with free complex coefficients and unrestricted finite channel count they can approximate the bow as well as required even after the exact Gallagher box. The first unsupported implication must therefore retain actual zero structure: prove a quantitative remote-approximation lower bound charging term count, source-normalized coefficient norm, positive/integral multiplicity, functional-equation coupling, radial depth, or an equivalent covariance/source norm strongly enough to contradict the available zero population. Frequency separation alone has no coercive content in the relaxed screen class.

## Narrow lines

**Arithmetic Fidelity.** `AF-228` prices deterministic prime-tail localization, `AF-229` gives the exact zero-error stochastic information split through the perfect unit-interval proximity graph, and `AF-230` closes the remaining nonzero-error randomization escape at first order: Shannon rate-distortion has the same normalized phase `(1-delta) min(beta,1)` as deterministic marking. Stochasticizing the encoder therefore does not reduce the leading repair cost. A survivor must change a load-bearing ingredient such as the distortion notion, source law, retained geometry, ambiguity structure, or information objective, and ultimately supply a source-forced relational code rather than another generic localization scheme.

**Farey Discrepancy.** `FD-010` shows that absolute/star discrepancy of the complete Farey sequence has an exact endpoint barrier `1/n`, so the obvious `L^infinity` route is not RH-sensitive. `FD-011` classicalizes the raw nearest- and fixed-lag Pluecker increment fields as Farey-index/continuant algebra. A surviving route has to retain growing-depth multiplicative compatibility or genuinely nonlocal ordered Farey information before cumulative collapse.

**Möbius Cancellation.** The repaired `MC-176` now gives a settled critical-shell consequence: the Gallagher target would force Good--Churchhouse-scale square-root variance on a canonically selected shell. `MC-177` sharpens the warning by showing that uniform fixed-power improvements of the relevant local variance already transfer directly to nontrivial global power bounds for Mertens; the full Gallagher target is even stronger. The residual question is whether actual Möbius structure yields a cheaper signed short-interval theorem than these target-hard variance statements. More diagonal-energy reformulations do not create an intermediate problem for free.

**Nyman--Beurling.** `NB-010` shows that the parity normal equation forces early semigroup defect energy, and `NB-011` uses a prime ensemble of exact normal equations whose centered divisibility contrasts are harmonically almost orthogonal. For the actual best residual this yields `E_K(r_N) >> d_N^4 log(1/d_N)` at polynomial depth and, after inserting the known Burnol floor only to express section size, `E_K(r_N) >> log log N/(log N)^2` by `K=O((log N)^2)`. This is a substantial source-selected improvement over lcm-scale observability, but it is still a lower-bound amplification. The first unsupported implication is a residual-specific **return inequality** or normed dual certificate converting that forced prime-divisibility energy into a stronger bound on `d_N`; further lower bounds alone do not improve the approximation rate.

**Prime Circle.** `PC-233` shows that fixed-base Cauchy packets freeze bounded regular-polygon multipliers, and `PC-234` identifies the prime-base packet normalization itself with binary cyclotomic chord data. The packet representation is therefore increasingly classicalized. A survivor must retain complete multi-prime amplitudes and introduce a source-forced non-translation-invariant old/new operator or simultaneous-growth effect that does not reduce to cyclotomic refinement.

**Prime Lattice.** The reciprocity family did expose a genuine zeta coupling, but the newer residue analysis keeps collapsing it. `PL-243` shows that the remaining Blomer polar faces scalarize, while `PL-244` makes the higher-rank residue a separable zeta channel rather than a mixed-prime invariant. The route now needs an invariant, nonseparable Riemann channel together with a sign/self-adjointness/positivity mechanism; another multivariable continuation or residue factorization is not enough.

**Robin Extremal.** `RE-010` turns a clean self-tangent Robin counterexample peak into a sharp selected mixed-race value threshold. `RE-011` adds genuinely new source-selection information: at the same CA-selected coordinate the normalized mixed race must obey a simultaneous asymptotic value--slope law, rather than merely attain a large value. This is more discriminating than a global Mertens/prime-race bound, but it is not yet an exclusion theorem. The live residual question is whether clean CA self-tangent selection can be shown to avoid the required high-value branch while satisfying the forced slope law infinitely often, and separately whether hypothetical failures can concentrate in the sparse higher-layer/tied chamber, using input genuinely weaker than Robin/RH.

**Visual Exploration.** `VIS-122` canonically closed the frozen four-triad half-arc panel as negative. `VIS-123` now supplies the continuous CUE/Montgomery support-edge ramp and its logarithmic-ripple null calibration, and the accepted support-edge clue specifies the next arithmetic-residual test. This remains `narrow` because the current result is a null model, not a zeta-specific signal; any next positive claim must survive the frozen matched continuous-CUE control on fresh source material.

**Weil Positivity.** The source-forced Bost--Connes critical half-density remains mathematically real, but ordinary completion mechanisms continue to fail. `WP-228` places the required global critical completion beyond ordinary Dixmier class, and `WP-229` shows that standard Dixmier-style amplification of the linear critical amplitudes either loses the selector/sign or produces the wrong weights. The residual route is genuinely singular/non-trace-class or distributional and must still supply finite--archimedean Gamma/polar data plus an independent Weil sign theorem; ordinary finite-trace or standard singular-trace repairs are controls.

**Xi Flow.** `XF-146` and `XF-147` push transition blindness through full-future real scans, including root-spacing apertures and maximal-contact tuning: matched simple/colliding packets can remain superexponentially close in the observed histories. This strengthens the earlier distinction between exact observability and stable transition recovery. A viable Xi bridge must exclude those packets from the actual source, use observation geometry outside their blind family, or identify a stable transition-specific quotient that avoids full-state tomography.

## Research-line portfolio

| Research line | Recommendation | Strongest current boundary | First unsupported implication |
| --- | --- | --- | --- |
| `analytic_frontier` | **continue** | `ANF-150/151` reproduce finite and slowly growing local Hardy--Littlewood/Ramanujan structure while retaining the bow diagonal. | Prove actual centered post-sieve signed anti-covariance, or an equivalent source-coupled cancellation, and connect it to fatal non-excisability. |
| `arithmetic_fidelity` | **narrow** | `AF-230` closes stochastic nonzero-error localization at the same first-order rate-distortion phase as deterministic marking. | Change a load-bearing information geometry and retain a source-forced relational discriminator with usable conditioning. |
| `farey_discrepancy` | **narrow** | `FD-010/011` close absolute discrepancy and raw finite-lag Pluecker data as endpoint/classical index barriers. | Use growing-depth multiplicative compatibility or ordered information surviving cumulative collapse. |
| `mobius_cancellation` | **narrow** | `MC-176/177` show the diagonal Gallagher target already contains hard short-interval variance and strong Mertens power consequences. | Find a genuinely cheaper signed source theorem, not another equivalent diagonal-energy target. |
| `nyman_beurling` | **narrow** | `NB-011` forces prime-normal-equation defect energy by polylogarithmic depth. | Convert forced energy back into a stronger approximation-distance bound through a residual-specific upper/dual certificate. |
| `prime_circle` | **narrow** | `PC-233/234` classicalize fixed/prime-base packet multipliers and normalization into regular-polygon/cyclotomic chord data. | Preserve full multi-prime amplitudes under a source-forced non-translation-invariant operation. |
| `prime_flute` | **continue** | `PF-249/250` leave a favorable free separated window, while `PF-251` proves the actual zero edge is inverse-square critical and not generically trace-class transferable. | Prove an actual-chain low-energy/weighted separated estimate with some `R>1`, then complete geometric recoupling and weak-`S_1` reassembly. |
| `prime_lattice` | **narrow** | `PL-243/244` scalarize the remaining polar faces and separate the higher-rank zeta residue channel. | Isolate an invariant nonseparable Riemann channel with a sign or positivity mechanism. |
| `robin_extremal` | **narrow** | `RE-011` forces a joint value--slope law at clean self-tangent counterexample coordinates. | Exclude the selected high-value/slope event and exceptional-chamber concentration with weaker-than-RH input. |
| `visual_exploration` | **narrow** | `VIS-123` calibrates the continuous CUE/Montgomery support-edge null after the first frozen panel failed. | Test a preregistered zeta-specific support-edge residual against the exact matched null on fresh data. |
| `weil_inertia` | **continue** | `WI-218/219` make the fixed-band tariff order-`M` and sharp; `WI-220` kills support-only remote localization in the free-coefficient class. | Price remote screening with actual zero count/coefficients/multiplicity/radial structure strongly enough to force a contradiction. |
| `weil_positivity` | **narrow** | `WP-228/229` push the critical completion beyond ordinary Dixmier class and standard singular-trace amplification. | Build a source-forced singular finite--archimedean completion that retains the linear critical weights and proves the Weil sign. |
| `xi_flow` | **narrow** | `XF-146/147` keep full-future root-spacing and maximal-contact scans superexponentially transition-blind. | Produce a stable source-restricted transition observable outside the matched blind family. |

## Cross-line routing and portfolio discipline

No new Master clue is created in this pass. Prime Flute already has the accepted `CLUE-critical-jacobi-square-root-decay-window`, and the owner has strengthened it with `PF-248/249`; `PF-251` now identifies the exact next variable-chain obstruction. Visual Exploration already has the accepted Montgomery support-edge residual clue. The remaining similarities across Analytic Frontier, Weil Inertia, Nyman--Beurling, Robin Extremal, and Xi Flow are methodological -- source-selected information must survive a lossy endgame operation -- but there is not yet a common mathematical dictionary strong enough to justify another handoff.

All thirteen line-specific Research Watches remain enabled, so the observed concurrency budget is unchanged. No active line is a strong `pause-candidate` or `merge-candidate`, and there is no dormant initialized line whose evidence justifies displacing one of them. The task portfolio therefore remains unchanged.