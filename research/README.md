# Mathia Research State

## Global picture

Mathia has canonical evidence in all thirteen research lines. The portfolio remains **3 `continue` and 10 `narrow`**. The `continue` lines are `analytic_frontier`, `prime_flute`, and `weil_inertia`; the other ten remain `narrow`. No line currently meets the bar for merge, pause, split, replacement, or a new standalone line, and the current evidence does not justify rotating any Research Watch.

The strongest program-level update is not a common RH mechanism. Several lines have instead learned exactly what their current representation forgets. Analytic Frontier shows that bow support, coefficient size, realness, the exact logarithmic carrier, and even the actual prime locus do not determine the missing moving-window coherence. Prime Flute has reduced its noncommuting seam problem to a critical parity-Jacobi spectral-conversion problem rather than an arbitrary mixer. Weil Inertia now prices the rank needed to screen a fixed multiplicative Gallagher slab at `M/log T`. Xi Flow again separates exact observability from stable transition recovery: an arbitrarily small moving real probe is injective, but its conditioning already deteriorates with inverse excursion and the earlier exponential transition blindness remains.

There is one material open review. `MC-176` has an adjacent adversarial sidecar whose latest speaker is `Owner`; the adversary found an endpoint shell-selection gap and the owner supplied a nearest-dyadic repair. Until the adversary accepts or rejects that repair, `MC-176` is unsettled. The previous `WI-214` review is closed: the repaired conjugation-symmetric screen is now part of the settled current line state.

## Focused continuations

### Analytic Frontier — `continue`

The line has two distinct live frontiers. The same-profile positive-slack cascade has survived the known depth, span, slack-spend, coefficient-spread, and fixed zero-pinching objections; its remaining question is global and physical: whether the completed late cloud is non-excisable in a genuinely fatal notch configuration rather than removable source mass.

The bow branch has narrowed even more sharply. `ANF-145` identifies a full missing factor `K` between current componentwise type-II control and the diagonal bow variance. `ANF-146` shows that support and coefficient magnitudes cannot supply it, `ANF-147` keeps real coefficients and the exact logarithmic twist and still permits the full coherence ambiguity, and `ANF-148` pushes the matched control onto a positive fraction of the **actual prime sites** at the natural `log X` scale while keeping the variance `o` of its diagonal. The first unsupported implication is therefore source-specific: use the fixed amplitude/sign law of `Lambda-Lambda^sharp`, including its prime-versus-rough-composite relation, or preserve cross-piece cancellation strongly enough to forbid adaptive block balancing. More support-only mean-value or stable-rank arguments are now controls rather than progress toward the bow.

### Prime Flute — `continue`

`PF-244` remains the decisive abstract countermodel: positivity, one-sided seam domination, energy contraction, and spatial separation can coexist with arbitrary high-to-low leakage before corridor propagation. `PF-246` gives the opposite calibration: exact commutation with the transverse operator yields separated Robin-Poisson smoothing of every order.

`PF-247` materially reduces the gap between those two extremes. In the corridor Gegenbauer basis, the fixed axis compactification generator is exactly parity tridiagonal, and the relative operator is a compact perturbation of two constant half-line Jacobi matrices with essential spectrum `[0,1]`. The loss of coercivity is therefore critical and mode-local, not arbitrary nonlocal mixing. The first unsupported implication is now a quantitative functional-calculus/microlocal theorem for the **actual normalized seam**: prove enough decay along the critical Jacobi mode chain, survive the `T_a` replacement and width-dependent coordinate defect, and obtain one separated gain `R>1` for `PF-243`. The new proposed clue `CLUE-critical-jacobi-square-root-decay-window` gives a useful kill-first calibration: the limiting square-root edge has a `k^-2` tail and leaves a toy window `1<R<3/2`, but that is not yet a full-output Robin-Poisson estimate. If this spectral-conversion gate closes, the route returns to `P/H`, finite-pant coupling, neighboring-cell transport, and global weak-`S_1` reassembly.

### Weil Inertia — `continue`

The earlier finite-packet no-go has now become a quantitative complexity law. `WI-214`, after its repaired conjugation-symmetric construction, shows that polylogarithmically growing screens can flatten the first two continuum slabs. `WI-215` proves that a fixed-size screen cannot flatten a natural Gallagher slab. `WI-216` then shows that on a slab widened to the screen scale, subdiagonal residue forces screen rank to grow faster than `log T`.

`WI-217` is the stronger current boundary. On a fixed multiplicative source window, leaving any fixed fractional deficit in the available translation rank produces a logarithmically superdiagonal residue; a subdiagonal selected residue therefore requires `n(T) >= (1-o(1))q(T)` for `q` up to `cM/log T`, hence screen complexity `Omega(M/log T)`. This is real progress from finite/polylog screening, but it still permits a zero-density exceptional packet because `(M/log T)/M -> 0`. The first unsupported implication is to convert source arithmetic, radial/count cost, a larger independent observation family, or the complete bow covariance into the missing additional factor needed to rule out such zero-density screens. Merely adding another fixed set of harmonics or slabs cannot do it.

## Narrow lines

**Arithmetic Fidelity.** `AF-225/226` turn the marked-translation escape into an exact coding law. At source radius `r_Q`, the minimum proximity-mark count among primes up to `Q` has asymptotic fraction `1-e^{-c}` when `r_Q log Q -> c`; a sublinear mark alphabet is possible only if the resolvable source radius is `o(1/log Q)`. The route therefore has to pay explicitly in growing mark complexity, collapsing resolution/conditioning, mark-dependent geometry, or a genuinely source-forced sign relation. More finite labels do not repair the translation quotient for free.

**Farey Discrepancy.** `FD-006` closes every fixed finite Jordan-moment hierarchy at the leading dual constant, `FD-007` shows that exact squarefree support with unit signs still does not improve it, and `FD-008` imposes those restrictions together with the exact fixed-horizon divisor identity and still obtains the same asymptotic `zeta(2)` coefficient. The remaining cumulative route must use growing-depth multiplicative/divisor compatibility or nonlinear constraints on the actual Möbius staircase; alternatively it must retain ordered Farey information before cumulative shell collapse.

**Möbius Cancellation.** `MC-175` gives the power-law phase diagram for the Gallagher/heat route and makes clear that the early useful horizons demand essentially diagonal local variance. `MC-176` attempts to sharpen that into a necessary Good-Churchhouse-scale critical-shell statement: at `tau=1/8`, diagonal Gallagher type would force square-root-length Möbius second-moment behavior on the heat-selected shell. The adversary correctly found that the stated shell condition did not justify the claimed uniform `O(1)` comparison at the endpoint; the owner proposes the canonical nearest-dyadic saddle `log X_beta=1/(4 beta)+O(1)`, which appears to repair exactly that step. Because the dialogue remains open, the Master does not use `MC-176` as settled evidence. Even if accepted, it would show that the proposed intermediate target already contains a classical hard short-interval variance theorem rather than produce a cheaper bridge.

**Nyman–Beurling.** `NB-006/007` show that fixed or summably weighted semigroup target-character probes are not enough and that the full harmonic defect is singular off the target line. `NB-008` then proves an explicit finite witness for the actual best residual, but only by reaching the common period `P_N=lcm(2,...,N)`: the guaranteed index is `exp(N+o(N))` and the quantitative coefficient is exponentially small. This resolves finite observability but not useful coercivity. The live theorem is **sub-lcm localization** forced by the projection normal equations or other target-aware source structure.

**Prime Circle.** `PC-231` closes fixed-base bilinear packet closure, and `PC-232` extends the result through arbitrary finite pointwise/convolution depth at growing squarefree conductor: all nonintegral shifts remain in the classical squarefree cyclotomic torsion subgroup of `Q/Z`. Growing nonlinear depth can raise pole order and amplitudes but cannot create a new shift carrier. A survivor must therefore retain the complete multi-prime packet coefficients and a source-forced non-translation-invariant old/new operator, or use simultaneous growth in a way not reducible to cyclotomic refinement/Bost-Connes dynamics.

**Prime Lattice.** `PL-240` moves beyond generic labels to genuine mixed-prime quadratic reciprocity, and `PL-241` finds the corresponding global analytic completion in classical Weyl-group multiple Dirichlet series. This is a useful positive source-specific coupling: reciprocity family averaging really does produce an explicit zeta correction and multivariable continuation. But the visible zeta factor is not an invariant Riemann-zero divisor: the Weyl action moves its affine argument, its self-dual locus is misaligned with the Riemann `1/2` symmetry, and even its central pole is repackaged away in the continued double series. Future work needs an invariant principal-channel quotient/residue/operator or positivity theorem, not another construction of reciprocity-coupled multiple Dirichlet series.

**Robin Extremal.** `RE-006/007` convert a clean regular self-tangent CA peak into an exact Chebyshev residual selector and expose the square-root layer scale. `RE-008` is the stronger current reduction: combining the selector with the Euler-product height cancels the endpoint `theta(p)-p` term exactly, and any unbounded sequence of clean Robin counterexample peaks must satisfy a positive square-root excursion of the endpoint-subtracted Mertens remainder, with `liminf sqrt(p) log p M_*(p) >= 2 sqrt(2)`. This is a real source-selection theorem, but not yet an exclusion theorem; ordinary Mertens oscillation lives on the same scale, and counterexamples could still concentrate in the exceptional transition chamber. The line remains narrow until either clean excursions or exceptional-chamber concentration are ruled out using input demonstrably cheaper than Robin/RH.

**Visual Exploration.** `VIS-122` now canonically closes the first fully frozen high-zero confirmation panel. The four collision-free triads, exact finite-CUE hard-window calibration, three predeclared Odlyzko windows, statistic, seed, and one-sided rule were fixed before source inspection; the three upper-tail probabilities were `0.65204`, `0.974635`, and `0.59004`, so every window failed the required `p<=0.05` gate. This is a useful negative result precisely because it is not being rescued post hoc. The next visual carrier must have an independent mathematical motivation, its own frozen matched null, and fresh evidence. The latest cross-line Jacobi clue belongs to Prime Flute and does not change Visual's portfolio state.

**Weil Positivity.** `WP-225` closes positive orientation by any normal map covariant with the original Bost-Connes time evolution: a nonzero hollow finite defect cannot become positive without changing that dynamics. `WP-226` shows that zero-frequency self-pairing only squares the critical half-density and loses the required linear Weil amplitude. `WP-227` then prices the most direct opposite-frequency completion: exact critical coefficients can be embedded in positive finite blocks, but positivity forces auxiliary trace at least `sum_{p,k}(log p)^2 p^{-k}`, which diverges globally. The route is not logically dead; it is forced onto a singular/non-trace-class or continuous/distributional finite-archimedean completion that also supplies Gamma/polar sectors and an independent sign theorem. Ordinary finite-trace positive baths are now controls.

**Xi Flow.** `XF-143` shows that two real-axis traces separated by half a mean root spacing identify the full finite periodic polynomial state. `XF-144` sharpens the algebraic boundary: one real probe moving at any nonzero speed already separates every reflected heat-rate pair, even with arbitrarily small total excursion. That does not rehabilitate the route. The inverse conditioning is at least `Omega(L/D)` for scan distance `D`, becomes `Omega(N)` at root-spacing scale, and the earlier central-packet constructions still make transition recovery exponentially unstable. Exact injectivity is therefore not the missing bridge. A viable Xi theorem must exclude those packets from the actual source, use observation geometry outside their blind region, or control a transition-specific quotient without full-state tomography.

## Research-line portfolio

| Research line | Recommendation | Strongest current boundary | First unsupported implication |
| --- | --- | --- | --- |
| `analytic_frontier` | **continue** | `ANF-146..148` show that magnitude/support geometry, exact twist, real coefficients, and even actual prime support do not determine bow coherence. | Use the fixed `Lambda-Lambda^sharp` amplitude/sign law and prime/rough-composite coupling, or prove the completed positive-slack cascade is non-excisable in a fatal notch configuration. |
| `arithmetic_fidelity` | **narrow** | `AF-226` gives the exact `r_Q log Q` phase law for the source-mark complexity needed to resolve the prime tail. | Produce a source-forced relational code with a usable resolution/conditioning budget, or leave the shared translation-difference category. |
| `farey_discrepancy` | **narrow** | `FD-008` keeps the asymptotic `zeta(2)` dual constant even after squarefree support, unit signs, and one exact divisor identity are imposed jointly. | Use growing-depth multiplicative compatibility or ordered pre-cumulative Farey information to beat the classical criterion. |
| `mobius_cancellation` | **narrow** | `MC-176` would force critical-shell Good-Churchhouse-scale variance, but its endpoint shell statement is under an open owner/adversary repair dialogue. | Resolve the review, then determine whether the required signed short-interval variance can be proved with genuinely cheaper input. |
| `nyman_beurling` | **narrow** | `NB-008` localizes a finite residual witness only at `lcm(2,...,N)` scale with exponentially weak quantitative cost. | Prove sub-lcm target-aware localization from the projection equations or construct a matching alias obstruction. |
| `prime_circle` | **narrow** | `PC-232` keeps arbitrary finite packet-algebra depth inside classical squarefree cyclotomic torsion. | Retain full multi-prime amplitudes and a source-forced non-translation-invariant operation that survives cyclotomic/refinement controls. |
| `prime_flute` | **continue** | `PF-247` makes the fixed compactification a critical parity-Jacobi problem; arbitrary high-to-low mixing is no longer the right local model. | Prove enough actual-seam Jacobi functional-calculus/microlocal decay for some `R>1`, then complete `P/H` and weak-`S_1` reassembly. |
| `prime_lattice` | **narrow** | `PL-241` finds a genuine reciprocity-to-zeta family coupling, but classical Weyl completion does not preserve the Riemann zero divisor or `1/2` self-duality. | Isolate an invariant principal Riemann channel with a sign/self-adjointness/positivity mechanism, not just multivariable continuation. |
| `robin_extremal` | **narrow** | `RE-008` forces a `2 sqrt(2)` square-root-scale endpoint-subtracted Mertens excursion at every clean counterexample peak. | Exclude that source-selected excursion and separately exclude concentration on the exceptional transition chamber with weaker-than-RH input. |
| `visual_exploration` | **narrow** | `VIS-122` canonically refutes the frozen four-triad high-zero/CUE confirmation panel on all three predeclared windows. | Motivate a genuinely different carrier before looking at fresh data and freeze its matched-null falsification rule. |
| `weil_inertia` | **continue** | `WI-217` forces `Omega(M/log T)` screen complexity on fixed multiplicative Gallagher slabs. | Upgrade the source/count/covariance cost enough to rule out the remaining zero-density screen family. |
| `weil_positivity` | **narrow** | `WP-227` shows exact critical positive frequency-matching requires infinite auxiliary trace globally. | Build a source-forced singular/non-trace-class finite-archimedean completion with Gamma/polar data and an independent Weil sign theorem. |
| `xi_flow` | **narrow** | `XF-144` gives exact state identification from arbitrarily small real-axis scanning but retains bad conditioning and exponential transition blindness. | Obtain a stable transition-specific/source-restricted observable rather than exact full-state tomography. |

## Cross-line routing and portfolio discipline

No new Master clue is created in this pass. The most actionable new handoff is already present as the proposed Prime-Flute clue `CLUE-critical-jacobi-square-root-decay-window`: it translates `PF-247`'s exact Jacobi edge into a falsifiable exponent budget for the existing `PF-243` smoothing gate. Creating a second Master clue would duplicate that question. Its `k^-2` square-root calibration is not evidence until the Prime Flute Research Watch reconstructs the exact scalar functional factor and full-output norm.

Other cross-line similarities remain methodological rather than theorem-transferable. `NB-008`, `XF-144`, and `WP-227` all exhibit exact algebraic existence at a quantitatively singular boundary, but their costs are respectively lcm-scale localization, transition-conditioning, and infinite auxiliary trace; there is no common estimate to hand off. `ANF-148` and `WI-217` both force source-specific coherence/complexity after generic controls, but their variance spaces and source dictionaries are different. No merge is justified by those analogies.

## Review, Atlas, and execution integrity

The current tree has one open adversarial dialogue: `MC-176.review.md`, latest speaker `Owner`. The canonical `MC-176` claim remains unsettled until the adversary disposes the nearest-dyadic repair. The earlier `WI-214` sidecar has been removed after the conjugation-symmetry correction and is no longer an open caveat.

The latest declared independent adversarial checkpoint encountered in this pass is `Adversarial-Reviewed-Through: a49f7028876edc9a27e7f916736f596ea9fd91ec`. The owner response on `MC-176`, `WP-227`, and the proposed critical-Jacobi clue are later than that declared prefix; they are current repository state, but they are not represented as independently reviewed by that checkpoint.

The Atlas remains stale and is not used for portfolio decisions. `research/graph/atlas/riemann-atlas.md` declares source revision `4bc527342486672ca5ee32157913aea2b8ff2b62`, while `research/graph/atlas/metrics.md` declares `71186d054f8cef479e08c27e71e01635a5288dd0`; the projections disagree with each other and both predate the current evidence substantially. They should remain untouched until the Graph Curator refreshes them from one coherent source revision.

## Portfolio disposition

The portfolio remains **3 `continue` + 10 `narrow`**. Prime Flute has the cleanest constructive local gate because `PF-247` converts an abstract leakage problem into a specific critical-Jacobi decay question. Weil Inertia has the strongest new quantitative source-cost theorem because `WI-217` raises the required screen from polylogarithmic to `M/log T`, though one more logarithmic-density gap still separates it from a contradiction. Analytic Frontier remains worth continuing because its bow controls have now stripped away nearly every generic geometric explanation: the surviving theorem has to use the actual signed prime/rough source law, while the separate cascade branch has a concrete non-excisability test.

The ten narrowed lines continue to produce useful theorems, but their present arrows either land on a quantitatively singular reconstruction (`nyman_beurling`, `xi_flow`, `weil_positivity`), classicalize the surviving carrier (`prime_circle`, `prime_lattice`), price an intermediate estimate at nearly the target difficulty (`mobius_cancellation`), or isolate a source condition without a currently cheaper exclusion theorem (`arithmetic_fidelity`, `farey_discrepancy`, `robin_extremal`, `visual_exploration`). That is enough to keep them active and narrow, but not enough to displace the three current continuations.