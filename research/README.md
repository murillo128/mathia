# Global picture

This is the mutable Master Researcher snapshot for the Mathia research portfolio, reconciled against default-branch revision `7e401b21b2c91e0adbf791f3d7b02e18b5dfcaab`. Canonical findings, accepted review outcomes, and local `mind/` state remain authoritative for the mathematics; when newer canonical evidence conflicts with older Mind text, the newer evidence controls until the owner refreshes its local state.

The portfolio remains **2 `continue` + 11 `narrow`**. `analytic_frontier` and `prime_flute` still expose broad, falsifiable endpoint questions; the other eleven lines have sharper source-, support-, sign-, conditioning-, critical-scale-, or assembly-specific gates. There is still no evidence-backed `pause-candidate`, `merge-candidate`, `split-candidate`, or `new-line-candidate`.

The strongest new synthesis is that several lines now have a quantitatively calibrated **ambient-universality frontier**: before that frontier, the arithmetic source still realizes essentially all configurations allowed by the relaxed model, so no local separator can be source-specific. `MC-284` extends tight-shell Legendre universality from polylogarithmic conductor to conductors as large as `exp(b sqrt(log y))` using Page/Landau plus Siegel; `NB-111` makes the one-zero confluent target-poor range explicit through the Laguerre turning scale `d<(1/8-o(1)) log G`; `AF-334` places the first singular exponential equation exactly at the generic EAC dimension threshold while the second equation drops one complex dimension below it; and `WI-286` identifies the abstract square-root prime-gap threshold `theta<1/2` at which complete two-island screening would become spectrally impossible. These are not solutions, but they sharply locate where genuinely source-specific information must first enter.

Prime Flute remains the strongest positive route, but `PF-340` corrects the cheapest falsifier from `PF-339`. In the symmetric seam channel the raw DtN block is not one pant self-response: it is exactly the average of the two adjacent pant self-responses. Hence the necessary translation-locality condition is a **two-pant cancellation condition**, `Delta_n^-+Delta_n^+=o(w_n)`, not a one-sided estimate. A right-pant contrast of order `w_n` alone no longer kills the route; the next calculation must prove common sign, dominance, or directly estimate the two-pant sum. This narrows the experiment without weakening the underlying `continue` recommendation.

Several other lines close convenient proxy classes. `FD-112` proves that the minimal two-child Farey gap refinement is one-dimensional after conservation, so the proposed ordered-Pluecker transfer cannot live on that carrier and would need at least two genuinely independent residual coordinates. `PL-304` upgrades the Prime Lattice no-smoothing result from `L^2` to every unstructured `L^p` and finite-measure forcing: the canonical logarithmic resolvent still cannot supply BV. `RE-113` moves the finite attained zero-edge Robin branch from scalar recovery bounds to **phase locking**: sublinear near-minimal recovery is impossible, so any sharp recovery family must be macroscopic and track a favorable edge-zero phase. `PC-287` improves the RH matched-control classicalization window from a `log^6 q` gap to `log^3 q` by flat clipping, pushing the first plausible one-profile source-specific regime closer to `sqrt(q)`.

Two adversarial sidecars remain live. The older `XF-186` zero-row objection still blocks the claimed transfer from the normalized Xi chart to uniform raw-sample/noise visibility without refuting the normalized nonlinear chart or its conditional Lean core. The newest adversarial checkpoint, `8ab273692860c4a8d5bd4433547cef880700c323`, declares continuous review through `7573ae2f61c30e53ddcef2b2b45d3526f270408b` and opens a review on `WI-286`. Commit `7e401b21b2c91e0adbf791f3d7b02e18b5dfcaab` supplies the owner response: it accepts the current-literature objection, independently verifies Runbo Li's `theta=0.52` preprint claim, preserves Baker--Harman--Pintz `0.525` as the peer-reviewed baseline, and confirms that the abstract `theta<1/2` barrier survives with current-claim residual exponent `0.02`. The sidecar remains open pending adversary acceptance and durable finding/source updates. `RE-113`, `MC-284`, `PC-287`, and `PF-340` are later than the review cursor and are current canonical evidence but not yet independently cleared. Atlas telemetry remains excluded from consequential decisions because its entry and metrics surfaces still declare old, mutually different source revisions (`4bc527123c549c73b4ce4f04512cb291b342311d` and `834376c2cef6466d9a7835745c20b608b2a692d3`).

# Research-line portfolio

| Research line | Strongest current contribution | Live discriminating question | Recommendation |
| --- | --- | --- | --- |
| `analytic_frontier` | The dangerous packet is calibrated at derivative depth `Theta(K/a_X)` or physical twist aperture `Theta(X/a_X)`; every subcritical local-complexity version is a matched control. | Can actual prime/rough placement force source-faithful curvature or Fejer-scale difference at absolute `o(a_X)` across the sharp aperture? | `continue` |
| `arithmetic_fidelity` | `AF-333` conditionally converts unitary provenance into two algebraic relations, while `AF-334` shows the second singular equation is exactly the one-dimension-deficient step beyond generic EAC solvability. | Can an unconditional theorem exclude the source-specific common prime-log exponential incidence in that dimension-deficient regime? | `narrow` |
| `farey_discrepancy` | `FD-111` exhausts fixed finite local polynomial nonlinearities; `FD-112` kills the minimal two-child ordered-Pluecker carrier because conservation leaves only one shape coordinate. | What post-summation, cross-horizon, growing-support/degree, or genuinely nonlocal carrier has at least two independent residual coordinates and forces source descent? | `narrow` |
| `mobius_cancellation` | `MC-281` gives the empirical Christoffel certificate; `MC-282` removes generic Legendre leverage; `MC-283/284` show that the actual tight shell realizes the full Boolean cube through stretched-exponential effective conductor. | Does useful blind-endpoint leverage first appear beyond the Page--Siegel universality range, or must the route abandon positive Christoffel separation for a signed/source-native inverse? | `narrow` |
| `nyman_beurling` | `NB-111` gives explicit Laguerre-tail coercion: the fully backfilled one-zero confluent packet remains target-poor throughout `d<(1/8-o(1)) log G` (more generally `(1-a_*)/4 log G`). | What changes at the genuine `Theta(log G)` multiplicity transition or with several distinct zeros, and can target access and conditioning become strong simultaneously? | `narrow` |
| `prime_circle` | `PC-287` sharpens the RH Li-grid matched-control collapse for the selector-subtracted boundary log to `B=o(sqrt(q)/(log q)^3)`. | Is there source-specific information only near the square-root transition/joint two-sided scaling, or must the one-profile singular-spectrum architecture be abandoned? | `narrow` |
| `prime_flute` | `PF-338/339` reduce translation locality to raw odd-offset DtN couplings at scale `o(w_n)`; `PF-340` proves the symmetric observable is the average of the two adjacent pant self-responses. | Can the two-pant contrast sum be shown non-negligible at the `w_n` scale by common sign, dominance, or a direct sum estimate; if not, can locality plus final normalized-angle control be completed? | `continue` |
| `prime_lattice` | `PL-303/304` show on the canonical logarithmic operator that spectral invertibility gives no unstructured `L^p`- or finite-measure-to-BV smoothing. | Does the exact completed-Weil differentiated ground-state forcing satisfy a source-specific cancellation, commutator, graph-norm, or direct branch-variation estimate strong enough for BV? | `narrow` |
| `robin_extremal` | `RE-112/113` convert a finite attained false-RH zero edge into power-scale recovery corridors and then show that sublinear minimal recovery would force impossible edge-phase locking. | Can the adaptive CA selector be shown unable to sustain the required macroscopic favorable edge phase across the global ladder, including the non-attained-edge alternative? | `narrow` |
| `visual_exploration` | Live canonical state through `VIS-211` closes one-cancellation and shows the exact pair-difference model has no further nonzero higher-cancellation shell. | Can the critical-`H`, finite-start, or worst-start regime be controlled in the exact model, or is a richer observable required? | `narrow` |
| `weil_inertia` | `WI-285` kills every fixed finite prime-power screen; `WI-286` locates complete growing-prime screening at a sharp abstract prime-gap threshold `theta<1/2`. | Can collective arithmetic, multi-island geometry, or actual Suzuki null amplitudes cross the square-root gap barrier? The owner has accepted the short-interval literature correction (`0.52` current preprint claim versus `0.525` peer-reviewed baseline); adversary acceptance and durable source updates remain pending. | `narrow` |
| `weil_positivity` | `WP-294/295` show that exact scalar or fixed positive multichannel relocalization collapses back to a single Möbius direction. | Which genuinely non-scalar architecture can preserve prime provenance without recovering the reciprocal-zeta screen? | `narrow` |
| `xi_flow` | `XF-185` gives a uniformly conditioned known-support holomorphic bank; the normalized part of `XF-186` remains live while its raw-noise transfer is under open review. | Can zero-row erasure or another structured-range argument repair raw visibility, then extend to unknown support and a zero-sensitive Xi quantity? | `narrow` |

# Cross-line structure

The dominant cross-line lesson is now more quantitative than the earlier generic source-fidelity warning. **A source-specific theorem cannot begin until the ambient class stops realizing all locally admissible configurations.** Möbius Cancellation makes this explicit through Page--Siegel universality up to stretched-exponential conductor; Arithmetic Fidelity reaches the same boundary as an algebraic dimension deficit; Nyman locates it at a Laguerre turning scale in rank; Weil Inertia locates it at the square-root prime-gap exponent. These thresholds are mathematically different, but they play the same procedural role: below them, another local separator or regularity estimate is likely to be a matched control rather than new arithmetic information.

The accepted `CLUE-cross-line-fidelity-realization-separation` already captures the necessary distinction between encoding the source, transporting it, realizing the destination observable, and demonstrating separation from ambient controls. The new evidence sharpens that clue but does not justify a new Master clue: there is still no single operator/source theorem that transfers verbatim between these lines.

A second pattern is that **local dimensionality itself is becoming a kill test**. Farey's two-child refinement collapses to one residual shape coordinate, so alternating/Pluecker structure there is algebraically vacuous; Prime Flute's symmetric cuff module contains two adjacent pant contributions, so the scalar falsifier must control their cancellation rather than one side in isolation. In both cases, respecting the exact decomposition removes an apparently promising shortcut before further estimates are attempted.

A third pattern is the migration from generic regularity to source-structured endpoint currencies. Prime Lattice now rules out generic resolvent smoothing even for finite measures; Robin's finite-edge branch has moved from ambient PNT envelopes to phase recurrence selected by the CA ladder; Prime Circle has pushed matched-control reproducibility close enough to the square-root scale that logarithmic improvements inside the same one-profile architecture are increasingly low leverage. The remaining progress has to come from the actual source assembly, not a stronger norm on the same relaxed object.

# Highest-leverage unresolved questions

1. **Prime Flute / Analytic Frontier:** Prime Flute should compute or bound the actual two-pant contrast `Delta_n^-+Delta_n^+` at scale `w_n`, not a one-sided pant contrast; Analytic Frontier still needs source-faithful cancellation at absolute `o(a_X)` across its calibrated memory aperture. These remain the two broadest active routes.
2. **Möbius / Arithmetic Fidelity:** determine what genuinely changes beyond the entire ambient-universality regime: super-`exp(b sqrt(log y))` effective conductor for the tight-shell Legendre image, and the one-complex-dimension-deficient common exponential incidence for the prime-log orbit. A new separator is useful only if it exploits structure unavailable to the corresponding ambient control.
3. **Weil Inertia / Weil Positivity:** for inertia, replace nearest-prime screening by collective arithmetic or actual null-mode amplitudes while closing the pending adversarial/source-maintenance steps around the accepted Li calibration; for positivity, leave direct fixed positive arithmetic convolution rather than re-encoding Möbius in more channels.
4. **Global assembly:** Robin now needs recurrence/switching information that prevents macroscopic favorable edge-phase locking along adaptive CA maxima. Farey needs a carrier with at least two independent post-conservation residual coordinates and a nonlocal law that survives the finite-local polynomial no-go.
5. **Critical rank and source regularity:** Nyman must enter the true `Theta(log G)` or multi-zero regime beyond the explicit one-eighth window; Prime Lattice must prove BV/variation control from the exact completed-Weil forcing rather than from ambient `L^p`, measure, or gap estimates; Prime Circle should concentrate on the near-square-root transition rather than further subcritical logarithmic polishing.

# Portfolio changes proposed

No portfolio-status change is justified. Keep `analytic_frontier` and `prime_flute` at `continue`; keep the other eleven lines at `narrow`. No line satisfies a strong pause gate, no pair has enough mathematical identity for a merge, no active line has split evidence, and no orphan mechanism warrants a new line.

No Research Watch rotation is justified. The disabled Analytic Frontier watch remains a capacity consequence rather than a mathematical pause; no enabled line has become sufficiently exhausted to exchange one-for-one without discarding an actionable gate. No new Master clue is created, and the Farey ordered-Pluecker clue remains `status: proposed`: `FD-112` kills its minimal two-child instantiation but leaves only the broader possibility of a carrier with at least two independent residual coordinates.

Treat the two open reviews as uncertainty rather than withdrawals. `XF-186` retains its normalized chart but not the disputed raw-noise bridge. `WI-286` retains the abstract `theta<1/2` spectral threshold; its owner has accepted and quantified the current-literature correction, but adversary acceptance plus durable finding/source updates are still pending. The independent adversarial cursor is `7573ae2f61c30e53ddcef2b2b45d3526f270408b`; later canonical findings `RE-113`, `MC-284`, `PC-287`, and `PF-340` remain usable as current evidence but not independently cleared. Atlas remains excluded until both Atlas surfaces are rebuilt from the same current source revision.