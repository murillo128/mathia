# Mathia Research State

Canonical evidence synthesized through `e68b74b87cc446bd3aec978f7e35b0bf07006cd2`. The latest independent adversarial checkpoint is `f3176e76d8c92ddd0d09ae7f67c439a3952f1467`, with `Adversarial-Reviewed-Through: f018ca27d38c587456c6cc5a2d116d0f4e343801`. `WP-277`, `MC-255`, and `VIS-198` are later substantive evidence and are used as current canonical findings without being treated as adversarially cleared. No `.review.md` sidecar is open in the current tree.

## Global picture

Portfolio recommendations remain unchanged: `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other research line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

The strongest update is that several apparent scarcity or generic-structure bottlenecks have now been removed, leaving **composition geometry and source-specific rigidity** as the recurring destination-facing gates. `FD-083` eliminates the cardinality explanation in Farey: constant-strength physical occupation has full polynomial counting exponent, even inside every fixed-power future window, yet those many horizons need not form the multiplicative/integer-ratio geometry consumed by the existing accumulation argument. `NB-082` gives the exact healthy-control calibration for Nyman: after depth `L`, the stationary continuation charge is governed by the finite-prediction excess `a_(psi,L)`, and bounded screening requires the quantitative scale `R a_(psi,L)=O(1)`, not merely qualitative outerness. `RE-085` similarly shows that on long stretched Robin fans every bounded fixed-order statistic of the normalized mixed event measure collapses to the endpoint-determined first layer; any surviving higher-layer influence must act through a singular state-selection channel such as adjacency, chamber splitting, ties, or exposed-state changes.

Weil Inertia sharpens this distinction most strongly. `WI-265` keeps the complete endpoint-screening geometry of `WI-264` and embeds it as the exact terminal zero mode of a positive rank-one-defect operator whose every proper centered and translated shorter-window compression stays strictly positive. Generic zero-modehood, nested positivity, translated-window coercivity, and the abstract sliding-localization reserve therefore still do not break the screening model. The live discriminator is now explicitly **Suzuki-specific**: the actual equality between localization defect and the von-Mangoldt/archimedean autocorrelation functional, or the detailed arithmetic null equation, must forbid the endpoint gap. The accepted line clue has been updated to this source-specific test, so no duplicate Master clue is needed.

Several other lines now have equally sharp categorical boundaries. `AF-305` proves that joint sparsity and coefficient caps remain useless throughout the entire strict-slack regime `k alpha>1`; only exact saturation `k alpha=1` changes the source from a continuous reachable body to discrete uniform `k`-subsets. `MC-255` closes the natural higher-class-group repair after `MC-254`: genuine prime-discriminant controls can have arbitrarily large shell cross-block nullity while `r_4` and every higher `2^j` class-group rank vanish, and the blind directions do not even enter the classical `R_8` domain. The remaining Möbius invariant must see the localized bipartite block itself or the route must bypass rank with signed rough-coset cancellation.

Visual Exploration produces the clearest positive closure in this source window. `VIS-198` uses a dimension-three upper-bound sieve on the affine gap fibers of `VIS-196`--`VIS-197` and proves `D_v=o(y^2)` throughout every polynomially growing strictly subcritical corridor `H>=y^epsilon`, `H log y/y ->0`. Local congruence inflation and the aggregate weighted fiber count are therefore no longer obstructions in that regime. What remains is the subpolynomial-start corridor, the critical-scale transition, optimality/necessity of the current dephasing resource, or a different observable.

Weil Positivity strengthens its provenance no-go beyond the trace-class boundary. `WP-277` observes that Mathia's present logarithmic arithmetic Hamiltonian has compact resolvent. If the auxiliary sector also has compact resolvent, then every bounded self-adjoint coupling—and more generally every symmetric coupling with relative bound strictly below one—preserves compact resolvent. The absolutely continuous pole continuum therefore cannot be manufactured by ordinary bounded or subcritical relatively bounded mixing of discrete sectors. A viable source-first architecture must contain a noncompact/absolutely-continuous sector already at zeroth order, or cross a genuinely supercritical domain-changing/infinite-volume boundary before the Weil sign is read.

Prime Flute remains the main constructive operator route. `PF-315`--`PF-316` reduce the physical endpoint to the two genuine channels `T_H` and `Z_LH`; the residual map cannot hide a bad conditional angle without paying through the direct channel. The accepted direct-angle clue now narrows the `Z_LH` test further: subtract the exact translation-invariant local DtN model in the same energy normalization and prove or refute a truncation-independent weak-`S_1` count for the degenerating remainder. Fixed-domain pseudodifferential smoothing is explicitly not enough.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make the matched endpoint packet quantitatively sharp: derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` are equivalent discrimination boundaries. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-305` gives the exact joint cap/support boundary: strict slack `k alpha>1` still permits recurrent same-fiber zero/safe collisions, while `k alpha=1` collapses the source to discrete uniform `k`-subsets. | Test the saturated discrete family or another genuinely arithmetic/discrete source law. The next restriction must change the reachable failure set categorically, not merely tighten continuous caps or sparsity. |
| `farey_discrepancy` | `narrow` | `FD-083` removes the cardinality obstruction: constant-strength occupied horizons have full counting exponent globally and inside every fixed-power future window. | Convert the dense-but-unstructured occupied set into a compatible integer-ratio/multiplicative chain, a transport inequality across many horizons, or another accumulation law that the RH-facing GCD/Schur mechanism can actually compose. |
| `mobius_cancellation` | `narrow` | `MC-252`--`MC-255` show that separated-prime universality, bounded-order shell pseudorandomness, maximal full Rédei rank, and even the entire higher `2`-class rank tower can all miss a large lower/shell cross-block kernel. | Control the moving-shell bipartite block directly, constrain the complementary shell block on `ker A`, derive a genuinely localized higher residue/Kummer invariant, or bypass rank with fixed-power signed cancellation of the exact rough coset. |
| `nyman_beurling` | `narrow` | `NB-082` identifies the exact stationary screening currency: the depth-`L` charge is `asymp R a_(psi,L)`, so outerness only gives eventual decay while bounded screening requires finite-prediction error `O(1/R)`. | Derive the analogous finite-prediction rate for the actual nonstationary Nyman innovations at an affordable future depth, or find a stable-tail certificate that does not pay this boundary layer. |
| `prime_circle` | `narrow` | `PC-274` proves the raw ordered near-resonance law is prime-blind at every bandwidth, while `PC-275` identifies the first centered fixed-bandwidth correction as the classical logarithmic-integral source bias. | Move to a target-sensitive residual: normalized moving/microscopic bandwidth, Li-centered higher correction, singular/directional data, transfer across levels, or another quantity matched controls cannot reproduce. |
| `prime_flute` | `continue` | `PF-315`--`PF-316` give the exact physical two-channel factorization and endpoint visibility: compactness/weak trace of the full angle is equivalent to that of both `T_H` and `Z_LH`. | Prove singular-value counts for `T_H` and `Z_LH`. For the direct channel, test the accepted DtN-remainder clue with uniform constants through shrinking collars; then address infinite realization and signed reassembly separately. |
| `prime_lattice` | `narrow` | `PL-289` gives the actual Weil eigenstate a squared-log Fourier moment and an `O(log^-2(1/|delta|))` translation modulus, removing the generic inverse-log regularity barrier. | Reduce the state-specific constant enough to meet the tiny certified positivity margin, exploit an eigen-equation/source cancellation, or replace asymptotic transport by a direct interval certificate. |
| `robin_extremal` | `narrow` | `RE-085` proves total-variation collapse of the normalized mixed event measure to its endpoint-determined first layer on long stretched fans; every bounded fixed-order mass statistic is asymptotically first-layer. | Exploit a singular cross-layer mechanism—adjacency, exceptional-event conditioning, chamber/supporting-slope changes, ties, exponent-state admissibility, or rightmost-maximizer selection—that can remain order one despite vanishing higher-layer mass. |
| `visual_exploration` | `narrow` | `VIS-198` closes weighted near-collision crowding for every polynomially growing strictly subcritical observation scale by summing a three-form sieve over the affine prime-gap fibers, yielding `D_v=o(y^2)`. | Push the result into subpolynomial `H`, understand the critical `H~y/log y` transition or necessity of `D_v`, or move to a different source-sensitive observable if the remaining regime is not destination-relevant. |
| `weil_inertia` | `continue` | `WI-264`--`WI-265` show that all finite prime lags can be screened and that the same screening survives an exact abstract terminal zero mode together with strict positivity on every smaller centered/translated window. | Use the actual Suzuki source identity or arithmetic null equation to exclude the entire screened endpoint family. Another generic first-crossing/compression argument is already defeated. |
| `weil_positivity` | `narrow` | `WP-275`--`WP-277` progress from pure-point/AC incompatibility to a compact-resolvent provenance no-go: bounded and relative-bound-`<1` mixing of compact-resolvent sectors cannot manufacture the pole continuum. | Derive an intrinsic noncompact/AC archimedean sector independently at zeroth order, or justify a canonical supercritical/domain-changing/infinite-volume transition; only then prove the arithmetic coupling and Weil sign. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**Abundance and complexity are no longer credible substitutes for composability.** `FD-083` proves that Farey has almost the maximal possible number of constant-strength occupied horizons on the polynomial counting scale, yet the endpoint still needs a composition law across those horizons. `AF-305` shows that several quantitative source restrictions can remain jointly ineffective until exact saturation changes the category of the admissible fibers. `MC-255` shows that even an entire hierarchy of global class-group ranks can be trivial while the destination-visible cross-block remains highly blind. The shared lesson is not that these lines have one common theorem; it is that a large source statistic matters only when it is organized in the same geometry as the target map.

**Benign completion has to be calibrated, not merely named.** `NB-082` turns “future screening” into the exact finite-prediction rate `a_(psi,L)`. `PF-316` turns residual attenuation into a paid direct-angle defect, eliminating a hidden third regularizer. `WI-265` goes further negatively: even exact zero-modehood and all shorter-window positivity can be manufactured around the screened vector. These three results separate a quantitative healthy-control calibration from genuine source-specific rigidity.

**Regular observables can erase structurally decisive rare information.** `RE-085` proves that the complete mixed Robin event measure is asymptotically first-layer for every bounded fixed-order mass statistic, while explicitly leaving discontinuous state selection open. `WI-264`--`WI-265` provide the autocorrelation analogue: a broad family of ordinary positivity/firstness summaries is compatible with a source-screening gap. A future proof may therefore need a singular selector or an exact arithmetic identity rather than a stronger regular moment.

**Spectral category is now a causal obligation, not post-processing.** `WP-277` strengthens `WP-275`--`WP-276`: for the current arithmetic Hamiltonian, merely leaving trace class does not create the pole continuum if the zeroth-order sectors still have compact resolvent and the coupling is subcritical relatively bounded. `PL-289` gives a milder version of the same discipline: the correct eigenstate category improves regularity, but the destination still consumes a much smaller quantitative margin. Representation choice and target margin remain independent resources.

**Visual has crossed its previous aggregate sieve gate in a substantial regime.** `VIS-198` supplies the coefficient-weighted global summation that `VIS-197` left open whenever `H` is polynomially large and strictly subcritical. This is not evidence for the final RH bridge; it removes one deterministic-start dephasing obstruction and cleanly identifies the remaining observation-scale boundary.

The existing proposed `CLUE-global-cross-line-fidelity-realization-separation` already covers the program-level separation between source fidelity, target transport, destination realization, and assembly. The new findings sharpen its examples but still do not supply one common source family, operator, normalization, and target from which an exact transfer theorem follows. No additional Master clue is warranted, and the accepted Prime-Flute and Weil-Inertia clues already route the two concrete source-local tests exposed in this pass.

The Graph-owned Riemann Atlas remains stale for portfolio decisions: its entrypoint and metrics advertise different old source revisions (`4bc527...` and `834376...`), both far behind current canonical evidence. Atlas telemetry is navigation context only and supports no recommendation here.

## Highest-leverage unresolved questions

1. **Prime Flute endpoint singular counts.** Derive weak-`S_1`/compactness counting laws for both `T_H` and `Z_LH`. The accepted direct-angle clue gives a concrete `Z_LH` test: isolate the exact translation-invariant local DtN model and track the normalized remainder uniformly through the shrinking-collar exhaustion. If the endpoint passes, treat infinite realization and signed reassembly as separate gates.
2. **Weil Inertia source-specific first-crossing rigidity.** Starting from the certified `a_*>0.8`, test the actual family `F_v(r)>=r lambda_r` on the complete screened endpoint-bump class or use Suzuki's explicit null equation to force autocorrelation mass inside the gap. The result must distinguish the true arithmetic operator from the `WI-265` rank-one control.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet after the sharp `Theta(X/a_X)` aperture, with positive-cascade non-excisability kept as a distinct final gate.
4. **Farey compatible accumulation.** Cardinality is no longer the issue. Extract multiplicative/integer-ratio structure from the `X^(1-o(1))` occupied set or prove a nonlocal transport law that composes gains across unstructured occupied horizons.
5. **Nyman arithmetic prediction rate.** Define and estimate the nonstationary analogue of `a_(psi,L)` for the genuine Nyman innovations. A useful theorem must reach the `1/R` prediction scale at a future depth affordable in the arithmetic horizon, or avoid that payment altogether.
6. **Möbius localized bipartite control.** Classical higher class-group ranks are now an explicit matched control, not an escape. Prove a moving-shell invariant that sees the lower/shell block itself, control the complementary block on its kernel, or obtain fixed-power signed cancellation of the complete rough coset.
7. **Weil Positivity continuum provenance.** Derive the required noncompact/AC archimedean sector independently of the target carrier, or justify a canonical supercritical/domain-changing/infinite-volume mechanism that destroys compact resolvent. Only then does the older coupling/sign problem become meaningful.

## Portfolio changes proposed

No recommendation change is proposed. Prime Flute remains `continue` because its endpoint has been reduced to two exact singular-value problems and an accepted clue now supplies a direct falsifiable test for one of them. Weil Inertia remains `continue` because the generic route has been aggressively falsified while a sharply different source-specific Suzuki test survives. Analytic Frontier remains `continue` because its sharp source-continuation gate is unchanged and distinct from both operator routes.

The strongest movements among `narrow` lines are real but do not yet supply the missing target bridge. Farey has crossed its abundance/anti-concentration barrier but still lacks composability; Visual has crossed the polynomial subcritical weighted-sieve barrier but not the zero-sensitive bridge; Nyman has an exact matched-control calibration but not the arithmetic rate; Robin has isolated singular state geometry but not a contradiction; Möbius and Weil Positivity have closed broader classes of proxy repairs. No active line is therefore a strong `pause-candidate` or `merge-candidate`, and no Research Watch allocation change is justified. The current Watch concurrency is left unchanged.