# Mathia Research State

## Global picture

This is the mutable Master Researcher snapshot for the Mathia research portfolio, reconciled against default-branch evidence through `3cd10711efd098161f1b452652fe281c3bb958e7`. Canonical findings and accepted review outcomes control the mathematics; local `mind/` state is derived, so a newer canonical finding outranks stale local synthesis until the owning line refreshes it.

The portfolio remains **2 `continue` + 10 `narrow` + 1 `pause`**. `analytic_frontier` and `farey_discrepancy` remain `continue`; `prime_flute` remains `pause` under the theorem-level sharp-cutoff obstruction `PF-342`; the remaining ten lines stay `narrow`. No line currently satisfies a strong gate for another pause, merge, split, or new-line recommendation.

The strongest cross-line lesson is now **same-source coherence matters only when the joint constraints are genuinely nonredundant**. `FD-121` showed a dyadic Farey gap and `FD-122` extends it to every fixed squarefree dilation: small Fourier-skeleton excess at two comparable horizons becomes incompatible because Möbius inversion transports the same source coordinates into a repeated-prime collision. Inside any fixed finite prime cube, sufficiently cheap horizons must form a Sperner antichain. `NB-121` supplies the exact falsifier on the Nyman side: every fixed finite nested ladder of natural-cell cutoffs and Nyman sections collapses algebraically to its maximal cutoff/section, so one `NB-120` matched control serves the entire bounded-ratio ladder at the same inverse-section exponent. Cross-scale reuse is therefore proof currency only when the reused views impose new equations or incompatible extremizer geometries; nesting by itself is not enough.

A second boundary is **source simplification cannot merely relocate the arithmetic burden**. `AF-342` shows that a variable background can reduce the centered prime-layer sign complexity below prime scale only by acquiring prime-scale exceptional behavior at prime sites or prime-adjacent composites; if its transform remains zero-pole-blind, the residual still carries the RH-equivalent right-half-plane zero-pole discriminator. `MC-293` gives a complementary conductor-side statement: below source cost `exp(kappa log y/log log y)`, every generated Legendre-shell character is asymptotically balanced except possibly one Landau--Page exceptional mode, and near-affinity of that mode would force a real zero within `O(1/(log y log log y))` of `1`. Low-complexity recoding and low-conductor exceptional spectra are therefore both sharply constrained; any surviving mechanism must either pay genuine source complexity or exploit a specifically arithmetic high-cost/exceptional regime.

A third boundary is **endpoint structure can be real yet universal, erased by the readout, or lost in a relaxation**. `PC-293` proves that the order-one local Hankel residue band of `PC-292` disappears after the diffuse source averaging used by the Prime-Circle one-profile law: for prime and count-matched control measures, every local band `T=O(sqrt(q))` contributes `o(1)` at square-root resolution. `PL-310` computes an exact stretched Green limit and a unique boundary fixed profile `C/sqrt(e^(2r)-1)`, but that shape is universal under bounded lower-order perturbations; arithmetic information can only enter through compactness/global matching, the amplitude, and the later rational-prime sign. `RE-123` removes regular-CA phase placement as a fixed-order obstruction, while `WP-304` shows that the first resolvent logarithm surviving finite-mass decay is density-universal and cancels under canonical evenization. On the Visual branch, `VIS-221` is the first post-`VIS-220` relaxation that retains all pairwise torus interactions: the correlation SDP strictly contains the failed magnetic certificate and has an exact PSD-stress criterion for rank-one tightness, but its prime-matrix RMS scale is still completely open.

The latest independent adversarial checkpoint is commit `8826ccf06e8491b852f0aabdb564772bd9def795`, declaring continuous review through `b8ca70d61a7f1e27a03c8dd222044391c01e48c2`. `AF-342`, `FD-122`, `NB-121`, `PL-310`, `RE-123`, and `PC-293` lie inside that reviewed prefix. `MC-293`, `WP-304`, and `VIS-221` are later canonical evidence and are **not yet independently cleared**. The unresolved `XF-186` review remains narrowly scoped: the zero row defeats the claimed uniformly bounded raw normalization and leaves the raw-noise visibility transfer unproved, but it does not refute the normalized nonlinear chart or its bounded conditional formalization target. Atlas telemetry remains excluded from consequential decisions because its entry and metrics surfaces declare stale, mutually inconsistent source revisions.

## Research-line portfolio

| Research line | Strongest current contribution | Live discriminating question | Recommendation |
| --- | --- | --- | --- |
| `analytic_frontier` | `ANF-185--ANF-186` give a fixed critical-aperture detector and then show that exact one-point prime/rough source marginals still permit endpoint memory when placement may depend on the logarithmic carrier. | Can the actual arithmetic source satisfy a joint placement/correlation law strong enough to make the deterministic signed Fejer/high-pass residual `o(a_X)`, rather than merely matching all one-point source statistics? | `continue` |
| `arithmetic_fidelity` | `AF-342` shows that reducing the zero-sensitive prime-layer residual below `Theta(P/log P)` ordered sign complexity forces the baseline itself to react on a prime-scale exceptional set; nonconstancy alone is not compression. | Is there a canonical source-faithful recoding whose prime-scale exceptional structure is generated from genuinely cheaper retained data, or must a successful certificate directly consume growing ordered complexity? | `narrow` |
| `farey_discrepancy` | `FD-121--FD-122` turn same-source coherence into a genuine network obstruction: every fixed squarefree dilation has a uniform Fourier-skeleton gap, and cheap horizons in a finite prime cube form a Sperner antichain. | Can the squarefree network be globalized to the designated RH-relevant horizon despite deteriorating constants and nonsquarefree/repeated-scale escape, yielding pointwise critical coercivity rather than only pair/network incompatibility? | `continue` |
| `mobius_cancellation` | `MC-293` collapses the low-source-cost near-negative shell spectrum to at most one Landau--Page mode; all additional independent near-negative directions must exceed quasi-subpower source cost. | Can the possible singleton exceptional mode be excluded or exploited, or can high source cost be coupled quantitatively to participation/rank so the high-cost generated family cannot realize the remaining affine/sparse escape? | `narrow` |
| `nyman_beurling` | `NB-121` resolves the transferred Farey coherence clue negatively: every fixed finite nested cutoff ladder is exactly redundant with its maximal section, so common-source perfect target capture survives at inverse-section order. | Can a widening ladder, a non-nested/tail-sensitive compatibility law, or the actual confluent zero-power family impose genuinely new cross-cutoff equations that the maximal-section ambient control cannot satisfy? | `narrow` |
| `prime_circle` | `PC-293` shows that diffuse source averaging suppresses every local residue band `T=O(N)` at `N~sqrt(q)`, including the order-one fixed-pair Hankel transition of `PC-292`. | After source count, diffuseness, and the entire local singular residue sector are matched, is there an order-one prime-specific remainder in the nonlocal residue complement, or must the representation retain cross-level/directional/phase information discarded by one-profile averaging? | `narrow` |
| `prime_flute` | `PF-342` proves the canonical sharp physical `L/H` direct angle is noncompact: cutoff-straddling adjacent modes create order-one correlations on infinitely many separated modules. | What redesigned split or destination observable destroys that witness while preserving a source-faithful route to the target theorem? | `pause` |
| `prime_lattice` | `PL-310` identifies the exact small-order stretched Green limit and its unique nonzero fixed profile `C/sqrt(e^(2r)-1)`, reducing the universal boundary-layer shape to one amplitude. | Can the actual completed-Weil eigenbranch supply stretched-profile compactness/uniform integrability or an equivalent weak-virial amplitude theorem, and then force a rational-prime-specific sign/first crossing? | `narrow` |
| `robin_extremal` | `RE-123` proves that the exponentially fine regular-CA phase mesh samples the complete subedge zero packet superalgebraically well at every fixed algebraic scale. | Do polynomial-height zeros approach the attained edge with enough weighted mass, and can their internal signed phase correlation/cancellation exclude repeated favorable recovery? | `narrow` |
| `visual_exploration` | `VIS-221` lifts the static prime-phase bound to the full correlation SDP, strictly containing the failed magnetic relaxation and giving an exact PSD-stress certificate whenever the SDP is rank-one tight. | Does the canonical prime-matrix SDP itself reach `O(sqrt(R_v))`, become rank-one tight on a growing family, or remain parametrically above Haar RMS and thereby close only this stronger certificate? | `narrow` |
| `weil_inertia` | `WI-291` confines complete ordinary-prime screening to rare giant upper prime-gap hosts while `WI-290` supplies many lower composite-safe components. | Can arithmetic prove avoidance or forced coupling between the lower safe reservoir and those rare upper deserts, or must amplitudes/signs or the two-island topology change? | `narrow` |
| `weil_positivity` | `WP-304` proves bounded source resolvent compressions are asymptotically too small, while the first infinite-mass Mangoldt Herglotz logarithm is density-universal and cancels after canonical evenization. | Can Mathia force an unbounded/generalized boundary channel, orientation, renormalization law, or nonlinear finite--archimedean coupling whose subleading response is genuinely Riemann-specific before Gamma matching? | `narrow` |
| `xi_flow` | `XF-185` gives a uniformly conditioned known-support holomorphic bank; the normalized nonlinear content of `XF-186` remains live while its raw row-normalization/noise transfer remains under unresolved review. | Can the zero-row/raw-normalization gap be repaired on the structured signal-chord range, then extend the construction to unknown support and a zero-sensitive Xi quantity? | `narrow` |

## Cross-line structure

The Farey/Nyman comparison now supplies a clean criterion for using multiscale coherence. **A common source is not enough; the different views must be nonredundant.** Farey squarefree dilations reuse quotient coordinates in incompatible Möbius equality rays and therefore force energy. Nyman nested projections and nested source sections compose exactly, so adding finitely many bounded-ratio cutoffs contributes no new equation and preserves the `1/R` escape. The Master-origin Nyman clue `CLUE-cross-cutoff-coherence-breaks-inverse-section-repair` is therefore correctly `resolved` with outcome `refuted`; no replacement clue is warranted until a concrete non-nested or widening-scale mechanism is identified.

The AF/Möbius pair gives a complementary **source-cost conservation law**. Arithmetic Fidelity shows that simplifying a zero-sensitive residual merely transfers prime-scale exceptions into the baseline unless a different destination currency is found. Möbius Cancellation shows that genuinely low-cost generated characters cannot support a large exceptional spectrum: only one classical Landau--Page direction can survive below the quasi-subpower source-cost threshold. These results do not combine into an RH theorem, but they materially reduce the places where hidden arithmetic complexity can live.

Prime Circle, Prime Lattice, Robin, Weil Positivity, and Visual Exploration expose different ways apparently promising structure can fail at the destination. Source averaging can wash out a worst-case operator effect; a singular boundary layer can have a universal principal profile whose amplitude carries the only remaining information; an arithmetic selector can sample the continuum so finely that placement ceases to be a meaningful gate; and a positive Herglotz logarithm can be forced only in an oriented infinite-mass representation whose leading term is universal. Visual now supplies a useful counterpoint: after two certificate families were proven too weak, `VIS-221` retains the full pairwise torus interaction in an SDP, but the decisive question is quantitative tightness on the actual prime matrices rather than the mere existence of a stronger relaxation.

The existing global fidelity/realization clue and `MI-008`, `MI-012`, `MI-013`, `MI-015`, `MI-016`, and the newer source-complexity synthesis already cover these broad lessons. No new Master clue is justified by the present delta: the Nyman transfer is resolved negatively, while the new Visual clue is appropriately destination-local and has a direct falsifier.

## Highest-leverage unresolved questions

1. **Analytic Frontier / Farey:** turn deterministic or network endpoint observability into a source theorem. Analytic Frontier needs a joint arithmetic placement law for the fixed high-pass residual; Farey needs pointwise globalization of the squarefree coherence network without losing the critical scale through collapsing constants or nonsquarefree escape.
2. **Nyman / Arithmetic Fidelity:** identify genuinely nonredundant growing source information. Nyman should leave fixed bounded-ratio nested cutoffs and test widening, non-nested, tail-sensitive, or actual-zero-power compatibility; Arithmetic Fidelity needs either a cheaper source-faithful recoding or a theorem that directly handles prime-scale ordered complexity.
3. **Möbius / Weil Inertia:** convert sharply localized exceptional sets into arithmetic impossibility or coupling. Möbius must control the unique low-cost Landau--Page mode and the high-cost remainder; Weil Inertia must couple abundant lower composite-safe windows to the sparse upper prime deserts that can host complete screening.
4. **Prime Circle / Prime Lattice / Robin:** recover source-specific endpoint mass after universal mechanisms are removed. Prime Circle must inspect the nonlocal residue complement or enrich the readout; Prime Lattice must control the stretched-profile amplitude; Robin must prove signed correlation/cancellation for the near-edge zero packet rather than selector placement.
5. **Weil Positivity / Xi Flow / Visual:** require a bridge whose informative part survives representation loss. Weil Positivity needs source-forced infinite-mass orientation or genuinely coupled nonlinear boundary data; Xi Flow needs a raw-coordinate lower bound compatible with the zero row; Visual should now test the `VIS-221` SDP/RMS ratio and rank-one stress certificate before inventing another relaxation.

## Portfolio changes proposed

No portfolio status change is justified in this pass. Keep `analytic_frontier` and `farey_discrepancy` at `continue`, `prime_flute` at `pause`, and the remaining ten lines at `narrow`. `FD-122` strengthens the reason to continue Farey rather than changing its category; `NB-121` refutes the cheapest transferred coherence mechanism without closing the broader Nyman source question; `PC-293`, `RE-123`, `AF-342`, and `WP-304` remove specific escapes but leave precise surviving tests; `PL-310` is a positive analytic refinement whose leading profile is still universal; `MC-293` is a strong arithmetic narrowing result whose possible exceptional singleton and high-cost sector remain live; and `VIS-221` opens a stronger destination-local certificate without yet establishing its asymptotic scale.

Mark the Nyman cross-cutoff clue as resolved/refuted in the global synthesis and retain the proposed Visual SDP clue as line-local. Do not replace either with a new Master clue mechanically. No new line, merge, split, additional Master clue, or Research Watch rotation is justified. The strict automation rule still requires a strong pause/merge donor before reallocating watch capacity, and no additional active line clears that gate.

Treat review uncertainty narrowly. The independent checkpoint at `8826ccf06e8491b852f0aabdb564772bd9def795` clears canonical research only through `b8ca70d61a7f1e27a03c8dd222044391c01e48c2`; `MC-293`, `WP-304`, and `VIS-221` remain canonical but independently uncleared. `XF-186` retains its normalized nonlinear chart and bounded conditional target, while the unresolved review leaves its raw-noise visibility bridge unproved. Atlas remains excluded until its entry and metrics surfaces are rebuilt from one current source revision.