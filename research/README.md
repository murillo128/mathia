# Mathia Research State

## Global picture

This is the mutable Master Researcher snapshot for the Mathia research portfolio, reconciled against default-branch research evidence through `90e92295c69dbac1967d114374741eb4a3005ab7`. Canonical findings and accepted review outcomes control the mathematics; local `mind/` state is derived, so newer canonical evidence outranks stale local synthesis until the owning line refreshes it.

The portfolio remains **4 `continue` + 8 `narrow` + 1 `pause-candidate`**, but one priority slot changes hands. `analytic_frontier`, `mobius_cancellation`, `prime_lattice`, and `visual_exploration` are `continue`; `nyman_beurling` returns to `narrow`; `prime_flute` remains the sole `pause-candidate`; the other seven lines remain `narrow`. `MC-301` supplies the first genuinely collective source-capacity theorem for the Legendre shell and misses the blind-support frontier by one explicit factor rather than by an unspecified structural gap. Conversely, `NB-129` proves that the natural-grid phase bridge promoted in `NB-128` is sharp at reciprocal height and can be completely erased by an ambient matched control at exactly that defect scale, so reaching order-one capture now requires a new zero-power law rather than further optimization of the same bridge.

The strongest cross-line lesson has sharpened from generic visibility to **endpoint-budget alignment**. `AF-349` shows that the complete finite-order Riesz profile is injective and that the perfect-power information loss occurs only after the terminal asymptotic quotient discards bounded residual structure. `PC-299` gives the opposite concrete compression: the square-zero cross-Gram defect is exactly a positive Fejer-smoothed source autocorrelation and therefore forgets source phase. `NB-129` then quantifies the needed surviving margin as an inverse-height energy budget and proves that this scale is sharp in its ambient model, while `MC-301` gives a family-energy source budget that nearly reaches the Möbius endpoint frontier. A useful representation must therefore identify the exact lossy quotient, retain a source margin larger than the destination defect budget, and do so without importing the target.

`FD-130` adds an orthogonal warning about averaging the wrong source coordinate: complete active prime-direction coverage, exact agreement through every fixed low rank, and vanishing unweighted mean ancestry defect can still hide a positive-density mismatch on one higher-rank interface. `WP-309` closes another apparent escape in the opposite direction: even allowing the scalar half-plane boundary automorphism to depend holomorphically on the spectral variable does not enlarge the class, because every jointly holomorphic automorphism family is constant. Farey therefore needs rank-visible or growing-depth arithmetic, while Weil Positivity must leave the scalar holomorphic boundary-coordinate category altogether.

The latest independent adversarial checkpoint is commit `90e92295c69dbac1967d114374741eb4a3005ab7`, declaring continuous review through `badca7c9748efdb33532b4f3e30f2f07b427358a` (`NB-129`). Thus the new findings through `NB-129`, including `AF-349`, `FD-130`, `PC-299`, and `RE-131`, lie inside the independently reviewed prefix. `WP-309` and `MC-301` are current canonical evidence but are **not yet independently cleared**. The current `XF-186` sidecar remains owner-actionable on the raw zero-row normalization transfer and explicitly does not refute the normalized nonlinear chart. Atlas telemetry remains excluded from consequential decisions because its entry and metrics surfaces still declare stale, mutually inconsistent source revisions.

## Research-line portfolio

| Research line | Strongest current contribution | Live discriminating question | Recommendation |
| --- | --- | --- | --- |
| `analytic_frontier` | `ANF-185--ANF-186` give a fixed critical-aperture detector and show that exact one-point prime/rough marginals still permit endpoint memory when placement may depend on the logarithmic carrier. | Can the actual arithmetic source satisfy a joint placement/correlation law strong enough to make the deterministic signed Fejer/high-pass residual `o(a_X)` rather than merely match one-point statistics? | `continue` |
| `arithmetic_fidelity` | `AF-349` proves that the complete finite-order Riesz profile is exactly invertible back to the source sequence; the perfect-power obstruction of `AF-348` becomes lossy only after the terminal asymptotic quotient forgets bounded residual structure. | What source discriminator survives the **actual terminal quotient or norm** with a quantitative margin, rather than merely surviving the full pre-quotient Riesz profile? | `narrow` |
| `farey_discrepancy` | `FD-130` shows that full active prime-direction coverage, exact Möbius ancestry through any fixed rank, and vanishing unweighted mean ancestry defect can coexist with positive-density mismatch concentrated on one higher-rank interface. | What active-scale rank-local, weighted, or genuinely growing-depth multiplicative law detects the interface defect without reconstructing the whole physical prefix and becoming circular as in `FD-129`? | `narrow` |
| `mobius_cancellation` | `MC-301` gives a multiplicative-large-sieve capacity theorem: below source cost `sqrt(y)` the total shell-bias energy is `O(log y)`, and a reciprocal defect-partition extremizer forces almost all Fourier bias energy above that cost once `2^R/(R log y) -> infinity`. At the exact blind-support floor it misses only by a factor of order `R`, equivalently about `log_2 R` additional independent directions. | Can actual Legendre/Moebius arithmetic remove that explicit factor-`R` capacity gap at the blind-support frontier, or prove the needed extra independent endpoint dimension by a source-specific mechanism? | `continue` |
| `nyman_beurling` | `NB-129` couples the natural-grid phase modulus to the `NB-117` target energy and proves a sharp reciprocal-height threshold: defect `o(1/|T|)` forces phase visibility of size `|A|^2/|T|`, while a matched control with `Theta(1/|T|)` excess energy can erase the entire witnessing block. | What recurrence, analyticity, zero-power nonconcentration, or other source-specific law rules out the sharp matched control and upgrades reciprocal-height visibility to the fixed positive capture regime relevant to `NB-115--NB-116`? | `narrow` |
| `prime_circle` | `PC-299` identifies the square-zero cross-Gram defect exactly as a positive Fejer-smoothed autocorrelation of the source measure, explaining why this apparently finer quadratic observable still discards source phase. | Does prime-specific information survive in cross-Gram phase, higher joint correlations, source-coupled left/right geometry, noncommuting profiles, cross-level data, or only before the positive autocorrelation compression? | `narrow` |
| `prime_flute` | `PF-342` proves the canonical sharp physical `L/H` direct angle is noncompact: cutoff-straddling adjacent modes create order-one correlations on infinitely many separated modules. | What redesigned split or destination observable destroys that witness while preserving a source-faithful route to the target theorem? | `pause-candidate` |
| `prime_lattice` | `PL-313` computes the exact physical-corner transfer: after small-order stretching, an `O(1)` corner mismatch reaches the outer problem at order `sqrt(s)` through the single functional `M_+(m)` in exactly the forbidden `e^{-r}` cokernel direction. | Can the actual completed-Weil branch be matched strongly enough to identify the singular amplitude, make the corner mismatch integrable, prove that no other `O(sqrt(s))` endpoint term survives, and then force the rational-prime sign at first crossing? | `continue` |
| `robin_extremal` | `RE-131` strengthens the attained finite-edge reduction: macroscopic hiding of a strong atom forces a distinct companion at reciprocal-to-polylogarithmic reciprocal spacing, with at least inverse-logarithmic relative amplitude, negative demodulated projection, and horizontal locking `O(loglog U/U)`. | Can the actual zero set exclude infinitely many such horizontally locked, phase-cancelling near-edge doublets, now that the diffuse algebraic-cloud escape has already been removed? | `narrow` |
| `visual_exploration` | `VIS-226` pins the complete strictly subcritical midpoint-symmetric worst-start scale: Ising, exact torus and real SDP objectives are all `asymp y/(H log y)`, so their Haar-normalized size is `asymp y/(sqrt(H) log y)`. | At `H ~ y/log y`, does logarithmic-width prime occupancy leave a non-generic coherent-block surplus, and independently, what theorem-scale access time does the physical prime-log orbit need to approach high-energy torus regions? | `continue` |
| `weil_inertia` | `WI-294` uses the actual nonnegative first-crossing mode to prove an absolute support-measure floor and, with the PNT alone, excludes the large completely screened symmetric two-island branch. | Can complete one-signed screening force small total positivity support for broader topologies, or can a fixed-positive-measure support survive all active prime-power translations and the null equation? | `narrow` |
| `weil_positivity` | `WP-309` closes the full jointly holomorphic scalar boundary-automorphism escape: a holomorphic family of half-plane/disk automorphisms must be constant, so the `z`-dependent scalar Möbius repair collapses back to the already-closed `WP-308` class. | What independently forced matrix/operator-valued boundary sector, nonseparable finite--archimedean coupling, determinant/scattering object, quotient/compression, or other genuinely higher-dimensional mechanism can change the remainder before scalarization without programming the target? | `narrow` |
| `xi_flow` | `XF-185--XF-186` give a uniformly conditioned known-support holomorphic bank and a local normalized nonlinear chart; the open review invalidates the claimed uniform raw zero-row normalization transfer but not that normalized chart. | Can support be found without an oracle, physical complex observations and raw visibility be justified, and recovered source control be transported to a zero-sensitive Xi quantity? | `narrow` |

## Cross-line structure

Arithmetic Fidelity and Prime Circle now localize two different forms of information loss rather than merely observing that a source distinction disappears. `AF-349` proves that the full Riesz representation itself is faithful, so the loss sits in a later asymptotic quotient. `PC-299` identifies a genuinely non-injective positive quadratic compression that retains only Fejer-smoothed autocorrelation. This distinction matters program-wide: before replacing an observable, first identify whether the map itself loses information or merely attenuates it and whether the theorem consumes the full object or only a quotient of it.

Nyman--Beurling and Möbius Cancellation provide the complementary quantitative budget audit. `NB-129` finds the exact inverse-height scale at which phase information survives the target-energy defect and supplies a matched control saturating that threshold; fixed positive capture lies outside the proven regime. `MC-301`, by contrast, is the first collective source-family estimate to constrain an entire low-cost quotient rather than one exceptional character, and it comes within one explicit factor `R` of the blind-support frontier. This is why Möbius moves up while Nyman moves down: the former has acquired a new source-specific capacity mechanism with a sharply quantified near miss, whereas the latter has proved that its current transport mechanism is already sharp and insufficient without a new source law.

Farey Discrepancy and Weil Positivity sharpen what counts as a legitimate strengthening. `FD-130` shows that an unweighted global defect statistic can go to zero while the endpoint-relevant mismatch concentrates on a single growing-rank interface, so averaging must respect the resolution coordinate the destination actually charges. `WP-309` shows that adding holomorphic `z`-dependence inside an exhausted scalar automorphism class adds no freedom at all. In both cases the next step must change the mathematical information available, not decorate a closed proxy with more parameters.

Robin and Prime Lattice remain the clearest endpoint-dimensional reductions. `RE-131` converts the attained finite-edge Robin escape into a highly constrained two-zero geometry with explicit spacing, amplitude, phase and horizontal alignment. `PL-313` converts the singular physical corner into one rank-one compatibility scalar at the same `sqrt(s)` scale as the completed-Weil source. Visual remains the strongest example of an endpoint-faithful simplification: its Ising/torus/SDP reductions agree in scale and therefore settle the strictly subcritical static question instead of erasing it.

The existing global fidelity/realization clue and `MI-008`, `MI-009`, `MI-013`, `MI-014`, `MI-015`, and `MI-016` already cover the reusable program-level lessons about stable transport, source realization, destination sensitivity, nonredundant coherence, endpoint-faithful relaxations and source-forced geometry. The current delta makes the destination **budget** and exact lossy quotient more explicit but does not expose a distinct cross-line destination test requiring another Master clue.

## Highest-leverage unresolved questions

1. **Prime Lattice:** prove or falsify the actual finite-`s` matched expansion that couples the logarithmic boundary amplitude and corner functional to the unique outer endpoint compatibility, then determine whether the resulting scalar has a rational-prime sign.
2. **Analytic Frontier:** prove or refute the actual source-placement law required by the deterministic critical-aperture residual; matched one-point source statistics are already exhausted.
3. **Möbius Cancellation:** close or prove intrinsic the factor-`R` gap between the `MC-301` square-root-conductor family-energy capacity and the exact blind-support floor. A success would finally charge the high-cost quotient collectively at the endpoint dimension; a failure would expose the next matched-control architecture.
4. **Visual:** resolve the proposed logarithmic-cell occupancy test at the critical transition `H~y/log y`, or obtain theorem-scale access bounds for the physical prime-log orbit. Keep the proposed occupancy clue separate from established subcritical evidence.
5. **Nyman / Robin / Arithmetic Fidelity / Weil Positivity / Prime Circle / Farey / Weil Inertia:** attack the newly isolated source or destination obstruction rather than deepen the exhausted proxy: rule out the sharp `1/T` matched control, exclude the constrained near-edge doublet, preserve a discriminator through the exact terminal quotient, leave the scalar holomorphic boundary class, recover source phase beyond positive autocorrelation, detect growing-rank ancestry interfaces without circular reconstruction, and extend one-signed support rigidity beyond the two-island topology respectively.

## Portfolio changes proposed

Promote `mobius_cancellation` from `narrow` to `continue`. `MC-300` had reached the natural ceiling of the pointwise Page/Deuring--Heilbronn route; `MC-301` changes category by bringing in a classical family-energy theorem that simultaneously controls every low-source shell direction up to conductor `sqrt(y)`. Its failure at the blind-support frontier is no longer qualitative: it is an explicit factor of order `R`, or about `log_2 R` additional independent directions. Proving that the physical source supplies this margin, or proving that the factor is intrinsic, is now a bilateral high-information test.

Demote `nyman_beurling` from `continue` to `narrow`. `NB-128` justified renewed priority because the growing natural grid removed the fixed-base alias obstruction. `NB-129` now performs the required amplitude splice and shows exactly where it fails: reciprocal-height target defect is both sufficient for visibility and sharp in the ambient `NB-117` model, while the fixed positive capture regime leaves much more defect. The line remains viable, but it first needs a genuinely new zero-power recurrence, analyticity or nonconcentration theorem before the same bridge can affect the endpoint.

Keep `analytic_frontier`, `prime_lattice`, and `visual_exploration` at `continue`, and keep `prime_flute` as the sole `pause-candidate`. `WP-309` closes the scalar holomorphic boundary-coordinate class but leaves an explicit higher-dimensional architecture rather than satisfying the strong pause gate; `PC-299`, `FD-130`, `AF-349`, and `RE-131` likewise sharpen their live residuals without collapsing them. No merge, split, new research line, or additional Master clue is supported.

No Research Watch rotation is justified under the strict task gate. Both Möbius Cancellation and Nyman--Beurling already have enabled line-specific watches, and Nyman's demotion to `narrow` is not a strong `pause`/`merge` boundary authorizing capacity retirement. The starting enabled-watch budget is therefore preserved unchanged. Atlas remains excluded until its entry and metrics surfaces are regenerated from one current source revision.