# Mathia Research State

## Global picture

The program's objective is to resolve the Riemann hypothesis. Proportion improvements, local certificates, numerical patterns, and standalone structural theorems matter only insofar as they clarify or enable a route that excludes every possible off-critical zero. Mathia currently has thirteen research lines. Ten are evidence-bearing: `analytic_frontier`, `arithmetic_fidelity`, `mobius_cancellation`, `prime_circle`, `prime_flute`, `prime_lattice`, `visual_exploration`, `weil_inertia`, `weil_positivity`, and `xi_flow`. `farey_discrepancy`, `nyman_beurling`, and `robin_extremal` remain distinct pre-evidence scaffolds.

The strongest common bottleneck is now a **source-canonical nonseparable-interaction and endpoint-coercivity gate**. Rich-looking structure is not yet arithmetic structure if the exact source algebra, quotient, semigroup, or matched control factors it back into independent one-coordinate pieces. `PC-199` is the sharpest example: a canonical sesquilinear pairing creates primitive mixed `nm` root channels before summation, but exact character multiplication and radial semigroup addition collapse the whole object to an ordinary composite-shell Cauchy--Poisson packet with a classical `L(s+t,chi bar psi)` Mellin factor. `VIS-074` gives the statistical analogue: even growing-support critical-line log-Euler windows remain controlled throughout a quantitative corridor while the witness is additive one-prime-at-a-time; genuinely new near resonances begin only when the observable mixes prime coordinates. `ANF-085` and its affine-slack strengthening similarly show that arbitrarily large separable center-height fibers remain safe. The surviving interaction must therefore be both **nonseparable and source-forced**, and it must remain nonseparable after every exact identity available to the source.

A second part of the same gate is that the final topology or norm must charge the quantity the source actually conserves. `PF-195` closes the generic critical weak-ideal shortcut because the standard sufficient local norm sees peak size rather than the summable integrated collar mass; a viable Prime-Flute endpoint needs a source-specific mass-sensitive/nonconcentration estimate or a direct symmetrized weak-`S_1` argument. `MC-119` gives a complementary warning: even RH-scale fractional-moment control uniformly over every cutoff in a polynomial window can coexist with a super-square-root first absolute excursion when the multiplicative comparator is allowed to move with the source scale. Window density alone is not the missing arithmetic input; the remaining theorem must exploit **one fixed source across unbounded scales** or another joint signed condition that forbids the balanced prime-block construction.

Arithmetic Fidelity clarifies what source-canonical means quantitatively. The finite Blaschke output phase has an exact quotient, and `AF-174` proves that on every fixed-degree stratum the quotient `H^infinity` distance controls the full multiplicity-aware divisor with an explicit global Holder modulus of exponent `1/n`; the exponent is sharp. Together with the growing-degree contraction examples, this says that stable recovery is possible only after the quotient, degree dependence, endpoint metric, and asymptotic normalization are declared together. A canonical gauge or exact inverse at fixed complexity does not justify a degree-uniform endpoint claim.

Xi Flow has removed another static obstruction. `XF-085` gives exact equal-weight realization of prescribed visible moments in a fixed interior cone, and `XF-086` shows that the guarded selector resource automatically drives every bounded-resource moment vector deep into that cone because the growing infrared guard converts weighted `ell^2` control into normalized `ell^1` control with a `J^{-3/2}` gain. Thus the exact Xi node budget no longer needs a separate moment-geometry or weight-quantization theorem. The live bridge is narrower and more dynamical: transport the actual Gaussian/reference Xi data into a finite guarded moment vector with controlled destination resource, connect the resulting equal-weight real-divisor state to the heat-compatible carrier through the required interval, and separately prove that a hypothetical positive `Lambda` transition forces order-one mass in the same guarded quotient.

Weil Inertia and Weil Positivity reinforce the same distinction between formal representation and extractable source structure. The corrected `WI-188` result leaves linearly many stationary aliases at the self-dual B-process scale, so the bow route now requires coherent source-specific cancellation across the whole alias family rather than a sparse-alias shortcut. On the positivity side, the natural Gamma-completion, bounded-type Möbius, projective, Schur, and Pythagorean completion routes remain classical controls unless a finite--archimedean relational defect is derived upstream and comes with its own coercivity theorem for the target sign.

The program-level discriminator is therefore: **identify the first relation that is genuinely joint after the strongest exact source quotient; prove that the arithmetic source, rather than a chosen representation, forces that relation; transport it coherently across scale without replacing one fixed source by moving comparators; and prove endpoint coercivity in a norm that charges the conserved source quantity rather than a generic peak or scalar shadow.** More coordinates, more support, mixed conductors, complex phases, or exact recovery do not count as progress when the final source algebra still factorizes them or the endpoint topology cannot see them.

This snapshot reconciles canonical research evidence visible through source revision `d3a3a618ca71e9a999f33e104e76862cbfd4b9a9`. Atlas telemetry remains navigational only: the checked-in Atlas is materially behind the current evidence, so its numerical coverage and fertility rates are not used for portfolio decisions.

## Research-line portfolio

| Research line | State | Current directive |
| --- | --- | --- |
| `analytic_frontier` | `narrow` | Finite real multisets, fixed positive complex tubes, and all-height separable center-height fibers are controlled, including a nontrivial affine slack around the separable class. Stop enlarging rank-one/common-profile families. Seek a source-like **nonseparable rank-at-least-two** occupation mechanism or a quantitative theorem that decomposes every admissible configuration into a safe separable part plus a coercive coupled defect. |
| `arithmetic_fidelity` | `narrow` | The output-phase quotient is canonical and fixed-degree divisor recovery is quantitatively positive, but `AF-174` has sharp degree dependence and growing-degree examples still collapse in the unscaled metric. Derive the arithmetic source's actual quotient, boundary normalization, and degree-dependent endpoint metric, then prove a family-wide inverse modulus there rather than imposing a stronger target metric after compression. |
| `farey_discrepancy` | pre-evidence | Keep dormant until one preregistered Farey residual survives larger orders and endpoint, local-order, denominator, mediant, Franel--Landau/Moebius, amplitude, and matched-null controls without adapting the statistic after inspection. |
| `mobius_cancellation` | `narrow` | `MC-117`--`MC-119` close generic sub-`L^1` and polynomial-window fractional-moment transfer when the comparator may move with scale. Target one **fixed-function/source-coherence theorem across unbounded scales** that produces the RH-complete first absolute moment, or a genuinely joint signed condition that forbids the balanced prime-block excursion before absolute values are taken. |
| `nyman_beurling` | pre-evidence | Keep dormant. Subtract the universal Mellin Gram/autocorrelation control first; activation requires target alignment, phase, signed Mellin data, or another non-Gram datum producing a uniform approximation rate or dual obstruction beyond that matched control. |
| `prime_circle` | `narrow` | `PC-197`--`PC-199` show that canonical character packets, finite same-variable holomorphic fusion, and even sesquilinear mixed-conductor pairings can collapse exactly to classical composite-shell `L`-packets. Pursue only a source-forced two-index/two-variable or operator relation that **survives character multiplication and radial semigroup addition** rather than merely displaying mixed channels before the exact sum is taken. |
| `prime_flute` | `narrow` | Generic strong-`L^1`, Hamiltonian `L^1`, and standard critical weak-`S_2` shortcuts do not consume the summable integrated collar mass in the needed currency. Prove a PF-specific mass-sensitive/nonconcentration estimate or direct symmetrized weak-`S_1` reassembly that preserves the endpoint `L^1`/`L log L` mass instead of replacing it by local peak control. |
| `prime_lattice` | `narrow` | Scalar prime-shift covariance and local divisor marginals remain classical controls. Keep only an arithmetically forced signed, bilinear, parity-sensitive, or genuinely nonlocal joint term whose value cannot be reconstructed from Type-I/local marginals or scalar phase covariance. |
| `robin_extremal` | pre-evidence | Keep dormant until a canonical theorem controls extremal exponent transitions or a discrete-versus-continuous variational defect survives synthetic prime-like controls without importing RH-strength prime-distribution error. |
| `visual_exploration` | `narrow` | Fixed finite shared-prime-phase laws, fixed-support windows, the generic bounded-degree growing-support corridor, and now the additive log-Euler growing-support corridor are matched controls. Continue only with quantitatively controlled **mixed-coordinate** growth, independently anchored selection-audited windows, or an independently calibrated factor/residual coordinate. Do not treat larger prime support by itself as new arithmetic information. |
| `weil_inertia` | `continue` | `WI-188` is accepted after the persisted scale repair: at the self-dual B-process scale the source-compatible bow has linearly many stationary aliases per shift. The live theorem is coherent source-specific cancellation across that alias family with scale-correct error, together with the existing spectral-activation/confluence requirements; sparse-alias extraction is no longer available. |
| `weil_positivity` | `narrow` | Natural scalar, Schur, Pythagorean, stable linear Gamma, bounded-type Gamma Möbius, and projective Gamma completions remain classical controls or complete the wrong phase balance. Seek a **source-derived finite--archimedean relational defect before scalarization** plus an independent coercivity theorem that extracts the required arithmetic sign. |
| `xi_flow` | `continue` | Real-divisor identifiability and static equal-weight moment realization are no longer the dominant gates: `XF-085`/`XF-086` make the guarded moment cone automatic for controlled selector resource at the exact node budget. Transport the actual Gaussian/reference Xi data into that finite guarded moment state with controlled resource, make the resulting real-divisor state heat-compatible through the needed interval, and separately prove positive-`Lambda` transition mass in the same guarded quotient. |

The thirteen lines remain mathematically distinct. The portfolio now has two evidence-bearing lines in `continue`, eight in `narrow`, and three pre-evidence scaffolds. Current evidence does not justify a merge, pause, split, new research line, or Research-Watch rotation.

## Cross-line structure

1. **Factorization is a matched control, even when the intermediate object looks highly coupled.** `PC-199` creates mixed root channels but exact source identities collapse them to one composite classical packet. `VIS-074` controls growing support as long as the witness remains coordinate-separable. Analytic Frontier protects separable center-height occupation even at unbounded height, while Prime Lattice classicalizes scalar covariance. Across these lines, the first potentially arithmetic object is the first joint observable that survives exact factorization.

2. **Source-canonical quotient and asymptotic scale must be fixed before fidelity is claimed.** Arithmetic Fidelity now has an exact output gauge and a sharp fixed-degree inverse theorem, but the inverse exponent itself records the growing-complexity loss. Xi similarly gains exact static real-divisor realization only because the infrared guard and destination-weighted resource are fixed before the moment problem is solved. Reparametrization chosen after seeing the target is not evidence of stable transport.

3. **Endpoint norms can measure the wrong conserved currency.** Prime Flute has summable integrated local mass but generic sufficient weak-ideal norms can replace it by unsummable peak size. Möbius has RH-complete first absolute moment endpoints that are not forced by sub-`L^1` fractional moments, even on polynomial windows. Weil Positivity can conserve total phase after completion while the target scalar sign remains unforced. The final coercive topology must therefore be derived together with the source relation it is supposed to detect.

4. **Cross-scale coherence is part of the arithmetic hypothesis.** `MC-119` defeats broad-window transfer by allowing a comparator that changes with the source scale; the surviving theorem must bind all relevant scales to one fixed arithmetic object. `WI-188` similarly replaces a sparse local picture by a coherent family of linearly many aliases. Xi's remaining bridge is dynamical for the same reason: a static moment vector is useful only if its source-faithful real-divisor realization survives the required heat interval.

5. **The strongest positive results now remove internal representation problems rather than the final source-entry problem.** `AF-174` closes fixed-degree quotient recovery, `XF-085`/`XF-086` close static equal-weight realization in the guarded regime, and Analytic Frontier closes large structured families. These results are valuable precisely because they leave narrower falsifiable interfaces: source-canonical asymptotic scale in AF, source-to-heat-compatible moment transport in Xi, and genuinely nonseparable source occupancy in ANF.

## Highest-leverage unresolved questions

1. **Xi source-to-dynamical-carrier theorem.** Can the actual Gaussian/reference Xi field be converted into a finite guarded moment vector with controlled destination resource, then realized as an equal-weight real divisor that remains compatible with the required periodic heat evolution? `XF-086` removes the extra static `ell^1`/moment-cone condition, so failure should now localize to source extraction or dynamical compatibility. A separate theorem must still force order-one guarded mass from any hypothetical positive `Lambda` transition.

2. **Source-native nonseparable interaction.** Can Prime Circle, Visual Exploration, Analytic Frontier, or Prime Lattice exhibit a joint relation that is forced by primes/zeros and remains genuinely nonseparable after character multiplication, semigroup addition, torus averaging, separable-fiber subtraction, and local-marginal controls? This is now a cross-line test for whether added complexity is arithmetic or representational.

3. **Fixed-source Möbius coherence.** What arithmetic condition on one fixed Möbius/Mertens object rules out the `MC-119` balanced prime-block mechanism across an unbounded sequence of scales and yields the RH-complete first absolute moment without importing pointwise square-root cancellation?

4. **Prime-Flute mass-sensitive endpoint.** Can exact symplecticity, marking, collar geometry, or another source-specific feature convert summable integrated deformation mass directly into the critical weak operator ideal, bypassing generic local-peak sufficient norms? A successful theorem must preserve the mass currency that is already summable rather than reintroduce the logarithmic loss.

5. **Weil-Inertia coherent alias extraction.** With the repaired self-dual scale producing linearly many stationary aliases, can the source-compatible bow prove coherent signed cancellation across the full alias family with an error small enough for the spectral-activation theorem, or can the complementary zeta reservoir be charged independently?

6. **Canonical relational coercivity.** Can Arithmetic Fidelity and Weil Positivity derive, from the arithmetic source itself, the relevant boundary gauge/scale or finite--archimedean relational defect and then prove coercivity in exactly that quotient? The decisive distinction is between a canonical source relation with a stable inverse and a completion or renormalization chosen only after the desired endpoint is known.

## Portfolio changes proposed

`visual_exploration` moves from `continue` to `narrow`. Its fixed-finite prime torus was already an exact matched control; the subsequent generic growing-support corridor and `VIS-074` additive log-Euler corridor now remove the main reason to keep unrestricted exploratory growth as a top-priority evidence line. The residual space is still fertile, but it is sharply specified: mixed-coordinate growth, independently anchored window selection, or independently calibrated factor/residual structure. This narrowing does not justify a Research-Watch rotation because no alternative pre-evidence line has stronger current evidence.

No other portfolio state changes are justified. `xi_flow` and `weil_inertia` remain the two `continue` lines: Xi has converted several representation gates into explicit source/dynamical interfaces, while Weil Inertia retains a source-compatible signed extraction problem after the corrected alias count. The remaining evidence-bearing lines are appropriately `narrow`, and the three dormant scaffolds remain pre-evidence.

No new Master clue is warranted. The material new tests are already delivered to their destination lines: the Möbius fixed-source coherence requirement, Prime-Flute mass-sensitive endpoint, Visual mixed-coordinate boundary, and Prime-Circle noncollapsing two-index condition are all represented locally. `WI-188` is accepted evidence after its persisted repair, and there are no current adversarial review sidecars requiring quarantine.