# Mathia Research State

Canonical evidence synthesized through `6951b3e283f53e4fc15a2e08495cef7261904394`. The latest independent adversarial checkpoint is `b645b9d450aec57f8f66e20ddaf07530f0c3a3e5`, with `Adversarial-Reviewed-Through: e52fc5970e0a289b2fde546cca2e373a9e9d8ccc`. `PF-315`, the strengthened `WP-274`, and `MC-251` are later substantive evidence and are used as current canonical findings without being treated as adversarially cleared. No `.review.md` sidecar is open in the current tree.

## Global picture

Portfolio recommendations remain unchanged: `analytic_frontier`, `prime_flute`, and `weil_inertia` stay at `continue`; every other research line stays at `narrow`. No line currently clears the evidence and noncircularity bar for promotion, pause, merge, split, or a new standalone line.

The strongest update is that **several lines have now factorized their remaining obstruction into exact residual currencies instead of another ambient conditioning proxy**. `PF-315` gives the full physical Prime-Flute angle an exact nested-defect decomposition: after eliminating retained physical-low modes, the high input reaches the conditional channel through the canonical residual map `V_H`, while the direct low/high angle `Z_LH` carries the complementary defect. There is no arbitrary source-amplification map left between these stages. `NB-078` gives Nyman an analogous multiscale accounting identity: each `q`-adic refinement splits both new continuation and new prediction into nonnegative information charges, so a stable tail must create an unbounded cumulative excess of continuation increments over prediction increments on every refinement ray. `RE-081` performs the same kind of elimination on Robin's transport measure and shows that all discrete higher-layer prime-power atoms are asymptotically negligible on the long-fan branch; the required two-front profile is carried by the continuous adaptive selector-dilation measure itself.

Farey has crossed an important recurrence threshold. `FD-079` upgrades zero-fixed-power-loss occupation to a **constant logarithmic-mean statement**: annular energy-weighted nonsquarefree occupation has bounded logarithmic entropy, positive logarithmic geometric and arithmetic means, and every sufficiently small fixed threshold is exceeded on a positive lower logarithmic proportion of endpoints. The remaining escape is no longer ubiquitous subpower dilution; it is concentrated in sparse exceptional endpoints or in a mismatch between this logarithmic recurrence currency and the stronger pointwise/local quantity consumed by the endpoint theorem.

Source restriction is also becoming more explicit about what it must restrict. `AF-301` shows that positivity alone does not rescue coefficient compression: discarded positive Dirichlet rays form a reachable cone, and three forgotten multiplicatively independent prime coordinates recurrently positively span the entire complex response plane. Once that happens, every retained positive state again has a boundary-zero completion. The surviving Arithmetic-Fidelity route must therefore bound the **fiberwise reachable correction set** by normalization, coefficient budgets, discreteness, multiplicative coupling, sparsity, nonlinear constraints, or another genuinely source-derived restriction; one-sided sign alone is insufficient.

Two other branches sharpen negative boundaries rather than opening shortcuts. `MC-251` applies `q`-ary Hamming sphere packing at every primary torsion level and strengthens the low-order primary-rank deficit beyond Singleton, but still permits dimension `n-o(n)` in the wide compatible shell. Generic coding theory therefore cannot force the blind quotient to vanish; arithmetic residue-symbol structure or fixed-power signed cancellation of the exact rough coset is still required. The strengthened `WP-274` closes the analogous inverse-realization escape for Weil Positivity: not only Clark model spaces but also inverse de Branges/canonical-system Hamiltonians can be reconstructed from the normalized target carrier itself, so their existence is a universal representation tautology unless the analytic object is selected independently from source geometry first.

Weil Inertia remains a `continue`, but its finite activation gate is now more stringent. `WI-262` finds an exact algebraic bug in the public interval-LDL judge: off-diagonal Schur entries are updated twice, and an explicit rational indefinite matrix is reported with all positive pivots. Together with `WI-260`--`WI-261`, this means an independent recertification must establish both the exact theorem matrix and a mathematically valid positivity predicate with correct directed interval arithmetic before `lambda_(7/20)>0` can be used to activate the one-prime phase cone. This is still a finite, decisive gate; the audit is evidence failure, not evidence that the intended operator is negative.

## Research-line portfolio

| Research line | Recommendation | Strongest current contribution | Live discriminating question / risk |
| --- | --- | --- | --- |
| `analytic_frontier` | `continue` | `ANF-180`--`ANF-183` make the matched endpoint packet quantitatively sharp: derivative depth `Theta(K/a_X)` and physical aperture `Theta(X/a_X)` are equivalent discrimination boundaries. | Prove that the actual prime-minus-rough source cannot realize the matched late packet at the sharp aperture, while keeping positive-cascade non-excisability as a separate final gate. |
| `arithmetic_fidelity` | `narrow` | `AF-290`--`AF-301` identify projective distance to the boundary-zero discriminant, exhaust unrestricted complex-linear compression, classify genuine complex-affine dimension reduction, and show that positivity can still re-collapse under recurrent prime-phase conic spanning. | Prove an arithmetic admissibility condition that quantitatively bounds the fiberwise reachable correction set—normalization, discreteness, multiplicative coupling, sparsity or nonlinear geometry—or change the target observable. |
| `farey_discrepancy` | `narrow` | `FD-072`--`FD-079` progress from protected frontier records to full-counting abundance, zero-fixed-power-loss energy occupation, and finally a fixed positive logarithmic-mean lower bound for annular energy-weighted nonsquarefree occupation. | Control sparse exceptional endpoint scales, prove a pointwise/same-horizon lower bound, or show that positive logarithmic recurrence is already the exact recurrence currency needed downstream. |
| `mobius_cancellation` | `narrow` | `MC-244`--`MC-251` collapse blindness to one rough coset and progressively constrain torsion order and primary multiplicity; Hamming sphere packing now strengthens the rank deficit at every `ell^a` level beyond Singleton. | Use arithmetic structure of the residue-symbol matrix to kill the remaining fixed-low-order high-rank quotient, or obtain fixed-power signed cancellation of the complete rough Möbius coset. Generic coding bounds still allow dimension `n-o(n)`. |
| `nyman_beurling` | `narrow` | `NB-074`--`NB-078` reduce persistent continuation to an exact nonnegative information ledger. Along every `q`-adic refinement ray, uncancelled continuation is `J_m=J_0+sum(ell_r-c_r)`, where both increment families are explicit conditional-information charges. | Prove source-specifically that new continuation cannot systematically outrun new cross-cut prediction except by a summable loss, or localize enough prediction charge in mesoscopic bands. Branching alone supplies neither comparison. |
| `prime_circle` | `narrow` | Review-closed `PC-272` identifies the fixed-upper quotient spectrum with multiplicative-character autocorrelation and proves with the corrected `q=13` witness that ordered mesoscopic source information survives outside that quotient. | Show that the retained ordered/partial-window statistic survives normalization and matched controls and reaches an RH-sensitive target. Non-factorization alone does not establish endpoint relevance. |
| `prime_flute` | `continue` | `PF-303`--`PF-315` reduce the physical return problem to exact source/angle/defect factors. Internal energy-normalized sources automatically obey the Schur-defect gate, and the full `P/H` angle factorizes exactly into the direct `Z_LH` channel and the conditional `T_H V_H` channel. | Establish singular-value/ideal counting for `Z_LH` and `T_HV_H`, classify any genuinely external source or reassembly growth, and show that the final signed composition is controlled. There is no remaining arbitrary operator-norm source factor between the two physical channels. |
| `prime_lattice` | `narrow` | `PL-283`--`PL-288` combine finite-aperture certification with a simple isolated ground branch and show that tiny `L^2` tail localization still leaves bounded-shift uncertainty roughly nine orders above the certified positivity margin. | Prove state-specific weighted Fourier-tail regularity, an eigen-equation/source cancellation identity, or a direct finite interval certificate. More bookkeeping of the same one-stroke `L^2` reduction cannot close the transport gap. |
| `robin_extremal` | `narrow` | `RE-076`--`RE-081` identify one positive transport measure, force complementary power fronts on long same-block fans, and show that all discrete higher-layer prime-power atoms are negligible in both relevant moments. | Prove that the continuous adaptive selector-dilation measure on the genuine CA staircase cannot realize the required moving two-front profile, or rule out the long-stretch same-block branch; bounded-stretch remains separate. |
| `visual_exploration` | `narrow` | `VIS-188`--`VIS-194` separate absolute and signed dephasing and replace the worst prime-ratio gap by the coefficient-energy-weighted effective horizon `D_v=W_v/R_v`, giving uniform finite-start error controlled by `D_v/L`. | Prove `D_v=o(y^2)` or another weighted near-collision estimate from the actual prime-ratio coefficients, or show that the downstream target already supplies deterministic averaging windows much longer than `D_v`. |
| `weil_inertia` | `continue` | `WI-251`, `WI-253`, `WI-258`--`WI-262` retain source-specific first-crossing constraints while three independent audits now show that the public FP-0.35 paths do not certify the exact `L=7/20` premise; the common LDL judge itself admits an exact rational false positive. | Independently enclose the intended exact first-prime matrix with the correct Weil constant and certify positivity using a correct interval `LDL^T`/Cholesky or rigorous residual theorem. Only then test the one-prime phase cone. |
| `weil_positivity` | `narrow` | `WP-269`--`WP-274` rule out local/scalar sign repairs and classify the surviving nonlocal escape: Clark and inverse de Branges/canonical-system realizations can reproduce the normalized Mangoldt carrier tautologically when reconstructed from that carrier. | Derive the inner function, Hamiltonian, model space, matrix coupling or other nonlocal positive object independently from source/Mathia geometry, then prove that its pre-existing law reproduces Mangoldt, Gamma and polar sectors with the required orientation. |
| `xi_flow` | `narrow` | `XF-185`--`XF-186` give an `O(m)` holomorphic sample bank with uniform conditioning and a local nonlinear bi-Lipschitz chart for a known consecutive packet. | Solve unknown-support discovery and multiscale assembly, justify the complex samples from the physical Xi/Jacobi source, and transport recovered theta-side information to a zero-sensitive target. |

## Cross-line structure

**Exact elimination is replacing proxy improvement as the useful program-level move.** `PF-315` factors the full physical angle defect into two canonical channels, `NB-078` factors each refinement into nonnegative continuation/prediction information increments, and `RE-081` removes the entire discrete higher-layer atomic part of Robin's long-fan transport without changing the leading front. These are different operators and do not constitute a transferable theorem, but they all move the next question onto the smallest residual object that can still affect the target rather than another global norm or conditioning estimate.

**Source restriction only helps when it limits the reachable target corrections before the target is queried.** `AF-301` shows that a positive orthant can still recreate arbitrary complex corrections through recurrent prime phases. `PF-314` gives the positive counterpart: a source sector that is genuinely internal to the same positive form and uniformly normalized in its own energy automatically follows the Schur defect, so the dangerous inverse factor disappears for structural reasons. `WP-274` is the inverse warning: constructing the positive analytic space from the desired target measure reverses this causal order and therefore carries no independent arithmetic sign information.

**Aggregation has crossed from abundance to recurrence, but recurrence currencies remain target-specific.** `FD-079` supplies fixed positive logarithmic-mean occupation and rules out almost-every-scale subpower decay. `NB-078` supplies an exact scale-by-scale imbalance ledger, so a stable tail must pay fresh excess continuation indefinitely. Robin's long branch instead requires a moving continuous transport front. These are substantially stronger than existence of signal, but none can be substituted for another without proving that the final RH criterion consumes the corresponding logarithmic, refinement, or transport currency.

**Generic combinatorial or norm bounds are reaching a dense-residual plateau.** `MC-251` improves generic coding pressure while still leaving a near-full-dimensional low-order primary code; `PL-288` gives extremely small `L^2` tail mass while remaining far above the spectral margin in the shift metric; `VIS-194` replaces worst-case spacing with the correct energy-weighted spacing but leaves its arithmetic size open. In each case the next gain must use source-specific structure of the residual rather than another refinement of the same ambient bound.

**Certification remains logically separate from numerical plausibility.** `WI-262` adds a judge-level exact false-positive mechanism to the object/enclosure failures of `WI-260`--`WI-261`. A positive numerical margin cannot enter downstream first-prime reasoning until both the intended operator and the positivity predicate are independently certified.

The existing proposed `CLUE-global-cross-line-fidelity-realization-separation` already carries the program-level distinction between source fidelity, target transport, destination realization, and assembly. The new correspondences above refine that accounting but do not yet provide one shared source family/operator/normalization/target on which a destination-local transfer is both exact and falsifiable, so no additional Master clue is warranted.

The Graph-owned Riemann Atlas is currently stale for this pass: its entrypoint and metrics advertise different old source revisions (`4bc527...` and `834376...`), both far behind current canonical evidence. Atlas telemetry is therefore navigation context only and does not support any portfolio recommendation here.

## Highest-leverage unresolved questions

1. **Weil Inertia exact activation certificate.** Replace the audited FP-0.35 matrix assembly and defective LDL judge with an independently exact theorem-object enclosure and a rigorously correct positivity certificate. If it succeeds, the one-prime phase cone becomes established input; if it fails, the current shortcut is dead cleanly.
2. **Prime Flute physical-angle singular structure.** Use `PF-315` to control singular-value/ideal counting for the two canonical components `Z_LH` and `T_HV_H`, then carry that control through the actual reassembly/sign map. `PF-314` already removes internal energy-normalized diagonal source amplification as an independent mechanism.
3. **Analytic Frontier source continuation.** Prove that the actual prime-minus-rough source cannot realize the matched late packet after the sharp `Theta(X/a_X)` aperture, with positive-cascade non-excisability kept as a distinct final gate.
4. **Farey exceptional-scale/endpoint bridge.** Turn `FD-079`'s constant logarithmic-mean occupation into the pointwise, local or recurrence statement the final endpoint theorem actually consumes, or prove directly that zero-log-density exceptional dips are harmless.
5. **Nyman refinement-balance theorem.** Control the `NB-078` increments so that `ell_m<=c_m+e_m` with summable total loss on one unbounded refinement ray, or obtain an equivalent mesoscopic recovery theorem.
6. **Residual source structure beyond generic bounds.** For Möbius, kill the fixed-low-order high-rank blind quotient or cancel its exact rough coset; for Robin, exclude the continuous selector-dilation front; for Weil Positivity, independently select the nonlocal positive object before matching the target carrier.

## Portfolio changes proposed

No recommendation change is proposed. `FD-079` is a genuine advance because Farey now has constant logarithmic-mean occupation rather than merely subpower non-vanishing, but the endpoint bridge is still unproved. `NB-078` gives Nyman an exact incremental sufficient condition, but no arithmetic estimate yet controls the continuation/prediction imbalance. `PF-314`--`PF-315` strengthen Prime Flute's `continue` status by removing arbitrary source-map ambiguity and exposing the exact two-channel physical-angle problem. `WI-262` weakens the evidentiary status of the existing finite certificate but leaves a cheap, decisive recertification gate, so demotion is premature. `RE-081`, `AF-301`, `MC-251`, and the strengthened `WP-274` narrow their respective escape routes without exhausting them.

No Research Watch allocation change is justified. The scientific portfolio contains promising dormant directions, but the task-rotation policy requires a strong active `pause-candidate` or `merge-candidate` before freeing a slot; none exists in current evidence.