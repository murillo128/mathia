# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Control the shrinking positive-frequency endpoint of the exact Xi Volterra field

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet` and `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`.

XF-041--XF-047 isolate the actual memory-scale obstruction: a critical `q~log^2 T` nonlinear memory wave survives every fixed heat-time interval at critical flux scale while passing the source-counting tolerances used by the earlier argument. XF-048 supplies the source distinction: the Guinand--Weil explicit formula leaves a fixed prime-free Fourier gap below `log 2/2`, and a memory-band probe has `o(1)` response on the actual endpoint while the coherent control has an order-one response.

XF-049 gives the finite one-sided Volterra evolution. XF-050 removes the apparent reality/collision obstruction: the symmetric finite zero field obeys the same triangular law for complex roots and through multiple-root collisions, and a compactly bandlimited endpoint probe can kill prime-power frequencies exactly even off RH. XF-051 then removes the qualitative infinite-renormalization obstruction. The horizontal logarithmic derivative on any zero-free line defines a canonical positive-frequency tempered distribution, independent of the auxiliary height, whose Fourier evolution is exactly Volterra-triangular on `(0,infinity)`.

The remaining theorem is now genuinely quantitative and endpoint-local. The memory band shrinks to `xi~1/log T`, so one must decompose the exact infinite field near `xi=0` into deterministic archimedean/background and fluctuation parts and prove that the lower-positive-band quadratic input contributes only `o(1)` to the moving endpoint statistic over the relevant heat interval. A decisive negative must exhibit an order-one contribution from the zero-frequency singularity or frequencies **below** the memory band; complex roots, collisions, opposite-frequency mixing, and raw infinite-sum nonexistence are no longer structural excuses.

## Convert source-rigid Cauchy geometry into signed tapered derivative control

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information`, `MI-002-scale-free-gap-dynamics-still-need-an-xi-specific-law`, `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`, and `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`.

Once the endpoint selector is propagated through the shrinking positive-frequency band and excludes the critical slow mode, the conditional flux/Cauchy rigidity of XF-036--XF-038 becomes usable at the needed scale. The next bridge must convert that source-rigid long-range geometry into the signed fixed-time coercive estimate required by the zero-side argument without reintroducing a finite-jet, periodic, counting-only, or unrenormalized zero-sum surrogate.

## Add a new nonlocal observable only if the exact half-line transport cannot close the source gap

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet` and `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`.

The earlier qualitative transport uncertainties are largely gone: endpoint source selection is exact on a compact one-sided band; finite transport survives complex collisions; and the infinite positive-frequency carrier exists canonically as a horizontal-log-derivative distribution. A new observable is justified only if the remaining `xi downarrow 0` background/fluctuation estimate fails structurally. Adding more static near-lattice summaries would not address that boundary.

## Match analytic-frontier input to the mesoscopic carrier without collapsing its horizontal information

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information` and `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

Any handoff from zero-side scalar/coherence information must survive the `log^2 T` memory window, collision-safe normalization, and long-range Cauchy coupling used by Xi flow. A local or fixed-scale certificate that disappears under this mesoscopic assembly is not a usable source term.
