# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history.

## Lift the positive-time tangent selector frame to the nonlinear Xi transition problem

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information`, `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`, and `MI-005-pointwise-selector-control-is-not-time-frequency-coercivity`.

XF-052--XF-061 show why static pointwise, unweighted, and derivative-weighted slow-frequency norms fail on progressively sharper matched controls, culminating in a sparse root defect whose information lies outside the shrinking slow cone. XF-062 changes that conclusion at fixed positive tangent heat time: the exact periodic lattice semigroup damps the high-frequency escape and forces any surviving critical triple flux into the source-controlled slow band. XF-063 then proves an exact lower frame from the derivative-weighted moving-line selector family to the same tangent `H^3` flux energy by disjoint Fourier sidebands.

The tangent spectral-support and frame gaps are therefore closed. The live burden is nonlinear/dynamical: show that a relevant finite-amplitude transition defect can be compared to this tangent regime after positive time, that critical transition flux cannot disappear before the comparison becomes valid, and that the argument survives nonperiodicity, collisions, or complex-root intervals.

## Identify the nonlinear destination quantity corresponding to the tangent `H^3` frame

At positive tangent time the derivative-weighted selector norm is already destination-matched. A nonlinear successor must reduce to that frame near the lattice while controlling the errors generated by finite gap distortion and any collision-safe coordinate change. Adding more slow-frequency weights without such a nonlinear bridge is no longer the main issue.

## Match analytic-frontier input without averaging away horizontal information

Any upstream certificate must retain the microscopic horizontal/local information needed to enter the positive-time Xi comparison and rule out transition defects, not only averaged low-frequency control.
