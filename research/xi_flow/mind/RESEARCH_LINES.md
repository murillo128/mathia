# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history.

## Derive source-faithful antipodal/complex reach or exclude the maximal-contact packet

**Linked intuitions:** `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`, `MI-006-periodic-vieta-coordinates-diagonalize-the-nonlinear-heat-flow`, `MI-007-ultra-infrared-vieta-modes-should-be-quotiented-by-destination-energy`, `MI-008-gaussian-reference-localization-is-relatively-exact-but-not-global`, `MI-009-guarded-energy-can-enforce-static-real-divisor-feasibility`, `MI-010-endpoint-renormalization-is-source-fixed-before-discretization`, `MI-011-cross-scale-observability-can-compress-to-one-off-circle-heat-trace`.

XF-124--XF-148 show that exact state recovery can be easy while the de Bruijn--Newman transition remains exponentially ill-conditioned unless observation reaches a shrinking antipodal layer. XF-149--XF-151 close the main derivative/distributional loopholes for the same maximal-contact family. Root-scale derivatives trade at most one suppression power per derivative; the complete finite-degree jet remains exponentially blind off the layer; and even the infinite normalized analytic jet remains blind on the real axis.

Complex continuation introduces a precise reach tradeoff rather than an automatic escape. A collar of depth `h_N` can compensate real antipodal deficit only against the exponential-type cost, and near the antipode the current certificate has parabolic scale `h_N ~ d_N^2/L`. The live theorem is therefore source-faithful access to sufficient antipodal/deep-complex reach with controlled normalization, or a source theorem proving that Xi cannot realize the maximal-contact packet or an equivalent transition-sharp direction.

## Separate state identifiability, derivative richness, and transition conditioning

More derivatives, full future time, disconnected sensors, exact Vandermonde recovery, or shallow off-circle continuation are controls unless they improve the inverse modulus for `Lambda_per` itself. The current matched family survives all of those enlargements within the quantified reach regime.

Future results should state the source class, spatial/complex support, derivative normalization, observation weight, and transition modulus explicitly. Reaching the current visibility boundary only removes this particular obstruction; it is not a sufficiency theorem for stable reconstruction.

## Keep q-scale and prime-band routes separate

The q-scale source cone and the prime-power distributional source live in different regimes. A prime-band continuation must derive its own source-to-destination interface rather than importing q-scale observability controls.