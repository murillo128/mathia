# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Derive the borderline `V_M=O(1/M)` flux budget from the actual Xi dynamics

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information` and `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

XF-032--XF-034 identify geometric gap ramps as universal finite-range null modes and show that sufficiently coherent Xi ramps must flatten. XF-035--XF-037 now close the static source-coercivity bridge much further: once the total triple-flux variation satisfies `M V_M=O(1)`, translated super-mesoscopic Xi counting forces uniform gap flattening and vanishing total logarithmic gap variation. The fixed-amplitude tent obstruction belongs only to the reduced two-span data package and is not source-admissible.

The live theorem is therefore dynamical rather than another near-kernel estimate. Derive `V_M=O(1/M)`, or an equivalent compactness resource, from the exact `L_lambda`/`L_w` interaction on the active `M=R(T)log^2 T` buffer while retaining collision-safe control.

## Convert the source-rigid near-lattice regime into a signed tapered derivative

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information`, `MI-002-scale-free-gap-dynamics-still-need-an-xi-specific-law`, and `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

Under the borderline flux budget, XF-036--XF-037 leave only sparse/sub-super-mesoscopic order-`1/M` contrasts; macroscopic folds and positive-density near-lattice waves are excluded. A positive continuation must combine this rigidity with XF-028 collision-positive overlap and the exact nonlinear taper product rule to obtain a height-uniform favorable sign or coercive heat-time budget.

A decisive negative must exhibit a **dynamically source-compatible sparse microfold or `L_lambda`/`L_w` misalignment** that survives translated counting, collision coverage, and the full tapered derivative. Static tents are no longer a relevant adversary.

## Add a nonlocal/source-sensitive observable only if the long-range dynamics cannot control the borderline flux budget

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

XF-033 closes the idea that increasing a fixed local block size repairs the affine log-gap blind mode. If the `L_lambda`/`L_w` route cannot deliver `V_M=O(1/M)`, any added observable must break finite-range locality, translation-invariant sliding, or common-scale invariance for a source-justified reason and still retain the mesoscopic `log^2 T` dynamical scale.

## Match analytic-frontier inputs to the mesoscopic carrier

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

Any imported zero-statistics theorem must retain width `~log^2 T`, the relevant reciprocal-gap/conductance or flux-variation norm, translated-span information, and enough precision to interact with the `1/M` borderline resource. Fixed finite ordered-gap improvements are insufficient by themselves.
