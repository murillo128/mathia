# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history.

## Treat the source-to-periodic bridge as closed at the current guarded scales

**Linked intuitions:** `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`, `MI-006-periodic-vieta-coordinates-diagonalize-the-nonlinear-heat-flow`, `MI-007-ultra-infrared-vieta-modes-should-be-quotiented-by-destination-energy`, `MI-008-gaussian-reference-localization-is-relatively-exact-but-not-global`, `MI-009-guarded-energy-can-enforce-static-real-divisor-feasibility`, `MI-010-endpoint-renormalization-is-source-fixed-before-discretization`.

XF-087--XF-094 isolate the periodic Volterra model, source-fixed pole subtraction, reference quadrature, and the deterministic linear endpoint floor. XF-095 identifies the positive-time correction forced by the source: the residual develops a universal `xi log^2 xi` cusp whose coefficient is fixed, and subtracting that dynamic endpoint jet makes both the corrected Xi residual and corrected reference defect guarded-small.

XF-096 supplies the mesh-scale anti-concentration that XF-093 had shown could not be inferred from generic fixed-strip quotient accuracy. The actual corrected Xi residual has the required endpoint `C^1` control, so its sampled guarded resource is `o(1)`. XF-097 then closes the remaining finite-state interface: the entire source endpoint prefix has normalized `ell^1=o(1)`, admits an exact equal-weight real periodic realization, and shadows the canonical Xi Volterra trajectory for fixed heat time in the same guarded resource.

At the current asymptotic scales, source extraction, endpoint renormalization, mesh sampling, full-prefix realization, and continuum-to-periodic stability are therefore no longer the primary frontier. Reopen one of those gates only if a later destination observable requires a different topology or scale.

## Force a collective Xi-specific transition signal in the same guarded quotient

XF-098 gives an exact matched transition control against the simplest destination theorem. A real-rooted periodic heat trajectory can have a genuine threshold at one isolated simple double collision while its whole guarded low-frequency prefix stays `o(1)` for every fixed positive heat time under the full nonlinear log-Vieta dynamics.

Therefore the remaining positive-`Lambda` coercivity theorem cannot follow from the existence of a collision, real-root preservation, or exact periodic heat alone. It must show that the **actual Xi transition geometry forces a collective defect** that cannot be represented by a bounded sparse displacement of the reference lattice, or it must replace the guarded observable by one that detects the missing transition information while retaining the source bridge.

## Quantify what collective transition geometry must survive the guarded bandwidth

The live destination question is now to identify a source-faithful resource that a hypothetical `Lambda>0` transition must make macroscopic after all source-fixed reference sectors have been removed. Candidate mechanisms may involve a sufficiently large/correlated family of near-collisions, a nonlocal zero-displacement pattern, or another Xi-specific constraint, but none should be promoted without a theorem linking it to nonvanishing guarded mass.

The decisive comparison is against XF-098 and its sparse-displacement extension: a proposed transition invariant must fail on those matched periodic controls for a reason supplied by the Xi source rather than by an arbitrary choice of coordinates.

## Keep transition coercivity separate from the already-closed transport theorem

A successful source-to-grid theorem does not imply `Lambda<=0`; XF-097 closes that transport without supplying destination mass, and XF-098 shows why an isolated collision is insufficient. Conversely, failure of a candidate destination observable does not reopen the corrected source bridge. Future results should state explicitly whether they change source admission, transport stability, or transition coercivity.