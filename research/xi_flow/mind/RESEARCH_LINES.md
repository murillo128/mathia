# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history.

## Prove the Xi-to-periodic interface in the already-correct weighted log-Vieta resource

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information`, `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`, `MI-005-pointwise-selector-control-is-not-time-frequency-coercivity`, `MI-006-periodic-vieta-coordinates-diagonalize-the-nonlinear-heat-flow`, and `MI-007-ultra-infrared-vieta-modes-should-be-quotiented-by-destination-energy`.

XF-052--XF-069 solve the periodic support/frame, collision-safe Vieta diagonalization, Newton conditioning, and exact translated-center extraction problems. XF-070 identifies the exact selector-induced weighted log-Vieta `H^3` resource and proves that the complete source-unresolved ultra-infrared block is asymptotically null in that destination-matched norm under bounded displacement.

XF-071 closes the nonlinear periodic transport concern. The exact triangular log-Vieta heat system plus a growing guard band prevents bounded unknown infrared modes from repopulating the standard transition band over fixed positive heat time. The remaining source-to-state gap is therefore not raw low-mode control or nonlinear Vieta mixing.

XF-072 closes the simplest localization repair: increasing the periodic aspect ratio reduces seam exposure by exactly the same `1/R` factor by which full-period center averaging dilutes the local transition frame. Period dilation cannot simultaneously buy a negligible generic interface and retain order-one local coercivity. The decisive theorem must exploit Xi-specific analytic cancellation in the interface or replace full-period center extraction by a genuinely different interior-localized construction with its own conditioning bound.

## Identify a transition state that remains nontrivial in the weighted quotient

The periodic bridge now says what source resource would be sufficient if the actual Xi selector can be transferred into it. A useful positive-transition theorem must also show that a relevant `Lambda>0` state carries order-one mass in this same weighted/guarded quotient; collision-safe diagonalization alone does not provide that nontriviality.

## Match analytic-frontier input without averaging away horizontal information

Any upstream certificate must retain the microscopic horizontal/local information needed by the Xi interface and transition theorem. Averaged low-frequency control is insufficient if the localization step or destination frame requires source-specific center dependence.
