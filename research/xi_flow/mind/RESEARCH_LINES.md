# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history.

## Move beyond slow-frequency selector norms to a spatially local source-to-transition control

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information`, `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`, and `MI-005-pointwise-selector-control-is-not-time-frequency-coercivity`.

XF-052--XF-057 close pointwise slow-frequency routes; XF-058 repairs the chirp with unweighted square aggregation; XF-059 extends the Xi source estimate far into the infrared; XF-060 then shows derivative calibration is required for high-slow pure waves. XF-061 closes that weighted slow-selector candidate as a general inverse norm: moving a single root by `kappa/M` gives order-one normalized triple-flux variation while the entire derivative-weighted energy on the shrinking slow cone tends to zero.

The new obstruction is spectral support rather than frequency calibration. Compact local defects store the relevant `L^1` flux variation outside the slow cone. A surviving source-to-transition theorem must therefore include spatial localization, sufficiently broad/high-frequency information, a direct local defect norm, or Xi dynamics that forbids such sparse root defects. Reweighting the same shrinking slow cone cannot be the whole mechanism.

## Identify a destination norm that sees both distributed and sparse critical flux

Any candidate must simultaneously survive the XF-057 chirp, the XF-060 high-slow wave, and the XF-061 single-root defect while remaining small on the transported Xi source. This likely requires a genuinely phase-space/multiscale object rather than a single frequency-band square function.

## Match analytic-frontier input without averaging away horizontal information

Any upstream certificate must retain the microscopic horizontal/local information needed to rule out sparse transition defects, not only averaged low-frequency control.