# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history.

## Bypass instantaneous local jet tomography, including linear-depth and mixed heat jets

**Linked intuitions:** `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`, `MI-006-periodic-vieta-coordinates-diagonalize-the-nonlinear-heat-flow`, `MI-007-ultra-infrared-vieta-modes-should-be-quotiented-by-destination-energy`, `MI-008-gaussian-reference-localization-is-relatively-exact-but-not-global`, `MI-009-guarded-energy-can-enforce-static-real-divisor-feasibility`, `MI-010-endpoint-renormalization-is-source-fixed-before-discretization`, `MI-011-cross-scale-observability-can-compress-to-one-off-circle-heat-trace`.

XF-124--XF-139 establish exact periodic observability while showing that complete future/local-patch tomography, zero-delay local data, and every sublinear logarithmic jet can be exponentially transition-blind. XF-140 pushes this much further: order-wise Cauchy radii hide even the **full degree-order spatial logarithmic jet** at a fixed linear exterior height strictly closer to the divisor than the previous common-radius bound.

XF-141 shows that a large mixed space-time jet can agree exactly between transition-separated matched controls. XF-142 then identifies the structural reason: after a universal gauge, the local trace obeys the one-dimensional heat equation, so every instantaneous time derivative is exactly a spatial derivative in disguise. The mixed parabolic `K`-jet carries no more information than the pure spatial `K`-jet at the same slice.

The live theorem must therefore use a **transition-specific quotient**, a source theorem excluding the central packets, genuinely nonlocal information, or observation across independent slices/geometries whose data are not generated from one spatial jet by the flow PDE. Adding instantaneous time derivatives to local exterior tomography is closed.

## Keep q-scale and prime-band routes separate

The q-scale source cone can be coercive and realizable while target observability remains unstable. A prime-band continuation begins in a different source regime with distributional prime-power atoms and must derive a new source-to-destination interface rather than importing q-scale controls.

## Separate exact identifiability from target conditioning and coordinate count from information count

The periodic divisor remains algebraically reconstructible from tiny data, but exact recovery is not stable transport. Likewise, a large mixed derivative grid need not contain many independent coordinates when the evolution equation algebraically collapses it.

Future results should state whether they improve source access, transition-specific conditioning, spatial aperture, independent temporal information, or the actual modulus controlling `Lambda`.