# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history.

## Build a weighted/quotient Vieta bridge and pay only the nonperiodic interface error

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information`, `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`, `MI-005-pointwise-selector-control-is-not-time-frequency-coercivity`, `MI-006-periodic-vieta-coordinates-diagonalize-the-nonlinear-heat-flow`, and `MI-007-ultra-infrared-vieta-modes-should-be-quotiented-by-destination-energy`.

XF-052--XF-067 solve the positive-time periodic support/frame problem and diagonalize the full periodic zero heat flow in collision-safe Vieta coordinates. XF-068 removes the feared growing Newton-map obstruction: uniformly source-small raw power sums produce uniformly small low Vieta coefficients with no mode-count amplification, and after fixed positive heat time the inverse map loses only a fixed polynomial power of `log T` on the whole slow cone.

XF-069 removes taper inversion as well. Translating the localized selector center through one periodic block recovers each source-visible raw power sum by an exact Fourier coefficient with a condition number independent of the growing period. The unresolved localization burden is therefore a center-averaged Xi/periodic interface mismatch, not inversion of the compact window.

The same finding exposes the real gap: the source selector cannot reach the ultra-infrared fixed Vieta modes, those modes are essentially undamped at fixed positive heat time, and an explicit periodic control shows they can remain order one while the destination third-difference transition energy vanishes. The next bridge should therefore quotient or weight away this destination-harmless sector and recover only the Vieta content capable of carrying order-one transition energy.

## Prove a center-averaged Xi-to-periodic interface theorem on the source-visible band

For Vieta indices above the selector-resolution edge, XF-069 gives an exact extraction formula once a periodic surrogate is available. The decisive source-local theorem is to bound the normalized center-averaged mismatch between the actual moving-line Xi statistic and that surrogate uniformly across the `k`-band consumed by the weighted Vieta state. Boundary continuation, not Newton conditioning or taper inversion, is now the load-bearing localization error.

## Identify a transition state that remains nontrivial in the destination-weighted Vieta resource

Collision-safe diagonalization does not by itself supply the source information or prove a positive de Bruijn--Newman transition. A useful theorem must connect the arithmetic/moving-line selector to the weighted Vieta quotient, preserve a nontrivial third-difference-scale quantity through positive heat time, and show that a relevant Xi transition state reaches the XF-065--XF-066 destination criteria.

## Match analytic-frontier input without averaging away horizontal information

Any upstream certificate must retain the microscopic horizontal/local information needed to enter the positive-time Xi comparison and rule out transition defects, not only averaged low-frequency control.
