# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history.

## Bypass local exterior tomography, including every sublinear logarithmic jet

**Linked intuitions:** `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`, `MI-006-periodic-vieta-coordinates-diagonalize-the-nonlinear-heat-flow`, `MI-007-ultra-infrared-vieta-modes-should-be-quotiented-by-destination-energy`, `MI-008-gaussian-reference-localization-is-relatively-exact-but-not-global`, `MI-009-guarded-energy-can-enforce-static-real-divisor-feasibility`, `MI-010-endpoint-renormalization-is-source-fixed-before-discretization`, `MI-011-cross-scale-observability-can-compress-to-one-off-circle-heat-trace`.

XF-124--XF-133 establish exact periodic observability and then show that complete future/local-patch state tomography can be exponentially ill-conditioned. XF-134--XF-139 push the obstruction into the observation interface itself. Central matched controls can remain transition-sharp while zero-delay local heat data, logarithmic jets, and moving exterior probes are exponentially insensitive.

XF-139 gives the current boundary: **every sublinear raw logarithmic jet `K_N=o(N)` can be hidden while the observation height is only `Theta(K_N)` root-spacing units and hence tends to zero as a fraction of the physical period**. Moving the probe arbitrarily close to the divisor on the macroscopic scale therefore does not make sublinear local jet data a stable transition coordinate.

The live theorem must use a transition-specific quotient not requiring reconstruction of those hidden directions, a source theorem excluding the central packets, or genuinely linear-scale/nonlocal information with a quantitatively favorable modulus. Adding more finite or sublinear exterior derivatives is closed.

## Keep q-scale and prime-band routes separate

The q-scale source cone can be coercive and realizable while target observability remains unstable. A prime-band continuation begins in a different source regime with distributional prime-power atoms and must derive a new source-to-destination interface rather than importing q-scale controls.

## Separate exact identifiability from target conditioning

The periodic divisor remains algebraically reconstructible from tiny data, but exact recovery is not a transport theorem. Future results should state whether they improve source access, transition-specific conditioning, spatial aperture, derivative depth, or the actual modulus controlling `Lambda`.