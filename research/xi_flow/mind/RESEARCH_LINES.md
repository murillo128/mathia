# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Convert the continuous endpoint selector family into a coercive transition-side norm

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information`, `MI-002-scale-free-gap-dynamics-still-need-an-xi-specific-law`, `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`, `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`, and `MI-005-pointwise-selector-control-is-not-time-frequency-coercivity`.

XF-052--XF-054 establish the prime-free endpoint normal form and uniform fixed-time transport of the shrinking memory statistic. XF-055--XF-056 strengthen this from discrete periodic harmonics to a continuous slow-frequency cone: every pure critical slow wave has an order-one matched control response while the actual Xi carrier is uniformly `o(1)` throughout the cone.

XF-057 then identifies the remaining loss exactly. A slowly chirped finite-gap control can distribute critical triple-flux variation over many `1/M` selector cells so that **every individual selector coefficient is uniformly `o(1)`**, even while the transition-side quantity remains at its borderline nonvanishing scale. Taking a supremum over frequency centers is therefore not a coercive inverse theorem.

The live bridge must aggregate the selector family in a time-frequency/square-function/frame norm, or use additional Xi dynamics that forbids chirped distribution of the shape budget. The decisive theorem must control the exact transition-side signed flux at the `q~log^2 T` scale; another pointwise-frequency estimate is not enough.

## Identify the exact source-to-transition transform that can preserve aggregate information

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet` and `MI-005-pointwise-selector-control-is-not-time-frequency-coercivity`.

Frequency detuning is closed by XF-056, but frequency spreading is not. Determine the precise transform or inequality carrying the prime-free endpoint sector into the Cauchy/flux observable and ask whether it admits a lower-frame, square-function, variation, or phase-space estimate on source-conditioned Xi configurations. A new nonlocal observable is justified only if this handoff is structurally noncoercive in the existing selector category.

## Match analytic-frontier input to the mesoscopic carrier without averaging away horizontal information

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information` and `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

Analytic Frontier now bounds the full Montgomery--Taylor five-point geometry, including the common-translation direction. Any upstream certificate used here must preserve horizontal information across the `log^2 T` memory window and the long-range Cauchy coupling rather than collapse it to a local scalar average.
