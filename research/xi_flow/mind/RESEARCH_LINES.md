# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Propagate the endpoint prime-free memory selector through localized Xi heat flow

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet` and `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`.

XF-041--XF-047 isolate the actual memory-scale obstruction. Universal Cauchy interaction damps short-period structure and remote forcing, but an exact `q~log^2 T` nonlinear memory wave at amplitude `kappa/q^2` survives every fixed heat-time interval at critical flux scale while passing the source-counting tolerances used by the earlier argument. Counting, gap envelopes, and universal repulsion therefore do not exclude the remaining mode.

XF-048 supplies the missing endpoint distinction. The Guinand--Weil explicit formula leaves a fixed prime-free Fourier gap below the first prime-power line `log 2/2`; a width `Theta(log^3 T)` Gaussian probe isolates the memory frequency `Theta(1/log T)`. The actual endpoint zero set has `o(1)` response while the XF-047 coherent wave has a nonzero limit. XF-049 then shows that every finite real-simple heat-zero system has a one-sided Volterra Fourier evolution whose low positive frequencies cannot be generated directly by high-positive/high-negative down-conversion, and whose linear memory damping exactly matches the XF-047 clock.

The decisive theorem is now a transport/localization statement: prove that this one-sided low-frequency structure survives the infinite Xi renormalization and the `Theta(log^3 T)` taper with `o(1)` commutator/buffer error over fixed heat time, so the endpoint source selector controls the entire relevant interval. A decisive negative must exhibit a source-compatible localized mechanism that creates the compensating low-frequency coefficient despite that triangular structure.

## Convert source-rigid Cauchy geometry into signed tapered derivative control

**Linked intuitions:** `MI-001-collision-safe-coordinates-preserve-the-bulk-signal`, `MI-002-scale-free-gap-dynamics-need-xi-specific-law`, `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`, and `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`.

Once the endpoint selector is transported through the memory interval and excludes the critical slow mode, the conditional flux/Cauchy rigidity of XF-036--XF-038 becomes usable at the needed scale. The next bridge must convert that source-rigid long-range geometry into the signed fixed-time coercive estimate required by the zero-side argument without reintroducing a finite-jet, periodic, or counting-only surrogate.

## Add a new nonlocal observable only if localized Fourier transport cannot close the source gap

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet` and `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`.

The previous need for unspecified near-buffer cancellation has become much more precise: the endpoint source already kills the coherent critical memory wave, and finite heat flow already has the correct one-sided frequency architecture. A new observable is justified only if the infinite/tapered transport theorem fails structurally; adding more static near-lattice summaries would not address the remaining localization/renormalization boundary.

## Match analytic-frontier input to the mesoscopic carrier without collapsing its horizontal information

**Linked intuitions:** `MI-001-collision-safe-coordinates-preserve-the-bulk-signal` and `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

Any handoff from zero-side scalar/coherence information must survive the `log^2 T` memory window, collision-safe normalization, and long-range Cauchy coupling used by Xi flow. A local or fixed-scale certificate that disappears under this mesoscopic assembly is not a usable source term.
