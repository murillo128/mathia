# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history.

## Treat the source-to-periodic bridge as closed only at the current guarded, prime-free bandwidth

**Linked intuitions:** `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`, `MI-006-periodic-vieta-coordinates-diagonalize-the-nonlinear-heat-flow`, `MI-007-ultra-infrared-vieta-modes-should-be-quotiented-by-destination-energy`, `MI-008-gaussian-reference-localization-is-relatively-exact-but-not-global`, `MI-009-guarded-energy-can-enforce-static-real-divisor-feasibility`, `MI-010-endpoint-renormalization-is-source-fixed-before-discretization`.

XF-087--XF-097 isolate the periodic Volterra model, source-fixed pole/dynamic-cusp subtraction, reference quadrature, endpoint regularity, exact real-prefix realization, and fixed-time guarded shadowing. At the current scales, source extraction, endpoint renormalization, mesh sampling, full-prefix realization, and continuum-to-periodic stability are therefore closed in the exact guarded norm.

XF-100 identifies the boundary of that success. The live cutoff `K asymp q log log T` satisfies `K Delta -> 0`, so the entire sampled endpoint prefix lies below the first prime frequency `lambda_2=(log 2)/2` and is purely archimedean/polar. Because the periodic Vieta prefix is exactly autonomous and XF-097 shadows it at fixed time, the actual Xi guarded trajectory is `o(1)`-close to a trajectory generated from **no prime data**. Reaching the first arithmetic atom requires index `K_prime asymp Delta^(-1) asymp q^(3/2)`, where the present small-`K Delta` stability and point-sampling interface both fail.

Do not reopen the current low-band transport gates unless a later observable changes the topology or scale. A prime-scale route is a new source-interface problem: it must handle distributional prime-power atoms and replace the current perturbative shadow estimate rather than merely enlarging `K`.

## Force Xi-specific symmetry breaking, not merely a collective collision signal

XF-098 shows that an isolated simple periodic collision threshold can remain guarded-small. XF-099 closes the obvious collective repair: the exact family `(w^M-1)^2` has `M=N/2` simultaneous simple double collisions at its transition, yet every power sum below the symmetry frequency `M` vanishes identically for the entire heat trajectory. Even maximal collision participation and `O(1)` multiplicity-counting discrepancy from the fine lattice need not create any visible guarded mass.

Therefore a positive-`Lambda` coercivity theorem in the current state space must show that the **actual Xi transition forces visible symmetry breaking/aperiodicity** that cannot organize above the guarded bandwidth. Collision count, density, total bounded lattice displacement, and generic real-rooted heat flow are insufficient by themselves.

## Decide the low-band versus prime-band fork explicitly

There are now two mathematically distinct destination routes. Staying at `K asymp q log log T` requires a theorem using the archimedean low-frequency trajectory itself: together with a hypothetical positive transition, it must force nonvanishing distance from the collision-invisible periodic cone for an Xi-specific reason. Prime atoms cannot be invoked as hidden data that the triangular heat flow will later bring down into the prefix.

Alternatively, redesign the source-to-grid observable to reach `xi=Theta(1)`, beginning at `lambda_2`, while handling atomic explicit-formula data and proving a new stability/resource estimate at `K asymp q^(3/2)`. The decisive test for either route is a matched periodic control: the claimed coercive resource must fail on XF-098/XF-099 for a source-faithful reason rather than by coordinate choice.

## Keep transition coercivity separate from transport at each declared scale

A successful source-to-grid theorem does not imply `Lambda<=0`, and a failed destination observable does not invalidate a source bridge proved in a weaker bandwidth. Future results should state explicitly whether they change source admission, transport stability, observable bandwidth, or transition coercivity, and should not transfer conclusions across those gates without a theorem.
