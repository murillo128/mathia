# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Assemble collision-safe symmetric discriminants beyond fixed-stencil mean removal

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information` and `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

Finite collision jets and fixed ordered-gap centered quadratic repairs are universal or collision-unstable. XF-026--XF-028 reopen the localization question in a different category: exchange-symmetric root observables have removable repulsion poles; normalized block discriminants have square internal production and cancel affine exterior fields; overlapping them makes every positively covered collision have a dominant positive `1/epsilon^2` contribution.

The live theorem is now global rather than local: choose a noncompact/mesoscopic overlap and normalization that covers all relevant collision walls while controlling support edges and finite non-affine exterior flux. The construction must still pass matched backward-heat controls because its collision algebra is universal.

## Organize near-buffer and non-affine exterior flux after the far exterior has been controlled

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information`, `MI-002-scale-free-gap-dynamics-still-need-an-xi-specific-law`, and `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

The fixed-time core has `Theta(log^2 T)` gaps and physical span `Theta(log T)`. Source-valid super-mesoscopic buffers make aggregate far-tail leakage `o(1)`. The normalized discriminant additionally annihilates affine exterior drift inside each block, so the remaining burden is the near buffer, support edges, and genuinely non-affine exterior curvature.

A positive must convert source information into a height-uniform bound on that residual flux rather than estimate each boundary term absolutely.

## Assemble bulk coercivity and boundary capacity into fixed backward time

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

The proof must combine finite-amplitude bulk dissipation, slowly growing buffers, collision-positive overlap coverage, and a source-specific estimate for the residual near/exterior flux. A negative should build source-compatible collective configurations saturating that aggregate boundary.

## Match analytic-frontier inputs to the mesoscopic carrier

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

Any imported zero-statistics theorem must retain width `~log^2 T`, the relevant norm, and enough span/exterior information at `log^-2 T` dynamical precision. Fixed finite ordered-gap improvements are insufficient by themselves.
