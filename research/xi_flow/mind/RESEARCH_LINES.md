# Xi-flow research lines

This file holds the current mathematical questions suggested by the durable Xi-flow intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Convert the uniform endpoint memory exclusion into transition-side signed coercivity

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information`, `MI-002-scale-free-gap-dynamics-still-need-an-xi-specific-law`, `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`, and `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`.

XF-048--XF-054 now close the source-selector transport problem at the matched-statistic level. The endpoint positive-frequency carrier has an explicit prime-free background below `log 2/2`; higher-frequency arithmetic data form a one-sided convolution ideal and cannot repopulate that band at any fixed heat order; and a moving high-line argument gives

`sup_{0<=t<=t_0}|S_T(t)|=o(1)`

for the actual shrinking XF-050 memory probe over every fixed heat interval. Complex roots, collisions, raw infinite sums, endpoint finite jets, and infinite-order replenishment of this matched statistic are no longer the missing mechanism.

The live bridge is now to feed that source exclusion into the transition-side Cauchy/flux rigidity of XF-036--XF-038 and derive the signed tapered derivative/coercive estimate required by the zero-side argument. The decisive theorem must preserve the `q~log^2 T` mesoscopic scale and quantify the margin after the full source-conditioned transport, not return to a local finite-jet or counting surrogate.

## Identify whether the source-to-transition handoff loses the selector

**Linked intuitions:** `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet` and `MI-004-endpoint-prime-free-fourier-sector-is-volterra-triangular`.

The endpoint selector itself is now quantitatively stable for the matched statistic. Any remaining failure must occur in the handoff from that compact Fourier observable to the long-range Cauchy geometry/coercive quantity used near transition. Determine the exact transform/inequality that carries the excluded memory mode into the required signed estimate, and test whether it introduces a new leakage channel or loses the source-conditioned phase information.

A new nonlocal observable is justified only if this bridge is structurally non-injective or non-coercive. Adding more static near-lattice summaries would not address the remaining boundary.

## Match analytic-frontier input to the mesoscopic carrier without collapsing its horizontal information

**Linked intuitions:** `MI-001-collision-safe-coordinates-separate-singularity-from-source-information` and `MI-003-fixed-time-memory-is-mesoscopic-not-finite-jet`.

Any handoff from zero-side scalar/coherence information must survive the `log^2 T` memory window, collision-safe normalization, and long-range Cauchy coupling used by Xi flow. Analytic Frontier now gives phase-aware horizontal separation constraints at cardinality five; a local certificate is useful here only if its horizontal scale remains visible after the mesoscopic Xi assembly rather than being averaged away.
