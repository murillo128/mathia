# MI-007 — The weighted log-Vieta quotient is transport-stable; the remaining Xi bottleneck is localization

**Evidence level:** exact periodic/source-band and matched-control results through XF-072

## Core intuition

The destination-matched Vieta quotient is no longer merely a proposed repair. Center Parseval gives it an exact source meaning, bounded displacement makes the entire unresolved ultra-infrared block negligible in that norm, and the exact triangular log-Vieta heat equation shows that a growing guard band prevents those unknown low modes from repopulating the transition band during fixed positive heat time.

The remaining loss is geometric/source-local rather than algebraic: compare the actual nonperiodic Xi selector with the periodic surrogate in this same weighted resource. Simply enlarging the period does not buy that comparison for free, because full-period center averaging dilutes the local coercive frame by exactly the same factor as it dilutes seam exposure.

## Strongest justified principle

XF-070 applies Parseval in the translated selector center and obtains the exact coefficient weight `w_k asymp k^4/M^2` on power sums, equivalently an `H^3` weight on the logarithmic Vieta coefficients `P_k/k`. Its tangent form is exactly the third-difference destination energy. Under bounded displacement, the total weighted mass of all modes below `q^delta` is `O(q^(7 delta-4))`; for every `delta<4/7`, including the standard `1/2` edge, the whole source-invisible block is asymptotically null.

XF-071 then proves the nonlinear transport statement. The formal log-Vieta coefficients satisfy an exact triangular Burgers system. Taking a source edge near `q^(1/4)` and a destination edge near `q^(1/2)` creates a growing guard band: bounded unknown infrared data generate exponentially small tail beyond the guard, while source-visible `o(1)` normalized `H^3` resource remains `o(1)` for fixed positive heat time. This closes the periodic weighted-quotient transport problem without root labels or collision assumptions.

XF-072 identifies the surviving interface obstruction. For period `N=RM`, the exact center-Parseval local frame is diluted by `1/R`, and a one-point seam control has interface resource diluted by the same `1/R`. Renormalizing by `R` to retain order-one local coercivity removes the apparent seam gain. **Period dilation is therefore not an independent localization resource inside the full-period Parseval architecture.**

## What remains possible

An Xi-specific interface theorem may exploit cancellation or analytic structure unavailable to the generic seam control. Alternatively, a center scan restricted to a deep interior window may suppress seams without averaging over the full period, but then exact center Fourier orthogonality is lost and a new localization/conditioning theorem is required.

The other load-bearing gap is destination nontriviality: a positive de Bruijn--Newman transition must be shown to carry order-one mass in the weighted quotient that survives the source/interface comparison. The periodic transport theorem does not supply that source fact.

## Status / novelty

Parseval, Newton/logarithmic Vieta identities, triangular Burgers algebra, and window/periodization tradeoffs are classical or exact consequences of the persisted model. The durable synthesis is the relocation of the bottleneck: **the weighted quotient and its nonlinear periodic transport are now controlled; the unresolved theorem is a source-specific Xi-to-periodic localization with no free aspect-ratio gain.**

## Falsification criterion

Show that the XF-070 weighted infrared block can carry order-one destination energy under its assumptions, that the XF-071 guard-band flow can repopulate the visible band at order one, or that period dilation suppresses a generic seam faster than it dilutes the normalized local frame under XF-072's exact full-period geometry. Otherwise the next bridge must attack the Xi-specific interface or replace full-period center extraction by a new localization scheme.
