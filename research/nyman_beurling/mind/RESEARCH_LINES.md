# Nyman--Beurling research lines

This file holds the current mathematical questions suggested by the durable Nyman--Beurling intuitions. It is not a roadmap, task queue, status page, or history.

## Keep the signed exterior route signed after Hadamard cancellation

**Linked intuitions:** `MI-001-target-aware-gram-geometry-must-carry-absolute-scale` through `MI-032-zero-mass-poisson-cancellation-exports-an-equal-negative-lobe`.

NB-123--NB-143 separate recurrence visibility, conditioning, target occupation and generic xi-type realizability. NB-144--NB-147 then classify the positive exterior-Poisson resource. For any tight interior packet, the charged object is its weighted functional-equation **reflection closure**; near the critical line the positive proof cone cannot beat its logarithmic occupancy budget while the prime side is controlled only by an absolute Euler-product majorant.

NB-148 identifies the natural signed escape and its exact debt. A zero-mass exterior sampler cancels the universal leading Hadamard `(1/2)log G` term, but its zero-side Poisson kernel then has zero vertical integral: every positive lobe exports equal integrated negative mass.

NB-149 closes the obvious absolute-value repair. Bounding the adverse lobe by the negative Jordan sampler, or bounding the whole signed kernel by total variation, returns to the positive Poisson cone and reinstates a genuine `Theta(log G)` zero budget under the inexpensive exterior scaling. The cancellation gain therefore survives only if the later argument continues to use sign or zero location.

The fixed-interior problem is now a location-sensitive sign-allocation problem. A stronger theorem must show that actual zeros avoid the negative lobe strongly enough, obtain prime-side cancellation/correlation that pays for it without absolute values, couple the signed kernel directly to the Nyman target, or use higher target-bearing information not representable by one signed Poisson kernel. “Use signed combinations and then dominate the bad part positively” is no longer a live escape.

## Treat reflection closure, signed-lobe conservation and Jordan cost as distinct currencies

Positive sampling charges orbit exposure. Zero-mass signed sampling removes the universal scalar budget only by exporting equal negative kernel mass. Jordan domination then forgets where that negative mass sits and restores the positive logarithmic budget. A future sampler must state which of these three regimes it uses and identify the independent theorem that controls the negative part without collapsing back to the previous cone.

Near maximal positive occupancy still forces near reflection saturation but allows `Theta(log G)` packets. Signed sampling changes that proof obligation from occupancy to location-sensitive sign control; NB-149 shows that generic positivity cannot perform that conversion.

## Push canonical shell discrepancy independently

**Linked intuition:** `MI-002-small-shell-discrepancy-can-destroy-rooted-visibility`.

The shell decomposition still reduces the rooted-mixing channel to a quantitative coefficient discrepancy. The live theorem is whether the canonical arithmetic coefficients force near-linear discrepancy on the relevant shell family without assuming the Nyman distance estimate being targeted. This route remains distinct from the zero-packet Poisson budgets above.