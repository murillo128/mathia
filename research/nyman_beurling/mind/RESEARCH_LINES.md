# Nyman--Beurling research lines

This file holds the current mathematical questions suggested by the durable Nyman--Beurling intuitions. It is not a roadmap, task queue, status page, or history.

## Turn multibase phase information into a height-dependent stability modulus

**Linked intuitions:** `MI-001-target-aware-gram-geometry-must-carry-absolute-scale`, `MI-003-low-gram-spectrum-is-cheap-target-occupation-is-the-signal` through `MI-019-finite-multibase-dealiasing-is-injective-but-not-height-coercive`.

NB-123--NB-125 produce a genuine source-faithful separator on a geometric grid. Actual zero-power packets obey finite annihilating recurrences, the missing harmonic root gives a positive normalized target-angle margin, and the product structure converts near-perfect capture into a rootwise rank--horizontal-depth--height bill.

NB-126 shows that one fixed base is exactly periodic in ordinate while Burnol target mass is an absolute-height observable. NB-127 now closes the immediate two-base repair at the representation level. Any finite family of geometric bases has arbitrarily high **simultaneous near-aliases**. Along suitable common vertical shifts, labelled cross-base roots, annihilator coefficient vectors, normalized margins and every fixed finite geometric sample window return arbitrarily close to their original data, while the Burnol target mass decays like `T^-2`.

Multiplicatively independent bases such as `2,3` do remove exact nonzero aliases, so infinite-precision phase data is injective. But the inverse is globally unstable: for `2,3`, near-returns already occur with phase error `O(1/T)`. Exact de-aliasing is therefore not quantitative height control.

The live bridge must retain a **height-dependent phase modulus** whose required resolution tightens with the target-capture scale, use a number/complexity of sampling scales that grows with the cutoff, retain the full natural-grid primitive, or exploit extra structure of actual zeta zeros that forbids the formal near-alias controls. A fixed continuous functional of finitely many recurrence phases cannot by itself lower-bound absolute-height Burnol mass.

## Push the canonical shell discrepancy toward a near-linear scale

**Linked intuition:** `MI-002-small-shell-discrepancy-can-destroy-rooted-visibility`.

The shell decomposition still reduces the rooted-mixing channel to a quantitative coefficient discrepancy. The live theorem is whether the canonical arithmetic coefficients can force near-linear discrepancy on the relevant shell family without assuming the Nyman distance estimate one is trying to prove.

## Keep exact identifiability and stable height recovery separate

NB-123--NB-125 establish recurrence rank, target separation and rootwise horizontal-depth bills. NB-126 separates fixed-base recurrence phase from absolute height. NB-127 adds the decisive multibase distinction: finitely many independent phases may identify height exactly while having no uniform inverse modulus at infinity.

Future source-faithful interfaces must state the resolution at which phase/root identity is retained and compare it to the height scale demanded by the Burnol target. “No exact alias” is a qualitative property; the endpoint needs quantitative anti-recurrence.
