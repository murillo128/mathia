# Nyman--Beurling research lines

This file holds the current mathematical questions suggested by the durable Nyman--Beurling intuitions. It is not a roadmap, task queue, status page, or history.

## Push the canonical shell discrepancy toward a near-linear scale

**Linked intuition:** `MI-002-small-shell-discrepancy-can-destroy-rooted-visibility`.

The shell decomposition still reduces the rooted-mixing channel to a quantitative coefficient discrepancy. The live theorem remains whether the canonical arithmetic coefficients can force near-linear discrepancy on the relevant shell family without assuming the Nyman distance estimate one is trying to prove.

## Control the finite-window visible natural leakage of the canonical residual

**Linked intuitions:** `MI-001-target-aware-gram-geometry-must-carry-absolute-scale`, `MI-003-low-gram-spectrum-is-cheap-target-occupation-is-the-signal`, `MI-004-weighted-tail-shell-quotient-is-a-conditioned-source-skeleton`.

NB-049--NB-058 identify the finite repair gap, remove the common off-critical Blaschke factor, calibrate the raw large-ratio scale, close the moving-single-shift escape, and identify the collective late quotient with finite-time arithmetic defect sectors

\[
\mathcal K_R=\mathcal D\cap\ker U_{\log R}^*,
\qquad \mathcal D=\mathcal A^\perp.
\]

NB-060 reduces the moving family to the generalized nullspace of one dyadic backward shift. NB-061 now settles the existence gate completely. If `J_R` truncates to `[0,\log R]` and

\[
\mathcal G_R:=J_R\mathcal A,
\]

then the integer-log cocycle gives

\[
\mathcal G_R=\operatorname{span}\{J_RF_n:2\le n\le R\},
\qquad \dim\mathcal G_R\le R-1,
\]

and

\[
\mathcal K_R=\mathcal E_R\ominus\mathcal G_R.
\]

Since `\mathcal E_R\simeq L^2([0,\log R])` is infinite-dimensional, every finite-time defect sector is infinite-dimensional. In particular `ker B_2=\mathcal K_2` is nonzero; the zero-kernel branch of NB-060 is impossible.

The canonical tail question is now exactly target-aware. For `h_*=Qe`,

\[
\operatorname{dist}(h_*,\mathcal K_R)^2
=
\|(I-J_R)h_*\|^2
+
\|P_{\mathcal G_R}J_Rh_*\|^2.
\]

The ordinary time tail tends to zero automatically. Hence the live theorem is

\[
\|P_{\mathcal G_R}J_Rh_*\|\longrightarrow0,
\]

or an equivalent source-controlled certificate. Compact-time defects are abundant; what remains is whether the finite family of natural directions visible in each growing window retains a nonvanishing component of the distinguished residual. Finite rank alone does not imply this projection decays.

## Treat individual-shift disappearance, defect existence, finite-window rank, and target leakage as separate gates

Large-ratio raw columns can be small, and every sufficiently late individual shifted block can be invisible to the canonical residual. Neither statement controls the collective span. NB-061 further closes defect existence while leaving target approximation open: an infinite-dimensional localized kernel can coexist with persistent leakage into a moving finite-dimensional visible space.

Future work should therefore attack the target projection or its dual moments/conditioning directly. Constructing another compactly supported annihilator without controlling its approximation to `h_*` no longer advances the live problem.