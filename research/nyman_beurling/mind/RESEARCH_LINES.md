# Nyman--Beurling research lines

This file holds the current mathematical questions suggested by the durable Nyman--Beurling intuitions. It is not a roadmap, task queue, status page, or history.

## Push the canonical shell discrepancy toward a near-linear scale

**Linked intuition:** `MI-002-small-shell-discrepancy-can-destroy-rooted-visibility`.

The shell decomposition still reduces the rooted-mixing channel to a quantitative coefficient discrepancy. The live theorem is whether the canonical arithmetic coefficients can force near-linear discrepancy on the relevant shell family without assuming the Nyman distance estimate one is trying to prove.

## Turn the exact zero-power recurrence separator into a stable target-angle gap

**Linked intuitions:** `MI-001-target-aware-gram-geometry-must-carry-absolute-scale`, `MI-003-low-gram-spectrum-is-cheap-target-occupation-is-the-signal`, `MI-004-weighted-tail-shell-quotient-is-a-conditioned-source-skeleton`, `MI-005-persistent-forward-memory-leakage-is-a-target-near-kernel-phenomenon`, `MI-006-boundary-zeros-force-reciprocal-depth-screening`, `MI-007-aliased-radial-prediction-has-a-positive-szego-floor`, `MI-008-finite-blaschke-defects-are-silent-in-terminal-scalar-energy`, `MI-009-summable-horizontal-zero-mass-keeps-full-terminal-gram-entropy-bounded`, `MI-010-growing-depth-mobius-annulus-exposes-off-critical-blaschke-amplification`, `MI-011-canonical-deflation-cancels-raw-cauchy-crowding-volume`, `MI-012-three-state-transverse-confluence-exposes-collective-shear`, `MI-013-absolute-height-and-global-zero-counts-are-invisible-to-the-collective-shear-obstruction`, `MI-014-quadratic-horizontal-tangency-is-the-three-state-conditioning-threshold`, `MI-015-confluent-volume-is-curvature-blind-and-controlled-by-radial-depth`, `MI-016-growing-confluent-rank-is-limited-by-endpoint-extrapolation-cost`, `MI-017-finite-high-zero-target-access-requires-a-target-bearing-compression-near-kernel`, `MI-018-actual-zero-power-packets-pay-logarithmic-confluent-rank-for-perfect-harmonic-profile-capture`.

NB-115 localizes the canonical finite-section escape to target-bearing cell-compression modes. NB-116 uses horizontal Burnol mass plus Selberg density to put every high-zero packet above height `G` on retention scale `O(1/G+1/R)`. NB-117 identifies finite target access exactly with weighted interpolation of the harmonic minimizer `C+D/n` by confluent actual-zero power sums.

NB-118 adds an independent source equation: every finite packet of actual zeta zeros is orthogonal to every fixed finite Nyman generator space `V_M`. NB-119 proves that the inverse-section exponent is sharp for the broader class `V_M^perp`: for each fixed `M`, a minimum-norm invisible tail repairs the perfect visible target into exact Nyman orthogonality with retained fraction `1/(kappa_M R)+O_M(1/R^2)`.

NB-120 closes the most immediate growing-section escape in a substantial mixed-tail regime. If `M=M(R)->infinity` with `M^6(1+log M)/R->0`, exact controls remain in `V_M^perp`, satisfy `Q_Rf=e_R`, and retain `(1+o(1))/(kappa_infty R)`. NB-121 shows every fixed nested ladder collapses to the maximal cutoff/section, and NB-122 extends that redundancy to the **entire growing prefix** `2<=r<=R`: the same maximal repair captures every smaller target exactly. Ordinary nested same-source coherence is therefore exhausted as an independent resource.

NB-123 supplies the first exact source-faithful separator. On a geometric grid `n=q^k`, a finite actual zero-power primitive is a confluent exponential-polynomial sequence and obeys an annihilating recurrence of order at most the packet's total multiplicity `d(F)`. Perfect target capture forces its first `floor(log_q R)` block increments to equal the harmonic root `q^-1`, but no actual nontrivial zero has sampled root `q^-1` because `Re rho<1`. Therefore

`q_R(f)=1, Q_Rf!=0  =>  d(F)>=floor(log_q R)`,

and in particular `d(F)>=floor(log_2 R)`. Bounded-rank and sublogarithmic-rank actual zero packets cannot inhabit the perfect-target matched-control class that remains available to generic inverse-section controls.

The live theorem is now quantitative rather than merely representational: turn this exact recurrence mismatch into a stable gap for `q_R(f)=1-o(1)` or another order-one capture regime. The decisive conditioning object is the annihilator evaluated at the missing target root, `|A_(F,q)(q^-1)|`, together with coefficient amplification when sampled zero nodes cluster and the confluent rank grows. Alternatively, force a stronger source-section transition outside `M^6 log M << R` or introduce genuinely non-nested/tail-sensitive information. Adding more nested cutoffs is no longer a candidate mechanism.

Sending `M` toward the full source is still not free. NB-120 gives only the sufficient regime `M^6 log M << R`; it does not classify the transition when `M` is comparable with a larger power of `R`, and `d_M->0` remains the Nyman/RH approximation problem. NB-123 likewise controls exact perfect capture, not near-perfect capture or high-rank packet conditioning. Any proposed closure must expose rather than hide these dependencies.

## Keep target baseline, zero height, continuum mass, cell retention, recurrence rank and source coherence separate

NB-118 is not another Gram-conditioning estimate: it combines actual-zero annihilation of Nyman generators with finite target geometry. NB-119--NB-120 supply matched controls showing exactly how far those source equations can go without using the actual zero-power representation. NB-121--NB-122 complete the representation audit for nested cutoffs: one maximal repair already determines the target at every smaller natural-cell section.

NB-123 then separates a new currency from all of them: **finite annihilating-recurrence rank of the actual zero-power primitive**. Exact nested equations can be satisfied by a generic control that has no such recurrence. Future claims should distinguish zero rank, horizontal zero mass, Nyman-section depth, continuum norm, retained cell energy, target interpolation, recurrence conditioning, and whether a proposed cross-scale law is nonredundant under nesting. The immediate unresolved object is whether the actual zero recurrence stays sufficiently conditioned to turn the logarithmic exact-rank separator into a quantitative target-angle obstruction.