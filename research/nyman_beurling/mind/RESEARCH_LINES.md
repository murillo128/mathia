# Nyman--Beurling research lines

This file holds the current mathematical questions suggested by the durable Nyman--Beurling intuitions. It is not a roadmap, task queue, status page, or history.

## Break scalar-positive carrier coherence without renormalizing the Ford packet

**Linked intuitions:** `MI-062-the-critical-ford-mark-is-a-growing-moment-observable`, `MI-063-demodulated-ford-energy-is-bandlimited-at-the-window-scale`.

NB-245--NB-250 fix the representation and order of operations. The Ford-scaled Cauchy pole must be Hardy-projected through positive Mellin frequencies before Hilbert-space squaring; this exactly recovers the finite depth-marked zero current. The remaining second moment is a positive carrier-band form factor with natural width `Theta(H)=Theta(X/J)`.

NB-251 shows that independently available carrier width `W>>H` disperses the standard matched packet. NB-252 closes literal single-shell dilation because widening the shell also replaces `J` by `J/B_J`. NB-253 closes the canonical equal-weight translated-shell comb because its Dirichlet factor creates a `1/W` coherence peak and replaces the effective population by `R/W`.

NB-254 now shows that equal spacing and equal weights were inessential. For every positive scalar mixture whose carrier centers lie in a span `W`, positivity forces the characteristic factor to remain coherently positive on a window of width `Theta(1/W)`. The effective selected-height population is again `J_W=R/W`, and an order-one matched packet reappears at `X(1-beta)=log J_W+O(1)`, equivalently `d=-log(W/H)+O(1)` in the old Ford coordinate.

The live alternative is therefore no longer “find a better positive multi-shell spacing.” It is to leave scalar-positive recombination or to prove genuinely zeta-specific cancellation on the legal band. Signed/complex coefficients, carrier channels retained separately in `ell^2` until after projection, or another non-scalar operation remain possible only if their exact coherence geometry is shown not to recreate a `1/W` peak with population `R/W`. Any claimed super-`H` bandwidth must preserve the Ford normalization and selected-height capacity rather than exporting the gain into a shallower matched layer.

## Keep sufficient carrier-energy bounds separate from the actual zero theorem

NB-250 gives a sufficient full-line carrier-energy route, not an equivalence. NB-251--NB-254 use matched packets as adversarial compatibility controls rather than asserting actual zeta-zero configurations. A future argument must state which carrier interval and recombination category are physically generated, what coherence kernel and effective population they support, which Ford normalization remains fixed, and what pair-form-factor estimate is actually proved there. Macroscopic support, artificial frequency thickening or scalar positive mixtures cannot be credited as independent averaging bandwidth without that audit.