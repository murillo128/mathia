# Nyman--Beurling research lines

This file holds the current mathematical questions suggested by the durable Nyman--Beurling intuitions. It is not a roadmap, task queue, status page, or history.

## Control the legal Hardy/Nyman geometry after the critical-depth tilt

**Linked intuitions:** `MI-062-the-critical-ford-mark-is-a-growing-moment-observable`, `MI-063-demodulated-ford-energy-is-bandlimited-at-the-window-scale`, `MI-064-signed-scalar-coherence-shrinks-only-by-paying-total-variation`, `MI-065-signed-carrier-cost-must-be-measured-after-quotienting-unresolved-microscopic-dipoles`, `MI-066-resolved-carrier-channels-can-amortize-hilbert-cancellation-at-j-two-thirds`, `MI-067-critical-hardy-zero-sampling-is-logarithmically-displaced-from-the-real-ford-axis`, `MI-068-a-critical-ford-notch-exports-the-scalar-carrier-into-a-depth-ladder`.

NB-245--NB-250 fix the representation and order of operations: the Ford-scaled Cauchy pole must be Hardy-projected through positive Mellin frequencies before Hilbert-space squaring, and the surviving second moment is a positive carrier-band form factor at natural width `Theta(H)=Theta(X/J)`.

NB-251--NB-258 separate coherence cost from representation artifacts. `H`-separation makes shell-scaled source `L^2` equivalent to coefficient `ell^2`; one real Ford-scale notch has bounded cost at `K~J^(2/3)`, while suppressing a whole fixed **real** Ford interval at bounded cost requires leaving every fixed-power mesoscopic regime, necessarily `K=J^(1-o(1))`.

NB-259 shows why that real-axis floor cannot simply be pushed through the legal zero evaluation. A critical-depth zero is sampled at imaginary Ford height `A_J=Theta(log J)`. The minimum cost of imposing `C_J(0)=1` and `C_J(iA_J)=0` is `J/(A_J K^(3/2))`, so bounded cost already occurs at `K~(J/log J)^(2/3)`. At that crossover the derivative on a fixed complex Ford patch is `O(1/log J)`, and the single notch suppresses every fixed critical-depth Ford cell while the real-axis carrier still retains the NB-258 energy floor.

NB-260 now resolves what that cheap critical notch does to the depths below it. With effective depth resolution `N_J=J/A_J~J/log J`, every bounded-cost resolved scalar carrier with `K<=N_J^(1-delta)` is asymptotically finite-degree along the normalized depth segment. It therefore retains positive `L^2` mass on every fixed subinterval of the depth ladder; suppressing a positive depth fraction requires `K>=N_J^(1-o(1))`. At the NB-259 crossover `K~N_J^(2/3)`, an exact critical notch forces the explicit profile `C_J(isA_J)=1-s+o(1)`. A shallower carrier-locked packet at depth `theta log J`, for any fixed `theta<1/6`, then remains compatible with the current zero-free and counting inputs and carries order-one signed Hardy residue.

The live scalar question is therefore no longer another isolated complex notch. Can one control the **two-dimensional destination rectangle** consisting simultaneously of near-full effective depth resolution and the hard Ford ordinate width `Theta(J)` at bounded physical cost, or prove that the actual Hardy/Nyman destination supplies a coercive quantity on that rectangle? Any proposed escape has to prevent cancellation from migrating from the critical cell into shallower depth packets while also surviving the full ordinate range.

## Keep source-band coercivity, complex zero sampling and final Nyman energy distinct

NB-258 remains a valid source-only full-real-band obstruction, NB-259 shows that one critical complex cell is cheap to cancel, and NB-260 shows that this cancellation does not extend cheaply to a proportional depth ladder. None of those statements alone is a lower bound for the final Nyman distance.

A surviving scalar mechanism must be tested in the actual complex sampling geometry and against the matched depth packets allowed by the current zeta-information ledger. The remaining obstruction is joint: depth-ladder coercivity plus the `Theta(J)` Ford-width hard window, or another legal Hardy/Nyman quantity that couples them. The near-full depth-rank lower bound is architecture-specific; vector-valued carriers, unresolved overlapping dictionaries and different legal Hardy operations remain separate escapes.