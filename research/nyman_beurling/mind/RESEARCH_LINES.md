# Nyman--Beurling research lines

This file holds the current mathematical questions suggested by the durable Nyman--Beurling intuitions. It is not a roadmap, task queue, status page, or history.

## Push the canonical shell discrepancy toward a near-linear scale

**Linked intuition:** `MI-002-small-shell-discrepancy-can-destroy-rooted-visibility`.

The shell decomposition still reduces the rooted-mixing channel to a quantitative coefficient discrepancy. The live theorem remains whether the canonical arithmetic coefficients can force near-linear discrepancy on the relevant shell family without assuming the Nyman distance estimate one is trying to prove.

## Combine integer-dilation branching with signed finite Schur certificates

**Linked intuitions:** `MI-001-target-aware-gram-geometry-must-carry-absolute-scale`, `MI-003-low-gram-spectrum-is-cheap-target-occupation-is-the-signal`, `MI-004-weighted-tail-shell-quotient-is-a-conditioned-source-skeleton`, `MI-005-persistent-forward-memory-leakage-is-a-target-near-kernel-phenomenon`.

NB-066--NB-067 reduce a persistent late quotient to a global continuation mode and expose it through finite normalized Schur certificates. NB-068 showed that zero one-cell memory and fixed finite Gram bandwidth alone are insufficient by constructing a nearest-neighbor boundary chain with a stable tail.

NB-069 proves that this strongest cheap control omits the exact source law. Canonical Nyman innovations satisfy integer-dilation branching, so every coarse Gram entry is a normalized rectangular block sum of finer Gram entries. Uniformly bounded exact-branching innovations with fixed finite Gram bandwidth must be orthogonal.

NB-070 now strengthens “the obstruction must be long-range.” Every nonzero coarse correlation forces at least linear absolute Gram mass in each pair of descendant blocks. If

`omega(L)=sup_k sum_(|l-k|>=L)|G_kl|`,

then uniform boundedness plus exact branching and `omega(L)->0` imply complete off-diagonal orthogonality. Thus every bounded nonorthogonal branch-compatible obstruction must carry an order-one aggregate absolute Gram tail at arbitrarily large index separation. Uniformly summable polynomial/exponential decay is no longer an admissible matched escape.

The remaining issue is signed. NB-070 does not control cancellation inside the forced far mass, while the NB-067 continuation certificate is a positive-semidefinite Schur complement. The next theorem should combine the exact block-renormalization identities with Schur geometry to show that the required persistent far mass either forces controllable continuation cost or construct a genuinely branch-compatible nonlocal stable mode.

## Treat local memory, branching covariance, absolute far mass, global continuation cost, and finite certification separately

NB-068 remains a warning against one-cell arguments, but NB-069--NB-070 show that source-matched controls must include cross-scale covariance and cannot be uniformly absolutely localized unless orthogonal. Further refinement of `Gamma_R` or replacement by a rapidly decaying long-range Gram ansatz is irrelevant; the live currency is signed nonlocal mass under exact branching.