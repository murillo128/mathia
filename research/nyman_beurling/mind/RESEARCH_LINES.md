# Nyman--Beurling research lines

This file holds the current mathematical questions suggested by the durable Nyman--Beurling intuitions. It is not a roadmap, task queue, status page, or history.

## Control the legal Hardy/Nyman geometry after the critical-depth tilt

**Linked intuitions:** `MI-062-the-critical-ford-mark-is-a-growing-moment-observable`, `MI-063-demodulated-ford-energy-is-bandlimited-at-the-window-scale`, `MI-064-signed-scalar-coherence-shrinks-only-by-paying-total-variation`, `MI-065-signed-carrier-cost-must-be-measured-after-quotienting-unresolved-microscopic-dipoles`, `MI-066-resolved-carrier-channels-can-amortize-hilbert-cancellation-at-j-two-thirds`, `MI-067-critical-hardy-zero-sampling-is-logarithmically-displaced-from-the-real-ford-axis`.

NB-245--NB-250 fix the representation and order of operations: the Ford-scaled Cauchy pole must be Hardy-projected through positive Mellin frequencies before Hilbert-space squaring, and the surviving second moment is a positive carrier-band form factor at natural width `Theta(H)=Theta(X/J)`.

NB-251--NB-258 separate coherence cost from representation artifacts. `H`-separation makes shell-scaled source `L^2` equivalent to coefficient `ell^2`; one real Ford-scale notch has bounded cost at `K~J^(2/3)`, while suppressing a whole fixed **real** Ford interval at bounded cost requires leaving every fixed-power mesoscopic regime, necessarily `K=J^(1-o(1))`.

NB-259 shows why that real-axis floor cannot simply be pushed through the legal zero evaluation. A critical-depth zero is sampled at imaginary Ford height `A_J=Theta(log J)`. The minimum cost of imposing `C_J(0)=1` and `C_J(iA_J)=0` is `J/(A_J K^(3/2))`, so bounded cost already occurs at `K~(J/log J)^(2/3)`. At that crossover the derivative on a fixed complex Ford patch is `O(1/log J)`, and the single notch suppresses every fixed critical-depth Ford cell while the real-axis carrier still retains the NB-258 energy floor.

The direct route “real source-band coercivity => critical Hardy-cell coercivity” is therefore closed. The next obstruction must control analytic continuation through the logarithmic imaginary displacement, use a Hardy/Nyman quantity coercive on the sampled complex line, or exploit the much wider hard `1/H` ordinate window and the shallower replacement layers that NB-259 does not suppress.

## Keep source-band coercivity, complex zero sampling and final Nyman energy distinct

NB-258 remains a valid source-only full-real-band obstruction, and NB-259 is not a construction making the final Nyman distance small. It isolates a category mismatch: a fixed real Ford interval and a fixed critical complex Ford cell have different interpolation costs because their vertical separation grows like `log J`.

A surviving scalar mechanism must now be tested in the actual complex sampling geometry before any source floor is credited downstream. Fixed critical cells can be suppressed at bounded cost below the real one-notch crossover; the unresolved question is whether the full Hardy/Nyman destination, the `Theta(J)` Ford-width hard window, or shallower zero layers restore a coercive quantity. Near-Ford channel counts and structures outside the resolved scalar dictionary remain separate escapes.