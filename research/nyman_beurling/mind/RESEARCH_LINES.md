# Nyman--Beurling research lines

This file holds the current mathematical questions suggested by the durable Nyman--Beurling intuitions. It is not a roadmap, task queue, status page, or history.

## Break scalar carrier coherence only after paying the signed-conditioning budget

**Linked intuitions:** `MI-062-the-critical-ford-mark-is-a-growing-moment-observable`, `MI-063-demodulated-ford-energy-is-bandlimited-at-the-window-scale`, `MI-064-signed-scalar-coherence-shrinks-only-by-paying-total-variation`.

NB-245--NB-250 fix the representation and order of operations. The Ford-scaled Cauchy pole must be Hardy-projected through positive Mellin frequencies before Hilbert-space squaring; this exactly recovers the finite depth-marked zero current. The remaining second moment is a positive carrier-band form factor with natural width `Theta(H)=Theta(X/J)`.

NB-251 shows that independently available carrier width `W>>H` disperses the standard matched packet. NB-252 closes literal single-shell dilation because widening the shell also replaces `J` by `J/B_J`. NB-253 closes the canonical equal-weight translated-shell comb because its Dirichlet factor creates a `1/W` coherence peak and replaces the effective population by `R/W`.

NB-254 removes equal spacing and equal weights for positive scalar mixtures. Every positive mixture with carrier span `W` has a compulsory `Theta(1/W)` coherence window, so an order-one matched packet reappears with effective population `R/W`.

NB-255 extends the source-side obstruction to arbitrary signed or complex scalar mixtures after the actual Euler return is normalized. If the Euler-weighted coefficient measure has total variation `V`, its normalized Fourier--Laplace factor stays order-one coherent on a disk of radius `c/(WV)`. Hence the effective matched population is `J_*=R/(WV)`, and whenever `J_*->infinity` a matched Ford packet still contributes order one. In the original coordinate the critical layer shifts to `d=-log((W/H)V)+O(1)`. Signs therefore buy coherence only by spending conditioning: escaping this forced-coherence mechanism requires variation at least on the scale `R/W` up to asymptotic constants.

The live alternative is now narrower than “use signed shells.” One must show that the large variation needed to collapse scalar coherence is not repaid by the actual Nyman--Beurling/Hardy projection, or leave scalar recombination altogether by retaining carrier channels separately until a genuinely non-scalar operation. The source-side theorem does not yet prove a target norm cost, because overlapping signed shells may cancel after convolution or projection. That projected cost is now the precise missing bridge.

## Keep source conditioning, projected norm cost and the actual zero theorem separate

NB-250 gives a sufficient full-line carrier-energy route, not an equivalence. NB-251--NB-255 use matched packets as adversarial compatibility controls rather than asserting actual zeta-zero configurations. NB-255 prices only the conditioning required to destroy the central scalar coherence window; it does not prove that coefficient total variation is inherited unchanged by the destination norm.

A future argument must therefore state the physical recombination category, Euler normalization, carrier span, coefficient variation and projection before crediting signed cancellation. Macroscopic support or arbitrary signs are not free averaging bandwidth: scalar cancellation can shrink the guaranteed coherence radius only in proportion to the variation budget spent, and a successful route must prove that this budget can be hidden or transformed after projection without recreating the Ford obstruction.