# MI-070 — A cheap signed boundary notch exports its conditioning into a Ford-scale side-lobe

**Evidence level:** exact synthesis from [NB-262](../../findings/NB-262-minimum-hilbert-boundary-notch-exports-the-ford-obstruction-into-a-packet-scale-side-lobe.md), refining the signed/complex escape left by NB-261. The minimum-`ell^2` two-functional interpolation is elementary/classical; the line-specific content is its shallow Ford scaling and matched residue consequence.

In the near-full regime `K~J/m` with `1<<m<<A_J`, impose the Euler anchor `C_J(0)=1` and one exact signed/complex notch at the live shallow depth `u=X(1-beta)=log m+O(1)`. The unique minimum-`ell^2` resolved coefficient vector has

`||c||_2^2 ~ 12 Y^2 m^3/(J u^2) -> 0`

under the NB-261 scale separation. Thus signs really can erase the positive boundary trace at vanishing shell-Hilbert cost; Hilbert norm alone does not close the signed route.

The cost moves into conditioning. Every coefficient vector satisfying the two constraints obeys `||c||_1 >= (1+o(1))Ym/u`, and the minimum-Hilbert interpolant has the sharp scale `||c||_1~3Ym/u`. Its continuum coefficient profile is asymptotically a ramp, so at horizontal Ford distance `x~J/K~m` the carrier develops a side-lobe of size `Theta(m/u)`.

At `u=log m+O(1)`, a carrier-locked subpacket of only `Theta(u)` admissible points can sit in one cone-aligned side-lobe. The Ford damping contributes `m^-1`, the carrier contributes `m/u`, and the `Theta(u)` packet therefore restores an order-one signed residue contribution. A single cheap center notch moves the obstruction horizontally rather than suppressing the shallow boundary strip.

The durable destination question is now two-dimensional but at a **shrinking depth**: a signed design must suppress enough of the horizontal interval `x=Theta(m)` at depth `u~log(J/K)` to exclude matched subpackets, while controlling Hilbert norm, variation and rank. Additional horizontal interpolation constraints may flatten the minimum-Hilbert side-lobe, so the one-notch calculation identifies the next tariff rather than closing the signed route.

**Boundary.** The explicit side-lobe theorem is for the unique minimum-Hilbert one-notch interpolant; the universal statement for arbitrary one-notch solutions is only the `ell^1` lower bound. NB-262 does not rule out multi-point signed/complex carriers with larger Hilbert cost or rank, and its packet is an adversarial compatibility control against current zeta information, not a claim about actual zero locations.