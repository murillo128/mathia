# NB-063 — each new log cell splits into an infinite defect sector and a finite-rank memory channel

**Status:** `EXACT-DERIVED + ONE-CELL-GRAPH-DECOMPOSITION + INFINITE-PURE-CELL-DEFECT + FINITE-RANK-FORWARD-MEMORY + TARGET-ENERGY-RECURSION + FLAT-CONTROL + METHOD-ADVANCE`.

`NB-061` reduces the collective late-quotient problem to the visible finite-window leakage

\[
\ell_R:=\|P_{\mathcal G_R}J_Rh_*\|^2,
\qquad
h_*=Qe,
\qquad
\mathcal G_R=J_R\mathcal A,
\tag{1}
\]

while `NB-062` proves that the visible spaces form an exact causal flag: restriction from `G_(R+1)` onto `G_R` is surjective with one-dimensional kernel generated by the newest-cell innovation `C_R`. The remaining obstruction was described globally through the dense Gram inverse `H_R^{-1}`. The present finding turns that global conditioning problem into an exact **one-cell recursion**.

Let

\[
\mathcal X_R:=(J_{R+1}-J_R)H^2
\simeq L^2\!\left([\log R,\log(R+1)]\right)
\tag{2}
\]

be the newly opened logarithmic cell and retain

\[
C_R=(J_{R+1}-J_R)D_R\ne0.
\tag{3}
\]

Write

\[
\mathcal X_R^\circ
:=\mathcal X_R\ominus\operatorname{span}\{C_R\}.
\tag{4}
\]

There is a canonical finite-rank operator

\[
\boxed{
\Gamma_R:\mathcal G_R\longrightarrow\mathcal X_R^\circ
}
\tag{5}
\]

which records exactly how the old visible natural space continues into the new cell after removing the genuinely new innovation. It gives the orthogonal graph decomposition

\[
\boxed{
\mathcal G_{R+1}
=
\{g+\Gamma_Rg:g\in\mathcal G_R\}
\ \oplus\
\operatorname{span}\{C_R\}.
}
\tag{6}
\]

Consequently the new compact-time defect sector has the exact form

\[
\boxed{
\mathcal K_{R+1}
=
\mathcal K_R
\ \oplus\
\{v-\Gamma_R^*v:v\in\mathcal X_R^\circ\},
}
\tag{7}
\]

where `K_R=E_R\ominus G_R` as in `NB-061`. In particular the purely local subspace

\[
\boxed{
\mathcal N_R
:=
\mathcal X_R
\ominus
\bigl(\operatorname{span}\{C_R\}+\operatorname{Ran}\Gamma_R\bigr)
\subseteq\mathcal K_{R+1}
}
\tag{8}
\]

is infinite-dimensional, while the cell channel that can carry old natural-space memory has dimension at most `R`:

\[
\boxed{
\dim\bigl(\operatorname{span}\{C_R\}+\operatorname{Ran}\Gamma_R\bigr)
\le R.
}
\tag{9}
\]

Thus **every newly opened logarithmic cell contributes an infinite-dimensional exact defect sector immediately**. The arithmetic deformation can obstruct automatic target capture only through one new innovation plus the at-most-`R-1` dimensional forward-memory range of the old causal traces.

For the canonical residual this yields an exact local energy law. Put

\[
p_R=P_{\mathcal G_R}J_Rh_*,
\qquad
x_R=(J_{R+1}-J_R)h_*,
\qquad
\widehat C_R=\frac{C_R}{\|C_R\|},
\tag{10}
\]

and let

\[
x_R^\circ=P_{\mathcal X_R^\circ}x_R.
\tag{11}
\]

Then the next visible leakage is

\[
\boxed{
\ell_{R+1}
=
|\langle x_R,\widehat C_R\rangle|^2
+
\left\langle
p_R+\Gamma_R^*x_R^\circ,
(I+\Gamma_R^*\Gamma_R)^{-1}
\bigl(p_R+\Gamma_R^*x_R^\circ\bigr)
\right\rangle .
}
\tag{12}
\]

Equivalently, if

\[
\delta_R^2:=\operatorname{dist}(h_*,\mathcal K_R)^2
=\|P_{\mathcal T_R}h_*\|^2,
\tag{13}
\]

then with

\[
z_R:=x_R^\circ-\Gamma_Rp_R
\tag{14}
\]

one has the exact defect-capture identity

\[
\boxed{
\delta_R^2-\delta_{R+1}^2
=
\left\langle
z_R,
(I+\Gamma_R\Gamma_R^*)^{-1}z_R
\right\rangle .
}
\tag{15}
\]

The part of the new target cell outside the finite-rank memory channel is captured with **no conditioning loss at all**:

\[
\boxed{
\delta_R^2-\delta_{R+1}^2
\ge
\|P_{\mathcal N_R}x_R\|^2.
}
\tag{16}
\]

Hence the live obstruction after `NB-062` is sharper than generic bad conditioning. Persistent leakage requires the distinguished target cell to remain aligned, scale after scale, with the finite-dimensional channel generated by the new natural innovation and the forward continuations of the old natural traces. All orthogonal cell detail is automatically absorbed into compact-time arithmetic defects.

## 1. The one-cell memory operator is canonical

`NB-062` gives the surjective restriction map

\[
r_R:=J_R|_{\mathcal G_{R+1}}:\mathcal G_{R+1}\to\mathcal G_R
\tag{17}
\]

with

\[
\ker r_R=\operatorname{span}\{C_R\}.
\tag{18}
\]

For `g in G_R`, choose any lift `\widetilde g in G_(R+1)` satisfying `J_R\widetilde g=g`. Define

\[
\Gamma_R g
:=
P_{\mathcal X_R^\circ}
(J_{R+1}-J_R)\widetilde g.
\tag{19}
\]

This is independent of the chosen lift: two lifts differ by a multiple of `C_R`, and projection onto `X_R^circ` removes exactly that ambiguity. Since `G_R` is finite-dimensional, `Gamma_R` is automatically bounded and

\[
\operatorname{rank}\Gamma_R\le\dim\mathcal G_R=R-1.
\tag{20}
\]

Subtracting the `C_R` component from a lift shows that `g+Gamma_R g` itself lies in `G_(R+1)`. Conversely every vector in `G_(R+1)` decomposes into such a graph vector plus its kernel component. The two pieces are orthogonal because `Gamma_Rg` lies in `X_R^circ`, while the old component `g` lies in `E_R`. This proves (6).

The operator is source-explicit in the causal basis of `NB-062`. For `1<=j<R`, the canonical lift of `J_RD_j` is `J_(R+1)D_j`, hence

\[
\boxed{
\Gamma_R(J_RD_j)
=
P_{\mathcal X_R^\circ}
(J_{R+1}-J_R)D_j.
}
\tag{21}
\]

Thus `Gamma_R` contains no new target oracle. It is precisely the one-cell forward tail of the old causal innovations, modulo the newest innovation. In the flat `O=1` control all old `D_j^(0)` end in their birth cells, so the right side of (21) vanishes and `Gamma_R=0` identically.

## 2. Orthogonal complementation turns the trace graph into a defect graph

Use the orthogonal decomposition

\[
\mathcal E_{R+1}
=
\mathcal K_R
\oplus
\mathcal G_R
\oplus
\operatorname{span}\{C_R\}
\oplus
\mathcal X_R^\circ.
\tag{22}
\]

A vector in the last three summands may be written as

\[
g+\alpha C_R+v,
\qquad
g\in\mathcal G_R,\quad
v\in\mathcal X_R^\circ.
\tag{23}
\]

Orthogonality to the kernel direction in (6) forces `alpha=0`. Orthogonality to every graph vector `x+Gamma_Rx` forces

\[
\langle g,x\rangle
+
\langle v,\Gamma_Rx\rangle=0
\qquad(x\in\mathcal G_R),
\tag{24}
\]

so

\[
g=-\Gamma_R^*v.
\tag{25}
\]

Every `K_R` vector is already orthogonal to `G_(R+1)` because its support lies in the old window and the old restriction of every new natural trace lies in `G_R`. This proves (7).

If

\[
L_Rv:=v-\Gamma_R^*v,
\tag{26}
\]

then

\[
L_R^*L_R=I+\Gamma_R\Gamma_R^*,
\tag{27}
\]

and therefore

\[
\|L_Rv\|^2
=
\|v\|^2+\|\Gamma_R^*v\|^2
\ge\|v\|^2.
\tag{28}
\]

The graph range is closed. Moreover any `v in ker Gamma_R^*` satisfies `L_Rv=v`, so it is already a pure new-cell defect. Since

\[
\ker\Gamma_R^*
=
\mathcal X_R^\circ
\ominus
\operatorname{Ran}\Gamma_R,
\tag{29}
\]

this is exactly `N_R` in (8). The cell `X_R` is infinite-dimensional while `span(C_R)+Ran Gamma_R` has dimension at most `1+(R-1)`, proving (9) and the infinite-dimensionality of `N_R`.

This is stronger than the abundance statement in `NB-061`. That result showed that each cumulative `K_R` is infinite-dimensional. Here the abundance is **incremental and cell-local**: at every single step `R -> R+1`, infinitely many new defect directions live entirely inside the newly opened cell and require no backward correction at all.

## 3. The canonical target obeys an exact local projection recursion

Let `h` be any ambient vector and abbreviate

\[
p=P_{\mathcal G_R}J_Rh,
\qquad
x=(J_{R+1}-J_R)h,
\qquad
x^\circ=P_{\mathcal X_R^\circ}x.
\tag{30}
\]

Because the two pieces in (6) are orthogonal, projection onto `G_(R+1)` separates into the scalar `C_R` direction and projection onto the graph of `Gamma_R`. The graph synthesis operator is

\[
S_Rg=g+\Gamma_Rg,
\tag{31}
\]

with

\[
S_R^*S_R=I+\Gamma_R^*\Gamma_R,
\qquad
S_R^*(J_{R+1}h)=p+\Gamma_R^*x^\circ.
\tag{32}
\]

The standard projection formula for an injective finite-dimensional synthesis map gives

\[
\|P_{\operatorname{Ran}S_R}J_{R+1}h\|^2
=
\left\langle
p+\Gamma_R^*x^\circ,
(I+\Gamma_R^*\Gamma_R)^{-1}
(p+\Gamma_R^*x^\circ)
\right\rangle.
\tag{33}
\]

Taking `h=h_*` proves (12). This formula is equivalent to the global `b_R^*H_R^{-1}b_R` identity of `NB-062`, but it exposes the update through one cell and one finite-rank memory operator instead of hiding the mechanism inside the full growing Gram inverse.

For the defect side, `K_(R+1)=K_R direct-sum Ran L_R`. Therefore

\[
\operatorname{dist}(h,\mathcal K_R)^2
-
\operatorname{dist}(h,\mathcal K_{R+1})^2
=
\|P_{\operatorname{Ran}L_R}h\|^2.
\tag{34}
\]

Now

\[
L_R^*h
=
x^\circ-\Gamma_Rp,
\tag{35}
\]

and (27) gives

\[
\|P_{\operatorname{Ran}L_R}h\|^2
=
\left\langle
x^\circ-\Gamma_Rp,
(I+\Gamma_R\Gamma_R^*)^{-1}
(x^\circ-\Gamma_Rp)
\right\rangle.
\tag{36}
\]

For `h=h_*`, `NB-058` identifies the distances with the collective late-tail target gains, proving (15).

Because `(I+Gamma_R Gamma_R^*)^(-1)` acts as the identity on `ker Gamma_R^*`, while `Gamma_Rp` lies in `Ran Gamma_R`, the `N_R` component of (36) is exactly

\[
\|P_{\mathcal N_R}x_R\|^2.
\tag{37}
\]

All other contributions are nonnegative, proving (16). Also

\[
\frac{\|z_R\|^2}{1+\|\Gamma_R\|^2}
\le
\delta_R^2-\delta_{R+1}^2
\le
\|z_R\|^2,
\tag{38}
\]

so the only conditioning constant entering a single update is the one-cell extension norm `||Gamma_R||`.

## 4. Visible leakage is a cell-by-cell conservation law

Set

\[
\tau_R:=\|(I-J_R)h_*\|^2.
\tag{39}
\]

`NB-061` gives

\[
\delta_R^2=\tau_R+\ell_R.
\tag{40}
\]

Since

\[
\tau_R-\tau_{R+1}=\|x_R\|^2,
\tag{41}
\]

combining (15) and (40) yields the exact recursion

\[
\boxed{
\ell_{R+1}
=
\ell_R+\|x_R\|^2
-
\left\langle
z_R,
(I+\Gamma_R\Gamma_R^*)^{-1}z_R
\right\rangle.
}
\tag{42}
\]

Thus each new target cell brings `||x_R||^2` units of potential visible mass. The enlarged compact-time defect space immediately absorbs the amount in the final term; only the remainder can persist as visible natural leakage. Equation (16) says that all target energy in the infinite-dimensional memory-free part `N_R` is absorbed with unit efficiency.

This isolates the remaining failure mode sharply. A nonzero limiting leakage cannot be sustained merely by saying that the causal basis is ill-conditioned somewhere. The canonical target must repeatedly place the relevant new-cell data in the finite-dimensional channel

\[
\mathcal M_R^{\rm cell}
:=
\operatorname{span}\{C_R\}
+
\operatorname{Ran}\Gamma_R,
\qquad
\dim\mathcal M_R^{\rm cell}\le R,
\tag{43}
\]

and the old visible component `p_R` must couple through `Gamma_R` so that the mismatch `z_R` is small or heavily attenuated. This is the exact one-step form of the forward-memory obstruction identified qualitatively in `NB-062`.

The finding does not prove that this repeated alignment is impossible. The dimension bound in (43) is not useful by itself against one distinguished vector `x_R`; an `R`-dimensional channel inside an infinite-dimensional cell can contain that vector exactly. A useful next theorem needs arithmetic information showing that the canonical target cell is transverse to this source-generated memory channel, or quantitative control of `Gamma_R` together with the target mismatch `z_R`.

## 5. Flat control recovers perfect cellwise absorption

For `O=1`, `NB-062` gives

\[
D_j^{(0)}
\longleftrightarrow
e^{t/2}\mathbf1_{I_j}(t).
\tag{44}
\]

Hence old innovations have no continuation into later cells. Equation (21) gives

\[
\boxed{\Gamma_R^{(0)}=0.}
\tag{45}
\]

The memory channel reduces to the single coarse direction `span(C_R^(0))`, while

\[
\mathcal N_R^{(0)}
=
\mathcal X_R
\ominus
\operatorname{span}\{C_R^{(0)}\}
\tag{46}
\]

is exactly the Brownian-bridge/detail cell space of `NB-056`. The canonical flat residual `Q_0e` is cellwise orthogonal to the coarse direction, so

\[
x_R^{(0)}\in\mathcal N_R^{(0)}.
\tag{47}
\]

Equation (15) therefore becomes

\[
\delta_R^2-\delta_{R+1}^2
=
\|x_R^{(0)}\|^2,
\qquad
\ell_R^{(0)}=0
\quad(R\ge2).
\tag{48}
\]

Summing the cell energies reproduces the cubic flat tail

\[
\delta_R^2
=
\frac1{36R^3}+O(R^{-4})
\tag{49}
\]

from `NB-056`. The contrast with the actual source is now exact: the flat model loses no target energy to visible finite-window leakage because it has **zero forward-memory operator**. The arithmetic source differs precisely through `Gamma_R` and the new-innovation component of the canonical target cell.

## 6. Prior-art and falsification boundary

The graph-of-an-operator formula, orthogonal-complement graph, and projection identities used in (6)--(16) are standard Hilbert-space linear algebra; no novelty is claimed for those abstract facts. The Nyman-specific input is the exact finite-window flag of `NB-062`, which identifies the restriction kernel with the newest logarithmic-cell innovation and makes the graph operator `Gamma_R` canonical and source-explicit through (21).

A bounded literature audit covered the classical Báez-Duarte/Bagchi semigroup formulation, the Hardy-space orthogonal-complement work of Calderaro--Manzur--Noor--Santos, recent Nyman Gram decompositions, and recent work on smoothed Gram block-compressibility. These sources study completeness, orthogonal complements, Gram structure, or decay, but the search did not locate the specific cell-by-cell decomposition (6)--(9), the infinite pure-cell increment `N_R`, or the canonical target recursion (12)/(15). No priority claim is made.

No specialized external estimate is load-bearing, so `SOURCES.md` remains unchanged.

The falsification boundary is strict. This finding proves no decay of `ell_R`, no uniform bound on `||Gamma_R||`, no lower angle between `x_R` and the memory channel, and no improved Nyman approximation rate. Its durable advance is a mechanism reduction: **after the exact causal flag, every new cell is mostly defect space; only a finite-rank forward-memory channel plus one new natural innovation can prevent immediate capture of the canonical target cell, and the resulting target leakage obeys an exact one-step energy recursion.**