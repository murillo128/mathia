# PF-010 — infinite algebraic trace complexity; no finite-type cover underneath

> Retroactive persistence from the legacy `FINDINGS.md`; no new mathematical claim or evidence upgrade is introduced here.

**Status:** `EXACT-DERIVED` modulo standard cyclotomic/field facts.

## Claim

For consecutive endpoint values `a=u_p`, `b=u_q`, the canonical trace is

```text
T_{p,q}=2(a+b)/(b-a),
```

so

```text
b/a=(T_{p,q}+2)/(T_{p,q}-2).
```

Successive trace ratios generate `u_p/u_3=sqrt(3) cot(pi/p)` for every odd prime `p`. The cyclotomic degrees grow without bound (at least `(p-1)/2` for these ratios).

If the prime-flute group lay inside a finitely generated subgroup of `PSL(2,R)`, the field generated by finitely many lifted matrix entries would have algebraic elements of uniformly bounded degree over `Q`, contradicting the unbounded trace-ratio degrees.

The proposed conclusion is

```text
Gamma_prime is not contained in any finitely generated Fuchsian group.
```

## Audit boundary

The lift/sign argument in `PSL(2,R)` should be stated carefully. The matrix/field reduction and cyclotomic degree input are logically separable.
