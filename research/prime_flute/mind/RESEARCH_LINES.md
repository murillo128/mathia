# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Prove the physical-cuff endpoint without erasing the small-support scale

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`, `MI-006-the-critical-endpoint-needs-structure-and-the-right-concentration-currency`.

PF-196--PF-204 establish the prime-summable critical `L log L` currency, matrix-valued local Cwikel control, lossless fixed-width/finite-color localization, regular Lambert-body reassembly, form-level gluing across unsmoothed decomposition cuffs, and the exact two-Hilbert-space identity with both native smooth gradient resolvents. The unsmoothed route is therefore concentrated in the mixed critical product `G_g^* M_C P_(F_0) G_+`, with nonsmoothness isolated in one bounded a.e. cotangent transport.

PF-205 removes the apparent derivative obstruction in the optional smooth route. Choosing the canonical seam width `w_n asymp d_n` gives `DF-I=O(d_n)`, uniformly bounded `D^2F`, uniformly elliptic pulled metrics with local `C^1` control, and a summable physical critical Orlicz budget. PF-206 then identifies the real scale gate: isotropic physical meshing preserves that budget, but dilating each `d_n x d_n` cell to a fixed unit box erases the small-area factor and cannot justify a summable endpoint estimate by unweighted fixed-domain Ponge bounds.

The smooth route therefore has an exact dichotomy: either prove a critical operator estimate that keeps the physical `w_n^2` scale in the middle factor, or prove a weighted local gradient-resolvent/square-function mechanism that restores that scale after normalization. The unsmoothed route remains the branchwise/two-manifold critical estimate for `P_(F_0)`. A proof should choose one architecture and preserve the physical currency rather than reintroduce rough resolvents or arbitrary seam thinning.

## Keep interface regularity, physical-scale localization, and true short-collar geometry as distinct gates

The decomposition cuffs contribute no extra form-level surface term; PF-204 keeps both resolvents smooth; PF-205 supplies a controlled natural-width smoothing when desired; and PF-206 shows that the long-cuff aspect ratio is not itself a coefficient-space obstruction. None of these facts proves weak `S_1` after a scale-destroying normalization.

Conversely, solving either the piecewise-cotangent transport estimate or the scale-aware smooth seam estimate would not construct the PF-138/PF-183 genuinely short Margulis-collar splice. The canonical conservative body/core splice remains an independent geometric obligation in the same endpoint currency.

## Resolve ordered multi-neck memory in marked spectral data

Finite graded chains retain directed neck order while scalar determinants erase it. Any arithmetic claim must identify a marked Weyl/Green/spectral-measure or boundary observable that survives the exact gap-order controls and the localized endpoint comparison.

## Require prime/clone separation before assigning arithmetic meaning

The all-composite shift clone shares the area-preserving geometry, endpoint summability, local weak-trace architecture, form-level interface structure, and the new natural-width seam controls. These are analytic infrastructure, not arithmetic evidence. A surviving observable must separate the prime flute from matched non-prime endpoint geometries.
