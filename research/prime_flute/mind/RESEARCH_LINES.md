# Prime-flute mathematical questions

## Prove locality or delocalization of the completed DtN square-root on critical packets

**Linked intuitions:** `MI-008-complete-cut-transmission-needs-extension-mass-not-only-boundary-impedance`, `MI-009-bidirectional-scalar-survival-is-killed-by-accumulated-pant-loss`, `MI-010-scalar-green-response-is-killing-normalized-markov-averaging`, `MI-011-sequential-physical-band-elimination-factorizes-the-full-angle-defect`, `MI-012-corridor-form-smallness-needs-seam-metric-locality`, `MI-013-compactification-critical-low-packets-block-uniform-taylor-smallness`, `MI-014-critical-packet-leakage-occupies-a-positive-measure-center-family-with-explicit-parity-scales`, `MI-015-intrinsic-angle-packets-must-be-normalized-by-completed-diagonal-energy`.

PF-303--PF-328 reduce the physical angle to direct/conditional channels, localize the direct contribution and produce a positive-length family of compactification-critical physical packets before final completed normalization. PF-329 identifies the correct total-energy pullback.

PF-330--PF-331 now remove two possible explanations for disappearance. The final low normalization preserves the packet family as a unitary-rotated tight frame with exactly unit raw completed energy, so it does not suppress the total Hilbert--Schmidt mass. Its only new packet effect is the polar rotation `U_L`. But the concrete factor

`F=A_0^(1/2)Q_L^(-1/2)`

satisfies `F^*F>=I`, and the gapped polar estimate gives `||[U_L,X]||<=||[F,X]||` for translations/localizers. The polar factor cannot amplify a locality defect already controlled for `F`.

The direct endpoint is therefore exact: establish sufficiently uniform locality/quasilocality of the completed DtN square-root factor `F` on the PF-328 critical center family, separately across the two arithmetic terminal regimes, or prove that `F` genuinely delocalizes almost all those packets enough to satisfy the PF-318 Hilbert--Schmidt budget. Seam-only amplitudes and scalar bounds on `A^(-1/2)` are no longer the right quantities.

## Close the global heavy-range ideal counts after the locality decision

For the PF-287 heavy low/high extension-range projection product, determine whether the conditional channel `T_H` and the shear-deleted reference-angle family satisfy the required ideal counts once the direct completed-packet behavior is known. PF-318 supplies endpoint counting; PF-320 supplies the shear-error budget; PF-325 supplies the position-space frame; PF-330 gives the exact raw total-energy packet average; PF-331 reduces final packet locality to the concrete square-root factor.

Any argument replacing the terminal geometry by one fixed finite-pant model is invalid because the critical packet sees both finite-boundary and escaping-boundary subsequences. Any argument obtaining the needed gain merely from seam normalization is also invalid because the completed intrinsic angle is unitary under that congruence and total packet mass is conserved.

## Prime realization within the resolved upstream-memory window

Can consecutive prime-gap isolation realize the moderate graded neck ratios of MI-001, or can the surface-to-graph error be sharpened enough to resolve `w_j^2/w_(j-1)` in the hierarchies already known to occur? Arbitrarily strong separation is not the required condition: the correction must still dominate `w_j sqrt(w_1)`.

This older fixed-topology realization problem remains logically separate from the completed-angle endpoint. Arithmetic boundary location can survive into a pre-angle packet while the final destination still depends on the locality of the completed energy square root.