# Prime-flute mathematical questions

## Estimate the completed total-energy packet response in both arithmetic critical-boundary regimes

**Linked intuitions:** `MI-008-complete-cut-transmission-needs-extension-mass-not-only-boundary-impedance`, `MI-009-bidirectional-scalar-survival-is-killed-by-accumulated-pant-loss`, `MI-010-scalar-green-response-is-killing-normalized-markov-averaging`, `MI-011-sequential-physical-band-elimination-factorizes-the-full-angle-defect`, `MI-012-corridor-form-smallness-needs-seam-metric-locality`, `MI-013-compactification-critical-low-packets-block-uniform-taylor-smallness`, `MI-014-critical-packet-leakage-occupies-a-positive-measure-center-family-with-explicit-parity-scales`, `MI-015-intrinsic-angle-packets-must-be-normalized-by-completed-diagonal-energy`.

PF-303--PF-325 reduce the physical angle to a direct channel plus the conditional channel `T_H`, localize the direct channel and express the local Hilbert--Schmidt mass as an exact integral over translated physical packets. PF-326--PF-328 show that seam normalization preserves the fixed-band critical packet, its pre-pant transverse-energy floor and a positive-length interval of critical centers, with explicit seam-only parity scales.

PF-329 identifies the missing representation step. A unit packet `psi_t` in the final normalized low angle coordinate corresponds in raw pant coordinates to

`g_t = Q_L^(-1/2) A^(-1/2) psi_t`,

where `A` is the completed low diagonal block after seam normalization. This raw trace has exactly unit **total** low energy. Block-diagonal seam congruence is absorbed by unitaries at the intrinsic-angle level, so the `w_n^-1` and `w_n` seam-only parity amplitudes of PF-328 are not angle lower bounds and cannot be repaired by a scalar renormalization. The noncommuting operator `A^(-1/2)` may reshape the entire translated packet family.

The direct theorem is therefore now exact: control the family `A_n^(-1/2) psi_(n,t)` on a positive-measure subset of the PF-328 critical interval, or estimate the equivalent raw total-energy correlation directly, separately in the two PF-324 arithmetic boundary regimes. Either prove that completed diagonal preconditioning preserves enough critical localization/correlation to obstruct the PF-318 Hilbert--Schmidt budget, or prove that it suppresses/redistributes almost all of the family strongly enough to meet that budget. Pre-angle seam energy by itself decides neither outcome.

## Close the global heavy-range ideal counts after strength/angle factorization

For the PF-287 heavy low/high extension-range projection product, determine whether the conditional channel `T_H` and the shear-deleted reference-angle family satisfy the required ideal counts. PF-318 supplies endpoint counting; PF-320 supplies the shear-error budget; PF-325 supplies the exact position-space representation; PF-329 supplies the correct completed-energy packet entering that representation.

Any argument that replaces the terminal geometry by one fixed finite-pant model is invalid: the critical packet sees both finite-boundary and escaping-boundary subsequences. Any argument that obtains the needed gain solely from the seam normalizer is also invalid: PF-329 proves that seam congruence is coordinate-invariant at the intrinsic-angle level and that the completed diagonal inverse square root is part of the actual raw packet. A successful ideal-count theorem must obtain its suppression from completed pant/projection/total-energy correlation geometry or prove that the destination erases the arithmetic boundary difference on almost all critical centers.

## Prime realization within the resolved upstream-memory window

Can consecutive prime-gap isolation realize the moderate graded neck ratios of MI-001, or can the surface-to-graph error be sharpened enough to resolve `w_j^2/w_(j-1)` in the hierarchies already known to occur? Arbitrarily strong separation is not the required condition: the correction must still dominate `w_j sqrt(w_1)`.

PF-324 does not solve this older fixed-topology realization problem. Its gap ratio is retained as a critical-boundary location before the completed angle is formed; PF-326--PF-328 preserve the corresponding fixed-band localization and positive-measure pre-angle calibration, while PF-329 shows that final angle survival additionally depends on completed diagonal preconditioning. Survival in a final spectral observable remains a separate theorem.