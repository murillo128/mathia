# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Analyze the actual shorted block-Jacobi boundary form, not raw module coercivity

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`, `MI-006-the-critical-endpoint-needs-structure-and-the-right-concentration-currency`.

PF-196--PF-206 establish the prime-summable critical `L log L` currency, matrix-valued local Cwikel control, lossless localization/reassembly, the native two-Hilbert-space factorization, and a natural-width smooth seam whose physical-scale Orlicz budget is summable. PF-207--PF-213 then reduce the smooth decomposition-cuff route to one exact global requirement: enough off-diagonal locality of the simultaneous energy-normalized Schur factor `D` to reassemble the local low/high boundary pieces in weak trace class. PF-213 gives a sufficient summable block-decay criterion and proves that positivity/contractivity alone is too weak.

PF-214 identifies the exact topology behind that factor. The normalized precision `K` with `D=(I+K)^(-1)` is block Jacobi: long-range boundary mixing is created only by inversion of a nearest-neighbor one-dimensional precision. But the simplest locality proof fails. PF-215 shows the adjacent normalized hopping diverges on constant modes, PF-216 shows that this is accompanied by a growing raw diagonal mass/conductance floor, and PF-217 shows that tangential boundary screening destroys any uniform fixed fraction of that raw floor after the complementary modes are variationally shorted out.

The live smooth-route theorem is therefore a **scale-sensitive inverse-locality estimate for the actually shorted block-Jacobi form**: a weighted/variable-conductance mechanism strong enough to imply PF-213-compatible decay without assuming uniformly bounded hopping or inheriting raw constant-mode coercivity through shorting. The unsmoothed PF-204 mixed critical transport remains an independent route, and the genuine PF-183 short-Margulis-collar conservative splice remains a separate geometric gate.

## Keep interface regularity, physical-scale localization, and true short-collar geometry distinct

The decomposition cuffs contribute no extra form-level surface term; both native resolvents remain smooth; natural-width seam smoothing is controlled; and the global smooth reassembly problem has been reduced to the Schur factor above. None of these facts proves weak `S_1` after a scale-destroying normalization, and none constructs the true short-collar splice.

A proof should therefore state which gate it closes: unsmoothed mixed transport, shorted-Schur inverse locality, or the independent short-collar geometry. Success at one is not evidence for the others.

## Resolve ordered multi-neck memory in marked spectral data

Finite graded chains retain directed neck order while scalar determinants erase it. Any arithmetic claim must identify a marked Weyl/Green/spectral-measure or boundary observable that survives the exact gap-order controls and the localized endpoint comparison.

## Require prime/clone separation before assigning arithmetic meaning

The all-composite shift clone shares the area-preserving geometry, endpoint summability, local weak-trace architecture, form-level interface structure, and natural-width seam controls. These are analytic infrastructure, not arithmetic evidence. A surviving observable must separate the prime flute from matched non-prime endpoint geometries.
