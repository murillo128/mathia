# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Control polar transport in the actual relative seam while preserving the Bessel-four smoothing margin

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`, `MI-006-the-critical-endpoint-needs-structure-and-the-right-concentration-currency`.

PF-247--PF-252 identify the actual parity Jacobi edge, its growing zero-energy branch, and an exact ground-state transform with radial Bessel-four scaling. PF-253 upgrades the earlier free-model heuristic to a sharp theorem for the actual chain: for fractional power `sigma`, the separated weighted window is exactly `R<2+2sigma`, so the square-root channel has margin up to `R<3`.

PF-254 then isolates the genuinely noncommutative part of the Robin source map. In finite/Galerkin regularization, `(K+Q)^{-1}Q^{1/2}` factors as `K^{-1/2}h(R)U`, where the positive relative amplitude `h(lambda)=sqrt(lambda)/(1+lambda)` has the favorable square-root edge and the polar partial isometry `U` carries all unsmoothed spectral orientation. The live local theorem is therefore to relate the actual complete-lift relative seam to the PF-247 Jacobi model and prove enough locality/weighted control for `U` in the corridor basis to spend the established `R<3` margin.

## Keep local polar transport separate from physical and global reassembly

The critical fixed-axis fractional kernel is now a positive result, not an unresolved perturbative guess. It still does not prove the PF-243 Robin-Poisson estimate for the full seam, and PF-254 does not justify an unbounded complete-lift limit by itself.

Only after the relative-seam/polar-transport theorem is proved should the remaining margin be spent on the `T_a` replacement, hypercycle-coordinate perturbations, physical `P/H` recoupling, finite-pant completion, neighboring-cell extension, and global weak-`S_1` reassembly.