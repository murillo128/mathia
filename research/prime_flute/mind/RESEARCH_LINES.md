# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Prove the endpoint conservative splice or source-specific `C^1` entry; the body energy itself is no longer the bottleneck

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, and `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`.

PF-171--PF-188 remove the collapsing-central-sector, volume, long-body, artificial-interface, infinite-collar-multiplicity, annular-flux, and linearized Killing obstructions above the trace endpoint. PF-188 gives qualitative marked `W^{1,r}` rigidity for every `r>1` on one fixed larger positive-side collar without boundary normalization.

PF-191 now sharpens the geometric energy boundary to `r=1`. The explicit exact-area Lambert transport retains the hidden `1/cosh(a)` localization of the deformation; for the prime/shift family its strong-`L^1` body masses are summable. Re-running the assembled-body bookkeeping therefore reaches unweighted `L^1`, and the true-short-collar transition slabs also have summable inverse-unit-ball weighted endpoint mass because those slabs are uniformly thick.

The remaining geometric gate is not generic body integrability. It is an **endpoint local conservative splice/fixed-germ confinement theorem with energy-linear cost**, or genuinely source-specific `C^1` entry into the exact-area cutoff chart. The theorem must control the collar/body reassembly rather than charge the already-summable Lambert body again.

## Isolate the trace endpoint from the reassembly mechanism

PF-189 places the complete fixed-central Dirichlet-decoupled short-collar sector in `S_{1,infinity}\setminus S_1` with summable weak-endpoint mass. PF-190 gives a conditional full dual-resolvent envelope `s_n=O(log^2 n/n)` from a two-sided weighted `L^1` metric budget, but explicitly does **not** prove weak trace class.

Together with PF-191, this localizes the unresolved endpoint. Collapsing central multiplicity and exact-area body transport both have endpoint budgets; any genuine loss beyond `1/n` for the uncut relative resolvent must arise from the critical gradient half-factors, collar/body transmission, cutoff commutators, or another nonlocal reassembly term.

A decisive theorem should either produce project-specific weak-`S_2` control of the gradient halves and hence a true weak-`S_1` splice, or exhibit a source-faithful assembly where the logarithmic loss is unavoidable. Merely proving all `S_r`, `r>1`, or re-estimating decoupled collars cannot decide the endpoint.

## Resolve ordered multi-neck memory in marked spectral data

Finite graded chains retain directed neck order while scalar determinants erase it. Any arithmetic claim must identify a marked Weyl/Green/spectral-measure or boundary observable that survives the exact gap-order controls and the now tightly localized geometric comparison.

## Require prime/clone separation before assigning arithmetic meaning

The all-composite shift clone shares substantial geometric and operator structure. Area preservation, near-isometry, Schatten membership, weak trace membership, exact symplecticity, fixed-germ Sobolev rigidity, or marked collar localization is not arithmetic evidence unless a source-specific observable separates the prime flute from its matched clone.