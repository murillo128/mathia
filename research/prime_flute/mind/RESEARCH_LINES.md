# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Resolve the endpoint conservative splice in a topology whose concentration cost is prime-summable

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`, and `MI-006-the-critical-endpoint-needs-structure-and-the-right-concentration-currency`.

PF-171--PF-191 localize the unresolved geometry to the conservative splice/reassembly and provide summable endpoint body mass. PF-192--PF-194 then close the generic strong-`L^1` route much more sharply: strong geometric/Korn rigidity fails at the endpoint, and the obstruction survives determinant-one Hamiltonian/reflection-marked matrix structure.

PF-195 also closes the naive one-sided critical weak-`S_2` repair: endpoint `L^1` mass alone does not control the local-sup coefficient norm charged by generic Cwikel theory. PF-196 identifies a more compatible critical currency. For the exact Lambert deformation, the symmetrized Cwikel--Solomyak `L log L` Luxemburg cost is `d(1+a)/cosh(a)`, not mass-linear, but the actual prime/shift tail sums that loss -- indeed any fixed polynomial in the local Lambert depth remains summable.

The live endpoint theorem should therefore use either additional PF-specific structure to obtain a stronger local estimate, or a symmetrized critical estimate for the actual localized hyperbolic vector-gradient form with only prime-summable depth losses. Generic incompressibility, raw `L^1` mass, and one-sided weak-`S_2` localization are no longer sufficient hypotheses.

## Separate local coefficient summability from global weak-trace reassembly

PF-196 is a positive coefficient-space result, not yet a weak-`S_1` theorem for the uncut relative resolvent. A local logarithm of Lambert depth is harmless because the exact prime tail absorbs it; a logarithm in singular-value index, number of localization blocks, or global off-diagonal reassembly is a different resource and is not automatically summable.

A decisive route must transfer the local Orlicz control to the matrix-valued hyperbolic operator with uniform constants and then reassemble without introducing a new nonlocal critical loss. Alternatively it may prove that the canonical PF maps possess additional flux, boundary, germ, or regularity structure that excludes the endpoint laminate mechanism.

## Resolve ordered multi-neck memory in marked spectral data

Finite graded chains retain directed neck order while scalar determinants erase it. Any arithmetic claim must identify a marked Weyl/Green/spectral-measure or boundary observable that survives the exact gap-order controls and the localized endpoint comparison.

## Require prime/clone separation before assigning arithmetic meaning

The all-composite shift clone shares substantial geometric and operator structure. Area preservation, endpoint Orlicz summability, weak-trace membership, exact symplecticity, or marked collar localization is not arithmetic evidence unless a source-specific observable separates the prime flute from its matched clone.