# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among mathematically different mechanisms.

## Resolve how ordered multi-neck memory enters marked spectral data

**Linked intuitions:** `MI-001-relative-multigap-geometry-carries-the-spectral-signal` and `MI-002-localize-in-space-before-spectral-compression`.

Finite graded chains show that a resolved local eigenvalue can retain a leading neck scale together with an upstream Feshbach correction, while scalar determinants erase that directed relation. Determine which marked Weyl, Green, spectral-measure, or boundary data on controlled flute pieces recover ordered neck information before the complete-tail comparison classicalizes it.

## Require reference stability and prime/clone separation before assigning arithmetic meaning to global length or cusp data

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`.

PF-156--PF-167 classify the visible relative and absolute separator boundaries. The `1/4` threshold is one-ended propagation, selected cusp data depend on admissible reference choices, and the positive primitive-length accumulation interval/finite-length primitive-count explosion are inherited by the exact all-composite shift clone. Neither ordinary global Selberg failure nor positive-window length non-discreteness is a primality selector.

A surviving relative or absolute invariant must therefore be intrinsic under the admitted reference category and separate the prime flute from the clone at the full-surface level.

## Test genuinely global trace-ideal, wave, scattering, or infinite-orbit assembly beyond tail norm-resolvent equivalence

**Linked intuitions:** `MI-004-coordinate-amplification-is-not-operator-amplification` and `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`.

PF-166 gives complete marked-tail length equivalence and PF-168 now gives generalized norm-resolvent equivalence of matched Dirichlet tail Laplacians. Every fixed bounded `C_0` spectral filter on farther-out tails, including positive-time heat operators, becomes asymptotically composite-blind. Longer primitive words, more pants, tail eigenvalue filters, or another bounded tail functional calculus therefore cannot by themselves restore primality specificity.

The remaining operator route is genuinely global: determine whether local squared-resolvent `S_1` budgets plus the full metric comparison assemble into trace-class/spectral-shift/wave equivalence, or whether infinite spectral/orbit multiplicity accumulates norm-small tail defects into a canonical reference-stable scattering/resonance value that separates the clone. The moving Dirichlet cut removes head--tail interaction, so this global assembly question is not settled by PF-168.
