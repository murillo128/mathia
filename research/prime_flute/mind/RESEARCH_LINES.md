# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Control the combined normalized Robin transform in the actual corridor

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`, `MI-006-the-critical-endpoint-needs-structure-and-the-right-concentration-currency`.

PF-247--PF-253 establish the actual critical Jacobi/Bessel-four edge and its sharp square-root separated smoothing window. PF-255 proves that prime-dependent hypercycle compactification has a two-derivative high-to-low barrier, and PF-256 transports that locality to the actual variable-corridor scale `K_a` without derivative loss. Those coordinate/corridor changes cannot realize the arbitrary rotation obstruction of PF-244.

PF-257 then sharpens the noncommutative gate. The physical normalized Robin injection is exactly the polar-free transform `(I+XX*)^{-1}X=X(I+X*X)^{-1}`, equivalently an off-diagonal block of `F(D_X)` with `F(t)=t/(1+t^2)`. A naked polar partial isometry need not be controlled separately; what matters is locality/weighted smoothing of this **combined source transform** for the actual seam.

The live theorem is to prove the required graph/off-diagonal bound for that transform in the `K_a` basis, pass the finite/Galerkin identity to the complete lift with correct domains, and then spend the remaining Bessel-four margin through physical recoupling and global weak-`S_1` reassembly.

## Keep combined local transport separate from physical and global reassembly

The favorable scalar amplitude, hypercycle flow and corridor graph equivalence are genuine positives, but none proves PF-243. Conversely, failure to bound the polar factor `U` alone no longer kills the route because the physical map recombines orientation with its vanishing singular amplitude. The canonical object is the full normalized Robin transform and its final assembled norm.
