# Prime-flute mathematical questions

## Estimate the completed packet response in both arithmetic critical-boundary regimes

**Linked intuitions:** `MI-008-complete-cut-transmission-needs-extension-mass-not-only-boundary-impedance`, `MI-009-bidirectional-scalar-survival-is-killed-by-accumulated-pant-loss`, `MI-010-scalar-green-response-is-killing-normalized-markov-averaging`, `MI-011-sequential-physical-band-elimination-factorizes-the-full-angle-defect`, `MI-012-corridor-form-smallness-needs-seam-metric-locality`, `MI-013-compactification-critical-low-packets-block-uniform-taylor-smallness`, `MI-014-critical-packet-leakage-occupies-a-positive-measure-center-family-with-explicit-parity-scales`.

PF-303--PF-325 reduce the physical angle to a direct channel plus the conditional channel `T_H`, localize the direct channel and express the local Hilbert--Schmidt mass as an exact integral over translated physical packets. PF-326--PF-327 show that seam normalization preserves the fixed-band critical packet and its pre-pant transverse-energy floor.

PF-328 strengthens the witness in exactly the integration variable used by PF-325. Compactification-critical centers occupy intervals of nonzero limiting physical length, the shape-normalized energy limit is uniform across them, and restoring the true seam normalization gives explicit parity scales: symmetric raw critical energy is of order `w_n^-1`, antisymmetric energy of order `w_n` before final angle normalization.

Therefore any completed block satisfying the PF-318 Hilbert--Schmidt budget must suppress order-one completed response on **almost all** of a positive-length family of critical centers; killing one exceptional center is insufficient. The next direct calculation should propagate this entire weighted packet family through the actual one-cusp-pant crossing, physical `L/H` projections, common positive completion and final intrinsic diagonal normalization, in both PF-324 arithmetic boundary regimes. The parity scales must be carried until the destination normalization decides whether they survive or cancel.

## Close the global heavy-range ideal counts after strength/angle factorization

For the PF-287 heavy low/high extension-range projection product, determine whether the conditional channel `T_H` and the shear-deleted reference-angle family satisfy the required ideal counts. PF-318 supplies endpoint counting; PF-320 supplies the shear-error budget; PF-325 supplies the exact position-space representation; PF-326--PF-328 show that the correct seam coordinates preserve a positive-measure critical packet family rather than an isolated pre-pant artifact.

Any argument that replaces the terminal geometry by one fixed finite-pant model is invalid: the critical packet sees both finite-boundary and escaping-boundary subsequences. Any argument that obtains the needed gain solely from the seam normalizer is also invalid before the pant stage. A successful ideal-count theorem must obtain its suppression from downstream pant/projection/completion geometry or prove that the destination erases the arithmetic boundary difference on almost all critical centers.

## Prime realization within the resolved upstream-memory window

Can consecutive prime-gap isolation realize the moderate graded neck ratios of MI-001, or can the surface-to-graph error be sharpened enough to resolve `w_j^2/w_(j-1)` in the hierarchies already known to occur? Arbitrarily strong separation is not the required condition: the correction must still dominate `w_j sqrt(w_1)`.

PF-324 does not solve this older fixed-topology realization problem. Its gap ratio is retained as a critical-boundary location before the completed angle is formed; PF-326--PF-328 preserve the corresponding fixed-band localization and a positive-measure family of critical pre-pant energy witnesses, but survival in a final spectral observable remains a separate theorem.