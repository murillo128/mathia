# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Prove fixed-germ confinement plus energy-linear Sobolev collar localization, or source-specific `C^1` entry

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, and `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`.

PF-171--PF-188 remove the collapsing-central-sector, volume, long-body, artificial-interface, infinite-collar-multiplicity, annular-flux, and linearized Killing obstructions above the trace endpoint. PF-188 gives qualitative marked `W^{1,r}` rigidity for every `r>1` on one fixed larger positive-side collar without boundary normalization; the remaining geometric gate is quantitative fixed-germ confinement plus an energy-linear Sobolev modulus and exact-symplectic cutoff with comparable cost, or genuinely source-specific `C^1` entry.

PF-189 sharpens the endpoint bookkeeping. The complete fixed-central Dirichlet-decoupled short-collar sector lies in weak trace class `S_{1,infinity}` but not in `S_1`, with summable weak-endpoint mass over the full pinching family. The collapsing central cores therefore cannot by themselves create behavior worse than weak trace class. Any worse endpoint for the **uncut** relative resolvent must be generated by body defect, collar/body transmission, cutoff commutators, or nonlocal reassembly.

This does not solve the `S_r`, `r>1`, global splice and does not turn weak `S_1` into trace class. The live comparison remains the energy-linear marked localization needed to control precisely those reassembly channels absent from the decoupled model.

## Isolate the trace endpoint from the reassembly mechanism

The local order-`-2` obstruction keeps every genuinely changed collar outside `S_1`, while PF-189 shows that the entire decoupled short family nevertheless reaches the canonical weak endpoint. A useful endpoint theorem should therefore identify whether the uncut assembly preserves weak `S_1`, degrades it, or admits a different cancellation/renormalization mechanism.

Do not attribute an endpoint failure to collar multiplicity once the complete central family has already been summed. Any new obstruction must be traced to an explicit coupling term introduced by gluing the local pieces back into the full surface.

## Resolve ordered multi-neck memory in marked spectral data

Finite graded chains retain directed neck order while scalar determinants erase it. Any arithmetic claim must identify a marked Weyl/Green/spectral-measure or boundary observable that survives the exact gap-order controls and the now tightly localized geometric comparison.

## Require prime/clone separation before assigning arithmetic meaning

The all-composite shift clone shares substantial geometric and operator structure. Area preservation, near-isometry, Schatten membership, weak trace membership, exact symplecticity, fixed-germ Sobolev rigidity, or marked collar localization is not arithmetic evidence unless a source-specific observable separates the prime flute from its matched clone.
