# Prime-flute mathematical questions

## Control the killing-normalized conversion vertices of the full mixed response

**Linked intuitions:** `MI-008-complete-cut-transmission-needs-extension-mass-not-only-boundary-impedance`, `MI-009-bidirectional-scalar-survival-is-killed-by-accumulated-pant-loss`, `MI-010-scalar-green-response-is-killing-normalized-markov-averaging`.

The scalar constant-mode return problem is now sharply normalized. PF-303--PF-304 assemble the finite chain as

\[
H_N=L_{c,N}+D_N,
\qquad G_N=H_N^{-1},
\]

with `G_ND_N` a reversible Markov kernel and with depth-uniform response control in the natural killing-weighted `ell^p` scales.

PF-305 upgrades this from scalar norm control to an exact map-level factorization. Put

\[
S_N=D_N^{1/2}G_ND_N^{1/2}.
\]

Then

\[
0<S_N\le I,
\qquad
\|S_N\|=1,
\]

and every scalar-mediated physical response

\[
M_N=R_NG_NF_N
\]

factors as

\[
M_N=(R_ND_N^{-1/2})\,S_N\,(D_N^{-1/2}F_N).
\]

Therefore the entire scalar Green reservoir is **symmetric-ideal neutral** after killing normalization. If either normalized conversion vertex is compact or belongs to a two-sided symmetric ideal while the other is uniformly bounded, the same property propagates through the scalar middle. Conversely, the exact unit direction of `S_N` shows that the middle supplies no strict contraction and no automatic compactifying gain.

The live gate is now the actual physical conversion geometry. Derive the genuine finite `P/H` source map `F_N` and reassembly map `R_N`, and determine whether

\[
D_N^{-1/2}F_N
\qquad\text{and}\qquad
R_ND_N^{-1/2}
\]

are uniformly bounded, compact, or in the weak/Schatten ideal required by the PF-281/PF-290 target. If the true nonconstant response does not factor through the scalar chain at all, identify the first operator where that factorization fails. Further estimates of the scalar Green depth cannot resolve this question.

## Global heavy-range projection counts after local witnesses vanish

For the PF-287 heavy low/high extension-range projection product, does the full tail have vanishing essential norm, and can its singular-value counting function satisfy the required weak-trace bound? Local high-pass annihilation in MI-008 alone supplies neither conclusion. The subtraction estimate in MI-004 controls only the artificial central-collar model, not this global product.

PF-305 gives a useful localization principle for any part of this product that genuinely factors through the scalar reservoir: ideal decay must be paid at a normalized entrance/reassembly vertex, not attributed to accumulated scalar return.

## Prime realization within the resolved upstream-memory window

Can consecutive prime-gap isolation realize the moderate graded neck ratios of MI-001, or can the surface-to-graph error be sharpened enough to resolve `w_j^2/w_(j-1)` in the hierarchies already known to occur? Arbitrarily strong separation is not the required condition: the correction must still dominate `w_j sqrt(w_1)`.

The fixed-axis `q>1` Robin leakage route is excluded by MI-007, not an outstanding estimate. Generic conservative `L1` rigidity is excluded by MI-005; a construction matching only the particular canonical germ is a different claim.
