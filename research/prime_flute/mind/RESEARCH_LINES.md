# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among mathematically different mechanisms.

## Resolve how ordered multi-neck memory enters marked spectral data

**Linked intuitions:** `MI-001-relative-multigap-geometry-carries-the-spectral-signal` and `MI-002-localize-in-space-before-spectral-compression`.

Finite graded chains show that a resolved local eigenvalue can retain a leading neck scale together with an upstream Feshbach correction, while scalar determinants erase that directed relation. Determine which marked Weyl, Green, spectral-measure, or boundary data on controlled flute pieces recover the ordered neck sequence and which invariants collapse to symmetric summaries.

## Require reference stability and prime/clone separation before assigning arithmetic meaning to global length or cusp data

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`.

PF-156--PF-165 classify the visible relative separator boundaries: the `1/4` threshold is one-ended propagation, the bottom connected Ruelle layer has a finite positive value with a logarithmic cusp, and the selected cusp coefficient/singularity depends on shift, dilation, and compact reference choices.

PF-167 adds the matching absolute control. The positive primitive-length accumulation interval and the resulting finite-length primitive-count explosion of PF-069/PF-077 are inherited by the exact all-composite shift clone via PF-166 complete marked-tail equivalence. Thus neither ordinary global Selberg failure nor positive-window length non-discreteness is a primality selector.

A surviving relative or absolute invariant must therefore be intrinsic under the admitted reference category and separate the prime flute from the clone at the full-surface level.

## Test collective infinite-orbit or operator assembly beyond complete marked-tail equivalence

**Linked intuitions:** `MI-004-coordinate-amplification-is-not-operator-amplification` and `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`.

PF-166 shows that the prime flute and exact all-composite shift clone have asymptotically identical complete marked-tail translation-length functions, uniformly over words of arbitrary complexity; PF-167 shows that even the positive accumulation interval transfers. Choosing longer primitive words, more pants, or another escaping separator family cannot by itself restore primality specificity.

The remaining operator route asks whether local squared-resolvent `S_1` budgets/global metric comparison assemble into a full trace-ideal or wave/scattering theorem. The remaining dynamical route asks whether infinite orbit multiplicity and interference amplify individually vanishing length defects. A decisive positive must compute a full-surface nonlocal value that separates the clone and is reference-stable; a decisive negative would prove that the global comparison identifies the relevant operator/scattering class.
