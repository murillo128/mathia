# Prime-flute mathematical questions

## Estimate the completed packet response in both arithmetic critical-boundary regimes

**Linked intuitions:** `MI-008-complete-cut-transmission-needs-extension-mass-not-only-boundary-impedance`, `MI-009-bidirectional-scalar-survival-is-killed-by-accumulated-pant-loss`, `MI-010-scalar-green-response-is-killing-normalized-markov-averaging`, `MI-011-sequential-physical-band-elimination-factorizes-the-full-angle-defect`, `MI-012-corridor-form-smallness-needs-seam-metric-locality`, `MI-013-compactification-critical-low-packets-block-uniform-taylor-smallness`.

PF-303--PF-320 reduce the physical angle to a direct channel plus the independent conditional channel `T_H`, localize the direct channel, and show that hypercycle shear is endpoint-small in the intrinsic angle metric. PF-322--PF-324 produce the physical-space negative control: compactification-critical low packets survive away from the seam, and their outgoing boundary has source-realized finite-distance and escaping-distance subsequences determined by consecutive prime-gap ratios.

PF-325 makes the PF-318 fallback exact in the same physical representation:

`||B||_(S_2)^2 = (m/L) integral ||B^* psi_t||^2 dt`, with `m/L~kappa/pi`.

PF-326 closes seam-normalization delocalization. On every fixed physical band, the exact seam-energy inverse square root only reweights the two parity channels by bounded nonvanishing multipliers tending to `(1+xi^2)^(-1/2)` and `1` after common scaling; it commutes exactly with tangential translation.

PF-327 now shows that the same seam-normalized packet also retains a **strictly positive compactification-critical transverse-energy floor** at PF-323's critical centers. After raw shape normalization, local `L^2` convergence of the weighted packet combines with `s_n^2 cosh^2(tau_(n,lambda)+x)->lambda^2e^(2x)` to give an order-one lower bound for the unprojected compactified potential energy and, via PF-233, for the canonical variable-width diagonal transverse form.

Thus neither physical-state density, seam-coordinate localization nor pre-pant critical energy supplies the needed smallness. The next direct calculation should retain the common seam-scale amplitudes and propagate the exact PF-326/PF-327 weighted packet family through the actual one-cusp-pant crossing, physical `L/H` projections, common positive completion and final intrinsic diagonal normalization, proving the required integrated response estimate in both PF-324 boundary regimes. Any fixed-width set of centers retaining order-one completed leakage violates the Hilbert--Schmidt budget through PF-325.

## Close the global heavy-range ideal counts after strength/angle factorization

For the PF-287 heavy low/high extension-range projection product, determine whether the conditional channel `T_H` and the shear-deleted reference-angle family satisfy the required ideal counts. PF-318 supplies endpoint counting; PF-320 supplies the shear-error budget; PF-325 supplies the exact position-space representation; PF-326--PF-327 show that the correct seam coordinate neither delocalizes nor removes the compactification-critical unprojected energy of the fixed-band witness.

Any argument that replaces the terminal geometry by one fixed finite-pant model is invalid: the critical packet sees both finite-boundary and escaping-boundary subsequences. Any argument that obtains superlinear gain solely from the seam normalizer is now also invalid at the shape-normalized pre-pant stage. A successful ideal-count theorem must obtain its suppression from downstream pant/projection/completion geometry or prove that the destination erases the arithmetic boundary difference.

## Prime realization within the resolved upstream-memory window

Can consecutive prime-gap isolation realize the moderate graded neck ratios of MI-001, or can the surface-to-graph error be sharpened enough to resolve `w_j^2/w_(j-1)` in the hierarchies already known to occur? Arbitrarily strong separation is not the required condition: the correction must still dominate `w_j sqrt(w_1)`.

PF-324 does not solve this older fixed-topology realization problem. Its gap ratio is retained as a critical-boundary location before the completed angle is formed; PF-326--PF-327 preserve the corresponding fixed-band packet localization and critical transverse energy through seam normalization, but survival in a final spectral observable remains a separate theorem.
