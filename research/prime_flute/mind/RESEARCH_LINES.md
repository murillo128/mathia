# Prime-flute mathematical questions

## Control the shear-deleted local pant angle in both arithmetic critical-boundary regimes, and the independent conditional channel

**Linked intuitions:** `MI-008-complete-cut-transmission-needs-extension-mass-not-only-boundary-impedance`, `MI-009-bidirectional-scalar-survival-is-killed-by-accumulated-pant-loss`, `MI-010-scalar-green-response-is-killing-normalized-markov-averaging`, `MI-011-sequential-physical-band-elimination-factorizes-the-full-angle-defect`, `MI-012-corridor-form-smallness-needs-seam-metric-locality`, `MI-013-compactification-critical-low-packets-block-uniform-taylor-smallness`.

PF-303--PF-316 reduce the physical angle to a direct channel and an independent conditional channel `T_H`. PF-317--PF-320 then localize the direct channel and show that hypercycle shear is endpoint-small in the intrinsic angle metric even when raw seam-normalized mixed blocks amplify. With local low rank `O(1+log P_n)`, the shear correction costs only `O((1+log P_n)/P_n^2)` in squared Hilbert--Schmidt mass.

The remaining direct theorem is about the **shear-deleted local pant angle** with actual seam normalization and common finite-pant completion. PF-322 rules out uniform Taylor smallness on the whole unprojected physical-low sector. [PF-323](../findings/PF-323-interior-critical-low-packets-avoid-the-cuff-seam.md) removes the seam-artifact escape by recentering the same fixed-band witness at an interior point `tau_(n,lambda)` where `s_n cosh tau_(n,lambda)=lambda`.

[PF-324](../findings/PF-324-critical-packet-seam-distance-remembers-consecutive-prime-gap-ratio.md) now proves that the outgoing seam seen from this critical coordinate is itself arithmetic. With `r_n=h_(n+1)/h_n`,

`D_(n,lambda)=ell_n/2-tau_(n,lambda)=log((1+r_n)/lambda)+o(1)`

and `s_n cosh(ell_n/2)=1+r_n+o(1)`. Since `r_n=(g_(n+1)/g_n)(1+o(1))`, audited consecutive-gap results give subsequences where the seam remains at the finite limiting distance `log(1/lambda)` and subsequences where it escapes every fixed critical window.

The next calculation must propagate the same interior normalized witness through the actual one-cusp-pant quotient, PF-205 seam-energy normalization, physical `L/H` projections and PF-320 common completion **in both regimes**. A proof of uniform superlinear angle decay must identify the suppressing mechanism even when the outgoing seam has escaped to infinity in the natural critical coordinate. If suppression fails, price the surviving singular-value leakage with PF-318 rather than replacing the actual boundary by one universal local model. The conditional channel `T_H` remains independent.

## Global heavy-range projection counts after strength/angle factorization

For the PF-287 heavy low/high extension-range projection product, determine whether the conditional channel `T_H` and the shear-deleted reference-angle family satisfy the required ideal counts. PF-318 supplies the endpoint counting mechanism and PF-320 supplies the shear-error budget.

PF-324 sharpens the negative control for the reference angle: the compactification-critical input has not one but two source-realized boundary regimes. Any ideal-count argument that uses one fixed finite-pant terminal geometry must be replaced by a uniform treatment of the finite-boundary and escaping-boundary subsequences, or by a theorem showing that the destination projection erases their difference.

## Prime realization within the resolved upstream-memory window

Can consecutive prime-gap isolation realize the moderate graded neck ratios of MI-001, or can the surface-to-graph error be sharpened enough to resolve `w_j^2/w_(j-1)` in the hierarchies already known to occur? Arbitrarily strong separation is not the required condition: the correction must still dominate `w_j sqrt(w_1)`.

PF-324 does not solve this older fixed-topology realization problem. Its ratio `h_(n+1)/h_n` is retained as a critical-boundary location before the completed angle is formed; showing that such source dependence survives in a final spectral observable is a separate theorem.

The fixed-axis `q>1` Robin leakage route is excluded by MI-007, not an outstanding estimate. Generic conservative `L1` rigidity is excluded by MI-005; a construction matching only the particular canonical germ is a different claim.
