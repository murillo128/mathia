# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Transfer the free separated Jacobi window through the exact growing zero-edge branch

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`, `MI-006-the-critical-endpoint-needs-structure-and-the-right-concentration-currency`.

PF-228 and PF-233--PF-249 reduce the high-high block to separated Robin-Poisson smoothing and then to two parity Jacobi chains. The free half-line model has a calibrated full-output window `R<5/2`, while the actual chains carry an inverse-square `O(j^{-2})` tail at a first-moment-critical threshold.

PF-250 shows that fractional edge powers retain a supercritical separated weighted window in the half-line inverse-square regime, so the desired `R>1` margin is not automatically destroyed by that tail. PF-251 then identifies the exact threshold state: the zero-edge recurrence selects the **growing inverse-square Jacobi branch**, not the square-root-decaying branch suggested by the earlier heuristic clue.

The live theorem is therefore to analyze the low-energy Green/Jost/kernel behavior around the actual growing branch and prove that the specific parity chains preserve some separated weighted gain `R>1` in the required full-output norm. Only after that local theorem should the margin be spent on the `T_a` replacement and hypercycle-coordinate perturbations.

## Keep local Jacobi conversion separate from physical and global reassembly

The constant half-line model and its full-output separated window are positive controls; the exact threshold branch is now fixed. The unresolved local issue is variable critical-edge stability for the source map, not identification of the free decay exponent.

If that estimate crosses the PF-243 threshold, the frontier moves to physical `P/H` recoupling, one-cusp-pant completion, neighboring-cell extension, and global weak-`S_1` reassembly. Low-touch shear terms remain strongly trace summable under the existing reciprocal-prime weight; the all-composite endpoint clone remains an independent arithmetic control.
