# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they discriminate among mathematically different mechanisms.

## Resolve how ordered multi-neck memory enters marked spectral data

**Linked intuitions:** `MI-001-relative-multigap-geometry-carries-the-spectral-signal` and `MI-002-localize-in-space-before-spectral-compression`.

Finite graded chains show that a resolved local eigenvalue can retain a leading neck scale together with an upstream Feshbach correction, while scalar determinants erase that directed relation. Determine which marked Weyl, Green, spectral-measure, or boundary data on controlled flute pieces recover the ordered neck sequence and which invariants collapse to symmetric summaries.

## Separate geometric prime-gap memory from universal hyperbolic, one-ended, and Selberg/Ruelle propagation

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`.

PF-156--PF-158 make the canonical matched Selberg sectors zero-free through the critical half-plane. PF-159 identifies the apparent `1/4` boundary as one-ended propagation. PF-160 isolates the connected `s=0` obstruction in the bottom layer, and PF-161 shows that the corresponding bottom-Ruelle product has a finite strictly positive value at zero with an `s log(1/s)` branch cusp: the divergent logarithmic derivative is **not** a hidden zero or pole.

Thus both visible canonical boundaries are explained control phenomena. A surviving Selberg/Ruelle mechanism must use primitive word classes outside this separator sector, a genuinely global continuation coupling sectors, or another nonlocal relative invariant whose value survives subtraction of the one-ended response and the finite-nonzero bottom cusp. PF-162 is not used here while its adversarial review remains open.

## Close the global clone comparison, then test the first genuinely nonlocal relative value

**Linked intuitions:** `MI-004-coordinate-amplification-is-not-operator-amplification` and `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`.

The exact prime flute and all-composite shift clone already share compact-resolvent class. The operator route asks for the global estimate `(H_++1)^(-2)-(H+1)^(-2) in S_1`, whose fixed short-collar analogue is known; the geometric route asks for the corresponding global weighted metric/interface assembly.

If that closure succeeds, the remaining target is a **value theorem**, not another regularization: relative spectral shift/scattering, resonance continuation, noncanonical primitive-word coupling, or another nonlocal invariant that separates the exact composite control. Changing resolvent exponent, local heat coefficient, canonical separator subfamily, or reinterpreting the `1/4`/`0` ordinary-sum boundaries is no longer a distinct route.
