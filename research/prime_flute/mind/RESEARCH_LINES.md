# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Control the full normalized Robin source at the mass-one leakage edge

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`, `MI-006-the-critical-endpoint-needs-structure-and-the-right-concentration-currency`, `MI-007-square-root-functional-calculus-closes-the-finite-seam-mass-gap`.

PF-269--PF-271 show that separated propagation changes the endpoint currency from bare global smoothing to high-input leakage into a sublinear, logarithmic under exponential damping, low-output window. PF-272 evaluates that leakage for the naked fixed-axis relative seam: its lowest discrete Green mass gives a strict sublinear exponent and logarithmic leakage room, so the finite-seam pole that kills proportional bare weighting is not itself a deep-low-output obstruction.

PF-273 then shows why this margin cannot be inherited by the physical source. The square-root factor `Q_w^(1/2)=g_w(L)` remains complete Bernstein but its positive representing measure is an absolutely continuous continuum reaching `t=0`, equivalently the critical Green edge `mu=1`; the first-pole gap behind PF-272 closes under the square root.

PF-274 now turns that boundary from an abstract parameter-uniform Jacobi problem into an exact classical transform. The fixed-axis massive Jacobi chain is unitarily equivalent to `I+4 H_*^2`, where `H_*` is the equal-parameter symmetric continuous-Hahn Jacobi operator. Every bounded functional calculus of `L`, including the massive Green kernels and the PF-273 square-root multiplier, therefore has an exact continuous-Hahn spectral integral. This does not prove the required leakage estimate, but it supplies a concrete coefficient representation in which the low-mass edge and the normalized source can be analyzed directly.

The live fixed-axis theorem is the high-input to logarithmic/sublinear-low-output leakage of the **full normalized source/conversion** `(I+XX*)^-1 X`, expressed if useful through the continuous-Hahn transform and with the `t downarrow 0` continuum retained exactly. Determine whether the normalization cancels or suppresses the mass-one edge strongly enough before paying shear and corridor uniformity.

## Treat the naked seam exponent and exact diagonalization as positive controls, not the physical conclusion

Sublinear propagation genuinely rescues the raw seam, and PF-274 exactly diagonalizes the fixed-axis scalar operator. Neither fact transfers the raw first-pole gap through square-root functional calculus or proves the matrix coefficients of the complete normalized source. Future work should use the exact transform to evaluate the composed source rather than stop at solvability of the intermediate operator.
