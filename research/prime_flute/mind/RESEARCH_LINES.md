# Prime-flute research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-flute intuitions. It is not a roadmap, task queue, status page, or history.

## Preserve the intrinsic-six fixed-axis margin through finite seam scale and the actual corridor

**Linked intuitions:** `MI-003-standard-global-selberg-machinery-is-structurally-incompatible`, `MI-004-coordinate-amplification-is-not-operator-amplification`, `MI-005-short-collar-multiplicity-reduces-to-one-local-splice`, `MI-006-the-critical-endpoint-needs-structure-and-the-right-concentration-currency`.

PF-247--PF-257 establish the Jacobi/Bessel-four control geometry, two-derivative hypercycle/corridor transport, and the polar-free form of the normalized Robin injection. PF-258 corrects the fixed-axis seam tangent: the physical first-order normalization inserts a quarter-order corridor sandwich, giving `H=A^{-1/4}LA^{-1/4}` rather than the bounded Bessel-four Jacobi operator `J`. Its ground-state geometry has `m_j~j^2`, `c_j~j^3`, intrinsic distance `sqrt(j)` and volume growth of dimension six.

PF-259--PF-260 then resolve the fixed-axis upper smoothing gate despite unbounded jump rates. Intrinsic Poincare/Sobolev control plus anchored Gaussian large-time bounds and an all-time Davies--Gaffney estimate give separated decay `|<e_i,H^sigma e_j>| \lesssim (i+1)(j+1)^(-2-sigma)` and Hilbert--Schmidt weights for every `R<3/2+sigma`; at `sigma=1/2`, every strict `1<R<2` survives. The corrected tangent therefore retains enough supercritical margin for route admission.

The live theorem is no longer fixed-axis heat-kernel locality. It is to control the finite-`s` crossover in `s^{-1}A^{-1/4}f_{rs}(L)A^{-1/4}` uniformly over the growing high-frequency window, retain some strict `R>1` margin, and then transfer that margin through the actual `K_a` corridor and the combined normalized Robin/Robin--Poisson map with correct complete-lift domains and global weak-`S_1` reassembly.

## Treat Bessel-four locality, intrinsic-six fixed-axis smoothing, and naked polar control as completed local controls

The Bessel-four window belongs to `J`, not directly to the physical relative seam. The corrected intrinsic-six tangent has its own proved upper window, while the physical normalized injection is the combined resolvent transform rather than a naked polar partial isometry. Further fixed-axis sharpness work is secondary unless it changes the finite-`s` or assembled transport budget.
