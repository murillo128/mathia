# MI-004 — Tail equivalence and the sharp central Schatten threshold push Prime Flute to genuinely global transmission

**Evidence level:** supported through PF-171; complete marked-tail asymptotic length equivalence, generalized norm-resolvent tail equivalence, trace summability of all short central squared-resolvent blocks, and the sharp `S_r`, `r>1`, first-resolvent threshold for the complete short central family are proved, while full uncut trace-ideal/scattering separation remains open

## Core intuition

The exact prime flute and the all-composite shift clone are asymptotically indistinguishable on complete marked tails and under every fixed bounded `C_0` spectral filter of farther-out matched Dirichlet tails. Infinite proliferation and pinching of the canonical short collars do not rescue the difference: after central Dirichlet decoupling their squared relative resolvents are trace-summable, and their first relative resolvents already satisfy the sharp two-dimensional threshold `S_r` for every `r>1` while failing `S_1`.

A surviving spectral mechanism must therefore live in **uncut global transmission and assembly**, not in amplifying coordinate defects, adding more escaping primitive words, or summing independent central short-collar contributions. In particular, the interval `1<r<2` cannot be blamed on the complete short central sector.

## Strongest justified principle

PF-121--PF-167 establish the geometric/operator baseline and remove local heat, determinant, cusp, reference, and separator escape routes. PF-168 upgrades complete matched tails to generalized norm-resolvent equivalence, so every fixed bounded `C_0` filter becomes asymptotically clone-blind.

PF-169 assembles every Margulis-short central collar at the squared-resolvent trace-class level. PF-171 then combines the local PF-127 first-resolvent estimate, the uniform `O(P^-3)` source/clone matching, and the complete `O(P^0.525)` short-core multiplicity count. The resulting orthogonal direct sum lies in all `S_r`, `r>1`, with a vanishing tail, and cannot lie in `S_1` because infinitely many nontrivial blocks already have the local trace-class obstruction.

This is an exact localization of the global operator question. Neither zero systole nor the number of short canonical separators creates extra singular-value mass beyond the expected local dimension threshold once the blocks are decoupled.

## What remains possible

The decisive route is an uncut assembly theorem. One outcome would show that complementary body pieces, outer collars, boundary transmission, cutoff commutators, and head--tail coupling preserve `S_r` for every `r>1`, potentially supporting spectral-shift or wave equivalence. The opposite outcome would identify one genuinely global interaction that accumulates despite local/tail summability and yields a canonical reference-stable discriminator.

The non-`S_1` central result is not itself arithmetic and does not imply a full-surface endpoint obstruction. Likewise Dirichlet orthogonality is the feature that makes the complete short-family summation exact; removing the interfaces is now the load-bearing step.

## Status / novelty

Bilipschitz spectral stability, generalized norm-resolvent convergence, functional calculus, collar theory, pseudodifferential Schatten thresholds, and direct-sum criteria are classical. The project-specific synthesis is the narrowed boundary: **bounded tail amplification fails and the complete short central sector already realizes exactly the sharp `S_r`, `r>1`, threshold; only uncut global transmission/assembly can change the prime/clone operator classification**.

## Falsification criterion

Produce a fixed bounded tail filter violating PF-168, a complete short central family violating the PF-171 `S_r`, `r>1`, summability under its hypotheses, or prove a full uncut trace-ideal/wave equivalence that closes the remaining route. A canonical uncut scattering/transmission invariant separating the clone would extend rather than falsify the synthesis.

## Lean-formalizable core

- Generalized norm-resolvent and `C_0` functional-calculus stability.
- Multiplicity-weighted `S_r` summability for every `r>1`.
- Compression argument for the non-`S_1` endpoint.
- Separation between decoupled central ideals and uncut transmission terms.
