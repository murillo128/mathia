# MI-019 — Shell-square blindness descends to one possible blind pretender, but generic dispersion cannot beat support

**Evidence level:** proved for the shell-square full-annihilator geometry through MC-259 under each finding's stated hypotheses. MC-257 removes the wild prime-square kernel, MC-258 determines the descended projector normalization, and MC-259 composes that projector with the current smooth-modulus variance theorem. No fixed-power signed cancellation or full tame shell generation is proved.

MC-238--MC-246 identify smooth-dilation blindness with the annihilator generated by lower primes and reduce the blind family to one rough Möbius multiplicative coset. MC-257 then eliminates the wild order-`r` factor in the actual moving shell: every blind character modulo `q=R^2` factors through the squarefree shell modulus `R`.

MC-258 shows why this conductor descent is not a free support gain. The original blind source projector becomes `B_S^(A)(X)=R^(-1) P_R(g_y;T,-1)`. The smaller modulus is exactly accompanied by the inherited exterior `1/R`, so direct support counting keeps the scale `T/R^2` and the square-root support threshold remains `R=X^(1/4+o(1))`.

MC-259 sharpens the analytic frontier. Applying the Klurman--Mangerel--Teräväinen smooth-modulus variance theorem at the descended conductor and summing over the exact source coset kills the rank-one pretentious main term unless its best-pretending character lies in the blind annihilator. If it survives, **only one blind character survives as structured main term**, and it is precisely the reciprocal-`L` obstruction already exposed by the line.

The generic variance remainder still cannot buy the missing power. The smoothness condition forces the theorem parameter to be no smaller than `(log y)^(-1/3+o(1))`, leaving a remainder at best of scale `T/R^2 (log y)^(-1/6+o(1))` before any blind-index cost. This is weaker than the direct support bound `T/(R^2 log y)`. Thus generic all-residue dispersion compresses the structured obstruction to one mode, but its transverse error is too coarse to improve the support frontier.

The live gain is now sharply localized: control that single blind pretender, prove a **source-coset-scaled** cancellation theorem whose remainder sees the much thinner rough support, or derive genuinely moving-shell tame coupling not visible to generic all-residue variance. Recounting support or invoking the current smooth-modulus theorem through Cauchy--Schwarz is closed.

**Boundary.** MC-259 bounds the certificate supplied by the cited variance theorem, not the true residual. The actual residual may be smaller. The surviving character need not exist, and none of MC-257--MC-259 proves `H_R=G_R`, a tame quotient bound, a critical-strip zero-free region, or RH.