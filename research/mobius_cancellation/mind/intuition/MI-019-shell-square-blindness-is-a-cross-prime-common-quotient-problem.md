# MI-019 — Shell-square blindness is a localized bipartite problem filtered out by the completed higher 2-class rank tower

**Evidence level:** proved for the shell-square full-annihilator geometry through MC-255. MC-247 supplies the conditional GRH/ERH support instance; MC-248--MC-251 give deterministic coding consequences from stated support bounds; MC-252 is an exact separated-prime universality obstruction; MC-253 gives an unconditional fixed-order shell parity law and exact low-rank matched control; MC-254--MC-255 give genuine-prime Rédei controls. No full shell generation, quotient collapse, moving-shell higher-symbol theorem, or fixed-power signed cancellation is proved.

MC-238--MC-246 identify smooth-dilation blindness with the annihilator of the subgroup generated by lower primes and reduce the complete blind family to one rough Möbius multiplicative coset. At quadratic level the destination-visible object is a rectangular lower-prime/shell-prime `F_2` code.

MC-252 shows that quadratic-residue algebra without localization can realize arbitrary finite binary cross-prime matrices. MC-253 shows that the actual moving shell has real bounded-order parity pseudorandomness, but a logarithmic-rank Walsh control has the same qualitative statistics. Fixed-order shell moments are therefore rank-blind.

MC-254 identifies the shell matrix as an off-diagonal block `A` of the classical Rédei matrix. Yet the completed matrix

`R=[[C,A],[A^T,E]]`

sees shell-supported blind directions only through `ker A ∩ ker E`. Genuine prime-discriminant path examples have maximal full Rédei rank while the selected cross-block has rank one.

MC-255 closes the natural higher-rank repair. In those same path controls, maximal full Rédei rank gives `r_4=0`, hence every higher class-group rank `r_(2^j)` for `j>=2` is also zero while `dim ker A` is arbitrarily large. More sharply, every nonzero `v in ker A` satisfies `Ev!=0`, so `(0,v)` is excluded from `ker R_4` before the classical `R_8` map is even defined. The completed higher 2-class filtration does not merely summarize the wrong quantity; it can discard the entire destination-blind subspace at its input boundary.

The missing algebraic resource must therefore remain **localized and bipartite**. A useful higher invariant must act directly on the lower/shell interaction, control `E` on `ker A`, or use shell-localized Kummer/Chebotarev data that provably does not factor through the completed class-group rank tower. The alternative is to bypass generation and estimate the exact rough Möbius coset by signed cancellation.

**Boundary.** The path-prime controls are nonlocal CRT/Dirichlet constructions and do not realize the active second block inside `(y,2y]`. MC-255 does not rule out raw higher Rédei symbols or Kummer data when organized directly on the moving bipartite block. What is ruled out is using scalar `8`-rank, higher `2^j` class-group ranks, or any argument that reaches the block only through that completed filtration as a shortcut to high rank of `A`.
