# MI-019 — Shell-square blindness reduces to one rough Möbius quotient charge after support, dimension, and smooth-convolution coordinates are removed

**Evidence level:** proved for the shell-square quadratic subgroup geometry through MC-245; no fixed-power cancellation, full-rank theorem, or higher-order blind-character elimination follows.

MC-238--MC-243 identify the quadratic blind kernel and show that minimum support cannot determine its dimension. MC-244 then performs exact character orthogonality: the complete blind family is one affine Legendre-cell projector, and the blind-mode count cancels exactly against the density of that cell. Reducing a positive blind dimension therefore gives no automatic power saving; only zero dimension removes the sector.

MC-245 goes one step further and removes the apparent all-powers smooth weight. Because every `y`-smooth factor has zero charge in the quotient, the identity `g_y=mu*s_y` can be regrouped inside the affine cell without approximation. The complete quadratic-blind source becomes exactly a rough Möbius sum restricted to the fixed charge `[lambda(n)]=[lambda(-1)]`.

This gives a genuine logarithmic support improvement from rough density, but the fixed-power boundary is unchanged. More importantly, it reconnects the newest blind-sector problem to the earlier rough-Möbius barrier rather than creating a new `Psi`-weighted object.

Modewise, each blind character still sees `1/L(s,chi_v)` times a harmless finite small-prime Euler factor. Roughing therefore does not erase the reciprocal-Dirichlet-`L` obstruction. But the affine-cell aggregate is a signed sum over all those characters, so cancellation across blind modes is a separate possible mechanism that cannot be inferred from individual mode bounds.

The reusable separation is now: **minimum support, blind dimension, affine-cell density, arithmetic rank, roughness, individual-character difficulty, cross-character cancellation, and final signed source amplitude are different currencies**. Exact inversion has stripped away two misleading proxies—mode count and smooth weight—leaving one source-native quotient statistic.

**Boundary.** MC-245 concerns the quadratic quotient and the strict separation of smooth and shell primes. It does not prove that the affine-cell sum is large, small, or equivalent to an individual character estimate. Higher-order blind sectors can have different quotient geometry. The live alternatives are full quadratic generation or a genuine signed theorem for the rough affine-cell aggregate.