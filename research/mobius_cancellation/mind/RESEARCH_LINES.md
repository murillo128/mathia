# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Control the uncentered mixed sign-support aggregate before principal information is lost

**Linked intuitions:** `MI-013-bost-connes-regularity-needs-an-intermediate-transverse-category`, `MI-014-centered-parity-and-zero-mode-are-separate-currencies`, `MI-015-sign-support-covariance-gives-a-conditioned-quadratic-bridge`.

MC-188--MC-199 show that canonical equivariant/CRT/character rearrangements remain sector-diagonal and that arbitrary nonlinear processing after centering cannot recover the discarded rough principal mode. MC-200 finds the first cheaper algebraic bridge when the squarefree support field is retained alongside the sign field: a mixed quadratic covariance decodes the principal sum with a polynomially large denominator, reducing the cubic source bill of earlier residue aggregation.

MC-201 then removes the transverse support covariance from the RH-scale obstruction. For a sufficiently small logarithmic primorial exponent, its contribution after decoding is already `O_epsilon(X^(1/2+epsilon/2))`; the entire unresolved target-level burden is concentrated in one uncentered same-residue sign-support aggregate `E_{1,2}`. MC-202 shows that affine centering cannot improve this balance. Exact cancellation of the principal coefficient destroys the decoder at the same instant, while approximate cancellation pays the reciprocal conditioning loss.

The live theorem is therefore precise: bound the uncentered mixed source aggregate `E_{1,2}` at the scale required by the well-conditioned decoder using Möbius/squarefree structure that is genuinely cheaper than controlling the principal sum itself. Any preprocessing that centers the support factor must prove an additional source relation; algebra alone cannot both cancel and retain the target coordinate.

## Treat centered-profile processing and affine support centering as controls

The centered signed quotient is information-theoretically blind to the principal sum, the squarefree transverse covariance is already RH-scale harmless in the small-primorial regime, and the full affine centering family has an exact principal/transverse conditioning dichotomy. A useful nonlinear escape must act before the irreversible quotient and replace the remaining mixed source correlation by a demonstrably cheaper theorem rather than hide the principal coefficient in normalization.
