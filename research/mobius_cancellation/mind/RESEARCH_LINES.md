# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Price any factor-lift escape beyond the shell-width state budget

**Linked intuitions:** `MI-073-source-mode-averaging-collapses-to-a-signature-projector`, `MI-074-thin-hyperbolic-shells-tax-independent-coordinate-bandwidth`, `MI-075-exact-product-tangent-fibers-have-subpolynomial-state-volume`, `MI-076-fixed-depth-factor-lifts-cannot-amplify-the-short-shell-state-exponent`.

MC-414 localizes the surviving character information in the thin divisor-dependent endpoint domain. MC-415--MC-418 show that one-variable positive differencing re-squares the sieve on the zero shift and that finite-band stationary positivity pays the sharp Fejer `1/H` floor, unchanged by operator-valued channel lifting.

MC-419 identifies the genuine multivariable possibility: independent translation axes can multiply positive-kernel bandwidth. MC-420 makes independence algebraic: if Fourier support lies in an injected rank-`r` lattice, the positive symbol factors through an `r`-torus, so ambient tensor coordinates do not increase effective dimension. MC-421 then shows that axes manufactured by iterated ordinary van der Corput bring coordinate-zero source-free square faces with reciprocal one-axis Fejer mass.

MC-422 prices axis-aligned factor geometry inside the physical shell. A full positive coordinate box in `X<prod u_j<=X+Delta` obeys `sum B_j/U_j <= Delta/P_0` and `prod B_j <= Delta^d/(d^d P_0^(d-1))`. MC-423 closes the exact-tangent extreme: at fixed depth one product fiber has only `d_d(m)=m^(o(1))` states, and a completely multiplicative total phase is constant across that fiber.

MC-424 now closes the remaining fixed-depth *state-count* interpolation between those extremes. The whole ordered factor shell

`S_d(X,Delta)={(u_1,...,u_d): X<u_1...u_d<=X+Delta}`

has `|S_d|=sum_(X<n<=X+Delta)d_d(n)=X^(theta+o(1))` whenever `d` is fixed and `Delta=X^(theta+o(1))`. Shears, tilted lattices, curved patches and cross-fiber tangent charts therefore cannot manufacture an extra fixed polynomial exponent of distinct support-preserving factor states. If an additional state-volume exponent `delta>0` is sought through growing depth, the Norton/Pollack regime `d=o(log X)` already requires `d >= (log X)^(delta-o(1))`.

The live question is no longer whether fixed-depth near-tangent geometry contains enough hidden states. Can a concrete construction obtain the needed gain from **analytic organization of the bounded shell states**—oscillatory weights, a transformed nonseparable source phase, or a genuinely additional arithmetic coordinate—or from boundary-crossing correlation families not represented by an orbit inside the shell? If multiplicity itself is the resource, can polylogarithmically growing factor depth survive the decomposition, coefficient, source-phase and positive-closure costs that accompany it?

## Audit state count, source phase and closure faces in the same coordinates

Scalar filter banks, positive Toeplitz forms and operator-valued channels stay inside the MC-417--MC-418 cone. MC-419--MC-420 show that formal tensorization earns no bandwidth without effective support rank; MC-421 shows that genuinely independent variables can still arrive with source-free codimension-one faces; MC-422 prices full coordinate boxes; MC-423 freezes both exact-fiber state volume and total completely multiplicative phase; MC-424 bounds the entire fixed-depth shell independently of how its states are parameterized.

A viable escape must therefore identify what survives after quotienting parameter labels to actual arithmetic states, then certify the source phase or weight variation carried by those states and the closure-specific diagonal ledger. A larger continuous tangent dimension, more factor coordinates, or a clever shear is not itself a new resource. The unresolved mechanisms are precisely those not reduced to fixed-depth support-preserving state multiplicity: signed analytic cancellation, extra arithmetic labels that survive quotienting, boundary-crossing correlations, or sufficiently growing factor depth.