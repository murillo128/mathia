# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Measure the weighted quartic source-incidence structure required by the endpoint frame

**Linked intuitions:** `MI-013-bost-connes-regularity-needs-an-intermediate-transverse-category` through `MI-071-exceptional-sets-must-be-measured-on-the-source-difference-frame`.

MC-366--MC-384 progressively separate exact source leakage, finite-resolution quotienting, package conductor, theorem admission and analytic saving. The common-source obstruction is already linear in rank until an analytic ceiling intervenes, and stronger-looking character estimates can simply repay their gain through modulus depth, family breadth or theorem-admission cost.

MC-385--MC-389 identify the correct exceptional geometry. Ambient exceptional counts must be pulled back to the source-difference frame. MC-386 gives the sharp generic Welch scale `H/R`; MC-387 classifies near-extremizers; MC-388 shows that the exact endpoint Bochner support is a high-dimensional projected simplex rather than the required low-dimensional coset; and MC-389 converts that mismatch into an explicit fixed source gap. In the high-rank branch, if ordinary good modes satisfy `epsilon_R=o(R^(-1/2))`, every exceptional set satisfies

`|B| >= (1+1/4096) H/R`

for all sufficiently large `R`.

MC-390 shows that high dual distance is worse rather than better. If `d^perp(W)>=5`, the exact coordinate signs are four-wise independent, and their second/fourth moments force `Omega(H)` source differences to have correlation at least the critical frame scale `1/sqrt(2R)`. Hence the desired `o(R^(-1/2))` good-mode theorem outside `O(H/R)` exceptions is impossible in that regime.

MC-391 sharpens the surviving structural class. Quotient the coordinate characters by `C=W^perp` and merge duplicate characters exactly. If the distinct character classes have masses `w_a`, define

`sigma_A^2=sum_a w_a^2`

and let `Q_4` be the weighted mass of genuine four-distinct collisions `a+b+c+d=0`. The exact fourth moment is

`E|g|^4 = 3 sigma_A^4 - 2 sum_a w_a^4 + 24 Q_4`.

Paley--Zygmund then implies that an `O(H/R)` exceptional family with all other modes `o(R^(-1/2))` requires

`Q_4/sigma_A^4 >= R/(96K) - 1/8 - o(1)`

when `|B|<=K H/R`. In the balanced rank-linear regime this means `Omega(R^3)` genuine four-character relations. A single low-weight dual word is therefore nowhere near enough. Weight-2 words merely identify coordinate characters and are absorbed into the exact quotient; odd-weight words matter to this even-moment obstruction only insofar as they combine to create genuine four-character collisions.

The immediate arithmetic target is now precise: determine whether the actual large-source incidence system has the **weighted quartic collision mass** demanded above while respecting the conductor/source-cost constraints already isolated by the line. Sidon-like or generic high-distance projected character systems are hostile: they force macroscopic critical-scale correlations rather than cancellation.

## Keep source incidence, conductor, exceptional geometry and additive collision mass distinct

The current resource ledger separates exact-real conditioning, finite-resolution quotienting, source-incidence balance, package conductor, common-radical size, q-vdC factor depth, collective family size, theorem admission, terminal saving, source-frame energy, source-image affine rank, additive-energy deficit, dual distance and now weighted genuine four-character collision mass after duplicate-character quotienting.

MC-386--MC-391 show why these currencies cannot be collapsed. Source-image geometry improves the abstract Welch threshold, high coding-theoretic pseudorandomness forces anti-concentration, and low dual distance by itself does not repair the problem. The relevant escape is quantitatively additive: the projected endpoint characters must carry enough weighted nontrivial quartic relations to move the fourth moment far from the Sidon/four-wise-independent regime.

A proposed endpoint theorem should therefore state both the exceptional-set target and the exact quartic source-incidence structure it uses. Further generic Gram inequalities, abstract code-distance improvements or isolated low-weight relations are not useful unless they produce the normalized `Omega(R)` collision mass needed downstream.

## Repair probabilistic comparators only after exact-stratum and algebraic-collapse gates

Probabilistic proxies remain secondary until their conditioning is arithmetically consistent and their observables survive exact multiplicative simplification. A model that becomes degenerate after conditioning on an exact arithmetic stratum, or whose auxiliary statistic collapses algebraically to `M(x)`, has not created an independent cancellation mechanism.

The endpoint results add the same control at the theorem interface. A plausible comparator must preserve the exact source architecture rather than replacing it by an ambient modulus, frame or code class whose extremizers or typical behavior are impossible after source pullback. MC-391 makes the warning quantitative: after exact duplicate-character collapse, a source that remains too Sidon-like cannot support the desired exceptional geometry.
