# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Control the unbounded-support blind sector or prove the required signed amplitude directly

**Linked intuitions:** `MI-013-bost-connes-regularity-needs-an-intermediate-transverse-category`, `MI-014-centered-parity-and-zero-mode-are-separate-currencies`, `MI-015-sign-support-covariance-gives-a-conditioned-quadratic-bridge`, `MI-016-source-aligned-shifts-face-a-defect-or-mertens-dichotomy`, `MI-017-first-shell-crt-common-mode-is-a-source-coupled-energy-coordinate`, `MI-018-nonnegative-transference-does-not-create-fixed-power-sign-balance`, `MI-019-shell-square-blindness-is-a-cross-prime-common-quotient-problem`.

MC-238 identifies smooth-dilation blindness with characters annihilating the subgroup generated by small primes. MC-239 removes prime-square lift defects and one-coordinate blindness. MC-240 first forced every surviving blind character to involve at least seven shell primes.

MC-241 closes the entire **bounded-support** version of that escape. If `k` is the number of active shell-prime factors, then for every fixed `K`, sufficiently large shells have no nonprincipal blind character with `k <= K`. Equivalently, any sequence of blind characters must satisfy `k -> infinity`. In the quadratic Legendre-matrix sector, every nonzero kernel vector therefore has Hamming weight tending to infinity rather than merely exceeding six.

The mechanism uses the exact shell conductor together with the 2026 smooth-modulus character-sum theorem: blindness forces a linear-sized character sum on a dense block of `y`-smooth integers, while a fixed-support shell conductor is smooth enough for power cancellation at the same scale. The contradiction is qualitative in support size; it is not uniform as `k` grows.

The live algebraic obstruction is now genuinely collective. One must obtain character-sum control as the smoothness exponent tends to zero, prove eventual triviality of the annihilator by another source-specific argument, or show that every surviving high-support mode couples too weakly to the actual Möbius progression vector. Even triviality of the blind sector would still leave the fixed-power signed-amplitude/Fourier-decay gate.

## Keep support size, global generation, Fourier decay, and signed amplitude separate

Local factor generation and every fixed-size cross-prime relation are now closed resources. MC-241 does not give a rate for the minimum blind support and does not turn algebraic visibility into cancellation. Future credit should go only to a theorem controlling genuinely growing-support relations or the source-selected signed amplitude at the required power scale.