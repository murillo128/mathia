# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Control the surviving blind pretender or prove source-coset-scaled cancellation

**Linked intuitions:** `MI-013-bost-connes-regularity-needs-an-intermediate-transverse-category`, `MI-014-centered-parity-and-zero-mode-are-separate-currencies`, `MI-015-sign-support-covariance-gives-a-conditioned-quadratic-bridge`, `MI-016-source-aligned-shifts-face-a-defect-or-mertens-dichotomy`, `MI-017-first-shell-crt-common-mode-is-a-source-coupled-energy-coordinate`, `MI-018-nonnegative-transference-does-not-create-fixed-power-sign-balance`, `MI-019-shell-square-blindness-is-a-cross-prime-common-quotient-problem`.

MC-238--MC-246 reduce complete blindness to one rough Möbius coset and the localized lower-prime/shell-prime quotient. MC-252--MC-256 eliminate generic fixed-order residue rank as a source of rigidity once localization is removed. MC-257 closes the wild prime-square escape in the actual factor-of-two shell: the full wild kernel lies in the lower-prime-generated subgroup, so every blind character factors through the squarefree shell modulus `R`.

MC-258 audits the normalization of that descent. Although `q=R^2` descends exactly to `R`, the source projector carries an exterior factor `1/R`, so direct support counting keeps the main scale `T/R^2` and does not improve the `R=X^(1/4+o(1))` support threshold.

MC-259 tests the strongest obvious analytic follow-up. The current smooth-modulus variance theorem applies to the exact shell radical and, after summing over the source coset, its rank-one pretentious main term either cancels or is carried by a **single character in the blind annihilator**. That surviving mode is the reciprocal-`L` obstruction already isolated by the line. But the theorem's smoothness parameter forces a residual weaker than the direct support estimate even before any blind-index cost, so generic all-residue dispersion still cannot supply the missing fixed power.

The surviving problem is therefore narrower: prove cancellation for the one possible blind pretender, obtain a source-coset estimate whose remainder scales with the thin rough support rather than the full residue family, or show that moving-shell localization constrains the tame cross-prime quotient in a way absent from generic dispersion. Recounting support at `R` and feeding the present smooth-modulus variance theorem through Cauchy--Schwarz are both closed.

## Keep conductor descent, projector normalization, pretentious rank, residual scale and signed cancellation separate

MC-257 is an exact structural reduction; MC-258 is an exact normalization audit; MC-259 is a structural rank-one compression plus a quantitative negative result for the available generic variance bound. Together they remove the wild conductor layer and all but at most one structured pretentious mode without shrinking the support exponent.

A smaller conductor and a rank-one surviving obstruction are useful only if the source-specific analytic estimate exploits them. The next theorem must visibly improve the residual at the exact coset scale or control the surviving character; it must not infer a fixed-power gain merely from conductor descent, generic smoothness, or the fact that only one pretender remains.