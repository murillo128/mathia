# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Move fixed-rank gap geometry to genuinely different source resources

**Linked intuitions:** `MI-073-source-mode-averaging-collapses-to-a-signature-projector`, `MI-074-thin-hyperbolic-shells-tax-independent-coordinate-bandwidth`, `MI-075-exact-product-tangent-fibers-have-subpolynomial-state-volume`, `MI-076-fixed-depth-factor-lifts-cannot-amplify-the-short-shell-state-exponent`, `MI-077-factor-multiplicity-does-not-survive-a-mobius-sign-projection`, `MI-078-unbounded-additive-factor-data-buys-state-capacity-only-through-retained-precision`, `MI-079-permutation-invariant-factor-size-needs-inverse-logarithmic-precision-for-polynomial-state-capacity`, `MI-080-fixed-dimensional-symmetric-factor-data-obeys-a-dimension-precision-law`, `MI-081-exact-factor-identity-has-near-linear-orbit-capacity-but-mobius-sign-erases-it`, `MI-082-prime-mark-entropy-controls-factor-quotient-capacity`, `MI-083-sparse-relational-asymmetry-can-preserve-the-full-factor-state-exponent`, `MI-084-global-ordinal-prime-relations-can-saturate-source-capacity-only-by-paying-identity-scale-information`, `MI-085-mobius-divisor-convolution-sees-only-the-full-prime-interaction`, `MI-086-additive-consecutive-chain-statistics-lose-every-interior-motif-under-mobius-projection`, `MI-087-full-order-determinant-interaction-can-collapse-to-fixed-dimensional-additive-moments`, `MI-088-path-distance-determinants-retain-gap-order-only-below-two-permutation-blind-scale-layers`.

MC-423--MC-439 separate raw source diversity, representation fidelity and the top Boolean interaction that survives divisor Möbius convolution. MC-440--MC-442 then show that growing-rank path-distance determinants can retain gap order while their leading layers and all coefficient signs remain source-generic.

MC-443 proves that at every fixed rank the projective gap shapes of selected rational primes are dense in the full positive simplex, and MC-444 shows that compact-interior target shapes already have selected-prime realizations at polynomial source-height cost in inverse precision. Continuous normalized fixed-rank observables therefore cannot regain robust prime specificity through a forbidden range, a positive topological margin, or an unspecified superpolynomial realization tariff.

MC-445 closes the simplest natural-frequency escape for the canonical bounded-height fixed-rank ensemble. Uniformly sampled squarefree `n<=X` with fixed `omega(n)=r` have linearly normalized prime-gap shape converging in probability to the terminal simplex vertex. Dense support and polynomial exceptional realizability coexist with a degenerate natural-source measure. Thus switching from support to the most direct fixed-rank counting distribution does not produce a rich interior carrier.

The surviving gap resources are now genuinely different: growing rank, another explicitly justified source measure, logarithmic or other coordinates that resolve the smaller factors, absolute scale, discontinuous/arithmetic invariants, or cross-rank structure. Any such carrier must still survive the Möbius top-interaction gate and produce an analytic cancellation estimate.

## Test structure beyond generic lcm synchronization and Fourier recombination

**Linked intuition:** `MI-089-ambient-cancellation-helps-a-source-family-only-after-its-weighted-image-pays-the-occupancy-tariff`.

MC-446 computes the exact full-box second moment of the incomplete translated character correlations left by MC-413. The ambient `(u,a)` family has genuine `sqrt(H)` RMS cancellation, but plain Cauchy transfers it to weighted source frames only when their effective occupancy exceeds about `q^2/H`.

MC-447 shows that in the separated range `d,e<=D`, `D^2<q`, `(h,q)=1`, the frame determines the exact lcm and CRT residue. Collisions are elementary and disappear for shifts coprime to the sieve support; fixing the constrained shift coordinate is simply fixing `L`.

MC-448 then prices that fixed-coordinate problem correctly. A mixed-Weil Fourier envelope plus Parseval lowers the generic occupancy tariff to order `p/H`, but a fixed-lcm CRT slice has only `2^omega(L)=p^(o(1))` distinct starts. This closes every fixed-power incomplete regime `H<=p^(1-delta)` for that generic method.

MC-449 removes the near-complete caveat. After centering away the universal complete-period scalar, length `H` is exactly dual to complementary length `p-H` up to translation of the source weights. The generic bound is controlled by `m(H)=min(H,p-H)`, so the occupancy tariff is `p/m(H)`. Near both endpoints the source slice is polynomially deficient; in the central range its subpolynomial occupancy can yield at most a subpolynomial conductor saving. The entire **one-fixed-lcm, uniform Fourier-envelope plus generic Parseval/Cauchy route is therefore power-closed for all interval lengths**.

MC-450 tests the next apparent escape directly. The exact dilation covariance `widehat g_(h/L)(mL)=widehat g_h(m)` really does synchronize fixed-lcm slices on a common frequency variable, but after full recombination the synchronized dual expression is exactly the Fourier representation of the original centered additive-difference autocorrelation. Generic dual Cauchy is therefore the same estimate as physical-space Cauchy, not a new cancellation mechanism. Moreover a fixed-lcm CRT start set has a deterministic low-frequency resonant band unless the relevant coefficient zero mode is cancelled.

The accepted q-vdC clue must now retain information discarded before that recombination: signed well-factorable coefficients, conductor factorization, higher-order CRT/cycle structure, structured cancellation across several lcm values before generic scalarization, genuinely collective variable-length structure, or another nonuniform estimate correlated with the actual source coefficients. More ambient moments, hidden fibre counts, treating `a` as free, moving toward a complete interval, or merely reindexing several lcm slices onto common Fourier frequencies are closed as conductor-power mechanisms in the present framework.

## Separate description sparsity, source measure, quotient entropy, interaction depth and analytic usefulness

Small alphabets, rich support, high nominal pair count and favorable ambient averages are all representation-level quantities until the relevant quotient is taken. MC-445 shows that support and natural-source measure can point in opposite directions; MC-446--MC-450 show that ambient moment strength, constrained-coordinate improvement, source-image occupancy and a seemingly richer synchronized frequency representation can do the same.

A surviving mechanism must state the resource after scale quotienting and Möbius interaction extraction, specify the source measure or weighted image actually used, remove universal periodic modes before pricing the remainder, and prove that the retained structure reaches the analytic destination **before a generic recombination erases it**. Representation richness, frequency synchronization or collective cancellation outside the admissible arithmetic image does not count by itself.
