# Möbius-cancellation research lines

This file records the current mathematical questions that survive the Möbius-cancellation evidence. It is not a roadmap, task queue, status page, or history.

## Move fixed-rank source geometry to a genuinely retained arithmetic resource

MC-423--MC-445 separate source diversity, representation fidelity, Möbius top-interaction and natural-source measure. Fixed-rank normalized prime-gap geometry is dense in the ambient simplex and polynomially realizable, while the most direct bounded-height natural ensemble concentrates on a terminal simplex vertex. Continuous fixed-rank normalized observables therefore do not acquire robust prime specificity merely from support richness or from switching to the canonical counting measure.

The surviving resources are qualitatively different: growing rank, another justified source measure, absolute scale, discontinuous arithmetic data, cross-rank structure, or coordinates that retain enough factor identity to survive the Möbius projection. Any such carrier still has to produce an analytic cancellation estimate after the exact top-interaction quotient.

## Keep first-exit progression, cutoff and source weights coupled after affine normalization

MC-446--MC-483 progressively close generic well-factorability, positive Fejer/q-vdC averaging, complete-period CRT cancellation, divisor-by-divisor completion, full-primorial Fourier scalarization, ordinary `l^2` coupling, coefficient-blind restriction, positive Selberg transfer and two-sided beta-sieve bracketing as independent sources of a conductor-power gain. The common failure is premature scalarization: retained factor variables are erased before the character estimate can exploit them.

MC-484 removes the uncontrolled bracket selector without weakening the hard mask. Buchstab first exit gives an exact decomposition in which the same first-exit prime `q` controls the shortened source interval, a translated-ratio character shift and the residual dimension-two pair-sieve mask. MC-485 then rules out the complete coefficient-blind phase simplification: the bare inverse-shift character family has Gram `pI-J-vv*` and norm `sqrt(p)` on the codimension-two complement. MC-486 closes the sparse-subset escape: every incomplete family with at least three distinct nonzero `q` residues still has operator norm exactly `sqrt(p)`. Prime incompleteness is therefore not a new mechanism once the moving coefficients have been frozen.

MC-487 sharpens the representation itself. The common visible inverse shift `h q^(-1)` in the character phase and pair-sieve mask is **affine gauge**, not an independent arithmetic resource. On each first-exit root branch, returning to the intrinsic integer coordinate (`x=qm=n` or `x=qm=n+h`) simultaneously fixes the translated-ratio separation to `+/-h` and the pair-sieve polynomial to `x(x+/-h)`. What genuinely remains `q`-dependent is the arithmetic-progression support, exact endpoints, nested roughness cutoff `P(q)`, and the actual outer/staged coefficients.

The live theorem must therefore be stated in that normalized frame. One needs a conductor-sensitive weighted Gram, dispersion, bilinear or reciprocity estimate in which the **q-dependent progression incidence, changing pair-sieve cutoff and exact source weights remain inside the cross-q sum before absolute values**. An argument that derives its gain only from the motion of `h q^(-1)` fails the affine-gauge test; an argument that then freezes or common-majorizes the progression/cutoff profile falls back into the coefficient-blind obstructions of MC-485--MC-486.

A separately surviving possibility is a proved arithmetic alignment of the actual first-exit coefficient vector with a favorable subspace of the normalized family. That would have to be a theorem about the real source coefficients, not a generic consequence of sparse `q` support. For nonempty rough-block pieces, retain the exact bin-product support pruning and price only the surviving construction-specific coefficient mass. Redesigned non-Fejer architectures remain separate and must re-pay interval geometry, diagonal cost and favorable-set concentration rather than treating reweighting as free.

The accepted clue `CLUE-averaged-qvdc-source-frame-variable-retention` has already been narrowed through MC-487 to this affine-normalized progression/cutoff problem and needs no Mind-owned status change. The next obstruction test should be applied only after this normalization: if the proposed estimate is invariant under arbitrary rowwise relabeling that erases progression incidence and cutoff nesting, it is not using the intrinsic first-exit source.