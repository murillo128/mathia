# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Prove fixed-power signed near-balance or eliminate the high-support cross-prime blind sector

**Linked intuitions:** `MI-013-bost-connes-regularity-needs-an-intermediate-transverse-category`, `MI-014-centered-parity-and-zero-mode-are-separate-currencies`, `MI-015-sign-support-covariance-gives-a-conditioned-quadratic-bridge`, `MI-016-source-aligned-shifts-face-a-defect-or-mertens-dichotomy`, `MI-017-first-shell-crt-common-mode-is-a-source-coupled-energy-coordinate`, `MI-018-nonnegative-transference-does-not-create-fixed-power-sign-balance`, `MI-019-shell-square-blindness-is-a-cross-prime-common-quotient-problem`.

MC-188--MC-237 reduce the hard first-shell obstruction to source-selected signed Möbius progression sums. Support closes `2alpha+beta>=1/2`; in the strict hard region the target requires a fixed-power relative sign gain. Both signs already have essentially full occupancy, while audited transference and individual smooth-modulus variance improve the exact CRT scale only subpower relatively. None reaches the required signed amplitude.

MC-238 identifies the aggregate smooth-dilation character gate. Every selected residue lies in one coset of

\[
H_y(Q_S)=\langle p\bmod Q_S:p\le y\text{ prime}\rangle,
\]

so characters in `H_y(Q_S)^\perp` see no cancellation from residue spreading.

MC-239 reduces the shell-square blind sector to genuine cross-prime common-quotient relations. The prime-square congruence kernel is already generated and every individual shell-prime factor is generated asymptotically, so local lift defects and one-coordinate blindness are closed.

MC-240 now removes every **bounded-support** blind relation through support six. If

\[
\chi=\prod_{r\in S}\chi_r\in H_y(Q_S)^\perp
\]

is nonprincipal and `T(chi)={r:chi_r!=1}`, then for sufficiently large `y`,

\[
|T(\chi)|\ge7.
\]

The mechanism is the classical prime character-nonresidue exponent: a character supported on at most six shell primes has conductor at most `(2y)^6`, so Pollack's prime witness appears below `y` and contradicts blindness on all small primes. In the quadratic sector this gives

\[
\operatorname{spark}_{\mathbb F_2}(A)\ge7
\]

for the Legendre matrix of MC-238.

The remaining algebraic obstruction is therefore not merely cross-prime; it is **high-support cross-prime compatibility**. One must determine whether simultaneous relations coupling seven or more shell factors can persist, and if so whether the source-selected progression vector couples to them harmlessly or produces an exact blind mode. Even triviality of the annihilator would still leave the quantitative Fourier-decay/fixed-power signed-amplitude gate.

## Treat signed amplitude, subgroup support, support size of blind characters, and Fourier decay separately

High incidence, two-sign abundance, smooth modulus applicability, local factor generation, and elimination of low-support annihilator characters are all weaker than the live target. MC-240 is an exact frontier narrowing, not a power-saving cancellation theorem.

Future progress should be credited only when it controls the source-selected signed amplitude at the required power scale, rules out or quantitatively neutralizes the **high-support** blind sector, or exploits a source-value coupling beyond residue support. More local primitive-root results or bounded-support character exclusions attack resources that are now already closed.
