# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Control the surviving blind pretender or prove support-sensitive cancellation below the square-root modulus barrier

**Linked intuitions:** `MI-013-bost-connes-regularity-needs-an-intermediate-transverse-category`, `MI-014-centered-parity-and-zero-mode-are-separate-currencies`, `MI-015-sign-support-covariance-gives-a-conditioned-quadratic-bridge`, `MI-016-source-aligned-shifts-face-a-defect-or-mertens-dichotomy`, `MI-017-first-shell-crt-common-mode-is-a-source-coupled-energy-coordinate`, `MI-018-nonnegative-transference-does-not-create-fixed-power-sign-balance`, `MI-019-shell-square-blindness-is-a-cross-prime-common-quotient-problem`.

MC-238--MC-246 reduce complete blindness to one rough Möbius coset and the localized lower-prime/shell-prime quotient. MC-252--MC-256 eliminate generic fixed-order residue rank as a source of rigidity once localization is removed. MC-257 closes the wild prime-square escape in the actual factor-of-two shell: the full wild kernel lies in the lower-prime-generated subgroup, so every blind character factors through the squarefree shell modulus `R`.

MC-258 audits the normalization of that descent. Although `q=R^2` descends exactly to `R`, the source projector carries an exterior factor `1/R`, so direct support counting keeps the main scale `T/R^2` and does not improve the `R=X^(1/4+o(1))` support threshold.

MC-259 tests the strongest obvious individual-modulus analytic follow-up. The current smooth-modulus variance theorem applies to the exact shell radical and, after summing over the source coset, its rank-one pretentious main term either cancels or is carried by a **single character in the blind annihilator**. That surviving mode is the reciprocal-`L` obstruction already isolated by the line. But the theorem's residual is weaker than direct rough support, so generic all-residue dispersion cannot supply the missing fixed power.

MC-260 tests the complementary support-sensitive variance route. Harper's sparse BDH theorem has the desired diagonal scale `Q sum |a_n|^2`, but only for `Q>sqrt(2x)`. At the descended modulus `R=X^(c+o(1))` this would require `c>1/2`; at the original square modulus `R^2` it requires `c>1/4`, exactly where the elementary `T/R^2` support bound already reaches square-root size. Thus the currently available sparse diagonal regime begins only after the live shell problem has disappeared.

The surviving problem is therefore precise: control the one possible blind pretender, prove a **fixed/sub-square-root smooth-modulus support-sensitive variance or source-coset cancellation theorem** for the actual rough Möbius source, or find an exact source transformation that reaches such a regime without losing normalization. Moving-shell tame coupling remains another possible route. Recounting support, feeding the current smooth-modulus theorem through Cauchy--Schwarz, or importing a large-modulus sparse BDH theorem without changing the source problem are closed.

## Keep conductor descent, projector normalization, pretentious rank, support-sensitive normalization, modulus range and signed cancellation separate

MC-257 is an exact structural reduction; MC-258 is an exact normalization audit; MC-259 is a structural rank-one compression plus a quantitative negative result for the available smooth-modulus variance bound; MC-260 shows that the closest generic sparse-variance theorem has the right support scaling but the wrong modulus range.

A smaller conductor, a rank-one surviving obstruction and a support-sensitive diagonal are useful only if they coexist at the **actual source modulus**. The next theorem must visibly improve the residual in the hard range `0<c<1/4` or control the surviving character; it must not infer a fixed-power gain merely from conductor descent, generic smoothness, sparse normalization at unrelated large moduli, or the fact that only one pretender remains.