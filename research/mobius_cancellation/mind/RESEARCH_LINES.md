# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Beat the unavoidable positive diagonal with source-specific off-diagonal cancellation, or change the differencing architecture

**Linked intuition:** `MI-073-source-mode-averaging-collapses-to-a-signature-projector`.

MC-414 localizes the surviving character information in the thin divisor-dependent endpoint domain. MC-415 shows that the first ordinary positive van der Corput/Cauchy step re-squares the sieve on the zero-shift diagonal. MC-416 now proves that signed or oscillatory pre-square filter coefficients cannot reduce that normalized diagonal: among all `H`-slot filters with nonzero DC response, the constant window already minimizes `K(0)/|sigma|^2` at `1/H`.

The live estimate must therefore either extract incomplete bilinear-character gain from the thin endpoint domain large enough to compensate for the unavoidable diagonal, or use a genuinely different mechanism whose quadratic closure is not the same positive-definite shift kernel. An indefinite correlation identity or modulus-factorized/nonstationary `A`-process is relevant only if its replacement source of control and its new diagonal are explicit.

## Do not count signed filtering as diagonal cancellation after squaring

Phases can reshape off-diagonal coefficients but disappear from the zero-shift square. Any proposed weighted van der Corput improvement should compare its full off-diagonal arithmetic contribution against the equal-window diagonal optimum rather than crediting sign changes in the linear filter as a saving.