# Möbius-cancellation research lines

This file holds the current mathematical questions suggested by the durable Möbius-cancellation intuitions. It is not a roadmap, task queue, status page, or history.

## Find a source-aligned relation that is neither support-sparse nor Mertens-complete after averaging

**Linked intuitions:** `MI-013-bost-connes-regularity-needs-an-intermediate-transverse-category`, `MI-014-centered-parity-and-zero-mode-are-separate-currencies`, `MI-015-sign-support-covariance-gives-a-conditioned-quadratic-bridge`, `MI-016-source-aligned-shifts-face-a-defect-or-mertens-dichotomy`.

MC-188--MC-208 show that canonical equivariant/CRT/character rearrangements remain sector-diagonal, symmetric nonlinear enrichment loses source order, support-based shifts collapse to sparse defects, and completed logarithmic prime incidence becomes fixed-power equivalent to Mertens. The square-defect branch retains a genuine ordered relation but pays a square-modulus level-doubling cost if its tail is discarded absolutely.

MC-209 gives a more faithful organization. Dyadically, the entire square-defect tail is controlled by the weighted prescribed-residue energy `V_D=sum d |A_d|^2`; its diagonal already has the target `O(X)` size. The missing statement becomes aggregate two-point Möbius cancellation at diagonal scale, a BDH/averaged-Chowla type variance theorem rather than pointwise polynomial Chowla for every frozen cubic.

MC-210 identifies the remaining trap. Standard centered progression variance removes the residue-class mean, but that mean is the rough prefix `R_{qd}` and is fixed-power equivalent to the original Mertens prefix through exact subpower-complexity transforms. Controlling centered fluctuations and the principal mode separately therefore does not make the problem cheaper.

The live theorem must control the **uncentered source-prescribed square-modulus energy**, or exploit cancellation between its principal and centered components, without importing a Mertens-strength bound for the principal mode. A different relational observable remains credible only if its dense nonprincipal information survives both completion and averaging without becoming either a sparse defect statistic or an invertible re-encoding of Mertens.

## Treat support defects, completed incidence, and centered variance as information-loss controls

Permutation symmetry can erase source order, support shifts can become automatically small on rare holes, prime-power completion can reconstruct the principal sum, and centered variance can project that principal mode away. A viable nonlinear escape must state exactly which ordered source relation remains after all four controls and why the theorem estimating it is quantitatively cheaper than Mertens itself.