# MI-007 — Density-scale screening is a depth-weighted lower-tail problem after multiplicity slack and free screens are removed

**Evidence level:** supported by WI-126--WI-136; the zeta-scale localization inputs are literature-backed in the persisted findings

## Core intuition

One smallest Schur eigenvalue and raw rank are not the right quantitative invariants for off-line screening. Density-scale horizontal depth can be hidden near Lamzouri sharpness only through a macroscopic low-energy sector in the **correct quotient after multiplicity and zero-cost screening reservoirs are removed**.

The latest remainder bookkeeping strengthens that quotient. Duplicate/multiplicity directions can create automatic zero modes and must be removed, while simple-real directions are not a free screen once Lamzouri's `U`-remainder is recombined with the horizontal remainder. The source theorem should therefore control a depth-weighted lower spectral distribution relative to the smaller exceptional space `U`, not the earlier `V`-projected minimum.

## Strongest justified principle

WI-133 shows that endpoint taper can force `lambda_min` to zero while total horizontal charge remains extensive. WI-134 retains the full spectrum and proves `H>=a(D_{2,A}-A^2 r_a)_+`; macroscopic bounded depth near sharpness requires a macroscopic lower spectral sector, not one soft mode.

WI-135 removes automatic zero modes created only by multiplicity expansion/slack, so the lower-tail statement must be applied to the genuinely independent quotient rather than count algebraic duplicates as screening.

WI-136 then recombines Lamzouri's exact nonnegative remainders. If `U` is generated by repeated-real and off-line-even data and `V` further includes simple-real directions, near saturation forces

`sum_z m_z dist(h_z,U)^2=o(N)`,

not merely distance to `V`. The component represented by `V\ominus U` is charged by the `U`-remainder; simple-real zeros cannot absorb off-line odd directions for free. The normalized `U`-projected Schur matrix is therefore the stronger source-aligned carrier.

## What remains possible

A decisive positive should derive, for actual zeta zeros, a positive spectral quantile, depth-weighted trace lower bound, cluster-aware divided-difference frame estimate, or equivalent coercivity theorem after all multiplicity and external-reservoir slack is quotiented out. A negative would construct source-admissible configurations with macroscopic bounded depth and the required macroscopic `U`-near-null sector.

## Status / novelty

Schur complements, majorization, trace rearrangement, and divided-difference frame theory are classical. The synthesis is the corrected screening invariant: **macroscopic hiding requires macroscopic depth-matched spectral collapse in the non-free quotient**.

## Falsification criterion

Produce macroscopic bounded depth with subextensive full Lamzouri slack but without a macroscopic low spectral sector in the corrected independent `U` quotient, or prove a source theorem excluding that collapse.

## Lean-formalizable core

- Multiplicity-zero-mode quotient.
- Depth/spectrum rearrangement bound.
- Exact decomposition `U^perp=(V\ominus U)\oplus V^perp` and charging of both parts.
