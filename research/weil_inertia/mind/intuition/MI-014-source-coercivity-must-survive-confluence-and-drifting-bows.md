# MI-014 — Source coercivity must survive confluence, drifting bows, and the self-dual prime covariance gate

**Evidence level:** exact source-geometry, zero-count, visibility, local-resolution, and shifted-prime reductions through WI-188

## Core intuition

The missing Weil-inertia source theorem cannot come from a more faithful low-rank correction to the existing finite geometry, and it cannot be recovered by simply localizing the known Montgomery form-factor estimate around a Maynard--Pratt bow. The bow controls now force a much more specific arithmetic target.

At source-compatible count saturation, functional-equation reflection and zero counting force reciprocal support frequency `alpha=1/2`; the associated prime-side length is therefore `sqrt(T)`. A bow of height `T^epsilon/log T`, `epsilon<1/2`, is too short for black-box local mean-value theory, while reopening the von-Mangoldt coefficients leaves a self-dual chirped shifted-prime covariance whose oscillation has many lattice aliases. The required gain must be genuinely signed and source-specific.

## Strongest justified principle

WI-182 proves that the natural proximal correction is dominated by the Lamzouri remainder and vanishes on confluence controls. WI-183 then imports the slowly drifting bow as a stronger source falsifier: locally it looks like screened vertical arithmetic progressions while its horizontal depth varies globally.

WI-184 adds exact source compatibility. Mirror closure and Riemann--von Mangoldt counting force spacing parameter `c>=4 pi-o(1)`; at count saturation `c=4 pi`, the first reciprocal line is `alpha=1/2`. WI-185 shows that the complete principal self-form of a `T^epsilon` selected bow is nevertheless globally subscale, and WI-186 shows that a black-box signed portfolio cannot improve the certified signal-to-error ratio when the pointwise arithmetic error is propagated by total variation.

WI-187 tests the obvious short-height repair. At reciprocal frequency `alpha_*=2 pi/c`, local diagonal dominance from the classical Montgomery--Vaughan theorem needs vertical length exceeding the Dirichlet length `T^{alpha_*}` at exponent scale. Count saturation gives `alpha_*=1/2`, impossible for fixed `epsilon<1/2`. Increasing `c` to lower the arithmetic length forces the selected mirror-closed bow to occupy less than `2 epsilon` of the local zeros, so a large complementary reservoir necessarily appears.

WI-188 then keeps the special von-Mangoldt coefficients rather than using an arbitrary-coefficient theorem. Positive prime-pair bounds improve the absolute off-diagonal only logarithmically; even conjecturally sharp **untwisted** pair counts remain insufficient after triangle inequality. At `x=sqrt(T)`, the height chirp has derivative sweeping `Theta(h)` integers for shift `h`, so rapid phase variation does not give free nonstationarity on the lattice. The live target is a signed, chirped shifted-von-Mangoldt covariance for shifts up to the growing local range.

The durable source criterion is therefore: **a successful anti-confluence theorem must charge information that survives slow horizontal drift and must prove signed arithmetic cancellation at the self-dual localized scale, rather than relying on positive density, extraction, or generic oscillation separately.**

## What remains possible

A source-specific localized explicit formula may exploit prime coefficients more deeply than positive pair bounds, a smoother dual observable may shorten the effective arithmetic length, or bow--reservoir cross terms may carry a useful sign/inertia relation. A theorem ruling out the bow geometry directly would also be valid coercivity.

What is no longer justified is to treat local normalization, prime sparsity, Hardy--Littlewood-sized positive pair counts, or a first-derivative exponential-sum heuristic as sufficient. Each loses the signed covariance that WI-188 isolates.

## Status / novelty

The finite spectral controls, zero-count bookkeeping, local mean-value reduction, and covariance expansion are persisted exact results assembled from classical or audited arithmetic machinery. The durable synthesis is the source boundary: **the count-saturating bow converts the qualitative source problem into a self-dual growing-shift signed-prime covariance problem.** No unconditional zero-density improvement or RH consequence is claimed.

## Falsification criterion

Derive the required localized bow exclusion using only black-box Montgomery--Vaughan mean-value control at count saturation; prove diagonal dominance from positive untwisted prime-pair bounds after absolute summation; or show that the WI-188 chirp is uniformly nonstationary on the integer lattice in the claimed range. Any such result would remove one of the gates above.