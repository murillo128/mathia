# MI-017 — Finite-aperture source screening survives every prime layer; only genuine first-crossing rigidity can defeat it

**Evidence level:** the phase-cone identities through WI-258, the one-prime screening obstruction WI-263, and the all-finite-prime screening theorem WI-264 are exact. WI-253 records certified unrestricted positivity at radius `0.8`, so prime activation is already guaranteed before any hypothetical first global crossing. No negativity of the true first-crossing mode, no incompatibility of screening with the null equation, and no RH conclusion is proved.

At a hypothetical first unrestricted localized Weil crossing, WI-253--WI-258 reduce useful source information to the aperture lower envelope and phase-coupled inequalities. Earlier work treated independent positivity at `r=7/20` as the activation gate for the first prime. WI-264 corrects that dependency: the certified theorem already used in WI-253 gives `lambda_0.8>0` for arbitrary complex tests, hence monotonicity places every hypothetical first crossing beyond `0.8`. The FP-0.35 audits of WI-259--WI-262 remain valid checks of those artifacts, but are not a prerequisite for the live phase-defect program.

WI-263 shows that one active prime is not enough: a full-span antisymmetric two-bump function can have a positive-definite autocorrelation gap containing `log 2`, while its nonzero autocorrelation pieces align with the favorable signs of Suzuki's continuous source.

WI-264 removes the hope that additional finite prime layers repair this. At any finite aperture there are only finitely many active von-Mangoldt lags, and the largest lies strictly below `2a`. Choosing endpoint bumps narrower than both the first-prime lag and the final source-to-endpoint gap puts **every** active `log n` inside the same autocorrelation zero interval. Simultaneously the self-correlation sits where the continuous source is positive and the endpoint cross-correlation where it is negative. Thus the source-weighted phase defects and scalar aperture transform can remain nonnegative for all modulation frequencies while every discrete prime-power term is screened exactly.

The missing resource is therefore not source cardinality, support span, positive-definiteness or sign bookkeeping. It is **first-crossing rigidity**: the exact equation `A_(a_*)v=0`, the whole lower-envelope family `F_v(r)>=r lambda_r`, or another equivalent constraint strong enough to forbid the endpoint-gap geometry or force quantitative autocorrelation mass at source lags. A proof that reduces those constraints to a scalar inequality already satisfied by WI-264 has discarded the needed information.

**Boundary.** The endpoint-bump family is not a null vector of the localized Weil operator and is not constructed to satisfy the quantitative firstness envelope. WI-264 therefore narrows rather than refutes the first-crossing program. The decisive next test is whether actual zero-mode/firstness information excludes all screened finite-aperture autocorrelations; merely activating more primes cannot do so.