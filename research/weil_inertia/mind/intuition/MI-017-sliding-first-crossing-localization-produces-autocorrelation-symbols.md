# MI-017 — The phase-coupled source cone first needs exact finite-scale recertification past `log 2/2`

**Evidence level:** the phase-cone identities of WI-253--WI-259 are exact, while WI-260 proves that the current public post-fix `FP-0.35` generator does not yet rigorously certify the exact `lambda_(7/20)>0` statement as written. No incompatibility with a compactly supported global first zero mode is proved.

At a hypothetical first unrestricted localized Weil crossing, translation freedom produces an exact sliding-aperture profile. WI-253--WI-258 reduce the useful source information to phase-coupled inequalities of the form

`S_v(r)+J_v(r,t)>=0`,

where `J_v` is a localized cosine transform of the von-Mangoldt/archimedean source-weighted autocorrelation. A source-forced negative localized defect would create strict firstness slack.

WI-259 identifies the activation prerequisite. The first Mangoldt atom enters only above `r=(log 2)/2`. If strict positivity at `r=7/20` is established, monotonicity forces every hypothetical first crossing past that radius and, because `log 2<7/10<log 3`, the fixed phase cone contains exactly the single atom `n=2`.

WI-260 audits the latest public `FP-0.35` source after its midpoint/radius repair and shows that **cold replay is not enough**. The generator still constructs parts of the exact first-prime matrix from rationalized or binary-float point approximations without enclosing the intended exact transcendental quantities. Its Arb residual loop also uses partial-order comparisons to form an apparent maximum and, even if that aggregation were repaired, checks an entrywise maximum rather than an induced submultiplicative matrix norm. The reported positive margin is therefore numerical evidence, not yet an exact certificate of the theorem gate.

The required next action is mathematically precise: independently recertify the exact matrix. Enclose `log 2`, `sqrt 2`, `tau=20 log 2/7`, exact harmonic values and the `Q[tau]` Legendre-polynomial entries directly; then certify positivity using a valid interval factorization or a rigorous induced-norm inverse residual. Only the resulting exact lower margin may activate the one-prime phase program.

This refines the reusable lesson. **Source activation is a separate gate from source sign, and certificate correctness is a separate gate from numerical reproducibility.** Reproducing the same flawed proof boundary does not move the epistemic state, even when the numerical candidate looks robust.

**Boundary.** WI-260 does not show `lambda_(7/20)<=0`; the nominal margin may survive exact recertification. It does not alter WI-259's conditional implication or prove a negative phase defect. Until recertification closes positively, the single-prime cone remains a conditional research surface rather than established input.
