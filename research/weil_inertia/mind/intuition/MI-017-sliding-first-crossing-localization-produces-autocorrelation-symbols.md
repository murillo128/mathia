# MI-017 — The one-prime phase route needs both exact recertification and first-crossing rigidity beyond autocorrelation positivity

**Evidence level:** the phase-cone identities of WI-253--WI-259 and the screening obstruction WI-263 are exact. WI-260--WI-262 prove that the audited public FP-0.35 paths do not certify the required exact `lambda_(7/20)>0` premise as written. No negativity of the true operator, no actual first-crossing counterexample, and no incompatibility of screening with the zero-mode equation is proved.

At a hypothetical first unrestricted localized Weil crossing, WI-253--WI-258 reduce the useful source information to phase-coupled inequalities `S_v(r)+J_v(r,t)>=0`. WI-259 identifies the activation prerequisite: if strict positivity at `r=7/20` is certified, monotonicity pushes every hypothetical first crossing beyond `(log 2)/2`, while `log 2<7/10<log 3` makes the fixed phase cone contain exactly the single Mangoldt atom `n=2`.

WI-260--WI-262 separate three certification gates before that implication can be used. The theorem matrix must be assembled with the exact prime/Gamma data, the enclosure must genuinely contain that matrix, and the positivity judge must have correct Schur algebra plus outward rounding. The existing public paths fail at least one of those obligations, so the nominal positive margin is not yet theorem input.

WI-263 then shows that successful recertification would not by itself finish the one-prime phase step. A full-span antisymmetric two-bump function can have a positive-definite autocorrelation `C_v` with a gap containing `log 2`, so the prime atom is killed exactly. The same geometry places the positive short-lag self-correlation where Suzuki's archimedean density is positive and the negative long-lag cross-correlation where it is negative. Hence the resulting weighted source has

`J_v(r,t)>=0`

for every modulation frequency in the one-prime regime.

This is a decisive generic-geometry barrier. Full support span, compact support, positive-definiteness and knowledge of the exact first-prime source do not force `C_v(log 2)` away from zero or produce a negative phase defect. The missing rigidity must come from **firstness itself**: the lower envelope `F_v(r)>=r lambda_r`, the exact equation `A_(a_*)v=0`, or another consequence coupling the zero mode to its source strongly enough to forbid the screening autocorrelation.

The reusable ledger is now five gates: **source activation, exact-object identity, certificate-judge validity, generic autocorrelation screening, and genuine first-crossing rigidity are distinct**. Passing the first three merely makes the one-prime problem legitimate; it does not supply the fourth-to-fifth bridge.

**Boundary.** WI-263's model is not a null vector of the actual localized Weil operator and need not satisfy the quantitative firstness inequalities. It therefore does not refute the first-crossing program. It narrows the next theorem: any proof using only support and Bochner-type autocorrelation constraints is insufficient even after `lambda_(7/20)>0` is rigorously certified.