# MI-017 — Endpoint screening survives generic first-crossing structure; the discriminator must be source-specific

**Evidence level:** the phase-cone identities through WI-258, the one-prime screening obstruction WI-263, the all-finite-prime screening theorem WI-264, and the abstract first-crossing compression countermodel WI-265 are exact. WI-253 records certified unrestricted positivity at radius `0.8`. No contradiction for the true Suzuki zero mode, no failure of the source-specific lower-envelope inequality, and no RH conclusion is proved.

WI-253--WI-258 reduce useful first-crossing information to aperture lower envelopes and phase-coupled source inequalities. WI-264 then removes source activation as the main issue: every finite aperture has only finitely many active von-Mangoldt lags, and narrow antisymmetric endpoint bumps can place all of them simultaneously inside one open autocorrelation zero interval while the nonzero self/cross pieces lie on favorable signs of the continuous source. Compact/full support, positive-definiteness, and arbitrarily many finite prime layers do not prevent this screening geometry.

WI-265 now shows that **generic first-crossing spectral structure does not prevent it either**. Reuse the same full-span screened endpoint-bump vector `f` and define the positive rank-one-defect operator

`A=I-|f><f|`.

Then `Af=0` exactly, every proper centered compression is strictly positive, every translated interval of any fixed shorter length has a uniform positive Rayleigh reserve, and localized copies of the terminal mode satisfy the corresponding averaged firstness inequality. Thus an exact terminal zero mode plus strict nested and translated-window positivity is fully compatible with the endpoint autocorrelation gap.

The barrier is now precise. Any proof that reduces the true first crossing to generic Hilbert-space facts such as positivity of proper compressions, exact zero-modehood, or an abstract localization reserve has already discarded the arithmetic information needed to defeat screening. The indispensable extra structure is the **Suzuki source identity itself**: in the actual Weil problem the localization defect is identified with the von-Mangoldt/archimedean autocorrelation functional `F_v(r)`, giving the source-specific inequality `F_v(r)>=r lambda_r`, and the exact null equation uses the arithmetic kernel rather than an arbitrary rank-one defect.

The next decisive test is therefore source-facing: evaluate or bound the true `F_v(r)` for the screened family against rigorous lower information on `r lambda_r`, derive from Suzuki's exact null equation an identity incompatible with a long autocorrelation gap, or find another arithmetic Fourier/moment constraint that the rank-one model cannot imitate. Merely strengthening generic first-crossing variational language cannot close the route.

**Boundary.** WI-265 is not a counterexample to Suzuki's localized Weil operator. Its rank-one operator is deliberately flexible and need not satisfy the zeta-specific relation between localization defect and source autocorrelation. The result is a no-go only for deductions based on generic first-crossing operator structure before the arithmetic kernel is used. The true source-specific inequality and null equation remain open discriminators.
