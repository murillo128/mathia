# Weil-inertia research lines

This file holds the current mathematical questions suggested by the durable weil-inertia intuitions. It is not a roadmap, task queue, status page, or history.

## If using Bombieri truncations, control coefficient cost and height tightness rather than raw span separation

**Linked intuitions:** `MI-008-inertia-counts-offline-pairs-but-not-their-distance`, `MI-014-source-coercivity-must-survive-confluence-and-drifting-bows`.

WI-231--WI-237 show that finite negative inertia detects off-line packets exactly but can lose coercivity as height grows. The unconditional simple critical-line exponentials are complete on bounded windows and are not a Bessel family, so ordinary separation/Riesz arguments cannot exclude spectral collapse. The surviving currency is coefficient price together with height tightness.

## Turn the first-crossing PSD multiplier family into non-screenable prime overlap

**Linked intuition:** `MI-017-sliding-first-crossing-localization-produces-autocorrelation-symbols`.

WI-253--WI-267 isolate the phase-coupled source cone, defeat finite-lag screening and generic first-crossing substitutes, and prove from Suzuki's actual null equation that every point outside a nonzero null mode's support is reached by at least one active prime-power translation. WI-268 closes the generic sign shortcut: the unrestricted archimedean cross kernel changes sign before the first prime lag, so ordinary positivity-preserving-semigroup theory is unavailable.

WI-269 now derives a source-specific replacement. At a hypothetical first unrestricted crossing, the ground-state transform of every bounded Lipschitz multiplier is nonnegative. Polarization gives a PSD signed-source bilinear form, and every finite multiplier partition produces a PSD signed Laplacian with constants in its kernel. The nodal decomposition further yields an uncancelled tariff: opposite-sign overlap at active prime lags and at short positive-`w` archimedean lags must be compensated by long-range negative-`w` archimedean overlap.

The live theorem is therefore sharper than generic activity-to-overlap. Combine this **whole multiplier PSD family**, Suzuki support domination and first-crossing constraints to force a nonzero/quantitative source-relevant prime overlap, or construct a source-compatible mode whose long-range archimedean compensation satisfies all these constraints while screening every prime autocorrelation. Either outcome would decide whether the new order structure reaches the arithmetic atoms.

## Treat translated activity, scalar autocorrelation, nodal overlap and PSD multiplier rigidity as separate gates

WI-267 proves support domination, not `C_v(log n)!=0`. WI-268 proves failure of standard positivity preservation, not sign change of the particular mode. WI-269 proves positive-semidefinite multiplier energies and a nodal compensation inequality, not a Markov sign pattern or a prime-lag lower bound.

Any successful next step must use the exact source-specific PSD family rather than revert to generic ground-state positivity or support counting. Long-range archimedean compensation is now the explicit escape that has to be bounded or realized.