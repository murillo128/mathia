# Weil-inertia research lines

This file holds the current mathematical lines of investigation suggested by the durable weil-inertia intuitions. It is not a roadmap, task queue, status page, or history.

## Force supercritical spectral shape beyond screening, activation, and theorem-scale visibility

**Linked intuitions:** `MI-002-global-fenchel-dual-separates-losses`, `MI-009-scalar-universality-collapses-to-fourier-positivity`, `MI-011-local-nonlinearity-is-lost-by-a-universal-scalar-tax`, `MI-012-affine-vector-retention-is-still-scalarization-at-the-witness`, and `MI-013-exceptional-populations-have-finite-spectral-screening-rank`.

WI-172--WI-182 show that scalar pressure, finite additive vector storage, low-rank screening, unit-trace activation, and the natural source-proximal correction all fail to produce a free extensive coercive gain. Multiplicity, distinct exceptional count, spectral activation, and source remainder remain separate resources.

The decisive source theorem must therefore force supercritical participation or another signed invariant at the scale actually consumed by the final Weil form, not merely exhibit an exceptional block that exists algebraically.

## Treat count-saturating bows as a self-dual localized arithmetic problem, not a removable normalization artifact

**Linked intuition:** `MI-014-source-coercivity-must-survive-confluence-and-drifting-bows`.

WI-183--WI-186 establish the first bow controls: source compatibility forces mirror closure and `c>=4 pi-o(1)`, the count-saturating spacing has reciprocal support frequency `alpha=1/2`, the selected principal form is globally invisible below the relevant population scale, and black-box signed portfolios cannot improve the published pointwise error because signal and total-variation error scale together.

WI-187 closes the naive short-height repair. At reciprocal frequency `alpha_*=2 pi/c`, the required prime-side length is `x=T^{alpha_*}` while the bow occupies only `H~T^epsilon/log T`. Standard Montgomery--Vaughan mean-value control needs `epsilon>alpha_*`; at count saturation this is impossible for every fixed `epsilon<1/2`. Moving to larger `c` to buy arithmetic resolution forces the selected bow to become a small minority of the local zero population, leaving a large complementary reservoir.

WI-188 then reopens the actual prime coefficients and identifies the remaining source object precisely. Prime sparsity or even Hardy--Littlewood-scale **untwisted positive** pair counts save at most a logarithm after absolute values and still miss the bow scale polynomially. At `x=sqrt(T)` the height chirp also has linearly many lattice stationary aliases per shift. The surviving escape is therefore cancellation in a self-dual, chirped, growing-shift von-Mangoldt covariance, or a genuinely different source coupling.

A useful theorem must control the signed shifted-prime covariance before absoluteization, use a source-specific smoother that changes the arithmetic length, or exploit the compulsory complementary reservoir through a separate sign/inertia relation. Simply localizing the global form factor, using prime sparsity positively, or invoking rapid phase variation is now classified as insufficient.

## Distinguish source signal, extractability, activation, theorem-scale visibility, local arithmetic resolution, and error correlation

The current program has several logically separate gates. A selected exceptional geometry may carry a coherent signal; the rest of the zeta amplitude may cancel it; unit trace can prevent a nonlinear tail from activating; a perfectly extracted block may be too small under global normalization; a localized theorem may lack enough vertical height to resolve the required Dirichlet length; and a signed postprocessing may fail because its arithmetic error is only controlled after absolute values.

Future candidate inequalities must state which gate they cross and at what scale. In particular, support-one global form-factor control can coexist with sparse mesoscopic off-line configurations whose reciprocal line is both globally invisible and locally a hard shifted-prime problem.

## Keep off-line mass, multiplicity, distinct count, activation, visibility, resolution, and cancellation reservoirs separate

None of these quantities may be substituted for another without a proved bridge. WI-184--WI-188 show especially that making the selected bow arithmetically resolvable can force a large unselected zero reservoir, while positive information about that reservoir does not by itself supply the signed covariance needed to close the bow.