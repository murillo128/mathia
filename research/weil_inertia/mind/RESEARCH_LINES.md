# Weil-inertia research lines

This file holds the current mathematical lines of investigation suggested by the durable weil-inertia intuitions. It is not a roadmap, task queue, status page, or history.

## Prove collective source-fixed covariance after closing isolated-residue window optimization

**Linked intuition:** `MI-014-source-coercivity-must-survive-confluence-and-drifting-bows`.

WI-183--WI-197 reduce the count-saturating bow to an exact source-fixed von-Mangoldt covariance problem. Triangular height localization identifies the coupled multiplicative short-interval `L^2` norm, while long twist averaging and available black-box one-variable Type-II input fail at the required diagonal scale. The hard quantity is distinguished cancellation, not typical variance.

WI-198--WI-205 then prune several alternatives. Audited every-interval exponent-pair input removes fixed-power bows above `3943/12011`, the ordinary exponent-pair architecture has an exact floor `1/4`, almost-all interval results do not cover the source-selected bow at their present exception budget, and a bounded packet of near-line explicit-formula residues is polynomially subdiagonal in the normalized-depth Gallagher norm.

WI-206 closes the single-window optimization loophole. For any bounded-variation profile on the same physical span, the isolated residue is an exact Hilbert-space linear functional whose norm is controlled by the same profile `L^2` energy appearing in the prime-square diagonal. The box window is asymptotically the matched filter for one source-matched near-line zero, so smoothing, tapering, sign changes, or oracle profile tuning cannot recover the missing power.

WI-207 extends this to arbitrary positive multiwindow lifting. Any collection of linear windows combined by a positive-semidefinite quadratic channel form factors through one Hilbert-valued profile. The residue-to-diagonal visibility bound is dimension-free and remains `M_F^2 T^{-vartheta+o(1)}` in normalized depth, with no gain from channel count or coherent PSD mixing.

The current frontier is therefore more decisively **collective and sign-sensitive**. Progress below the reservoir boundary requires the full source-fixed `Lambda-Lambda^sharp` covariance, an every-interval theorem outside the quarter-floor architecture, a source relation forcing a mesoscopic coherent exceptional population, or an observable that leaves the positive same-translation quadratic window class. More windows or smoother positive kernels do not amplify an isolated exception.

## Keep screening, activation, visibility, resolution, alias geometry, norm strength, residue amplitude, and coupling sign separate

A theorem may match interval length and twist range and still be unusable because its norm is one power of the short interval too large. A long-average or almost-all theorem may certify typical behavior while leaving the distinguished center exceptional. A pole can be perfectly phase-aligned yet remain polynomially below the quadratic diagonal, and adding many positive window channels does not change that scale.

The audited fixed-physical-depth reservoir frontier is `3943/12011`; the ordinary exponent-pair floor is `1/4`; the normalized-depth screening frontier remains `27/82`; and bounded near-line packets remain subdiagonal by `T^{-vartheta+o(1)}` under arbitrary single-window shaping and PSD multiwindow lifts. These are different currencies and should not be substituted for one another.

## Use the Gallagher bridge before further transforms

The exact multiplicative triangular identity remains the most source-faithful representation of the surviving short-bow problem. Further dispersion, bilinear, large-sieve, spectral, differencing, or multiwindow methods are relevant only if they act on the coupled norm and gain beyond the diagonal scale. A positive Hilbert lift that merely repackages the same local linear source cannot supply that gain by itself.

## Keep off-line mass, multiplicity, distinct count, critical-line reservoir, and arithmetic coefficient structure separate

No improvement in one reservoir substitutes for another without a proved bridge. Above the audited cutoff, zero reservoirs remove count-saturating models; below the structural quarter barrier the unresolved information is the shifted von-Mangoldt coefficient and its joint source-fixed variance unless a genuinely different source theorem is found. WI-205--WI-207 make the population issue explicit: a bounded hypothetical zero packet is not automatically large enough in any positive linear-window realization, so a proof using off-line information must show how the source configuration collectively amplifies or coherently couples it.
