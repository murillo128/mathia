# Weil-inertia research lines

This file holds the current mathematical questions suggested by the durable weil-inertia intuitions. It is not a roadmap, task queue, status page, or history.

## If using Bombieri truncations, control coefficient cost and height tightness rather than raw span separation

**Linked intuitions:** `MI-008-inertia-counts-offline-pairs-but-not-their-distance`, `MI-014-source-coercivity-must-survive-confluence-and-drifting-bows`.

WI-231--WI-237 show that finite negative inertia detects off-line packets exactly but can lose coercivity as height grows. The unconditional simple critical-line exponentials are complete on bounded windows and are not a Bessel family, so ordinary separation/Riesz arguments cannot exclude spectral collapse. The surviving currency is coefficient price together with height tightness.

## Use genuine first-crossing rigidity to defeat finite-aperture autocorrelation screening

**Linked intuition:** `MI-017-sliding-first-crossing-localization-produces-autocorrelation-symbols`.

WI-253--WI-258 isolate the phase-coupled source cone and the exact lower-envelope information available at a hypothetical first localized Weil crossing. WI-259--WI-262 audited a separate `r=7/20` certificate path, but WI-264 corrects its role: the already-persisted certified theorem `lambda_0.8>0` for arbitrary complex tests implies directly that any first unrestricted crossing satisfies `a_*>0.8`. Recertifying `FP-0.35` is therefore not a prerequisite for activating prime-power source terms.

The stronger obstruction is now source screening at **every finite aperture**. WI-264 extends the two-endpoint-bump control of WI-263: because only finitely many von-Mangoldt lags satisfy `log n<2a`, their largest lag has a positive gap to `2a`. Narrow antisymmetric endpoint bumps can place all active prime-power lags simultaneously inside one open zero interval of the autocorrelation, while placing its nonzero self/cross pieces on the favorable sign regions of Suzuki's continuous source. The resulting scalar phase defects and aperture transforms are nonnegative for every modulation frequency.

Adding more finite prime layers, knowing their signs, imposing compact/full support, or using positive-definiteness therefore cannot force the desired negative defect. The live theorem must consume information the screening model does not satisfy: the full family `F_v(r)>=r lambda_r`, the exact null equation `A_(a_*)v=0`, or an equivalent first-crossing rigidity that forbids a macroscopic autocorrelation gap or forces quantitative mass at source lags.

## Treat source activation, finite-source screening and true zero-mode rigidity as separate gates

The public FP-0.35 audits remain valid artifact/certificate findings, but they are no longer on the live dependency chain for prime activation. Conversely, WI-264 is not a first-crossing counterexample: its endpoint-bump model need not satisfy the null equation or the quantitative lower envelope. The unresolved bridge is precisely between those genuine firstness constraints and the screened autocorrelation geometry.