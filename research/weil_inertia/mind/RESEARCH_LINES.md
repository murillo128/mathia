# Weil-inertia research lines

This file holds the current mathematical questions suggested by the durable Weil-inertia intuitions. It is not a roadmap, task queue, status page, or history.

## Leave the scalar additive-cube hierarchy and retain source-specific signed covariance

**Linked intuitions:** `MI-036-exact-zero-density-locks-bow-reciprocal-scale-at-riemann-siegel-length` through `MI-045-unit-time-bandwidth-compresses-one-window-height-diversity`.

WI-331--WI-338 classify ordinary local Gowers smallness at the bow scale. Below the deterministic diagonal wall, matched-energy Rademacher sources can be small on every relevant interval while retaining full sliding variance; at the wall, the source's own energy prevents ordinary `U^s=o(1)`. There is therefore no remaining ordinary-Gowers order window in which generic smallness can force the distinguished WI-195 covariance saving.

WI-339 shows that deleting the literal zero-increment diagonal does not rescue the interface. The diagonal-deleted statistic is the centered Rademacher cube chaos, with `L^2` scale `K^(-(s+1)/2+o(1))`; all-start countermodels survive until the much higher transition `R_s=s+1`, apart from a bounded additive strip.

WI-340--WI-342 close the obvious scalar repairs. Sparse deterministic geometric deletion leaves the same renormalized chaos scale; deterministic scalar reweighting remains inside the Walsh character quotient; distinct injective cube orders are orthogonal in Rademacher `L^2`; and the canonical multiplicative-derivative coupling of parallel faces is just the next ordinary Gowers order. Scalar weighting, deletion and linear order mixing therefore remain inside the matched-energy quotient.

WI-343 identifies an independent carrier-dimension obstruction on the natural Gallagher source window. Its bow-height span `H` and log-aperture `delta` satisfy `H delta=1`, so a power-growing number of heights lies near a fixed-dimensional linear carrier space. WI-344 upgrades this from one window presentation to any finite source set contained in a bounded log-aperture and any bounded height-independent linear preprocessing. For degree `d`, every height carrier lies within relative error

`epsilon_d=(H Delta/4)^(d+1)/(d+1)!`

of a common `(d+1)`-dimensional span after centering the aperture. If normalized outputs retain transmission factor `eta`, their Gram tail beyond rank `d+1` is at most `m(epsilon_d/eta)^2`. Extensive normalized height diversity can therefore be manufactured only by letting the transmitted scale collapse comparably fast. On the centered natural window `H Delta=1`, `epsilon_6=1/82575360`.

The live theorem must therefore change the information carrier, not tune scalar cube geometry, count more heights, or linearly remix a fixed-aperture height bank. Viable routes include an aperture whose total `H Delta` genuinely grows, cross-scale/cross-window covariance, a height- or arithmetic-dependent coefficient bank outside the fixed linear map covered by WI-344, a genuinely nonlinear post-quotient observable, or a direct signed two-point covariance identity for `Lambda-Lambda^sharp`. The accepted clue `CLUE-bow-distinguished-twist-offdiagonal-cancellation` remains the destination test: prove the needed negative off-diagonal contribution with `Lambda^sharp` retained in the same norm, or prove that the proposed statistic cannot force it.

## Keep diagonal removal, scalar quotienting, carrier dimension, transmission scale and covariance sign separate

Diagonal-free does not mean destination-sensitive. WI-339 removes the universal zero-increment floor but leaves generic chaos; WI-340 shows that broader sparse geometric deletion does not change that floor; WI-341 identifies the exact Walsh quotient seen by deterministic scalar weights; WI-342 shows that different ordinary orders do not cancel each other linearly; and WI-343--WI-344 show that height multiplicity inside bounded physical log-aperture has small approximate linear dimension even after arbitrary bounded height-independent linear preprocessing.

A proposed bridge should therefore state the source-start quantifier, exact monomial/character or carrier family, normalization, whether coefficients depend on the arithmetic source or height, the physical logarithmic aperture and resulting time--bandwidth product, the transmitted amplitude after preprocessing, which nonlinear operations create genuinely new support, and the exact off-diagonal covariance sign it forces. A lower generic scalar chaos norm, a larger count of coherent height samples, or an ill-conditioned linear map that reveals tiny residual directions is not enough unless the destination receives those directions at a quantitatively useful scale.
