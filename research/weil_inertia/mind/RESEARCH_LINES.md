# Weil-inertia research lines

This file holds the current mathematical lines of investigation suggested by the durable weil-inertia intuitions. It is not a roadmap, task queue, status page, or history.

## Convert localized weighted source mass into an actual zero-budget contradiction

**Linked intuition:** `MI-014-source-coercivity-must-survive-confluence-and-drifting-bows`.

WI-218--WI-223 price free screening by rank and weighted source mass. WI-224--WI-225 close the scalar compact-detector localization order: adaptive fixed-support detectors achieve dimensionless cutoff `O(log T)`, while quantitative Logvinenko--Sereda forces `Omega(log T)`. The corresponding physical scale is `Theta(M)` and cannot be improved to the bow scale merely by optimizing the same scalar window architecture.

WI-226 tests the newest generic short-interval zero-statistics input. Wang's pair formula extends to bow-length `T^{theta+o(1)}` intervals, but its simple-critical and distinct-zero proportion bounds become positive only above exponents strictly larger than the entire surviving count-saturating bow range. Generic short-interval proportions therefore do not supply the missing reservoir contradiction.

The live theorem must turn the already localized **radial-weighted source** into incompatible local zero count, multiplicity, coefficient/radial depth, or retain source-fixed sign/phase/covariance structure discarded by scalar total-mass domination. Wang's local pair formula may be useful only through such a source-aware coupled observable.

## Price rank, weighted mass, physical reach, and zero count separately

Sharp scalar localization is not a count theorem, and a short-interval pair formula is not automatically a source-coercive statistic. Further scalar window optimization or standalone generic proportion estimates are controls unless they change the source-to-zero-budget conversion.
