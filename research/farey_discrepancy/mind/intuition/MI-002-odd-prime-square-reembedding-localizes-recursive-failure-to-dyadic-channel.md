# MI-002 — Odd repeated-prime channels descend; the dyadic obstruction is one source-head atom plus a composition gate

**Evidence level:** exact inside the false-RH frontier construction through [FD-093](../../findings/FD-093-dyadic-terminal-obstruction-collapses-to-the-single-row-two-head-atom.md). The lower masks retain the full frontier exponent under the stated hypotheses, but no fixed lower-state occupation fraction, indefinitely iterable descent, or RH conclusion is proved.

After the first canonical prime deflation, every selected row in the `p`-channel is still divisible by `p`. For odd `p`, stripping that second copy and reembedding `s=pt` as `4t` preserves the quotient-shell source argument exactly and moves the packet to a genuinely lower horizon of order `4H/p^2`. The Jordan weight changes by a uniformly bounded factor, every destination row is divisible by `4`, and the normalized predecessor family carries summable `p^(-4 sigma)` weight. Thus odd repeated-prime channels have an exact recursively eligible representation at lower scale; their remaining problem is quantitative composition, not representability.

The dyadic channel is subtler but much narrower than a generic scale-critical branch. The withdrawn FD-092 missed the parent row `r=4`: after two dyadic deflations it lands at `m=1`, outside the physical `D=1` tail. FD-093 gives the corrected exact decomposition

`M_(2,H)^term = (3/4)|H(B)|^2 + (3/4) Q_(B,H)^+`, with `B=H/4+O(1)`,

where `Q_(B,H)^+` is supported on the complete odd-squarefree prefix with `m>=3`. Every coordinate in that nonhead prefix has an odd prime divisor. Stripping its smallest odd prime and reembedding four-adically converts it exactly into nonsquarefree physical energy at a horizon at most `(1/3+o(1))H`, again with summable prime-normalized entropy.

Therefore the universal dyadic failure has collapsed to **one coordinate**: the lower row `s=2`, equivalently the source head `|H(B)|^2`. It cannot be discarded by cardinality because the source value itself can carry the full zeta-zero growth exponent, and preserving that source argument while forcing a nonsquarefree row requires the smallest multiplier `4`, which returns the horizon to `H+O(1)` and destroys multiplicative descent.

The false-RH frontier now has three possibilities in the dyadic branch: a fixed share is already nonsquarefree at half scale; a fixed share concentrates on the single source-head atom; or a fixed share passes through exact lower nonsquarefree packets. The last alternative still inherits the old composition gap: a frontier-sized mask need not occupy a fixed fraction of the complete lower state.

**Research consequence.** The remaining recurrence problem is no longer “repair the dyadic squarefree terminal sector.” All nonhead terminal coordinates are exactly repairable. A successful theorem must either rule out persistent frontier concentration on the single source-head atom using additional source coherence, or convert the repaired frontier-normalized masks into fixed-strength recursively composable lower states. Those are distinct currencies and neither follows from full exponent alone.

**Boundary.** The entire statement is conditional on the false-RH near-frontier packet already constructed in the line. FD-093 corrects FD-092 rather than validating its whole-mask quarter-scale identity; the deleted finding must not be used as current evidence.