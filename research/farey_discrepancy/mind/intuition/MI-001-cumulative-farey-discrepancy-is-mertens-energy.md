# MI-001 — Farey transfer is governed by a source-coherence threshold after the normalization ledger is paid

**Evidence level:** supported by exact source/occupation reductions through FD-066 plus the cited all-short-interval Möbius theorem; no positive global occupation theorem is claimed.

The cumulative Farey obstruction is controlled by the physical shell source

\[
c(n)=\Delta\mathcal H(n)=\sum_{dm=n}\frac{\mu(m)}d.
\]

FD-054--FD-058 separate deterministic transfer, genuine short-interval cancellation, sparse represented-start sampling, and maximal ambient-window localization. FD-059 identifies the deterministic critical host scale `Q_X=X/A_X`, where `A_X=|\mathcal H(X)|`: favorable squarefree hosts at `q\asymp Q_X` force a nearby nonsquarefree row that retains a fixed fraction of the spike. FD-060 shows that subpower slack can be spent on packet multiplicity.

FD-062--FD-063 show that physical short-interval cancellation changes the geometry and removes combinatorial host scarcity. FD-064 then classifies the larger power-gap family. On a zero-frontier spike `A_X=X^{\Theta+o(1)}`, hosts `q\asymp X^\lambda` can support polynomial packets, but after paying the later horizon the optimized loss retains the exact `\Theta=2/3` crossover. FD-065 shows that moving the Schur head cannot repair the ledger: the denominator saving and scalar-leverage loss carry the same head penalty.

FD-066 exposes the more invariant quantity hidden in those coordinates. Assume the source spike stays comparable on a backward window

\[
L_X=X^{\ell+o(1)}.
\]

Reciprocal-floor geometry turns that window into

\[
K_X=X^{\lambda+\ell-1+o(1)}
\]

near a host `q=X^{\lambda+o(1)}`. After the generic zero-frontier denominator and Schur scalarization are included, the direct coherent packet and deterministic terminal obstruction differ by the exact power

\[
\boxed{\gamma_{\rm coh}-\gamma_{\rm term}
=\frac{2-2\Theta-\ell}{1+\lambda}.}
\]

Therefore the direct packet improves the terminal scalar obstruction **iff**

\[
\boxed{\ell>2(1-\Theta).}
\]

Host exponent and Schur-head exponent cannot change the sign. Their apparent freedoms are bookkeeping transformations inside a fixed source-coherence budget.

The present all-short-interval input supplies coherence for every fixed `\ell<\Theta` in its admissible range. Hence a direct improvement is possible from that input only when `\Theta>2/3`. Below two thirds the missing source theorem is qualitatively stronger than “shorter intervals”: one needs **super-amplitude coherence** `\ell>2(1-\Theta)>\Theta`, meaning variation `o(A_X)` across a window longer in power scale than the spike amplitude itself, or a quantifier mechanism that finds such a window around the adaptive spike.

The reusable separation is therefore sharper: **source amplitude, source-coherence length, local packet capacity, host entropy, head truncation, Schur leverage, and normalized horizon are different currencies**. Multiplicity is the reciprocal-floor image of coherence length, not an independent gain. Shortening the interval threshold, enlarging the packet, moving the host, or moving the head can all improve intermediate objects without crossing the final exponent.

A genuine escape must change a load-bearing currency: prove source-relative coherence beyond `2(1-Theta)`, exploit source/denominator correlation at the same horizon, obtain an adaptive localization theorem with the required amplitude-relative error, or use another interaction that avoids the direct reciprocal-floor/horizon ledger.

**Boundary.** FD-066 is a conditional classification of direct coherent packets inside the current zero-frontier denominator and Schur scalarization. It does not assert that the physical source has windows with `\ell>2(1-\Theta)`, does not rule out source-coupled denominator estimates or aggregate host interactions, and gives no new zero-free region or RH proof.