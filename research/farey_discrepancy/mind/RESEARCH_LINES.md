# Farey-discrepancy research lines

This file holds the current mathematical questions suggested by the durable Farey-discrepancy intuitions. It is not a roadmap, task queue, status page, or history.

## Break the flat physical prime-incidence algebra, or make source descent pay the selection tax

**Linked intuitions:** `MI-001-cumulative-farey-discrepancy-is-mertens-energy`, `MI-002-odd-prime-square-reembedding-localizes-recursive-failure-to-dyadic-channel`, `MI-003-normalized-record-defect-is-the-composition-loss`, `MI-004-multiplicative-horizon-descent-can-preserve-the-source-quotient`, `MI-005-source-neutral-carrier-descent-spends-a-strict-finite-row-potential`, `MI-006-physical-coefficient-law-is-the-terminal-endpoint-resource`, `MI-007-prime-incidence-tensorizes-and-spends-product-depth`, `MI-008-linear-least-prime-order-is-positive-rough-sieve-filtration`.

FD-102--FD-106 isolate the critical endpoint. Deterministic floor geometry cannot shorten the source-neutral logarithmic window; direct compression transports a fixed-power normalized mass; the adaptive cutoff keeps selected source coordinates large; and every predecessor family except the repaired terminal prime fan is critical-lossless. The only scale-dependent transport loss is the harmonic family `C_p/H~1/p`.

FD-107 closes generic shell coherence: a single fixed nonnegative one-Lipschitz sublinear source can realize a polynomial high-mass state dominated by the exact terminal `4p` rows while every complete repaired child has vanishing relative critical excess. FD-108 then restores the physical coefficient law `Delta H(n)=mu(rad n)phi(rad n)/n`; its exact one-prime covariance and weighted large-sieve control are genuine arithmetic information, but still fit inside the terminal power budget.

FD-109 closes raw higher-order simultaneous prime incidence. The physical prime-direction coefficient cube is pointwise rank one, all raw two-prime Pluecker minors vanish, and simultaneous divisibility sums are commuting products of one-prime dilation resolvents. Their new source sample sits at product depth `X/q`, with source-rich constraint `q lesssim P_*(X)`, not in a Cartesian box of independently source-rich prime directions.

FD-110 closes the simplest ordered escape as well. Finite prime-prefix exclusions and least-prime-factor packets remain inside the same commuting resolvent algebra; after inserting the physical coefficient law, inclusion-exclusion becomes a positive rough-sieve filtration. Every source term in `L_p(X)` occurs at a denominator `r>=p`, so order does not extend the one-prime source-rich cutoff. More generally, every finite linear statistic of finitely many prime-divisibility bits is still a polynomial in the same commuting incidence operators.

The direct endpoint route must therefore change the operation rather than merely add or reorder linear divisibility labels. Live possibilities include nonlinear products before summation, cross-horizon persistence/global source descent, signed structure not reducible to finite Boolean divisibility statistics, or refinement vectors with nonzero exterior defect. The alternative remains global amortization: combine the epsilon-transport law with actual source descents/head events often enough that the fixed packet-selection factor cannot survive a full false-RH chain.

## Locate the exact boundary of the high-mass mechanism

The adaptive source threshold is intrinsic: `L_H` tends to infinity exactly when `U_H/H` does. At the linear floor `U_H=H^(1+o(1))`, neither the source-rich core nor the current error normalization gives a growing source tag. A continuation that reaches this boundary must either prove the false-RH chain stays a fixed power above the floor long enough, or introduce a different observable whose compression error is still negligible at near-linear mass.

FD-107 shows that generic coherence already fails at polynomial high mass; FD-108 shows first-order physical covariance is still critical; FD-109 shows fixed-order simultaneous prime incidence only moves the source to product depth; FD-110 shows finite linear prime-order bookkeeping is the same positive commuting rough-sieve filtration. The obstruction is therefore not an artifact of marginal mass, shallow source access, absence of first-order arithmetic covariance, failure to try raw higher-order divisibility, or failure to impose finite least-prime order.

## Keep deterministic budget, transport mass, fan entropy and physical source law distinct

Source records provide recurrent entry states; FD-102 supplies an index-free deterministic charge; FD-104 transports normalized mass; FD-105 transports an adaptive source tag; FD-106 localizes the unresolved entropy to `C_p~H/p`; FD-107 proves generic coherent shell geometry can saturate that endpoint; FD-108 proves first-order physical large-sieve control still fits inside the terminal power budget; FD-109 proves the raw multi-prime incidence hierarchy is a flat commuting tensor product with a product-scale source budget; and FD-110 proves finite linear prime-order refinements remain a positive commuting rough-sieve transform of the same scalar source. Future arguments must identify the **nonlinear, cross-horizon, signed or non-Boolean physical information** they use rather than relabeling more linear divisibility depth or order as higher-order coherence.
