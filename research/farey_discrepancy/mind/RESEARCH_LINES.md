# Farey-discrepancy research lines

This file holds the current mathematical questions suggested by the durable Farey-discrepancy intuitions. It is not a roadmap, task queue, status page, or history.

## Leave the fixed local/refinement algebra with a genuinely moving source coordinate

**Linked intuitions:** `MI-001-cumulative-farey-discrepancy-is-mertens-energy`, `MI-002-odd-prime-square-reembedding-localizes-recursive-failure-to-dyadic-channel`, `MI-003-normalized-record-defect-is-the-composition-loss`, `MI-004-multiplicative-horizon-descent-can-preserve-the-source-quotient`, `MI-005-source-neutral-carrier-descent-spends-a-strict-finite-row-potential`, `MI-006-physical-coefficient-law-is-the-terminal-endpoint-resource`, `MI-007-prime-incidence-tensorizes-and-spends-product-depth`, `MI-008-linear-least-prime-order-is-positive-rough-sieve-filtration`, `MI-009-finite-local-polynomial-nonlinearity-splits-by-parity`, `MI-010-fixed-rational-anchor-horizon-is-a-periodic-mobius-filter`.

FD-102--FD-111 close the fixed local prime algebra: the only scale-dependent transport loss is the terminal harmonic fan, generic shell coherence can saturate it, physical one-prime covariance stays inside the same power budget, higher prime incidence tensorizes through commuting resolvents, finite least-prime ordering is a positive rough-sieve filtration, and fixed finite local polynomial nonlinearities reduce to parity-separated power sources.

FD-112--FD-115 then close the canonical local refinement escape. Two-child shape has one residual coordinate; deeper fixed descendant templates and the denominator-truncated local refinement movie remain functions of the parent denominator ratio (plus parent scale at absolute order); centered descendant discrepancy inside one cone is determined by that movie. The only discrepancy datum left by FD-115 was the absolute common anchor `D_Q(A)`.

FD-116 classifies the simplest cross-horizon use of that anchor. For a fixed reduced rational `A=a/b`,

`D_Q(A)-D_(Q-1)(A) = sum_(m|Q) mu(Q/m) g_A(m)`,

where `g_A` is a zero-mean `b`-periodic table, and cumulatively

`D_Q(A)=A+sum_(m<=Q) f_A(m) M(floor(Q/m))`.

A fixed finite family of rational anchors is therefore only a finite-dimensional periodic kernel passed through the same Möbius/Mertens source. Exteriorizing those paths can increase ambient dimension but cannot create an independent Farey source channel.

The live representation frontier is consequently narrower: any anchor-based escape must make the kernel itself grow or move with the horizon (for example `A=A_Q`, growing denominator, or a number of anchors growing with `Q`), use a genuinely global cross-cone selection law, or retain another source-conditioned coordinate not reconstructible from fixed periodic Möbius filters. The decisive test is information independence, not ambient dimension.

## Locate the exact boundary of the high-mass mechanism

The adaptive source threshold is intrinsic: the source-rich tag grows exactly while `U_H/H` does. Near the linear floor, neither the current source core nor its error normalization supplies a growing tag. A continuation must either keep a false-RH chain a fixed power above that floor long enough, or introduce an observable whose compression error remains negligible at near-linear mass.

FD-107--FD-116 now rule out an increasingly broad family of substitutes for that missing information: generic coherence, first-order arithmetic covariance, fixed-order prime incidence, finite prime ordering, fixed local polynomial nonlinearity, finite local refinement geometry, canonical moving ancestry, centered discrepancy, and fixed finite rational absolute anchors.

## Keep deterministic budget, transport mass and source identity separate

A fixed-strength statement after lower-scale normalization is not automatically a recursively eligible source recurrence. Likewise, a nonzero exterior minor or a larger state vector is not a new coordinate when it is reconstructed from one scalar refinement parameter or one periodic Möbius kernel. Future arguments must identify exactly which source variable changes with the horizon and why it cannot be reconstructed from the already-classified local/refinement/Mertens channels.
