# Farey-discrepancy research lines

This file holds the current mathematical questions suggested by the durable Farey-discrepancy intuitions. It is not a roadmap, task queue, status page, or history.

## Break the fixed local physical prime algebra, or make source descent pay the selection tax

**Linked intuitions:** `MI-001-cumulative-farey-discrepancy-is-mertens-energy`, `MI-002-odd-prime-square-reembedding-localizes-recursive-failure-to-dyadic-channel`, `MI-003-normalized-record-defect-is-the-composition-loss`, `MI-004-multiplicative-horizon-descent-can-preserve-the-source-quotient`, `MI-005-source-neutral-carrier-descent-spends-a-strict-finite-row-potential`, `MI-006-physical-coefficient-law-is-the-terminal-endpoint-resource`, `MI-007-prime-incidence-tensorizes-and-spends-product-depth`, `MI-008-linear-least-prime-order-is-positive-rough-sieve-filtration`, `MI-009-finite-local-polynomial-nonlinearity-splits-by-parity`.

FD-102--FD-106 isolate the critical endpoint. Deterministic floor geometry cannot shorten the source-neutral logarithmic window; direct compression transports a fixed-power normalized mass; the adaptive cutoff keeps selected source coordinates large; and every predecessor family except the repaired terminal prime fan is critical-lossless. The only scale-dependent transport loss is the harmonic family `C_p/H~1/p`.

FD-107 closes generic shell coherence: a single fixed nonnegative one-Lipschitz sublinear source can realize a polynomial high-mass state dominated by the exact terminal `4p` rows while every complete repaired child has vanishing relative critical excess. FD-108 then restores the physical coefficient law `Delta H(n)=mu(rad n)phi(rad n)/n`; its exact one-prime covariance and weighted large-sieve control are genuine arithmetic information, but still fit inside the terminal power budget.

FD-109 closes raw higher-order simultaneous prime incidence. The physical prime-direction coefficient cube is pointwise rank one, all raw two-prime Pluecker minors vanish, and simultaneous divisibility sums are commuting products of one-prime dilation resolvents. Their new source sample sits at product depth `X/q`, with source-rich constraint `q lesssim P_*(X)`, not in a Cartesian box of independently source-rich prime directions.

FD-110 closes the simplest ordered escape as well. Finite prime-prefix exclusions and least-prime-factor packets remain inside the same commuting resolvent algebra; after inserting the physical coefficient law, inclusion-exclusion becomes a positive rough-sieve filtration. Every source term in `L_p(X)` occurs at a denominator `r>=p`, so order does not extend the one-prime source-rich cutoff. More generally, every finite linear statistic of finitely many prime-divisibility bits is still a polynomial in the same commuting incidence operators.

FD-111 now closes the first fixed-local nonlinear escape. Every fixed polynomial in finitely many multiplicative shifts of the physical coefficient reduces to commuting divisibility transforms of power sources `H_k`. Odd `k` sources are exactly invertible floor-convolution re-encodings of `H` and therefore have the same power frontier; even `k` sources are a deterministic linear mean plus `O(X^(1/2+epsilon))`. Fixed finite local polynomial multiplication before summation therefore creates no independent false-RH power-scale channel.

FD-112 adds a representation-level restriction on the refinement escape. For one Farey mediant insertion, the two child gap lengths normalized by their parent gap are `(d/(b+d), b/(b+d))`; after removing the conserved total they lie in a one-dimensional zero-sum space. Every centered two-child Pluecker minor therefore vanishes, while every raw normalized minor is only a first difference of the scalar split imbalance `(d-b)/(d+b)`. A viable refinement exterior carrier must retain at least two genuinely independent residual coordinates after nuisance control; left/right child gaps alone cannot supply them.

FD-113 extends that restriction to deeper fixed local refinement. For any fixed or predeclared finite Stern--Brocot descendant template inside a parent edge, every affine-normalized descendant position and gap is a rational function of `r=b/d`, hence of the same root split imbalance `(d-b)/(d+b)`. Deeper gap vectors may have nonzero centered Pluecker minors because this one-scalar image is curved, but the minors remain deterministic kernels of the ordered split-scalar sequence rather than evidence of a second local residual coordinate. Increasing fixed descendant depth or ambient dimension therefore does not by itself escape the representation boundary.

FD-114 removes the canonical state-dependent ancestry escape. The entire denominator-truncated local refinement movie at relative horizon—visible primitive rays, birth times, order, normalized positions and gaps—is a deterministic function of the same denominator ratio `r=b/d`; at fixed absolute order `Q`, the only extra local dependence is parent scale `G=1/(bd)`. Ordinary horizon-dependent Stern--Brocot ancestry therefore does not manufacture another residual coordinate.

FD-115 then removes the centered-discrepancy version of the next obvious escape. On a fixed unimodular parent cone at order `Q`, every visible descendant satisfies `D_Q(x)=D_Q(A)+Lambda_Q(x)-C_Q G y(x)`. The second and third terms are already determined by the canonical local movie, so the entire descendant discrepancy profile modulo a common translation is fixed locally. Pairwise, parent-centered, mean-centered, or other zero-sum discrepancy residuals cannot supply the missing coordinate. The only discrepancy channel left by this one-cone classification is the absolute anchor `D_Q(A)` and its cross-cone or cross-horizon structure.

The direct endpoint route must now leave the **fixed finite local algebra and the centered one-cone refinement algebra** rather than merely make either one nonlinear. Live possibilities are nonlinear functionals after cumulative summation, cross-horizon persistence/global source descent, degree or shift support growing with the horizon, signed structure not reducible to fixed local polynomial/divisibility statistics, or a refinement carrier that explicitly retains absolute discrepancy anchors or another genuinely nonlocal/source-conditioned coordinate. The alternative remains global amortization: combine the epsilon-transport law with actual source descents/head events often enough that the fixed packet-selection factor cannot survive a full false-RH chain.

## Locate the exact boundary of the high-mass mechanism

The adaptive source threshold is intrinsic: `L_H` tends to infinity exactly when `U_H/H` does. At the linear floor `U_H=H^(1+o(1))`, neither the source-rich core nor the current error normalization gives a growing source tag. A continuation that reaches this boundary must either prove the false-RH chain stays a fixed power above the floor long enough, or introduce a different observable whose compression error is still negligible at near-linear mass.

FD-107 shows that generic coherence already fails at polynomial high mass; FD-108 shows first-order physical covariance is still critical; FD-109 shows fixed-order simultaneous prime incidence only moves the source to product depth; FD-110 shows finite linear prime-order bookkeeping is the same positive commuting rough-sieve filtration; and FD-111 shows fixed-degree local polynomial nonlinearities split into invertible odd re-encodings and smooth even backgrounds. FD-112 additionally rules out the bare two-child mediant split as a genuinely two-dimensional controlled exterior carrier, FD-113 rules out deeper fixed/predeclared Euclidean descendant fans, FD-114 rules out the canonical denominator-cutoff ancestry movie as an independent state-dependent coordinate, and FD-115 rules out centered global discrepancy on one parent cone as the missing residual. The obstruction is therefore not an artifact of marginal mass, shallow source access, absence of first-order arithmetic covariance, failure to try raw higher-order divisibility, failure to impose finite least-prime order, failure to multiply finitely many local source coordinates before summation, failure to enlarge the local descendant gap vector, or failure to attach relative global rank/discrepancy to that local packet.

## Keep deterministic budget, transport mass, fan entropy and physical source law distinct

Source records provide recurrent entry states; FD-102 supplies an index-free deterministic charge; FD-104 transports normalized mass; FD-105 transports an adaptive source tag; FD-106 localizes the unresolved entropy to `C_p~H/p`; FD-107 proves generic coherent shell geometry can saturate that endpoint; FD-108 proves first-order physical large-sieve control still fits inside the terminal power budget; FD-109 proves the raw multi-prime incidence hierarchy is a flat commuting tensor product with a product-scale source budget; FD-110 proves finite linear prime-order refinements remain a positive commuting rough-sieve transform of the same scalar source; FD-111 proves fixed local polynomial nonlinearities do not escape through parity-separated power sources; FD-112 proves the minimal two-child Farey refinement has only one residual shape degree after conservation control; FD-113 proves that any fixed/predeclared deeper Stern--Brocot descendant metric is still a one-scalar Möbius lift even when nonlinear embedding creates nonzero exterior minors; FD-114 proves that the canonical moving refinement template itself is still generated by parent ratio and scale; and FD-115 proves that global descendant discrepancy becomes no richer after common-mode removal. Future arguments must identify the **cross-horizon, cross-cone, translation-sensitive, signed or nonlocal physical information** they use rather than relabeling more fixed local algebra, one-scalar refinement shape, canonical moving ancestry, or centered discrepancy as higher-order coherence.
