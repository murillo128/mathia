# Farey-discrepancy research lines

This file holds the current mathematical questions suggested by the durable Farey-discrepancy intuitions. It is not a roadmap, task queue, status page, or history.

## Quantify active-scale Möbius breadth without importing the Mertens target

**Linked intuitions:** `MI-001-cumulative-farey-discrepancy-is-mertens-energy` through `MI-016-harmonic-scale-balancing-still-misses-the-moving-rank-interface`.

FD-117--FD-120 reduce same-horizon Farey information to the reversible floor-quotient skeleton. FD-121--FD-124 price common-source horizon coherence, while FD-125--FD-128 construct sparse multiplicative escapes. FD-129 marks the opposite circularity boundary: complete growing prime-prefix ancestry plus a stage-uniform critical envelope reconstructs the physical Möbius prefix and imports the Mertens target.

FD-130--FD-134 progressively close counting-measure mean-ancestry repairs. Global mean ancestry can hide a fixed-rank cut; rankwise means can dilute a finite-prime coordinate cut; convergence for every fixed prime misses a moving central-rank boundary; and the same boundary survives broad explicit `H^(1-o(1))` active-prime families while the displaced strip remains small relative to one squarefree Erdős--Kac standard deviation.

FD-135 closes the hard-local repair in the opposite direction. In the squarefree unit-sign class, local ancestry defects are only `0` or `2`, so any bound below `2` is exact ancestry. With a rank-`K` Möbius anchor, exact ancestry through primes `p<=Y` decomposes the source into `Y`-rough-core components and reconstructs the whole horizon exactly iff

`Q_(K+1)(Y)=prod_(j=1)^(K+1) p_j^+(Y)>H`.

FD-136 now removes a natural intermediate escape. Replacing counting measure by the scale-balanced weight `1/n` still allows one common squarefree-unit source with vanishing worst active-prime harmonic ancestry defect throughout the same almost-full prime regime, while its coefficient disagreement has positive logarithmic density `1/(2 zeta(2))`. The defect is sparse inside essentially every multiplicative shell because it remains concentrated on the moving central `omega(n)` interface.

The active source law must therefore live between three failures: counting-measure means are diluted, logarithmic scale balancing still misses multiplicative rank, and fully local control becomes exact component rigidity. The useful theorem must be visible at the active scale in both physical scale and rank without simply encoding the physical Möbius prefix.

## Locate the first joint scale-rank law between harmonic dilution and exact reconstruction

**Linked intuitions:** `MI-015-local-ancestry-has-an-exact-rough-core-rigidity-threshold`, `MI-016-harmonic-scale-balancing-still-misses-the-moving-rank-interface`.

FD-136 shows that `dn/n` is not the missing interpolation: balancing multiplicative scales does not assign nonvanishing weight to a moving `o(sqrt(log log n))` rank interface. FD-135 shows that edgewise `L^infinity` is already exact ancestry in the unit-sign model and crosses a sharp rough-core reconstruction threshold.

The surviving question is therefore genuinely two-coordinate. Can one derive a joint law in `(log n, omega(n))` — for example rank-conditioned harmonic ancestry, simultaneous short windows, or a comparably local quantile/coercivity condition — that kills the FD-132--FD-136 moving-rank witness while retaining nontrivial source freedom below the FD-135 exact-reconstruction threshold? A destination-side Fourier-skeleton coercivity theorem that bypasses coefficient recovery remains a distinct route and must be tested directly on the repeated-square target.

## Keep information equivalence, horizon entropy, scale visibility, rank visibility, local rigidity and target recovery separate

FD-117--FD-120 classify information, FD-121--FD-124 price common-source horizon coherence, FD-125--FD-128 identify sparse multiplicative escape, FD-129 identifies circular reconstruction, FD-130--FD-134 expose successive counting-measure dilution loopholes, FD-135 identifies the exact rough-core threshold for local ancestry, and FD-136 shows that harmonic physical-scale balancing still misses the same moving-rank defect.

“More arithmetic breadth” is not a single resource. A coercive law must specify which prime coordinates and rank layers remain visible after normalization, how physical scales are weighted, how local the constraint is, and how much source freedom remains before the condition simply reconstructs Möbius.