# Farey-discrepancy research lines

This file records current mathematical questions for the Farey-discrepancy line. It is a mutable synthesis, not a chronology or task list.

## Isolate the physical Mertens cushion after removing reservoir-resolution and inversion artifacts

**Linked intuition:** `MI-039-periodic-scale-switching-can-create-a-supercritical-floquet-blind-mode`.

FD-212--FD-224 separate periodic switched blindness, exact-Euler source quantifiers and literal coverage. The long blind constructions keep one absolute cutoff `p<=beta N<N`; a fixed positive fractional exact-Euler rule across dyadic horizons instead reaches the original prefix after finitely many steps and identifies the source there. Cofinal exhaustive exact Euler compatibility is therefore source identity, not a remaining switched-filter problem.

FD-225 removes the earlier switched-matrix conditioning explanation. The full divisor-reservoir response has an exact inverse, so all integer-multiple samples through depth `D` can be flattened to one common value up to unit rounding error while retaining an `N^(1-o(1))` earlier defect through the certified Vinogradov--Korobov range.

FD-226 now separates the two remaining quantitative costs. Replacing equal-width reservoirs by exact floor cells whose width scales with the divisor removes one power of `D` from the **prime-resolution** error: its relative cost is only `D exp(-c Phi(N))` up to polylogarithmic factors. Yet the overall certified depth does not improve. Exact common flattening forces the same positive occupancy cushion in every reservoir, the smallest reservoir sets that cushion at scale `N/(D log N)`, and fitting the physical Mertens correction restores a quadratic `D^2 D^(o(1))(log N)^2 exp(-c Phi(N))` pressure.

The current bottleneck is therefore not low rank, switched conditioning or an avoidable quadratic prime-resolution loss. It is the interaction between **common positive cushion geometry and the physical Mertens baseline**. The next discriminating question is whether a nonconstant null target or another capacity-adapted exact response can let larger reservoirs carry more of the correction without violating the switched observations, or whether positivity/source constraints force a smallest-cell bottleneck for every admissible repair.

## Keep literal coverage, exact inversion, prime resolution and common-cushion capacity distinct

The present frontier has four different currencies. Literal exact-Euler coverage controls whether relaxed sources exist at all. Full-divisor inversion controls whether the switched observation equations are algebraically hard. Scale-adapted cells control how accurately primes resolve those equations. The remaining Mertens/common-cushion term controls whether a positive physical realization has enough source capacity once all three representational costs have been cleaned up.

A stronger blind construction must therefore improve the last currency rather than repackage the first three. Conversely, an intrinsic coercive result must show that the common-cushion pressure survives alternative exact targets and admissible reservoir geometries, not merely that the present construction runs out at Vinogradov--Korobov scale.