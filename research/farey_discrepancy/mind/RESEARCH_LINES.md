# Farey-discrepancy research lines

This file holds the current mathematical questions suggested by the durable Farey-discrepancy intuitions. It is not a roadmap, task queue, status page, or history.

## Make the physical coefficient law beat the terminal harmonic fan, or make source descent pay the selection tax

**Linked intuitions:** `MI-001-cumulative-farey-discrepancy-is-mertens-energy`, `MI-002-odd-prime-square-reembedding-localizes-recursive-failure-to-dyadic-channel`, `MI-003-normalized-record-defect-is-the-composition-loss`, `MI-004-multiplicative-horizon-descent-can-preserve-the-source-quotient`, `MI-005-source-neutral-carrier-descent-spends-a-strict-finite-row-potential`, `MI-006-physical-coefficient-law-is-the-terminal-endpoint-resource`.

FD-102--FD-106 isolate the critical endpoint. Deterministic floor geometry cannot shorten the source-neutral logarithmic window; direct compression transports a fixed-power normalized mass; the adaptive cutoff keeps selected source coordinates large; and every predecessor family except the repaired terminal prime fan is critical-lossless. The only scale-dependent transport loss is the harmonic family `C_p/H~1/p`.

FD-107 now shows that the obvious generic coherence repair is insufficient. A single fixed nonnegative one-Lipschitz sublinear shell source can realize a polynomial high-mass state dominated by the exact terminal `4p` rows while every complete repaired child has vanishing relative critical excess. Thus common quotient-shell provenance, bounded increments, sublinear growth, source-richness and the least-prime-factor partition still permit the harmonic matched allocation.

The missing endpoint resource must therefore be genuinely more arithmetic. The direct route is to extract from the physical law

`Delta H(n)=mu(rad n) phi(rad n)/n`

a quantitative incompatibility with the terminal harmonic allocation: signed oscillation, covariance across reciprocal prime samples, a conservation law, or another consequence strong enough to force one repaired child to retain critical excess. A generic Lipschitz/coherence theorem cannot do this.

The alternative remains global amortization: combine the `epsilon`-transport law with actual source descents/head events often enough that the fixed packet-selection factor cannot survive a full false-RH chain.

## Locate the exact boundary of the high-mass mechanism

The adaptive source threshold is intrinsic: `L_H` tends to infinity exactly when `U_H/H` does. At the linear floor `U_H=H^(1+o(1))`, neither the source-rich core nor the current error normalization gives a growing source tag. A continuation that reaches this boundary must either prove the false-RH chain stays a fixed power above the floor long enough, or introduce a different observable whose compression error is still negligible at near-linear mass.

FD-107 strengthens rather than removes this boundary: its coherent countermodel already lives at polynomial high mass. The terminal obstruction is therefore not an artifact of marginal mass or shallow source access.

## Keep deterministic budget, transport mass, fan entropy and physical source law distinct

Source records provide recurrent entry states; FD-102 supplies an index-free deterministic charge; FD-104 transports normalized mass; FD-105 transports an adaptive source tag; FD-106 localizes the unresolved entropy to `C_p~H/p`; and FD-107 proves that generic coherent one-Lipschitz source geometry still saturates that endpoint. Future arguments must identify the specific physical coefficient information they use rather than relabel generic coherence as arithmetic structure.