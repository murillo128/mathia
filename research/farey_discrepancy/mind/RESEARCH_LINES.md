# Farey-discrepancy research lines

This file holds the current mathematical questions suggested by the durable Farey-discrepancy intuitions. It is not a roadmap, task queue, status page, or history.

## Make higher-order physical coefficient structure beat the terminal harmonic fan, or make source descent pay the selection tax

**Linked intuitions:** `MI-001-cumulative-farey-discrepancy-is-mertens-energy`, `MI-002-odd-prime-square-reembedding-localizes-recursive-failure-to-dyadic-channel`, `MI-003-normalized-record-defect-is-the-composition-loss`, `MI-004-multiplicative-horizon-descent-can-preserve-the-source-quotient`, `MI-005-source-neutral-carrier-descent-spends-a-strict-finite-row-potential`, `MI-006-physical-coefficient-law-is-the-terminal-endpoint-resource`.

FD-102--FD-106 isolate the critical endpoint. Deterministic floor geometry cannot shorten the source-neutral logarithmic window; direct compression transports a fixed-power normalized mass; the adaptive cutoff keeps selected source coordinates large; and every predecessor family except the repaired terminal prime fan is critical-lossless. The only scale-dependent transport loss is the harmonic family `C_p/H~1/p`.

FD-107 closes generic shell coherence: a single fixed nonnegative one-Lipschitz sublinear source can realize a polynomial high-mass state dominated by the exact terminal `4p` rows while every complete repaired child has vanishing relative critical excess.

FD-108 then tests the first genuinely physical replacement. The exact coefficient law `Delta H(n)=mu(rad n)phi(rad n)/n` yields a prime-divisibility covariance and a weighted large-sieve bound for the reciprocal-prime samples. But at the terminal source-rich scale the harmonic endpoint spends only `o(X^2)` inside an unavoidable `X^2` large-sieve budget. First-order physical `L^2` control is therefore **power-critical** and still cannot remove the `epsilon` loss.

The missing endpoint resource is now narrower. The direct route must use structure stronger than one-prime marginals or their first-order large-sieve energy: higher-order prime incidence, multiplicative sign coherence, correlations across the least-prime-factor tree, or another consequence of the actual coefficient law that makes the matched harmonic allocation impossible. The alternative remains global amortization: combine the `epsilon`-transport law with actual source descents/head events often enough that the fixed packet-selection factor cannot survive a full false-RH chain.

## Locate the exact boundary of the high-mass mechanism

The adaptive source threshold is intrinsic: `L_H` tends to infinity exactly when `U_H/H` does. At the linear floor `U_H=H^(1+o(1))`, neither the source-rich core nor the current error normalization gives a growing source tag. A continuation that reaches this boundary must either prove the false-RH chain stays a fixed power above the floor long enough, or introduce a different observable whose compression error is still negligible at near-linear mass.

FD-107 shows that generic coherence already fails at polynomial high mass, and FD-108 shows that the first physical prime-divisibility `L^2` constraint is still critical at the terminal fan. The obstruction is therefore not an artifact of marginal mass, shallow source access or absence of first-order arithmetic covariance.

## Keep deterministic budget, transport mass, fan entropy and physical source law distinct

Source records provide recurrent entry states; FD-102 supplies an index-free deterministic charge; FD-104 transports normalized mass; FD-105 transports an adaptive source tag; FD-106 localizes the unresolved entropy to `C_p~H/p`; FD-107 proves generic coherent shell geometry can saturate that endpoint; and FD-108 proves the first exact physical large-sieve control still fits inside the terminal power budget. Future arguments must identify the **higher-order physical information** they use rather than relabel first-order covariance or generic coherence as sufficient arithmetic structure.