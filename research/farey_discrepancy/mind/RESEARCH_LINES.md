# Farey-discrepancy research lines

This file holds the current mathematical questions suggested by the durable farey-discrepancy intuitions. It is not a roadmap, task queue, status page, or history.

## Turn arithmetic swarms inside amplitude-length protected record blocks into a global recurrence theorem

**Linked intuition:** `MI-001-cumulative-farey-discrepancy-is-mertens-energy`.

FD-072 supplies a source-selected positive mechanism: at a `sigma`-power record with `sigma>1/2`, the causal horizon `T=4X`, `D=3` confines the complete denominator to the record-controlled prefix and forces fixed positive nonsquarefree occupation and Schur defect. FD-073 shows that these protected episodes are amplitude-length blocks, not isolated points.

FD-074 adds an arithmetic restriction at every strict record center: `phi(rad X)>sigma|H(X)|`, and once `|H(X)|>1` the record sign equals `mu(rad X)=(-1)^omega(X)`. Along a false-RH supercritical record sequence, `rad X_j/X_j^sigma -> infinity`.

FD-075 now propagates arithmetic structure through the block itself. A strict record of amplitude `A` forces aligned physical coefficient mass in every backward window, and on a fixed amplitude-scale window there are `>> A^2/X` indices with record-matching squarefree-kernel parity and radical size `>> A`. Under false RH with `1/2<sigma<Theta`, this produces a diverging cloud

`>> X_j^(2sigma-1) R_sigma(X_j)^2`

of large-radical causal anchors inside each protected block. Every one of those points is simultaneously a quantitative near-record by FD-073.

The live problem is therefore no longer whether the source can concentrate all relevant arithmetic at one exceptional record center. It cannot. One must determine whether these nested signed-mass constraints and growing same-parity large-radical swarms prevent the protected blocks from being arbitrarily multiplicatively sparse, or force enough near-record quality to recur on logarithmic scale. A negative model must preserve substantially more of the exact coefficient law than one-Lipschitz regularity and must reproduce the whole local cloud, not only eligible record centers.

At the critical endpoint `sigma=1/2`, the causal mechanism still survives with the explicit `1/log` loss, but the polynomial cloud-growth argument no longer follows from `A^2/X` alone.

## Keep coherence, capture, recurrence, causal horizon selection, block thickness, host arithmetic, local host multiplicity, endpoint loss, leverage, and denominator growth separate

FD-071 explains why arbitrary-later persistence can be normalized away; FD-072 protects a causal record; FD-073 thickens it; FD-074 constrains its center; FD-075 forces a growing arithmetic population inside the same protected block. None of these controls global record spacing. Future progress must convert the local multiplicity plus exact signed source law into a recurrence/density theorem or exhibit a source-faithful sparse countergeometry.