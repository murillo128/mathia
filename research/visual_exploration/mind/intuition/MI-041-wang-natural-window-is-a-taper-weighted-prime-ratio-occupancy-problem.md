# MI-041 — Wang's arithmetic escape, if any, is prime-only collective and must survive derivative repayment

**Evidence level:** exact/literature-backed synthesis from VIS-313--[VIS-338](../../findings/VIS-338-proper-prime-power-tail-negligible.md). No prime-pair asymptotic or RH consequence is claimed.

VIS-330--VIS-334 classify the fixed-source transform: no real log-frequency is annihilated, high frequencies are attenuated by one order, and exact inversion charges one derivative plus memory. VIS-335--VIS-336 show that this derivative tax is sharp generically, including on one increasing-frequency trajectory.

VIS-337 tests the actual squared-von-Mangoldt source. Distributionally, for `F(u)=e^{-u}E_A(e^u)` one has `(1+D)F=mu-u du`, and Wang's output satisfies `(4-D^2)Q=4(mu-u du)`. Every prime-power jump is copied exactly into the derivative channel: `Delta Q'(log n)=-4 Lambda(n)^2/n`. Individual atoms are exponentially below the current Korobov--Vinogradov normalized envelope.

VIS-338 removes the higher-prime-power branch collectively. If `mu_pp` is the `p^k`, `k>=2`, part of the source, then `mu_pp([U,infinity))=O(U^2 e^(-U/2))`, and the complete Wang Green response, one-sided derivatives and total far-tail jump mass of `mu_pp` have the same exponential bound. This is smaller than every fixed Korobov--Vinogradov subexponential envelope. Proper prime powers therefore cannot carry the live source scale even when aggregated.

Any remaining arithmetic scale migration must come specifically from the signed organization of the `k=1` prime measure `mu_prime=sum_p (log p)^2/p delta_(log p)` against the continuous baseline `u du`. The destination burden is independent: even a genuine prime-only collective cancellation is useful only if Wang's complete error decomposition consumes the smaller `Q` scale without requiring `Q'` or equivalent unsmoothed information that reconstructs the larger source scale.

**Boundary.** VIS-338 proves no cancellation among the prime atoms and no improved PNT remainder. It removes proper prime powers as the collective carrier. The accepted Wang clue already records the surviving prime-only test, so no new Mind clue is needed.