# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Leave source-free positive Hilbert preconditioning after quotienting the growing-depth baseline

**Linked intuitions:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem` through `MI-060-nested-vandermonde-chains-pay-cumulative-newton-separation-costs`, `MI-061-finite-positive-branching-tropicalizes-to-subset-energies`, `MI-062-growing-positive-compact-depth-and-bounded-hilbert-correlations-are-source-free-conditioning-resources`, `MI-063-source-free-positive-metric-degeneration-is-preconditioning-unless-its-geometry-is-independently-fixed`, `MI-064-external-positive-geometry-must-carry-gap-resolved-noncommuting-orientation`.

VIS-341--VIS-361 reduce every fixed finite Wang bank to source-free confluent/Vandermonde geometry and identify the normalized positive weighted moment Gram as the compact conditioning invariant. VIS-362--VIS-366 price weighted rank collapse, vanishing masses, nested scales, arbitrary fixed finite positive branching and finally every positive compact coefficient measure through the Vandermonde partition sums.

VIS-367 shows that merely letting polynomial depth grow is not arithmetic structure. Every positive probability measure on `[-1,1]` already has the universal Chebyshev-capacity collapse `Z_q(mu)<=2^(-(q-1)(q-2))`, `s_q(q)<=2^(2-q)` for the raw monomial map. Super-exponential determinant collapse and exponential monomial ill-conditioning must therefore be quotiented before a growing-depth effect is credited.

VIS-368 closes uniformly comparable positive coefficient metrics: if `mW<=H<=MW`, every Wang singular value changes only by fixed factors. VIS-369 closes unconstrained positive degeneration as evidence: alignment with the baseline right-singular basis can prescribe any desired positive active singular spectrum, with metric eigenvalues paying for the gain.

VIS-370 closes the claim that a positive metric becomes meaningful merely because it is canonical or fixed independently of the desired output. If its relative operator `C` commutes with the source-free Gram `G=A_W^*A_W`, it is only sectorwise spectral reweighting; if `C=f(G)`, it is exactly a scalar filter of the baseline singular spectrum. VIS-371 removes the remaining one-Gram equivariance loophole: every unitary-equivariant positive rule built from one Gram already commutes with that Gram.

VIS-372 shows that adding several external Hermitian operators does not help when they all commute. A simultaneous-unitary-equivariant positive constructor is scalar on the joint eigenspaces, so the resulting metric still commutes with `G`; commuting external structure can refine degeneracies but remains joint spectral reweighting.

VIS-373 prices the first genuine escape quantitatively. Gram-block pinching gives an exactly commuting positive metric, and across resolved Gram gaps `delta_G`, `||C-C_0||_F<=||[G,C]||_F/delta_G`. With `C>=mI`, the entire modified singular spectrum is close to the commuting/preconditioning class when the gap-normalized commutator is small. Formal noncommutation is therefore not enough.

VIS-374 now closes the near-degenerate-cluster loophole for bounded positive metrics. If the Gram spectrum is grouped into clusters of width `eta` and inter-cluster separation `gamma`, with `mI<=C<=MI`, there is an exactly `G`-commuting positive metric `C_*` preserving the clusterwise metric eigenvalue multisets and satisfying the squared-singular-spectrum bound

`error <= ||G||_op sqrt(M/m) ||[G,C]||_F/gamma + 2M eta`.

If `C` is already block diagonal across clusters, only `2M eta` remains. Large rotations hidden inside a collapsing cluster are therefore spectrally cheap: the same small Gram width that hides them from the commutator also limits their effect on the singular spectrum.

The positive-metric frontier is now a fully scale-priced orientation problem. A surviving bounded positive-Hilbert candidate must have geometry fixed by physical coefficient/destination topology, arithmetic source information, or another independently justified operator, and must carry substantial orientation across resolved Gram gaps, retain a non-negligible within-cluster width that itself reaches the destination, or pay through metric degeneration/condition-number growth. Rapid eigenvector rotation inside an almost scalar cluster is not a free escape.

Genuinely indefinite/non-Hilbert topology, noncompact/global phase geometry with outer costs repaid, nonlinear source coupling, or an infinite-dimensional limit with a controlled physical norm remain different categories.

## Quotient packet and summatory geometry through theta before crediting arithmetic structure

The prime and square-log summatory sources remain exact functionals of the same normalized Chebyshev-theta remainder `G`. Positive compact geometry is now audited at fixed depth, growing depth, bounded metric equivalence, freely degenerating metrics, Gram-generated spectral filters, every unitary-equivariant positive one-Gram rule, commuting multi-operator rules, gap-normalized cross-sector orientation, and bounded near-degenerate cluster orientation.

A claimed Wang gain should therefore be reduced in this order: identify the exact source statistic, normalize the compact polynomial coordinate, price the partition/capacity geometry, expose the complete relative metric operator, choose the physically relevant Gram resolution, report the cluster width `eta`, inter-cluster gap `gamma`, metric bounds `m,M` and commutator size, charge basis and coefficient norms, and only then propagate the residual through the destination. If the apparent improvement disappears under the VIS-374 error budget, it is conditioning or near-preconditioning rather than arithmetic cancellation.
