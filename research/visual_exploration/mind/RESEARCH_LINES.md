# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Quantify the confluent phase budget of the first unresolved Wang tail

**Linked intuitions:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem`, `MI-042-fixed-polynomial-smoothing-preserves-the-korobov-vinogradov-envelope-class`, `MI-043-finite-scalar-wang-dilation-banks-have-a-vandermonde-cancellation-ceiling`, `MI-044-translations-split-wang-tail-cancellation-into-independent-phase-fibers`, `MI-045-phase-resolving-wang-windows-inherit-the-fiberwise-moment-cost`, `MI-046-turan-nazarov-forces-fixed-wang-cancellation-onto-microscopic-windows`, `MI-047-the-first-surviving-wang-tail-moment-always-pays-the-phase-entropy-gate`, `MI-048-finite-wang-cross-order-interactions-are-confluent-exponential-polynomials`.

VIS-330--VIS-340 establish the transform/source boundary. Wang smoothing attenuates high log frequency by one order and exact inversion charges one derivative; the surviving prime and square-log summatory sources are exact functionals of the same normalized Chebyshev-theta remainder `G(u)=e^(-u)theta(e^u)-1`.

VIS-341--VIS-347 price finite scalar smoothing through scale count, translation fibers and Turán--Nazarov concentration. VIS-348 shows that after exact cancellation of any number of lower moments, the first surviving moment still pays the same phase-count/gap/window/conditioning budget.

VIS-349 removes the remaining algebraic ambiguity about interactions among several nonzero tail orders. For any fixed truncation depth `q`, the full interacting tail is exactly a confluent exponential polynomial `sum_b exp(-ib xi) Q_b(xi)` with `deg Q_b<q`. Exact vanishing on an interval forces all phase-fiber moments through order `q` to vanish separately. If an extra local Wang order is claimed instead, the same interacting object must be `O(X^-1)` on the working window with an explicit next-order remainder budget.

The live finite-bank question is therefore quantitative rather than algebraic. What sharp Turán-type concentration bound holds for these scale-dependent confluent systems when phases can collide and polynomial coefficients can be ill-conditioned? A viable finite-bank escape has to exhibit controlled phase geometry and coefficient norms and then pay for any local smallness. Frequency/source-dependent coefficients, infinite expansions and direct signed information in `G` remain genuinely different mechanisms.

## Quotient packet and summatory geometry through theta before crediting arithmetic structure

Proper prime powers remain too small at the relevant scale, and the centered prime measure and prime-only square-log summatory source remain exact images of `G`. Proposed packets, weights or multiscale organizations should therefore first be translated into a quantitative statement about `G`.

For finite translated-dilate banks, VIS-341--VIS-349 now expose the full finite-order tail ledger. Exact low-order cancellation, cross-order interaction and local concentration are not separate unpriced freedoms: the first two reduce to ordinary or confluent exponential-polynomial geometry, and the remaining freedom is the quantitative cost of making that finite-dimensional object small on the target window without paying through phase growth, collisions, window collapse or coefficient degeneration.