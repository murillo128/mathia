# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Leave positive compact Hilbert polynomial geometry after quotienting its growing-depth baseline

**Linked intuitions:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem` through `MI-060-nested-vandermonde-chains-pay-cumulative-newton-separation-costs`, `MI-061-finite-positive-branching-tropicalizes-to-subset-energies`, `MI-062-growing-positive-compact-depth-and-bounded-hilbert-correlations-are-source-free-conditioning-resources`.

VIS-341--VIS-361 reduce every fixed finite Wang bank to source-free confluent/Vandermonde geometry and identify the normalized positive weighted moment Gram as the compact conditioning invariant. VIS-362--VIS-365 price weighted rank collapse, vanishing masses, nested scales and arbitrary fixed finite positive branching. VIS-366 removes support-cardinality and power-valuation restrictions entirely at fixed depth: every positive compact coefficient measure is priced by the Vandermonde partition sums `Z_1,...,Z_q`.

VIS-367 shows that merely letting the polynomial depth grow is not yet arithmetic structure. Every positive probability measure on `[-1,1]` already satisfies the universal Chebyshev-capacity bounds

`Z_q(mu) <= 2^(-(q-1)(q-2))`, `s_q(q) <= 2^(2-q)`

for the raw monomial map. Super-exponential determinant collapse and exponential monomial ill-conditioning are therefore generic source-free high-degree effects. A growing-depth proposal must quotient this baseline and include the physical coefficient/basis-change cost before claiming localization.

VIS-368 closes the bounded positive-correlation loophole. If a non-diagonal positive covariance `H` is uniformly Loewner-comparable to an already priced diagonal covariance `W`, then the corresponding Wang singular values are termwise comparable by fixed factors. Positive off-diagonal correlation is only bounded preconditioning unless the relative metric spectrum degenerates. Static source-independent sign/phase changes are an exact gauge special case.

The accepted Wang clue is consequently narrower. A continuation must exhibit a residual gain after the VIS-367 capacity baseline, coefficient topology, basis conversion, the VIS-342 order/truncation cost and the full Wang destination repayment are all charged. Plausible remaining categories are source-dependent signed selectors, a positive metric whose relative conditioning degenerates for a source-justified reason, genuinely indefinite/non-Hilbert topology, noncompact/global phase geometry with its outer costs repaid, nonlinear source coupling, or an infinite-dimensional limit with a controlled physical norm.

## Quotient packet and summatory geometry through theta before crediting arithmetic structure

The prime and square-log summatory sources remain exact functionals of the same normalized Chebyshev-theta remainder `G`. Fixed-depth compact positive geometry is exhausted by the moment/partition-sum audit; growing positive depth brings a universal Chebyshev capacity collapse; uniformly comparable positive coefficient correlations change only constants.

A claimed Wang gain should therefore be reduced in this order: identify the exact source statistic, normalize the compact polynomial coordinate, price the partition/capacity geometry, expose any relative metric degeneration, charge basis and coefficient norms, and only then propagate the residual through the destination. If the apparent improvement disappears at one of those source-free steps, it is conditioning rather than arithmetic cancellation.
