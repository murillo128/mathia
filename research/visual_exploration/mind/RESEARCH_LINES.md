# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Treat prime-phase population and finite-order Gram-clock motion as deterministic nulls

**Linked intuitions:** `MI-001-visual-residuals-must-survive-exact-coordinate-controls`, `MI-005-random-prime-phase-controls-have-deterministic-resonance-geometry`.

VIS-083--VIS-091 progressively separate fixed-prime population, local Gram-clock motion, finite-order inverse-theta corrections, polynomial sensitivity, and longer sampling windows. The lesson is stable: crossing the validity range of one finite-order clock model does not establish arithmetic structure, because a higher-order deterministic timing correction or a larger-scale equidistribution theorem may absorb the apparent residual.

A surviving Gram/prime-phase route must therefore specify the null at the same support, window, and approximation order as the claimed signal, or add an independently informative source coordinate such as zero geometry, `Z(g_n)`, derivatives, or another observable not determined by prime phases and the Gram clock.

## Use pre-registered quantized Markov types, but move beyond threshold refinement on the spent low-zero block

VIS-092--VIS-099 establish the exact information boundary behind the chronology experiment. Ordinary persistence, several delay summaries, and low signature levels collapse to local-block data; level four in three-delay dimension can retain chronology; exact real-valued block reassembly is generically singleton; and a fixed finite quantization converts the problem into an exact nondegenerate order-three Markov-type class with a parameter-free conditional-uniform null.

VIS-100 makes the finite null computationally exact by matching Whittle's type-class count with exhaustive completion. VIS-101 then tests a training-defined binary-median quotient of a frozen low-zero confirmation block at the pre-existing direction-sensitive level-four coordinate and finds no residual. VIS-102 repeats the same pre-registered architecture with a richer balanced three-bin training quantizer: the exact type class has `1752` words and the observed coordinate lies essentially at its center (`p=209/219`). The second negative strengthens the methodological result but not a theorem that zero chronology is null.

The current block has now been inspected under both binary and ternary threshold quotients at the same coordinate, so further threshold refinement on it is no longer a clean source-specific test. A live continuation must freeze a **materially different information carrier or functional** — for example an independently justified level-four/log-signature statistic or a new source scale — and use fresh confirmation material whenever the choice is informed by VIS-101--VIS-102. The exact Markov-type null remains the admission mechanism after quantization; returning to the real-valued path still requires a dequantization/control theorem.

## Treat the signed finite-Euler triangular witness as classified by `log X/L`

VIS-067--VIS-082 separate finite-torus null geometry, reciprocal-frequency certificates, coefficient mass, and actual sinc-filtered energy. The normalized signed triangular energy tends to zero exactly when `log X/L -> 0`. Tiny prime-log beat frequencies or divergent `l^1`/reciprocal certificates do not override that exact scale.

Further work on this witness is justified only if it asks for sharper asymptotics/constants or changes the observable/source law in a way not covered by the exact energy theorem.

## Build an information-loss atlas and promote only exact handoff theorems

Related renderings, invertible coordinate changes, and visually richer summaries are not independent evidence. Promotion should terminate in an exact rank, support, conditioning, information-distance, sampling-error, local-block, resonance-weight, energy, reconstruction, or null-nondegeneracy statement that remains meaningful without the image.