# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Find genuine theta-remainder nonstationarity at the live envelope, then audit derivative repayment

**Linked intuitions:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem`, `MI-042-fixed-polynomial-smoothing-preserves-the-korobov-vinogradov-envelope-class`.

VIS-330--VIS-340 establish the transform/source boundary. Wang smoothing attenuates high log frequency by one order and exact inversion charges one derivative. Proper prime powers are exponentially below the live envelope, while the surviving prime source and the square-log summatory source are exact/invertible images of the same normalized Chebyshev-theta remainder `G(u)=e^(-u)theta(e^u)-1`.

VIS-341 shows that any fixed polynomial truncation power leaves the `u^(3/5)(log u)^(-1/5)` Korobov--Vinogradov class unchanged up to its leading constant. VIS-342 strengthens the gate for variable scalar order: `Omega_r(u)=r(u)Omega_1(u/r(u))`, so subpolynomial `r(u)` still cannot produce a fixed improvement in the `u`-power. Inside the same scalar model, a gain `delta>0` requires `r(u)=u^(5 delta/2+o(1))`.

The source question is therefore precise. Does `G` have a genuinely nonstationary or scale-migrating property at the live envelope that survives the exact quotients in VIS-339--VIS-340 and is either non-scalar in a demonstrable signed/arithmetic sense or produces the polynomially growing effective order required by VIS-342 with all leakage under control? The destination question remains separate: does Wang's complete error decomposition retain the smaller `Q` scale without requiring `Q'` or equivalent unsmoothed information that repays the gain?

## Quotient packet and summatory geometry through theta before crediting arithmetic structure

Proper prime powers are too small at the relevant scale. The centered prime measure and the prime-only square-log summatory source are exact functionals of `G`. Any proposed packet, weighting, summatory organization or multiscale smoothing should first be translated back to a quantitative statement about `G`.

If the mechanism scalarizes to `u nu(t)+r log t`, VIS-341--VIS-342 supply the control: fixed or subpolynomial effective order cannot change the power class, and a claimed fixed gain requires polynomial order growth. If it does not scalarize, the signed or arithmetic estimate invalidating that reduction must be explicit. In either case the mechanism must preserve baseline cancellation and survive the downstream derivative ledger; many scales or coefficients by themselves do not constitute new source information.
