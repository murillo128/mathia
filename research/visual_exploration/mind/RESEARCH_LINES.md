# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Expose the resource that leaves the positive weighted Hermite/Newton valuation regime

**Linked intuitions:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem`, `MI-042-fixed-polynomial-smoothing-preserves-the-korobov-vinogradov-envelope-class`, `MI-043-finite-scalar-wang-dilation-banks-have-a-vandermonde-cancellation-ceiling`, `MI-044-translations-split-wang-tail-cancellation-into-independent-phase-fibers`, `MI-045-phase-resolving-wang-windows-inherit-the-fiberwise-moment-cost`, `MI-046-turan-nazarov-forces-fixed-wang-cancellation-onto-microscopic-windows`, `MI-047-the-first-surviving-wang-tail-moment-always-pays-the-phase-entropy-gate`, `MI-048-finite-wang-cross-order-interactions-are-confluent-exponential-polynomials`, `MI-049-confluent-turan-propagation-charges-total-multiplicity-not-phase-spacing`, `MI-050-fixed-phase-collisions-are-conditioned-in-divided-difference-coordinates`, `MI-051-bounded-wang-collisions-have-a-hermite-budget-and-block-riesz-floor`, `MI-052-fixed-wang-tail-coefficients-are-a-source-free-block-vandermonde-map`, `MI-053-fixed-depth-wang-bank-growth-is-controlled-by-anchor-packing-not-raw-scale-count`, `MI-054-dense-cluster-occupancy-repays-the-deepest-fixed-depth-wang-jet-at-square-root-rate`, `MI-055-regular-wang-multiclusters-add-hermite-jet-capacities`, `MI-056-colliding-wang-centers-matter-only-when-the-pooled-rescaled-gram-degenerates`, `MI-057-positive-wang-weights-are-just-a-weighted-moment-measure-until-its-gram-degenerates`, `MI-058-one-inner-scale-reprices-weighted-gram-collapse-by-balanced-hermite-depth`, `MI-059-polynomially-vanishing-cluster-masses-are-additive-shifts-in-the-hermite-cost-spectrum`, `MI-060-nested-vandermonde-chains-pay-cumulative-newton-separation-costs`.

VIS-341--VIS-355 reduce every fixed finite Wang bank to confluent exponential-polynomial geometry, identify the upstream block-Vandermonde moment map, and prove a uniform quotient floor throughout every fixed compact nondegenerate family even when the arithmetic source chooses the bank pointwise.

VIS-356 removes raw scale-count growth as an independent fixed-depth escape whenever one uniformly conditioned `q`-scale anchor survives. VIS-357 identifies the complementary loss of every compact square anchor with concentration into fewer than `q` shrinking inverse-scale clusters.

VIS-358 shows that square-anchor collapse is not enough: an equally spaced one-cluster bank has `s_q(A) asymp sqrt(N) epsilon^(q-1)`, so occupancy can repay the deepest cluster-jet loss. VIS-359 closes the regular separated multicluster extension by showing that local Hermite-jet capacities add across separated centers.

VIS-360 closes generic colliding-center geometry as a separate escape. If all nodes are pooled at their common outer width `rho` and the normalized degree-`q-1` polynomial-sampling Gram has a uniform positive eigenvalue floor, then `s_q(A) asymp sqrt(N) rho^(q-1)`. The nominal subdivision into colliding subclusters contributes no additional cancellation resource.

VIS-361 closes positive diagonal nonuniform weighting as a separate compact escape. With positive weights `w_j`, total mass `M`, and the natural weighted coefficient norm, the relevant invariant is the normalized weighted moment Gram `M^(-1)S^*WS`. If its smallest eigenvalue stays bounded below, then `s_q(A_w) asymp sqrt(M) rho^(q-1)`.

VIS-362 prices the first genuine hierarchical failure of that Gram gate. If the outer weighted measure collapses onto exactly `L<q` separated atoms, each carrying a nonvanishing mass fraction, and one common finer rescaling exposes a uniformly healthy local weighted polynomial cloud at every atom, then with `h=floor((q-1)/L)`

`s_q(A_w) asymp sqrt(M) rho^(q-1) epsilon^h`.

VIS-363 removes the nonvanishing-mass assumption whenever all mass fractions have polynomial valuations in the same inner scale. If `pi_ell asymp epsilon^(2a_ell)`, assign each Hermite jet `(ell,d)` the cost `a_ell+d` and let `tau_q` be the `q`-th smallest cost. Then

`lambda_min(M^(-1)S_epsilon^*WS_epsilon) asymp epsilon^(2tau_q)`

and

`s_q(A_w) asymp sqrt(M) rho^(q-1) epsilon^tau_q`.

VIS-364 shows that even a strictly nested hierarchy beyond one healthy inner scale can remain completely priceable. For the maximally nested chain `0,epsilon^(kappa_1),...,epsilon^(kappa_(q-1))`, Newton interpolation gives cumulative pivots `K_r=sum_(i<=r)kappa_i` and the full singular spectrum

`s_(r+1) asymp epsilon^(K_r)`.

Transferred to the Wang map, the floor is `rho^(q-1) epsilon^(K_(q-1))`. A finite one-branch hierarchy therefore does not create a new source-free resource merely by stacking progressively smaller positive separations; each deeper degree of freedom pays all previous scales.

The live compact question is now **recursive branching or active-subspace degeneration beyond a single finite Hermite/Newton valuation ledger**. A surviving positive-diagonal fixed-depth escape must involve competing branches with local ranks/masses that cannot be ordered into one cumulative chain, recursively changing active subspaces, incomparable scale relations, or another coefficient topology. Distinct exits remain growing tail depth/order, global phase geometry, shrinking windows, nonlinear source dependence, coefficient geometry outside the positive diagonal Hilbert setting, or a new signed arithmetic estimate.

## Quotient packet and summatory geometry through theta before crediting arithmetic structure

The prime and square-log summatory sources remain exact functionals of the same normalized Chebyshev-theta remainder `G`. Any finite translated-dilate packet should therefore be reduced first to the block-Vandermonde scale map and then to stable confluent phase coordinates.

After VIS-350--VIS-364, bounded phase collisions, bounded separated inter-cluster cancellation, compact source-adaptive selection, raw fixed-depth scale-count growth, square-anchor collapse, regular one-cluster occupancy, regular separated multicluster occupancy, colliding centers with a nondegenerate pooled rescaled Gram, positive nonuniform weights, one healthy inner scale after outer rank collapse, polynomially vanishing cluster masses in that same scale, and finite one-branch polynomially nested scale chains are explicit source-free geometry. A claimed compact gain must now survive a genuinely branching, incomparable or active-subspace-changing hierarchical rescaling. Merely stacking smaller positive separations is not by itself evidence that the full bank operator acquires an unpriced arithmetic cancellation mechanism.