# Visual-exploration research lines

This file records current mathematical questions for the visual-exploration line. It is a mutable synthesis, not a task queue or history.

## Exploit source-specific nonstationary log-frequency structure without repaying Wang's inverse derivative tax

**Linked intuition:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem`.

VIS-329 closes the moving-upper branchwise occupancy channel at the required natural-window upper-bound scale. The fixed-source route is now isolated much more sharply by VIS-330--VIS-334. A true power saving for the Wang source already buys a fixed zero-free half-plane; a positive lower linear logarithmic decay rate has the same Mellin cost. Coherent regularly varying secondary terms pass through the transform rather than disappearing, and the exact real log-frequency multiplier has no fixed-frequency null.

VIS-334 gives the decisive inverse boundary. For the normalized squared-von-Mangoldt summatory remainder `F` and adjusted Wang remainder `Q`, the exact causal inverse contains `(Q-Q')/4` plus exponentially weighted memory. Therefore any ordinary smooth sublinear envelope that controls both `Q` and its first log derivative transfers back to `F` at the same scale. Wang smoothing cannot manufacture such a pointwise gain for free.

The live transform-specific possibility is genuinely nonstationary/high-frequency arithmetic: make `Q` smaller while the derivative carries the missing scale, and then prove that the **complete Wang destination uses the smaller `Q` without paying that derivative cost back**. The alternative is a genuinely stronger direct estimate for `F`, which is new source arithmetic rather than transform leverage.

## Keep occupancy closure, direct source gain, transform attenuation and destination repayment distinct

The original-period Minkowski decimation means center-gap/Christoffel refinements are no longer load-bearing for the VIS-316 occupancy destination. On the fixed-source side, a small transform output alone is also not evidence of a new arithmetic scale: coherent low-frequency behavior survives, fixed frequencies are not annihilated, and simultaneous `Q,Q'` improvement inverts to the source.

Future candidates should therefore identify where the source spectrum moves, quantify the derivative/high-frequency asymmetry and carry that asymmetry through every later Wang error term. If the final estimate requires precisely the derivative size that the transform hid, the apparent smoothing gain has only been postponed.