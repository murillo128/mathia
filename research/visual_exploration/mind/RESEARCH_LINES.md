# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Find genuine theta-remainder nonstationarity at the live envelope, then audit derivative repayment

**Linked intuition:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem`.

VIS-330--VIS-336 establish the generic transform boundary: Wang smoothing attenuates high log frequency by one order and exact inversion charges one derivative. VIS-337 shows that every actual prime-power jump enters `Q'` exactly, while VIS-338 proves that the whole `k>=2` source, including its Green response and derivative jumps, is exponentially below the live Korobov--Vinogradov envelope.

VIS-339 identifies the surviving `k=1` source exactly as `nu_prime=u(1+D)G`, where `G(u)=e^(-u)theta(e^u)-1`. VIS-340 shows that the normalized prime square-log summatory remainder is another explicitly invertible image of the same `G`: `P=uG-H+2e^(-u)`, with `H` the one-sided exponential average, while proper prime powers contribute only `O(u^2e^(-u/2))`. At envelope-saturating heights the summatory source has leading scale `uG`, so ordinary partial summation cannot manufacture the desired pointwise gain.

The source question is now precise: does `G` itself have a genuinely nonstationary or scale-migrating property at the live envelope scale that Wang's nonvanishing filter turns into a smaller `Q` without merely restating a stronger PNT remainder? If so, the destination question remains separate: does Wang's complete error decomposition retain that smaller `Q` scale without requiring `Q'` or equivalent unsmoothed information that repays the gain?

## Quotient both packet and summatory geometry through the theta identities before crediting arithmetic structure

Proper prime powers are too small at the relevant scale. The remaining centered prime measure is exactly a weighted differential image of `G`, and the prime-only square-log summatory source is an invertible partial-summation image of `G`. Any proposed smooth packet, weighting or summatory organization should therefore first be translated back to a quantitative statement about `G`. Only structure that remains nontrivial after both exact reparameterizations, survives signed baseline cancellation, and then passes the downstream derivative ledger can count as a live Wang mechanism.
