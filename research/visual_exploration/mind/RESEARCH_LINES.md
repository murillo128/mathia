# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Leave source-free positive Hilbert preconditioning after quotienting the growing-depth baseline

**Linked intuitions:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem` through `MI-060-nested-vandermonde-chains-pay-cumulative-newton-separation-costs`, `MI-061-finite-positive-branching-tropicalizes-to-subset-energies`, `MI-062-growing-positive-compact-depth-and-bounded-hilbert-correlations-are-source-free-conditioning-resources`, `MI-063-source-free-positive-metric-degeneration-is-preconditioning-unless-its-geometry-is-independently-fixed`, `MI-064-external-positive-geometry-must-carry-gap-resolved-noncommuting-orientation`.

VIS-341--VIS-361 reduce every fixed finite Wang bank to source-free confluent/Vandermonde geometry and identify the normalized positive weighted moment Gram as the compact conditioning invariant. VIS-362--VIS-366 price weighted rank collapse, vanishing masses, nested scales, arbitrary fixed finite positive branching and finally every positive compact coefficient measure through the Vandermonde partition sums.

VIS-367 shows that merely letting the polynomial depth grow is not arithmetic structure. Every positive probability measure on `[-1,1]` already has the universal Chebyshev-capacity collapse

`Z_q(mu) <= 2^(-(q-1)(q-2))`, `s_q(q) <= 2^(2-q)`

for the raw monomial map. Super-exponential determinant collapse and exponential monomial ill-conditioning must therefore be quotiented before a growing-depth effect is credited.

VIS-368 closes uniformly comparable positive coefficient metrics: if `mW<=H<=MW`, every Wang singular value changes only by the fixed factors `sqrt(m)` and `sqrt(M)`. VIS-369 then closes unconstrained positive degeneration as evidence: alignment with the baseline right-singular basis can prescribe any desired positive active singular spectrum, with the metric eigenvalues paying exactly for the manufactured gain.

VIS-370 closes the weaker claim that a positive metric becomes meaningful merely because it is canonical or fixed independently of the desired output. If its relative operator `C` commutes with the source-free Gram `G=A_W^*A_W`, then it is only sectorwise spectral reweighting; if `C=f(G)`, it is exactly a scalar filter of the baseline singular spectrum. Whitening and ridge whitening make the transfer of inverse conditioning into the coefficient metric explicit.

VIS-371 removes the remaining one-Gram equivariance loophole. For any positive Hermitian metric rule `F(G)` satisfying unitary conjugation equivariance, stabilizer invariance forces `F(G)` to be scalar on each eigenspace of `G`. Hence `[F(G),G]=0`: every coordinate-free, source-free, unitary-equivariant metric built from a single Gram matrix is already a spectral filter of that Gram.

VIS-372 shows that adding several external Hermitian operators does not help when they all commute. A simultaneous-unitary-equivariant positive constructor is scalar on the joint eigenspaces, so the resulting metric still commutes with `G`; commuting external structure can refine degeneracies but remains joint spectral reweighting.

VIS-373 prices the first genuine escape quantitatively. For a noncommuting positive metric `C`, Gram-block pinching gives an exactly commuting positive metric `C_0`, and

`||[G,C]||_F^2 = sum_(alpha!=beta)|lambda_alpha-lambda_beta|^2 ||P_alpha C P_beta||_F^2`.

Across resolved Gram gaps `delta_G`, `||C-C_0||_F<=||[G,C]||_F/delta_G`; if `C>=mI`, the entire modified singular spectrum is correspondingly close to that of the commuting/preconditioning class when `||[G,C]||_F/(sqrt(m)delta_G)` is small. Formal noncommutation is therefore not enough: the imported orientation must be large after the spectral gaps and metric scale are charged.

The positive-metric frontier is now a gap-resolved orientation problem. A surviving candidate must have geometry fixed by a physical coefficient/destination topology, arithmetic source information, or a second independently justified noncommuting operator, and must carry non-negligible off-diagonal Gram-block mass across the relevant resolved spectral gaps. Alternatively it may exploit collapsing near-degenerate clusters, but then it must show that the gain is not already explained by the same source-free Gram collapse. The full eigenvalue, orientation and gap cost must reach the destination.

Genuinely indefinite/non-Hilbert topology, noncompact/global phase geometry with outer costs repaid, nonlinear source coupling, or an infinite-dimensional limit with a controlled physical norm remain different categories.

## Quotient packet and summatory geometry through theta before crediting arithmetic structure

The prime and square-log summatory sources remain exact functionals of the same normalized Chebyshev-theta remainder `G`. Positive compact geometry is now audited at fixed depth, growing depth, bounded metric equivalence, freely degenerating metrics, Gram-generated spectral filters, every unitary-equivariant positive one-Gram rule, commuting multi-operator rules, and quantitatively small noncommuting perturbations across resolved Gram gaps.

A claimed Wang gain should therefore be reduced in this order: identify the exact source statistic, normalize the compact polynomial coordinate, price the partition/capacity geometry, expose the complete relative metric operator, test whether its spectrum or orientation is freely engineerable or jointly spectral, compute the gap-weighted off-diagonal Gram blocks, charge basis and coefficient norms, and only then propagate the residual through the destination. If the apparent improvement disappears at one of those source-free steps, it is conditioning or near-preconditioning rather than arithmetic cancellation.
