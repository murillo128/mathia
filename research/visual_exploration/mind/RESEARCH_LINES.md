# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Calibrate the collision-free third-factorial channel under the exact finite-window null

**Linked intuitions:** `MI-001-visual-residuals-must-survive-exact-coordinate-controls`, `MI-006-nonlocal-information-and-macroscopic-amplitude-are-separate-gates`.

VIS-111--VIS-114 establish that growing-lag third-order information can escape every fixed block quotient, remain homometric at second order, and retain `O(1)` normalized synthetic amplitude after centering. VIS-115--VIS-119 then sharpen null calibration: permutation overlap changes variance/covariance, low-degree full-circle CUE trace moments are exact, and wrapping a finite zero block does not inherit the full Haar trace null automatically.

VIS-120 removes repeated-index contamination exactly. The collision-free triad is a pure distinct-triple statistic, but this transformation changes the null: independent uniform phases center it at zero whereas full-circle `CUE_N` has a nonzero low-degree mean `2(N-a-b)`. For a deterministic window its expectation is governed by the source's third factorial correlation law.

The next source-specific test is therefore precise: **freeze the window/compactification/selection rule and frequency panel, derive or simulate the same collision-free triad's finite-CUE mean/covariance under that exact transformation, and only then inspect zeta confirmation data**. Full-circle Haar or independent-null calibration is not interchangeable with the finite-window null.

## Treat finite-block escape and macroscopic synthetic amplitude as admission tests, not significance

The homometric witnesses prove that the channel can carry nonlocal information at constant amplitude. They do not show that zeta occupies it atypically. A visual residual is promotable only after lower-order collisions, window geometry, selection, and matched finite-size source covariance are removed at the same scale.

## Treat prime-phase and Gram-clock residuals as deterministic nulls

The earlier prime-phase/Gram-clock classifications remain controls. Crossing one finite-order clock approximation is not arithmetic evidence when a higher deterministic correction or equidistribution law absorbs the residual.