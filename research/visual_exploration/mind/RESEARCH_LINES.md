# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Target nonradial zero-entry, cross-center, or noncircular residuals after Poisson--Jensen subtraction

**Linked intuitions:** `MI-001-visual-residuals-must-survive-exact-coordinate-controls`.

The local critical-strip picture now has exact controls on both sides of zero entry. The multiplicity monomial is universal, critical-line modulus symmetry is reflection-forced, fixed finite log-residual jets are reciprocal-zero moments, and VIS-013 makes every nested circular shell inside a zero-free disk one Poisson orbit. VIS-014 then shows that the zeroth angular mode after zeros enter is exactly Jensen zero-counting and is equivalent to the centered radial-distance multiset.

A decisive visual target must therefore preserve information these scalar radial baselines delete: nonzero angular modes after explicit entry subtraction, several centers/heights jointly, a noncircular or moving domain, phase data, or multi-zero angular organization. Smooth radial evolution and the circular zero-entry staircase are no longer independent multiscale leads.

## Convert persistent visual residuals into exact handoff theorems

**Linked intuitions:** `MI-001-visual-residuals-must-survive-exact-coordinate-controls`.

VIS-002 remains the model: a rendered Prime-Flute residual became an exact achievement-set theorem with a clear matched-control consequence. Future handoffs should similarly end in a support, rank, transition, conditioning, angular, cross-center, or scale-coupling theorem whose statement no longer depends on seeing the image.

The gate is strict: apparent self-similarity, clustering, phase texture, finite jets, nested-shell coherence, radial zero-entry structure, or high average overlap are not durable evidence until the exact baseline is removed and the owning research line's aggregate/operator statistic is proved to retain the residual.
