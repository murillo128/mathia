# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Test whether the actual squared-von-Mangoldt remainder has useful log-frequency migration and whether Wang's destination avoids derivative repayment

**Linked intuition:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem`.

The moving-upper occupancy branch is closed at the required upper-bound scale by VIS-329. For the fixed-source branch, VIS-330--VIS-334 classify the generic transform cost: no real log-frequency is annihilated, high frequencies are attenuated by one order, and exact inversion charges one derivative plus memory.

VIS-335 and VIS-336 show that the remaining high-frequency channel is real rather than formal. A fixed mode of frequency `omega` has Wang output of size `O(1/omega)` while its derivative keeps order-one source amplitude, and a single quadratic chirp realizes the same separation along one trajectory whose instantaneous frequency grows with scale. Producing more abstract chirps therefore does not advance the line.

The source-specific question is now whether `F(u)=e^{-u}E_A(e^u)` for the actual squared-von-Mangoldt remainder exhibits quantitatively useful frequency migration strong enough to improve the pointwise Wang observable beyond the current source envelope. The destination-specific question is then whether the complete Wang error decomposition consumes the smaller observable without requiring `Q'` or equivalent unsmoothed information that reconstructs the lost source scale.

## Do not credit transform smoothing until the repayment ledger is complete

A small transformed output can coexist with a large derivative channel. Any proposed Wang gain must identify where the missing amplitude goes and trace every downstream occurrence of that carrier. A stronger direct bound on the source is valid mathematics, but it is source arithmetic rather than transform leverage.
