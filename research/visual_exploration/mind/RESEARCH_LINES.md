# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Target zero-entry, cross-center, or noncircular residuals after exact local and Poisson baselines

**Linked intuitions:** `MI-001-visual-residuals-must-survive-exact-coordinate-controls`.

The critical-strip local picture now has a sharp hierarchy of exact controls. The multiplicity monomial is universal, critical-line modulus symmetry is reflection-forced, every fixed finite log-residual jet is a reciprocal-zero moment, and VIS-013 shows that the full nested circular shell family inside a zero-free disk is determined by one outer shell through the Poisson semigroup.

A decisive visual target must therefore cross a genuine information event: another zero enters the domain, several centers/heights are compared, the domain changes noncircularly, or phase/multi-zero organization is retained. Smooth inward radial evolution in one zero-free disk is no longer a multiscale lead.

## Convert persistent visual residuals into exact handoff theorems

**Linked intuitions:** `MI-001-visual-residuals-must-survive-exact-coordinate-controls`.

VIS-002 remains the model: a rendered Prime-Flute residual became an exact achievement-set theorem with a clear matched-control consequence. Future handoffs should similarly end in a support, rank, transition, conditioning, zero-entry, or scale-coupling theorem whose statement no longer depends on seeing the image.

The gate is strict: apparent self-similarity, clustering, phase texture, finite jets, nested-shell coherence, or high average overlap are not durable evidence until the exact baseline is removed and the owning research line's aggregate/operator statistic is proved to retain the residual.
