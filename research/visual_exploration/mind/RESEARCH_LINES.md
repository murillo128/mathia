# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Find a source-sensitive low-frequency statistic that survives moment-null normalization

**Linked intuitions:** `MI-006-nonlocal-information-and-macroscopic-amplitude-are-separate-gates`, `MI-007-moment-null-packets-pay-an-order-matched-variation-gate`.

VIS-130--VIS-150 show that one edge factor forces any support-safe companion into a shrinking low-frequency channel, where the finite-CUE response can be expanded and cancelled by signed moment-null packets. The remaining question is whether the zeta/arithmetic correction occupies a different functional and survives the required normalization, variance, moving-window, and transfer costs.

The decisive evidence must come from the source-side lower-order term, not from a visually striking residual after universal control subtraction.

## Treat every fixed-dimensional consecutive Gram-lag picture as a deterministic Newton-clock control

**Linked intuition:** `MI-008-fixed-dimensional-gram-lags-are-a-newton-clock-null`.

VIS-150 isolates the `log^2 N` single-lag transition. VIS-151 removes the earlier Taylor-range artifact: throughout the well-sampled supercritical single-lag regime, exact inverse-Gram shear alone makes every fixed-prime torus character equidistribute to Haar. VIS-152 extends the control to an `r`-lag equal-step vector. The deepest fixed character is an order-`r` finite difference of the inverse Gram clock, with transition scale

`h^r / (N^(r-1) log^2 N)`.

VIS-153 closes the fixed-dimensional critical window as well. After the exact triangular Newton-difference torus automorphism, the first `r-1` coordinates become jointly Haar while the top difference converges to a one-parameter deterministic clock curve. Both the subcritical coherent regime and the critical/supercritical visual geometries are therefore generated by the sampling clock without any new prime source.

The live visual escape must leave this null family: growing lag dimension or prime support, irregular/nonconsecutive sampling, a zeta-value/zero coordinate, or another analytic relation that cannot be rewritten as finitely many inverse-clock differences. A fixed-dimensional consecutive lag cloud is a control even when its marginals or joint projections look random.

## Keep source quartic structure separate from clock-generated phase geometry

The finite-CUE/moment-null branch asks whether a source-specific arithmetic correction survives universal low-frequency cancellation. The Gram-lag branch classifies deterministic sampling geometry. Do not use clock-generated Haar appearance as evidence for arithmetic mixing, and do not use a source-sensitive moment residual unless the same statistic survives the exact clock and random-control baselines appropriate to its sampling scheme.
