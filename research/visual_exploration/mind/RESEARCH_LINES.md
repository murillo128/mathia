# Visual-exploration research lines

This file records the current mathematical questions that survive the Visual-Exploration evidence. It is not a roadmap, task queue, status page, or history.

## Test intrinsic source-subspace localization beyond the isotropic defect baseline

VIS-367--VIS-374 show that growing polynomial depth, bounded positive metric changes, one-Gram equivariant rules, commuting external operators and rotations hidden inside near-degenerate Gram clusters are all source-free conditioning resources unless an independently fixed geometry survives the exact destination budget. VIS-379 restricts sensitivity to the true simultaneous-similarity orbit, and VIS-380--VIS-382 show that persistent bounded output orientation with vanishing Gram defect requires the actual Gram tangent to carry substantial mass in positive joint-commutator modes at the `O(epsilon^2)` scale.

VIS-383 exposes a representation false positive: anisotropic reweighting of source generators can manufacture defect-normalized low-mode mass without changing their span or exact joint commutant. VIS-384 removes that entire generator-coordinate gauge relative to the fixed coefficient-space Hilbert geometry. After centering modulo the identity, the Hilbert-Schmidt source subspace `S` has an intrinsic commutator Laplacian `Delta_S`, equivalently obtained from any redundant frame by contracting with its Gram pseudoinverse. Invertible anisotropic reparameterizations, common scaling, permutations and redundant expansions of the same `S` no longer change the statistic.

VIS-385 then prices the first source-free subspace baseline. At fixed matrix size and source rank the total commutator spectral mass is exactly fixed, and under a Haar-uniform rank-`r` source subspace the defect energy `<G,Delta_S G>` has an exact mean and variance determined by `d`, `r` and the second/fourth moments of the fixed traceless Gram tangent. Thus an anomalous quadratic defect and an ordinary defect with anomalous defect-normalized low-mode projector mass are distinct mechanisms.

The live resource is now **source-subspace-specific spectral organization after generator gauge and the isotropic rank baseline have both been removed**. First decide whether the actual `(G,S)` is already exceptional at the quadratic defect-energy level; if not, ask whether `G` has excess mass in the `O(epsilon_S^2)` positive spectrum of `Delta_S` relative to stronger matched nonarithmetic subspaces preserving the remaining matrix resources. Any surviving excess still needs a mathematical source mechanism and a Wang destination payoff after metric, rank-rule, Gram-gap and normalization costs are charged.

## Quotient universal compact geometry before crediting a Wang gain

For any proposed positive-Hilbert improvement, first reduce the source to the exact normalized statistic, price Vandermonde/capacity collapse, expose the full relative metric, restrict the differential to the true simultaneous-similarity orbit, replace arbitrary generator coordinates by the intrinsic centered source subspace, freeze the ambient Hilbert metric and numerical rank rule, and calibrate the fixed-`G` isotropic defect-energy null before interpreting nonlinear low modes.

The decisive question remains the accepted Wang localization clue in its narrowed form: does defect-squared low-mode projector mass survive these representation quotients and source-free controls because of arithmetic/source structure? VIS-384 closes the generator-coordinate loophole rather than merely pricing it, while VIS-385 shows that source rank already fixes substantial first-order spectral information. A low-mode effect earns credit only for structure beyond those exact baselines.