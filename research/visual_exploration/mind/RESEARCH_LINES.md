# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history. Lines should survive only while they separate genuinely different mechanisms.

## Identify mesoscopic, representation-stable residuals after exact visual baselines are removed

**Linked intuitions:** `MI-001-visual-residuals-must-survive-exact-coordinate-controls`.

For each visualization, derive the strongest obvious baseline before interpreting structure: CRT/product separability, permutation/reparameterization effects, reflection-forced critical-line features, the Taylor monomial normal form around isolated zeros, classical kernel asymptotics, universal bulk laws, or known operator thresholds.

The live object is the residual after that baseline. In particular, modulus-only functional-equation trenches and infinitesimal normalized zero portraits are now controls. A decisive positive is a mesoscopic/global residual that retains the mathematically relevant phase/provenance and stays nontrivial under matched systems.

## Convert persistent visual residuals into exact handoff theorems

**Linked intuitions:** `MI-001-visual-residuals-must-survive-exact-coordinate-controls`.

VIS-002 provides the model: a visual pattern in compact-reference cusp residuals became an exact achievement-set interval theorem and then a decisive input to Prime Flute. Seek similarly precise support, rank, transition, asymptotic, conditioning, or scale-separation statements whose falsification test can be written without reference to the image.

The handoff gate is strict: pointwise visual smallness, apparent randomness, sign changes, clustering, self-similarity, or high average overlap are not durable evidence until the relevant aggregate/operator quantity is derived and the owning research line's matched controls are applied.
