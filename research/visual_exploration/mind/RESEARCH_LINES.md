# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Find genuine theta-remainder nonstationarity at the live envelope, then audit derivative repayment

**Linked intuition:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem`.

VIS-330--VIS-336 establish the generic transform boundary: Wang smoothing attenuates high log frequency by one order and exact inversion charges one derivative. VIS-337 shows that every actual prime-power jump enters `Q'` exactly, while VIS-338 proves that the whole `k>=2` source, including its Green response and derivative jumps, is exponentially below the live Korobov--Vinogradov envelope.

VIS-339 identifies the surviving `k=1` source exactly. For `G(u)=e^(-u) theta(e^u)-1` and the centered prime measure `nu_prime=mu_prime-u du`,
`nu_prime = u(1+D)G`.
Every smooth packet obeys
`int phi d nu_prime = int [(u-1)phi-u phi']G du`,
and the transformation is invertible after one boundary value. A taper, phase, block or multiscale linear statistic of the prime atoms therefore has no independent evidential status merely because it looks organized in atomic coordinates; it must translate into a genuinely nontrivial property of the normalized Chebyshev-theta remainder.

The source question is now precise: does `G` have a nonstationary or multiscale property at the live envelope scale that the nonvanishing Wang filter turns into a smaller `Q` without simply restating a stronger PNT remainder? If so, the destination question remains separate: does Wang's complete error decomposition retain that smaller `Q` scale without requiring `Q'` or equivalent unsmoothed information that repays the gain?

## Quotient packet geometry through the theta identity before crediting arithmetic structure

Proper prime powers are too small at the relevant scale, and the remaining prime source is exactly a weighted differential image of `G`. Any proposed smooth source organization should first be pushed through the VIS-339 test-function identity. Only structure that remains nontrivial as a statement about `G`, survives signed baseline cancellation, and then passes the downstream derivative ledger can count as a live Wang mechanism.
