# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Freeze the Wang gate and sieve fixed-gate subperiod mechanical paths

**Linked intuitions:** `MI-040-wang-pointwise-upper-ceiling-is-a-localization-coherence-budget` and `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem`.

VIS-299--VIS-316 reduce the natural Wang mean-square window to a taper-weighted prime-ratio occupancy problem. VIS-317--VIS-318 show why scalar determinant projection and fixed determinant fibres each throw away a load-bearing resource. VIS-319 resolves the representation dilemma with a unimodular Bezout chart `(t,h)->(q,r)` that preserves both prime exclusions modulo every prime while retaining a long transverse coordinate.

VIS-320 reduces the resulting thin domain to at most three branches `t_j(h)=floor(U(h))-j` governed by one rational rotation of period `max(u,v) asymp M`. At natural resolution `D asymp M/log log M`, the physical segment is shorter than one full period, so full-cycle equidistribution is not the missing theorem.

VIS-321 removes the remaining moving-gate complication. Consecutive branch steps are the two vectors

`s_0=(-b,-a)`, `s_1=(u-b,v-a)`,

with determinant `1`; all three branches are translates of the same rational mechanical/Christoffel path. More importantly, the exact affine branch-admission threshold can be frozen with at most `1+floor(2D/min(u,v))` membership errors per branch. At the natural Wang scale this is eventually at most one error per branch, so the full strip differs from three fixed-gate translates by only `O(1)` points.

The live theorem is therefore genuinely arithmetic: control simultaneous primality on a **fixed-gate subperiod mechanical path** uniformly in the moving rational slope/Bezout data, strongly enough after Wang taper weighting. A direct sieve/discrepancy theorem may do this, or an additive-energy/coefficient-weighted estimate may bypass worst-cell occupancy. Further analysis of the `O(1/(uv))` gate drift does not address the remaining obstruction unless it changes the bounded-error conclusion.

## Keep local arithmetic dimension, subperiod length and destination taper together

The exact chart now preserves the two prime coordinates, the path geometry is unimodular, and the gate motion is negligible at the destination scale. What remains is not a coordinate problem but the interaction of two-prime exclusions with a subperiod segment whose denominator moves with the represented ratio.

A successful argument should therefore keep the fixed branch mask, simultaneous-prime condition and Wang coefficient mass visible until the final estimate. Generic two-dimensional lattice bounds prove more than the representation needs; full-period rational balance proves less than the physical window requires. The next gain must come from arithmetic occupancy or destination-weighted cancellation on this precise one-dimensional object.