# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Find collective prime-only organization at the live envelope scale, then audit derivative repayment

**Linked intuition:** `MI-041-wang-natural-window-is-a-taper-weighted-prime-ratio-occupancy-problem`.

VIS-330--VIS-336 establish the generic transform boundary: Wang smoothing attenuates high log frequency by one order and exact inversion charges one derivative. VIS-337 shows that every actual prime-power jump enters `Q'` exactly but an isolated jump is exponentially too small. VIS-338 strengthens this to the whole `k>=2` source: all proper prime powers together, including their Green response and derivative jumps, are `O(U^2 e^(-U/2))`, exponentially below the live Korobov--Vinogradov envelope.

The source-specific question is therefore no longer about generic prime-power packets. It is specifically whether a growing signed organization of the `k=1` weighted prime atoms against the continuous baseline produces useful nonstationary cancellation at the current envelope scale.

If such prime-only migration exists, the destination question remains separate: does Wang's complete error decomposition retain the smaller `Q` scale without requiring `Q'` or equivalent unsmoothed data? The accepted `CLUE-wang-fixed-source-packet-localization` already contains this narrowed test; no duplicate clue is needed.

## Do not credit singularity or smoothing until the scale and repayment ledger match

Exact derivative jumps identify where arithmetic singular information is stored, but VIS-337--VIS-338 show that neither isolated atoms nor the entire proper-prime-power tail has enough magnitude to carry the live envelope. Any proposed gain must first exhibit collective `k=1` prime structure relative to the baseline, then trace every downstream occurrence of the derivative carrier.