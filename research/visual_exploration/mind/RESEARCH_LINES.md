# Visual-exploration research lines

This file holds the current mathematical questions suggested by the durable visual-exploration intuitions. It is not a roadmap, task queue, status page, or history.

## Test frozen zeta windows at the weakest claim strength that still answers the question

**Linked intuitions:** `MI-001-visual-residuals-must-survive-exact-coordinate-controls`, `MI-002-gauge-invariant-visual-geometry-can-still-be-information-flat`, `MI-003-overlap-uncertainty-must-be-separated-from-residual-geometry`, and `MI-004-dimension-free-confirmation-comes-from-narrowing-the-destination-question`.

VIS-060--VIS-063 separate fixed-table comparison from population claims, control dependence among whole-control replicates, and distinguish the unavoidable `sqrt(K/B)` cost of full-law certification from the dimension-free `B^{-1/2}` cost of one frozen quadratic witness. Choose the destination question first and calibrate uncertainty to that question; representation richness does not remove a real statistical dimension cost.

## Treat the hybrid residual as a full coordinate, not an omitted nuisance

VIS-064 shows that independently defined hybrid prime and zero factors satisfy an exact three-channel scale identity: the sum of their log-modulus increments is the hybrid-residual increment, with the analogous phase relation modulo `2 pi`. Approximate prime/zero compensation is therefore expected whenever the hybrid residual is small.

VIS-065 closes the simple contrast repair: after retaining the residual, `A-B` is just an invertible coordinate change of one factor plus the residual. VIS-066 lifts this to the complete fields. Since `B=R-A` pointwise, **every deterministic nonlinear, lagged, multiscale, topological, or other post-processing of the complete prime/zero increment fields is reconstructible from one factor field and the residual field**. Post-processing cannot manufacture a third information channel.

The honest hybrid visual questions are therefore within-factor scale geometry against matched controls, unexpected residual structure beyond the hybrid error theorem, or the calibrated joint law of `(factor,residual)`. A genuinely new prime/zero interaction requires an independently defined variable or constraint not reconstructible from that shear.

## Generalize only after separating overlap, long memory, and adaptive representation error

A zeta-population claim still requires an honest source-side uncertainty model. Deterministic overlap geometry, long-range dependence, nonlinear closure sensitivity, normalization conditioning, and adaptive partition/support/reference changes are distinct channels. Choosing a witness, partition, window, gauge, hybrid scale, or post-processing on confirmation controls invalidates the corresponding fixed-target radius unless the selection is covered explicitly.

## Test Farey residuals against endpoint-safe exact nulls

Endpoint-safe sublinear bands and reflection-preserving same-gap controls remain the correct comparator category for Farey residuals.

## Build an information-loss atlas and promote only exact handoff theorems

Related renderings or invertible coordinate changes of one field are not independent evidence. Promotion should terminate in an exact rank, support, conditioning, information-distance, orientation, sensitivity, transport, sampling-error, scale, or reconstruction statement that remains meaningful without the image.
