# PC-268 — nonresonant Schur rotation is the torus IDS

**Status:** `EXACT-DERIVED` + `LITERATURE+DERIVED` + `PRIOR-ART-CLASSICALIZATION` + `DECISIVE-BOUNDARY` for the normalized rotation/winding channel left open by PC-257 and PC-262--PC-267.

Fix a nonresonant simultaneous-growth sequence as in PC-262,

\[
2<p_n<r_n<q_n,
\qquad
\frac{p_n}{q_n}\to\alpha,
\qquad
\frac{r_n}{q_n}\to\beta,
\qquad
0<\alpha<\beta<1,
\]

with `1,alpha,beta` linearly independent over `Q`. Let

\[
\Delta_n=
\frac{D_{p_n,r_n;q_n}}{q_n\sqrt{p_nr_n}},
\qquad
\mathscr H_n=
\begin{pmatrix}
0&\Delta_n\\
\Delta_n^T&0
\end{pmatrix}.
\tag{1}
\]

PC-257 proves that the complete finite spectral problem of `mathscr H_n` is generated by a fixed-width symmetric Schur/Riccati sweep along the ordered collision chain. A natural unresolved question was whether a projective rotation or winding accumulated by that long transfer could retain information beyond the bulk integrated density of states of PC-262.

For the **canonical total Schur rotation index**, the answer is exact and negative. For every real energy `E`, away from the harmless convention at an eigenvalue, let `Rot_n(E)` be the cumulative signed focal/pivot count of the symmetric Schur sweep for

\[
\mathscr H_n-EI.
\tag{2}
\]

Equivalently, use any nonsingular symmetric block-pivoting realization of the PC-257 elimination and sum the negative inertia of the successive pivot blocks. Then

\[
\boxed{
\operatorname{Rot}_n(E)
=
 n_-\!\left(\mathscr H_n-EI\right)
=
\#\{\lambda\in\operatorname{Spec}(\mathscr H_n):\lambda<E\}.
}
\tag{3}
\]

Thus the finite total projective crossing number is not an additional long-cocycle observable: it is exactly the finite eigenvalue-counting function. In the nonresonant limit, if

\[
\mu_n=
\frac1{p_n+r_n}
\sum_{\lambda\in\operatorname{Spec}(\mathscr H_n)}\delta_\lambda,
\tag{4}
\]

then PC-262 and the exact singular-value dilation give

\[
\mu_n\Longrightarrow\mu_{\alpha,\beta},
\tag{5}
\]

where `mu_{alpha,beta}` is the deterministic induced-torus IDS already written explicitly in PC-265. Therefore at every continuity point of its distribution function,

\[
\boxed{
\frac{\operatorname{Rot}_n(E)}{p_n+r_n}
\longrightarrow
\mu_{\alpha,\beta}(( -\infty,E)).
}
\tag{6}
\]

The same limit holds for the admissible pairwise-coprime rational-partition controls with the same irrational limiting ratios. In particular, the normalized **total** Schur/Pruefer/Maslov rotation does not retain prime-specific information beyond the classical torus IDS.

At the chiral center there is an even sharper exact audit. If `k_n` is the PC-251 collision rank, then `mathscr H_n` has exactly `k_n` negative eigenvalues, `k_n` positive eigenvalues, and `p_n+r_n-2k_n` zeros, so

\[
\boxed{
\operatorname{Rot}_n(0)=k_n,
\qquad
\frac{\operatorname{Rot}_n(0)}{p_n+r_n}
\longrightarrow
\frac{\alpha\beta}{\alpha+\beta}.
}
\tag{7}
\]

This closes the most canonical normalized rotation-number escape left after the bulk IDS classicalization. It does **not** identify every internal matrix-valued projective coordinate with the IDS, and it does not control subleading winding fluctuations, individual Pruefer eigenphases, near-resonant scaling, or Lyapunov norm growth. Those remain genuinely outside the theorem.

## 1. The PC-257 transfer is a symmetric Schur elimination

PC-257 gives an explicit path decomposition of the support of `mathscr H_n` with bags of size three. Sweeping from left to right and eliminating vertices that leave the frontier produces a symmetric Schur-complement recursion with a frontier of size at most two. Over `C(z)` the same recursion generates the complete characteristic polynomial; on the real axis it is a symmetric elimination of (2).

Suppose first that a chosen leading pivot block `A` is nonsingular and write

\[
\mathscr H_n-EI
=
\begin{pmatrix}
A&B\\
B^T&C
\end{pmatrix}.
\tag{8}
\]

The standard congruence factorization is

\[
\begin{pmatrix}
I&0\\
-B^TA^{-1}&I
\end{pmatrix}
\begin{pmatrix}
A&B\\
B^T&C
\end{pmatrix}
\begin{pmatrix}
I&-A^{-1}B\\
0&I
\end{pmatrix}
=
\begin{pmatrix}
A&0\\
0&C-B^TA^{-1}B
\end{pmatrix}.
\tag{9}
\]

Sylvester inertia invariance under congruence therefore gives the Haynsworth additivity identity

\[
\operatorname{In}(\mathscr H_n-EI)
=
\operatorname{In}(A)
+
\operatorname{In}(C-B^TA^{-1}B).
\tag{10}
\]

Iterating (10) along the PC-257 path decomposition proves that the sum of the negative inertias of all eliminated pivot blocks is exactly

\[
n_-(\mathscr H_n-EI).
\tag{11}
\]

This is independent of the admissible symmetric pivot choices. Intermediate scalar pivots can vanish at isolated energies even when `E` is not a final eigenvalue; using `2 x 2` symmetric pivots or taking a one-sided energy limit removes that chart singularity without changing the total inertia. Hence the total index is intrinsic even though a particular Riccati coordinate is not.

Because the eigenvalues of `mathscr H_n-EI` are `lambda-E`,

\[
n_-(\mathscr H_n-EI)
=
\#\{\lambda<E\},
\tag{12}
\]

which proves (3) without any asymptotic input.

## 2. Why this is the canonical rotation count rather than a new wrapper

The quantity in (3) is not introduced by attaching an external spectral functional to Prime Circle. It is the integer accumulated by the exact symmetric Schur/Riccati transfer that PC-257 already derives from the native collision geometry. A Riccati chart becomes singular precisely at a focal crossing; lifting those crossings to a projective/Lagrangian phase gives the usual discrete Pruefer/Maslov index.

The abstract identification of such a lifted phase count with eigenvalue counting is classical matrix oscillation theory. Hermann Schulz-Baldes, **Rotation numbers for Jacobi matrices with matrix entries**, *Mathematical Physics Electronic Journal* 13 (2007), Paper 5, develops matrix-valued transfer rotation numbers precisely to count eigenvalues of self-adjoint block Jacobi problems. Hermann Schulz-Baldes and Liam Urban, **Space versus energy oscillations of Pruefer phases for matrix Sturm-Liouville and Jacobi operators**, *Electronic Journal of Differential Equations* 2020:76 (2020), 1--23, DOI `10.58997/ejde.2020.76`, proves that the lifted matrix Pruefer space-oscillation count equals the number of eigenvalues below the energy.

The PC-257 pathwidth-two chain need not be forced into one globally nonsingular block-Jacobi chart for (3): the symmetric Schur proof above already establishes the invariant total count directly. The matrix-Pruefer literature is the prior-art boundary explaining why interpreting that count as an independent rotation number would not produce a new mechanism.

## 3. The nonresonant normalized index is exactly the IDS distribution function

By definition of (4), equation (3) gives the finite identity

\[
\boxed{
\frac{\operatorname{Rot}_n(E)}{p_n+r_n}
=
\mu_n(( -\infty,E)).
}
\tag{13}
\]

PC-262 proves weak convergence of the singular-square empirical measures, and the symmetric-dilation factorization used in PC-265 converts this into (5). Weak convergence of probability measures implies convergence of distribution functions at every continuity point of the limiting measure, so (13) immediately gives (6).

This step is stronger than the off-spectrum logarithmic-potential statement of PC-265 in one specific direction. Equation (6) remains meaningful **inside** the limiting spectral interval, where the kernel `log|E-lambda|` is singular and PC-265 deliberately makes no convergence claim. The price is that the rotation count contains only cumulative spectral mass: it cannot see microscopic placement inside that mass.

The limiting measure is determined by the induced Haar return process of the rational-torus collision coding and the explicit Green weight of PC-258. Consequently the limiting rotation number is a deterministic torus quantity as well. No additional long-transfer invariant is created by passing from the IDS to its canonical oscillation/rotation representation.

## 4. Exact zero-energy control

The chiral form (1) gives

\[
\operatorname{Spec}(\mathscr H_n)
=
\{\pm\sigma_j(\Delta_n):\sigma_j>0\}
\cup\{0\}^{p_n+r_n-2\operatorname{rank}\Delta_n}.
\tag{14}
\]

PC-251 identifies

\[
\operatorname{rank}\Delta_n=k_n,
\tag{15}
\]

where `k_n` is the number of double-boundary collision cells. Hence exactly `k_n` eigenvalues lie below zero, proving the finite identity in (7). PC-258/PC-262 give

\[
\frac{k_n}{q_n}\to\alpha\beta,
\qquad
\frac{p_n+r_n}{q_n}\to\alpha+\beta,
\tag{16}
\]

and therefore the stated limit `alpha beta/(alpha+beta)`.

This audit is useful because zero is a macroscopic atom of the chiral dilation in general, so it is not automatically a continuity point of (5). Equation (7) does not appeal to continuity at zero; it is an exact finite rank count followed by the collision-density limit.

For the persisted finite control `(p,r,q)=(7,11,13)`, PC-257 has six collision coordinates. Thus the `18 x 18` dilation has six negative eigenvalues, six positive eigenvalues and six zero eigenvalues, giving

\[
\operatorname{Rot}(0)=6
\tag{17}
\]

exactly, independently of which admissible symmetric Schur pivoting chart is used.

## 5. Matched controls and RH-facing consequence

The finite identity (3) is pure self-adjoint linear algebra. The source-specific input enters only through the matrix `mathscr H_n`. PC-262 already proves that admissible pairwise-coprime rational-partition controls with the same irrational limiting ratios have the same limiting singular-square measure, hence the same limiting dilation measure `mu_{alpha,beta}`. Equation (6) therefore gives the same normalized total rotation for those controls.

Accordingly the route

\[
\text{native collision transfer}
\longrightarrow
\text{total projective/Pruefer rotation}
\longrightarrow
\text{new nonresonant arithmetic invariant}
\]

fails at leading normalized order. The rotation number is a different representation of the IDS, not additional spectral information. It supplies neither a new complex variable, nor a functional equation, nor a critical-line mechanism, and it cannot distinguish prime triples from the matched rational-partition controls in the nonresonant regime.

The theorem is intentionally narrower than a full projective-cocycle no-go. The following remain outside it:

- individual eigenphases of a matrix-valued Pruefer variable before taking the total crossing count;
- `O(1)` or smaller fluctuations of `Rot_n(E)` around `(p_n+r_n) mu_{alpha,beta}(( -infinity,E))`;
- energy windows shrinking with `n`, where fine spacing enters;
- projective data not homotopic to the inertia/focal crossing index;
- norm growth and top Lyapunov exponents of the transfer product; and
- rationally resonant or near-resonant sequences excluded by PC-262.

Any surviving rotation-based proposal must therefore use one of those finer channels and pass the same matched-control test. Merely computing a total winding, Maslov index, focal count, or integrated Pruefer rotation of the native Schur transfer is now classical IDS data.

## 6. Prior-art and novelty audit

The abstract ingredients are established. E. V. Haynsworth, **Determination of the inertia of a partitioned Hermitian matrix**, *Linear Algebra and its Applications* 1 (1968), 73--81, is the classical inertia-additivity source behind (10). Schulz-Baldes (2007) treats rotation numbers for Jacobi matrices with matrix entries, and Schulz-Baldes--Urban (2020) explicitly identifies lifted matrix Pruefer oscillations with the eigenvalue counting function. Related density-of-states work likewise interprets normalized Pruefer winding as an IDS mechanism.

A targeted search across Schur-complement inertia, block-Jacobi rotation numbers, matrix Pruefer phases, Maslov/focal indices and integrated density of states found exactly this established oscillation-theory relationship. No novelty is claimed for inertia additivity, projective phase lifting, matrix Pruefer theory, Maslov indices, or rotation-number/IDS equivalence.

The project-local contribution is instead the exact placement of the **specific PC-257 source-forced width-two transfer** inside that classical boundary, followed by the combination with PC-262's induced-torus IDS. This removes one explicitly surviving Prime-Circle channel: the leading normalized total rotation of the growing transfer cannot carry more nonresonant prime information than the already classicalized bulk spectrum.

## 7. Falsification and boundary audit

The result has direct failure tests.

1. **Finite inertia:** exhibit a real `E` and a completed symmetric PC-257 Schur sweep whose cumulative pivot inertia differs from `n_-(mathscr H_n-EI)` after chart singularities are treated by admissible symmetric pivoting or one-sided limits.
2. **Spectral count:** exhibit an eigenvalue of `mathscr H_n` for which the equality `n_-(mathscr H_n-EI)=#{lambda<E}` fails.
3. **Zero-energy rank:** produce a native shared-upper defect with `rank Delta_n` different from the PC-251 collision count `k_n`.
4. **Nonresonant limit:** produce a sequence satisfying PC-262's hypotheses for which the dilation empirical measures do not converge to `mu_{alpha,beta}`.
5. **Matched control:** preserve the same limiting IDS in an admissible control but obtain a different normalized total Schur rotation, contradicting the exact identity (13).

The finding does not claim that every transfer-matrix norm, every matrix-valued phase coordinate, or every subleading projective fluctuation is fixed by the IDS. It classicalizes only the canonical **total** rotation/focal index and its leading nonresonant normalization.