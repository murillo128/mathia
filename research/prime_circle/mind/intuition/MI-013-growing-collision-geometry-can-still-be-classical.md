# MI-013 — Prime-Circle raw near-resonance laws and every finite fixed-bandwidth logarithmic centering are classical source data

**Evidence level:** proved structural boundary through PC-276. PC-272 establishes ordered information beyond the ratio quotient; PC-274 proves the unscaled near-resonance law is matched-control prime-blind for every bandwidth; PC-275 identifies the first fixed-bandwidth centered term; PC-276 identifies the complete finite `1/log q` hierarchy as normalized logarithmic-integral source data. No theorem is claimed for moving-bandwidth normalized observables, exact `Li`-centered residual geometry, singular minimizer labels or nonlocal/cross-level couplings.

Growing collision geometry is not enough. PC-272 finds a real information boundary: ordered finite-window timing retains common source placement discarded by the exact ratio quotient. PC-273--PC-274 then show that this retained information does not survive as a raw arithmetic discriminator. Small bandwidth is controlled by prime/grid convergence, while large bandwidth forces `Delta_B` itself to zero; together they close the entire unscaled bandwidth axis.

PC-275 shows that the first centered fixed-resolution layer is not a hidden critical-line statistic either. After multiplication by `log q`, the prime/grid difference is exactly the pushforward of the smooth normalized `Li` correction `-(1+log t)dt` in the source coordinates.

PC-276 makes that statement all-orders at every fixed finite depth. The normalized prime source has a Poincaré expansion in powers of `1/log q`, with each coefficient a polynomial in `log t` coming from normalization of `dt/log(qt)`. Applying the fixed-bandwidth Lipschitz near-resonance map simply pushes those source coefficients forward. Subtracting any finite number of them and multiplying by the next power of `log q` therefore reveals another coefficient of the same classical `Li` hierarchy.

The genuine prime-minus-exact-`Li` residual is beyond every fixed logarithmic power under the classical zero-free-region PNT estimate. That residual may contain zeta-zero information through the explicit formula, but detecting it with a fixed source pushforward does not by itself make Prime-Circle geometry explanatory. A new mechanism must show a residual, normalization or coupling not already equivalent to classical source fluctuations.

**Boundary.** Moving `B=B(q)` after normalization, discontinuous/directional minimizer data, shrinking targets, conditioned families, microscopic or transfer observables and nonlocal/cross-level operators remain outside PC-276. The theorem closes finite algebraic logarithmic recentering at fixed bandwidth; it does not close every way of rescaling an exact `Li`-centered residual.