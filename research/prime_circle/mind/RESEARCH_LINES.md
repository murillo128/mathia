# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Find arithmetic information beyond the ratio quotient, raw near-resonance law and fixed-bandwidth Li bias

**Linked intuitions:** `MI-010-two-prime-resonance-needs-a-cross-prime-relation`, `MI-011-first-mixed-moment-can-still-be-pairwise-scalar`, `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra`, `MI-013-growing-collision-geometry-can-still-be-classical`.

PC-224--PC-271 close fixed-conductor packets, finite trace words, complete finite singular data, common-dilation growth and the main bounded-mode orbit regimes, then identify the classical dual-lattice carriers `lambda_(p,r;q)` and `Delta_B`.

PC-272 proves that the finite-window `Delta_B` statistic genuinely retains ordered source placement lost by the exact ratio quotient. PC-273 shows that this extra information is nevertheless prime-blind in raw distribution for `B=o(log q)`.

PC-274 closes the apparent remaining raw-bandwidth window. The PNT/Wasserstein bound at small `B` and the universal Dirichlet collapse `Delta_B<=(B+1)^(-2)` at large `B` combine to give uniform prime/grid convergence over **all** bandwidths. There is no unscaled intermediate resolution at which the raw `Delta_B` law becomes asymptotically prime-specific.

PC-275 then closes the first centered fixed-bandwidth escape. After multiplying the prime/grid difference by `log q`, the limiting signed law is exactly the pushforward of the classical normalized logarithmic-integral source correction `-(1+log t)dt`. At fixed resolution, the first subleading bias is smooth PNT data, not a new zeta-zero statistic.

The live ordered-time branch must therefore retain more structure than the raw scalar law and remove the deterministic source profile it already inherits. Honest continuations include moving-bandwidth normalized variables such as `(B+1)^2 Delta_B`, Li-centered residuals, singular/directional minimizer data, conditioned source families, microscopic or transfer observables, or additional nonlocal/cross-level couplings. Each still needs its own matched control and prior-art audit.

## Keep information retention, scale normalization and arithmetic discrimination separate

PC-272 proves finite ordered information survives the ratio quotient. PC-274 proves that the unscaled scalar law loses asymptotic discrimination at every bandwidth, while PC-275 proves that its first fixed-resolution centered layer is already classical source-density data. A durable Prime-Circle mechanism must specify what is normalized or subtracted, which source fluctuations survive that operation, and why the destination is sensitive to them rather than merely magnifying a known PNT profile.