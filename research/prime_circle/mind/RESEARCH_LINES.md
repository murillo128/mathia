# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether any prime-specific mesoscopic one-profile signal survives beyond the universal Hankel threshold

**Linked intuitions:** `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra`, `MI-013-growing-collision-geometry-can-still-be-classical`, `MI-014-single-source-positive-spectral-lifts-are-pure-gauge`, `MI-015-selector-subtraction-leaves-a-classical-digamma-green-symbol`.

PC-224--PC-285 reduce the positive spectral construction to a source-independent cyclic convolution finite section, isolate the modular collision selector and show unconditional matched-control reproduction through the Vinogradov--Korobov range.

PC-286--PC-290 show that under RH the exact normalized boundary-log singular-spectrum law classicalizes for every `B=o(sqrt(q))` once the matched control is given the total source cardinality `M_q`. The former `log^2 q` gap was one scalar normalization mismatch, not positional prime information.

PC-291 shows that the first denominator-microscopic exact or bounded-residue relations forced at `B~sqrt(q)` are spectrally negligible: every band `T=o(sqrt(q)/log q)` has vanishing normalized operator norm under the stated bound. PC-292 now proves that this threshold is sharp in order. In the intrinsic same-source geometry `p=r=1`, a band of width

`T_q~alpha N/log q`, `N~sqrt(q)`,

already has normalized operator norm tending to `alpha`.

But the first order-one band is a universal Hankel/anti-diagonal effect: it is present before any prime-specific placement law enters. Thus the square-root frontier has split into two distinct thresholds. Exact denominator resonance begins microscopically but is too weak; order-one mesoscopic amplitude begins near `N/log q` but, in the clean saturating example, is source-independent.

The surviving one-profile question is therefore sharper: **after subtracting or matching the universal same-source Hankel contribution at mesoscopic width, does any order-one remainder depend on exact prime placement?** If not, a useful RH-sensitive architecture must retain information discarded before this summary—cross-level, multi-source, directional, phase-sensitive, raw-resonance or noncommuting data. The scalar source count remains a separate resource because the PC-290 control is explicitly given `M_q`.

## Keep count information, denominator geometry, microscopic onset, mesoscopic amplitude and source specificity separate

PC-285 is unconditional. PC-286--PC-290 import RH through prime-counting information. PC-290 deliberately gives the control one scalar arithmetic datum, `M_q`, to falsify whether exact source positions matter after count is fixed. PC-291 is unconditional and separates the **appearance** of denominator-microscopic relations from their amplitude. PC-292 then separates order-one amplitude from arithmetic provenance: the operator-norm threshold is real, but the saturating same-source Hankel geometry is universal.

Future claims must identify whether their signal comes from total count, prime placement, a universal mesoscopic Green/Hankel remainder, raw rational resonance, or a richer observable. Neither the first exact resonance nor the first order-one residue band is by itself evidence of a prime-specific RH mechanism.