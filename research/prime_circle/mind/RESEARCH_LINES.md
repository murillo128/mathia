# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Find source-forced structure beyond universal root-fiber renormalization

**Linked intuitions:** `MI-052-proper-grade-hamiltonians-do-not-spectralize-bounded-raising-data`, `MI-053-shell2-order-escapes-parity-but-classicalizes-to-ramanujan-data`, `MI-054-the-half-density-line-is-an-arcsine-gauge-not-an-arithmetic-critical-line`, `MI-055-finite-coprime-chebyshev-transfer-families-are-universal-polydisk-shifts`, `MI-056-infinite-chebyshev-prime-sums-need-l2-coefficients-before-closure`, `MI-057-chebyshev-euler-products-collapse-to-classical-divisor-multipliers`, `MI-058-branch-noncommutativity-can-be-a-flat-common-refinement-relabeling`, `MI-059-common-anchor-chord-weighting-is-a-universal-qmf-shift`, `MI-060-mesh-scale-inverse-chord-regularization-is-poisson-renormalization-not-prime-spectral-data`.

PC-369--PC-379 reduce shell-2 order to real-cyclotomic/Chebyshev refinement, remove the arcsine half-density gauge, classify finite coprime refinements as universal polydisk shifts, price infinite scalar sums by `ell^2`/`ell^1`, reduce the commuting Euler-product repair to the classical divisor/zeta multiplier, and show that full branch noncommutativity is flat after common-refinement quotienting.

PC-380--PC-382 close every fixed regular scalar branch profile. Exact QMF collapse holds for finite Fourier bandwidth, while uniform root-fiber quadrature sends every fixed nonzero continuous profile to the universal disk as the covering degree grows. Infinite Fourier support is therefore not an escape by itself.

PC-383 tests the canonical mesh-scale singular escape rather than merely excluding it abstractly. The regularized inverse chord `(|1-z|^2+epsilon^2)^(-1/2)`, after intrinsic normalization, has squared modulus exactly equal to a Poisson kernel. Root refinement acts by `E_m P_rho=P_(rho^m)`, so all singular-value data depend only on `tau=2m asinh(epsilon/2)`. At `epsilon~1/m` with fixed `tau`, `T^*T` is identical for every prime or composite degree. The surviving order-one defect is universal Poisson renormalization, not arithmetic spectral shape.

The live question is consequently narrower than “use a mesh-scale singularity.” A surviving refinement mechanism must break the universal root-fiber renormalization seen in PC-382--PC-383: a singular profile whose normalized fiber law is genuinely conductor/source-sensitive, phase or nonnormal information not determined by the positive modulus, forced magnification of a nonuniversal defect, nonlinear/tensor interaction, or cross-level structure that couples fibers before scalarization.

## Apply the strongest matched control after the representation is fixed

For order-forgetting constructions, use reciprocal common-unitary control. For shell-2 order, use the real-cyclotomic/Chebyshev identification. Quotient derivative/Jacobian weights by the arcsine half-density conjugacy; reduce finite coprime families to the polydisk-shift model; apply the `ell^2`/`ell^1` thresholds to direct infinite scalar aggregates; reduce commuting Euler products to their classical divisor multiplier; and label branch paths by their common final tent branch before crediting matrix structure.

For scalar weights, first ask whether root-fiber averaging itself closes the proposed family. Fixed regular profiles collapse by PC-382, and the canonical inverse-chord mesh regularization closes exactly under the Poisson semigroup by PC-383. Mesh-scale variation is therefore necessary for escaping fixed-profile averaging but not sufficient for arithmetic discrimination. A successor must identify the nonuniversal datum that survives normalization and refinement.