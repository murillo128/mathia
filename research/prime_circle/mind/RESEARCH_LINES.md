# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether any prime-specific one-profile signal survives in the nonlocal square-root residue complement

**Linked intuitions:** `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra`, `MI-013-growing-collision-geometry-can-still-be-classical`, `MI-014-single-source-positive-spectral-lifts-are-pure-gauge`, `MI-015-selector-subtraction-leaves-a-classical-digamma-green-symbol`.

PC-224--PC-285 reduce the positive spectral construction to a source-independent cyclic convolution finite section, isolate the modular collision selector and show unconditional matched-control reproduction through the Vinogradov--Korobov range.

PC-286--PC-290 show that under RH the exact normalized boundary-log singular-spectrum law classicalizes for every `B=o(sqrt(q))` once the matched control is given the total source cardinality `M_q`. The former `log^2 q` gap was one scalar normalization mismatch, not positional prime information.

PC-291 shows that the first denominator-microscopic exact or bounded-residue relations forced at `B~sqrt(q)` are spectrally negligible: every band `T=o(sqrt(q)/log q)` has vanishing normalized operator norm under the stated bound. PC-292 proves that this threshold is sharp in worst-case operator norm. In the intrinsic same-source geometry `p=r=1`, a band `T~alpha N/log q`, `N~sqrt(q)`, already has order-one normalized norm through universal anti-diagonal/Hankel alignment.

PC-293 now shows that the worst-case transition does **not** survive the source averaging used by the one-profile spectral law. For any source probability measure `mu` with maximal atom `beta(mu)`, the source-averaged normalized Frobenius mass of a local residue band satisfies

`E ||K^(<=T)/N||_F^2 <= beta(mu) E_q(T) + (log q)^2/N^2`.

At `N~sqrt(q)`, both the prime measure and the count-matched Li-grid control have `beta=O(log q/q)`. Consequently every band `T=O(N)` changes the averaged ordered singular-spectrum vector by `o(1)`; at the PC-292 threshold `T~N/log q` the mean perturbation is only `O(log q/q^(1/4))`. This uses only diffuse source mass, not prime equidistribution or RH.

Thus the local logarithmic singularity, including the first order-one worst-case Hankel band, is not the missing **source-averaged** critical-scale carrier. The live one-profile question has moved to the complement `|ph+rk|_q>C sqrt(q)` and to any nonlocal organization of those residues. Does that complement retain an order-one prime-versus-count-matched difference at `B~sqrt(q)`, or does the whole one-profile singular-spectrum observable classicalize once source count and diffuseness are matched?

If the nonlocal complement also collapses, a useful RH-sensitive architecture must retain information discarded before this summary—cross-level, multi-source, directional, phase-sensitive, raw-resonance or noncommuting data. A fixed exceptional source pair is not a substitute for the averaged observable.

## Keep count information, worst-case residue geometry, source averaging and nonlocal provenance separate

PC-285 is unconditional. PC-286--PC-290 import RH through prime-counting information and deliberately give the control the scalar source count `M_q`. PC-291 separates microscopic relation onset from destination amplitude. PC-292 shows that worst-case operator amplitude appears at the mesoscopic `N/log q` scale. PC-293 then separates that worst-case geometry from the actual source-averaged law: diffuse averaging suppresses every local band up to `O(N)` regardless of source positions.

Future claims must identify whether their signal comes from total count, a fixed exceptional source pair, averaged prime placement, the nonlocal residue complement, or a richer observable. Neither the first exact resonance nor the first worst-case order-one band is evidence of a prime-specific averaged RH mechanism.