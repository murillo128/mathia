# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether prime specificity survives only in spectral shape beyond universal Hilbert--Schmidt mass

**Linked intuitions:** `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra`, `MI-013-growing-collision-geometry-can-still-be-classical`, `MI-014-single-source-positive-spectral-lifts-are-pure-gauge`, `MI-015-selector-subtraction-leaves-a-classical-digamma-green-symbol`.

PC-224--PC-285 reduce the positive spectral construction to a source-independent cyclic convolution finite section, isolate the modular collision selector and show unconditional matched-control reproduction through the Vinogradov--Korobov range. PC-286--PC-290 show under RH that the exact normalized boundary-log singular-spectrum law classicalizes for every `B=o(sqrt(q))` once the matched control is given the total source cardinality `M_q`.

PC-291--PC-293 resolve the first local square-root-scale escape. Microscopic residue relations are spectrally negligible; a fixed same-source pair develops order-one normalized operator norm on a mesoscopic band `T~N/log q`; but diffuse source averaging suppresses every local band `T=O(N)` at `N~sqrt(q)` in averaged normalized Frobenius mass. Both the prime source and the count-matched Li-grid control have the required maximal atom `O(log q/q)`.

PC-294 removes a broader scalar escape. For the **full** normalized boundary-log matrix, not only its local residue bands, the source-averaged Hilbert--Schmidt mass satisfies

`|E(mu)-E(u_q)| <= 2(log q)^2 sqrt(q beta(mu)/N)`.

When `N~sqrt(q)` and `beta(mu)=O(log q/q)`, this tends to zero, while the uniform benchmark tends to `pi^2/12`. Hence the prime and count-matched Li-grid laws have the same total squared singular-value mass unconditionally at square-root resolution.

Any surviving one-profile prime signal must therefore be a **redistribution of spectral mass**, not an excess in total mass. The live question is whether the nonlocal residue complement changes higher spectral moments, the top or bottom of the singular spectrum, small-singular-value structure, or another shape statistic by order one after count and diffuseness are matched. If the entire spectral shape classicalizes, the architecture must retain richer pre-spectral information: cross-level, multi-source, directional, phase-sensitive, raw-resonance or noncommuting data.

## Keep count information, worst-case geometry, source averaging, total mass and spectral shape separate

PC-285 is unconditional. PC-286--PC-290 deliberately give the control `M_q` and are RH-conditional in their square-root approach. PC-291 separates microscopic relation onset from destination amplitude. PC-292 separates worst-case operator amplitude from average behavior. PC-293 shows diffuse averaging washes out every local band. PC-294 then shows that even the aggregate full-operator Hilbert--Schmidt mass is universal under the same diffuseness scale.

A future claim must identify whether its signal is total count, an exceptional fixed source pair, averaged prime placement, a nonlocal spectral-shape statistic, or richer information discarded before singular-value compression. Neither first resonance, worst-case local norm nor total Hilbert--Schmidt mass can now serve as the prime-specific averaged carrier.