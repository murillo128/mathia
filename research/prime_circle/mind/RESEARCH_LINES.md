# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether any mesoscopic one-profile signal survives once total source count is matched

**Linked intuitions:** `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra`, `MI-013-growing-collision-geometry-can-still-be-classical`, `MI-014-single-source-positive-spectral-lifts-are-pure-gauge`, `MI-015-selector-subtraction-leaves-a-classical-digamma-green-symbol`.

PC-224--PC-285 reduce the positive spectral construction to a source-independent cyclic convolution finite section, isolate the modular collision selector and show unconditional matched-control reproduction through the Vinogradov--Korobov range.

PC-286--PC-290 show that under RH the exact normalized boundary-log singular-spectrum law classicalizes for every `B=o(sqrt(q))` once the matched control is given the total source cardinality `M_q`. The former `log^2 q` gap was one scalar normalization mismatch, not positional prime information.

PC-291 now tests the natural square-root explanation suggested by PC-271. Although `B~sqrt(q)` is the first scale where denominator-`q` exact or bounded-residue relations are forced into the coefficient window, the corresponding microscopic residue band is spectrally negligible after the established `1/N` normalization. Every fixed residue band moves the full ordered singular spectrum by only `O(log q/sqrt(q))`, and the same conclusion holds under the stated sufficient bound for `T=o(sqrt(q)/log q)`.

Thus the first rational resonance is **not** the missing order-one one-profile carrier. PC-291 does not extend the full count-matched collapse to `B~sqrt(q)` because it does not control the mesoscopic Green remainder. The surviving one-profile question is now whether a wider residue band can couple to exact prime placement at order one at square-root bandwidth.

If not, a useful RH-sensitive architecture must retain information discarded before this summary—cross-level, multi-source, directional, phase-sensitive or noncommuting data. The scalar source count remains a separate resource because the PC-290 control is explicitly given `M_q`.

## Keep count information, denominator geometry, microscopic amplitude and mesoscopic structure separate

PC-285 is unconditional. PC-286--PC-290 import RH through prime-counting information. PC-290 deliberately gives the control one scalar arithmetic datum, `M_q`, to falsify whether exact source positions matter after count is fixed. PC-291 is unconditional and separates the **appearance** of denominator-microscopic relations from their amplitude in the normalized spectral observable.

Future claims must identify whether their signal comes from total count, prime placement, a genuinely mesoscopic part of the log-sine remainder, raw rational-resonance structure, or a richer observable. Exact resonance at `B~sqrt(q)` by itself is no longer a viable explanation for an order-one one-profile transition.