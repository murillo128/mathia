# Prime-circle research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Require source-forced nonseparable or growing structure before scalar contraction

**Linked intuitions:** `MI-011-scalar-fourier-zero-existence-is-deformation-dependent`, `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra`.

PC-197--PC-206 close the fixed finite scalar packet algebra, anchored jets, fixed matrix filters, scale-self-similar scalar filters, and the bilateral regular representation of the total signed mismatch. PC-207 closes the most canonical fixed-finite-valence repair that keeps **every individual positive Fourier leg**: before the common-angle quotient the coefficient array factorizes, and the Hardy lift is exactly a tensor product of one-variable rational Toeplitz multipliers with continuous separated spectrum.

PC-208 also closes the singular zero-depth version of that same fixed-valence lift. The limiting symbols are reciprocal cyclotomic Smirnov multipliers whose poles are precisely the primitive shell; the natural unbounded multiplication operator still has a spectral continuum, the bounded linear renormalization tends strongly to zero, and the quadratic boundary layer recovers only the classical primitive-root measure.

A viable cross-shell mechanism must therefore create coupling **before** this separated multiplier stage: shell/refinement-dependent nonseparable transfer, genuinely growing/infinite valence formed before contraction, source-forced noncommuting state, or global uniformization/monodromy. Merely retaining more finite Fourier coordinates or taking their canonical zero-depth boundary is classified.

## Leave one-shell reciprocal Fourier zeros unless the source selects the deformation

PC-193--PC-196 show that scalar reciprocal-shell Fourier sign, zero existence, and first-zero scale depend on the chosen exponent. A useful scalar law needs a source-forced deformation/operator rather than a parameter chosen because it creates favorable zeros.

## Require a prime-specific discriminator after every operator enlargement

PC-207 remains valid after replacing Dirichlet characters by arbitrary bounded periodic sequences, and PC-208's operator/spectral architecture is classical Hardy/cyclotomic boundary theory. Any surviving enlargement must identify which relation is forced by the prime-circle geometry and fails on matched periodic/composite controls.

## Keep topology and realization separate from scalar cyclotomic identities

Potential-theoretic, geometric, infinite-refinement, nonabelian, or monodromy enlargements remain live only when the required object exists canonically and retains information beyond the classical packet/Toeplitz/cyclotomic data. A larger state space is not a mechanism if a natural transform makes it separated scalar symbols again.