# Prime-circle research lines

This file holds the current mathematical lines of investigation suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Require old/new fiber coupling beyond the scalar Haar-refinement star algebra

**Linked intuitions:** `MI-011-scalar-fourier-zero-existence-is-deformation-dependent`, `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra`.

PC-197--PC-206 close the fixed finite scalar packet algebra, anchored jets, fixed matrix filters, scale-self-similar scalar filters, and the bilateral regular representation of the total signed mismatch. PC-207 closes the canonical fixed-finite-valence repair that keeps every positive Fourier leg, while PC-208 closes its singular zero-depth boundary: both reduce to classical separated Hardy/cyclotomic multiplier structure.

PC-209 closes the simplest source-forced noncommuting shell/refinement operator before scalar contraction. The intrinsic weighted power cover `W_n=M_(Phi_n) C_n`, after normalization, is a pure isometry with countably infinite wandering multiplicity, hence the same universal shift for every `n>=2`; the unnormalized class retains only the classical coefficient-energy scalar and cannot distinguish `p^r` from `2p^r`.

PC-210 now closes the tempting scalar cross-level repair. At prime levels the normalized weighted covers are **exactly integer Haar-refinement isometries**: prime products telescope to `H_N`, adjoint overlaps are compressed rational dilations, the prime-generated star algebra is the universal integer refinement algebra, and canonical infinite-depth prefixes converge only weakly to zero. No primitive-root placement survives in that scalar star algebra. Even arbitrary finite forward words of composite weighted covers retain only the product of coefficient energies at the abstract-operator level.

A viable cross-level mechanism must therefore retain structure outside this Haar/Wold quotient: joint direct-composite primitive data and refinement-path data, old/new fiber incidence, a distinguished source algebra coupled to the operator, matrix-valued or non-Haar fiber sectors, genuinely growing tensor relations, or global uniformization/monodromy. Scalar weighted-cover products, adjoints, refinement cocycles, and their canonical infinite-depth prefix are classified.

## Leave one-shell reciprocal Fourier zeros unless the source selects the deformation

PC-193--PC-196 show that scalar reciprocal-shell Fourier sign, zero existence, and first-zero scale depend on the chosen exponent. A useful scalar law needs a source-forced deformation/operator rather than a parameter chosen because it creates favorable zeros.

## Require a prime-specific discriminator after every operator enlargement

PC-207 remains valid after replacing Dirichlet characters by arbitrary bounded periodic sequences. PC-208's singular boundary architecture is classical Hardy/cyclotomic theory. PC-209's normalized weighted cover is universal for every degree-below-cover polynomial, and PC-210 identifies the entire scalar prime weighted-cover star algebra with ordinary Haar refinement/rational dilation geometry already present for the positive integers.

Any surviving enlargement must identify which relation is forced by the prime-circle geometry and fails on matched periodic/composite/refinement controls. Generating a semigroup from prime birth levels is not enough when the resulting operator algebra forgets the primitive-shell geometry before the invariant is formed.

## Keep topology and realization separate from cyclotomic/operator identities

Potential-theoretic, geometric, old/new-fiber, nonabelian, non-Haar infinite-refinement, joint-algebra, or monodromy enlargements remain live only when the required object exists canonically and retains information beyond the classified packet/Toeplitz/Smirnov/Haar-refinement data. A larger state space is not a mechanism if a natural transform, equivalence, or refinement identity sends it back to universal operator structure.
