# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Preserve oriented relational source information before centered Gram formation

**Linked intuitions:** `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra` through `MI-020-centered-same-source-gram-formation-erases-coordinatewise-orientation-before-averaging`.

PC-295--PC-298 identify the diffuse fixed-scale boundary: the normalized boundary-log operator approaches a universal square-zero class, with source information surviving only at or below the explicit defect scale `r_q`. PC-299 then identifies the first positive quadratic defect there as a Fejer-smoothed source autocorrelation, so squaring and averaging loses Fourier phase and lands in classical additive prime exponential-sum data.

PC-300 closes the most direct independently sampled multi-profile repair. Source-coupled pair profiles can form genuinely noncommuting matrices before averaging, but independent source residues around the cycle factor every averaged `k`-loop exactly through powers of one real symmetric Hankel-type average. The full independently averaged hierarchy therefore keeps only reflection-even source Fourier data.

PC-301 closes the obvious correlated/reused-source escape **while the centered same-source Gram carrier is retained**. The centered window gives `G_(-s)=G_s` pointwise, before averaging. Hence every cyclic word formed from the `G_s` is invariant under independent reflection of every source coordinate for arbitrary joint source laws. Correlation can retain relations among the unoriented classes `{s,-s}`, but it cannot recover coordinatewise reflection-odd orientation discarded by the map `s -> G_s` itself.

The surviving route must therefore preserve orientation **before** centered same-source contraction, or prove that orientation is unnecessary. Concrete possibilities are oriented frame-level observables, a genuinely asymmetric window/observable justified by Prime-Circle geometry, cross-level carriers not determined by `G_s`, or relational mechanisms living intrinsically on the sign quotient. Reusing/correlating source variables inside the same centered Gram construction is no longer a sufficient escape.

## Keep defect resolution, source phase, carrier quotient and dependence structure separate

Reaching `O(r_q)` is necessary to escape the universal square-zero bulk but not sufficient. PC-299 shows that positive quadratic compression at that scale forgets phase. PC-300 shows that noncommutativity before expectation is insufficient when independence factorizes the product into powers of one averaged matrix. PC-301 shows that even without independence, coordinatewise source orientation is already lost if every observable factors through centered same-source `G_s`.

Any next observable must state **where** source information is retained: before or after the reflection quotient, and before or after averaging. A higher-order or correlated statistic is not automatically higher-information when its input carrier has already identified `s` with `-s`.
