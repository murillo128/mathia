# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Find source-forced structure beyond scalar Chebyshev quotients, flat branch refinement and QMF chord weights

**Linked intuitions:** `MI-052-proper-grade-hamiltonians-do-not-spectralize-bounded-raising-data`, `MI-053-shell2-order-escapes-parity-but-classicalizes-to-ramanujan-data`, `MI-054-the-half-density-line-is-an-arcsine-gauge-not-an-arithmetic-critical-line`, `MI-055-finite-coprime-chebyshev-transfer-families-are-universal-polydisk-shifts`, `MI-056-infinite-chebyshev-prime-sums-need-l2-coefficients-before-closure`, `MI-057-chebyshev-euler-products-collapse-to-classical-divisor-multipliers`, `MI-058-branch-noncommutativity-can-be-a-flat-common-refinement-relabeling`, `MI-059-common-anchor-chord-weighting-is-a-universal-qmf-shift`.

PC-369--PC-378 reduce shell-2 order to real-cyclotomic/Chebyshev refinement, remove the arcsine half-density gauge, classify finite coprime refinements as universal polydisk shifts, price infinite scalar sums by `ell^2`/`ell^1`, and reduce the commuting Euler-product repair to the classical divisor/zeta multiplier.

PC-379 keeps every inverse Chebyshev branch but still finds no arithmetic curvature: the full branch family is the generic tent Cuntz system, and different refinement orders are reflected mixed-radix relabelings of the same final branch with trivial holonomy after common-refinement quotienting.

PC-380 closes the most immediate source-geometric amplitude decoration left by that result. For the common-anchor chord `d(w)=|1-w|` on `w^m=z`,

`(1/m) sum_(w^m=z) d(w)^2 = 2`

for every degree `m>=2`, every base point and every rotated anchor. Hence `2^(-1/2) M_d V_m` is a proper isometry and the adjoint chord transfer has normalized closed-unit-disk spectrum independent of `m`. The full complex chord `1-w` gives the same result, and the Chebyshev/arcsine quotient reproduces the same constant fiber energy. The direct common-anchor chord is therefore a classical QMF/isometry carrier, not prime-specific structure.

The live question is consequently beyond branch labels and the canonical one-point chord amplitude. A surviving mechanism must retain source data not absorbed by the common refinement or QMF normalization: conductor/primitive-shell information, a non-QMF singular or nonlinear chord functional whose fiber energy is genuinely arithmetic, a nonlinear interaction before common-refinement identification, or a source-forced domain/topology. Merely choosing prime degrees, keeping noncommuting labels, or multiplying by the Euclidean chord does not create an arithmetic discriminator.

## Apply the strongest matched control after the representation is fixed

For order-forgetting constructions, use the reciprocal common-unitary control. For shell-2 order, use the real-cyclotomic/Chebyshev identification. Quotient derivative/Jacobian weights by the arcsine half-density conjugacy; reduce finite coprime families to the polydisk-shift model; apply the `ell^2`/`ell^1` thresholds to direct infinite scalar aggregates; reduce commuting Euler products to their classical divisor multiplier; and label branch paths by their common final tent branch before crediting matrix structure.

PC-380 adds a further exact control: test any source-forced scalar branch amplitude for constant preimage energy. The common-anchor chord satisfies the QMF condition exactly for prime and composite degrees alike, so its normalized transfer is universal. Arithmetic discrimination begins only after a candidate survives both common-refinement flatness and this fiber-energy classicalization.