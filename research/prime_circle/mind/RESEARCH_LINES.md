# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Find arithmetic information beyond the fixed-upper ratio-autocorrelation quotient

**Linked intuitions:** `MI-010-two-prime-resonance-needs-a-cross-prime-relation`, `MI-011-first-mixed-moment-can-still-be-pairwise-scalar`, `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra`, `MI-013-growing-collision-geometry-can-still-be-classical`.

PC-224--PC-260 close fixed-conductor packets, finite trace words, complete finite singular data, common-dilation growth, independent backward-word information, and establish the exact reflection symmetry of the shared-upper collision operator.

PC-261--PC-271 separate finite-lift effects from bulk arithmetic. Bounded-mode nonresonant irrational and rational limits have classical Haar/periodic-Haar bulk laws reproduced by matched controls; a persistent fixed primitive exact resonance selects one classical resonant subtorus. Growing Fourier resolution and finite-window near resonance are governed by the classical dual-lattice quantities `lambda_(p,r;q)` and `Delta_B`, with universal square-root/parabolic scales.

PC-272 now closes the first obvious source-statistics escape for the **fixed-upper exact lattice**. For fixed prime `q`,

`Lambda_(p,r;q) = Lambda_q(pr^-1 mod q)`,

so every swap-invariant exact-lattice/full-orbit observable factors through the ratio `a=pr^-1`. The prime-pair ratio law has character moments

`sum_a mu_q(a) chi(a) = |A_q(chi)|^2/M_q^2`,

where `A_q(chi)` is the classical prime character sum. This branch therefore retains only multiplicative autocorrelation/power-spectrum data and is governed by the Dirichlet-character/Dirichlet-`L` package; common multiplicative phase/placement is lost.

The claimed PC-272 source-domain counterexample showing that `Delta_B` does not factor through `a` is currently under adversarial review because the displayed example uses pairs outside the stated prime source. The review proposes an admissible repair, but until the canonical finding is corrected and the review converges, do not use prime-source non-factorization of `Delta_B` as established evidence.

The live source-sensitive question is therefore to identify an observable that provably retains information outside the fixed-upper ratio autocorrelation. Ordered/partial-window near-resonance data remains a candidate once its source-domain distinction is established; microscopic/shrinking spectra, singular log-determinants, Lyapunov/transfer growth, subleading projective fluctuations, and cross-level operators remain separate channels.

## Treat orbit closure, Fourier resolution, quotient source statistics, ordered time, and singular/fine observables as separate gates

Fixed finite lifts can select the orbit closure without making its bulk law arithmetic. Growing Fourier frequency can resolve the finite orbit, but its first scale is a shortest-vector problem. PC-272 adds another loss: even arithmetic variation of swap-invariant exact-lattice statistics is encoded only through the prime-ratio autocorrelation. A durable discriminator must survive controls matching the relevant quotient geometry and must retain source information that the ratio power spectrum erases.