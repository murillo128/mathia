# Prime-circle research lines

This file holds the current mathematical questions suggested by the durable prime-circle intuitions. It is not a roadmap, task queue, status page, or history.

## Decide whether the primitive parity-reduced torus-return cocycle retains nonclassical arithmetic information

**Linked intuitions:** `MI-010-two-prime-resonance-needs-a-cross-prime-relation`, `MI-011-first-mixed-moment-can-still-be-pairwise-scalar`, `MI-012-finite-scalar-cross-shell-fusion-is-closed-under-classical-packet-algebra`, `MI-013-growing-collision-geometry-can-still-be-classical`.

PC-224--PC-258 close fixed-conductor packets, pairwise cross-Grams, determinant volume, every fixed-depth multi-upper trace word, and the complete finite singular spectrum. The remaining operator is a fixed-width transfer cocycle driven by a rational two-torus return word; every fixed local window has a classical torus-translation description.

PC-259 removes common-dilation growth as fake long-range complexity. Simultaneous dilation `(p,r,q)->(mp,mr,mq)` makes the normalized defect an exact direct sum `I_m tensor Delta_(p,r;q)`. Singular data and length-normalized transfer invariants are simply repeated; only the primitive quotient can carry genuinely new depth.

PC-260 adds exact reversal parity. The weighted collision word is palindromic, the defect commutes with coordinate reversal, and the squared-singular-value transfer problem splits into even/odd sectors. The forward and backward halves are not independent information; the effective primitive object is one half-word plus a parity boundary condition.

The surviving question is therefore narrower: can a **primitive, parity-reduced, depth-growing transfer invariant**—Lyapunov behavior, rare-return resonance, or another nonlocal statistic—distinguish the prime-linked rational torus from matched classical return models? Raw conductor growth, common refinement, fixed windows, and treating both halves as independent are controls.

## Treat primitive depth, common refinement, local coding, and reversal parity as separate gates

Growing collision rank is real, but common integer dilation only repeats blocks and exact reflection halves the independent word. Any remaining Prime-Circle carrier must survive both quotients before its long-range behavior is interpreted as arithmetic.